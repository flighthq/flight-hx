.quimby

# Pinned sibling repos, materialized by `npm run rehydrate` — disposable build inputs, never committed.
.dependencies/

# Node / Haxe build artifacts
node_modules/
.haxe/
.haxelib/
haxe_libraries/
.cache/
.artifacts/
build/
dist/
*.n
