# Project Status

Last updated: 2026-08-26

## Current State

- Public Haxe addressing now follows Flight intent rather than TypeScript files: package facades are the steered function/value surface, all distinct source-derived namespace classes are `@:noCompletion`, and each of the 2,223 exported `@flighthq/types` declarations owns `flighthq.types.<TypeName>` (1,425 formerly required a secondary-type path). Contract-only values are individually `private` with `@:allow(flighthq)` while contract-only types remain public `@:noCompletion`; the current inventory contains 1,490 restricted values and 15 completion-hidden types. The hermetic Haxe 4.3.7 guard covers allowed access, ordinary-consumer rejection, private-type rejection, private-sibling retention, and the compile-visible type ceiling. The generic transpiler/profile extraction remains future work pending a second real profile.
- Verification passes the deterministic repository check, all 169 generator/unit tests, the complete Haxe namespace, Eval/JavaScript/Python/C++ portability, and installed-package consumption. The full CI sweep reaches upstream parity before reproducing the pinned tree's unchanged `effects-wgpu` baseline at 351/364: `wgpuLogicalEffectResolution.test.ts` mocks `./wgpuEffectPass`, but the already-committed apply bridges do not synchronize that dependency, so 13 rows call the real pass with the fixture's deliberately skeletal render state. The failing test and affected bridges are outside this diff.
- The upstream `0.4.0` tag pins `cd6b6e71eb472b3a4249830a7743333f019fa7b7`, 49 commits after the preceding pin. The release inventory remains 153 packages and 319 public lanes, with 151 translated packages, two derived tooling exclusions, 36,488 canonical export records, and 1,599 routed tests. All 15,531 candidate declarations lower with zero diagnostics. `VelocityExplanation` is the sole new typed-struct identity and remains unreviewed audit-only; the browser `matchMedia` global is recorded as an explicit JavaScript-only host-toolkit capability. Object-literal methods now capture their owning object on every target instead of relying on JavaScript-only dynamic `this`; the 0.4.0 IPC reply method exposed that latent native null-receiver path, and the warning-free Eval, JavaScript, Python, and C++ portability matrix now passes.
- The `443275fa` pin advances 68 Flight `develop` commits and inventories 153 packages, 319 public lanes, 36,109 exports, and 1,570 tests. Generator census tests now validate complete package coverage, report consistency, and reviewed emission safety instead of duplicating exact totals that fail on legitimate upstream additions. The typed-struct report labels 1,426 new schemas as unreviewed audit-only since the maintained baseline; they remain reflective unless separately fingerprint-approved. Repository checks and all 162 generator unit tests pass.
- JavaScript direct calls now omit TypeScript-defaulted trailing arguments instead of materializing an observable extra `undefined`; static Haxe targets retain padded function arity, and `super` remains a direct constructor call. The historical `effects-canvas` default draw failures are closed, with focused parity passing 204/204. The upstream runner defaults to three package workers because each compiled bridge can consume multiple gigabytes; callers can still override `--jobs` explicitly.

The preceding `9db114b3fe1db90a90a11541649cb3af8bb5ca62` pin inventories 151 packages: 149 translated and two source-derived Node CLI tooling exclusions. It accounts for 2,703 production source files, 1,553 routed test files, 315 public lanes, and 35,605 export records.

- The `9db114b3` pin advances 18 Flight `develop` commits and translates the new public `@flighthq/physics3d-abi` package, including its persistent world handles, fixed little-endian command buffers, body/collider/joint mutations, selective structure-of-arrays readbacks, contact hooks, queries, buffer constructors, and reference TypeScript backend. The ABI is included through its package facade and SDK exposures rather than treated as a host toolkit implementation; it gives a future Rust/WASM physics runtime an explicit ownership seam while preserving the TypeScript reference path. Physics3D also gains its completed CCD impact lifecycle, rotational CCD envelope, friction/restitution/contact-hook and sensor behavior, broadphase publication, spatial-index guards, and diagnostics qualification. Focused Haxe parity passes 70/70 for `physics3d-abi`, 707/707 for `physics3d`, and 501/501 for `collision`. Eight new zero-escape data records (`Physics3DAbiBodyBuffer`, `Physics3DAbiCommandBuffer`, `Physics3DAbiContactBuffer`, `Physics3DAbiContactHooks`, `Physics3DAbiExecutionResult`, `Physics3DAbiJointBuffer`, `Physics3DAbiQueryBuffer`, and `Physics3DRotationalCcdEnvelope`) are fingerprint-approved for 213 direct accesses; the method-bearing `Physics3DAbi` backend remains audit-only because its receiver-sensitive methods require a typed Haxe-interface/Rust-trait lowering, now recorded in the strict-typing backlog. The refreshed audit records 788 direct schemas, 26,653 direct accesses, 10,705 pending accesses, 451 reflective survivors, and 26,320 classified type erasures. Generated output contains 54,386 `Dynamic` occurrences, 12,102 `_Runtime.field` calls, 144 `_Runtime.setField` calls, and zero `Reflect` calls; the combined Physics3D/ABI/collision/spatial surface accounts for 5,574 `Dynamic` occurrences and 2,324 field calls, of which the new ABI accounts for 587 and 437 respectively. All 162 generator unit tests, deterministic repository checks, the TypeScript build, the complete Haxe namespace, the JavaScript bundle, Eval/JavaScript/Python/C++ portability including a clean C++ compile/link/run, and isolated Haxelib packaging pass. The generated-tree SHA-256 is `f8301b0a025ecea1c7f40f1505c6a4fe4cb2cb3060be7831c68ea84a1ceec012`; run-specific parity timestamps are not committed.
- The `9b3b71b0` pin refresh materially expands Physics3D with automatic broadphase/narrow-phase contacts, uniform-grid and BVH spatial backends, contact manifolds and face clipping, GJK distance and sweep support, raycasts, hull/terrain/cylinder/cone/capsule collision, rotational CCD, world ray/sphere/AABB/frustum/shapecast queries, debug geometry, distance joints, compliant limits, reaction and break-event support, and extensive validation, lifecycle, warm-start, substep, terrain, and performance fixes. Generation now covers 62 Physics3D type sources and its focused parity suite passes 686/686 assertions (up from 386 at the prior pin); the complete Eval/JavaScript/Python/C++ portability matrix passes including a clean C++ compile/link/run. Reviewed declaration fingerprint changes preserve direct emission where mechanically safe, while `Physics3DContact`, `Physics3DSolverConfig`, and `Physics3DSequentialImpulseState` are deliberately audit-only because upstream legacy hydration intersections expose unknown-member escape paths. The refreshed audit records 780 direct schemas, 26,293 direct accesses, 10,056 pending accesses, 451 reflective survivors, and 25,718 classified type erasures. Generated output contains 53,465 `Dynamic` occurrences, 11,463 `_Runtime.field` calls, 144 `_Runtime.setField` calls, and zero `Reflect` calls. The generated-tree SHA-256 is `c72e78235c9e77bb8cf5f77185a0d51a20a4a76c46386a44e1632f06ca802787`; run-specific parity timestamps are not committed.
- The standard runtime-toolkit tranche gives native Haxe concrete values for the emitted `Date`, `RegExp`, `TextEncoder`, `AbortController`/`AbortSignal`, `DOMException`, `performance`, `console`, animation-frame, interval, base64, URI-component, `structuredClone`, `ArrayBuffer`, and typed-array constructor dependencies. Constructor-shaped globals use a maintained token that owns construction, instance checks, and the static members Flight reads, while browser services remain explicit JavaScript-only or backend capabilities. The deterministic host audit now records 211 Dynamic compatibility type entries (down from 218), 33 emitted portable global values (up from 15), 36 JavaScript-only global values (down from 54), and zero missing providers. Native smoke covers UTF-8, binary/base64 and URI conversion, dates, regexp execution, typed-array identity, abort propagation, snapshot cloning/freezing with shared references and cycles, monotonic time, numeric animation handles, and interval cancellation. All 162 generator unit tests, deterministic repository/API checks, the complete Haxe namespace, Eval/JavaScript/Python/C++ portability including a clean C++ compile/link/run, the TypeScript build, and isolated Haxelib packaging pass.
- The forty-fourth high-access zero-escape direct-emission tranche fingerprint-locks `Physics3DWorld`, `Physics3DContact`, `Physics3DMassData`, `Physics3DHingeJoint`, and `Physics3DSliderJoint`. Their 404 accesses now use typed structural fields across 11 generated Physics3D modules, taking the refreshed audit from 753 direct schemas and 25,114 direct accesses to 758 and 25,518 while pending work falls from 8,192 to 7,788. The zero-escape audit-only pool falls from 583 schemas and 2,378 accesses to 578 and 1,974. Generated `Dynamic` remains 50,694, `_Runtime.field` calls fall from 10,408 to 10,345, generated `Reflect` remains zero, and the 24,501-entry type-erasure report is unchanged. Test enumeration keeps the world structural; cross-schema transfer plus object-literal spread keep both joint records structural; and the provenance-closed contact and mass-data records remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 162 generator unit tests pass, deterministic generation and repository checks pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and affected Physics3D parity passes 386/386. The generated-tree SHA-256 is `500c57147ac115d3438f9df2d0b74649c3bcc5a6b6bf569389f98289d998384e`; run-specific parity timestamps are not committed.
- The `598ef6f6` pin refresh updates every generator-owned artifact without timestamp churn and establishes a new strict-typing baseline of 753 direct schemas, 25,114 direct accesses, 8,192 pending accesses, 50,694 generated `Dynamic` occurrences, 10,408 `_Runtime.field` calls, 144 `_Runtime.setField` calls, and zero generated `Reflect`. The refresh derives the new `@flighthq/tool-registry` exclusions from source, accepts 26 reviewed declaration-fingerprint changes (including upstream's `*2D` collision renames), lowers source-local interfaces and concrete mapped utilities, supports `vi.hoisted` mock bridges, adds typed WebGPU toolkit declarations and WebGL host endpoints, and moves shape-command cursor mutation plus numeric `Object.is` into portable typed runtime helpers. The type-erasure ledger classifies all 24,501 remaining boundaries: 18,231 standard-toolkit, 618 external-toolkit, 3,868 source-unknown, 907 source-any, 120 source-never, 83 source-null, 267 source-undefined, and 407 unclassified. All 162 generator unit tests, deterministic generation/repository checks, the complete Haxe namespace, the JavaScript bundle, and Eval/JavaScript/Python/C++ portability pass; C++ includes a clean compile/link/run. The generated-tree SHA-256 is `241fdc26a1b8eb215f0a059463fdee16f477486ec64eeca45f2d74ab68c9a113`. The resource-contended full upstream parity sweep was stopped at 71/150 packages; focused math parity reproduces two existing unsigned-32-bit failures at 322/324. Run-specific timestamped parity reports are not committed.
- The forty-third high-access zero-escape direct-emission tranche fingerprint-locks `MorphShapeLineEndpoint`, `MorphShapeColorEndpoint`, `MorphShapePathBinding`, `MorphShapeAnimationTarget`, and `SwfMorphShapePaths`. Their 25 accesses now use typed structural fields across four generated shape and SWF modules, taking the audit from 748 direct schemas and 24,651 direct accesses to 753 and 24,676 while pending work falls from 6,015 to 5,990. The zero-escape audit-only pool falls from 531 schemas and 1,261 accesses to 526 and 1,236. Generated `Dynamic` remains 46,938, `_Runtime.field` calls fall from 9,609 to 9,594, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Dynamic ingress and strict-equality observation keep the animation target structural, normalization provenance keeps the path binding structural, the two endpoint records have no closed construction provenance, and the provenance-closed SWF paths remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and all four exact affected shape/SWF parity files pass 37/37 assertions. The repository check passes. Run-specific parity timestamps will not be committed.
- The forty-second high-access zero-escape direct-emission tranche fingerprint-locks `AttachmentSkin2D`, `RegionAttachment2D`, `PathAttachment2D`, `PointAttachment2D`, and `ClippingAttachment2D`. Their 28 accesses now use typed structural fields across seven generated Skeleton2D and format-parser modules, taking the audit from 743 direct schemas and 24,623 direct accesses to 748 and 24,651 while pending work falls from 6,043 to 6,015. The zero-escape audit-only pool falls from 536 schemas and 1,289 accesses to 531 and 1,261. Generated `Dynamic` remains 46,938, `_Runtime.field` calls fall from 9,629 to 9,609, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Normalization provenance keeps the attachment skin structural, cross-schema transfer keeps the path attachment structural, and the other three attachment records have no closed nominal construction provenance. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and all seven exact affected Skeleton2D/Spine parity files pass 112/112 assertions. The repository check passes. Run-specific parity timestamps will not be committed.
- The forty-first high-access zero-escape direct-emission tranche fingerprint-locks `BitmapBevelOptions`, `BitmapDisplacementMapOptions`, `BitmapConvolutionOptions`, `BitmapGradientBevelOptions`, and `BitmapGradientGlowOptions`. Their 35 accesses now use typed structural fields across four generated bitmap modules, taking the audit from 738 direct schemas and 24,588 direct accesses to 743 and 24,623 while pending work falls from 6,078 to 6,043. The zero-escape audit-only pool falls from 541 schemas and 1,324 accesses to 536 and 1,289. Generated `Dynamic` falls from 46,939 to 46,938, `_Runtime.field` calls fall from 9,664 to 9,629, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Optional omission keeps the bevel and two gradient option records structural; the convolution and displacement-map options have no production construction sites; and none has closed nominal construction provenance. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and all four exact affected bitmap parity files pass 27/27 assertions. The repository check passes. Run-specific parity timestamps will not be committed.
- The fortieth high-access zero-escape direct-emission tranche fingerprint-locks `VelocityField`, `CreateExternalTextureOptions`, `RenderQueue`, `QuadBatchRuntime`, and `WgpuShapeRasterSurface`. Their 49 accesses now use typed structural fields across ten generated velocity, quad-batch, render, WebGL, and WebGPU modules, taking the audit from 733 direct schemas and 24,539 direct accesses to 738 and 24,588 while pending work falls from 6,127 to 6,078. The zero-escape audit-only pool falls from 546 schemas and 1,373 accesses to 541 and 1,324. Generated `Dynamic` remains 46,939, `_Runtime.field` calls fall from 9,674 to 9,664, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. The external-texture options have no production construction provenance; cross-schema transfer and dynamic ingress keep the quad-batch runtime structural; and the provenance-closed velocity field, render queue, and WebGPU raster surface remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and all ten exact affected source-adjacent parity files pass 187/187 assertions. The repository check passes. Run-specific parity timestamps will not be committed.
- The thirty-ninth high-access zero-escape direct-emission tranche fingerprint-locks `FlowStack`, `FlowState`, `TimelineAudioCue`, `TimelineLabel`, and `TimelineSignals`. Their 43 accesses now use typed structural fields across three generated Flow, SWF document, and Timeline modules, taking the audit from 728 direct schemas and 24,496 direct accesses to 733 and 24,539 while pending work falls from 6,170 to 6,127. The zero-escape audit-only pool falls from 551 schemas and 1,416 accesses to 546 and 1,373. Generated `Dynamic` falls from 46,946 to 46,939, `_Runtime.field` calls fall from 9,680 to 9,674, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. `FlowState` has no production construction provenance; `TimelineLabel` and `TimelineSignals` retain normalization provenance; and the provenance-closed flow stack and timeline audio cue remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and the three exact affected Flow/SWF/Timeline parity files pass 181/181 assertions. The repository check passes. Run-specific parity timestamps will not be committed.
- The thirty-eighth high-access zero-escape direct-emission tranche fingerprint-locks `AnisotropyPbrExtension`, `DepthMaterial`, `NormalMaterial`, `VertexColorMaterial`, and `WireframeMaterial`. Their 44 accesses now use typed structural fields across 12 generated materials, resource, WebGL, and WebGPU modules, taking the audit from 723 direct schemas and 24,452 direct accesses to 728 and 24,496 while pending work falls from 6,214 to 6,170. The zero-escape audit-only pool falls from 556 schemas and 1,460 accesses to 551 and 1,416. Generated `Dynamic` remains 46,946, `_Runtime.field` calls fall from 9,713 to 9,680, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer keeps all five records structural, and optional omission applies to the four core materials. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and all 12 exact affected material/resource/WebGL/WebGPU parity files pass 56/56 assertions. The repository check passes. Run-specific parity timestamps will not be committed.
- The thirty-seventh high-access zero-escape direct-emission tranche fingerprint-locks `CrtEffect`, `DirectionalBlurEffect`, `LensFlareEffect`, `RadialBlurEffect`, and `TiltShiftEffect`. Their 39 accesses now use typed structural fields across 12 generated effects, WebGL, and WebGPU modules, taking the audit from 718 direct schemas and 24,413 direct accesses to 723 and 24,452 while pending work falls from 6,253 to 6,214. The zero-escape audit-only pool falls from 561 schemas and 1,499 accesses to 556 and 1,460. Generated `Dynamic` remains 46,946, `_Runtime.field` calls fall from 9,752 to 9,713, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer plus object-literal spread keep all five effect records structural. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and all 12 exact affected effects/WebGL/WebGPU parity files pass 38/38 assertions. The repository check passes. Run-specific parity timestamps will not be committed.
- The thirty-sixth high-access zero-escape direct-emission tranche fingerprint-locks `GlColorLutTextureCache`, `GlScene3DIbl`, `GlShapeMeshColorScaleBiasShader`, `GlToonProgram`, and `GlWireframeUpload`. Their 48 accesses now use typed structural fields across ten generated WebGL modules, taking the audit from 713 direct schemas and 24,365 direct accesses to 718 and 24,413 while pending work falls from 6,301 to 6,253. The zero-escape audit-only pool falls from 566 schemas and 1,547 accesses to 561 and 1,499. Generated `Dynamic` remains 46,946, `_Runtime.field` calls fall from 9,762 to 9,752, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Object-literal spread keeps the toon program structural, optional omission keeps the shape-mesh shader structural, and the provenance-closed color-LUT cache, Scene3D IBL, and wireframe upload remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and all ten exact affected WebGL parity files pass 80/80 assertions. The repository check and post-generation drift gate pass. Run-specific parity timestamps will not be committed.
- The thirty-fifth high-access zero-escape direct-emission tranche fingerprint-locks `Physics2DDebugGeometryOptions`, `Physics2DGearJointOptions`, `Physics2DMouseJointOptions`, `Physics2DPrismaticJointOptions`, and `Physics2DWheelJointOptions`. Their 41 accesses now use typed structural fields, changing the generated joint factory while retaining typed partial debug options, and taking the audit from 708 direct schemas and 24,324 direct accesses to 713 and 24,365 while pending work falls from 6,342 to 6,301. The zero-escape audit-only pool falls from 571 schemas and 1,588 accesses to 566 and 1,547. Generated `Dynamic` remains 46,946, `_Runtime.field` calls fall from 9,796 to 9,762, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. The four joint inputs have no production construction provenance, and the provenance-closed debug options remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and the exact debug-geometry and joint-factory parity files pass 16/16 assertions. Run-specific parity timestamps will not be committed.
- The thirty-fourth high-access zero-escape direct-emission tranche fingerprint-locks `BlendEffect`, `BlurEffect`, `FilmGrainEffect`, `GlitchEffect`, and `OutlineEffect`. Their 46 accesses now use typed structural fields across 17 generated effects, Canvas, WebGL, and WebGPU modules, taking the audit from 703 direct schemas and 24,278 direct accesses to 708 and 24,324 while pending work falls from 6,388 to 6,342. The zero-escape audit-only pool falls from 576 schemas and 1,634 accesses to 571 and 1,588. Generated `Dynamic` remains 46,946, `_Runtime.field` calls fall from 9,842 to 9,796, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer plus object-literal spread keeps all five effect records structural. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and all 19 exact changed effects/Canvas/WebGL/WebGPU parity files pass 105/105 assertions. Run-specific parity timestamps will not be committed.
- The thirty-third high-access zero-escape direct-emission tranche fingerprint-locks `AnimationBlendTreeChannel`, `AnimationCrossfadeChannel`, `AnimationLayerStackChannel`, `AnimationStateMachineChannel`, and `AnimationStateMachineState`. Their 43 accesses now use typed structural fields across five generated animation modules, taking the audit from 698 direct schemas and 24,235 direct accesses to 703 and 24,278 while pending work falls from 6,431 to 6,388. The zero-escape audit-only pool falls from 581 schemas and 1,677 accesses to 576 and 1,634. Generated `Dynamic` remains 46,946, `_Runtime.field` calls fall from 9,871 to 9,842, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Normalization provenance keeps the four mechanically compatible channel records structural, and cross-schema transfer keeps the state record structural. The focused typed-struct suite passes 23/23, all 156 unit assertions pass, the complete Haxe namespace and JavaScript bundle pass, Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run, and all five exact changed animation parity files pass 47/47 assertions. Run-specific parity timestamps will not be committed.
- The thirty-second high-access zero-escape direct-emission tranche fingerprint-locks `TextSegment`, `TextInputHistoryEntry`, `FocusManager`, `SelectableRichTextManager`, and `TextInputManager`. Their 49 accesses now use typed structural fields across six generated modules, taking the audit from 693 direct schemas and 24,186 direct accesses to 698 and 24,235 while pending work falls from 6,480 to 6,431. The zero-escape audit-only pool falls from 586 schemas and 1,726 accesses to 581 and 1,677. Generated `Dynamic` remains 46,946, `_Runtime.field` calls remain 9,871, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Optional omission keeps the text segment structural, normalization provenance keeps the history entry structural, and the three provenance-closed manager records remain structural pending separate cpp review. All 156 unit assertions pass across focused and split serial runs; the monolithic unit run also reached 155/156 before its full-workspace inventory audit crossed the 180-second watchdog under contention, while that inventory file passes 9/9 alone in 175.99 seconds. The focused typed-struct suite passes 23/23, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All six exact changed source-adjacent parity files pass 182/182 assertions: interaction 19, textsegment 22, and textinput 141. Run-specific parity timestamps will not be committed.
- The thirty-first high-access zero-escape direct-emission tranche fingerprint-locks `GltfPunctualLight`, `GltfCamera`, `ThreeDsCamera`, `Scene3DDocumentScene`, and `Skin`. Their 49 accesses now use typed structural fields across 15 generated modules, taking the audit from 688 direct schemas and 24,137 direct accesses to 693 and 24,186 while pending work falls from 6,529 to 6,480. The zero-escape audit-only pool falls from 591 schemas and 1,775 accesses to 586 and 1,726. Generated `Dynamic` remains 46,946, `_Runtime.field` calls fall from 9,890 to 9,871, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Optional omission keeps the document scene structural; the two glTF records, document scene, and skin have no closed construction provenance; and the provenance-closed 3DS camera remains structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass, the JavaScript bundle and Haxe namespace compile pass, portable Eval/JavaScript/Python/C++ verification passes, and the repository check is green. Exact affected Scene3D/formats/WebGL/WebGPU/skeleton parity passes 623/624 assertions across 15 source-adjacent tests; the sole failure reproduces the documented MD5 keyword-key baseline (`default_` versus `default`) in an unchanged assignment. Run-specific parity timestamps are not committed.
- The thirtieth high-access zero-escape direct-emission tranche fingerprint-locks `WgpuTextureSourceTextureEntry`, `WgpuEffectPipeline`, `WgpuMeshPipeline`, `WgpuRenderOptions`, and `WgpuSavedPassState`. Their 48 accesses now use typed structural fields across 11 generated modules, taking the audit from 683 direct schemas and 24,089 direct accesses to 688 and 24,137 while pending work falls from 6,577 to 6,529. The zero-escape audit-only pool falls from 596 schemas and 1,823 accesses to 591 and 1,775. Generated `Dynamic` falls from 46,947 to 46,946, `_Runtime.field` calls fall from 9,902 to 9,890, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Object-literal spread keeps the texture-source entry structural, optional omission keeps the effect pipeline and render options structural, cross-schema transfer keeps the mesh pipeline structural, and the saved pass state has no closed construction provenance. The focused typed-struct suite passes 23/23, all 154 unit assertions pass, the JavaScript bundle and Haxe namespace compile pass, portable Eval/JavaScript/Python/C++ verification passes, and exact affected WebGPU parity passes 177/177 assertions across 11 source-adjacent tests. Run-specific parity timestamps are not committed.

- The twenty-ninth high-access zero-escape direct-emission tranche fingerprint-locks `AreaLightOptions`, `SpotLightOptions`, `PointLightOptions`, `DirectionalLightOptions`, and `Light`. Their 45 accesses now use typed structural fields across six generated modules, taking the audit from 678 direct schemas and 24,044 direct accesses to 683 and 24,089 while pending work falls from 6,622 to 6,577. The zero-escape audit-only pool falls from 601 schemas and 1,868 accesses to 596 and 1,823. Generated `Dynamic` remains 46,947, `_Runtime.field` calls fall from 9,905 to 9,902, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Optional omission keeps the spot-, point-, and directional-light options structural; the area-light options have no closed construction provenance; and cross-schema transfer keeps `Light` structural. The focused typed-struct suite passes 23/23, all 154 unit assertions pass, the JavaScript bundle and Haxe namespace compile pass, portable Eval/JavaScript/Python/C++ verification passes, and exact affected lighting/Scene3D parity passes 64/64 assertions across six source-adjacent tests. Run-specific parity timestamps are not committed.
- The twenty-eighth high-access zero-escape direct-emission tranche fingerprint-locks `AbcFile`, `AbcConstantPool`, `LottieTransform`, `LottieDashEntry`, and `LottieTextDocument`. Their 50 accesses now use typed structural fields across two generated modules, taking the audit from 673 direct schemas and 23,994 direct accesses to 678 and 24,044 while pending work falls from 6,672 to 6,622. The zero-escape audit-only pool falls from 606 schemas and 1,918 accesses to 601 and 1,868. Generated `Dynamic` remains 46,947, `_Runtime.field` calls fall from 9,932 to 9,905, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Anonymous structural transfer keeps `AbcFile` and `AbcConstantPool` structural; cross-schema transfer keeps `LottieTransform` structural; and `LottieDashEntry` and `LottieTextDocument` have no closed construction provenance. The focused typed-struct suite passes 23/23, all 154 unit assertions pass, the JavaScript bundle and Haxe namespace compile pass, portable Eval/JavaScript/Python/C++ verification passes, and exact affected SWF/Lottie parity passes 48/48 assertions across three source-adjacent tests. Run-specific parity timestamps are not committed.
- The twenty-seventh high-access zero-escape direct-emission tranche fingerprint-locks `CanvasRenderTargetPool`, `ColorLutCache`, `GlShapeMeshBinding`, `GlVelocityContext`, and `WgpuVelocityContext`. Their 55 accesses now use typed structural fields across nine generated modules, taking the audit from 668 direct schemas and 23,939 direct accesses to 673 and 23,994 while pending work falls from 6,727 to 6,672. The zero-escape audit-only pool falls from 611 schemas and 1,973 accesses to 606 and 1,918. Generated `Dynamic` remains 46,947, `_Runtime.field` calls fall from 9,963 to 9,932, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer keeps `CanvasRenderTargetPool` structural; the provenance-closed `ColorLutCache`, `GlShapeMeshBinding`, `GlVelocityContext`, and `WgpuVelocityContext` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass, the JavaScript bundle and Haxe namespace compile pass, portable Eval/JavaScript/Python/C++ verification passes, and exact affected parity passes 87/87 assertions across nine source-adjacent tests. Run-specific parity timestamps are not committed.
- The twenty-sixth high-access zero-escape direct-emission tranche fingerprint-locks `FlyCameraControllerOptions`, `MeshMorph`, `Scene3DDocumentMesh`, `MeshMorphBindPose`, and `Scene3DForwardLightSelection`. Their 58 accesses now use typed structural fields across 14 generated modules, taking the audit from 663 direct schemas and 23,881 direct accesses to 668 and 23,939 while pending work falls from 6,785 to 6,727. The zero-escape audit-only pool falls from 616 schemas and 2,031 accesses to 611 and 1,973. Generated `Dynamic` falls from 46,948 to 46,947, `_Runtime.field` calls fall from 9,978 to 9,963, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Normalization provenance keeps `MeshMorph` and `MeshMorphBindPose` structural; optional omission keeps `Scene3DDocumentMesh` structural; `FlyCameraControllerOptions` has no closed construction provenance; and the provenance-closed `Scene3DForwardLightSelection` remains structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass, the JavaScript bundle and Haxe namespace compile pass, and portable Eval/JavaScript/Python/C++ verification passes. Exact affected parity passes 1,913/1,914 assertions across camera controls, lighting, mesh, Scene3D, formats, WebGL, and WebGPU; the sole Scene3D-formats failure reproduces the documented MD5 keyword-key baseline (`default_` versus `default`) in an unchanged assignment, while this tranche only types `documentMesh.skin` in that file. Run-specific parity timestamps are not committed.
- The twenty-fifth high-access zero-escape direct-emission tranche fingerprint-locks `AnimationLayerStack`, `StatechartTransitionExplanation`, `StatechartCondition`, `StatechartRegion`, and `StatechartInput`. Their 55 accesses now use typed structural fields across three generated modules, taking the audit from 658 direct schemas and 23,826 direct accesses to 663 and 23,881 while pending work falls from 6,840 to 6,785. The zero-escape audit-only pool falls from 621 schemas and 2,086 accesses to 616 and 2,031. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,028 to 9,978, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer keeps `AnimationLayerStack` structural; `StatechartRegion` and `StatechartInput` have no closed construction provenance; and the provenance-closed `StatechartTransitionExplanation` and `StatechartCondition` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass, the JavaScript bundle and Haxe namespace compile pass, portable Eval/JavaScript/Python/C++ verification passes, and exact affected animation/statechart parity passes 174/174. Run-specific parity timestamps are not committed.
- The twenty-fourth high-access zero-escape direct-emission tranche fingerprint-locks `CapacitorApi`, `CapacitorDeviceInfo`, `ElectronDisplay`, `ElectronRectangle`, and `SoftKeyboard`. Their 56 accesses now use typed structural fields across 16 generated modules, taking the audit from 653 direct schemas and 23,770 direct accesses to 658 and 23,826 while pending work falls from 6,896 to 6,840. The zero-escape audit-only pool falls from 626 schemas and 2,142 accesses to 621 and 2,086. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,038 to 10,028, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Dynamic ingress keeps `ElectronDisplay` structural; `CapacitorDeviceInfo` has no closed construction provenance; and the provenance-closed `CapacitorApi`, `ElectronRectangle`, and `SoftKeyboard` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass, the JavaScript bundle and Haxe namespace compile pass, and portable Eval/JavaScript/Python/C++ verification passes. Fifteen exact changed host files pass, while the keyboard file reproduces its recorded 53/60 baseline with the same seven backend receiver-binding failures in unchanged call sites, for 108/115 affected assertions overall. Run-specific parity timestamps are not committed.
- The twenty-third high-access zero-escape direct-emission tranche fingerprint-locks `DisplayObject`, `GridLayoutItemStyle`, `NativeText`, `TextLabelRuntime`, and `AccessibilityNode`. Their 59 accesses now use typed structural fields across 11 generated modules, taking the audit from 648 direct schemas and 23,711 direct accesses to 653 and 23,770 while pending work falls from 6,955 to 6,896. The zero-escape audit-only pool falls from 631 schemas and 2,201 accesses to 626 and 2,142. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,053 to 10,038, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer keeps `DisplayObject`, `NativeText`, and `TextLabelRuntime` structural; anonymous structural transfer plus optional omission keep `GridLayoutItemStyle` structural; and `AccessibilityNode` has no closed construction provenance. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit under a 300-second allowance, the JavaScript bundle and Haxe namespace compile pass, all 11 exact changed parity files pass 230/230 assertions across accessibility, layout, Scene2D-DOM/formats, SWF, text, and text layout, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The twenty-second high-access zero-escape direct-emission tranche fingerprint-locks `CanvasRenderEffectPipeline`, `ColorAdjustmentRuntime`, `GlScene3DDrawEntry`, `ShadedMaterialOptions`, and `RenderEffectPadding`. Their 60 accesses now use typed structural fields across six generated modules, taking the audit from 643 direct schemas and 23,651 direct accesses to 648 and 23,711 while pending work falls from 7,015 to 6,955. The zero-escape audit-only pool falls from 636 schemas and 2,261 accesses to 631 and 2,201. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,061 to 10,053, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer keeps `ColorAdjustmentRuntime` structural; optional omission keeps `ShadedMaterialOptions` structural; and the provenance-closed `CanvasRenderEffectPipeline`, `GlScene3DDrawEntry`, and `RenderEffectPadding` remain structural pending separate cpp review. `GlShaderLocations` remains audit-only because its optional-location presence check does not yet receive a direct binding. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit under a 300-second allowance, the JavaScript bundle and Haxe namespace compile pass, all six exact changed parity files pass 62/62 assertions across effects, Canvas effects, node, render, Scene3D-WebGL, and shading, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The twenty-first high-access zero-escape direct-emission tranche fingerprint-locks `AccessibilityState`, `WgpuVideoTextureEntry`, `WgpuShapeMesh`, `WgpuScene3DDrawEntry`, and `VignetteEffect`. Their 61 accesses now use typed structural fields across seven generated modules, taking the audit from 638 direct schemas and 23,590 direct accesses to 643 and 23,651 while pending work falls from 7,076 to 7,015. The zero-escape audit-only pool falls from 641 schemas and 2,322 accesses to 636 and 2,261. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,086 to 10,061, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Optional omission keeps `AccessibilityState` and `WgpuVideoTextureEntry` structural; observed object spread keeps `WgpuShapeMesh` structural; cross-schema transfer plus object-literal spread keep `VignetteEffect` structural; and the provenance-closed `WgpuScene3DDrawEntry` remains structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit under a 300-second allowance, the JavaScript bundle and Haxe namespace compile pass, all seven exact changed parity files pass 89/89 assertions across accessibility, Canvas/WebGL/WebGPU effects, render-WebGPU, Scene2D-WebGPU, and Scene3D-WebGPU, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The twentieth high-access zero-escape direct-emission tranche fingerprint-locks `MatcapMaterial`, `LottieShapePath`, `GlRenderEffectApplicationExplanation`, `FlexLayoutItemStyle`, and `BitmapFingerprint`. Their 65 accesses now use typed structural fields across ten generated modules, taking the audit from 633 direct schemas and 23,525 direct accesses to 638 and 23,590 while pending work falls from 7,141 to 7,076. The zero-escape audit-only pool falls from 646 schemas and 2,387 accesses to 641 and 2,322. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,136 to 10,086, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer plus optional omission keep `MatcapMaterial` structural; dynamic ingress keeps `LottieShapePath` structural; anonymous structural transfer plus optional omission keep `FlexLayoutItemStyle` structural; and the provenance-closed `GlRenderEffectApplicationExplanation` and `BitmapFingerprint` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit under a 300-second allowance, the JavaScript bundle and Haxe namespace compile pass, all 11 exact changed parity files pass 121/121 assertions across bitmap, capture, WebGL effects, layout, materials, Scene2D formats, Scene3D-WebGL, and Scene3D-WebGPU, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The nineteenth high-access zero-escape direct-emission tranche fingerprint-locks `SpriteData`, `Skeleton2DIkConstraint`, `Physics2DRayResult`, `PbrExtension`, and `NativeTextRuntime`. Their 65 accesses now use typed structural fields across 15 generated modules, taking the audit from 628 direct schemas and 23,460 direct accesses to 633 and 23,525 while pending work falls from 7,206 to 7,141. The zero-escape audit-only pool falls from 651 schemas and 2,452 accesses to 646 and 2,387. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,153 to 10,136, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Normalization provenance keeps `SpriteData` structural; cross-schema transfer keeps `Skeleton2DIkConstraint`, `PbrExtension`, and `NativeTextRuntime` structural; and the provenance-closed `Physics2DRayResult` remains structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit under a 300-second allowance, the JavaScript bundle and Haxe namespace compile pass, all 15 exact changed parity files pass 226/226 assertions across interaction, movie clip, physics2d, Scene2D, Canvas, DOM, WebGL, WebGPU, Scene3D formats/resources/WebGL, Skeleton2D, SWF, and text, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The eighteenth high-access zero-escape direct-emission tranche fingerprint-locks `StrokeStyle`, `Socket`, `Physics2DDebugGeometry`, `Modifier`, and `StatechartState`. Their 69 accesses now use typed structural fields across 15 generated modules, taking the audit from 623 direct schemas and 23,391 direct accesses to 628 and 23,460 while pending work falls from 7,275 to 7,206. The zero-escape audit-only pool falls from 656 schemas and 2,521 accesses to 651 and 2,452. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,187 to 10,153, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Optional omission keeps `StrokeStyle` structural; normalization provenance keeps `Modifier` structural; `StatechartState` has no production construction site; and the provenance-closed `Socket` and `Physics2DDebugGeometry` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit under a 300-second allowance, the JavaScript bundle and Haxe namespace compile pass, all 15 exact changed parity files pass 159/159 assertions across path, physics2d, Scene3D, Scene3D-WebGL, Scene3D-WebGPU, shading, shape, socket, and statechart, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The seventeenth high-access zero-escape direct-emission tranche fingerprint-locks `WgpuScene3DIbl`, `WgpuColorLutTextureCache`, `WgpuMeshUpload`, `Viewport`, and `TauriApi`. Their 72 accesses now use typed structural fields across 20 generated modules, taking the audit from 618 direct schemas and 23,319 direct accesses to 623 and 23,391 while pending work falls from 7,347 to 7,275. The zero-escape audit-only pool falls from 661 schemas and 2,593 accesses to 656 and 2,521. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,196 to 10,187, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer keeps `Viewport` structural; `WgpuMeshUpload` has no provenance row; and the provenance-closed `WgpuScene3DIbl`, `WgpuColorLutTextureCache`, and `TauriApi` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit under a 300-second allowance, the JavaScript bundle and Haxe namespace compile pass, and portable Eval/JavaScript/Python/C++ verification passes. The 20 exact changed parity files pass 177/183 assertions across application, effects-WebGPU, host-Tauri, node, render-WebGL, and Scene3D-WebGPU; the same six Host-Tauri menu/tray constructor-shim failures reproduce the committed 36/42 package baseline, while every other affected assertion passes. Run-specific parity timestamps are not committed.
- The sixteenth high-access zero-escape direct-emission tranche fingerprint-locks `AbcTrait`, `CanvasRenderTextureEntry`, `NetRequest`, `SheenPbrExtension`, and `ThreeDsLight`. Their 76 accesses now use typed structural fields across eight generated modules, taking the audit from 613 direct schemas and 23,243 direct accesses to 618 and 23,319 while pending work falls from 7,423 to 7,347. The zero-escape audit-only pool falls from 666 schemas and 2,669 accesses to 661 and 2,593. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,241 to 10,196, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Container transfer plus normalization provenance keep `AbcTrait` structural, optional omission keeps `NetRequest` structural, cross-schema transfer keeps `SheenPbrExtension` structural, and the provenance-closed `CanvasRenderTextureEntry` and `ThreeDsLight` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit under a 300-second allowance, the JavaScript bundle and Haxe namespace compile pass, all eight exact changed parity files pass 125/125 assertions across ABC, materials, networking, Canvas render textures, Scene3D formats/resources/WebGL, and SWF, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The fifteenth high-access zero-escape direct-emission tranche fingerprint-locks `AnimationSampleAccumulator`, `AnimationLayer`, `AnimationBlendTreeInput`, `LottieKeyframe`, and `Skeleton2DTransformConstraint`. Their 77 accesses now use typed structural fields across five generated modules, taking the audit from 608 direct schemas and 23,166 direct accesses to 613 and 23,243 while pending work falls from 7,500 to 7,423. The zero-escape audit-only pool falls from 671 schemas and 2,746 accesses to 666 and 2,669. Generated `Dynamic` remains 46,948, `_Runtime.field` calls fall from 10,279 to 10,241, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Cross-schema transfer keeps `AnimationSampleAccumulator`, `AnimationLayer`, `AnimationBlendTreeInput`, and `Skeleton2DTransformConstraint` structural; `LottieKeyframe` has no production construction site. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit under a 300-second allowance, the JavaScript bundle and Haxe namespace compile pass, five exact changed parity files pass 44/44 assertions, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The fourteenth high-access zero-escape direct-emission tranche fingerprint-locks `TextShaperBackend`, `TextLayoutParams`, `SoftKeyboardInfo`, `LayoutTree`, and `Sprite`. Their 84 accesses now use typed structural fields across 17 generated modules, taking the audit from 603 direct schemas and 23,082 direct accesses to 608 and 23,166 while pending work falls from 7,584 to 7,500. The zero-escape audit-only pool falls from 676 schemas and 2,830 accesses to 671 and 2,746. Generated `Dynamic` falls from 46,949 to 46,948, `_Runtime.field` calls fall from 10,315 to 10,279, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. `TextShaperBackend` has no production construction site; optional omission plus observed object spread keep `TextLayoutParams` structural; the provenance-closed `SoftKeyboardInfo` and `LayoutTree` remain structural pending separate cpp review; and cross-schema transfer keeps `Sprite` structural. The focused typed-struct suite passes 23/23, all 154 unit assertions pass with the heavyweight inventory audit isolated under a 300-second timeout, the JavaScript bundle and Haxe namespace compile pass, and portable Eval/JavaScript/Python/C++ verification passes. Sixteen exact changed parity files pass 263/263 assertions across host, interaction, layout, movie clip, Scene2D, SWF, text layout, and text shaping; the keyboard file reproduces its recorded 53/60 baseline with the same seven receiver-binding failures in unchanged backend call sites. Run-specific parity timestamps are not committed.
- The thirteenth high-access zero-escape direct-emission tranche fingerprint-locks `WgpuRenderTexturePool`, `GlRenderTexturePool`, `GlShadedProgram`, `GlShapeRendererData`, and `WgpuQuadBatchWriterBufferSlot`. Their 83 accesses now use typed structural fields across ten generated modules, taking the audit from 598 direct schemas and 22,999 direct accesses to 603 and 23,082 while pending work falls from 7,667 to 7,584. The zero-escape audit-only pool falls from 681 schemas and 2,913 accesses to 676 and 2,830. Generated `Dynamic` remains 46,949, `_Runtime.field` calls fall from 10,329 to 10,315, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. The provenance-closed WebGPU and WebGL render-texture pools remain structural pending separate cpp review; object-literal spread keeps `GlShadedProgram` structural; dynamic ingress keeps `GlShapeRendererData` structural; and normalization provenance keeps `WgpuQuadBatchWriterBufferSlot` structural. The focused typed-struct suite passes 23/23, the full unit suite passes 154/154, the JavaScript bundle and Haxe namespace compile pass, all ten exact changed parity files pass 96/96 assertions across effects, render, Scene2D, and Scene3D, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The twelfth high-access zero-escape direct-emission tranche fingerprint-locks `GlColorScaleBiasInstancedShader`, `LambertMaterial`, `OrbitCameraControllerOptions`, `WgpuShapeRendererData`, and `AnimationBlendTree`. Their 89 accesses now use typed structural fields across 13 generated modules, taking the audit from 593 direct schemas and 22,910 direct accesses to 598 and 22,999 while pending work falls from 7,756 to 7,667. The zero-escape audit-only pool falls from 686 schemas and 3,002 accesses to 681 and 2,913. Generated `Dynamic` remains 46,949, `_Runtime.field` calls fall from 10,352 to 10,329, generated `Reflect` remains zero, and the 24,158-entry type-erasure report is unchanged. Normalization provenance keeps `GlColorScaleBiasInstancedShader` structural; cross-schema transfer plus optional omission keep `LambertMaterial` structural; `OrbitCameraControllerOptions` has no production construction provenance; the provenance-closed `WgpuShapeRendererData` remains structural pending separate cpp review; and cross-schema transfer keeps `AnimationBlendTree` structural. The focused typed-struct suite passes 23/23, the full unit suite passes 154/154, the JavaScript bundle and Haxe namespace compile pass, all 13 exact changed parity files pass 123/123 assertions across animation, camera controls, materials, render, Scene2D, WebGL, WebGPU, and Scene3D, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The eleventh high-access zero-escape direct-emission tranche fingerprint-locks `ExtendedPbrMaterial`, `TweenPropertyDetail`, `WgpuScene3DShadow`, `AnimationClipEvent`, and `BitmapTextRuntime`. Their 93 accesses now use typed structural fields across 18 generated modules, taking the audit from 588 direct schemas and 22,817 direct accesses to 593 and 22,910 while pending work falls from 7,849 to 7,756. The zero-escape audit-only pool falls from 691 schemas and 3,095 accesses to 686 and 3,002. Generated `Dynamic` remains 46,949, `_Runtime.field` calls fall from 10,359 to 10,352, generated `Reflect` remains zero, and exposing the checker-known animation-event `unknown` payload adds one explicit `source-unknown` erasure, taking the erasure ledger from 24,157 to 24,158 without guessing its type. Cross-schema transfer plus dynamic ingress keep `ExtendedPbrMaterial` structural; container transfer keeps `TweenPropertyDetail` structural; the provenance-closed `WgpuScene3DShadow` remains structural pending separate cpp review; and cross-schema transfer keeps `AnimationClipEvent` and `BitmapTextRuntime` structural. The focused typed-struct suite passes 23/23, the full unit suite passes 154/154, the JavaScript bundle and Haxe namespace compile pass, 17 exact changed parity files pass 292/292 assertions across animation, bitmap text, materials, Scene2D, Scene3D, and tween, and portable Eval/JavaScript/Python/C++ verification passes. The changed internal tween helper has no standalone upstream test and is covered through its three exact changed callers. Run-specific parity timestamps are not committed.
- The tenth high-access zero-escape direct-emission tranche fingerprint-locks `ToonMaterial`, `UnlitMaterial`, `ConvolutionEffect`, `EmissiveMaterial`, and `TransformInherit2D`. Their 102 accesses now use typed structural fields across 13 generated modules, taking the audit from 583 direct schemas and 22,715 direct accesses to 588 and 22,817 while pending work falls from 7,951 to 7,849. The zero-escape audit-only pool falls from 696 schemas and 3,197 accesses to 691 and 3,095. Generated `Dynamic` falls from 46,951 to 46,949, `_Runtime.field` calls fall from 10,427 to 10,359, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Cross-schema transfer and optional omission keep `ToonMaterial`, `UnlitMaterial`, and `EmissiveMaterial` structural; cross-schema transfer plus object-literal spread keep `ConvolutionEffect` structural; and anonymous structural transfer keeps `TransformInherit2D` structural. The focused typed-struct suite passes 23/23, the full unit suite passes 154/154, the JavaScript bundle and Haxe namespace compile pass, all 13 exact changed parity files pass 101/101 assertions across effects, materials, Scene3D formats/resources/WebGL/WebGPU, and Skeleton2D, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The ninth high-access zero-escape direct-emission tranche fingerprint-locks `AbcInstruction`, `AbcMultiname`, `AnimationRootMotionExtractor`, `CanvasRenderTexturePool`, and `LogEntry`. Their 107 accesses now use typed structural fields across six generated modules, taking the audit from 578 direct schemas and 22,608 direct accesses to 583 and 22,715 while pending work falls from 8,058 to 7,951. The zero-escape audit-only pool falls from 701 schemas and 3,304 accesses to 696 and 3,197. Generated `Dynamic` falls from 46,955 to 46,951, `_Runtime.field` calls fall from 10,469 to 10,427, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Container transfer keeps `AbcInstruction` structural, normalization provenance keeps `AbcMultiname` structural, cross-schema transfer keeps `AnimationRootMotionExtractor` structural, object-spread observation keeps `LogEntry` structural, and the provenance-closed `CanvasRenderTexturePool` remains structural pending separate cpp review. The focused typed-struct suite passes 23/23, the full unit suite passes 154/154, the JavaScript bundle and Haxe namespace compile pass, all six exact changed parity files pass 149/149 assertions across ABC, animation, Canvas effects/render textures, logging, and SWF, and portable Eval/JavaScript/Python/C++ verification passes. Run-specific parity timestamps are not committed.
- The eighth high-access zero-escape direct-emission tranche fingerprint-locks `Scene2DKindUsage`, `MotionPath`, `InnerGlowEffect`, `CustomShaderMaterial`, and `CreateRenderTextureOptions`. Their 110 accesses now use typed structural fields across 14 generated modules, taking the audit from 573 direct schemas and 22,498 direct accesses to 578 and 22,608 while pending work falls from 8,168 to 8,058. The zero-escape audit-only pool falls from 706 schemas and 3,414 accesses to 701 and 3,304. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 10,549 to 10,469, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Cross-schema transfer plus object-literal spread keep `InnerGlowEffect` structural, cross-schema transfer keeps `CustomShaderMaterial` and `CreateRenderTextureOptions` structural, and the provenance-closed `Scene2DKindUsage` and `MotionPath` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All 14 exact changed parity files pass 119/119 assertions across effects, materials, motion path, render, Scene2D, WebGL, WebGPU, and texture. Run-specific parity timestamps are not committed.
- The seventh high-access zero-escape direct-emission tranche fingerprint-locks `DirectionalLight`, `SurfaceMaterial`, `MorphShapeGradientEndpoint`, `SpatialIndexingNotice`, and `Scene3DRenderList`. Their 114 accesses now use typed structural fields across 20 generated modules, taking the audit from 568 direct schemas and 22,384 direct accesses to 573 and 22,498 while pending work falls from 8,282 to 8,168. The zero-escape audit-only pool falls from 711 schemas and 3,528 accesses to 706 and 3,414. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 10,639 to 10,549, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Cross-schema transfer keeps `DirectionalLight` structural; cross-schema transfer plus dynamic ingress keep `SurfaceMaterial` structural; object spread keeps `SpatialIndexingNotice` structural; `MorphShapeGradientEndpoint` has no production construction provenance; and the provenance-closed `Scene3DRenderList` remains structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All 20 exact changed parity files pass 430/430 assertions across lighting, materials, render, Scene3D, formats, WebGL, WebGPU, shape, and spatial. Run-specific parity timestamps are not committed.
- The sixth high-access zero-escape direct-emission tranche fingerprint-locks `Scene3DResourceResolverRuntime`, `GradientGlowEffect`, `BitmapTextData`, `WgpuShapeMeshBuffers`, and `Scene3DDocumentNode`. Their 123 accesses now use typed structural fields across 19 generated modules, taking the audit from 563 direct schemas and 22,261 direct accesses to 568 and 22,384 while pending work falls from 8,405 to 8,282. The zero-escape audit-only pool falls from 716 schemas and 3,651 accesses to 711 and 3,528. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 10,675 to 10,639, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Cross-schema transfer plus object-literal spread keep `GradientGlowEffect` structural, normalization provenance keeps `BitmapTextData` structural, optional omission keeps `Scene3DDocumentNode` structural, and the provenance-closed `Scene3DResourceResolverRuntime` and `WgpuShapeMeshBuffers` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. The 19 exact changed parity files reproduce 611/612 assertions; the sole MD5 keyword-mapping failure (`default_` versus JavaScript key `default`) reproduces at 53/54 in isolation, and its generated assignment is unchanged by this receiver-only diff. Run-specific parity timestamps are not committed.
- The fifth high-access zero-escape direct-emission tranche fingerprint-locks `TextInputOptions`, `LottieDocument`, `Scene3DLightBlock`, `GodRaysEffect`, and `NativeTextData`. Their 134 accesses now use typed structural fields across ten generated modules, taking the audit from 558 direct schemas and 22,127 direct accesses to 563 and 22,261 while pending work falls from 8,539 to 8,405. The zero-escape audit-only pool falls from 721 schemas and 3,785 accesses to 716 and 3,651. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 10,761 to 10,675, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Dynamic ingress and JSON serialization keep `LottieDocument` structural, anonymous structural transfer keeps `Scene3DLightBlock` structural, cross-schema transfer plus object-literal spread keep `GodRaysEffect` structural, normalization provenance keeps `NativeTextData` structural, and `TextInputOptions` has no production construction site. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All ten exact changed parity files pass 167/167 assertions across effects, WebGL, WebGPU, render, DOM, Lottie, Scene3D, text, and text input. Run-specific parity timestamps are not committed.
- The fourth high-access zero-escape direct-emission tranche fingerprint-locks `ElectronApi`, `GlLitProgram`, `LayoutState`, `MeshGeometryRuntime`, and `QuadBatch`. Their 140 accesses now use typed structural fields across 32 generated modules, taking the audit from 553 direct schemas and 21,987 direct accesses to 558 and 22,127 while pending work falls from 8,679 to 8,539. The zero-escape audit-only pool falls from 726 schemas and 3,925 accesses to 721 and 3,785. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 10,810 to 10,761, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Cross-schema transfer keeps `GlLitProgram` and `MeshGeometryRuntime` structural; cross-schema transfer plus dynamic ingress keep `QuadBatch` structural; and the provenance-closed `ElectronApi` and `LayoutState` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. The 32 exact changed parity files reproduce 289/291 assertions: layout 10/10, mesh 64/64, quadbatch 84/84, Canvas 5/5, Scene2D GL 21/21, Scene2D WGPU 17/17, Scene3D GL 9/9, Scene3D WGPU 14/14, and Electron 65/67. Electron's two subscription-unsubscribe callback-identity failures reproduce in single-file isolation; their generated callback registration/removal lines are unchanged by this receiver-only direct-field diff. Run-specific parity timestamps are not committed.
- The third high-access zero-escape direct-emission tranche fingerprint-locks `StandardPbrMaterial`, `TextSelectionRectangle`, `LayoutNode`, `Scene3DKindUsage`, and `TextureSource`. Their 147 accesses now use typed structural fields across 35 generated modules, taking the audit from 548 direct schemas and 21,840 direct accesses to 553 and 21,987 while pending work falls from 8,826 to 8,679. The zero-escape audit-only pool falls from 731 schemas and 4,072 accesses to 726 and 3,925. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 10,836 to 10,810, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Cross-schema transfer and dynamic ingress keep `StandardPbrMaterial` structural, anonymous structural transfer keeps `TextSelectionRectangle` structural, object spread keeps `LayoutNode` structural, cross-schema transfer keeps `TextureSource` structural, and the provenance-closed `Scene3DKindUsage` remains structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All 35 exact changed parity files pass 682/682 assertions across 17 packages. Run-specific parity timestamps are not committed.
- The second high-access zero-escape direct-emission tranche fingerprint-locks `Clock`, `AreaLight`, `LottieLayer`, `MovieClipData`, and `PathMesh`. Their 165 accesses now use typed structural fields across ten generated modules, taking the audit from 543 direct schemas and 21,675 direct accesses to 548 and 21,840 while pending work falls from 8,991 to 8,826. The zero-escape audit-only pool falls from 736 schemas and 4,237 accesses to 731 and 4,072. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 10,886 to 10,836, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Cross-schema transfer keeps `AreaLight` structural, normalization provenance keeps `MovieClipData` structural, `LottieLayer` has no production construction provenance, and the provenance-closed `Clock` and `PathMesh` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All ten exact changed parity files pass 122/122 assertions: clock 36, lighting 5, movieclip 49, path 18, scene2d-formats 2, scene2d-gl 6, and scene2d-wgpu 6. Run-specific parity timestamps are not committed.
- The highest-access zero-escape direct-emission tranche fingerprint-locks `FlyCameraController`, `ParticleEmitter3D`, `SocketRuntime`, `NodeOrderList`, and `PackableRectangle`. Their 241 accesses now use typed structural fields across 11 generated modules, taking the audit from 538 direct schemas and 21,434 direct accesses to 543 and 21,675 while pending work falls from 9,232 to 8,991. The zero-escape audit-only pool falls from 741 schemas and 4,478 accesses to 736 and 4,237. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 10,962 to 10,886, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Cross-schema transfer keeps `FlyCameraController` structural, dynamic ingress keeps `ParticleEmitter3D` structural, normalization provenance keeps `NodeOrderList` structural, and the provenance-closed `SocketRuntime` and `PackableRectangle` remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All 11 exact changed parity files pass 226/226 assertions: binpack 26, camera-controls 12, node 47, particleemitter 73, scene3d-gl 18, scene3d-wgpu 12, and socket 38. Run-specific parity timestamps are not committed.
- The PBR-extension direct-emission tranche fingerprint-locks `TransmissionVolumePbrExtension`, `ClearcoatPbrExtension`, `IridescencePbrExtension`, `WrappedDiffusePbrExtension`, and `SpecularPbrExtension`. Their 111 accesses now use typed structural fields across 16 generated materials, scene-format, WebGL, and resource modules, taking the audit from 533 direct schemas and 21,323 direct accesses to 538 and 21,434 while pending work falls from 9,343 to 9,232. The zero-escape audit-only pool falls from 746 schemas and 4,589 accesses to 741 and 4,478. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 11,066 to 10,962, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. Cross-schema transfer keeps all five PBR extension records structural and outside the cpp provenance candidate pool. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All 16 exact changed parity files pass 32/32 assertions: materials 10, scene3d-formats 7, scene3d-gl 10, and scene3d-resources 5. Run-specific parity timestamps are not committed.
- The Tiled-leaf direct-emission tranche fingerprint-locks `TiledTilesetTile`, `TiledTilesetRef`, `TiledProperty`, `TiledTilesetTileFrame`, and `TiledGid`. Their 34 accesses now use typed structural fields across three generated Tiled-format modules, taking the audit from 528 direct schemas and 21,289 direct accesses to 533 and 21,323 while pending work falls from 9,377 to 9,343. The zero-escape audit-only pool falls from 751 schemas and 4,623 accesses to 746 and 4,589. Generated `Dynamic` remains 46,955, `_Runtime.field` calls fall from 11,087 to 11,066, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. `TiledGid`, `TiledTilesetRef`, and `TiledTilesetTile` are provenance-closed but remain structural pending separate cpp review; container transfer keeps `TiledProperty` and `TiledTilesetTileFrame` structural. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All three exact changed Tiled-format parity files pass 13/13 assertions. Run-specific parity timestamps are not committed.
- The tilemap/Tiled direct-emission tranche fingerprint-locks `TilemapData`, `TiledObject`, `TiledMap`, `Tilemap`, and `TiledTileset`. Their 144 accesses now use typed structural fields across seven generated modules, taking the audit from 523 direct schemas and 21,145 direct accesses to 528 and 21,289 while pending work falls from 9,521 to 9,377. The zero-escape audit-only pool falls from 756 schemas and 4,767 accesses to 751 and 4,623. Generated `Dynamic` falls from 46,965 to 46,955, `_Runtime.field` calls fall from 11,192 to 11,087, generated `Reflect` remains zero, and the 24,157-entry type-erasure report is unchanged. `TiledObject`, `TiledMap`, and `TiledTileset` are provenance-closed but remain structural pending separate cpp review; container-transfer and normalization provenance keep `TilemapData` structural, and cross-schema transfer keeps `Tilemap` structural. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All seven exact changed parity files pass 83/83 assertions: tilemap 54, Tiled formats 13, Canvas 3, WebGL 9, and WebGPU 4. Run-specific parity timestamps are not committed.
- The interaction-state direct-emission tranche fingerprint-locks `InteractionManager`, `InputState`, `PointerEventData`, `NodeInteractionState`, and `InteractionPointerState`. Their 148 accesses now use typed structural fields across five generated modules, taking the audit from 518 direct schemas and 20,997 direct accesses to 523 and 21,145 while pending work falls from 9,669 to 9,521. The zero-escape audit-only pool falls from 761 schemas and 4,915 accesses to 756 and 4,767. Generated `Dynamic` remains 46,965, `_Runtime.field` calls fall from 11,200 to 11,192, and generated `Reflect` remains zero. `InteractionManager` and `InputState` are provenance-closed but remain structural pending separate cpp review; cross-schema transfer keeps `PointerEventData` structural, normalization provenance keeps `NodeInteractionState` structural, and container transfer keeps `InteractionPointerState` structural. Six newly visible `source-never` assignments in `NodeInteractionState` remain explicit rather than receiving guessed types, taking the type-erasure report from 24,151 to 24,157. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All five exact changed parity files pass 200/200 assertions: input 109 and interaction 91. Run-specific parity timestamps are not committed.
- The shading-modifier-options direct-emission tranche fingerprint-locks `AnimatedNormalModifierOptions`, `DissolveModifierOptions`, `EmissiveModifierOptions`, `FogModifierOptions`, and `VertexDisplaceModifierOptions`. Their 33 accesses now use typed structural fields across five generated factories, taking the audit from 513 direct schemas and 20,964 direct accesses to 518 and 20,997 while pending work falls from 9,702 to 9,669. The zero-escape audit-only pool falls from 766 schemas and 4,948 accesses to 761 and 4,915. Generated `Dynamic` remains 46,965, `_Runtime.field` calls fall from 11,233 to 11,200, generated `Reflect` remains zero, and the 24,151-entry type-erasure report is unchanged. All five records are mechanically class-compatible but have no production construction site, so they remain structural and outside the cpp provenance candidate pool. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All five exact changed shading parity files pass 20/20 assertions. Run-specific parity timestamps are not committed.
- The shading-modifier direct-emission tranche fingerprint-locks `VertexDisplaceModifier`, `AnimatedNormalModifier`, `EmissiveModifier`, `FogModifier`, and `DissolveModifier`. Their 130 accesses now use typed structural fields across seven generated modules, taking the audit from 508 direct schemas and 20,834 direct accesses to 513 and 20,964 while pending work falls from 9,832 to 9,702. The zero-escape audit-only pool falls from 771 schemas and 5,078 accesses to 766 and 4,948. Generated `Dynamic` remains 46,965, `_Runtime.field` calls fall from 11,357 to 11,233, generated `Reflect` remains zero, and the 24,151-entry type-erasure report is unchanged. Cross-schema transfer keeps all five records structural, while optional omission is additionally observable on every record except `FogModifier`; the five companion option schemas remain separately measured audit-only work. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All seven exact changed parity files pass 74/74 assertions: shading 25, scene3d-gl 31, and scene3d-wgpu 18. Run-specific parity timestamps are not committed.
- The backend-state/pipeline direct-emission tranche fingerprint-locks `GlScissorRect`, `WgpuScissorRect`, `CanvasRenderStateRuntime`, `GlRenderEffectPipeline`, and `WgpuRenderEffectPipeline`. Their 162 accesses now use typed structural fields across 16 generated modules, taking the audit from 503 direct schemas and 20,672 direct accesses to 508 and 20,834 while pending work falls from 9,994 to 9,832. The zero-escape audit-only pool falls from 776 schemas and 5,240 accesses to 771 and 5,078. Generated `Dynamic` remains 46,965, `_Runtime.field` calls fall from 11,403 to 11,357, and generated `Reflect` remains zero. Cross-schema transfer keeps the Canvas runtime and WebGL scissor record structural, normalization provenance keeps the WebGPU scissor record structural, and the two provenance-closed pipelines remain structural pending separate cpp review. Five newly visible `ImageSmoothingQuality` uses are explicitly classified as `standard-toolkit-boundary` rather than receiving guessed transpiler types, taking the type-erasure report from 24,146 to 24,151. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. All 16 exact changed parity files pass 197/197 assertions: effects-canvas 22, effects-gl 6, effects-wgpu 5, render-gl 62, render-wgpu 8, scene2d-canvas 86, scene2d-gl 5, and scene2d-wgpu 3. Run-specific parity timestamps are not committed.
- The directional-effect direct-emission tranche fingerprint-locks `BevelEffect`, `DropShadowEffect`, `GradientBevelEffect`, `InnerShadowEffect`, and `OuterGlowEffect`. Their 167 accesses now use typed structural fields across 21 generated modules, taking the audit from 498 direct schemas and 20,505 direct accesses to 503 and 20,672 while pending work falls from 10,161 to 9,994. The zero-escape audit-only pool falls from 781 schemas and 5,407 accesses to 776 and 5,240. Generated `Dynamic` remains 46,965, `_Runtime.field` calls fall from 11,570 to 11,403, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Cross-schema transfer and object-literal spread keep all five records structural. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including a clean C++ compile/link/run. The 21 exact changed parity files pass 121/123 assertions: effects 21/21, WebGL 25/25, WebGPU 25/25, and Canvas's unchanged 50/52 baseline. The two Canvas default draw-mode mocks still expect three arguments while the compiled bridge passes an unchanged fourth `undefined`; the direct-field diff does not touch those calls. Run-specific parity timestamps are not committed.
- The WebGL-program/upload direct-emission tranche fingerprint-locks `GlMeshProgram`, `GlClassicProgram`, `GlMeshUpload`, `GlParticleShader`, and `GlPbrProgram`. Their 179 accesses now use typed structural fields across 15 generated modules, taking the audit from 493 direct schemas and 20,326 direct accesses to 498 and 20,505 while pending work falls from 10,340 to 10,161. The zero-escape audit-only pool falls from 786 schemas and 5,586 accesses to 781 and 5,407. Generated `Dynamic` remains 46,965, `_Runtime.field` calls fall from 11,653 to 11,570, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Object-literal spread keeps the classic and PBR program records structural; anonymous/cross-schema transfer and optional omission keep the mesh program structural; optional omission keeps the mesh upload structural; and normalization provenance keeps the particle shader structural. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. All 15 exact changed parity files pass 148/148 assertions: render-gl 26, scene2d-gl 9, and scene3d-gl 113. Run-specific parity timestamps are not committed.
- The shape-data direct-emission tranche fingerprint-locks `Shape`, `Scale9Shape`, `ShapeData`, `MorphShape`, and `MorphShapeData`. Their 184 accesses now use typed structural fields across 20 generated modules, taking the audit from 488 direct schemas and 20,142 direct accesses to 493 and 20,326 while pending work falls from 10,524 to 10,340. The zero-escape audit-only pool falls from 791 schemas and 5,770 accesses to 786 and 5,586. Generated `Dynamic` falls from 46,972 to 46,965, `_Runtime.field` calls fall from 11,667 to 11,653, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Cross-schema transfer keeps `Shape`, `Scale9Shape`, and `MorphShape` structural, `Shape` additionally has dynamic ingress, and propagated normalization provenance keeps `ShapeData` and `MorphShapeData` structural. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. All 20 exact changed parity files pass 308/308 assertions in package-isolated serial groups: interaction 2, Canvas 18, DOM 11, GL 17, WGPU 18, shape 105, shape-formats 28, and SWF 109. Run-specific parity timestamps are not committed.
- The text-runtime/layout direct-emission tranche fingerprint-locks `RichTextRuntime`, `TextLabelData`, `BitmapTextPage`, `TextLabel`, and `ShapedRun`. Their 204 accesses now use typed structural fields across 23 generated modules, taking the audit from 483 direct schemas and 19,938 direct accesses to 488 and 20,142 while pending work falls from 10,728 to 10,524. The zero-escape audit-only pool falls from 796 schemas and 5,974 accesses to 791 and 5,770. Generated `Dynamic` falls from 46,976 to 46,972, `_Runtime.field` calls fall from 11,699 to 11,667, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Cross-schema transfer keeps `RichTextRuntime` and `TextLabel` structural, object spread keeps `ShapedRun` structural, and propagated normalization provenance keeps `BitmapTextPage` and `TextLabelData` structural. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. The 23-file grouped parity run passes 379/380 assertions; its sole `CanvasBitmapText` empty-text failure is shared-state contamination from earlier files and passes 3/3 when the changed file is run in isolation. Run-specific parity timestamps are not committed.
- The hit/contact direct-emission tranche fingerprint-locks `Scene3DHit`, `CollisionRaycastHit`, `Physics2DRayHit`, `CollisionContactPoint`, and `VelocitySample`. Their 200 accesses now use typed structural fields across nine generated modules, taking the audit from 478 direct schemas and 19,738 direct accesses to 483 and 19,938 while pending work falls from 10,928 to 10,728. The zero-escape audit-only pool falls from 801 schemas and 6,174 accesses to 796 and 5,974. Generated `Dynamic` remains 46,976, `_Runtime.field` calls fall from 11,789 to 11,699, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Cross-schema entity transfer keeps `Scene3DHit` structural; `CollisionRaycastHit`, `Physics2DRayHit`, `CollisionContactPoint`, and `VelocitySample` are mechanically compatible and provenance-closed but remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. All nine exact changed parity files pass 210/210 assertions across collision, physics2d, picking, and velocity. Run-specific parity timestamps are not committed.
- The texture-container/render-texture direct-emission tranche fingerprint-locks `TextureContainer`, `TextureContainerLevel`, `RenderTexture`, `GlRenderTextureEntry`, and `WgpuRenderTextureEntry`. Their 171 accesses now use typed structural fields across 12 generated modules, taking the audit from 473 direct schemas and 19,567 direct accesses to 478 and 19,738 while pending work falls from 11,099 to 10,928. The zero-escape audit-only pool falls from 806 schemas and 6,345 accesses to 801 and 6,174. Generated `Dynamic` remains 46,976, `_Runtime.field` calls fall from 11,852 to 11,789, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Object spread keeps `TextureContainer` structural, cross-schema transfer keeps `RenderTexture` structural, and propagated normalization provenance keeps the three mechanically compatible leaf records structural. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. All 12 exact changed parity files pass 118 assertions: image 5, render-gl 49, render-wgpu 30, scene2d-canvas 19, texture 2, and texture-formats 13. Run-specific parity timestamps are not committed.
- The state-machine/statechart direct-emission tranche fingerprint-locks `AnimationStateMachine`, `AnimationCrossfade`, `Statechart`, `StatechartInstance`, and `StatechartTransition`. Their 172 accesses now use typed structural fields across seven generated modules, taking the audit from 468 direct schemas and 19,395 direct accesses to 473 and 19,567 while pending work falls from 11,271 to 11,099. The zero-escape audit-only pool falls from 811 schemas and 6,517 accesses to 806 and 6,345. Generated `Dynamic` remains 46,976, `_Runtime.field` calls fall from 11,952 to 11,852, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Cross-schema transfers keep the two animation records structural, JSON serialization keeps `Statechart` structural, and the provenance-closed instance and transition remain structural pending separate cpp review. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. All seven exact changed parity files pass 78 assertions: animation 35 and statechart 43. Run-specific parity timestamps are not committed.
- The skeleton/skin direct-emission tranche fingerprint-locks `Skeleton2D`, `Skeleton3D`, `MeshSkinBindPose`, `SkinAttachment2D`, and `Skeleton2DPathConstraint`. Their 179 accesses now use typed structural fields across 24 generated modules, taking the audit from 463 direct schemas and 19,216 direct accesses to 468 and 19,395 while pending work falls from 11,450 to 11,271. The zero-escape audit-only pool falls from 816 schemas and 6,696 accesses to 811 and 6,517. Generated `Dynamic` falls from 46,982 to 46,976, `_Runtime.field` calls fall from 12,078 to 11,952, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Cross-schema transfers keep `Skeleton2D`, `Skeleton3D`, and `Skeleton2DPathConstraint` structural; propagated normalization provenance keeps the mechanically compatible `MeshSkinBindPose` and `SkinAttachment2D` leaves structural. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. All 24 exact changed parity files pass 348 assertions: mesh 2, scene2d-formats 38, scene3d-formats 2, scene3d-gl 44, scene3d-wgpu 37, skeleton2d 98, skeleton2d-formats 99, and skeleton3d 28. Run-specific parity timestamps are not committed.
- The physics-joint direct-emission tranche fingerprint-locks `Physics2DPrismaticJoint`, `Physics2DPulleyJoint`, `Physics2DGearJoint`, `Physics2DWheelJoint`, and `Physics2DRevoluteJoint`. Their 155 accesses now use typed structural fields across two generated modules, taking the audit from 458 direct schemas and 19,061 direct accesses to 463 and 19,216 while pending work falls from 11,605 to 11,450. The zero-escape audit-only pool falls from 821 schemas and 6,851 accesses to 816 and 6,696. Generated `Dynamic` remains 46,982, `_Runtime.field` remains 12,078, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Cross-schema transfer and object-literal spread keep all five records structural and outside the cpp provenance candidate pool. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. Both exact changed physics2d parity files pass 78/78 assertions. Run-specific parity timestamps are not committed.
- The collision/physics direct-emission tranche fingerprint-locks `Velocity2D`, `CollisionTimeOfImpact`, `Physics2DMassData`, `CollisionManifold`, and `CollisionContactManifold`. Their 281 accesses now use typed structural fields across 14 generated modules, taking the audit from 453 direct schemas and 18,780 direct accesses to 458 and 19,061 while pending work falls from 11,886 to 11,605. The zero-escape audit-only pool falls from 826 schemas and 7,132 accesses to 821 and 6,851. Generated `Dynamic` remains 46,982, `_Runtime.field` calls fall from 12,125 to 12,078, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. All five records are mechanically compatible and provenance-closed, but remain structural pending the separate cpp class review. The focused typed-struct suite passes 23/23, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. All 14 exact changed parity files pass 256 assertions: collision 102, physics2d 83, velocity 48, scene2d-gl 12, and scene2d-wgpu 11. Run-specific parity timestamps are not committed.
- The render/runtime direct-emission tranche fingerprint-locks `RenderStateRuntime`, `RenderProxy`, `DomRenderStateRuntime`, `ResolvedRenderTargetDescriptor`, and `Scene3DRenderProxy`. Their 297 accesses now use typed structural fields across 45 generated modules, taking the audit from 448 direct schemas and 18,483 direct accesses to 453 and 18,780 while pending work falls from 12,183 to 11,886. The zero-escape audit-only pool falls from 831 schemas and 7,429 accesses to 826 and 7,132. Generated `Dynamic` falls from 46,986 to 46,982, `_Runtime.field` calls fall from 12,156 to 12,125, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Cross-schema transfers keep both runtimes structural; anonymous and cross-schema transfers keep `RenderProxy` structural; observed object spread keeps `ResolvedRenderTargetDescriptor` structural; and optional omission keeps `Scene3DRenderProxy` structural. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. All 45 exact changed parity files pass 610 assertions: effects 8, render 157, render-gl 125, render-wgpu 55, scene2d-canvas 31, scene2d-dom 73, scene3d-gl 57, and scene3d-wgpu 104. Run-specific parity timestamps are not committed.
- The material-family direct-emission tranche fingerprint-locks `StandardPbrMaterialProperties`, `ShadedMaterial`, `SpecularGlossinessPbrMaterial`, `PhongMaterial`, and `BlinnPhongMaterial`. Their 286 accesses now use typed structural fields across 22 generated modules, taking the audit from 443 direct schemas and 18,197 direct accesses to 448 and 18,483 while pending work falls from 12,469 to 12,183. The zero-escape audit-only pool falls from 836 schemas and 7,715 accesses to 831 and 7,429. Generated `Dynamic` remains 46,986, `_Runtime.field` calls fall from 12,353 to 12,156, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. Cross-schema transfers keep all five identities structural, while optional omission is additionally observable on the specular-glossiness, Phong, and Blinn-Phong variants. Typed texture unions remain intact. The focused typed-struct suite passes 23/23, all 154 unit tests pass, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. The 21 exact changed parity files pass 294 assertions: materials 48, scene3d-formats 167, scene3d-gl 29, scene3d-resources 16, scene3d-wgpu 28, and shading 6. Run-specific parity timestamps are not committed.
- The input event/state direct-emission tranche fingerprint-locks `TextInputState`, `KeyboardEventData`, `InputManager`, `InputPointerData`, and `InputKeyboardData`. Their 288 accesses now use typed structural fields across 12 generated modules, taking the audit from 438 direct schemas and 17,909 direct accesses to 443 and 18,197 while pending work falls from 12,757 to 12,469. The zero-escape audit-only pool falls from 841 schemas and 8,003 accesses to 836 and 7,715. Generated `Dynamic` remains 46,986, `_Runtime.field` calls fall from 12,452 to 12,353, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. `KeyboardEventData` has a cross-schema transfer from `InputKeyboardData`, `InputManager` uses an object-literal spread, `InputPointerData` has observed spread semantics, and `TextInputState` inherits normalization provenance through `RichTextRuntime`; `InputKeyboardData` is provenance-closed but remains structural pending separate cpp review. The focused typed-struct suite passes 23/23, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. Affected parity passes 460 assertions: input 109, textinput 148, interaction 62, rich text 125, Canvas text input 5, DOM text input 4, GL text input 4, and WGPU text input 3. Redundant package-wide Canvas/DOM sweeps were stopped in their known compiled-bridge import hotspot after long failure-free prefixes; the exact changed adapter files pass.
- The Rive document-graph direct-emission tranche fingerprint-locks `RiveArtboardGraph`, `RiveProperty`, `RivePathRecord`, `RiveFileAsset`, and `RiveDocumentImportResult`. Their 205 accesses now use typed structural fields across 15 generated modules, taking the audit from 433 direct schemas and 17,704 direct accesses to 438 and 17,909 while pending work falls from 12,962 to 12,757. The zero-escape audit-only pool falls from 846 schemas and 8,208 accesses to 841 and 8,003. Generated `Dynamic` remains 46,986, `_Runtime.field` calls fall from 12,552 to 12,452, generated `Reflect` remains zero, and the 24,146-entry type-erasure report is unchanged. All five records are mechanically compatible and provenance-closed, but remain structural because cpp class emission is a separate explicit opt-in. The focused typed-struct suite passes 23/23, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. The complete 18-file Rive parity surface passes 244/244 assertions.
- The animation/timeline direct-emission tranche fingerprint-locks `AnimationTrack`, `Tween`, `AnimationChannel`, `AnimationPlayer`, and `Timeline`. Their 395 accesses now use typed structural fields across 25 generated modules, taking the audit from 428 direct schemas and 17,309 direct accesses to 433 and 17,704 while pending work falls from 13,357 to 12,962. The zero-escape audit-only pool falls from 851 schemas and 8,603 accesses to 846 and 8,208. Generated `Dynamic` occurrences fall from 46,990 to 46,986, `_Runtime.field` calls fall from 12,667 to 12,552, and generated `Reflect` remains zero. The type-erasure report falls from 24,167 to 24,146: 43 source-`any` erasures disappear while 22 checker-known source-`unknown` fields remain explicit. Cross-schema transfer keeps `AnimationTrack`, `AnimationChannel`, and `AnimationPlayer` structural; strict-equality observation keeps `Timeline` structural; and container-transfer provenance keeps `Tween` structural. The focused typed-struct suite passes 23/23, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. Affected parity passes 470 assertions: animation 131, tween 118, timeline 61, skeleton2d 52, scene2d-formats 40, Scene3D 6, Scene2D 6, shape 7, and movieclip 49. Run-specific parity timestamps are not committed.
- The next zero-escape direct-emission tranche fingerprint-locks `QuadBatchData`, `CanvasShapeDrawState`, `Scene3DDocument`, `OrbitCameraController`, and `RiveCoreObject`. Their 568 accesses now use typed structural fields across 37 generated modules, taking the audit from 423 direct schemas and 16,741 direct accesses to 428 and 17,309 while pending work falls from 13,925 to 13,357. The zero-escape audit-only pool falls from 856 schemas and 9,171 accesses to 851 and 8,603. Generated `Dynamic` occurrences fall from 47,000 to 46,990, `_Runtime.field` calls fall from 12,810 to 12,667, and generated `Reflect` remains zero. Three Canvas host-field erasures remain explicitly classified as `standard-toolkit-boundary`, taking the type-erasure report from 24,164 to 24,167. `CanvasShapeDrawState` is mechanically compatible and provenance-closed; quad-batch normalization provenance, Scene3D/Rive container transfers, and Orbit's cross-schema entity transfer correctly keep the other identities structural. `npm run fix`, the focused typed-struct suite at 23/23, all 154 generator tests, deterministic generation and repository checks, the complete Haxe namespace, the JavaScript bundle, and Eval/JavaScript/Python/C++ portability including C++ compile/link/run pass. Affected parity passes 924 assertions: quadbatch 84, camera controls 37, Scene3D documents 14, Scene3D resources 6, Canvas shapes 63, GL quad-batch/velocity 40, WGPU quad-batch/velocity 34, Scene3D formats 459, and Rive 187. The Scene3D-format subset also reproduces one pre-existing MD5 assertion where Haxe's keyword-safe `default_` property differs from the JavaScript key `default`; the generated assignment is identical before and after this tranche. Run-specific parity timestamps are not committed.
- The render-context/runtime direct-emission tranche fingerprint-locks `CanvasRenderEffectContext`, `GlRenderEffectContext`, `WgpuRenderEffectContext`, `GlScene3DRuntime`, and `WgpuScene3DRuntime`. Their 752 zero-escape accesses now use typed structural fields across 168 generated modules, taking the audit from 418 direct schemas and 15,989 direct accesses to 423 and 16,741 while pending work falls from 14,677 to 13,925. The zero-escape audit-only pool falls from 861 schemas and 9,923 accesses to 856 and 9,171. Generated `Dynamic` occurrences fall from 47,006 to 47,000, `_Runtime.field` calls fall from 13,154 to 12,810, and generated `Reflect` remains zero. Preserving checker-known `unknown` fields in `WgpuScene3DRuntime` adds 13 explicit `source-unknown` erasures, taking the type-erasure report from 24,151 to 24,164; the emitter does not guess their types or conflate them with maintained toolkit work. All five identities are mechanically class-compatible, and the three effect contexts are provenance-closed, but cpp class emission remains a separate explicit opt-in; optional-omission evidence and absent runtime provenance keep the two Scene3D identities structural. The focused typed-struct suite passes 23/23, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. The three complete effects parity suites reproduce 675/677 assertions: WebGL 266/266, WebGPU 239/239, and Canvas's unchanged 170/172 baseline with its two existing draw-mode failures. The four runtime-bearing Scene3D files pass 29/29 focused assertions: GL 8/8 and WGPU 21/21. The redundant package-wide GL sweep was stopped after 20 minutes in its previously documented compiled-bridge import hotspot; run-specific parity timestamps are not committed.
- The physics-and-clip direct-emission tranche fingerprint-locks `Physics2DWorld`, `Physics2DContact`, `Physics2DSolverConfig`, `Physics2DCollider`, and `ClipRegion`. Their 627 zero-escape accesses now use typed structural fields across 21 generated modules, taking the audit from 413 direct schemas and 15,362 direct accesses to 418 and 15,989 while pending work falls from 15,304 to 14,677. The zero-escape audit-only pool falls from 866 schemas and 10,550 accesses to 861 and 9,923. Generated `Dynamic` remains 47,006, `_Runtime.field` calls fall from 13,301 to 13,154, generated `Reflect` remains zero, and the 24,151-entry type-erasure report is unchanged. The cpp audit proves the four physics identities mechanically compatible and provenance-closed, but they remain structural because cpp class emission is a separate explicit opt-in; `ClipRegion` is mechanically compatible but correctly remains provenance-open through its cross-schema containment paths. The focused typed-struct suite passes 23/23, the complete Haxe namespace and JavaScript bundle pass, and Eval/JavaScript/Python/C++ portability passes including C++ compile/link/run. Eight complete affected upstream parity suites pass 1,788/1,788 assertions: physics2d 346, clip 62, scene2d-canvas 272, scene2d-dom 182, scene2d-formats 362, scene2d-gl 220, scene2d-wgpu 173, and SWF 171. Run-specific parity timestamps are not committed.
- The text-family direct-emission tranche fingerprint-locks `TextLayoutGroup`, `TextLayoutResult`, `RichTextData`, `RichText`, and `RichTextContent`. Their 891 zero-escape accesses now use typed structural fields across 27 generated modules, taking the audit from 408 direct schemas and 14,471 direct accesses to 413 and 15,362 while pending work falls from 16,195 to 15,304. The zero-escape audit-only pool falls from 871 schemas and 11,441 accesses to 866 and 10,550. Generated `Dynamic` occurrences fall from 47,009 to 47,006 and `_Runtime.field` calls from 13,431 to 13,301; generated `Reflect` remains zero and the 24,151-entry type-erasure report is unchanged. The cpp class audit independently keeps every identity structural: `RichText` and `RichTextData` have direct cross-schema transfers, while locally compatible `RichTextContent`, `TextLayoutGroup`, and `TextLayoutResult` inherit normalization provenance through their runtime parents. `npm run fix`, all 154 generator tests, deterministic generation and repository checks, the complete Haxe namespace, Eval/JavaScript/Python/C++ portability, and the JavaScript bundle pass. Nine affected upstream parity suites pass 1,874 assertions: text 195, textinput 148, textlayout 151, SWF 171, scene2d-formats 362, Canvas 272, DOM 182, GL 220, and WGPU 173. `text-markup` exactly reproduces the committed full-sweep baseline at 52/56; its four class-style resolver failures are unchanged and outside this direct-field diff.
- The render-target direct-emission tranche fingerprint-locks `CanvasRenderTarget`, `GlRenderTarget`, `RenderTarget`, `RenderTargetDescriptor`, and `WgpuRenderTarget`. Their 707 zero-escape accesses now use typed structural fields across 128 generated modules, taking the audit from 403 direct schemas and 13,764 direct accesses to 408 and 14,471 while pending work falls from 16,902 to 16,195. Generated `Dynamic` occurrences fall from 47,010 to 47,009 and `_Runtime.field` calls from 13,850 to 13,431; generated `Reflect` remains zero and the 24,151-entry type-erasure report is unchanged. The cpp class audit independently retains structural identity: anonymous and cross-schema transfers plus the descriptor's spread and optional-omission evidence prevent unsound nominalization. `npm run fix`, all 154 generator tests, deterministic generation and repository checks, the complete Haxe namespace, Eval/JavaScript/Python/C++ portability, and the JavaScript bundle pass. Ten complete affected parity suites pass 2,180 assertions; `effects-canvas` reproduces its unchanged committed 170/172 baseline, and the two exact changed-module `scene3d-gl` files pass 18/18. The redundant package-wide `scene3d-gl` run was stopped after 43 minutes of repeated compiled-bridge import once the focused files proved the changed surface.
- The next direct-emission tranche admits the escape-free `GlRenderStateRuntime` and `WgpuRenderStateRuntime` identities under exact declaration fingerprints. Their 1,274 accesses now use typed structural fields across 121 generated modules, taking the audit to 403 direct schemas and 13,764 direct accesses while pending work falls to 16,902. Generated `Dynamic` occurrences fall from 47,025 to 47,010 and `_Runtime.field` calls from 13,877 to 13,850. Retaining checker-derived field types exposes seven additional `standard-toolkit-boundary` IR erasures; these remain explicit host-toolkit debt rather than guessed transpiler types. The Haxe compile also exposed TypeScript's permissive trailing-callback-parameter assignment, so direct typed-struct function writes now receive a checker-typed arity adapter instead of weakening the field to `Dynamic`. The cpp class audit independently keeps both runtimes structural because their shared render-state base produces cross-schema transfers; Haxe direct access does not imply nominal construction identity. `npm run fix`, all 154 generator tests, deterministic generation and repository checks, the complete Haxe namespace, Eval/JavaScript/Python/C++ portable matrix, JavaScript bundle, and all six affected upstream parity suites pass: render-gl 373/373, render-wgpu 206/206, scene2d-gl 220/220, scene2d-wgpu 173/173, scene3d-gl 425/425, and scene3d-wgpu 332/332 (1,729 assertions total).
- The strict-Haxe/Rust-readiness campaign is tracked in [`strict-typing-todo.md`](strict-typing-todo.md). Its first slice replaces the one-off concrete `EntityWithoutRuntime<T>` rule with checker-identity materialization for closed, non-generic `Partial`, `Pick`, and `Omit` aliases and direct source wrappers. `ViewportLike`, both input-signal projections, `ApplicationRenderViewTargetOptions`, and the private physics joint projection now emit strict records while generic, open, shadowed, standard-library-only, and unresolved utilities remain symbolic. Named field types such as `RenderTargetFormat`, `RenderTargetDepth`, `RenderTargetColorSpace`, and `Kind` remain named rather than collapsing to their primitive representation. The generated counts fall from 347 to 346 `_Partial`, 26 to 24 `_Pick`, and 199 to 197 `_Omit` instantiations; generation drift and the complete Haxe namespace pass.
- The first direct-emission tranche admits `BitmapRegion` through a separate reviewed-addition registry rather than rewriting the historical migration baseline. Its stable public identity and declaration fingerprint are locked; generation now fails if it disappears, drifts, becomes ineligible, or gains any reflective escape. All 674 accesses emit directly, taking the audit from 400/11,816 to 401/12,490 direct schemas/accesses and pending work from 18,850 to 18,176 accesses. Direct typed-struct assignment also casts to the checker-derived field type instead of `Dynamic` across the existing direct set, changing 189 generated modules. Generated `Dynamic` occurrences fall from 49,168 to 47,025 and `_Runtime.field` calls from 14,548 to 13,877. All 153 generator tests, deterministic generation/repository checks, the complete Haxe namespace, Eval/JavaScript/Python/C++ portability, and upstream bitmap parity at 372/372 pass.
- `Rectangle`/`RectangleLike` now preserve the upstream structural API without reflective generated geometry access. Concrete `EntityWithoutRuntime<T>` aliases flatten to strict field typedefs, presence sensitivity is operation-local, and typed object destructuring retains its checker type and direct-field binding. `generated/flighthq/geometry/Rectangle.hx` has zero `Reflect`, `_Runtime.field`, or `_Runtime.setField` calls; the two `HitArea` discriminations remain `_Runtime.hasField`. The complete audit now records 1,536 eligible schemas, 400 direct schemas, and 11,816 direct accesses. Cpp nominalization remains correctly withheld for Rectangle: the class audit finds an `Entity` cross-schema transfer plus strict-identity observation, while `RectangleLike` is not provenance-closed through its Scale9 containment path. The resulting structural hxcpp output still contains 122 `__Field` and 62 `__SetField` operations in `geometry/Rectangle.cpp`; eliminating those requires a provenance-closed nominal class, not a cast or runtime wrapper. Eval and the full Eval/JavaScript/Python/C++ portable matrix pass.
- Checked typed calls no longer emit bare inference casts directly in argument position. Checker-identical values flow through unchanged; genuine TypeScript/Haxe structural mismatches use an explicit `Dynamic` carrier block, and omitted sentinels carry an explicit `Dynamic` type. This closes Neko's `Invalid call` in the compressed-SWF path under `-dce full`: the production `uncompressSwfSource` shape now passes its typed-array `subarray` result directly to `Decompressor`. A crafted CWS regression exercises `createScene2DFromSwf` through the full function shape. Repository checks, all 149 generator tests, the complete Haxe namespace, Neko with full DCE, and C++ compile/link/run pass. Canvas width/height integer coercion remains correctly owned by `CanvasElementBackend`; TypeScript's general DOM surface declares only `number`, so the emitter has no sound basis for coercing arbitrary numeric DOM writes.
- Host-backend campaign (user-directed): `flighthq.hostLime` now implements the backend side of upstream's original seams as named `create*` factories, and [`host-seams.md`](host-seams.md) records the full seam inventory plus the missing/bypassed-seam asks for upstream (audio node graph, image decode, scratch canvas; loaders bypassing NetBackend, window.ts document listeners). New in this campaign: `LimeAudio.createLimeAudioContext` (genuine browser context on js; native node-graph emulation over `lime.media.AudioSource`, with the `_internal/dom` audio types upgraded from Dynamic stubs to nominal off-js interfaces so emitted calls keep compile-time arity), `LimeLoop.createLimeLoopBackend` for `setLoopBackend` (makes `startApplicationLoop` work natively), and real `measureText` metrics in `NativeCanvas2dContext` (advance from the text path; ink and font boxes by rasterize-and-scan with caching, replacing width-only heuristics — closes the downstream fabricated-TextMetrics report). The sound example now uses the real audio context instead of a `{state:'running'}` stub; sound and text families pass the neko smoke both modes, html5 sound builds, `npm run check`, the full Haxe suite, and the portable matrix pass, and `flight.cjs` is byte-identical (`ddb7a2bd...` on this tree both with and without the change).
- The transpiler/runtime/toolkit boundary is strict. Checker-resolved host types map mechanically to same-name `flighthq._internal.dom` declarations, and ordinary host reads, writes, and calls emit one target-independent typed member expression; no native `_Runtime` field/call branch compensates for an incomplete toolkit. Ambient and external module values use `_HostValueLut` and `_HostModuleLut`, while `WebExterns`, `_internal/dom`, named backends, and host packages such as `hostLime` own declarations and implementations. The deterministic final-tree manifest records 194 host type keys, four external type keys, 68 ambient value keys (15 portable and 53 JavaScript-only), zero module value keys, and zero missing providers. It also exposes 197 `Dynamic` compatibility type entries—every current host type plus three renderer shapes—as toolkit debt rather than transpiler coverage. The final generated-tree SHA-256 is `3f8ac8f7180df5270fc3ab3386a99fa82bfdb7c8daad0ad655f7d869de43e9b2`, `flight.cjs` is `1f6dc396040cae9f58e16fe0553519073dfa41bad9e9a7456f6a6406e34748ed`, and `host-toolkit.json` is `19fb3b86098b7911903007b610c9b0fda993f0658e1d5c69d9670e11112bd913`. Repository checks, all 132 generator tests, the complete Haxe namespace suite, Eval/JavaScript/Python/Neko/C++ `CoreSmoke`, and focused `scene2d-gl` parity at 220/220 pass. Bridge-backed partial-mock hooks have a 30-second setup budget because a cold Vitest reset can spend more than 10 seconds transforming and evaluating the 13 MB compiled Haxe bundle; ordinary test timeouts remain unchanged. [`host-toolkit-boundary.md`](host-toolkit-boundary.md) records the ownership and fail-closed key model.
- The prior upstream `develop` refresh pinned `cad72aa3` and refreshed all generator-owned Haxe, JavaScript bridges, and audit reports. The update exposed a general Haxe secondary-type shadowing gap when a canonical type module also owns runtime constants; self-shadowed signatures now qualify those secondary types even for mixed type/value modules, with a `layout.LayoutState` regression fixture. The generated tree was `05d2f623...` and the JavaScript bundle was `9a0edc90...`; all 120 generator tests, the complete Haxe namespace suite, Eval/JavaScript/Python/C++ portability, and focused `layout` parity at 28/28 passed. Generator-owned artifacts contain no timestamps; reports deliberately record the pinned upstream commit, while timestamped parity-run reports are separate test artifacts.
- Contract completion visibility is now derived from manifest export lanes rather than generated module names. All 142 declaration-free Haxe source modules corresponding to translated `./contract` barrels are omitted and recorded in `reports/core.json`, while their generated JavaScript source bridges remain. Exactly 1,213 canonical declarations reachable through a contract lane and no non-contract lane anywhere in the inventory receive `@:noCompletion`; globally shared declarations such as `Vector2.createVector2` remain visible. Two complete generations produced the same `689b8d9b...` Haxe tree, and the compiled JavaScript bundle remains byte-identical at `9a0edc90...`.
- The pre-0.3.0 six-package correctness sprint closes every known residual assertion through four general rules: top-level const aliases retain reference identity; ECMAScript `Math.fround` uses one portable binary32 round; normal async fallthrough fulfills with `undefined` without collapsing explicit `null`; and 736 generated direct WebGL constant reads preserve the JavaScript context's enum surface while native targets retain specification fallbacks and endpoint-owned integer coercion. The focused formerly-red files pass 232/232 across Canvas, DOM, GL, WGPU, and scene-resource paths, including MorphShape aliases on all four backends, three premultiplied uploads, three fround assertions, two immediate settlements, even-odd stencil, and all seven remaining render-gl state/target assertions. Release gating then exposed one sentinel regression: a synchronous loop after an earlier await emitted an early `return null` as a bare callback result, which `finishFlow` correctly interpreted as normal fallthrough. All four loop forms now remain in the async-flow trampoline whenever they contain a function return. The glTF plus resolve-immediately files pass 10/10 and the complete `scene3d-resources` package passes 112/112. The final candidate generated tree is `ab75f2e1...` and `flight.cjs` is `1de8965e...`; repository checks, 125/125 generator tests, the full Haxe namespace suite, and Eval/JavaScript/Python/C++ portability all pass.
- Nested function declarations now initialize at their containing scope entry instead of at their textual statement position. The general rule covers all 41 translated declarations across 13 modules while leaving named function expressions in source order. It closes the shared `undefined.apply` cause behind all 16 `effects-gl` remainders: the focused suite is now green at 266/266. The combined generated tree is `39c83681...` and the JavaScript bundle is `c04283ff...`; repository checks, 119/119 generator tests, full Haxe compilation, and Eval/JavaScript/Python/Neko/C++ `CoreSmoke` all pass. [`upstream-7333e825-nested-function-hoisting.md`](upstream-7333e825-nested-function-hoisting.md) records the rule, census, hashes, and evidence.
- Native indexed reads now preserve JavaScript's nullish out-of-bounds result before touching Array, String, or typed-array storage. All 6,249 statically lowered reads use checked receiver endpoints except eight emitter-owned async-flow `for...of` loads with an immediately dominating length guard; 595 dynamic and 45 optional reads share the runtime rule. Negative fixtures cover every static endpoint, both mixed receiver families, and dynamic Array/typed-array/String reads. The primary-only candidate left the generated Haxe tree and JavaScript bundle byte-identical at `60aee396...` and `92b23961...`; C++ now compiles, links, and passes the assertion that previously stopped CoreSmoke, while Neko passes via a target-only non-inline form that avoids its module loader's function-scope limit. [`upstream-7333e825-native-indexed-read-bounds.md`](upstream-7333e825-native-indexed-read-bounds.md) records the rule, census, fast-path proof, hashes, and evidence.
- Native Dynamic numeric arithmetic now crosses a non-inline typed runtime frame whenever generated Haxe would otherwise operate directly on a runtime-Dynamic field/index/call result. Subtraction, multiplication, and division are intrinsically numeric; addition requires checker-proven NumberLike operands, so typed local arithmetic and ambiguous/string `+` stay unchanged. The generated census is 1,013 additions, 787 subtractions, 1,708 multiplications, and 425 divisions across 458 modules, with both `RenderAppearance` fractional-alpha paths repaired. All 118 maintained tests, Haxe Eval, JavaScript, Python, Neko, and five focused upstream suites pass; C++ compiles and links, passes the new fractional-product smoke, and reaches the independently known later indexed-read assertion. [`upstream-7333e825-native-dynamic-arithmetic.md`](upstream-7333e825-native-dynamic-arithmetic.md) records the policy, census, hashes, and evidence.
- Upstream `7333e825` Class D restores three JavaScript data semantics mechanically: composable grouped `instanceof Error` expressions, TypeScript switch-break ownership with nested-loop and continue propagation, and raw synchronous thrown-value identity through `_Runtime.throwValue`. Focused parity is fully green for `log` (115), `scene3d-formats` (554), and `render-wgpu` (205), while stale-census `particleemitter` remains green (194). Two consecutive generated trees and the post-rebase tree match at `c6255d0a...`; the candidate and post-rebase `flight.cjs` match at `bd114c53...`. Check, all 116 generator tests, Haxe, Eval, JavaScript, and Python pass. C++ compiles and links the full corpus before reaching the pre-existing indexed-read runtime assertion `array out-of-bounds read was not nullish`. [`upstream-7333e825-class-d-data-semantics.md`](upstream-7333e825-class-d-data-semantics.md) records the policy and evidence.
- Upstream `7333e825` Class A preserves lexical dynamic receivers across async/arrow continuations and keeps internal flow outcomes immediate until the first real await. The final candidate is green for `clipboard` 56/56, `geolocation` 25/25, and `loader` 88/88 with no unhandled rejection; two complete generations produced generated-tree `e2d9e9ca...` and `flight.cjs` `49f94013...`. Check, all 117 generator tests, Haxe, Eval, JavaScript, and Python pass. [`upstream-7333e825-class-a-async-semantics.md`](upstream-7333e825-class-a-async-semantics.md) records the implementation and evidence.
- The final D/A/P/T tranche has no focused package remainder: SWF independently verifies the existing general parameter-property repair at 76/76, and inventory records 140 translated packages plus the explicitly excluded `tool-capture` Node/Playwright CLI in a 141-package canonical scope. The Haxelib archive now installs and runs from an isolated consumer by merging both publishable source trees into one artifact-local classpath. [`upstream-7333e825-final-parity.md`](upstream-7333e825-final-parity.md) records the exact matrix and the sole target residual, the pre-existing C++ indexed-read assertion.
- Upstream `7333e825` Class R restores one positional callback/rest ABI across TypeScript, generated Haxe, JavaScript hosts, and portable Haxe reflection. Anonymous and named rest declarations collect their suffix exactly once; value and property dispatch flatten explicit spreads exactly once, preserve property receivers, and adapt to target reflection only at the maintained runtime boundary. Final native review hardens the proof boundary: a call-site assertion to a variadic type cannot invent a rest declaration, while emitted anonymous Haxe-rest closures carry explicit provenance for ordinary native reflection. The corrected generated tree is `64b8d1db...`, `flight.cjs` is `7c0f4773...`, generator tests pass 117/117, Neko `CoreSmoke` passes all signal arities, and the sound example advances beyond the former `Emitter` `Invalid call`. Twelve of 14 primary suites pass completely; the original `host-tauri` event-count failure is cleared, while its two menu/tray remainders and the one `assets` remainder are classified as Class A async scheduling/settlement. That checkpoint left `effects-gl` at 250/266; the later general nested-function hoisting repair closes it at 266/266. [`upstream-7333e825-class-r-callback-rest-abi.md`](upstream-7333e825-class-r-callback-rest-abi.md) records the mechanical policy and evidence.
- Upstream `7333e825` Class V now derives runtime-value emission from the canonical TypeScript checker: a public object exists exactly when upstream JavaScript emit has a value binding, with explicit type-only, ambient, pure-type, and const-enum erasure enforced before aliases. All 30,935 lane records carry the decision (21,909 runtime and 9,026 erased); 167 dual type/value records preserve their separate value identity mechanically. Root/package/SDK bridges expose const objects and enums without Flight-name allowlists, numeric enums include TypeScript reverse mappings, string enums omit them, and merged namespace members retain source order. The generated tree changes from `f6a19f78...` to `ee6b12e7...` and `flight.cjs` from `8765ac80...` to `b1bca1b7...`. Ten of 12 primary V suites pass completely; the remaining six assertions are pre-existing exception/async seams. Mixed `effects` and `scene3d-gl` pass 364/364 and 407/407. Its later-classified 16 `effects-gl` failures are now closed by the general nested-function hoisting rule. [`upstream-7333e825-class-v-runtime-values.md`](upstream-7333e825-class-v-runtime-values.md) records the policy and evidence.
- Update repair work adds the WebGL `SRGB8_ALPHA8` endpoint, exact export-lane bridge resolution, source-level `export *` bridges, same-name public-class/private-value module separation, checker-backed `TypeError`/`RangeError` instance checks, and TypeScript constructor-parameter-property lowering. The latter restores all 76 focused SWF tests; focused ABC (7), compression (15), SDK (21), geometry (997), math (278), mesh (179), and types (96) also pass.
- The upstream `7333e825` Class C bridge campaign now routes exact contract-lane Vitest mocks to canonical compiled callable owners, follows unmocked relative imports, preserves partial-mock boundaries, and fails closed on unsupported mock shapes. It adds 77 package-export synchronizations across 25 source bridges while leaving the compiled Haxe JavaScript bundle byte-identical. Focused production callable, backend-registration, test-helper, direct-mock, transitive-mock, and partial-mock gates pass; `effects-wgpu` is fully green at 239 tests, while the remaining `effects-gl` and `scene3d-resources` failures are reclassified outside Class C. [`upstream-7333e825-class-c-bridge.md`](upstream-7333e825-class-c-bridge.md) records the hashes and evidence.
- The historical upstream `c61de179` U7 parity report now has a complete analysis-only first-blocker census: all 65 failed suites partition into 31 callable/contract bridge losses, 12 runtime value-object losses, 14 callback/rest ABI losses, three JavaScript data/structural semantic losses, three browser/backend or async seams, one constructor-parameter-property loss, and one excluded Playwright tooling dependency. [`upstream-c61de179-u7-vitest-burn-down.md`](upstream-c61de179-u7-vitest-burn-down.md) locks the exact report and failed-name hashes, package-level evidence, and a reproducible accounting check; it authorizes no fixes.
- The full canonical upstream sweep accounts for all 141 package suites: 85 pass and 56 report failures. It ran from `2026-08-04T16:53:34Z` to `2026-08-04T17:09:49Z`; 55 failed suites record an unexpected Vitest worker exit under 12-way contention, while the excluded `tool-capture` suite has the sole failure without a worker exit. The focused `scene2d-formats` rerun passes 284/284 despite its contended worker exiting early after six assertions. The committed report preserves the full accounting rather than substituting focused results.
- Reflective toolkit retention is enforced by `runtime-retention.test.ts`: every bare class returned by `_HostValueLut` must be declared `@:keep`. The closed current class set is `_Map`, `_Set`, `_WeakMap`, `_WeakSet`, `_TextDecoder`, and `_DataView`; this protects toolkit construction under full DCE without putting ambient values back into `_Runtime`.
- Method-specific checker-proven typed-array `set` lowering converts all 68 censused dynamic calls: 34 `Float32Array`, 12 `Uint8Array`, nine `Uint8ClampedArray`, seven `Uint32Array`, four `Uint16Array`, one `Int16Array`, and one discriminator-correlated `Uint16Array | Uint32Array`. The source-array argument remains unchanged, only the optional offset receives `Std.int`, and the generic typed-array `subarray` binding remains separate. Schema-v9 final-output counters record 68 proven and 68 emitted calls with zero generated dynamic `set` survivors. Independent mixed-width unions, incompatible unions, optional/spread calls, structural lookalikes, and locally shadowed typed-array names remain dynamic. The generated-tree SHA-256 is `f6a19f78036cb15b191127acd7736b82873fd9b2ce8f44baab9b5006ba0b4e27`; focused mesh (179), geometry (997), bitmap (372), and SWF (76) JavaScript parity tests pass.
- The hxcpp cast/exception census covers 2,464 maintained, generated, and Haxe-test files. It finds 194 `try` statements (192 braced and two single-statement), 184 braced bodies containing ordinary emitted cast syntax, and 52 identifier-to-`Array` casts across the complete corpus (27 maintained and 25 generated). Exactly one identifier-to-pointer-backed collection cast occurs inside a protected body: `_Runtime.iterable`'s `Dynamic`-to-`Array` copy probe, whose review-owned replacement uses typed collection dispatch. The separate `Type.createInstance(cast constructor, ...)` retry is Neko-only arity recovery and has no hxcpp path. The 140-package production TypeScript input contains 285 `try` statements and 53 type assertions in their protected bodies; its sole array-bearing assertion is haptics' `number | number[]`, which lowers to `Dynamic`, not a pointer cast. Try lowering is otherwise structural, so the generator creates no additional cast-and-catch type probe. [`quality.md`](quality.md#native-cast-and-exception-safety) records the rule: never use exception-driven type discrimination, and require a C++ crash smoke because Neko masks this class of defect.
- Generation and generation drift verification pass at the new revision. Haxe JavaScript compilation and the complete Haxe target suite pass. Portable Eval, JavaScript, and Python callback/rest smokes pass for zero, one, and three arguments; the later indexed-read campaign also passes the full C++/hxcpp `CoreSmoke`.

Typed receiver lowering uses one cached TypeScript `Program` for the pinned upstream tree. This preserves imported, aliased, and contextual receiver types during generation.

- Upstream-update U0 pins `c61de179af8a12c2fa3b9b7d5389ee302f577a0d` for analysis. Raw generation fails loudly at the first stale typed-struct path (`ColorTransform.ts`). The complete census in [`upstream-c61de179-u0-census.md`](upstream-c61de179-u0-census.md) records 139 packages, 2,338 source files, 1,266 tests, 11,740 public export records, 172/405 stale typed-struct identities, the new two-lane `./contract` import model, one in-scope `for...in` lowering gap, 26 missing GL endpoints, Canvas `isContextLost`, all 28 changed ported examples, and 13 new examples. No generator/runtime/generated-output or class/direct-struct fix is included in U0; all green counts below remain the last verified `5d24729f` baseline until U1 is agreed and implemented.

- U1 item 1 derives package export lanes from every upstream manifest and source barrel. The pinned tree has 291 lanes and 30,378 lane-specific export records: all 139 packages expose `.` and `./contract`, while SDK adds 13 curated subpaths. Root facades remain tied to the 11,740 root-lane records; exact subpath imports now resolve only through their declared lane, so `@flighthq/entity/contract.createEntity` reaches its canonical defining declaration and undeclared lanes fail loudly. SDK inclusion and its exact target lane are derived from all 15 SDK barrels. No generated Haxe, bridge, runtime, class, or direct-struct output changes in this analysis-only item.

- U1 item 2 replaces source-keyed typed-struct IDs with stable `<public package>:<declaration kind>#<exported name>` identities. Manifest-derived public lanes resolve 231 candidates exactly and re-resolve 146 mechanical moves to `@flighthq/types` without editing their configured paths; the old package/path remain checked provenance and collision disambiguators. The remaining 26 missing names and two interface-to-type changes fail together and remain unresolved for review. [`upstream-c61de179-u1-item2-typed-struct-identities.md`](upstream-c61de179-u1-item2-typed-struct-identities.md) records evidence and a proposed disposition for every row. Item 1's focused `-t analyzeUpstream` verification intentionally skipped the known campaign-red lowering audit; an explicit conditional skip decision is deferred to item 3. No generated Haxe, bridge, runtime, class, direct-struct, or committed report output changes in this identity-only item.

- U1 item 4 derives Canvas, DOM, WebGL, and WebGPU receiver use from checker identities and consolidates backend admission into one operation-sensitive endpoint contract. The current audit records 3,474 accesses across 373 usage endpoints, 378 maintained-backend operations, 11 deliberate `_Runtime` fallbacks, and zero coverage gaps. It closes the 26 WebGL additions and Canvas `isContextLost` supplied by review's runtime prerequisite and an existing Canvas `roundRect` feature-read gap. Closed WebGL computed-constant domains now come from checker string-literal unions instead of a manual blend table. [`upstream-c61de179-u1-item4-host-endpoints.md`](upstream-c61de179-u1-item4-host-endpoints.md) records the rules, census, failure modes, and the finding that the `TextureSource` split does not cross the GL upload boundary. Generated artifacts remain intentionally stale until ordered item 7.

- U1 item 5 adds general `for...in` lowering with separate typed-record and dynamic enumeration paths. Checker-proven string-indexed records emit through `DynamicObject.keys`; unproven receivers use `_Runtime.forInKeys`, whose JavaScript implementation preserves native enumeration order and inherited enumerable keys. Synchronous and async-flow loops are supported, and non-declaration or non-identifier initializers fail explicitly. The production `scene3d-wgpu` occurrence is checker-proven direct. [`upstream-c61de179-u1-item5-for-in.md`](upstream-c61de179-u1-item5-for-in.md) records the contract and focused coverage. Generated artifacts remain intentionally stale until ordered item 7.

- U1 item 6 removes the package-name exclusion from `port.config.ts` and derives the sole exclusion from a tooling bin lane, absence from every SDK barrel, production Node and Playwright imports, a Playwright dependency, and no different known host family. The inventory now emits the rule, evidence, and derived reason. Partial claims, a new host rationale, zero matches, or an additional full match fail closed. The derived set controls every core, lowering, host-endpoint, and typed-struct production scope; the current translated lowering audit covers 138 packages with zero diagnostics. [`upstream-c61de179-u1-item6-exclusion-predicate.md`](upstream-c61de179-u1-item6-exclusion-predicate.md) records the evidence and failure contract. Generated artifacts remain intentionally stale until ordered item 7.

- Checker-proven primitive lowering emits non-null Boolean truthiness, Boolean conditional/logical expressions, and numeric relations directly. The generated tree removes 7,882 `_Runtime.truthy`, 1,689 `_Runtime.select`, 1,223 `_Runtime.andValue`, 1,364 `_Runtime.orValue`, and 4,021 `_Runtime.compare` calls; nullable/mixed truthiness and nonnumeric comparisons remain dynamic and are covered by negative tests.
- Checker-proven indexed access now uses typed inline endpoints for `Array`, all nine supported typed-array families, the storage-compatible part of the exact mixed-union pool, and proven Array destructuring sources. Exact final-output accounting records 4,898 direct reads and 2,930 direct writes; 406 `_Runtime.getIndex` and 126 `_Runtime.setIndex` calls remain for width-sensitive mixed writes, optional access, regexp-result destructuring, and otherwise dynamic access. JavaScript keeps raw fractional keys, portable targets retain `Std.int` keys, setters return the original right-hand value, and compound updates evaluate receiver/key once before the right-hand side.
- Emitter-known synthetic Array lowering adds exactly 79 for-of iteration-binding reads and 27 packed high-arity-argument reads. Both classes use `_StaticIndex.readArray` and have separate deterministic report counters. Of the 59 exact mixed typed-array-union sites, all 19 `Array | Float32Array` operations and the 27 `Uint16Array | Uint32Array` reads now use shared endpoints. The 13 width-sensitive unsigned writes are explicitly audited and remain dynamic because the portable fallback cannot distinguish 16-bit from 32-bit write coercion. Eight wider tuple/Array/Float32Array union writes were outside that pool and remain parked, as do dictionary operations and individual presence-sensitive operations.
- Destructuring source receivers are retained and audited at final output. Exactly 240 proven `Array` reads now use `_StaticIndex.readArray`: 20 assignment, 209 declaration, and 11 parameter reads. The remaining 12 declaration reads are four specialized `RegExpExecArray`/`RegExpMatchArray` destructures and remain explicitly parked on `_Runtime.getIndex`. Schema-v8 reports preserve the proven/direct, source-shape, receiver, and parked-reason split; the generated-tree SHA-256 is `7fb39b6fb779838926fded9d2aa44ff31f2ca54f7778801660fa046c7ea9914f`.
- Review's Step 2 `camera2d` Neko script benchmark improved from about 15.2 ms to 8.8 ms per frame (42% lower script cost, roughly 75% higher throughput). Step 3 found camera2d flat and particles 21% faster by median (35.0 to 42.9 fps, five runs per side with non-overlapping samples), confirming the indexed lever is positive.
- Every resolved `CanvasRenderingContext2D` call crosses `Canvas2dBackend` and is checked against a closed generation-time method/field inventory. Contextually typed shape-command callbacks no longer escape to `_Runtime.callProperty`. The maintained browser and native Cairo implementations cover the current upstream surface, including curves, arcs, ellipse/round-rectangle paths, and stroke cap/join/miter state.
- Resolved `Map`, `Set`, `WeakMap`, and `WeakSet` operations emit direct calls through `_Map`, `_Set`, `_WeakMap`, and `_WeakSet`; 1,174 generated receiver sites use this path. JavaScript-sensitive methods remain non-inline so Haxe dispatches to native collection members, and `delete` is mapped at compile time instead of by `_Runtime`.
- The typed-struct design is approved through phases 1–2. The generator now analyzes a fixed seven-schema candidate list, emits deterministic `reports/typed-structs.json`/`.md`, and attaches canonical schema/field bindings to safe property expressions in the IR. Direct struct expression emission is not enabled.
- The cpp-only `Camera2D` class pilot passed all acceptance gates after integration: exact generated SHA/delta, JS bundle identity apart from Lime's build-meta counter, clean cpp compile, pixel-identical render, and a six-pair interleaved 600-frame llvmpipe benchmark with a 102.0 fps parent mean versus 108.2 fps candidate mean. Sequential blocks are rejected for future class benchmarks because machine drift produced a false initial regression. Gate 4 retains only provenance-closed `ParticleEmitterState`; `ParticleEmitterData` was reverted because the outer emitter typedefs erase `data` to dynamic `NodeData`, making its parent taint realizable before class-typed renderer reads. The retained tree SHA is `7d3a64de84e772382b9d85b7adbf02694f0f4e820ebf667f61d6f3bec15103c9`. No Gate-5 class is enabled; the 137 closed rows are the accepted candidate pool for separately reviewed slices.
- Gate 6 now has a pure-design constructor and absence policy in [`typed-struct-optional-policy.md`](typed-struct-optional-policy.md); it enables no schema. Optional fields use nullable storage and optional constructor parameters on cpp, required `T | undefined` fields retain required nullable parameters, and domain defaults remain at use sites. The design rejects a bulk 92-schema enablement until the audit propagates nominal provenance through containers and nested schemas: dynamic `GltfDocument` JSON ingress currently taints 18 otherwise-clean child candidates, parsed spritesheet/texture-atlas roots taint at least nine more, and a `BitmapFontGlyphData` callback literal proves the direct construction census misses some container contexts.
- The original six direct typed-struct candidates have expanded through the checker-derived universe. Presence-sensitive `in` discriminations are now operation-local escapes, so they no longer make their entire receiver schema ineligible; inherited `[EntityRuntimeKey]` members remain separately audited computed-symbol escapes.
- Typed-struct negative tests cover unknown and readonly named fields, computed access, incompatible unions, presence sensitivity, receiver-sensitive methods, aliases, `Readonly<T>`, and the distinction between optional fields and required `T | undefined`. Tranche-3 emission remains gated on review of the audit report diff.
- `npm run check`, the current 119 generator tests, full Eval namespace compilation, all 278 upstream math tests, all 996 upstream geometry tests, and the indexed endpoint smoke on Eval/JavaScript/Python pass. The later indexed-read campaign also passes the full C++/hxcpp `CoreSmoke`.
- The complete current 131-package parity runner reports 88 package suites passing and 43 exposing existing gaps. Indexed-heavy suites including `camera`, `geometry`, `mesh`, `particles`, `particleemitter`, `scene`, `scene-wgpu`, `spritesheet`, `texture`, and `tween` pass. Failures remain concentrated in missing runtime value objects (`BlendMode`, `PathCommand`, `ImageChannel`, and related enums), existing signal/rest forwarding, async/backend seams, and the excluded Playwright tool package; none of the inspected failures point to indexed storage semantics.

The initial full port was completed through Phase 9 for upstream revision `5d24729f7360475e28a105ae0caeeaa2e1328260`. A source-derived namespace realignment is now implemented in the generator but cannot complete regeneration against that pinned upstream revision until its 12 Haxe identity collisions are reorganized upstream.

All 28 packages under `upstream/examples/packages` now have matching Lime projects under `examples/`. Each port has a `project.xml` and `Main.hx`, uses the shared Lime host adapter, and replaces the upstream Vite/browser lifecycle with Lime callbacks. The adapter registers the standard 2D GL renderers and exposes a specialized render hook for the effects and 3D examples. Source/API coverage was checked locally, but the Lime projects remain compile-unverified because this workspace has neither the repository-local Haxe compiler nor a Lime/Haxelib installation.

The latest complete local `npm run ci` finished successfully on 2026-07-22. Its canonical parity portion ran from `2026-07-22T13:16:01Z` through `2026-07-22T13:29:25Z` and wrote the committed-format report at `reports/upstream-parity.json`.

## Stable Decisions

- Input: every Flight package under the read-only `upstream` submodule is inventoried; exclusions are derived fail-closed from package lanes, SDK exposure, and production host dependencies/imports, with rule evidence and reason emitted in the inventory rather than stored by package name.
- Output: generated Haxe implementations, never extern-only declarations or handwritten generated edits.
- API: unchanged globally searchable free-function names such as `createVector2` and `getVector2Length`.
- Namespace: free functions and values use package facades such as `flighthq.geometry.Geometry` and `flighthq.renderGl.RenderGl`; source-derived implementation namespaces are completion-hidden.
- SDK: `flighthq.sdk.Sdk` is a generated facade over the actual upstream SDK export graph, including renamed cross-package re-exports.
- Types: every exported canonical declaration owns its direct type-name module, such as `flighthq.types.Vector2Like`; defining-file placement survives only as provenance.
- Distribution: Haxelib name `flight`, release version `0.4.0`.
- Sources: maintained Haxe lives under `src/` and generated Haxe under `generated/`; the Haxelib artifact merges both into its single `src` classpath while repository commands pass both source trees explicitly.
- Internals: maintained runtime types live under `flighthq._internal` and use underscore-prefixed names such as `_Runtime` and `_Promise`.
- Hosts: optional maintained adapters live in the main source tree; `flighthq.hostLime.LimeApp.createLimeAppBackend(application)` returns a Lime-specific App backend without owning or registering the application and is conditional on Lime's `lime` define.
- Tools: generator code, semantic patches, and operational scripts share the `tools/` hierarchy.
- Toolchain: npm-managed dependencies, repository-local Haxe 4.3.7, pinned Lix library specifications, and no required global Haxe installation.
- Exceptions: typed semantic patches with source identity and normalized AST fingerprints; generated text is never patched.

## Verified Accounting

- Inventory: 153 packages, 2,754 production source files, 1,599 routed test files, 319 public lanes, and 36,488 export records; 151 packages translate and `tool-capture` plus `tool-registry` are the two source-derived tooling exclusions.
- Lowering: 15,531 of 15,531 candidate declarations lowered across 151 packages, with zero diagnostics and no placeholder bodies.
- Patches: one applied semantic patch; zero stale, unmatched, or conflicting patches.
- Upstream parity: all 1,419 routed upstream test files have generated compiled-Haxe JavaScript bridges. Focused `scene2d-gl` parity passes 220/220 across 28 files with the bridge hook budget described above. The latest complete 143-package parity sweep has not been rerun for this Haxe synchronization; the historical `f1a7a9a0` sweep and its contention outcomes remain summarized above.
- Focused generator tests: 132 passing across 12 files, including deterministic output, patch drift, executable Haxe, typed receivers, host ambient identity and target-independent direct member lowering, ambient/module LUT emission, fail-closed host-toolkit keys and manifest equality, indexed-lowering coverage, secondary-type self-shadowing, contract-lane visibility and omission accounting, top-level alias identity, binary32 `Math.fround`, async-loop return identity, direct WebGL context constants, and direct-versus-parked destructuring receiver audits.
- Maintained TypeScript coverage: 77.03% statements, 70.79% branches, 86.69% functions, and 78.23% lines. This is intentionally distinct from complete upstream accounting.
- Portability: `CoreSmoke` compiles and runs across Eval, JavaScript, Python, Neko, and C++/hxcpp. Its indexed-read matrix covers every static Array/typed-array endpoint, mixed receivers, and dynamic Array/typed-array/String out-of-bounds behavior.
- Non-HTML5 audit: the complete namespace type-checks and runs its smoke test on Eval, Python, Neko, and C++/hxcpp. The reflection-free WebGL2 target binding and remaining native Lime renderer gaps are recorded in [`non-html5.md`](non-html5.md).
- Native typed arrays: maintained Float32/Int16/UInt16/UInt8 wrappers use `#if js` browser storage, `#elseif lime` Lime-native storage, and the existing generic array fallback. The generator routes their constructors to those wrappers, runtime helpers preserve their semantics, and native GL dispatch unwraps uploads to the Lime view with the concrete argument types and boolean conversions required by Lime's native `WebGL2RenderContext`. The bitmap example compiles to Neko against pinned Lime 8.4.0; execution reaches Lime's native loader but cannot open a window because the Git-pinned Lime source has no prebuilt `lime.ndll`. Live Lime window validation was not part of the indexed-read campaign. The focused fake-Lime smoke, full portable matrix, generator drift check, and repository `npm run check` pass.
- Desktop shaders: Lime's desktop compatibility context is OpenGL rather than OpenGL ES, so the native backend converts final WebGL2 `#version 300 es` shader sources to GLSL `#version 330 core` and removes ES precision syntax. This addresses the platformer Neko failure at the initial bitmap shader; browser and OpenGL ES source is unchanged. A packaged Lime 8.3.2 `lime.ndll` was validated through startup, but live rendering remains unavailable on this host because SDL has no video device.
- Focused GL parity audit: `render-gl` currently has 242 passing and 23 failing tests on the checked-out upstream revision. Every failure is an undefined runtime `BlendMode` or `AdvancedBlendMode` object in the JavaScript bridge; the Haxe port currently emits only their string typedefs and internal value tables. This is separate from the typed-array change and is the next GL parity issue to fix before treating the focused renderer suite as green.
- Packaging: `build/package/flight-0.4.0.zip` installs into an isolated Haxelib repository and a clean JavaScript consumer passes using `import flight.Sdk.*`. Validation pins `HAXELIB_PATH` to that repository and removes any installed same-name artifact before installation, so a stale same-version archive cannot shadow the candidate.

## Host Prerequisites

- Node.js 22, npm, Git, `curl`, and `tar` support the repository setup on Linux x64.
- Python 3 is required by `test:portable:python`.
- `g++` or `clang++` is required by `test:portable:cpp`.
- Neko is required to run the Haxe-distributed `haxelib` executable during package-install validation.
- The checked-in setup fallback currently installs Haxe itself only on Linux x64. Other hosts must provide an equivalent Haxe 4.3.7 installation path or extend `tools/setup-haxe.mjs` deliberately.

## Command Surface

- `npm run setup` installs the checksum-pinned local compiler, downloads the exact libraries described by `haxe_libraries/*.hxml`, and builds the hxcpp command runner when needed.
- `npm run generate` and `npm run generate:check` own all generated source, bridges, and reports.
- `npm run check` covers generation drift, TypeScript typechecking, lint, formatting, and API accounting.
- `npm run test` covers maintained unit tests, full Eval compilation, the four-target portability matrix, and all upstream package suites.
- `npm run package` assembles, installs, and consumes the Haxelib zip.
- `npm run ci` is the complete hosted and local gate.

## Remaining Release Decisions

The release policy is established: commit the Haxelib version and release note, verify the package from the final tree, then push a matching `0.4.0` or `v0.4.0` tag so the release workflow can publish with repository credentials. Generated API-documentation presentation remains a later release-management decision rather than a port gap.

## API Realignment In Progress

- The generator now builds source-derived modules, nested output paths, package/SDK facades, canonical imports, hidden implementation modules for `internal.ts` and test helpers, and nested JavaScript bridge paths. Raw generated Haxe indentation is canonical and covered by focused emitter tests.
- Generation now reports all Haxe package-level identity collisions together. The pinned upstream revision currently has 12 collisions requiring source reorganization: eight `host-electron` interface/module pairs, two `host-tauri` pairs, `scene-gl`'s `GlMeshUpload`, and `scene-wgpu`'s `WgpuMeshUpload`.
- Do not add Haxe-only renames for these collisions. Refactor the defining TypeScript files upstream, update the submodule revision, regenerate, and then complete the Haxe/parity/portability verification matrix.

For an upstream update, run setup, regenerate, review manifest and patch drift, then run `npm run ci`. Update this file whenever the upstream revision, support matrix, release policy, or verified counts change.
