// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Geometry.copyMatrix;
import flight.Geometry.createMatrix;
import flight.NodeApi.ensureNodeWorldMatrix;
import flight.NodeApi.getNodeChildAt;
import flight.NodeApi.getNodeChildCount;
import flight.NodeApi.getNodeWorldMatrix;
import flight.Types.Matrix;
import flight.Types.Transform2DNode;
import flight.Types.Velocity2D;
import flight.Types.VelocityField;
import flight.Types.VelocitySample;

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.Velocity")
class Velocity {
  @:keep public static function addVelocity(out:Velocity2D, a:Velocity2D, b:Velocity2D):Velocity2D {
    var ax:Dynamic = cast FlightRuntime.UNDEFINED;
    var ay:Dynamic = cast FlightRuntime.UNDEFINED;
    ax = FlightRuntime.field(a, 'x');
    ay = FlightRuntime.field(a, 'y');
    FlightRuntime.setField(out, 'x', (ax + FlightRuntime.field(b, 'x')));
    FlightRuntime.setField(out, 'y', (ay + FlightRuntime.field(b, 'y')));
    return cast out;
    return cast null;
  }

  @:keep public static function beginVelocityFrame(field:VelocityField):Void {
    FlightRuntime.incrementField(field, 'frameId', 1, true);
  }

  @:keep public static function clampVelocity(out:Velocity2D, velocity:Velocity2D, maxLength:Float):Velocity2D {
    var vx:Dynamic = cast FlightRuntime.UNDEFINED;
    var vy:Dynamic = cast FlightRuntime.UNDEFINED;
    var lenSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxSq:Dynamic = cast FlightRuntime.UNDEFINED;
    vx = FlightRuntime.field(velocity, 'x');
    vy = FlightRuntime.field(velocity, 'y');
    lenSq = ((vx * vx) + (vy * vy));
    maxSq = (maxLength * maxLength);
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(lenSq, maxSq, '>'), function():Dynamic return cast FlightRuntime.compare(lenSq, 0.0, '>')))) {
  var scale:Dynamic = (maxLength / FlightRuntime.callProperty(HxMath, 'sqrt', cast ([lenSq] : Array<Dynamic>)));
  FlightRuntime.setField(out, 'x', (vx * scale));
  FlightRuntime.setField(out, 'y', (vy * scale));
} else {
  FlightRuntime.setField(out, 'x', vx);
  FlightRuntime.setField(out, 'y', vy);
}
    return cast out;
    return cast null;
  }

  @:keep public static function contributeTransformVelocity<Traits>(field:VelocityField, root:Transform2DNode<Traits>):Void {
    FlightRuntime.callValue(Velocity.visitTransformVelocity__transformVelocity, cast ([field, root] : Array<Dynamic>));
  }

  @:keep public static function contributeVelocity(field:VelocityField, source:Dynamic, x:Float, y:Float):Void {
    var sample:Dynamic = cast FlightRuntime.UNDEFINED;
    sample = FlightRuntime.callValue(ensureVelocitySample, cast ([field, source] : Array<Dynamic>));
    FlightRuntime.setField(FlightRuntime.field(sample, 'velocity'), 'x', x);
    FlightRuntime.setField(FlightRuntime.field(sample, 'velocity'), 'y', y);
    FlightRuntime.setField(sample, 'lastFrameId', FlightRuntime.field(field, 'frameId'));
    FlightRuntime.setField(sample, 'explicitFrameId', FlightRuntime.field(field, 'frameId'));
  }

  @:keep public static function copyVelocity(out:Velocity2D, source:Velocity2D):Velocity2D {
    var sx:Dynamic = cast FlightRuntime.UNDEFINED;
    var sy:Dynamic = cast FlightRuntime.UNDEFINED;
    sx = FlightRuntime.field(source, 'x');
    sy = FlightRuntime.field(source, 'y');
    FlightRuntime.setField(out, 'x', sx);
    FlightRuntime.setField(out, 'y', sy);
    return cast out;
    return cast null;
  }

  @:keep public static function createVelocityField():VelocityField {
    return cast { samples: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []), frameId: 0.0 };
    return cast null;
  }

  @:keep public static function dampVelocity(out:Velocity2D, current:Velocity2D, previous:Velocity2D, factor:Float):Velocity2D {
    var cx:Dynamic = cast FlightRuntime.UNDEFINED;
    var cy:Dynamic = cast FlightRuntime.UNDEFINED;
    var px:Dynamic = cast FlightRuntime.UNDEFINED;
    var py:Dynamic = cast FlightRuntime.UNDEFINED;
    cx = FlightRuntime.field(current, 'x');
    cy = FlightRuntime.field(current, 'y');
    px = FlightRuntime.field(previous, 'x');
    py = FlightRuntime.field(previous, 'y');
    FlightRuntime.setField(out, 'x', ((cx * factor) + (px * (1.0 - factor))));
    FlightRuntime.setField(out, 'y', ((cy * factor) + (py * (1.0 - factor))));
    return cast out;
    return cast null;
  }

  @:keep public static function ensureVelocitySample(field:VelocityField, source:Dynamic):VelocitySample {
    var sample:Dynamic = cast FlightRuntime.UNDEFINED;
    sample = FlightRuntime.callProperty(FlightRuntime.field(field, 'samples'), 'get', cast ([source] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sample, FlightRuntime.UNDEFINED))) {
  (sample = cast ({ previousWorldTransform: null, velocity: { x: 0.0, y: 0.0 }, lastFrameId: -1.0, explicitFrameId: -1.0 } : Dynamic));
  FlightRuntime.callProperty(FlightRuntime.field(field, 'samples'), 'set', cast ([source, sample] : Array<Dynamic>));
}
    return cast sample;
    return cast null;
  }

  @:keep public static function getVelocity(field:VelocityField, source:Dynamic, out:Velocity2D):Velocity2D {
    var sample:Dynamic = cast FlightRuntime.UNDEFINED;
    sample = FlightRuntime.callProperty(FlightRuntime.field(field, 'samples'), 'get', cast ([source] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(sample, FlightRuntime.UNDEFINED), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(sample, 'lastFrameId'), FlightRuntime.field(field, 'frameId'))))) {
  FlightRuntime.setField(out, 'x', 0.0);
  FlightRuntime.setField(out, 'y', 0.0);
  return cast out;
}
    FlightRuntime.setField(out, 'x', FlightRuntime.field(FlightRuntime.field(sample, 'velocity'), 'x'));
    FlightRuntime.setField(out, 'y', FlightRuntime.field(FlightRuntime.field(sample, 'velocity'), 'y'));
    return cast out;
    return cast null;
  }

  @:keep public static function getVelocitySampleAt(sample:VelocitySample, currentWorldTransform:Matrix, pointX:Float, pointY:Float, out:Velocity2D):Velocity2D {
    var cx:Dynamic = cast FlightRuntime.UNDEFINED;
    var cy:Dynamic = cast FlightRuntime.UNDEFINED;
    var px:Dynamic = cast FlightRuntime.UNDEFINED;
    var py:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(sample, 'previousWorldTransform'), null))) {
  FlightRuntime.setField(out, 'x', 0.0);
  FlightRuntime.setField(out, 'y', 0.0);
  return cast out;
}
    cx = (((FlightRuntime.field(currentWorldTransform, 'a') * pointX) + (FlightRuntime.field(currentWorldTransform, 'c') * pointY)) + FlightRuntime.field(currentWorldTransform, 'tx'));
    cy = (((FlightRuntime.field(currentWorldTransform, 'b') * pointX) + (FlightRuntime.field(currentWorldTransform, 'd') * pointY)) + FlightRuntime.field(currentWorldTransform, 'ty'));
    px = (((FlightRuntime.field(FlightRuntime.field(sample, 'previousWorldTransform'), 'a') * pointX) + (FlightRuntime.field(FlightRuntime.field(sample, 'previousWorldTransform'), 'c') * pointY)) + FlightRuntime.field(FlightRuntime.field(sample, 'previousWorldTransform'), 'tx'));
    py = (((FlightRuntime.field(FlightRuntime.field(sample, 'previousWorldTransform'), 'b') * pointX) + (FlightRuntime.field(FlightRuntime.field(sample, 'previousWorldTransform'), 'd') * pointY)) + FlightRuntime.field(FlightRuntime.field(sample, 'previousWorldTransform'), 'ty'));
    FlightRuntime.setField(out, 'x', (cx - px));
    FlightRuntime.setField(out, 'y', (cy - py));
    return cast out;
    return cast null;
  }

  @:keep public static function hasVelocity(field:VelocityField, source:Dynamic):Bool {
    var sample:Dynamic = cast FlightRuntime.UNDEFINED;
    sample = FlightRuntime.callProperty(FlightRuntime.field(field, 'samples'), 'get', cast ([source] : Array<Dynamic>));
    return cast FlightRuntime.andValue(FlightRuntime.andValue(!FlightRuntime.strictEquals(sample, FlightRuntime.UNDEFINED), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(sample, 'lastFrameId'), FlightRuntime.field(field, 'frameId'))), function():Dynamic return cast FlightRuntime.orValue(!FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(sample, 'velocity'), 'x'), 0.0), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(sample, 'velocity'), 'y'), 0.0)));
    return cast null;
  }

  @:keep public static function isVelocityZero(velocity:Velocity2D, ?epsilon:Float):Bool {
    var e:Dynamic = cast FlightRuntime.UNDEFINED;
    e = FlightRuntime.coalesce(epsilon, function():Dynamic return cast 0.0);
    return cast FlightRuntime.andValue(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([FlightRuntime.field(velocity, 'x')] : Array<Dynamic>)), e, '<='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([FlightRuntime.field(velocity, 'y')] : Array<Dynamic>)), e, '<='));
    return cast null;
  }

  @:keep public static function lengthOfVelocity(velocity:Velocity2D):Float {
    return cast FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((FlightRuntime.field(velocity, 'x') * FlightRuntime.field(velocity, 'x')) + (FlightRuntime.field(velocity, 'y') * FlightRuntime.field(velocity, 'y')))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function lerpVelocity(out:Velocity2D, a:Velocity2D, b:Velocity2D, t:Float):Velocity2D {
    var ax:Dynamic = cast FlightRuntime.UNDEFINED;
    var ay:Dynamic = cast FlightRuntime.UNDEFINED;
    ax = FlightRuntime.field(a, 'x');
    ay = FlightRuntime.field(a, 'y');
    FlightRuntime.setField(out, 'x', (ax + ((FlightRuntime.field(b, 'x') - ax) * t)));
    FlightRuntime.setField(out, 'y', (ay + ((FlightRuntime.field(b, 'y') - ay) * t)));
    return cast out;
    return cast null;
  }

  @:keep public static function normalizeVelocity(out:Velocity2D, source:Velocity2D):Velocity2D {
    var sx:Dynamic = cast FlightRuntime.UNDEFINED;
    var sy:Dynamic = cast FlightRuntime.UNDEFINED;
    var len:Dynamic = cast FlightRuntime.UNDEFINED;
    sx = FlightRuntime.field(source, 'x');
    sy = FlightRuntime.field(source, 'y');
    len = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((sx * sx) + (sy * sy))] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(len, 0.0, '>'))) {
  var inv:Dynamic = (1.0 / len);
  FlightRuntime.setField(out, 'x', (sx * inv));
  FlightRuntime.setField(out, 'y', (sy * inv));
} else {
  FlightRuntime.setField(out, 'x', 0.0);
  FlightRuntime.setField(out, 'y', 0.0);
}
    return cast out;
    return cast null;
  }

  @:keep public static function scaleVelocity(out:Velocity2D, velocity:Velocity2D, scale:Float):Velocity2D {
    var vx:Dynamic = cast FlightRuntime.UNDEFINED;
    var vy:Dynamic = cast FlightRuntime.UNDEFINED;
    vx = FlightRuntime.field(velocity, 'x');
    vy = FlightRuntime.field(velocity, 'y');
    FlightRuntime.setField(out, 'x', (vx * scale));
    FlightRuntime.setField(out, 'y', (vy * scale));
    return cast out;
    return cast null;
  }

  @:keep public static function subtractVelocity(out:Velocity2D, a:Velocity2D, b:Velocity2D):Velocity2D {
    var ax:Dynamic = cast FlightRuntime.UNDEFINED;
    var ay:Dynamic = cast FlightRuntime.UNDEFINED;
    ax = FlightRuntime.field(a, 'x');
    ay = FlightRuntime.field(a, 'y');
    FlightRuntime.setField(out, 'x', (ax - FlightRuntime.field(b, 'x')));
    FlightRuntime.setField(out, 'y', (ay - FlightRuntime.field(b, 'y')));
    return cast out;
    return cast null;
  }

  @:keep public static function suppressVelocity(field:VelocityField, source:Dynamic):Void {
    FlightRuntime.callValue(contributeVelocity, cast ([field, source, 0.0, 0.0] : Array<Dynamic>));
  }

  @:keep public static function visitTransformVelocity__transformVelocity<Traits>(field:VelocityField, node:Transform2DNode<Traits>):Void {
    var mutableNode:Dynamic = cast FlightRuntime.UNDEFINED;
    var world:Dynamic = cast FlightRuntime.UNDEFINED;
    var sample:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    mutableNode = (cast node : Transform2DNode<Traits>);
    FlightRuntime.callValue(ensureNodeWorldMatrix, cast ([mutableNode] : Array<Dynamic>));
    world = FlightRuntime.callValue(getNodeWorldMatrix, cast ([mutableNode] : Array<Dynamic>));
    sample = FlightRuntime.callValue(ensureVelocitySample, cast ([field, node] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(sample, 'explicitFrameId'), FlightRuntime.field(field, 'frameId')))) {
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(sample, 'previousWorldTransform'), null))) {
  FlightRuntime.setField(FlightRuntime.field(sample, 'velocity'), 'x', (FlightRuntime.field(world, 'tx') - FlightRuntime.field(FlightRuntime.field(sample, 'previousWorldTransform'), 'tx')));
  FlightRuntime.setField(FlightRuntime.field(sample, 'velocity'), 'y', (FlightRuntime.field(world, 'ty') - FlightRuntime.field(FlightRuntime.field(sample, 'previousWorldTransform'), 'ty')));
} else {
  FlightRuntime.setField(FlightRuntime.field(sample, 'velocity'), 'x', 0.0);
  FlightRuntime.setField(FlightRuntime.field(sample, 'velocity'), 'y', 0.0);
}
  FlightRuntime.setField(sample, 'lastFrameId', FlightRuntime.field(field, 'frameId'));
}
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(sample, 'previousWorldTransform'), null))) { FlightRuntime.setField(sample, 'previousWorldTransform', FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>))); }
    FlightRuntime.callValue(copyMatrix, cast ([FlightRuntime.field(sample, 'previousWorldTransform'), world] : Array<Dynamic>));
    count = FlightRuntime.callValue(getNodeChildCount, cast ([mutableNode] : Array<Dynamic>));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, count, '<'))) {
        var child:Dynamic = FlightRuntime.callValue(getNodeChildAt, cast ([mutableNode, i] : Array<Dynamic>));
        if (FlightRuntime.truthy(!FlightRuntime.strictEquals(child, null))) { FlightRuntime.callValue(Velocity.visitTransformVelocity__transformVelocity, cast ([field, (cast (cast child : Dynamic) : Transform2DNode<Traits>)] : Array<Dynamic>)); }
        i++;
      }
    }
  }

  @:keep public static function zeroVelocity(out:Velocity2D):Velocity2D {
    FlightRuntime.setField(out, 'x', 0.0);
    FlightRuntime.setField(out, 'y', 0.0);
    return cast out;
    return cast null;
  }
}
