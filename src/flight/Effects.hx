// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Types.AdvancedBlendMode;
import flight.Types.AgxToneMapOptions;
import flight.Types.AutoExposureEffect;
import flight.Types.BarrelDistortionEffect;
import flight.Types.BevelEffect;
import flight.Types.BlendEffect;
import flight.Types.BloomEffect;
import flight.Types.BlurEffect;
import flight.Types.BokehDepthOfFieldEffect;
import flight.Types.CameraMotionBlurEffect;
import flight.Types.ChromaticAberrationEffect;
import flight.Types.CompositeEffect;
import flight.Types.CompositeOperator;
import flight.Types.ContactShadowsEffect;
import flight.Types.ConvolutionEffect;
import flight.Types.CrtEffect;
import flight.Types.CustomShaderEffect;
import flight.Types.DirectionalBlurEffect;
import flight.Types.DisplacementEffect;
import flight.Types.DitherEffect;
import flight.Types.DropShadowEffect;
import flight.Types.FilmEmulationEffect;
import flight.Types.FilmGrainEffect;
import flight.Types.FilmicToneMapOptions;
import flight.Types.FxaaEffect;
import flight.Types.GlitchEffect;
import flight.Types.GodRaysEffect;
import flight.Types.GradientBevelEffect;
import flight.Types.GradientGlowEffect;
import flight.Types.HalftoneEffect;
import flight.Types.InnerGlowEffect;
import flight.Types.InnerShadowEffect;
import flight.Types.KuwaharaEffect;
import flight.Types.LensDirtEffect;
import flight.Types.LensDistortionEffect;
import flight.Types.LensFlareEffect;
import flight.Types.MedianEffect;
import flight.Types.MotionBlurEffect;
import flight.Types.OuterGlowEffect;
import flight.Types.OutlineEffect;
import flight.Types.PanniniProjectionEffect;
import flight.Types.PixelateEffect;
import flight.Types.PosterizeEffect;
import flight.Types.RadialBlurEffect;
import flight.Types.RenderEffect;
import flight.Types.RenderEffectInput;
import flight.Types.ScanlinesEffect;
import flight.Types.ScreenSpaceFogEffect;
import flight.Types.SharpenEffect;
import flight.Types.SketchEffect;
import flight.Types.SmaaEffect;
import flight.Types.SsaoEffect;
import flight.Types.SsrEffect;
import flight.Types.TaaEffect;
import flight.Types.TiltShiftEffect;
import flight.Types.ToneMapEffect;
import flight.Types.VignetteEffect;
import flight.Types.VolumetricLightEffect;
import flight.Types.WhiteBalanceEffect;

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.Effects")
class Effects {
  @:keep public static function agxDefaultContrastApprox__toneMapMath(x:Float):Float {
    var x2:Dynamic = cast FlightRuntime.UNDEFINED;
    var x4:Dynamic = cast FlightRuntime.UNDEFINED;
    x2 = (x * x);
    x4 = (x2 * x2);
    return cast ((((((((15.5 * x4) * x2) - ((40.14 * x4) * x)) + (31.96 * x4)) - ((6.868 * x2) * x)) + (0.4298 * x2)) + (0.1191 * x)) - 0.00232);
    return cast null;
  }

  @:keep public static function blendLuminosity__blendModeMath(r:Float, g:Float, b:Float):Float {
    return cast (((0.3 * r) + (0.59 * g)) + (0.11 * b));
    return cast null;
  }

  @:keep public static function blendNonSeparableRgb(mode:AdvancedBlendMode, cbR:Float, cbG:Float, cbB:Float, csR:Float, csG:Float, csB:Float, out:Dynamic):Void {
    var r:Float = cast FlightRuntime.UNDEFINED;
    var g:Float = cast FlightRuntime.UNDEFINED;
    var b:Float = cast FlightRuntime.UNDEFINED;
    {
      var __switchValue = mode;
      if (__switchValue == Types.AdvancedBlendModeValue.Hue) {
        ({ var __destructure0:Dynamic = FlightRuntime.callValue(Effects.setBlendSaturation__blendModeMath, cast ([csR, csG, csB, FlightRuntime.callValue(Effects.blendSaturation__blendModeMath, cast ([cbR, cbG, cbB] : Array<Dynamic>))] : Array<Dynamic>)); r = cast FlightRuntime.getIndex(__destructure0, 0); g = cast FlightRuntime.getIndex(__destructure0, 1); b = cast FlightRuntime.getIndex(__destructure0, 2); __destructure0; });
        ({ var __destructure1:Dynamic = FlightRuntime.callValue(Effects.setBlendLuminosity__blendModeMath, cast ([r, g, b, FlightRuntime.callValue(Effects.blendLuminosity__blendModeMath, cast ([cbR, cbG, cbB] : Array<Dynamic>))] : Array<Dynamic>)); r = cast FlightRuntime.getIndex(__destructure1, 0); g = cast FlightRuntime.getIndex(__destructure1, 1); b = cast FlightRuntime.getIndex(__destructure1, 2); __destructure1; });
      }
      else if (__switchValue == Types.AdvancedBlendModeValue.Saturation) {
        ({ var __destructure2:Dynamic = FlightRuntime.callValue(Effects.setBlendSaturation__blendModeMath, cast ([cbR, cbG, cbB, FlightRuntime.callValue(Effects.blendSaturation__blendModeMath, cast ([csR, csG, csB] : Array<Dynamic>))] : Array<Dynamic>)); r = cast FlightRuntime.getIndex(__destructure2, 0); g = cast FlightRuntime.getIndex(__destructure2, 1); b = cast FlightRuntime.getIndex(__destructure2, 2); __destructure2; });
        ({ var __destructure3:Dynamic = FlightRuntime.callValue(Effects.setBlendLuminosity__blendModeMath, cast ([r, g, b, FlightRuntime.callValue(Effects.blendLuminosity__blendModeMath, cast ([cbR, cbG, cbB] : Array<Dynamic>))] : Array<Dynamic>)); r = cast FlightRuntime.getIndex(__destructure3, 0); g = cast FlightRuntime.getIndex(__destructure3, 1); b = cast FlightRuntime.getIndex(__destructure3, 2); __destructure3; });
      }
      else if (__switchValue == Types.AdvancedBlendModeValue.Color) {
        ({ var __destructure4:Dynamic = FlightRuntime.callValue(Effects.setBlendLuminosity__blendModeMath, cast ([csR, csG, csB, FlightRuntime.callValue(Effects.blendLuminosity__blendModeMath, cast ([cbR, cbG, cbB] : Array<Dynamic>))] : Array<Dynamic>)); r = cast FlightRuntime.getIndex(__destructure4, 0); g = cast FlightRuntime.getIndex(__destructure4, 1); b = cast FlightRuntime.getIndex(__destructure4, 2); __destructure4; });
      }
      else if (__switchValue == Types.AdvancedBlendModeValue.Luminosity) {
        ({ var __destructure5:Dynamic = FlightRuntime.callValue(Effects.setBlendLuminosity__blendModeMath, cast ([cbR, cbG, cbB, FlightRuntime.callValue(Effects.blendLuminosity__blendModeMath, cast ([csR, csG, csB] : Array<Dynamic>))] : Array<Dynamic>)); r = cast FlightRuntime.getIndex(__destructure5, 0); g = cast FlightRuntime.getIndex(__destructure5, 1); b = cast FlightRuntime.getIndex(__destructure5, 2); __destructure5; });
      }
      else  {
        (r = cast (csR : Dynamic));
        (g = cast (csG : Dynamic));
        (b = cast (csB : Dynamic));
      }
    }
    FlightRuntime.setIndex(out, 0.0, r);
    FlightRuntime.setIndex(out, 1.0, g);
    FlightRuntime.setIndex(out, 2.0, b);
  }

  @:keep public static function blendSaturation__blendModeMath(r:Float, g:Float, b:Float):Float {
    return cast (HxMath.max(HxMath.max(r, g), b) - HxMath.min(HxMath.min(r, g), b));
    return cast null;
  }

  public static final BLUR_DOWNSAMPLE_MAX_SIGMA__blurDownsample:Dynamic = 4.0;

  @:keep public static function canLerpRenderEffects(a:RenderEffect, b:RenderEffect):Bool {
    return cast FlightRuntime.strictEquals(FlightRuntime.field(a, 'kind'), FlightRuntime.field(b, 'kind'));
    return cast null;
  }

  @:keep public static function clipBlendColor__blendModeMath(r:Float, g:Float, b:Float):Array<Float> {
    var l:Dynamic = cast FlightRuntime.UNDEFINED;
    var min:Dynamic = cast FlightRuntime.UNDEFINED;
    var max:Dynamic = cast FlightRuntime.UNDEFINED;
    var cr:Dynamic = cast FlightRuntime.UNDEFINED;
    var cg:Dynamic = cast FlightRuntime.UNDEFINED;
    var cb:Dynamic = cast FlightRuntime.UNDEFINED;
    l = FlightRuntime.callValue(Effects.blendLuminosity__blendModeMath, cast ([r, g, b] : Array<Dynamic>));
    min = HxMath.min(HxMath.min(r, g), b);
    max = HxMath.max(HxMath.max(r, g), b);
    cr = r;
    cg = g;
    cb = b;
    if (FlightRuntime.truthy(FlightRuntime.compare(min, 0.0, '<'))) {
  var denom:Dynamic = (l - min);
  (cr = cast ((l + (((cr - l) * l) / denom)) : Dynamic));
  (cg = cast ((l + (((cg - l) * l) / denom)) : Dynamic));
  (cb = cast ((l + (((cb - l) * l) / denom)) : Dynamic));
}
    if (FlightRuntime.truthy(FlightRuntime.compare(max, 1.0, '>'))) {
  var denom:Dynamic = (max - l);
  (cr = cast ((l + (((cr - l) * (1.0 - l)) / denom)) : Dynamic));
  (cg = cast ((l + (((cg - l) * (1.0 - l)) / denom)) : Dynamic));
  (cb = cast ((l + (((cb - l) * (1.0 - l)) / denom)) : Dynamic));
}
    return cast cast ([cr, cg, cb] : Array<Dynamic>);
    return cast null;
  }

  @:keep public static function computeAcesToneMap(x:Float):Float {
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var c:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    var e:Dynamic = cast FlightRuntime.UNDEFINED;
    a = 2.51;
    b = 0.03;
    c = 2.43;
    d = 0.59;
    e = 0.14;
    return cast FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, ((x * ((a * x) + b)) / ((x * ((c * x) + d)) + e))] : Array<Dynamic>))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeAgxToneMap(x:Float, ?options:AgxToneMapOptions):Float {
    var min_ev:Dynamic = cast FlightRuntime.UNDEFINED;
    var max_ev:Dynamic = cast FlightRuntime.UNDEFINED;
    var val:Dynamic = cast FlightRuntime.UNDEFINED;
    var log:Dynamic = cast FlightRuntime.UNDEFINED;
    var normalized:Dynamic = cast FlightRuntime.UNDEFINED;
    min_ev = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'minEv'), function():Dynamic return cast -12.47393);
    max_ev = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'maxEv'), function():Dynamic return cast 4.026069);
    val = FlightRuntime.callProperty(HxMath, 'max', cast ([1e-10, x] : Array<Dynamic>));
    log = FlightRuntime.callProperty(HxMath, 'max', cast ([min_ev, FlightRuntime.callProperty(HxMath, 'min', cast ([max_ev, FlightRuntime.log2(val)] : Array<Dynamic>))] : Array<Dynamic>));
    normalized = ((log - min_ev) / (max_ev - min_ev));
    return cast FlightRuntime.callValue(Effects.agxDefaultContrastApprox__toneMapMath, cast ([normalized] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeBloomBlurRadius(effect:BloomEffect):Float {
    return cast FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.coalesce(FlightRuntime.field(effect, 'radius'), function():Dynamic return cast 8.0)] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeBloomIntensity(effect:BloomEffect):Float {
    return cast FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 1.0);
    return cast null;
  }

  @:keep public static function computeBloomThreshold(effect:BloomEffect):Float {
    return cast FlightRuntime.coalesce(FlightRuntime.field(effect, 'threshold'), function():Dynamic return cast 0.8);
    return cast null;
  }

  @:keep public static function computeBoxBlurLowerPassCount__boxBlurMath(sigma:Float, passes:Float, lowerWidth:Float):Float {
    return cast FlightRuntime.callProperty(HxMath, 'round', cast ([((((12.0 * sigma) * sigma) - (passes * (((lowerWidth * lowerWidth) + (4.0 * lowerWidth)) + 3.0))) / ((-4.0 * lowerWidth) - 4.0))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeBoxBlurLowerWidth__boxBlurMath(sigma:Float, passes:Float):Float {
    var width:Dynamic = cast FlightRuntime.UNDEFINED;
    width = FlightRuntime.callProperty(HxMath, 'floor', cast ([FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((((12.0 * sigma) * sigma) / passes) + 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals((width % 2.0), 0.0))) { (width = cast ((width - 1.0) : Dynamic)); }
    return cast width;
    return cast null;
  }

  @:keep public static function computeBoxBlurPassRadius(sigma:Float, passes:Float, pass:Float):Float {
    var lowerWidth:Dynamic = cast FlightRuntime.UNDEFINED;
    var lowerCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var width:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.compare(sigma, 0.0, '<='))) { return cast 0.0; }
    lowerWidth = FlightRuntime.callValue(Effects.computeBoxBlurLowerWidth__boxBlurMath, cast ([sigma, passes] : Array<Dynamic>));
    lowerCount = FlightRuntime.callValue(Effects.computeBoxBlurLowerPassCount__boxBlurMath, cast ([sigma, passes, lowerWidth] : Array<Dynamic>));
    width = FlightRuntime.select(FlightRuntime.compare(pass, lowerCount, '<'), function():Dynamic return cast lowerWidth, function():Dynamic return cast (lowerWidth + 2.0));
    return cast FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, ((width - 1.0) / 2.0)] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeBoxBlurRadius(sigma:Float, passes:Float):Float {
    if (FlightRuntime.truthy(FlightRuntime.compare(sigma, 0.0, '<='))) { return cast 0.0; }
    return cast FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'round', cast ([((-1.0 + FlightRuntime.callProperty(HxMath, 'sqrt', cast ([(1.0 + (((12.0 * sigma) * sigma) / passes))] : Array<Dynamic>))) / 2.0)] : Array<Dynamic>))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeColorTemperatureRgb(kelvin:Float, out:Array<Float>):Void {
    var temp:Dynamic = cast FlightRuntime.UNDEFINED;
    var r:Float = cast FlightRuntime.UNDEFINED;
    var g:Float = cast FlightRuntime.UNDEFINED;
    var b:Float = cast FlightRuntime.UNDEFINED;
    temp = (FlightRuntime.callProperty(HxMath, 'max', cast ([1000.0, FlightRuntime.callProperty(HxMath, 'min', cast ([40000.0, kelvin] : Array<Dynamic>))] : Array<Dynamic>)) / 100.0);
    if (FlightRuntime.truthy(FlightRuntime.compare(temp, 66.0, '<='))) {
  (r = cast (1.0 : Dynamic));
  (g = cast ((((99.4708025861 * FlightRuntime.callProperty(HxMath, 'log', cast ([temp] : Array<Dynamic>))) - 161.1195681661) / 255.0) : Dynamic));
  (b = cast (FlightRuntime.select(FlightRuntime.compare(temp, 19.0, '<='), function():Dynamic return cast 0.0, function():Dynamic return cast (((138.5177312231 * FlightRuntime.callProperty(HxMath, 'log', cast ([(temp - 10.0)] : Array<Dynamic>))) - 305.0447927307) / 255.0)) : Dynamic));
} else {
  (r = cast (((329.698727446 * FlightRuntime.callProperty(HxMath, 'pow', cast ([(temp - 60.0), -0.1332047592] : Array<Dynamic>))) / 255.0) : Dynamic));
  (g = cast (((288.1221695283 * FlightRuntime.callProperty(HxMath, 'pow', cast ([(temp - 60.0), -0.0755148492] : Array<Dynamic>))) / 255.0) : Dynamic));
  (b = cast (1.0 : Dynamic));
}
    FlightRuntime.setIndex(out, 0.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, r] : Array<Dynamic>))] : Array<Dynamic>)));
    FlightRuntime.setIndex(out, 1.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, g] : Array<Dynamic>))] : Array<Dynamic>)));
    FlightRuntime.setIndex(out, 2.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, b] : Array<Dynamic>))] : Array<Dynamic>)));
  }

  @:keep public static function computeCrtMaskParams(resolution:Float, curvature:Float, out:Array<Float>):Void {
    var maskScale:Dynamic = cast FlightRuntime.UNDEFINED;
    var curv:Dynamic = cast FlightRuntime.UNDEFINED;
    maskScale = (FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, resolution] : Array<Dynamic>)) / 360.0);
    curv = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, curvature] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.setIndex(out, 0.0, maskScale);
    FlightRuntime.setIndex(out, 1.0, (curv * 0.1));
  }

  @:keep public static function computeDepthOfFieldCoc(depth:Float, focusDistance:Float, aperture:Float, focalLength:Float):Float {
    var fd:Dynamic = cast FlightRuntime.UNDEFINED;
    var fl:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    fd = FlightRuntime.callProperty(HxMath, 'max', cast ([0.00001, focusDistance] : Array<Dynamic>));
    fl = (FlightRuntime.callProperty(HxMath, 'max', cast ([0.00001, focalLength] : Array<Dynamic>)) / 1000.0);
    d = FlightRuntime.callProperty(HxMath, 'max', cast ([0.00001, depth] : Array<Dynamic>));
    a = (fl / FlightRuntime.callProperty(HxMath, 'max', cast ([0.00001, aperture] : Array<Dynamic>)));
    return cast ((a * (d - fd)) / (d * (fd - fl)));
    return cast null;
  }

  @:keep public static function computeExposureScale(exposure:Float):Float {
    return cast FlightRuntime.callProperty(HxMath, 'pow', cast ([2.0, exposure] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeFilmicToneMap(x:Float, ?options:FilmicToneMapOptions):Float {
    var maxBrightness:Dynamic = cast FlightRuntime.UNDEFINED;
    var contrast:Dynamic = cast FlightRuntime.UNDEFINED;
    var linearStart:Dynamic = cast FlightRuntime.UNDEFINED;
    var linearLength:Dynamic = cast FlightRuntime.UNDEFINED;
    var blackTighten:Dynamic = cast FlightRuntime.UNDEFINED;
    var pedestal:Dynamic = cast FlightRuntime.UNDEFINED;
    var l0:Dynamic = cast FlightRuntime.UNDEFINED;
    var L0:Dynamic = cast FlightRuntime.UNDEFINED;
    var L1:Dynamic = cast FlightRuntime.UNDEFINED;
    var S0:Dynamic = cast FlightRuntime.UNDEFINED;
    var S1:Dynamic = cast FlightRuntime.UNDEFINED;
    var C2:Dynamic = cast FlightRuntime.UNDEFINED;
    var CP:Dynamic = cast FlightRuntime.UNDEFINED;
    var w0:Dynamic = cast FlightRuntime.UNDEFINED;
    var T:Dynamic = cast FlightRuntime.UNDEFINED;
    var L:Dynamic = cast FlightRuntime.UNDEFINED;
    var S:Dynamic = cast FlightRuntime.UNDEFINED;
    maxBrightness = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'maxBrightness'), function():Dynamic return cast 1.0);
    contrast = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'contrast'), function():Dynamic return cast 1.0);
    linearStart = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'linearStart'), function():Dynamic return cast 0.22);
    linearLength = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'linearLength'), function():Dynamic return cast 0.4);
    blackTighten = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'blackTighten'), function():Dynamic return cast 1.33);
    pedestal = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'pedestal'), function():Dynamic return cast 0.0);
    l0 = (((maxBrightness - linearStart) * linearLength) / contrast);
    L0 = (linearStart - (linearStart / contrast));
    L1 = (linearStart + ((1.0 - linearStart) / contrast));
    S0 = (linearStart + l0);
    S1 = (linearStart + (contrast * l0));
    C2 = (contrast / (maxBrightness - S1));
    CP = (-C2 / FlightRuntime.callProperty(HxMath, 'log', cast ([2.0] : Array<Dynamic>)));
    w0 = (1.0 - FlightRuntime.callValue(Effects.smoothstep01__toneMapMath, cast ([linearStart, S0, x] : Array<Dynamic>)));
    T = ((linearStart * FlightRuntime.callProperty(HxMath, 'pow', cast ([(x / linearStart), blackTighten] : Array<Dynamic>))) + pedestal);
    L = (linearStart + (contrast * (x - linearStart)));
    S = (maxBrightness - ((maxBrightness - S1) * FlightRuntime.callProperty(HxMath, 'exp', cast ([(CP * (x - S0))] : Array<Dynamic>))));
    return cast FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, ((((w0 * (1.0 - FlightRuntime.callValue(Effects.smoothstep01__toneMapMath, cast ([L0, L1, x] : Array<Dynamic>)))) * T) + (FlightRuntime.callValue(Effects.smoothstep01__toneMapMath, cast ([L0, L1, x] : Array<Dynamic>)) * L)) + ((1.0 - w0) * S))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeGaussianKernelWeights(sigma:Float, out:Array<Float>):Array<Float> {
    var size:Dynamic = cast FlightRuntime.UNDEFINED;
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    var twoSigmaSquared:Dynamic = cast FlightRuntime.UNDEFINED;
    var sum:Dynamic = cast FlightRuntime.UNDEFINED;
    var inverseSum:Dynamic = cast FlightRuntime.UNDEFINED;
    size = FlightRuntime.callValue(getGaussianKernelSize, cast ([sigma] : Array<Dynamic>));
    FlightRuntime.setLength(out, size);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(size, 1.0))) {
  FlightRuntime.setIndex(out, 0.0, 1.0);
  return cast out;
}
    radius = ((size - 1.0) / 2.0);
    twoSigmaSquared = ((2.0 * sigma) * sigma);
    sum = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, size, '<'))) {
        var x:Dynamic = (i - radius);
        var weight:Dynamic = FlightRuntime.callProperty(HxMath, 'exp', cast ([(-(x * x) / twoSigmaSquared)] : Array<Dynamic>));
        FlightRuntime.setIndex(out, i, weight);
        (sum = cast ((sum + weight) : Dynamic));
        i++;
      }
    }
    inverseSum = (1.0 / sum);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, size, '<'))) {
        FlightRuntime.setIndex(out, i, (FlightRuntime.getIndex(out, i) * inverseSum));
        i++;
      }
    }
    return cast out;
    return cast null;
  }

  @:keep public static function computeGaussianRadiusFromSigma(sigma:Float):Float {
    return cast FlightRuntime.callProperty(HxMath, 'ceil', cast ([(3.0 * FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, sigma] : Array<Dynamic>)))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeGaussianSigmaForBlurRadius(radius:Float, passes:Float):Float {
    var width:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(radius, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(passes, 0.0, '<=')))) { return cast 0.0; }
    width = ((2.0 * radius) + 1.0);
    return cast FlightRuntime.callProperty(HxMath, 'sqrt', cast ([(((passes * width) * width) / 12.0)] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeGaussianSigmaFromRadius(radius:Float):Float {
    return cast (FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, radius] : Array<Dynamic>)) / 3.0);
    return cast null;
  }

  @:keep public static function computeGodRaysAccumulationScale(effect:GodRaysEffect):Float {
    var samples:Dynamic = cast FlightRuntime.UNDEFINED;
    var weight:Dynamic = cast FlightRuntime.UNDEFINED;
    var exposure:Dynamic = cast FlightRuntime.UNDEFINED;
    samples = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.coalesce(FlightRuntime.field(effect, 'samples'), function():Dynamic return cast 100.0)] : Array<Dynamic>));
    weight = FlightRuntime.callProperty(HxMath, 'max', cast ([0.000001, FlightRuntime.coalesce(FlightRuntime.field(effect, 'weight'), function():Dynamic return cast 0.4)] : Array<Dynamic>));
    exposure = FlightRuntime.callProperty(HxMath, 'max', cast ([0.000001, FlightRuntime.coalesce(FlightRuntime.field(effect, 'exposure'), function():Dynamic return cast 0.1)] : Array<Dynamic>));
    return cast (1.0 / ((samples * weight) * exposure));
    return cast null;
  }

  @:keep public static function computeGodRaysLightCenter(effect:GodRaysEffect, out:Array<Float>):Void {
    FlightRuntime.setIndex(out, 0.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, FlightRuntime.coalesce(FlightRuntime.field(effect, 'centerX'), function():Dynamic return cast 0.5)] : Array<Dynamic>))] : Array<Dynamic>)));
    FlightRuntime.setIndex(out, 1.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, FlightRuntime.coalesce(FlightRuntime.field(effect, 'centerY'), function():Dynamic return cast 0.5)] : Array<Dynamic>))] : Array<Dynamic>)));
  }

  @:keep public static function computeGodRaysSampleWeight(effect:GodRaysEffect, sampleIndex:Float):Float {
    var decay:Dynamic = cast FlightRuntime.UNDEFINED;
    var weight:Dynamic = cast FlightRuntime.UNDEFINED;
    var exposure:Dynamic = cast FlightRuntime.UNDEFINED;
    decay = FlightRuntime.coalesce(FlightRuntime.field(effect, 'decay'), function():Dynamic return cast 0.96);
    weight = FlightRuntime.coalesce(FlightRuntime.field(effect, 'weight'), function():Dynamic return cast 0.4);
    exposure = FlightRuntime.coalesce(FlightRuntime.field(effect, 'exposure'), function():Dynamic return cast 0.1);
    return cast ((FlightRuntime.callProperty(HxMath, 'pow', cast ([decay, sampleIndex] : Array<Dynamic>)) * weight) * exposure);
    return cast null;
  }

  @:keep public static function computeGodRaysStepSize(effect:GodRaysEffect, px:Float, py:Float, out:Array<Float>):Void {
    var cx:Dynamic = cast FlightRuntime.UNDEFINED;
    var cy:Dynamic = cast FlightRuntime.UNDEFINED;
    var density:Dynamic = cast FlightRuntime.UNDEFINED;
    var samples:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    cx = FlightRuntime.coalesce(FlightRuntime.field(effect, 'centerX'), function():Dynamic return cast 0.5);
    cy = FlightRuntime.coalesce(FlightRuntime.field(effect, 'centerY'), function():Dynamic return cast 0.5);
    density = FlightRuntime.coalesce(FlightRuntime.field(effect, 'density'), function():Dynamic return cast 0.96);
    samples = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.coalesce(FlightRuntime.field(effect, 'samples'), function():Dynamic return cast 100.0)] : Array<Dynamic>));
    dx = (((cx - px) * density) / samples);
    dy = (((cy - py) * density) / samples);
    FlightRuntime.setIndex(out, 0.0, dx);
    FlightRuntime.setIndex(out, 1.0, dy);
  }

  @:keep public static function computeHalftoneCellParams(frequency:Float, angle:Float, out:Array<Float>):Void {
    var cellSize:Dynamic = cast FlightRuntime.UNDEFINED;
    cellSize = FlightRuntime.select(FlightRuntime.compare(frequency, 1e-10, '>'), function():Dynamic return cast (1.0 / frequency), function():Dynamic return cast 1.0);
    FlightRuntime.setIndex(out, 0.0, cellSize);
    FlightRuntime.setIndex(out, 1.0, FlightRuntime.callProperty(HxMath, 'cos', cast ([angle] : Array<Dynamic>)));
    FlightRuntime.setIndex(out, 2.0, FlightRuntime.callProperty(HxMath, 'sin', cast ([angle] : Array<Dynamic>)));
  }

  @:keep public static function computeKuwaharaGaussianWeights(radius:Float, out:flight.internal.FlightFloat32Array):Float {
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var size:Dynamic = cast FlightRuntime.UNDEFINED;
    var sigma:Dynamic = cast FlightRuntime.UNDEFINED;
    var twoSigmaSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var sum:Dynamic = cast FlightRuntime.UNDEFINED;
    var invSum:Dynamic = cast FlightRuntime.UNDEFINED;
    r = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'floor', cast ([radius] : Array<Dynamic>))] : Array<Dynamic>));
    size = (r + 1.0);
    sigma = (r / 2.0);
    twoSigmaSq = ((2.0 * sigma) * sigma);
    sum = 0.0;
    {
      var y:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(y, size, '<'))) {
        {
          var x:Dynamic = 0.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(x, size, '<'))) {
            var d:Dynamic = ((x * x) + (y * y));
            FlightRuntime.setIndex(out, ((y * size) + x), FlightRuntime.callProperty(HxMath, 'exp', cast ([(-d / twoSigmaSq)] : Array<Dynamic>)));
            (sum = cast ((sum + FlightRuntime.getIndex(out, ((y * size) + x))) : Dynamic));
            x++;
          }
        }
        y++;
      }
    }
    invSum = FlightRuntime.select(FlightRuntime.compare(sum, 1e-10, '>'), function():Dynamic return cast (1.0 / sum), function():Dynamic return cast 1.0);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, (size * size), '<'))) {
        FlightRuntime.setIndex(out, i, (FlightRuntime.getIndex(out, i) * invSum));
        i++;
      }
    }
    return cast (size * size);
    return cast null;
  }

  @:keep public static function computeKuwaharaSectorOffsets(radius:Float, out:Array<Float>):Void {
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var half:Dynamic = cast FlightRuntime.UNDEFINED;
    var v0:Dynamic = cast FlightRuntime.UNDEFINED;
    var v1:Dynamic = cast FlightRuntime.UNDEFINED;
    var v2:Dynamic = cast FlightRuntime.UNDEFINED;
    var v3:Dynamic = cast FlightRuntime.UNDEFINED;
    var v4:Dynamic = cast FlightRuntime.UNDEFINED;
    var v5:Dynamic = cast FlightRuntime.UNDEFINED;
    var v6:Dynamic = cast FlightRuntime.UNDEFINED;
    var v7:Dynamic = cast FlightRuntime.UNDEFINED;
    r = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'floor', cast ([radius] : Array<Dynamic>))] : Array<Dynamic>));
    half = r;
    v0 = -half;
    v1 = -half;
    v2 = 0.0;
    v3 = -half;
    v4 = -half;
    v5 = 0.0;
    v6 = 0.0;
    v7 = 0.0;
    FlightRuntime.setIndex(out, 0.0, v0);
    FlightRuntime.setIndex(out, 1.0, v1);
    FlightRuntime.setIndex(out, 2.0, v2);
    FlightRuntime.setIndex(out, 3.0, v3);
    FlightRuntime.setIndex(out, 4.0, v4);
    FlightRuntime.setIndex(out, 5.0, v5);
    FlightRuntime.setIndex(out, 6.0, v6);
    FlightRuntime.setIndex(out, 7.0, v7);
  }

  @:keep public static function computeKuwaharaSectorPixelCount(effect:KuwaharaEffect):Float {
    var s:Dynamic = cast FlightRuntime.UNDEFINED;
    s = FlightRuntime.callValue(computeKuwaharaSectorSize, cast ([effect] : Array<Dynamic>));
    return cast (s * s);
    return cast null;
  }

  @:keep public static function computeKuwaharaSectorSize(effect:KuwaharaEffect):Float {
    return cast (FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'floor', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'radius'), function():Dynamic return cast 3.0)] : Array<Dynamic>))] : Array<Dynamic>)) + 1.0);
    return cast null;
  }

  @:keep public static function computeLinearDepthFromNonlinear(depth:Float, near:Float, far:Float):Float {
    return cast ((near * far) / (far - (depth * (far - near))));
    return cast null;
  }

  @:keep public static function computeLinearSampledGaussian(sigma:Float, outWeights:Array<Float>, outOffsets:Array<Float>):Array<Float> {
    var size:Dynamic = cast FlightRuntime.UNDEFINED;
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    var tapCount:Dynamic = cast FlightRuntime.UNDEFINED;
    size = FlightRuntime.callValue(getGaussianKernelSize, cast ([sigma] : Array<Dynamic>));
    radius = ((size - 1.0) / 2.0);
    FlightRuntime.callValue(computeGaussianKernelWeights, cast ([sigma, Effects.scratchWeights__linearSampledGaussian] : Array<Dynamic>));
    tapCount = FlightRuntime.callValue(getLinearSampledGaussianTapCount, cast ([sigma] : Array<Dynamic>));
    FlightRuntime.setLength(outWeights, tapCount);
    FlightRuntime.setLength(outOffsets, tapCount);
    {
      var tap:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(tap, tapCount, '<'))) {
        var i:Dynamic = (tap * 2.0);
        var posA:Dynamic = (i - radius);
        if (FlightRuntime.truthy(FlightRuntime.compare((i + 1.0), size, '<'))) {
  var weightA:Dynamic = FlightRuntime.getIndex(Effects.scratchWeights__linearSampledGaussian, i);
  var weightB:Dynamic = FlightRuntime.getIndex(Effects.scratchWeights__linearSampledGaussian, (i + 1.0));
  var combined:Dynamic = (weightA + weightB);
  FlightRuntime.setIndex(outWeights, tap, combined);
  FlightRuntime.setIndex(outOffsets, tap, FlightRuntime.select(FlightRuntime.strictEquals(combined, 0.0), function():Dynamic return cast (posA + 0.5), function():Dynamic return cast (((posA * weightA) + ((posA + 1.0) * weightB)) / combined)));
} else {
  FlightRuntime.setIndex(outWeights, tap, FlightRuntime.getIndex(Effects.scratchWeights__linearSampledGaussian, i));
  FlightRuntime.setIndex(outOffsets, tap, posA);
}
        tap++;
      }
    }
    return cast outWeights;
    return cast null;
  }

  @:keep public static function computeOutlineEdgeParams(effect:OutlineEffect, out:Array<Float>):Void {
    var threshold:Dynamic = cast FlightRuntime.UNDEFINED;
    var feather:Dynamic = cast FlightRuntime.UNDEFINED;
    var color:Dynamic = cast FlightRuntime.UNDEFINED;
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var g:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    threshold = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.coalesce(FlightRuntime.field(effect, 'threshold'), function():Dynamic return cast 0.1)] : Array<Dynamic>));
    feather = (threshold * 0.5);
    color = FlightRuntime.coalesce(FlightRuntime.field(effect, 'color'), function():Dynamic return cast 255.0);
    r = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(color), Std.int(24.0))) & Std.int(255.0)) / 255.0);
    g = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(color), Std.int(16.0))) & Std.int(255.0)) / 255.0);
    b = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(color), Std.int(8.0))) & Std.int(255.0)) / 255.0);
    a = ((Std.int(color) & Std.int(255.0)) / 255.0);
    FlightRuntime.setIndex(out, 0.0, threshold);
    FlightRuntime.setIndex(out, 1.0, feather);
    FlightRuntime.setIndex(out, 2.0, r);
    FlightRuntime.setIndex(out, 3.0, g);
    FlightRuntime.setIndex(out, 4.0, b);
    FlightRuntime.setIndex(out, 5.0, a);
  }

  @:keep public static function computeOutlineThicknessPx(effect:OutlineEffect):Float {
    return cast FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'thickness'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeReinhardExtendedToneMap(x:Float, white:Float):Float {
    var w2:Dynamic = cast FlightRuntime.UNDEFINED;
    w2 = (white * white);
    return cast ((x * (1.0 + (x / w2))) / (1.0 + x));
    return cast null;
  }

  @:keep public static function computeReinhardToneMap(x:Float):Float {
    return cast (x / (1.0 + x));
    return cast null;
  }

  @:keep public static function computeScanlineParams(resolution:Float, intensity:Float, out:Array<Float>):Void {
    var scale:Dynamic = cast FlightRuntime.UNDEFINED;
    scale = (FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, resolution] : Array<Dynamic>)) / 480.0);
    FlightRuntime.setIndex(out, 0.0, scale);
    FlightRuntime.setIndex(out, 1.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, intensity] : Array<Dynamic>))] : Array<Dynamic>)));
  }

  @:keep public static function computeSeparableBlurPassCount(samples:Null<Float>):Float {
    return cast FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(samples, function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeSketchEdgeParams(effect:SketchEffect, out:Array<Float>):Void {
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var threshold:Dynamic = cast FlightRuntime.UNDEFINED;
    strength = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    threshold = FlightRuntime.callProperty(HxMath, 'max', cast ([0.01, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, (1.0 - (strength * 0.95))] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.setIndex(out, 0.0, threshold);
    FlightRuntime.setIndex(out, 1.0, strength);
  }

  @:keep public static function computeSsaoSampleKernel(samples:Float, out:flight.internal.FlightFloat32Array):Float {
    var n:Dynamic = cast FlightRuntime.UNDEFINED;
    n = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([samples] : Array<Dynamic>))] : Array<Dynamic>));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, n, '<'))) {
        var h2:Dynamic = FlightRuntime.callValue(Effects.halton__depthMath, cast ([(i + 1.0), 2.0] : Array<Dynamic>));
        var h3:Dynamic = FlightRuntime.callValue(Effects.halton__depthMath, cast ([(i + 1.0), 3.0] : Array<Dynamic>));
        var theta:Dynamic = ((h2 * 2.0) * HxMath.PI);
        var phi:Dynamic = FlightRuntime.callProperty(HxMath, 'acos', cast ([(1.0 - h3)] : Array<Dynamic>));
        var scale:Dynamic = (i / n);
        var dist:Dynamic = (0.1 + ((0.9 * scale) * scale));
        FlightRuntime.setIndex(out, ((i * 3.0) + 0.0), ((FlightRuntime.callProperty(HxMath, 'sin', cast ([phi] : Array<Dynamic>)) * FlightRuntime.callProperty(HxMath, 'cos', cast ([theta] : Array<Dynamic>))) * dist));
        FlightRuntime.setIndex(out, ((i * 3.0) + 1.0), ((FlightRuntime.callProperty(HxMath, 'sin', cast ([phi] : Array<Dynamic>)) * FlightRuntime.callProperty(HxMath, 'sin', cast ([theta] : Array<Dynamic>))) * dist));
        FlightRuntime.setIndex(out, ((i * 3.0) + 2.0), (FlightRuntime.callProperty(HxMath, 'cos', cast ([phi] : Array<Dynamic>)) * dist));
        i++;
      }
    }
    return cast n;
    return cast null;
  }

  @:keep public static function computeUncharted2ToneMap(x:Float):Float {
    var A:Dynamic = cast FlightRuntime.UNDEFINED;
    var B:Dynamic = cast FlightRuntime.UNDEFINED;
    var C:Dynamic = cast FlightRuntime.UNDEFINED;
    var D:Dynamic = cast FlightRuntime.UNDEFINED;
    var E:Dynamic = cast FlightRuntime.UNDEFINED;
    var F:Dynamic = cast FlightRuntime.UNDEFINED;
    A = 0.15;
    B = 0.5;
    C = 0.1;
    D = 0.2;
    E = 0.02;
    F = 0.3;
    return cast ((((x * ((A * x) + (C * B))) + (D * E)) / ((x * ((A * x) + B)) + (D * F))) - (E / F));
    return cast null;
  }

  @:keep public static function computeWhiteBalanceMultipliers(temperature:Float, tint:Float, out:Array<Float>):Void {
    var kelvin:Dynamic = cast FlightRuntime.UNDEFINED;
    var greenShift:Dynamic = cast FlightRuntime.UNDEFINED;
    kelvin = (6500.0 - (temperature * 4500.0));
    FlightRuntime.callValue(computeColorTemperatureRgb, cast ([kelvin, out] : Array<Dynamic>));
    greenShift = (-tint * 0.1);
    FlightRuntime.setIndex(out, 0.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.getIndex(out, 0.0)] : Array<Dynamic>)));
    FlightRuntime.setIndex(out, 1.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, (FlightRuntime.getIndex(out, 1.0) + greenShift)] : Array<Dynamic>)));
    FlightRuntime.setIndex(out, 2.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.getIndex(out, 2.0)] : Array<Dynamic>)));
  }

  @:keep public static function createAutoExposureEffect(?options:Dynamic):AutoExposureEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'AutoExposureEffect' }, options]);
    return cast null;
  }

  @:keep public static function createBarrelDistortionEffect(?options:Dynamic):BarrelDistortionEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'BarrelDistortionEffect' }, options]);
    return cast null;
  }

  @:keep public static function createBayerMatrix(order:Float, out:flight.internal.FlightFloat32Array):Float {
    var size:Dynamic = cast FlightRuntime.UNDEFINED;
    var sizeSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var raw:Dynamic = cast FlightRuntime.UNDEFINED;
    var currentSize:Dynamic = cast FlightRuntime.UNDEFINED;
    var invSizeSq:Dynamic = cast FlightRuntime.UNDEFINED;
    size = FlightRuntime.callProperty(HxMath, 'pow', cast ([2.0, FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([order] : Array<Dynamic>))] : Array<Dynamic>))] : Array<Dynamic>));
    sizeSq = (size * size);
    raw = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [sizeSq]);
    FlightRuntime.setIndex(raw, 0.0, 0.0);
    FlightRuntime.setIndex(raw, 1.0, 2.0);
    FlightRuntime.setIndex(raw, 2.0, 3.0);
    FlightRuntime.setIndex(raw, 3.0, 1.0);
    currentSize = 2.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(currentSize, size, '<'))) {
  var next:Dynamic = (currentSize * 2.0);
  var nextSq:Dynamic = (next * next);
  var tmp:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [nextSq]);
  {
    var y:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(y, currentSize, '<'))) {
      {
        var x:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(x, currentSize, '<'))) {
          var base:Dynamic = (FlightRuntime.getIndex(raw, ((y * currentSize) + x)) * 4.0);
          FlightRuntime.setIndex(tmp, ((y * next) + x), base);
          FlightRuntime.setIndex(tmp, ((y * next) + (x + currentSize)), (base + 2.0));
          FlightRuntime.setIndex(tmp, (((y + currentSize) * next) + x), (base + 3.0));
          FlightRuntime.setIndex(tmp, (((y + currentSize) * next) + (x + currentSize)), (base + 1.0));
          x++;
        }
      }
      y++;
    }
  }
  FlightRuntime.callProperty(raw, 'set', cast ([tmp] : Array<Dynamic>));
  (currentSize = cast (next : Dynamic));
}
    invSizeSq = (1.0 / sizeSq);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, sizeSq, '<'))) {
        FlightRuntime.setIndex(out, i, (FlightRuntime.getIndex(raw, i) * invSizeSq));
        i++;
      }
    }
    return cast size;
    return cast null;
  }

  @:keep public static function createBevelEffect(?options:Dynamic):BevelEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'BevelEffect' }, options]);
    return cast null;
  }

  @:keep public static function createBlendEffect(mode:AdvancedBlendMode, ?options:Dynamic):BlendEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'BlendEffect' }, { mode: mode }, options]);
    return cast null;
  }

  @:keep public static function createBloomEffect(?options:Dynamic):BloomEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'BloomEffect' }, options]);
    return cast null;
  }

  @:keep public static function createBlurEffect(?options:Dynamic):BlurEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'BlurEffect' }, options]);
    return cast null;
  }

  @:keep public static function createBokehDepthOfFieldEffect(?options:Dynamic):BokehDepthOfFieldEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'BokehDepthOfFieldEffect' }, options]);
    return cast null;
  }

  @:keep public static function createCameraMotionBlurEffect(?options:Dynamic):CameraMotionBlurEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'CameraMotionBlurEffect' }, options]);
    return cast null;
  }

  @:keep public static function createChromaticAberrationEffect(?options:Dynamic):ChromaticAberrationEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'ChromaticAberrationEffect' }, options]);
    return cast null;
  }

  @:keep public static function createCompositeEffect(operator_:CompositeOperator, ?options:Dynamic):CompositeEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'CompositeEffect' }, FlightRuntime.objectFromPairs([{ key: 'operator', value: operator_ }]), options]);
    return cast null;
  }

  @:keep public static function createContactShadowsEffect(?options:Dynamic):ContactShadowsEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'ContactShadowsEffect' }, options]);
    return cast null;
  }

  @:keep public static function createConvolutionEffect(options:Dynamic):ConvolutionEffect {
    return cast FlightRuntime.mergeObjects([{ kind: 'ConvolutionEffect' }, options]);
    return cast null;
  }

  @:keep public static function createCrtEffect(?options:Dynamic):CrtEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'CrtEffect' }, options]);
    return cast null;
  }

  @:keep public static function createCustomShaderEffect(options:Dynamic):CustomShaderEffect {
    return cast FlightRuntime.mergeObjects([{ kind: 'CustomShaderEffect' }, options]);
    return cast null;
  }

  @:keep public static function createDirectionalBlurEffect(?options:Dynamic):DirectionalBlurEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'DirectionalBlurEffect' }, options]);
    return cast null;
  }

  @:keep public static function createDisplacementEffect(?options:Dynamic):DisplacementEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'DisplacementEffect' }, options]);
    return cast null;
  }

  @:keep public static function createDitherEffect(?options:Dynamic):DitherEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'DitherEffect' }, options]);
    return cast null;
  }

  @:keep public static function createDropShadowEffect(?options:Dynamic):DropShadowEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'DropShadowEffect' }, options]);
    return cast null;
  }

  @:keep public static function createFilmEmulationEffect(?options:Dynamic):FilmEmulationEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'FilmEmulationEffect' }, options]);
    return cast null;
  }

  @:keep public static function createFilmGrainEffect(?options:Dynamic):FilmGrainEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'FilmGrainEffect' }, options]);
    return cast null;
  }

  @:keep public static function createFxaaEffect(?options:Dynamic):FxaaEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'FxaaEffect' }, options]);
    return cast null;
  }

  @:keep public static function createGaussianKernelWeights(radius:Float, sigma:Float, out:flight.internal.FlightFloat32Array):Float {
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var s:Dynamic = cast FlightRuntime.UNDEFINED;
    var twoSigmaSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var sum:Dynamic = cast FlightRuntime.UNDEFINED;
    var invSum:Dynamic = cast FlightRuntime.UNDEFINED;
    r = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'ceil', cast ([radius] : Array<Dynamic>))] : Array<Dynamic>));
    s = FlightRuntime.callProperty(HxMath, 'max', cast ([0.000001, sigma] : Array<Dynamic>));
    twoSigmaSq = ((2.0 * s) * s);
    sum = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, r, '<='))) {
        var w:Dynamic = FlightRuntime.callProperty(HxMath, 'exp', cast ([(-(i * i) / twoSigmaSq)] : Array<Dynamic>));
        FlightRuntime.setIndex(out, i, w);
        (sum = cast ((sum + FlightRuntime.select(FlightRuntime.strictEquals(i, 0.0), function():Dynamic return cast w, function():Dynamic return cast (2.0 * w))) : Dynamic));
        i++;
      }
    }
    invSum = (1.0 / sum);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, r, '<='))) {
        FlightRuntime.setIndex(out, i, (FlightRuntime.getIndex(out, i) * invSum));
        i++;
      }
    }
    return cast (r + 1.0);
    return cast null;
  }

  @:keep public static function createGlitchEffect(?options:Dynamic):GlitchEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'GlitchEffect' }, options]);
    return cast null;
  }

  @:keep public static function createGodRaysEffect(?options:Dynamic):GodRaysEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'GodRaysEffect' }, options]);
    return cast null;
  }

  @:keep public static function createGradientBevelEffect(options:Dynamic):GradientBevelEffect {
    return cast FlightRuntime.mergeObjects([{ kind: 'GradientBevelEffect' }, options]);
    return cast null;
  }

  @:keep public static function createGradientGlowEffect(options:Dynamic):GradientGlowEffect {
    return cast FlightRuntime.mergeObjects([{ kind: 'GradientGlowEffect' }, options]);
    return cast null;
  }

  @:keep public static function createHalftoneEffect(?options:Dynamic):HalftoneEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'HalftoneEffect' }, options]);
    return cast null;
  }

  @:keep public static function createInnerGlowEffect(?options:Dynamic):InnerGlowEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'InnerGlowEffect' }, options]);
    return cast null;
  }

  @:keep public static function createInnerShadowEffect(?options:Dynamic):InnerShadowEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'InnerShadowEffect' }, options]);
    return cast null;
  }

  @:keep public static function createKuwaharaEffect(?options:Dynamic):KuwaharaEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'KuwaharaEffect' }, options]);
    return cast null;
  }

  @:keep public static function createLensDirtEffect(?options:Dynamic):LensDirtEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'LensDirtEffect' }, options]);
    return cast null;
  }

  @:keep public static function createLensDistortionEffect(?options:Dynamic):LensDistortionEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'LensDistortionEffect' }, options]);
    return cast null;
  }

  @:keep public static function createLensFlareEffect(?options:Dynamic):LensFlareEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'LensFlareEffect' }, options]);
    return cast null;
  }

  @:keep public static function createMedianEffect(?options:Dynamic):MedianEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'MedianEffect' }, options]);
    return cast null;
  }

  @:keep public static function createMotionBlurEffect(?options:Dynamic):MotionBlurEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'MotionBlurEffect' }, options]);
    return cast null;
  }

  @:keep public static function createOuterGlowEffect(?options:Dynamic):OuterGlowEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'OuterGlowEffect' }, options]);
    return cast null;
  }

  @:keep public static function createOutlineEffect(?options:Dynamic):OutlineEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'OutlineEffect' }, options]);
    return cast null;
  }

  @:keep public static function createPanniniProjectionEffect(?options:Dynamic):PanniniProjectionEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'PanniniProjectionEffect' }, options]);
    return cast null;
  }

  @:keep public static function createPixelateEffect(?options:Dynamic):PixelateEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'PixelateEffect' }, options]);
    return cast null;
  }

  @:keep public static function createPosterizeEffect(?options:Dynamic):PosterizeEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'PosterizeEffect' }, options]);
    return cast null;
  }

  @:keep public static function createRadialBlurEffect(?options:Dynamic):RadialBlurEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'RadialBlurEffect' }, options]);
    return cast null;
  }

  @:keep public static function createScanlinesEffect(?options:Dynamic):ScanlinesEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'ScanlinesEffect' }, options]);
    return cast null;
  }

  @:keep public static function createScreenSpaceFogEffect(?options:Dynamic):ScreenSpaceFogEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'ScreenSpaceFogEffect' }, options]);
    return cast null;
  }

  @:keep public static function createSharpenEffect(?options:Dynamic):SharpenEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'SharpenEffect' }, options]);
    return cast null;
  }

  @:keep public static function createSketchEffect(?options:Dynamic):SketchEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'SketchEffect' }, options]);
    return cast null;
  }

  @:keep public static function createSmaaEffect(?options:Dynamic):SmaaEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'SmaaEffect' }, options]);
    return cast null;
  }

  @:keep public static function createSsaoEffect(?options:Dynamic):SsaoEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'SsaoEffect' }, options]);
    return cast null;
  }

  @:keep public static function createSsrEffect(?options:Dynamic):SsrEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'SsrEffect' }, options]);
    return cast null;
  }

  @:keep public static function createTaaEffect(?options:Dynamic):TaaEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'TaaEffect' }, options]);
    return cast null;
  }

  @:keep public static function createTiltShiftEffect(?options:Dynamic):TiltShiftEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'TiltShiftEffect' }, options]);
    return cast null;
  }

  @:keep public static function createToneMapEffect(?options:Dynamic):ToneMapEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'ToneMapEffect' }, options]);
    return cast null;
  }

  @:keep public static function createVignetteEffect(?options:Dynamic):VignetteEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'VignetteEffect' }, options]);
    return cast null;
  }

  @:keep public static function createVolumetricLightEffect(?options:Dynamic):VolumetricLightEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'VolumetricLightEffect' }, options]);
    return cast null;
  }

  @:keep public static function createWhiteBalanceEffect(?options:Dynamic):WhiteBalanceEffect {
    if (options == null) options = cast ({  } : Dynamic);
    return cast FlightRuntime.mergeObjects([{ kind: 'WhiteBalanceEffect' }, options]);
    return cast null;
  }

  public static final DEFAULTS__renderEffectDefaults:Dynamic = { AutoExposureEffect: { adaptationSpeed: 1.0, exposureCompensation: 0.0, maxExposure: 2.0, minExposure: -2.0 }, BarrelDistortionEffect: { amount: 0.5, scale: 0.9 }, BevelEffect: { angle: 45.0, bevelType: 'inner', blurX: 4.0, blurY: 4.0, distance: 4.0, highlightAlpha: 1.0, highlightColor: 16777215.0, quality: 1.0, shadowAlpha: 1.0, shadowColor: 0.0, sourceMode: 'draw', strength: 1.0 }, BlendEffect: { opacity: 1.0 }, BloomEffect: { brightness: 1.0, mipCount: 0.0, passes: 1.0, radius: 8.0, threshold: 0.8, thresholdKnee: 0.5 }, BlurEffect: { blurX: 4.0, blurY: 4.0 }, BokehDepthOfFieldEffect: { apertureBlades: 6.0, maxBlurRadius: 16.0, samples: 16.0 }, CameraMotionBlurEffect: { samples: 8.0 }, ChromaticAberrationEffect: { fringeStrength: 0.01, radial: true, samples: 3.0 }, ContactShadowsEffect: { distance: 0.5, opacity: 0.6, samples: 16.0, smoothness: 0.5 }, ConvolutionEffect: { bias: 0.0, clamp: true, preserveAlpha: true }, CrtEffect: { curvature: 0.1, scanlineIntensity: 0.5, vignette: 0.4, aberration: 0.0 }, DirectionalBlurEffect: { angle: 0.0, samples: 8.0 }, DisplacementEffect: { frequency: 12.0, intensity: 8.0 }, DitherEffect: { levels: 16.0 }, DropShadowEffect: { alpha: 1.0, angle: 45.0, blurX: 4.0, blurY: 4.0, distance: 4.0, quality: 1.0, sourceMode: 'draw', strength: 1.0 }, FilmEmulationEffect: { gateWeave: 0.0, grainIntensity: 0.1, halationRadius: 4.0, halationStrength: 0.3 }, FilmGrainEffect: { intensity: 0.2, size: 1.0, seed: 0.0 }, FxaaEffect: { edgeThreshold: 0.0312, subpixel: 0.75 }, GlitchEffect: { blockSize: 24.0, colorShift: 8.0, intensity: 0.5, seed: 0.0 }, GodRaysEffect: { centerX: 0.5, centerY: 0.5, decay: 0.96, density: 0.96, exposure: 0.1, samples: 100.0, weight: 0.4 }, GradientBevelEffect: { angle: 45.0, bevelType: 'inner', blurX: 4.0, blurY: 4.0, distance: 4.0, quality: 1.0, sourceMode: 'draw', strength: 1.0 }, GradientGlowEffect: { blurX: 6.0, blurY: 6.0, quality: 1.0, sourceMode: 'draw', strength: 1.0 }, HalftoneEffect: { angle: 0.785, scale: 8.0 }, InnerGlowEffect: { alpha: 1.0, blurX: 6.0, blurY: 6.0, color: 16711680.0, quality: 1.0, sourceMode: 'draw', strength: 1.0 }, InnerShadowEffect: { alpha: 1.0, angle: 45.0, blurX: 4.0, blurY: 4.0, distance: 4.0, quality: 1.0, sourceMode: 'draw', strength: 1.0 }, KuwaharaEffect: { radius: 3.0 }, LensDirtEffect: { intensity: 1.0, threshold: 0.55 }, LensDistortionEffect: { amount: 0.5, scale: 0.9 }, LensFlareEffect: { ghosts: 4.0, halo: 0.5, intensity: 1.0, threshold: 0.9 }, MedianEffect: { radius: 1.0 }, MotionBlurEffect: { intensity: 1.0, samples: 8.0, shutterAngle: 180.0, target: 'both' }, OuterGlowEffect: { alpha: 1.0, blurX: 6.0, blurY: 6.0, color: 16711680.0, quality: 1.0, sourceMode: 'draw', strength: 1.0 }, OutlineEffect: { color: 255.0, thickness: 1.0, threshold: 0.1 }, PanniniProjectionEffect: { compression: 0.5, crop: 0.0 }, PixelateEffect: { size: 8.0 }, PosterizeEffect: { levels: 8.0 }, RadialBlurEffect: { centerX: 0.5, centerY: 0.5, samples: 8.0, strength: 0.1 }, ScanlinesEffect: { count: 480.0, intensity: 0.25 }, ScreenSpaceFogEffect: { density: 0.5, far: 1000.0, near: 10.0 }, SharpenEffect: { amount: 0.5 }, SketchEffect: { strength: 1.0 }, SmaaEffect: { threshold: 0.1 }, SsaoEffect: { bias: 0.025, intensity: 1.0, radius: 0.5, samples: 16.0 }, SsrEffect: { maxDistance: 100.0, maxSteps: 64.0, resolution: 0.5, steps: 64.0, thickness: 0.1 }, TaaEffect: { feedback: 0.9 }, TiltShiftEffect: { blur: 1.0, center: 0.5, width: 0.2 }, ToneMapEffect: FlightRuntime.objectFromPairs([{ key: 'exposure', value: 0.0 }, { key: 'operator', value: 'aces' }]), VignetteEffect: { color: 255.0, intensity: 0.5, radius: 1.0, softness: 0.5 }, VolumetricLightEffect: { density: 0.5, lightColor: 4294967295.0, lightX: 0.5, lightY: 0.2, samples: 32.0, scattering: 0.7 }, WhiteBalanceEffect: { temperature: 0.0, tint: 0.0 } };

  @:keep public static function getAcesInputMatrix(out:flight.internal.FlightFloat32Array):Void {
    FlightRuntime.setIndex(out, 0.0, 0.59719);
    FlightRuntime.setIndex(out, 1.0, 0.076);
    FlightRuntime.setIndex(out, 2.0, 0.0284);
    FlightRuntime.setIndex(out, 3.0, 0.35458);
    FlightRuntime.setIndex(out, 4.0, 0.90834);
    FlightRuntime.setIndex(out, 5.0, 0.13383);
    FlightRuntime.setIndex(out, 6.0, 0.04823);
    FlightRuntime.setIndex(out, 7.0, 0.01566);
    FlightRuntime.setIndex(out, 8.0, 0.83777);
  }

  @:keep public static function getAcesOutputMatrix(out:flight.internal.FlightFloat32Array):Void {
    FlightRuntime.setIndex(out, 0.0, 1.60475);
    FlightRuntime.setIndex(out, 1.0, -0.10208);
    FlightRuntime.setIndex(out, 2.0, -0.00327);
    FlightRuntime.setIndex(out, 3.0, -0.53108);
    FlightRuntime.setIndex(out, 4.0, 1.10813);
    FlightRuntime.setIndex(out, 5.0, -0.07276);
    FlightRuntime.setIndex(out, 6.0, -0.07367);
    FlightRuntime.setIndex(out, 7.0, -0.00605);
    FlightRuntime.setIndex(out, 8.0, 1.07602);
  }

  @:keep public static function getAdvancedBlendRgb(mode:AdvancedBlendMode, cbR:Float, cbG:Float, cbB:Float, csR:Float, csG:Float, csB:Float, out:Dynamic):Void {
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var g:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.callValue(isNonSeparableBlendMode, cast ([mode] : Array<Dynamic>)))) {
  FlightRuntime.callValue(blendNonSeparableRgb, cast ([mode, cbR, cbG, cbB, csR, csG, csB, out] : Array<Dynamic>));
  return;
}
    r = FlightRuntime.callValue(getSeparableBlendChannel, cast ([mode, cbR, csR] : Array<Dynamic>));
    g = FlightRuntime.callValue(getSeparableBlendChannel, cast ([mode, cbG, csG] : Array<Dynamic>));
    b = FlightRuntime.callValue(getSeparableBlendChannel, cast ([mode, cbB, csB] : Array<Dynamic>));
    FlightRuntime.setIndex(out, 0.0, r);
    FlightRuntime.setIndex(out, 1.0, g);
    FlightRuntime.setIndex(out, 2.0, b);
  }

  @:keep public static function getBlurDownsampleLevel(sigma:Float):Float {
    if (FlightRuntime.truthy(FlightRuntime.compare(sigma, Effects.BLUR_DOWNSAMPLE_MAX_SIGMA__blurDownsample, '<='))) { return cast 0.0; }
    return cast FlightRuntime.callProperty(HxMath, 'ceil', cast ([FlightRuntime.log2((sigma / Effects.BLUR_DOWNSAMPLE_MAX_SIGMA__blurDownsample))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getBlurResidualSigma(sigma:Float, level:Float):Float {
    if (FlightRuntime.truthy(FlightRuntime.compare(sigma, 0.0, '<='))) { return cast 0.0; }
    return cast (sigma / HxMath.pow(2.0, level));
    return cast null;
  }

  @:keep public static function getCompositeOperatorFactors(operator_:CompositeOperator, sourceAlpha:Float, backdropAlpha:Float, out:Dynamic):Void {
    var as:Dynamic = cast FlightRuntime.UNDEFINED;
    var ab:Dynamic = cast FlightRuntime.UNDEFINED;
    var fa:Float = cast FlightRuntime.UNDEFINED;
    var fb:Float = cast FlightRuntime.UNDEFINED;
    as = sourceAlpha;
    ab = backdropAlpha;
    {
      var __switchValue = operator_;
      if (__switchValue == Types.CompositeOperatorValue.Clear) {
        (fa = cast (0.0 : Dynamic));
        (fb = cast (0.0 : Dynamic));
      }
      else if (__switchValue == Types.CompositeOperatorValue.Copy) {
        (fa = cast (1.0 : Dynamic));
        (fb = cast (0.0 : Dynamic));
      }
      else if (__switchValue == Types.CompositeOperatorValue.DestinationAtop) {
        (fa = cast ((1.0 - ab) : Dynamic));
        (fb = cast (as : Dynamic));
      }
      else if (__switchValue == Types.CompositeOperatorValue.DestinationIn) {
        (fa = cast (0.0 : Dynamic));
        (fb = cast (as : Dynamic));
      }
      else if (__switchValue == Types.CompositeOperatorValue.DestinationOut) {
        (fa = cast (0.0 : Dynamic));
        (fb = cast ((1.0 - as) : Dynamic));
      }
      else if (__switchValue == Types.CompositeOperatorValue.DestinationOver) {
        (fa = cast ((1.0 - ab) : Dynamic));
        (fb = cast (1.0 : Dynamic));
      }
      else if (__switchValue == Types.CompositeOperatorValue.SourceAtop) {
        (fa = cast (ab : Dynamic));
        (fb = cast ((1.0 - as) : Dynamic));
      }
      else if (__switchValue == Types.CompositeOperatorValue.SourceIn) {
        (fa = cast (ab : Dynamic));
        (fb = cast (0.0 : Dynamic));
      }
      else if (__switchValue == Types.CompositeOperatorValue.SourceOut) {
        (fa = cast ((1.0 - ab) : Dynamic));
        (fb = cast (0.0 : Dynamic));
      }
      else if (__switchValue == Types.CompositeOperatorValue.Xor) {
        (fa = cast ((1.0 - ab) : Dynamic));
        (fb = cast ((1.0 - as) : Dynamic));
      }
      else  {
        (fa = cast (1.0 : Dynamic));
        (fb = cast ((1.0 - as) : Dynamic));
      }
    }
    FlightRuntime.setIndex(out, 0.0, fa);
    FlightRuntime.setIndex(out, 1.0, fb);
  }

  @:keep public static function getGaussianKernelSize(sigma:Float):Float {
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.compare(sigma, 0.0, '<='))) { return cast 1.0; }
    radius = FlightRuntime.callProperty(HxMath, 'ceil', cast ([(3.0 * sigma)] : Array<Dynamic>));
    return cast ((radius * 2.0) + 1.0);
    return cast null;
  }

  @:keep public static function getLinearSampledGaussianTapCount(sigma:Float):Float {
    return cast FlightRuntime.callProperty(HxMath, 'ceil', cast ([(FlightRuntime.callValue(getGaussianKernelSize, cast ([sigma] : Array<Dynamic>)) / 2.0)] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getRenderEffectDefaults(kind:String):Dynamic {
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    entry = FlightRuntime.getIndex(Effects.DEFAULTS__renderEffectDefaults, kind);
    if (FlightRuntime.truthy(!FlightRuntime.truthy(entry))) { return cast {  }; }
    return cast FlightRuntime.mergeObjects([entry]);
    return cast null;
  }

  @:keep public static function getRenderEffectInputs(effect:RenderEffect):Array<RenderEffectInput> {
    return cast FlightRuntime.coalesce(FlightRuntime.getIndex(Effects.RENDER_EFFECT_INPUTS__renderEffectInputs, FlightRuntime.field(effect, 'kind')), function():Dynamic return cast cast ([] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getRenderEffectKinds():Array<String> {
    return cast RENDER_EFFECT_KINDS;
    return cast null;
  }

  @:keep public static function getSeparableBlendChannel(mode:AdvancedBlendMode, cb:Float, cs:Float):Float {
    {
      var __switchValue = mode;
      if (__switchValue == Types.AdvancedBlendModeValue.Overlay) {
        return cast FlightRuntime.select(FlightRuntime.compare(cb, 0.5, '<='), function():Dynamic return cast ((2.0 * cb) * cs), function():Dynamic return cast (1.0 - ((2.0 * (1.0 - cb)) * (1.0 - cs))));
      }
      else if (__switchValue == Types.AdvancedBlendModeValue.HardLight) {
        return cast FlightRuntime.select(FlightRuntime.compare(cs, 0.5, '<='), function():Dynamic return cast ((2.0 * cb) * cs), function():Dynamic return cast (1.0 - ((2.0 * (1.0 - cb)) * (1.0 - cs))));
      }
      else if (__switchValue == Types.AdvancedBlendModeValue.SoftLight) {
        {
          var d:Dynamic = FlightRuntime.select(FlightRuntime.compare(cb, 0.25, '<='), function():Dynamic return cast (((((16.0 * cb) - 12.0) * cb) + 4.0) * cb), function():Dynamic return cast FlightRuntime.callProperty(HxMath, 'sqrt', cast ([cb] : Array<Dynamic>)));
          return cast FlightRuntime.select(FlightRuntime.compare(cs, 0.5, '<='), function():Dynamic return cast (cb - (((1.0 - (2.0 * cs)) * cb) * (1.0 - cb))), function():Dynamic return cast (cb + (((2.0 * cs) - 1.0) * (d - cb))));
        }
      }
      else if (__switchValue == Types.AdvancedBlendModeValue.Difference) {
        return cast FlightRuntime.callProperty(HxMath, 'abs', cast ([(cb - cs)] : Array<Dynamic>));
      }
      else if (__switchValue == Types.AdvancedBlendModeValue.Exclusion) {
        return cast ((cb + cs) - ((2.0 * cb) * cs));
      }
      else if (__switchValue == Types.AdvancedBlendModeValue.ColorDodge) {
        if (FlightRuntime.truthy(FlightRuntime.compare(cb, 0.0, '<='))) { return cast 0.0; }
        if (FlightRuntime.truthy(FlightRuntime.compare(cs, 1.0, '>='))) { return cast 1.0; }
        return cast FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, (cb / (1.0 - cs))] : Array<Dynamic>));
      }
      else if (__switchValue == Types.AdvancedBlendModeValue.ColorBurn) {
        if (FlightRuntime.truthy(FlightRuntime.compare(cb, 1.0, '>='))) { return cast 1.0; }
        if (FlightRuntime.truthy(FlightRuntime.compare(cs, 0.0, '<='))) { return cast 0.0; }
        return cast (1.0 - FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, ((1.0 - cb) / cs)] : Array<Dynamic>)));
      }
      else  {
        return cast cs;
      }
    }
    return cast null;
  }

  @:keep public static function getSobelKernelCoefficients(out:flight.internal.FlightFloat32Array):Void {
    FlightRuntime.setIndex(out, 0.0, -1.0);
    FlightRuntime.setIndex(out, 1.0, 0.0);
    FlightRuntime.setIndex(out, 2.0, 1.0);
    FlightRuntime.setIndex(out, 3.0, -2.0);
    FlightRuntime.setIndex(out, 4.0, 0.0);
    FlightRuntime.setIndex(out, 5.0, 2.0);
    FlightRuntime.setIndex(out, 6.0, -1.0);
    FlightRuntime.setIndex(out, 7.0, 0.0);
    FlightRuntime.setIndex(out, 8.0, 1.0);
    FlightRuntime.setIndex(out, 9.0, -1.0);
    FlightRuntime.setIndex(out, 10.0, -2.0);
    FlightRuntime.setIndex(out, 11.0, -1.0);
    FlightRuntime.setIndex(out, 12.0, 0.0);
    FlightRuntime.setIndex(out, 13.0, 0.0);
    FlightRuntime.setIndex(out, 14.0, 0.0);
    FlightRuntime.setIndex(out, 15.0, 1.0);
    FlightRuntime.setIndex(out, 16.0, 2.0);
    FlightRuntime.setIndex(out, 17.0, 1.0);
  }

  @:keep public static function halton__depthMath(index:Float, base:Float):Float {
    var result:Dynamic = cast FlightRuntime.UNDEFINED;
    var f:Dynamic = cast FlightRuntime.UNDEFINED;
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    result = 0.0;
    f = 1.0;
    i = index;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, 0.0, '>'))) {
  (f = cast ((f / base) : Dynamic));
  (result = cast ((result + (f * (i % base))) : Dynamic));
  (i = cast (FlightRuntime.callProperty(HxMath, 'floor', cast ([(i / base)] : Array<Dynamic>)) : Dynamic));
}
    return cast result;
    return cast null;
  }

  @:keep public static function isNonSeparableBlendMode(mode:AdvancedBlendMode):Bool {
    return cast FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(mode, Types.AdvancedBlendModeValue.Hue), function():Dynamic return cast FlightRuntime.strictEquals(mode, Types.AdvancedBlendModeValue.Saturation)), function():Dynamic return cast FlightRuntime.strictEquals(mode, Types.AdvancedBlendModeValue.Color)), function():Dynamic return cast FlightRuntime.strictEquals(mode, Types.AdvancedBlendModeValue.Luminosity));
    return cast null;
  }

  @:keep public static function lerpRenderEffect(a:RenderEffect, b:RenderEffect, t:Float, out:RenderEffect):Bool {
    var tc:Dynamic = cast FlightRuntime.UNDEFINED;
    var numericKeys:Dynamic = cast FlightRuntime.UNDEFINED;
    var booleanKeys:Dynamic = cast FlightRuntime.UNDEFINED;
    var stringKeys:Dynamic = cast FlightRuntime.UNDEFINED;
    var aRec:Dynamic = cast FlightRuntime.UNDEFINED;
    var bRec:Dynamic = cast FlightRuntime.UNDEFINED;
    var outRecord:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(a, 'kind'), FlightRuntime.field(b, 'kind')))) { return cast false; }
    tc = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, t] : Array<Dynamic>))] : Array<Dynamic>));
    numericKeys = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Set'] : Array<Dynamic>)), []);
    booleanKeys = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Set'] : Array<Dynamic>)), []);
    stringKeys = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Set'] : Array<Dynamic>)), []);
    aRec = (cast a : Dynamic);
    bRec = (cast b : Dynamic);
    for (key in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Object'] : Array<Dynamic>)), 'keys', cast ([aRec] : Array<Dynamic>)))) {
      if (FlightRuntime.truthy(FlightRuntime.strictEquals(key, 'kind'))) { continue; }
      var va:Dynamic = FlightRuntime.getIndex(aRec, key);
      var vb:Dynamic = FlightRuntime.getIndex(bRec, key);
      if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.typeofValue(va), 'number'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.typeofValue(vb), 'number')))) {
  FlightRuntime.callProperty(numericKeys, 'add', cast ([key] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.typeofValue(va), 'boolean'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.typeofValue(vb), 'boolean')))) {
  FlightRuntime.callProperty(booleanKeys, 'add', cast ([key] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.typeofValue(va), 'string'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.typeofValue(vb), 'string')))) {
  FlightRuntime.callProperty(stringKeys, 'add', cast ([key] : Array<Dynamic>));
} } }
    }
    for (key in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Object'] : Array<Dynamic>)), 'keys', cast ([bRec] : Array<Dynamic>)))) {
      if (FlightRuntime.truthy(FlightRuntime.strictEquals(key, 'kind'))) { continue; }
      if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(!FlightRuntime.truthy(FlightRuntime.callProperty(numericKeys, 'has', cast ([key] : Array<Dynamic>))), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.callProperty(booleanKeys, 'has', cast ([key] : Array<Dynamic>)))), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.callProperty(stringKeys, 'has', cast ([key] : Array<Dynamic>)))))) {
  var vb:Dynamic = FlightRuntime.getIndex(bRec, key);
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.typeofValue(vb), 'number'))) { FlightRuntime.callProperty(numericKeys, 'add', cast ([key] : Array<Dynamic>)); } else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.typeofValue(vb), 'boolean'))) { FlightRuntime.callProperty(booleanKeys, 'add', cast ([key] : Array<Dynamic>)); } else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.typeofValue(vb), 'string'))) { FlightRuntime.callProperty(stringKeys, 'add', cast ([key] : Array<Dynamic>)); } } }
}
    }
    outRecord = (cast (cast out : Dynamic) : Dynamic);
    for (key in FlightRuntime.iterable(numericKeys)) {
      var va:Dynamic = (cast FlightRuntime.getIndex(aRec, key) : Null<Float>);
      var vb:Dynamic = (cast FlightRuntime.getIndex(bRec, key) : Null<Float>);
      if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(va, FlightRuntime.UNDEFINED), function():Dynamic return cast !FlightRuntime.strictEquals(vb, FlightRuntime.UNDEFINED)))) {
  FlightRuntime.setIndex(outRecord, key, (va + ((vb - va) * tc)));
} else {
  FlightRuntime.setIndex(outRecord, key, FlightRuntime.select(FlightRuntime.compare(tc, 0.5, '<'), function():Dynamic return cast va, function():Dynamic return cast vb));
}
    }
    for (key in FlightRuntime.iterable(booleanKeys)) {
      FlightRuntime.setIndex(outRecord, key, FlightRuntime.select(FlightRuntime.compare(tc, 0.5, '<'), function():Dynamic return cast FlightRuntime.getIndex(aRec, key), function():Dynamic return cast FlightRuntime.getIndex(bRec, key)));
    }
    for (key in FlightRuntime.iterable(stringKeys)) {
      FlightRuntime.setIndex(outRecord, key, FlightRuntime.select(FlightRuntime.compare(tc, 0.5, '<'), function():Dynamic return cast FlightRuntime.getIndex(aRec, key), function():Dynamic return cast FlightRuntime.getIndex(bRec, key)));
    }
    return cast true;
    return cast null;
  }

  @:keep public static function normalizeRenderEffect(effect:RenderEffect, out:RenderEffect):Bool {
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    var effectRec:Dynamic = cast FlightRuntime.UNDEFINED;
    var outRec:Dynamic = cast FlightRuntime.UNDEFINED;
    entry = FlightRuntime.getIndex(Effects.DEFAULTS__renderEffectDefaults, FlightRuntime.field(effect, 'kind'));
    if (FlightRuntime.truthy(!FlightRuntime.truthy(entry))) { return cast false; }
    effectRec = (cast effect : Dynamic);
    outRec = (cast (cast out : Dynamic) : Dynamic);
    for (key in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Object'] : Array<Dynamic>)), 'keys', cast ([entry] : Array<Dynamic>)))) {
      FlightRuntime.setIndex(outRec, key, FlightRuntime.select(!FlightRuntime.strictEquals(FlightRuntime.getIndex(effectRec, key), FlightRuntime.UNDEFINED), function():Dynamic return cast FlightRuntime.getIndex(effectRec, key), function():Dynamic return cast FlightRuntime.getIndex(entry, key)));
    }
    for (key in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Object'] : Array<Dynamic>)), 'keys', cast ([effectRec] : Array<Dynamic>)))) {
      if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.hasField(entry, key)))) {
  FlightRuntime.setIndex(outRec, key, FlightRuntime.getIndex(effectRec, key));
}
    }
    return cast true;
    return cast null;
  }

  public static final RENDER_EFFECT_INPUTS__renderEffectInputs:Dynamic = { AutoExposureEffect: cast (['Hdr'] : Array<Dynamic>), BloomEffect: cast (['Hdr'] : Array<Dynamic>), BokehDepthOfFieldEffect: cast (['Depth'] : Array<Dynamic>), CameraMotionBlurEffect: cast (['Motion'] : Array<Dynamic>), ContactShadowsEffect: cast (['Depth'] : Array<Dynamic>), MotionBlurEffect: cast (['Motion'] : Array<Dynamic>), ScreenSpaceFogEffect: cast (['Depth'] : Array<Dynamic>), SsaoEffect: cast (['Depth'] : Array<Dynamic>), SsrEffect: cast (['Depth'] : Array<Dynamic>), TaaEffect: cast (['Temporal'] : Array<Dynamic>), TiltShiftEffect: cast (['Depth'] : Array<Dynamic>), ToneMapEffect: cast (['Hdr'] : Array<Dynamic>), VolumetricLightEffect: cast (['Depth'] : Array<Dynamic>) };

  public static final RENDER_EFFECT_KINDS:Array<String> = cast (['AutoExposureEffect', 'BarrelDistortionEffect', 'BevelEffect', 'BlendEffect', 'BloomEffect', 'BlurEffect', 'BokehDepthOfFieldEffect', 'CameraMotionBlurEffect', 'ChromaticAberrationEffect', 'ContactShadowsEffect', 'ConvolutionEffect', 'CrtEffect', 'CustomShaderEffect', 'DirectionalBlurEffect', 'DisplacementEffect', 'DitherEffect', 'DropShadowEffect', 'FilmEmulationEffect', 'FilmGrainEffect', 'FxaaEffect', 'GlitchEffect', 'GodRaysEffect', 'GradientBevelEffect', 'GradientGlowEffect', 'HalftoneEffect', 'InnerGlowEffect', 'InnerShadowEffect', 'KuwaharaEffect', 'LensDirtEffect', 'LensDistortionEffect', 'LensFlareEffect', 'MedianEffect', 'MotionBlurEffect', 'OuterGlowEffect', 'OutlineEffect', 'PanniniProjectionEffect', 'PixelateEffect', 'PosterizeEffect', 'RadialBlurEffect', 'ScanlinesEffect', 'ScreenSpaceFogEffect', 'SharpenEffect', 'SketchEffect', 'SmaaEffect', 'SsaoEffect', 'SsrEffect', 'TaaEffect', 'TiltShiftEffect', 'ToneMapEffect', 'VignetteEffect', 'VolumetricLightEffect', 'WhiteBalanceEffect'] : Array<Dynamic>);

  public static final scratchWeights__linearSampledGaussian:Array<Float> = cast ([] : Array<Dynamic>);

  @:keep public static function setBlendLuminosity__blendModeMath(r:Float, g:Float, b:Float, target:Float):Array<Float> {
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    d = (target - FlightRuntime.callValue(Effects.blendLuminosity__blendModeMath, cast ([r, g, b] : Array<Dynamic>)));
    return cast FlightRuntime.callValue(Effects.clipBlendColor__blendModeMath, cast ([(r + d), (g + d), (b + d)] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function setBlendSaturation__blendModeMath(r:Float, g:Float, b:Float, target:Float):Array<Float> {
    var out:Array<Float> = cast FlightRuntime.UNDEFINED;
    var iMin:Dynamic = cast FlightRuntime.UNDEFINED;
    var iMax:Dynamic = cast FlightRuntime.UNDEFINED;
    var iMid:Dynamic = cast FlightRuntime.UNDEFINED;
    out = cast ([r, g, b] : Array<Dynamic>);
    iMin = 0.0;
    iMax = 0.0;
    {
      var i:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, 3.0, '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.getIndex(out, i), FlightRuntime.getIndex(out, iMin), '<'))) { (iMin = cast (i : Dynamic)); }
        if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.getIndex(out, i), FlightRuntime.getIndex(out, iMax), '>'))) { (iMax = cast (i : Dynamic)); }
        i++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(iMin, iMax))) {
  (iMax = cast (((iMin + 1.0) % 3.0) : Dynamic));
}
    iMid = ((3.0 - iMin) - iMax);
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.getIndex(out, iMax), FlightRuntime.getIndex(out, iMin), '>'))) {
  FlightRuntime.setIndex(out, iMid, (((FlightRuntime.getIndex(out, iMid) - FlightRuntime.getIndex(out, iMin)) * target) / (FlightRuntime.getIndex(out, iMax) - FlightRuntime.getIndex(out, iMin))));
  FlightRuntime.setIndex(out, iMax, target);
} else {
  FlightRuntime.setIndex(out, iMid, 0.0);
  FlightRuntime.setIndex(out, iMax, 0.0);
}
    FlightRuntime.setIndex(out, iMin, 0.0);
    return cast out;
    return cast null;
  }

  @:keep public static function smoothstep01__toneMapMath(edge0:Float, edge1:Float, x:Float):Float {
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    t = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, ((x - edge0) / (edge1 - edge0))] : Array<Dynamic>))] : Array<Dynamic>));
    return cast ((t * t) * (3.0 - (2.0 * t)));
    return cast null;
  }

  @:keep public static function validateRenderEffectList(effects:Array<RenderEffect>, available:Array<RenderEffectInput>):Null<RenderEffectInput> {
    for (effect in FlightRuntime.iterable(effects)) {
      var required:Dynamic = FlightRuntime.callValue(getRenderEffectInputs, cast ([effect] : Array<Dynamic>));
      for (input in FlightRuntime.iterable(required)) {
        if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.includes(available, input)))) {
  return cast input;
}
      }
    }
    return cast null;
    return cast null;
  }
}
