// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.PathApi.appendPathClose;
import flight.PathApi.appendPathLineTo;
import flight.PathApi.appendPathMoveTo;
import flight.PathApi.createPath;
import flight.PathApi.flattenPath;
import flight.Types.Path;
import flight.Types.PathBooleanBackend;
import flight.Types.PathBooleanContour;
import flight.Types.PathBooleanFillRule;
import flight.Types.PathBooleanOperation;
import flight.Types.PathBooleanOptions;
import flight.Types.PathOffsetEnd;
import flight.Types.PathOffsetJoin;
import flight.Types.PathOffsetOptions;

typedef ArrangementSegment__martinezKernel = { var ax:Float; var ay:Float; var bx:Float; var by:Float; var isSubject:Bool; var windingDelta:Float; };

@:expose("flight.DirectedGraph__martinezKernel")
class DirectedGraph__martinezKernel {
  private final keys:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []);
  private final xs:Array<Float> = cast ([] : Array<Dynamic>);
  private final ys:Array<Float> = cast ([] : Array<Dynamic>);
  private final outgoing:Array<Array<Float>> = cast ([] : Array<Dynamic>);
  private final edgeTo:Array<Float> = cast ([] : Array<Dynamic>);
  private final edgeFrom:Array<Float> = cast ([] : Array<Dynamic>);
  private final used:Array<Bool> = cast ([] : Array<Dynamic>);
  public function new():Void {
  }
  public function addEdge(fromX:Float, fromY:Float, toX:Float, toY:Float):Void {
    var from:Dynamic = cast FlightRuntime.UNDEFINED;
    var to:Dynamic = cast FlightRuntime.UNDEFINED;
    var edge:Dynamic = cast FlightRuntime.UNDEFINED;
    from = FlightRuntime.callProperty(this, 'vertex', cast ([fromX, fromY] : Array<Dynamic>));
    to = FlightRuntime.callProperty(this, 'vertex', cast ([toX, toY] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(from, to))) { return; }
    edge = FlightRuntime.field(FlightRuntime.field(this, 'edgeFrom'), 'length');
    FlightRuntime.callProperty(FlightRuntime.field(this, 'edgeFrom'), 'push', cast ([from] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(this, 'edgeTo'), 'push', cast ([to] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(this, 'used'), 'push', cast ([false] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.getIndex(FlightRuntime.field(this, 'outgoing'), from), 'push', cast ([edge] : Array<Dynamic>));
  }
  public function traceRings():Array<Array<Float>> {
    var rings:Array<Array<Float>> = cast FlightRuntime.UNDEFINED;
    rings = cast ([] : Array<Dynamic>);
    {
      var start:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(start, FlightRuntime.field(FlightRuntime.field(this, 'edgeFrom'), 'length'), '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.getIndex(FlightRuntime.field(this, 'used'), start))) { start++; continue; }
        var ring:Dynamic = FlightRuntime.callProperty(this, 'walk', cast ([start] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(ring, null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(ring, 'length'), 6.0, '>=')))) { FlightRuntime.callProperty(rings, 'push', cast ([ring] : Array<Dynamic>)); }
        start++;
      }
    }
    return cast rings;
    return cast null;
  }
  private function walk(startEdge:Float):Null<Array<Float>> {
    var ring:Array<Float> = cast FlightRuntime.UNDEFINED;
    var edge:Dynamic = cast FlightRuntime.UNDEFINED;
    var guard:Dynamic = cast FlightRuntime.UNDEFINED;
    var limit:Dynamic = cast FlightRuntime.UNDEFINED;
    ring = cast ([] : Array<Dynamic>);
    edge = startEdge;
    guard = 0.0;
    limit = (FlightRuntime.field(FlightRuntime.field(this, 'edgeFrom'), 'length') + 2.0);
    while (FlightRuntime.truthy(FlightRuntime.compare(guard++, limit, '<'))) {
  FlightRuntime.setIndex(FlightRuntime.field(this, 'used'), edge, true);
  var from:Dynamic = FlightRuntime.getIndex(FlightRuntime.field(this, 'edgeFrom'), edge);
  FlightRuntime.pushMany(ring, cast ([FlightRuntime.getIndex(FlightRuntime.field(this, 'xs'), from), FlightRuntime.getIndex(FlightRuntime.field(this, 'ys'), from)] : Array<Dynamic>));
  var to:Dynamic = FlightRuntime.getIndex(FlightRuntime.field(this, 'edgeTo'), edge);
  var next:Dynamic = FlightRuntime.callProperty(this, 'nextEdge', cast ([to, from] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(next, -1.0), function():Dynamic return cast FlightRuntime.strictEquals(next, startEdge)), function():Dynamic return cast FlightRuntime.getIndex(FlightRuntime.field(this, 'used'), next)))) { break; }
  (edge = cast (next : Dynamic));
}
    return cast ring;
    return cast null;
  }
  private function nextEdge(at:Float, cameFrom:Float):Float {
    var incomingAngle:Dynamic = cast FlightRuntime.UNDEFINED;
    var best:Dynamic = cast FlightRuntime.UNDEFINED;
    var bestGap:Dynamic = cast FlightRuntime.UNDEFINED;
    incomingAngle = FlightRuntime.callProperty(HxMath, 'atan2', cast ([(FlightRuntime.getIndex(FlightRuntime.field(this, 'ys'), cameFrom) - FlightRuntime.getIndex(FlightRuntime.field(this, 'ys'), at)), (FlightRuntime.getIndex(FlightRuntime.field(this, 'xs'), cameFrom) - FlightRuntime.getIndex(FlightRuntime.field(this, 'xs'), at))] : Array<Dynamic>));
    best = -1.0;
    bestGap = HxMath.POSITIVE_INFINITY;
    for (edge in FlightRuntime.iterable(FlightRuntime.getIndex(FlightRuntime.field(this, 'outgoing'), at))) {
      if (FlightRuntime.truthy(FlightRuntime.getIndex(FlightRuntime.field(this, 'used'), edge))) { continue; }
      var to:Dynamic = FlightRuntime.getIndex(FlightRuntime.field(this, 'edgeTo'), edge);
      var angle:Dynamic = FlightRuntime.callProperty(HxMath, 'atan2', cast ([(FlightRuntime.getIndex(FlightRuntime.field(this, 'ys'), to) - FlightRuntime.getIndex(FlightRuntime.field(this, 'ys'), at)), (FlightRuntime.getIndex(FlightRuntime.field(this, 'xs'), to) - FlightRuntime.getIndex(FlightRuntime.field(this, 'xs'), at))] : Array<Dynamic>));
      var gap:Dynamic = (incomingAngle - angle);
      while (FlightRuntime.truthy(FlightRuntime.compare(gap, 0.0, '<='))) { (gap = cast ((gap + (HxMath.PI * 2.0)) : Dynamic)); }
      while (FlightRuntime.truthy(FlightRuntime.compare(gap, (HxMath.PI * 2.0), '>'))) { (gap = cast ((gap - (HxMath.PI * 2.0)) : Dynamic)); }
      if (FlightRuntime.truthy(FlightRuntime.compare(gap, bestGap, '<'))) {
  (bestGap = cast (gap : Dynamic));
  (best = cast (edge : Dynamic));
}
    }
    return cast best;
    return cast null;
  }
  private function vertex(x:Float, y:Float):Float {
    var key:Dynamic = cast FlightRuntime.UNDEFINED;
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var index:Dynamic = cast FlightRuntime.UNDEFINED;
    key = '' + Std.string(FlightRuntime.callValue(PathBoolean.snap__martinezKernel, cast ([x] : Array<Dynamic>))) + ',' + Std.string(FlightRuntime.callValue(PathBoolean.snap__martinezKernel, cast ([y] : Array<Dynamic>))) + '';
    existing = FlightRuntime.callProperty(FlightRuntime.field(this, 'keys'), 'get', cast ([key] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED))) { return cast existing; }
    index = FlightRuntime.field(FlightRuntime.field(this, 'xs'), 'length');
    FlightRuntime.callProperty(FlightRuntime.field(this, 'keys'), 'set', cast ([key, index] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(this, 'xs'), 'push', cast ([x] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(this, 'ys'), 'push', cast ([y] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(this, 'outgoing'), 'push', cast ([cast ([] : Array<Dynamic>)] : Array<Dynamic>));
    return cast index;
    return cast null;
  }
}

@:expose("flight.EventHeap__martinezKernel")
class EventHeap__martinezKernel {
  private final data:Array<Dynamic> = cast ([] : Array<Dynamic>);
  public function new():Void {
  }
  public function isEmpty():Bool {
    return cast FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(this, 'data'), 'length'), 0.0);
    return cast null;
  }
  public function push(event:Dynamic):Void {
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    data = FlightRuntime.field(this, 'data');
    FlightRuntime.callProperty(data, 'push', cast ([event] : Array<Dynamic>));
    i = (FlightRuntime.field(data, 'length') - 1.0);
    while (FlightRuntime.truthy(FlightRuntime.compare(i, 0.0, '>'))) {
  var parent:Dynamic = (Std.int((i - 1.0)) >> Std.int(1.0));
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callValue(PathBoolean.compareEvents__martinezKernel, cast ([FlightRuntime.getIndex(data, i), FlightRuntime.getIndex(data, parent)] : Array<Dynamic>)), 0.0, '<'))) {
  FlightRuntime.callValue(PathBoolean.swap__martinezKernel, cast ([data, i, parent] : Array<Dynamic>));
  (i = cast (parent : Dynamic));
} else { break; }
}
  }
  public function pop():Dynamic {
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var top:Dynamic = cast FlightRuntime.UNDEFINED;
    var last:Dynamic = cast FlightRuntime.UNDEFINED;
    data = FlightRuntime.field(this, 'data');
    top = FlightRuntime.getIndex(data, 0.0);
    last = (cast FlightRuntime.callProperty(data, 'pop', cast ([] : Array<Dynamic>)) : Dynamic);
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(data, 'length'), 0.0, '>'))) {
  FlightRuntime.setIndex(data, 0.0, last);
  var i:Dynamic = 0.0;
  var n:Dynamic = FlightRuntime.field(data, 'length');
  {
    while (true) {
      var left:Dynamic = ((i * 2.0) + 1.0);
      var right:Dynamic = (left + 1.0);
      var smallest:Dynamic = i;
      if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(left, n, '<'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callValue(PathBoolean.compareEvents__martinezKernel, cast ([FlightRuntime.getIndex(data, left), FlightRuntime.getIndex(data, smallest)] : Array<Dynamic>)), 0.0, '<')))) { (smallest = cast (left : Dynamic)); }
      if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(right, n, '<'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callValue(PathBoolean.compareEvents__martinezKernel, cast ([FlightRuntime.getIndex(data, right), FlightRuntime.getIndex(data, smallest)] : Array<Dynamic>)), 0.0, '<')))) { (smallest = cast (right : Dynamic)); }
      if (FlightRuntime.truthy(FlightRuntime.strictEquals(smallest, i))) { break; }
      FlightRuntime.callValue(PathBoolean.swap__martinezKernel, cast ([data, i, smallest] : Array<Dynamic>));
      (i = cast (smallest : Dynamic));
    }
  }
}
    return cast top;
    return cast null;
  }
}

typedef SweepEvent__martinezKernel = { var x:Float; var y:Float; var left:Bool; var otherEvent:Dynamic; var isSubject:Bool; var id:Float; var windingDelta:Float; };

typedef UniqueSegment__martinezKernel = { var ax:Float; var ay:Float; var bx:Float; var by:Float; var subjectDelta:Float; var clipDelta:Float; };

@:expose("flight.PathBoolean")
class PathBoolean {
  public static var _backend__pathBooleanBackend:Null<PathBooleanBackend> = FlightRuntime.explicitNull();

  public static function addEdge__martinezKernel(ax:Float, ay:Float, bx:Float, by:Float, isSubject:Bool, heap:EventHeap__martinezKernel):Void {
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    a = FlightRuntime.callValue(PathBoolean.createEvent__martinezKernel, cast ([ax, ay, isSubject] : Array<Dynamic>));
    b = FlightRuntime.callValue(PathBoolean.createEvent__martinezKernel, cast ([bx, by, isSubject] : Array<Dynamic>));
    FlightRuntime.setField(a, 'otherEvent', b);
    FlightRuntime.setField(b, 'otherEvent', a);
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callValue(PathBoolean.pointOrder__martinezKernel, cast ([ax, ay, bx, by] : Array<Dynamic>)), 0.0, '<'))) {
  FlightRuntime.setField(a, 'left', true);
  FlightRuntime.setField(a, 'windingDelta', 1.0);
} else {
  FlightRuntime.setField(b, 'left', true);
  FlightRuntime.setField(b, 'windingDelta', -1.0);
}
    FlightRuntime.callProperty(heap, 'push', cast ([a] : Array<Dynamic>));
    FlightRuntime.callProperty(heap, 'push', cast ([b] : Array<Dynamic>));
  }

  public static function approxEqual__martinezKernel(a:Float, b:Float):Bool {
    return cast FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([(a - b)] : Array<Dynamic>)), PathBoolean.vertexSnap__martinezKernel, '<=');
    return cast null;
  }

  public static function booleanPaths(subject:Path, clip:Path, operation:PathBooleanOperation, ?out:Path, ?options:PathBooleanOptions):Path {
    var fillRule:Dynamic = cast FlightRuntime.UNDEFINED;
    var subjectContours:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipContours:Dynamic = cast FlightRuntime.UNDEFINED;
    var result:Dynamic = cast FlightRuntime.UNDEFINED;
    fillRule = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'fillRule'), function():Dynamic return cast 'nonZero');
    subjectContours = FlightRuntime.callValue(flattenPath, cast ([subject, FlightRuntime.optionalField(options, 'tolerance')] : Array<Dynamic>));
    clipContours = FlightRuntime.callValue(flattenPath, cast ([clip, FlightRuntime.optionalField(options, 'tolerance')] : Array<Dynamic>));
    result = FlightRuntime.callProperty(FlightRuntime.callValue(getPathBooleanBackend, cast ([] : Array<Dynamic>)), 'computePathBoolean', cast ([subjectContours, clipContours, operation, fillRule] : Array<Dynamic>));
    return cast FlightRuntime.callValue(PathBoolean.writeContours__booleanPaths, cast ([result, out] : Array<Dynamic>));
    return cast null;
  }

  public static function buildArrangement__martinezKernel(subject:Array<PathBooleanContour>, clip:Array<PathBooleanContour>):Array<ArrangementSegment__martinezKernel> {
    var heap:Dynamic = cast FlightRuntime.UNDEFINED;
    var status:Array<Dynamic> = cast FlightRuntime.UNDEFINED;
    var segments:Array<ArrangementSegment__martinezKernel> = cast FlightRuntime.UNDEFINED;
    heap = new EventHeap__martinezKernel();
    FlightRuntime.callValue(PathBoolean.fillQueue__martinezKernel, cast ([subject, true, heap] : Array<Dynamic>));
    FlightRuntime.callValue(PathBoolean.fillQueue__martinezKernel, cast ([clip, false, heap] : Array<Dynamic>));
    status = cast ([] : Array<Dynamic>);
    segments = cast ([] : Array<Dynamic>);
    while (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callProperty(heap, 'isEmpty', cast ([] : Array<Dynamic>))))) {
  var event:Dynamic = FlightRuntime.callProperty(heap, 'pop', cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.field(event, 'left'))) {
  var index:Dynamic = FlightRuntime.callValue(PathBoolean.insertStatus__martinezKernel, cast ([status, event] : Array<Dynamic>));
  var prev:Dynamic = FlightRuntime.select(FlightRuntime.compare(index, 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(status, (index - 1.0)), function():Dynamic return cast null);
  var next:Dynamic = FlightRuntime.select(FlightRuntime.compare(index, (FlightRuntime.field(status, 'length') - 1.0), '<'), function():Dynamic return cast FlightRuntime.getIndex(status, (index + 1.0)), function():Dynamic return cast null);
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(next, null))) { FlightRuntime.callValue(PathBoolean.possibleIntersection__martinezKernel, cast ([event, next, heap] : Array<Dynamic>)); }
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(prev, null))) { FlightRuntime.callValue(PathBoolean.possibleIntersection__martinezKernel, cast ([prev, event, heap] : Array<Dynamic>)); }
} else {
  var leftEvent:Dynamic = FlightRuntime.field(event, 'otherEvent');
  var index:Dynamic = FlightRuntime.callValue(PathBoolean.findStatus__martinezKernel, cast ([status, leftEvent] : Array<Dynamic>));
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(index, -1.0))) {
  var prev:Dynamic = FlightRuntime.select(FlightRuntime.compare(index, 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(status, (index - 1.0)), function():Dynamic return cast null);
  var next:Dynamic = FlightRuntime.select(FlightRuntime.compare(index, (FlightRuntime.field(status, 'length') - 1.0), '<'), function():Dynamic return cast FlightRuntime.getIndex(status, (index + 1.0)), function():Dynamic return cast null);
  FlightRuntime.splice(status, Std.int(index), Std.int(1.0), []);
  if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(prev, null), function():Dynamic return cast !FlightRuntime.strictEquals(next, null)))) { FlightRuntime.callValue(PathBoolean.possibleIntersection__martinezKernel, cast ([prev, next, heap] : Array<Dynamic>)); }
}
  FlightRuntime.callValue(PathBoolean.recordSegment__martinezKernel, cast ([leftEvent, event, segments] : Array<Dynamic>));
}
}
    return cast segments;
    return cast null;
  }

  public static function buildOffsetRing__offsetPath(vertices:Array<Float>, signedDelta:Float, capIndices:Dynamic, join:PathOffsetJoin, end:PathOffsetEnd, miterLimit:Float, arcTolerance:Float):Array<Float> {
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var dirX:Dynamic = cast FlightRuntime.UNDEFINED;
    var dirY:Dynamic = cast FlightRuntime.UNDEFINED;
    var normalX:Dynamic = cast FlightRuntime.UNDEFINED;
    var normalY:Dynamic = cast FlightRuntime.UNDEFINED;
    var ring:Array<Float> = cast FlightRuntime.UNDEFINED;
    count = (FlightRuntime.field(vertices, 'length') / 2.0);
    dirX = FlightRuntime.createArray(count);
    dirY = FlightRuntime.createArray(count);
    normalX = FlightRuntime.createArray(count);
    normalY = FlightRuntime.createArray(count);
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, count, '<'))) {
        var kn:Dynamic = ((k + 1.0) % count);
        var dx:Dynamic = (FlightRuntime.getIndex(vertices, (2.0 * kn)) - FlightRuntime.getIndex(vertices, (2.0 * k)));
        var dy:Dynamic = (FlightRuntime.getIndex(vertices, ((2.0 * kn) + 1.0)) - FlightRuntime.getIndex(vertices, ((2.0 * k) + 1.0)));
        var length:Dynamic = FlightRuntime.hypot(dx, dy);
        var inverse:Dynamic = FlightRuntime.select(FlightRuntime.compare(length, 0.0, '>'), function():Dynamic return cast (1.0 / length), function():Dynamic return cast 0.0);
        FlightRuntime.setIndex(dirX, k, (dx * inverse));
        FlightRuntime.setIndex(dirY, k, (dy * inverse));
        FlightRuntime.setIndex(normalX, k, FlightRuntime.getIndex(dirY, k));
        FlightRuntime.setIndex(normalY, k, -FlightRuntime.getIndex(dirX, k));
        k++;
      }
    }
    ring = cast ([] : Array<Dynamic>);
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, count, '<'))) {
        var previous:Dynamic = (((k - 1.0) + count) % count);
        var vx:Dynamic = FlightRuntime.getIndex(vertices, (2.0 * k));
        var vy:Dynamic = FlightRuntime.getIndex(vertices, ((2.0 * k) + 1.0));
        var previousEndX:Dynamic = (vx + (signedDelta * FlightRuntime.getIndex(normalX, previous)));
        var previousEndY:Dynamic = (vy + (signedDelta * FlightRuntime.getIndex(normalY, previous)));
        var thisStartX:Dynamic = (vx + (signedDelta * FlightRuntime.getIndex(normalX, k)));
        var thisStartY:Dynamic = (vy + (signedDelta * FlightRuntime.getIndex(normalY, k)));
        if (FlightRuntime.truthy(FlightRuntime.callProperty(capIndices, 'has', cast ([k] : Array<Dynamic>)))) {
  FlightRuntime.callValue(PathBoolean.emitOffsetEndCap__offsetPath, cast ([ring, vx, vy, previousEndX, previousEndY, thisStartX, thisStartY, FlightRuntime.getIndex(dirX, previous), FlightRuntime.getIndex(dirY, previous), FlightRuntime.getIndex(dirX, k), FlightRuntime.getIndex(dirY, k), FlightRuntime.callProperty(HxMath, 'abs', cast ([signedDelta] : Array<Dynamic>)), end, arcTolerance] : Array<Dynamic>));
} else {
  FlightRuntime.callValue(PathBoolean.emitOffsetJoin__offsetPath, cast ([ring, vx, vy, previousEndX, previousEndY, thisStartX, thisStartY, FlightRuntime.getIndex(dirX, previous), FlightRuntime.getIndex(dirY, previous), FlightRuntime.getIndex(dirX, k), FlightRuntime.getIndex(dirY, k), signedDelta, join, miterLimit, arcTolerance] : Array<Dynamic>));
}
        k++;
      }
    }
    return cast ring;
    return cast null;
  }

  public static function classifySegments__martinezKernel(unique:Array<UniqueSegment__martinezKernel>, operation:PathBooleanOperation, fillRule:PathBooleanFillRule):Array<Array<Float>> {
    var kept:Array<Array<Float>> = cast FlightRuntime.UNDEFINED;
    kept = cast ([] : Array<Dynamic>);
    for (seg in FlightRuntime.iterable(unique)) {
      var mx:Dynamic = ((FlightRuntime.field(seg, 'ax') + FlightRuntime.field(seg, 'bx')) / 2.0);
      var my:Dynamic = ((FlightRuntime.field(seg, 'ay') + FlightRuntime.field(seg, 'by')) / 2.0);
      var dx:Dynamic = (FlightRuntime.field(seg, 'bx') - FlightRuntime.field(seg, 'ax'));
      var dy:Dynamic = (FlightRuntime.field(seg, 'by') - FlightRuntime.field(seg, 'ay'));
      var len:Dynamic = FlightRuntime.hypot(dx, dy);
      if (FlightRuntime.truthy(FlightRuntime.strictEquals(len, 0.0))) { continue; }
      var eps:Dynamic = (len * 0.0001);
      var nlx:Dynamic = ((-dy / len) * eps);
      var nly:Dynamic = ((dx / len) * eps);
      var leftInSubject:Dynamic = FlightRuntime.callValue(PathBoolean.isInside__martinezKernel, cast ([FlightRuntime.callValue(PathBoolean.windingAt__martinezKernel, cast ([unique, (mx + nlx), (my + nly), true] : Array<Dynamic>)), fillRule] : Array<Dynamic>));
      var leftInClip:Dynamic = FlightRuntime.callValue(PathBoolean.isInside__martinezKernel, cast ([FlightRuntime.callValue(PathBoolean.windingAt__martinezKernel, cast ([unique, (mx + nlx), (my + nly), false] : Array<Dynamic>)), fillRule] : Array<Dynamic>));
      var rightInSubject:Dynamic = FlightRuntime.callValue(PathBoolean.isInside__martinezKernel, cast ([FlightRuntime.callValue(PathBoolean.windingAt__martinezKernel, cast ([unique, (mx - nlx), (my - nly), true] : Array<Dynamic>)), fillRule] : Array<Dynamic>));
      var rightInClip:Dynamic = FlightRuntime.callValue(PathBoolean.isInside__martinezKernel, cast ([FlightRuntime.callValue(PathBoolean.windingAt__martinezKernel, cast ([unique, (mx - nlx), (my - nly), false] : Array<Dynamic>)), fillRule] : Array<Dynamic>));
      var filledLeft:Dynamic = FlightRuntime.callValue(PathBoolean.combine__martinezKernel, cast ([operation, leftInSubject, leftInClip] : Array<Dynamic>));
      var filledRight:Dynamic = FlightRuntime.callValue(PathBoolean.combine__martinezKernel, cast ([operation, rightInSubject, rightInClip] : Array<Dynamic>));
      if (FlightRuntime.truthy(FlightRuntime.strictEquals(filledLeft, filledRight))) { continue; }
      if (FlightRuntime.truthy(filledLeft)) { FlightRuntime.callProperty(kept, 'push', cast ([cast ([FlightRuntime.field(seg, 'ax'), FlightRuntime.field(seg, 'ay'), FlightRuntime.field(seg, 'bx'), FlightRuntime.field(seg, 'by')] : Array<Dynamic>)] : Array<Dynamic>)); } else { FlightRuntime.callProperty(kept, 'push', cast ([cast ([FlightRuntime.field(seg, 'bx'), FlightRuntime.field(seg, 'by'), FlightRuntime.field(seg, 'ax'), FlightRuntime.field(seg, 'ay')] : Array<Dynamic>)] : Array<Dynamic>)); }
    }
    return cast kept;
    return cast null;
  }

  public static function combine__martinezKernel(operation:PathBooleanOperation, inSubject:Bool, inClip:Bool):Bool {
    {
      var __switchValue = operation;
      if (__switchValue == 'union') {
        return cast FlightRuntime.orValue(inSubject, function():Dynamic return cast inClip);
      }
      else if (__switchValue == 'intersection') {
        return cast FlightRuntime.andValue(inSubject, function():Dynamic return cast inClip);
      }
      else if (__switchValue == 'difference') {
        return cast FlightRuntime.andValue(inSubject, function():Dynamic return cast !FlightRuntime.truthy(inClip));
      }
      else if (__switchValue == 'xor') {
        return cast !FlightRuntime.strictEquals(inSubject, inClip);
      }
    }
    return cast null;
  }

  public static function compareEvents__martinezKernel(e1:Dynamic, e2:Dynamic):Float {
    var area:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(e1, 'x'), FlightRuntime.field(e2, 'x')))) { return cast FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(e1, 'x'), FlightRuntime.field(e2, 'x'), '<'), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0); }
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(e1, 'y'), FlightRuntime.field(e2, 'y')))) { return cast FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(e1, 'y'), FlightRuntime.field(e2, 'y'), '<'), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0); }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(e1, e2))) { return cast 0.0; }
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(e1, 'left'), FlightRuntime.field(e2, 'left')))) { return cast FlightRuntime.select(FlightRuntime.field(e1, 'left'), function():Dynamic return cast 1.0, function():Dynamic return cast -1.0); }
    area = FlightRuntime.callValue(PathBoolean.signedArea__martinezKernel, cast ([FlightRuntime.field(e1, 'x'), FlightRuntime.field(e1, 'y'), FlightRuntime.field(FlightRuntime.field(e1, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(e1, 'otherEvent'), 'y'), FlightRuntime.field(FlightRuntime.field(e2, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(e2, 'otherEvent'), 'y')] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(area, 0.0))) { return cast FlightRuntime.select(FlightRuntime.callValue(PathBoolean.isBelow__martinezKernel, cast ([e1, FlightRuntime.field(FlightRuntime.field(e2, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(e2, 'otherEvent'), 'y')] : Array<Dynamic>)), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0); }
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(e1, 'isSubject'), FlightRuntime.field(e2, 'isSubject')))) { return cast FlightRuntime.select(FlightRuntime.field(e1, 'isSubject'), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0); }
    return cast FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(e1, 'id'), FlightRuntime.field(e2, 'id'), '<'), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0);
    return cast null;
  }

  public static function compareSegments__martinezKernel(le1:Dynamic, le2:Dynamic):Float {
    var a1x:Dynamic = cast FlightRuntime.UNDEFINED;
    var a1y:Dynamic = cast FlightRuntime.UNDEFINED;
    var a2x:Dynamic = cast FlightRuntime.UNDEFINED;
    var a2y:Dynamic = cast FlightRuntime.UNDEFINED;
    var s1:Dynamic = cast FlightRuntime.UNDEFINED;
    var s2:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(le1, le2))) { return cast 0.0; }
    a1x = FlightRuntime.field(le1, 'x');
    a1y = FlightRuntime.field(le1, 'y');
    a2x = FlightRuntime.field(FlightRuntime.field(le1, 'otherEvent'), 'x');
    a2y = FlightRuntime.field(FlightRuntime.field(le1, 'otherEvent'), 'y');
    s1 = FlightRuntime.callValue(PathBoolean.signedArea__martinezKernel, cast ([a1x, a1y, a2x, a2y, FlightRuntime.field(le2, 'x'), FlightRuntime.field(le2, 'y')] : Array<Dynamic>));
    s2 = FlightRuntime.callValue(PathBoolean.signedArea__martinezKernel, cast ([a1x, a1y, a2x, a2y, FlightRuntime.field(FlightRuntime.field(le2, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(le2, 'otherEvent'), 'y')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(!FlightRuntime.strictEquals(s1, 0.0), function():Dynamic return cast !FlightRuntime.strictEquals(s2, 0.0)))) {
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(a1x, FlightRuntime.field(le2, 'x')), function():Dynamic return cast FlightRuntime.strictEquals(a1y, FlightRuntime.field(le2, 'y'))))) { return cast FlightRuntime.select(FlightRuntime.callValue(PathBoolean.isBelow__martinezKernel, cast ([le1, FlightRuntime.field(FlightRuntime.field(le2, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(le2, 'otherEvent'), 'y')] : Array<Dynamic>)), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0); }
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(a1x, FlightRuntime.field(le2, 'x')))) { return cast FlightRuntime.select(FlightRuntime.compare(a1y, FlightRuntime.field(le2, 'y'), '<'), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0); }
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.callValue(PathBoolean.compareEvents__martinezKernel, cast ([le1, le2] : Array<Dynamic>)), 1.0))) { return cast FlightRuntime.select(FlightRuntime.callValue(PathBoolean.isBelow__martinezKernel, cast ([le2, a1x, a1y] : Array<Dynamic>)), function():Dynamic return cast 1.0, function():Dynamic return cast -1.0); }
  return cast FlightRuntime.select(FlightRuntime.callValue(PathBoolean.isBelow__martinezKernel, cast ([le1, FlightRuntime.field(le2, 'x'), FlightRuntime.field(le2, 'y')] : Array<Dynamic>)), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0);
}
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(le1, 'isSubject'), FlightRuntime.field(le2, 'isSubject')))) { return cast FlightRuntime.select(FlightRuntime.field(le1, 'isSubject'), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0); }
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(a1x, FlightRuntime.field(le2, 'x')), function():Dynamic return cast FlightRuntime.strictEquals(a1y, FlightRuntime.field(le2, 'y'))))) { return cast FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(le1, 'id'), FlightRuntime.field(le2, 'id'), '<'), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0); }
    return cast FlightRuntime.select(FlightRuntime.strictEquals(FlightRuntime.callValue(PathBoolean.compareEvents__martinezKernel, cast ([le1, le2] : Array<Dynamic>)), 1.0), function():Dynamic return cast 1.0, function():Dynamic return cast -1.0);
    return cast null;
  }

  public static function computeMartinezBoolean__martinezKernel(subject:Array<PathBooleanContour>, clip:Array<PathBooleanContour>, operation:PathBooleanOperation, fillRule:PathBooleanFillRule):Array<PathBooleanContour> {
    var segments:Dynamic = cast FlightRuntime.UNDEFINED;
    var unique:Dynamic = cast FlightRuntime.UNDEFINED;
    var kept:Dynamic = cast FlightRuntime.UNDEFINED;
    (PathBoolean.nextEventId__martinezKernel = cast (0.0 : Dynamic));
    (PathBoolean.vertexSnap__martinezKernel = cast (FlightRuntime.callValue(PathBoolean.computeVertexSnap__martinezKernel, cast ([subject, clip] : Array<Dynamic>)) : Dynamic));
    segments = FlightRuntime.callValue(PathBoolean.buildArrangement__martinezKernel, cast ([subject, clip] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segments, 'length'), 0.0))) { return cast cast ([] : Array<Dynamic>); }
    unique = FlightRuntime.callValue(PathBoolean.mergeCoincidentSegments__martinezKernel, cast ([segments] : Array<Dynamic>));
    kept = FlightRuntime.callValue(PathBoolean.classifySegments__martinezKernel, cast ([unique, operation, fillRule] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(kept, 'length'), 0.0))) { return cast cast ([] : Array<Dynamic>); }
    return cast FlightRuntime.callValue(PathBoolean.traceRings__martinezKernel, cast ([kept] : Array<Dynamic>));
    return cast null;
  }

  public static function computeVertexSnap__martinezKernel(subject:Array<PathBooleanContour>, clip:Array<PathBooleanContour>):Float {
    var minX:Dynamic = cast FlightRuntime.UNDEFINED;
    var minY:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxX:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxY:Dynamic = cast FlightRuntime.UNDEFINED;
    var extent:Dynamic = cast FlightRuntime.UNDEFINED;
    minX = HxMath.POSITIVE_INFINITY;
    minY = HxMath.POSITIVE_INFINITY;
    maxX = -HxMath.POSITIVE_INFINITY;
    maxY = -HxMath.POSITIVE_INFINITY;
    for (contours in FlightRuntime.iterable(cast ([subject, clip] : Array<Dynamic>))) {
      for (contour in FlightRuntime.iterable(contours)) {
        {
          var i:Dynamic = 0.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(contour, 'length'), '<'))) {
            var x:Dynamic = FlightRuntime.getIndex(contour, i);
            var y:Dynamic = FlightRuntime.getIndex(contour, (i + 1.0));
            if (FlightRuntime.truthy(FlightRuntime.compare(x, minX, '<'))) { (minX = cast (x : Dynamic)); }
            if (FlightRuntime.truthy(FlightRuntime.compare(x, maxX, '>'))) { (maxX = cast (x : Dynamic)); }
            if (FlightRuntime.truthy(FlightRuntime.compare(y, minY, '<'))) { (minY = cast (y : Dynamic)); }
            if (FlightRuntime.truthy(FlightRuntime.compare(y, maxY, '>'))) { (maxY = cast (y : Dynamic)); }
            (i = cast ((i + 2.0) : Dynamic));
          }
        }
      }
    }
    extent = FlightRuntime.callProperty(HxMath, 'max', cast ([(maxX - minX), (maxY - minY)] : Array<Dynamic>));
    return cast FlightRuntime.select(FlightRuntime.compare(extent, 0.0, '>'), function():Dynamic return cast (extent * PathBoolean.VERTEX_SNAP_RELATIVE__martinezKernel), function():Dynamic return cast PathBoolean.VERTEX_SNAP_FALLBACK__martinezKernel);
    return cast null;
  }

  public static function createDefaultPathBooleanBackend():PathBooleanBackend {
    return cast FlightRuntime.callValue(createMartinezPathBooleanBackend, cast ([] : Array<Dynamic>));
    return cast null;
  }

  public static function createEvent__martinezKernel(x:Float, y:Float, isSubject:Bool):Dynamic {
    return cast { x: x, y: y, left: false, otherEvent: (cast (cast null : Dynamic) : Dynamic), isSubject: isSubject, id: PathBoolean.nextEventId__martinezKernel++, windingDelta: 0.0 };
    return cast null;
  }

  public static function createMartinezPathBooleanBackend():PathBooleanBackend {
    return cast { computePathBoolean: function(subject:Array<PathBooleanContour>, clip:Array<PathBooleanContour>, operation:PathBooleanOperation, fillRule:PathBooleanFillRule) {
  return cast FlightRuntime.callValue(PathBoolean.computeMartinezBoolean__martinezKernel, cast ([subject, clip, operation, fillRule] : Array<Dynamic>));
} };
    return cast null;
  }

  public static final DEFAULT_ARC_TOLERANCE__offsetPath:Dynamic = 0.25;

  public static final DEFAULT_MITER_LIMIT__offsetPath:Dynamic = 2.0;

  public static function differencePaths(a:Path, b:Path, ?out:Path, ?options:PathBooleanOptions):Path {
    return cast FlightRuntime.callValue(booleanPaths, cast ([a, b, 'difference', out, options] : Array<Dynamic>));
    return cast null;
  }

  public static function divideIfInterior__martinezKernel(le:Dynamic, x:Float, y:Float, heap:EventHeap__martinezKernel):Void {
    if (FlightRuntime.truthy(FlightRuntime.callValue(PathBoolean.pointStrictlyInside__martinezKernel, cast ([FlightRuntime.field(le, 'x'), FlightRuntime.field(le, 'y'), FlightRuntime.field(FlightRuntime.field(le, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(le, 'otherEvent'), 'y'), x, y] : Array<Dynamic>)))) { FlightRuntime.callValue(PathBoolean.divideSegment__martinezKernel, cast ([le, x, y, heap] : Array<Dynamic>)); }
  }

  public static function divideSegment__martinezKernel(le:Dynamic, x:Float, y:Float, heap:EventHeap__martinezKernel):Void {
    var right:Dynamic = cast FlightRuntime.UNDEFINED;
    var newRight:Dynamic = cast FlightRuntime.UNDEFINED;
    var newLeft:Dynamic = cast FlightRuntime.UNDEFINED;
    right = FlightRuntime.field(le, 'otherEvent');
    newRight = FlightRuntime.callValue(PathBoolean.createEvent__martinezKernel, cast ([x, y, FlightRuntime.field(le, 'isSubject')] : Array<Dynamic>));
    newLeft = FlightRuntime.callValue(PathBoolean.createEvent__martinezKernel, cast ([x, y, FlightRuntime.field(le, 'isSubject')] : Array<Dynamic>));
    FlightRuntime.setField(newRight, 'left', false);
    FlightRuntime.setField(newRight, 'otherEvent', le);
    FlightRuntime.setField(newLeft, 'left', true);
    FlightRuntime.setField(newLeft, 'otherEvent', right);
    FlightRuntime.setField(newLeft, 'windingDelta', FlightRuntime.field(le, 'windingDelta'));
    FlightRuntime.setField(le, 'otherEvent', newRight);
    FlightRuntime.setField(right, 'otherEvent', newLeft);
    FlightRuntime.callProperty(heap, 'push', cast ([newLeft] : Array<Dynamic>));
    FlightRuntime.callProperty(heap, 'push', cast ([newRight] : Array<Dynamic>));
  }

  public static function emitOffsetEndCap__offsetPath(ring:Array<Float>, vx:Float, vy:Float, previousEndX:Float, previousEndY:Float, thisStartX:Float, thisStartY:Float, previousDirX:Float, previousDirY:Float, thisDirX:Float, thisDirY:Float, radius:Float, end:PathOffsetEnd, arcTolerance:Float):Void {
    FlightRuntime.pushMany(ring, cast ([previousEndX, previousEndY] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(end, 'square'))) {
  FlightRuntime.pushMany(ring, cast ([(previousEndX + (radius * previousDirX)), (previousEndY + (radius * previousDirY))] : Array<Dynamic>));
  FlightRuntime.pushMany(ring, cast ([(thisStartX - (radius * thisDirX)), (thisStartY - (radius * thisDirY))] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(end, 'round'))) {
  var startAngle:Dynamic = FlightRuntime.callProperty(HxMath, 'atan2', cast ([(previousEndY - vy), (previousEndX - vx)] : Array<Dynamic>));
  var midX:Dynamic = FlightRuntime.callProperty(HxMath, 'cos', cast ([(startAngle + PathBoolean.HALF_PI__offsetPath)] : Array<Dynamic>));
  var midY:Dynamic = FlightRuntime.callProperty(HxMath, 'sin', cast ([(startAngle + PathBoolean.HALF_PI__offsetPath)] : Array<Dynamic>));
  var sweep:Dynamic = (FlightRuntime.select(FlightRuntime.compare(((midX * previousDirX) + (midY * previousDirY)), 0.0, '>='), function():Dynamic return cast 1.0, function():Dynamic return cast -1.0) * HxMath.PI);
  FlightRuntime.callValue(PathBoolean.pushOffsetArc__offsetPath, cast ([ring, vx, vy, radius, startAngle, sweep, arcTolerance] : Array<Dynamic>));
} }
    FlightRuntime.pushMany(ring, cast ([thisStartX, thisStartY] : Array<Dynamic>));
  }

  public static function emitOffsetJoin__offsetPath(ring:Array<Float>, vx:Float, vy:Float, previousEndX:Float, previousEndY:Float, thisStartX:Float, thisStartY:Float, previousDirX:Float, previousDirY:Float, thisDirX:Float, thisDirY:Float, signedDelta:Float, join:PathOffsetJoin, miterLimit:Float, arcTolerance:Float):Void {
    var turn:Dynamic = cast FlightRuntime.UNDEFINED;
    var convex:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.pushMany(ring, cast ([previousEndX, previousEndY] : Array<Dynamic>));
    turn = ((previousDirX * thisDirY) - (previousDirY * thisDirX));
    convex = FlightRuntime.compare((turn * signedDelta), 0.0, '>');
    if (FlightRuntime.truthy(!FlightRuntime.truthy(convex))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([turn] : Array<Dynamic>)), PathBoolean.PARALLEL_EPS__offsetPath, '>'))) {
  var advance:Dynamic = ((((thisStartX - previousEndX) * thisDirY) - ((thisStartY - previousEndY) * thisDirX)) / turn);
  FlightRuntime.pushMany(ring, cast ([(previousEndX + (advance * previousDirX)), (previousEndY + (advance * previousDirY))] : Array<Dynamic>));
}
} else {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(join, 'miter'))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([turn] : Array<Dynamic>)), PathBoolean.PARALLEL_EPS__offsetPath, '>'))) {
  var advance:Dynamic = ((((thisStartX - previousEndX) * thisDirY) - ((thisStartY - previousEndY) * thisDirX)) / turn);
  var apexX:Dynamic = (previousEndX + (advance * previousDirX));
  var apexY:Dynamic = (previousEndY + (advance * previousDirY));
  var miterLength:Dynamic = FlightRuntime.hypot((apexX - vx), (apexY - vy));
  if (FlightRuntime.truthy(FlightRuntime.compare(miterLength, (miterLimit * FlightRuntime.callProperty(HxMath, 'abs', cast ([signedDelta] : Array<Dynamic>))), '<='))) { FlightRuntime.pushMany(ring, cast ([apexX, apexY] : Array<Dynamic>)); }
}
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(join, 'square'))) {
  var radius:Dynamic = FlightRuntime.callProperty(HxMath, 'abs', cast ([signedDelta] : Array<Dynamic>));
  FlightRuntime.pushMany(ring, cast ([(previousEndX + (radius * previousDirX)), (previousEndY + (radius * previousDirY))] : Array<Dynamic>));
  FlightRuntime.pushMany(ring, cast ([(thisStartX - (radius * thisDirX)), (thisStartY - (radius * thisDirY))] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(join, 'round'))) {
  var radius:Dynamic = FlightRuntime.callProperty(HxMath, 'abs', cast ([signedDelta] : Array<Dynamic>));
  var startAngle:Dynamic = FlightRuntime.callProperty(HxMath, 'atan2', cast ([(previousEndY - vy), (previousEndX - vx)] : Array<Dynamic>));
  var endAngle:Dynamic = FlightRuntime.callProperty(HxMath, 'atan2', cast ([(thisStartY - vy), (thisStartX - vx)] : Array<Dynamic>));
  FlightRuntime.callValue(PathBoolean.pushOffsetArc__offsetPath, cast ([ring, vx, vy, radius, startAngle, FlightRuntime.callValue(PathBoolean.getShortSweep__offsetPath, cast ([startAngle, endAngle] : Array<Dynamic>)), arcTolerance] : Array<Dynamic>));
} } }
}
    FlightRuntime.pushMany(ring, cast ([thisStartX, thisStartY] : Array<Dynamic>));
  }

  public static final EMPTY_CONTOURS__resolvePathRegions:Array<PathBooleanContour> = cast ([] : Array<Dynamic>);

  public static final EMPTY_CONTOURS__unionAllPaths:Array<PathBooleanContour> = cast ([] : Array<Dynamic>);

  public static function fillQueue__martinezKernel(contours:Array<PathBooleanContour>, isSubject:Bool, heap:EventHeap__martinezKernel):Void {
    for (contour in FlightRuntime.iterable(contours)) {
      var count:Dynamic = (Std.int(FlightRuntime.field(contour, 'length')) >> Std.int(1.0));
      if (FlightRuntime.truthy(FlightRuntime.compare(count, 2.0, '<'))) { continue; }
      if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([FlightRuntime.getIndex(contour, 0.0), FlightRuntime.getIndex(contour, ((count - 1.0) * 2.0))] : Array<Dynamic>)), function():Dynamic return cast FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([FlightRuntime.getIndex(contour, 1.0), FlightRuntime.getIndex(contour, (((count - 1.0) * 2.0) + 1.0))] : Array<Dynamic>))))) {
  (count = cast ((count - 1.0) : Dynamic));
}
      if (FlightRuntime.truthy(FlightRuntime.compare(count, 3.0, '<'))) { continue; }
      {
        var i:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, count, '<'))) {
          var ax:Dynamic = FlightRuntime.getIndex(contour, (i * 2.0));
          var ay:Dynamic = FlightRuntime.getIndex(contour, ((i * 2.0) + 1.0));
          var j:Dynamic = ((i + 1.0) % count);
          var bx:Dynamic = FlightRuntime.getIndex(contour, (j * 2.0));
          var by:Dynamic = FlightRuntime.getIndex(contour, ((j * 2.0) + 1.0));
          if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([ax, bx] : Array<Dynamic>)), function():Dynamic return cast FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([ay, by] : Array<Dynamic>))))) { i++; continue; }
          FlightRuntime.callValue(PathBoolean.addEdge__martinezKernel, cast ([ax, ay, bx, by, isSubject, heap] : Array<Dynamic>));
          i++;
        }
      }
    }
  }

  public static function findStatus__martinezKernel(status:Array<Dynamic>, event:Dynamic):Float {
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(status, 'length'), '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.getIndex(status, i), event))) { return cast i; }
        i++;
      }
    }
    return cast -1.0;
    return cast null;
  }

  public static function getCleanContourVertices__offsetPath(contour:Array<Float>, closed:Bool, pointEpsSq:Float):Array<Float> {
    var out:Array<Float> = cast FlightRuntime.UNDEFINED;
    out = cast ([] : Array<Dynamic>);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(contour, 'length'), '<'))) {
        var x:Dynamic = FlightRuntime.getIndex(contour, i);
        var y:Dynamic = FlightRuntime.getIndex(contour, (i + 1.0));
        if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(out, 'length'), 2.0, '>='))) {
  var dx:Dynamic = (x - FlightRuntime.getIndex(out, (FlightRuntime.field(out, 'length') - 2.0)));
  var dy:Dynamic = (y - FlightRuntime.getIndex(out, (FlightRuntime.field(out, 'length') - 1.0)));
  if (FlightRuntime.truthy(FlightRuntime.compare(((dx * dx) + (dy * dy)), pointEpsSq, '<='))) { (i = cast ((i + 2.0) : Dynamic)); continue; }
}
        FlightRuntime.pushMany(out, cast ([x, y] : Array<Dynamic>));
        (i = cast ((i + 2.0) : Dynamic));
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.andValue(closed, function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(out, 'length'), 4.0, '>=')))) {
  var dx:Dynamic = (FlightRuntime.getIndex(out, 0.0) - FlightRuntime.getIndex(out, (FlightRuntime.field(out, 'length') - 2.0)));
  var dy:Dynamic = (FlightRuntime.getIndex(out, 1.0) - FlightRuntime.getIndex(out, (FlightRuntime.field(out, 'length') - 1.0)));
  if (FlightRuntime.truthy(FlightRuntime.compare(((dx * dx) + (dy * dy)), pointEpsSq, '<='))) { FlightRuntime.setLength(out, (out.length - 2.0)); }
}
    return cast out;
    return cast null;
  }

  public static function getContourPointEps__offsetPath(contours:Array<Array<Float>>):Float {
    var minX:Dynamic = cast FlightRuntime.UNDEFINED;
    var minY:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxX:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxY:Dynamic = cast FlightRuntime.UNDEFINED;
    var extent:Dynamic = cast FlightRuntime.UNDEFINED;
    minX = HxMath.POSITIVE_INFINITY;
    minY = HxMath.POSITIVE_INFINITY;
    maxX = -HxMath.POSITIVE_INFINITY;
    maxY = -HxMath.POSITIVE_INFINITY;
    for (contour in FlightRuntime.iterable(contours)) {
      {
        var i:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(contour, 'length'), '<'))) {
          var x:Dynamic = FlightRuntime.getIndex(contour, i);
          var y:Dynamic = FlightRuntime.getIndex(contour, (i + 1.0));
          if (FlightRuntime.truthy(FlightRuntime.compare(x, minX, '<'))) { (minX = cast (x : Dynamic)); }
          if (FlightRuntime.truthy(FlightRuntime.compare(x, maxX, '>'))) { (maxX = cast (x : Dynamic)); }
          if (FlightRuntime.truthy(FlightRuntime.compare(y, minY, '<'))) { (minY = cast (y : Dynamic)); }
          if (FlightRuntime.truthy(FlightRuntime.compare(y, maxY, '>'))) { (maxY = cast (y : Dynamic)); }
          (i = cast ((i + 2.0) : Dynamic));
        }
      }
    }
    extent = FlightRuntime.callProperty(HxMath, 'max', cast ([(maxX - minX), (maxY - minY)] : Array<Dynamic>));
    return cast FlightRuntime.select(FlightRuntime.compare(extent, 0.0, '>'), function():Dynamic return cast (extent * PathBoolean.POINT_EPS_RELATIVE__offsetPath), function():Dynamic return cast PathBoolean.POINT_EPS__offsetPath);
    return cast null;
  }

  public static function getOpenContourLoop__offsetPath(vertices:Array<Float>):Array<Float> {
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var loop:Dynamic = cast FlightRuntime.UNDEFINED;
    count = (FlightRuntime.field(vertices, 'length') / 2.0);
    loop = FlightRuntime.slice(vertices, 0, null);
    {
      var k:Dynamic = (count - 2.0);
      while (FlightRuntime.truthy(FlightRuntime.compare(k, 1.0, '>='))) {
        FlightRuntime.pushMany(loop, cast ([FlightRuntime.getIndex(vertices, (2.0 * k)), FlightRuntime.getIndex(vertices, ((2.0 * k) + 1.0))] : Array<Dynamic>));
        k--;
      }
    }
    return cast loop;
    return cast null;
  }

  public static function getPathBooleanBackend():PathBooleanBackend {
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(PathBoolean._backend__pathBooleanBackend, null))) { (PathBoolean._backend__pathBooleanBackend = cast (FlightRuntime.callValue(createDefaultPathBooleanBackend, cast ([] : Array<Dynamic>)) : Dynamic)); }
    return cast PathBoolean._backend__pathBooleanBackend;
    return cast null;
  }

  public static function getRingOrientationSign__offsetPath(vertices:Array<Float>):Float {
    var sign:Dynamic = cast FlightRuntime.UNDEFINED;
    sign = FlightRuntime.sign(FlightRuntime.callValue(PathBoolean.getRingSignedArea__offsetPath, cast ([vertices] : Array<Dynamic>)));
    return cast FlightRuntime.select(FlightRuntime.strictEquals(sign, 0.0), function():Dynamic return cast 1.0, function():Dynamic return cast sign);
    return cast null;
  }

  public static function getRingSignedArea__offsetPath(vertices:Array<Float>):Float {
    var area:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    area = 0.0;
    count = (FlightRuntime.field(vertices, 'length') / 2.0);
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, count, '<'))) {
        var kn:Dynamic = ((k + 1.0) % count);
        (area = cast ((area + ((FlightRuntime.getIndex(vertices, (2.0 * k)) * FlightRuntime.getIndex(vertices, ((2.0 * kn) + 1.0))) - (FlightRuntime.getIndex(vertices, (2.0 * kn)) * FlightRuntime.getIndex(vertices, ((2.0 * k) + 1.0))))) : Dynamic));
        k++;
      }
    }
    return cast (area * 0.5);
    return cast null;
  }

  public static function getShortSweep__offsetPath(startAngle:Float, endAngle:Float):Float {
    var sweep:Dynamic = cast FlightRuntime.UNDEFINED;
    sweep = (endAngle - startAngle);
    while (FlightRuntime.truthy(FlightRuntime.compare(sweep, -HxMath.PI, '<='))) { (sweep = cast ((sweep + PathBoolean.TWO_PI__offsetPath) : Dynamic)); }
    while (FlightRuntime.truthy(FlightRuntime.compare(sweep, HxMath.PI, '>'))) { (sweep = cast ((sweep - PathBoolean.TWO_PI__offsetPath) : Dynamic)); }
    return cast sweep;
    return cast null;
  }

  public static final HALF_PI__offsetPath:Dynamic = (HxMath.PI / 2.0);

  public static function insertStatus__martinezKernel(status:Array<Dynamic>, event:Dynamic):Float {
    var lo:Dynamic = cast FlightRuntime.UNDEFINED;
    var hi:Dynamic = cast FlightRuntime.UNDEFINED;
    lo = 0.0;
    hi = FlightRuntime.field(status, 'length');
    while (FlightRuntime.truthy(FlightRuntime.compare(lo, hi, '<'))) {
  var mid:Dynamic = (Std.int((lo + hi)) >> Std.int(1.0));
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callValue(PathBoolean.compareSegments__martinezKernel, cast ([FlightRuntime.getIndex(status, mid), event] : Array<Dynamic>)), 0.0, '<'))) { (lo = cast ((mid + 1.0) : Dynamic)); } else { (hi = cast (mid : Dynamic)); }
}
    FlightRuntime.splice(status, Std.int(lo), Std.int(0.0), [event]);
    return cast lo;
    return cast null;
  }

  public static final INTERSECTION_EPS__martinezKernel:Dynamic = 1e-12;

  public static function intersectPaths(a:Path, b:Path, ?out:Path, ?options:PathBooleanOptions):Path {
    return cast FlightRuntime.callValue(booleanPaths, cast ([a, b, 'intersection', out, options] : Array<Dynamic>));
    return cast null;
  }

  public static function isBelow__martinezKernel(le:Dynamic, x:Float, y:Float):Bool {
    return cast FlightRuntime.select(FlightRuntime.field(le, 'left'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callValue(PathBoolean.signedArea__martinezKernel, cast ([FlightRuntime.field(le, 'x'), FlightRuntime.field(le, 'y'), FlightRuntime.field(FlightRuntime.field(le, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(le, 'otherEvent'), 'y'), x, y] : Array<Dynamic>)), 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callValue(PathBoolean.signedArea__martinezKernel, cast ([FlightRuntime.field(FlightRuntime.field(le, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(le, 'otherEvent'), 'y'), FlightRuntime.field(le, 'x'), FlightRuntime.field(le, 'y'), x, y] : Array<Dynamic>)), 0.0, '>'));
    return cast null;
  }

  public static function isClosedContour__offsetPath(contour:Array<Float>, pointEpsSq:Float):Bool {
    var n:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    n = FlightRuntime.field(contour, 'length');
    if (FlightRuntime.truthy(FlightRuntime.compare(n, 6.0, '<'))) { return cast false; }
    dx = (FlightRuntime.getIndex(contour, 0.0) - FlightRuntime.getIndex(contour, (n - 2.0)));
    dy = (FlightRuntime.getIndex(contour, 1.0) - FlightRuntime.getIndex(contour, (n - 1.0)));
    return cast FlightRuntime.compare(((dx * dx) + (dy * dy)), pointEpsSq, '<=');
    return cast null;
  }

  public static function isInside__martinezKernel(winding:Float, fillRule:PathBooleanFillRule):Bool {
    {
      var __switchValue = fillRule;
      if (__switchValue == 'evenOdd') {
        return cast FlightRuntime.strictEquals((Std.int(FlightRuntime.callProperty(HxMath, 'abs', cast ([winding] : Array<Dynamic>))) & Std.int(1.0)), 1.0);
      }
      else if (__switchValue == 'positive') {
        return cast FlightRuntime.compare(winding, 0.0, '>');
      }
      else if (__switchValue == 'negative') {
        return cast FlightRuntime.compare(winding, 0.0, '<');
      }
      else  {
        return cast !FlightRuntime.strictEquals(winding, 0.0);
      }
    }
    return cast null;
  }

  public static function mergeCoincidentSegments__martinezKernel(segments:Array<ArrangementSegment__martinezKernel>):Array<UniqueSegment__martinezKernel> {
    var map:Dynamic = cast FlightRuntime.UNDEFINED;
    map = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []);
    for (seg in FlightRuntime.iterable(segments)) {
      var ax:Dynamic = FlightRuntime.field(seg, 'ax');
      var ay:Dynamic = FlightRuntime.field(seg, 'ay');
      var bx:Dynamic = FlightRuntime.field(seg, 'bx');
      var by:Dynamic = FlightRuntime.field(seg, 'by');
      if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callValue(PathBoolean.pointOrder__martinezKernel, cast ([ax, ay, bx, by] : Array<Dynamic>)), 0.0, '>'))) {
  (ax = cast (FlightRuntime.field(seg, 'bx') : Dynamic));
  (ay = cast (FlightRuntime.field(seg, 'by') : Dynamic));
  (bx = cast (FlightRuntime.field(seg, 'ax') : Dynamic));
  (by = cast (FlightRuntime.field(seg, 'ay') : Dynamic));
}
      var key:Dynamic = '' + Std.string(FlightRuntime.callValue(PathBoolean.snap__martinezKernel, cast ([ax] : Array<Dynamic>))) + ',' + Std.string(FlightRuntime.callValue(PathBoolean.snap__martinezKernel, cast ([ay] : Array<Dynamic>))) + ',' + Std.string(FlightRuntime.callValue(PathBoolean.snap__martinezKernel, cast ([bx] : Array<Dynamic>))) + ',' + Std.string(FlightRuntime.callValue(PathBoolean.snap__martinezKernel, cast ([by] : Array<Dynamic>))) + '';
      var unique:Dynamic = FlightRuntime.callProperty(map, 'get', cast ([key] : Array<Dynamic>));
      if (FlightRuntime.truthy(FlightRuntime.strictEquals(unique, FlightRuntime.UNDEFINED))) {
  (unique = cast ({ ax: ax, ay: ay, bx: bx, by: by, subjectDelta: 0.0, clipDelta: 0.0 } : Dynamic));
  FlightRuntime.callProperty(map, 'set', cast ([key, unique] : Array<Dynamic>));
}
      if (FlightRuntime.truthy(FlightRuntime.field(seg, 'isSubject'))) { FlightRuntime.setField(unique, 'subjectDelta', (FlightRuntime.field(unique, 'subjectDelta') + FlightRuntime.field(seg, 'windingDelta'))); } else { FlightRuntime.setField(unique, 'clipDelta', (FlightRuntime.field(unique, 'clipDelta') + FlightRuntime.field(seg, 'windingDelta'))); }
    }
    return cast FlightRuntime.concatArrays([FlightRuntime.toArray(FlightRuntime.callProperty(map, 'values', cast ([] : Array<Dynamic>)))]);
    return cast null;
  }

  public static var nextEventId__martinezKernel:Dynamic = 0.0;

  public static final NO_CAPS__offsetPath:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Set'] : Array<Dynamic>)), []);

  public static function offsetPath(path:Path, delta:Float, ?options:PathOffsetOptions):Path {
    var join:Dynamic = cast FlightRuntime.UNDEFINED;
    var end:Dynamic = cast FlightRuntime.UNDEFINED;
    var miterLimit:Dynamic = cast FlightRuntime.UNDEFINED;
    var arcTolerance:Dynamic = cast FlightRuntime.UNDEFINED;
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var pointEpsSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var rawRings:Array<Array<Float>> = cast FlightRuntime.UNDEFINED;
    join = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'join'), function():Dynamic return cast 'miter');
    end = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'end'), function():Dynamic return cast 'butt');
    miterLimit = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'miterLimit'), function():Dynamic return cast PathBoolean.DEFAULT_MITER_LIMIT__offsetPath);
    arcTolerance = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'arcTolerance'), function():Dynamic return cast PathBoolean.DEFAULT_ARC_TOLERANCE__offsetPath);
    contours = FlightRuntime.callValue(flattenPath, cast ([path, FlightRuntime.optionalField(options, 'tolerance')] : Array<Dynamic>));
    pointEpsSq = HxMath.pow(FlightRuntime.callValue(PathBoolean.getContourPointEps__offsetPath, cast ([contours] : Array<Dynamic>)), 2.0);
    rawRings = cast ([] : Array<Dynamic>);
    for (contour in FlightRuntime.iterable(contours)) {
      var closed:Dynamic = FlightRuntime.callValue(PathBoolean.isClosedContour__offsetPath, cast ([contour, pointEpsSq] : Array<Dynamic>));
      var vertices:Dynamic = FlightRuntime.callValue(PathBoolean.getCleanContourVertices__offsetPath, cast ([contour, closed, pointEpsSq] : Array<Dynamic>));
      if (FlightRuntime.truthy(closed)) {
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(vertices, 'length'), 6.0, '<'))) { continue; }
  var orientation:Dynamic = FlightRuntime.callValue(PathBoolean.getRingOrientationSign__offsetPath, cast ([vertices] : Array<Dynamic>));
  var ring:Dynamic = FlightRuntime.callValue(PathBoolean.buildOffsetRing__offsetPath, cast ([vertices, (delta * orientation), PathBoolean.NO_CAPS__offsetPath, join, end, miterLimit, arcTolerance] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(ring, 'length'), 6.0, '<'))) { continue; }
  var offsetArea:Dynamic = FlightRuntime.callValue(PathBoolean.getRingSignedArea__offsetPath, cast ([ring] : Array<Dynamic>));
  var inverted:Dynamic = !FlightRuntime.strictEquals(FlightRuntime.sign(offsetArea), orientation);
  var notReduced:Dynamic = FlightRuntime.andValue(FlightRuntime.compare(delta, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([offsetArea] : Array<Dynamic>)), FlightRuntime.callProperty(HxMath, 'abs', cast ([FlightRuntime.callValue(PathBoolean.getRingSignedArea__offsetPath, cast ([vertices] : Array<Dynamic>))] : Array<Dynamic>)), '>='));
  if (FlightRuntime.truthy(FlightRuntime.orValue(inverted, function():Dynamic return cast notReduced))) { continue; }
  FlightRuntime.callProperty(rawRings, 'push', cast ([FlightRuntime.select(FlightRuntime.compare(offsetArea, 0.0, '<'), function():Dynamic return cast FlightRuntime.callValue(PathBoolean.reverseVertexLoop__offsetPath, cast ([ring] : Array<Dynamic>)), function():Dynamic return cast ring)] : Array<Dynamic>));
} else {
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(vertices, 'length'), 4.0, '<'))) { continue; }
  var caps:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Set'] : Array<Dynamic>)), [cast ([0.0, ((FlightRuntime.field(vertices, 'length') / 2.0) - 1.0)] : Array<Dynamic>)]);
  var loop:Dynamic = FlightRuntime.callValue(PathBoolean.getOpenContourLoop__offsetPath, cast ([vertices] : Array<Dynamic>));
  var ring:Dynamic = FlightRuntime.callValue(PathBoolean.buildOffsetRing__offsetPath, cast ([loop, FlightRuntime.callProperty(HxMath, 'abs', cast ([delta] : Array<Dynamic>)), caps, join, end, miterLimit, arcTolerance] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(ring, 'length'), 6.0, '>='))) { FlightRuntime.callProperty(rawRings, 'push', cast ([FlightRuntime.select(FlightRuntime.compare(FlightRuntime.callValue(PathBoolean.getRingSignedArea__offsetPath, cast ([ring] : Array<Dynamic>)), 0.0, '<'), function():Dynamic return cast FlightRuntime.callValue(PathBoolean.reverseVertexLoop__offsetPath, cast ([ring] : Array<Dynamic>)), function():Dynamic return cast ring)] : Array<Dynamic>)); }
}
    }
    return cast FlightRuntime.callValue(resolvePathRegions, cast ([rawRings, 'positive'] : Array<Dynamic>));
    return cast null;
  }

  public static final PARALLEL_EPS__offsetPath:Dynamic = 1e-12;

  public static final POINT_EPS__offsetPath:Dynamic = 1e-9;

  public static final POINT_EPS_RELATIVE__offsetPath:Dynamic = 1e-9;

  public static function pointOnEndpoint__martinezKernel(le:Dynamic, x:Float, y:Float):Bool {
    return cast FlightRuntime.orValue(FlightRuntime.andValue(FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([FlightRuntime.field(le, 'x'), x] : Array<Dynamic>)), function():Dynamic return cast FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([FlightRuntime.field(le, 'y'), y] : Array<Dynamic>))), function():Dynamic return cast FlightRuntime.andValue(FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([FlightRuntime.field(FlightRuntime.field(le, 'otherEvent'), 'x'), x] : Array<Dynamic>)), function():Dynamic return cast FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([FlightRuntime.field(FlightRuntime.field(le, 'otherEvent'), 'y'), y] : Array<Dynamic>))));
    return cast null;
  }

  public static function pointOrder__martinezKernel(ax:Float, ay:Float, bx:Float, by:Float):Float {
    if (FlightRuntime.truthy(FlightRuntime.compare(ax, bx, '<'))) { return cast -1.0; }
    if (FlightRuntime.truthy(FlightRuntime.compare(ax, bx, '>'))) { return cast 1.0; }
    if (FlightRuntime.truthy(FlightRuntime.compare(ay, by, '<'))) { return cast -1.0; }
    if (FlightRuntime.truthy(FlightRuntime.compare(ay, by, '>'))) { return cast 1.0; }
    return cast 0.0;
    return cast null;
  }

  public static function pointStrictlyInside__martinezKernel(lx:Float, ly:Float, rx:Float, ry:Float, x:Float, y:Float):Bool {
    var cross:Dynamic = cast FlightRuntime.UNDEFINED;
    var lenSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var dot:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.andValue(FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([lx, x] : Array<Dynamic>)), function():Dynamic return cast FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([ly, y] : Array<Dynamic>))), function():Dynamic return cast FlightRuntime.andValue(FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([rx, x] : Array<Dynamic>)), function():Dynamic return cast FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([ry, y] : Array<Dynamic>)))))) { return cast false; }
    cross = (((rx - lx) * (y - ly)) - ((ry - ly) * (x - lx)));
    lenSq = (((rx - lx) * (rx - lx)) + ((ry - ly) * (ry - ly)));
    if (FlightRuntime.truthy(FlightRuntime.compare((cross * cross), ((PathBoolean.INTERSECTION_EPS__martinezKernel * lenSq) * lenSq), '>'))) { return cast false; }
    dot = (((x - lx) * (rx - lx)) + ((y - ly) * (ry - ly)));
    return cast FlightRuntime.andValue(FlightRuntime.compare(dot, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(dot, lenSq, '<'));
    return cast null;
  }

  public static function possibleIntersection__martinezKernel(le1:Dynamic, le2:Dynamic, heap:EventHeap__martinezKernel):Void {
    var inter:Dynamic = cast FlightRuntime.UNDEFINED;
    inter = FlightRuntime.callValue(PathBoolean.segmentIntersection__martinezKernel, cast ([FlightRuntime.field(le1, 'x'), FlightRuntime.field(le1, 'y'), FlightRuntime.field(FlightRuntime.field(le1, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(le1, 'otherEvent'), 'y'), FlightRuntime.field(le2, 'x'), FlightRuntime.field(le2, 'y'), FlightRuntime.field(FlightRuntime.field(le2, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(le2, 'otherEvent'), 'y')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(inter, null))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(inter, 'length'), 1.0))) {
  var ix:Dynamic = FlightRuntime.getIndex(FlightRuntime.getIndex(inter, 0.0), 0.0);
  var iy:Dynamic = FlightRuntime.getIndex(FlightRuntime.getIndex(inter, 0.0), 1.0);
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(PathBoolean.pointOnEndpoint__martinezKernel, cast ([le1, ix, iy] : Array<Dynamic>))))) { FlightRuntime.callValue(PathBoolean.divideSegment__martinezKernel, cast ([le1, ix, iy, heap] : Array<Dynamic>)); }
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(PathBoolean.pointOnEndpoint__martinezKernel, cast ([le2, ix, iy] : Array<Dynamic>))))) { FlightRuntime.callValue(PathBoolean.divideSegment__martinezKernel, cast ([le2, ix, iy, heap] : Array<Dynamic>)); }
  return;
}
    FlightRuntime.callValue(PathBoolean.divideIfInterior__martinezKernel, cast ([le1, FlightRuntime.field(le2, 'x'), FlightRuntime.field(le2, 'y'), heap] : Array<Dynamic>));
    FlightRuntime.callValue(PathBoolean.divideIfInterior__martinezKernel, cast ([le1, FlightRuntime.field(FlightRuntime.field(le2, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(le2, 'otherEvent'), 'y'), heap] : Array<Dynamic>));
    FlightRuntime.callValue(PathBoolean.divideIfInterior__martinezKernel, cast ([le2, FlightRuntime.field(le1, 'x'), FlightRuntime.field(le1, 'y'), heap] : Array<Dynamic>));
    FlightRuntime.callValue(PathBoolean.divideIfInterior__martinezKernel, cast ([le2, FlightRuntime.field(FlightRuntime.field(le1, 'otherEvent'), 'x'), FlightRuntime.field(FlightRuntime.field(le1, 'otherEvent'), 'y'), heap] : Array<Dynamic>));
  }

  public static function pushOffsetArc__offsetPath(ring:Array<Float>, cx:Float, cy:Float, radius:Float, startAngle:Float, sweep:Float, arcTolerance:Float):Void {
    var maxAngle:Dynamic = cast FlightRuntime.UNDEFINED;
    var steps:Dynamic = cast FlightRuntime.UNDEFINED;
    maxAngle = FlightRuntime.select(FlightRuntime.andValue(FlightRuntime.compare(arcTolerance, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(arcTolerance, radius, '<')), function():Dynamic return cast (2.0 * FlightRuntime.callProperty(HxMath, 'acos', cast ([(1.0 - (arcTolerance / radius))] : Array<Dynamic>))), function():Dynamic return cast HxMath.PI);
    steps = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'ceil', cast ([(FlightRuntime.callProperty(HxMath, 'abs', cast ([sweep] : Array<Dynamic>)) / maxAngle)] : Array<Dynamic>))] : Array<Dynamic>));
    {
      var i:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, steps, '<'))) {
        var angle:Dynamic = (startAngle + (sweep * (i / steps)));
        FlightRuntime.pushMany(ring, cast ([(cx + (radius * FlightRuntime.callProperty(HxMath, 'cos', cast ([angle] : Array<Dynamic>)))), (cy + (radius * FlightRuntime.callProperty(HxMath, 'sin', cast ([angle] : Array<Dynamic>))))] : Array<Dynamic>));
        i++;
      }
    }
  }

  public static function recordSegment__martinezKernel(leftEvent:Dynamic, rightEvent:Dynamic, segments:Array<ArrangementSegment__martinezKernel>):Void {
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.field(leftEvent, 'left')))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([FlightRuntime.field(leftEvent, 'x'), FlightRuntime.field(rightEvent, 'x')] : Array<Dynamic>)), function():Dynamic return cast FlightRuntime.callValue(PathBoolean.approxEqual__martinezKernel, cast ([FlightRuntime.field(leftEvent, 'y'), FlightRuntime.field(rightEvent, 'y')] : Array<Dynamic>))))) { return; }
    FlightRuntime.callProperty(segments, 'push', cast ([{ ax: FlightRuntime.field(leftEvent, 'x'), ay: FlightRuntime.field(leftEvent, 'y'), bx: FlightRuntime.field(rightEvent, 'x'), by: FlightRuntime.field(rightEvent, 'y'), isSubject: FlightRuntime.field(leftEvent, 'isSubject'), windingDelta: FlightRuntime.field(leftEvent, 'windingDelta') }] : Array<Dynamic>));
  }

  public static function resolvePathRegions(rings:Array<PathBooleanContour>, fillRule:PathBooleanFillRule):Path {
    var path:Dynamic = cast FlightRuntime.UNDEFINED;
    var resolved:Dynamic = cast FlightRuntime.UNDEFINED;
    path = FlightRuntime.callValue(createPath, cast (['nonZero'] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(rings, 'length'), 0.0))) { return cast path; }
    resolved = FlightRuntime.callProperty(FlightRuntime.callValue(getPathBooleanBackend, cast ([] : Array<Dynamic>)), 'computePathBoolean', cast ([rings, PathBoolean.EMPTY_CONTOURS__resolvePathRegions, 'union', fillRule] : Array<Dynamic>));
    for (ring in FlightRuntime.iterable(resolved)) {
      if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(ring, 'length'), 6.0, '<'))) { continue; }
      FlightRuntime.callValue(appendPathMoveTo, cast ([path, FlightRuntime.getIndex(ring, 0.0), FlightRuntime.getIndex(ring, 1.0)] : Array<Dynamic>));
      {
        var i:Dynamic = 2.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(ring, 'length'), '<'))) {
          FlightRuntime.callValue(appendPathLineTo, cast ([path, FlightRuntime.getIndex(ring, i), FlightRuntime.getIndex(ring, (i + 1.0))] : Array<Dynamic>));
          (i = cast ((i + 2.0) : Dynamic));
        }
      }
      FlightRuntime.callValue(appendPathClose, cast ([path] : Array<Dynamic>));
    }
    return cast path;
    return cast null;
  }

  public static function reverseVertexLoop__offsetPath(vertices:Array<Float>):Array<Float> {
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var out:Dynamic = cast FlightRuntime.UNDEFINED;
    count = (FlightRuntime.field(vertices, 'length') / 2.0);
    out = FlightRuntime.createArray(FlightRuntime.field(vertices, 'length'));
    FlightRuntime.setIndex(out, 0.0, FlightRuntime.getIndex(vertices, 0.0));
    FlightRuntime.setIndex(out, 1.0, FlightRuntime.getIndex(vertices, 1.0));
    {
      var k:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, count, '<'))) {
        var source:Dynamic = (count - k);
        FlightRuntime.setIndex(out, (2.0 * k), FlightRuntime.getIndex(vertices, (2.0 * source)));
        FlightRuntime.setIndex(out, ((2.0 * k) + 1.0), FlightRuntime.getIndex(vertices, ((2.0 * source) + 1.0)));
        k++;
      }
    }
    return cast out;
    return cast null;
  }

  public static function segmentIntersection__martinezKernel(a1x:Float, a1y:Float, a2x:Float, a2y:Float, b1x:Float, b1y:Float, b2x:Float, b2y:Float):Null<Array<Array<Float>>> {
    var vax:Dynamic = cast FlightRuntime.UNDEFINED;
    var vay:Dynamic = cast FlightRuntime.UNDEFINED;
    var vbx:Dynamic = cast FlightRuntime.UNDEFINED;
    var vby:Dynamic = cast FlightRuntime.UNDEFINED;
    var ex:Dynamic = cast FlightRuntime.UNDEFINED;
    var ey:Dynamic = cast FlightRuntime.UNDEFINED;
    var kross:Dynamic = cast FlightRuntime.UNDEFINED;
    var sqrKross:Dynamic = cast FlightRuntime.UNDEFINED;
    var sqrLenA:Dynamic = cast FlightRuntime.UNDEFINED;
    var sqrLenB:Dynamic = cast FlightRuntime.UNDEFINED;
    var krossE:Dynamic = cast FlightRuntime.UNDEFINED;
    var sa:Dynamic = cast FlightRuntime.UNDEFINED;
    var sb:Dynamic = cast FlightRuntime.UNDEFINED;
    var lo:Dynamic = cast FlightRuntime.UNDEFINED;
    var hi:Dynamic = cast FlightRuntime.UNDEFINED;
    vax = (a2x - a1x);
    vay = (a2y - a1y);
    vbx = (b2x - b1x);
    vby = (b2y - b1y);
    ex = (b1x - a1x);
    ey = (b1y - a1y);
    kross = ((vax * vby) - (vay * vbx));
    sqrKross = (kross * kross);
    sqrLenA = ((vax * vax) + (vay * vay));
    sqrLenB = ((vbx * vbx) + (vby * vby));
    if (FlightRuntime.truthy(FlightRuntime.compare(sqrKross, ((PathBoolean.INTERSECTION_EPS__martinezKernel * sqrLenA) * sqrLenB), '>'))) {
  var s:Dynamic = (((ex * vby) - (ey * vbx)) / kross);
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(s, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(s, 1.0, '>')))) { return cast null; }
  var t:Dynamic = (((ex * vay) - (ey * vax)) / kross);
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(t, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(t, 1.0, '>')))) { return cast null; }
  return cast cast ([cast ([(a1x + (s * vax)), (a1y + (s * vay))] : Array<Dynamic>)] : Array<Dynamic>);
}
    krossE = ((ex * vay) - (ey * vax));
    if (FlightRuntime.truthy(FlightRuntime.compare((krossE * krossE), ((PathBoolean.INTERSECTION_EPS__martinezKernel * sqrLenA) * ((ex * ex) + (ey * ey))), '>'))) { return cast null; }
    sa = (((ex * vax) + (ey * vay)) / sqrLenA);
    sb = (sa + (((vbx * vax) + (vby * vay)) / sqrLenA));
    lo = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([sa, sb] : Array<Dynamic>))] : Array<Dynamic>));
    hi = FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, FlightRuntime.callProperty(HxMath, 'max', cast ([sa, sb] : Array<Dynamic>))] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(lo, (hi + 1e-12), '>'))) { return cast null; }
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([(lo - hi)] : Array<Dynamic>)), 1e-12, '<='))) { return cast cast ([cast ([(a1x + (lo * vax)), (a1y + (lo * vay))] : Array<Dynamic>)] : Array<Dynamic>); }
    return cast cast ([cast ([(a1x + (lo * vax)), (a1y + (lo * vay))] : Array<Dynamic>), cast ([(a1x + (hi * vax)), (a1y + (hi * vay))] : Array<Dynamic>)] : Array<Dynamic>);
    return cast null;
  }

  public static function setPathBooleanBackend(backend:Null<PathBooleanBackend>):Void {
    (PathBoolean._backend__pathBooleanBackend = cast (backend : Dynamic));
  }

  public static function signedArea__martinezKernel(p0x:Float, p0y:Float, p1x:Float, p1y:Float, p2x:Float, p2y:Float):Float {
    return cast (((p0x - p2x) * (p1y - p2y)) - ((p1x - p2x) * (p0y - p2y)));
    return cast null;
  }

  public static function simplifyPath(path:Path, ?options:PathBooleanOptions):Path {
    var fillRule:Dynamic = cast FlightRuntime.UNDEFINED;
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    fillRule = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'fillRule'), function():Dynamic return cast 'nonZero');
    contours = FlightRuntime.callValue(flattenPath, cast ([path, FlightRuntime.optionalField(options, 'tolerance')] : Array<Dynamic>));
    return cast FlightRuntime.callValue(resolvePathRegions, cast ([contours, fillRule] : Array<Dynamic>));
    return cast null;
  }

  public static function snap__martinezKernel(v:Float):Float {
    return cast FlightRuntime.callProperty(HxMath, 'round', cast ([(v / PathBoolean.vertexSnap__martinezKernel)] : Array<Dynamic>));
    return cast null;
  }

  public static function swap__martinezKernel(data:Array<Dynamic>, i:Float, j:Float):Void {
    var tmp:Dynamic = cast FlightRuntime.UNDEFINED;
    tmp = FlightRuntime.getIndex(data, i);
    FlightRuntime.setIndex(data, i, FlightRuntime.getIndex(data, j));
    FlightRuntime.setIndex(data, j, tmp);
  }

  public static function traceRings__martinezKernel(edges:Array<Array<Float>>):Array<PathBooleanContour> {
    var graph:Dynamic = cast FlightRuntime.UNDEFINED;
    graph = new DirectedGraph__martinezKernel();
    for (e in FlightRuntime.iterable(edges)) {
      FlightRuntime.callProperty(graph, 'addEdge', cast ([FlightRuntime.getIndex(e, 0.0), FlightRuntime.getIndex(e, 1.0), FlightRuntime.getIndex(e, 2.0), FlightRuntime.getIndex(e, 3.0)] : Array<Dynamic>));
    }
    return cast FlightRuntime.callProperty(graph, 'traceRings', cast ([] : Array<Dynamic>));
    return cast null;
  }

  public static final TWO_PI__offsetPath:Dynamic = (HxMath.PI * 2.0);

  public static function unionAllPaths(paths:Array<Path>, ?out:Path, ?options:PathBooleanOptions):Path {
    var fillRule:Dynamic = cast FlightRuntime.UNDEFINED;
    var contours:Array<PathBooleanContour> = cast FlightRuntime.UNDEFINED;
    var result:Dynamic = cast FlightRuntime.UNDEFINED;
    var path:Dynamic = cast FlightRuntime.UNDEFINED;
    fillRule = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'fillRule'), function():Dynamic return cast 'nonZero');
    contours = cast ([] : Array<Dynamic>);
    for (path in FlightRuntime.iterable(paths)) {
      for (contour in FlightRuntime.iterable(FlightRuntime.callValue(flattenPath, cast ([path, FlightRuntime.optionalField(options, 'tolerance')] : Array<Dynamic>)))) {
        FlightRuntime.callProperty(contours, 'push', cast ([contour] : Array<Dynamic>));
      }
    }
    result = FlightRuntime.select(FlightRuntime.strictEquals(FlightRuntime.field(contours, 'length'), 0.0), function():Dynamic return cast PathBoolean.EMPTY_CONTOURS__unionAllPaths, function():Dynamic return cast FlightRuntime.callProperty(FlightRuntime.callValue(getPathBooleanBackend, cast ([] : Array<Dynamic>)), 'computePathBoolean', cast ([contours, PathBoolean.EMPTY_CONTOURS__unionAllPaths, 'union', fillRule] : Array<Dynamic>)));
    path = FlightRuntime.coalesce(out, function():Dynamic return cast FlightRuntime.callValue(createPath, cast (['nonZero'] : Array<Dynamic>)));
    FlightRuntime.setLength(FlightRuntime.field(path, 'commands'), 0.0);
    FlightRuntime.setLength(FlightRuntime.field(path, 'data'), 0.0);
    FlightRuntime.setField(path, 'winding', 'nonZero');
    for (ring in FlightRuntime.iterable(result)) {
      if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(ring, 'length'), 6.0, '<'))) { continue; }
      FlightRuntime.callValue(appendPathMoveTo, cast ([path, FlightRuntime.getIndex(ring, 0.0), FlightRuntime.getIndex(ring, 1.0)] : Array<Dynamic>));
      {
        var i:Dynamic = 2.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(ring, 'length'), '<'))) {
          FlightRuntime.callValue(appendPathLineTo, cast ([path, FlightRuntime.getIndex(ring, i), FlightRuntime.getIndex(ring, (i + 1.0))] : Array<Dynamic>));
          (i = cast ((i + 2.0) : Dynamic));
        }
      }
      FlightRuntime.callValue(appendPathClose, cast ([path] : Array<Dynamic>));
    }
    return cast path;
    return cast null;
  }

  public static function unionPaths(a:Path, b:Path, ?out:Path, ?options:PathBooleanOptions):Path {
    return cast FlightRuntime.callValue(booleanPaths, cast ([a, b, 'union', out, options] : Array<Dynamic>));
    return cast null;
  }

  public static final VERTEX_SNAP_FALLBACK__martinezKernel:Dynamic = 1e-9;

  public static final VERTEX_SNAP_RELATIVE__martinezKernel:Dynamic = 1e-9;

  public static var vertexSnap__martinezKernel:Dynamic = 1e-9;

  public static function windingAt__martinezKernel(unique:Array<UniqueSegment__martinezKernel>, px:Float, py:Float, subject:Bool):Float {
    var winding:Dynamic = cast FlightRuntime.UNDEFINED;
    winding = 0.0;
    for (v in FlightRuntime.iterable(unique)) {
      var lox:Dynamic = FlightRuntime.callProperty(HxMath, 'min', cast ([FlightRuntime.field(v, 'ax'), FlightRuntime.field(v, 'bx')] : Array<Dynamic>));
      var hix:Dynamic = FlightRuntime.callProperty(HxMath, 'max', cast ([FlightRuntime.field(v, 'ax'), FlightRuntime.field(v, 'bx')] : Array<Dynamic>));
      if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(px, lox, '<'), function():Dynamic return cast FlightRuntime.compare(px, hix, '>=')))) { continue; }
      var t:Dynamic = ((px - FlightRuntime.field(v, 'ax')) / (FlightRuntime.field(v, 'bx') - FlightRuntime.field(v, 'ax')));
      var yAt:Dynamic = (FlightRuntime.field(v, 'ay') + (t * (FlightRuntime.field(v, 'by') - FlightRuntime.field(v, 'ay'))));
      if (FlightRuntime.truthy(FlightRuntime.compare(yAt, py, '<'))) { (winding = cast ((winding + FlightRuntime.select(subject, function():Dynamic return cast FlightRuntime.field(v, 'subjectDelta'), function():Dynamic return cast FlightRuntime.field(v, 'clipDelta'))) : Dynamic)); }
    }
    return cast winding;
    return cast null;
  }

  public static function writeContours__booleanPaths(contours:Array<Array<Float>>, ?out:Path):Path {
    var path:Dynamic = cast FlightRuntime.UNDEFINED;
    path = FlightRuntime.coalesce(out, function():Dynamic return cast FlightRuntime.callValue(createPath, cast (['nonZero'] : Array<Dynamic>)));
    FlightRuntime.setLength(FlightRuntime.field(path, 'commands'), 0.0);
    FlightRuntime.setLength(FlightRuntime.field(path, 'data'), 0.0);
    FlightRuntime.setField(path, 'winding', 'nonZero');
    for (ring in FlightRuntime.iterable(contours)) {
      if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(ring, 'length'), 6.0, '<'))) { continue; }
      FlightRuntime.callValue(appendPathMoveTo, cast ([path, FlightRuntime.getIndex(ring, 0.0), FlightRuntime.getIndex(ring, 1.0)] : Array<Dynamic>));
      {
        var i:Dynamic = 2.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(ring, 'length'), '<'))) {
          FlightRuntime.callValue(appendPathLineTo, cast ([path, FlightRuntime.getIndex(ring, i), FlightRuntime.getIndex(ring, (i + 1.0))] : Array<Dynamic>));
          (i = cast ((i + 2.0) : Dynamic));
        }
      }
      FlightRuntime.callValue(appendPathClose, cast ([path] : Array<Dynamic>));
    }
    return cast path;
    return cast null;
  }

  public static function xorPaths(a:Path, b:Path, ?out:Path, ?options:PathBooleanOptions):Path {
    return cast FlightRuntime.callValue(booleanPaths, cast ([a, b, 'xor', out, options] : Array<Dynamic>));
    return cast null;
  }
}
