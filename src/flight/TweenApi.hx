// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Easing.easeOutExponential;
import flight.Signals.connectSignal;
import flight.Signals.createSignal;
import flight.Signals.emitSignal;
import flight.Types.EasingFunction;
import flight.Types.NumericProps;
import flight.Types.StopTweenOptions;
import flight.Types.Tween;
import flight.Types.TweenManager;
import flight.Types.TweenManagerOptions;
import flight.Types.TweenOptions;
import flight.Types.TweenPropertyDetail;

typedef ColorComponents__colorTween = { var b:Float; var g:Float; var r:Float; };

typedef TweenStaggerOptions = { @:optional var each:Float; @:optional var from:Dynamic; @:optional var staggerEase:EasingFunction; };

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.TweenApi")
class TweenApi {
  @:keep public static function applyTween<T>(manager:TweenManager, target:Dynamic, propertyMap:NumericProps<Dynamic>):Void {
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.callValue(stopTweens, cast ([manager, target, propertyMap] : Array<Dynamic>));
    t = (cast target : Dynamic);
    p = (cast propertyMap : Dynamic);
    for (key in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Object'] : Array<Dynamic>)), 'keys', cast ([p] : Array<Dynamic>)))) {
      var val:Dynamic = FlightRuntime.getIndex(p, key);
      if (FlightRuntime.truthy(!FlightRuntime.strictEquals(val, FlightRuntime.UNDEFINED))) { FlightRuntime.setIndex(t, key, val); }
    }
  }

  @:keep public static function completeTween<T>(tween:Tween<Dynamic>):Void {
    var effectiveT:Dynamic = cast FlightRuntime.UNDEFINED;
    var easedT:Dynamic = cast FlightRuntime.UNDEFINED;
    var target:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.field(tween, 'complete'))) { return; }
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.field(tween, 'initialized')))) { FlightRuntime.callValue(initializeTween, cast ([tween] : Array<Dynamic>)); }
    effectiveT = FlightRuntime.select(FlightRuntime.field(tween, 'reverse'), function():Dynamic return cast 0.0, function():Dynamic return cast 1.0);
    easedT = FlightRuntime.callProperty(tween, 'ease', cast ([effectiveT] : Array<Dynamic>));
    target = (cast FlightRuntime.field(tween, 'target') : Dynamic);
    for (detail in FlightRuntime.iterable(FlightRuntime.field(tween, 'properties'))) {
      var value:Dynamic = (FlightRuntime.field(detail, 'start') + (FlightRuntime.field(detail, 'change') * easedT));
      if (FlightRuntime.truthy(FlightRuntime.field(tween, 'snapping'))) { (value = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([value] : Array<Dynamic>)) : Dynamic)); }
      FlightRuntime.setIndex(target, FlightRuntime.field(detail, 'key'), value);
    }
    FlightRuntime.setField(tween, 'complete', true);
    FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(tween, 'onComplete')] : Array<Dynamic>));
  }

  @:keep public static function computeStaggerDelay__tweenStagger(index:Float, count:Float, each:Float, from:Dynamic, ?staggerEase:EasingFunction):Float {
    var normalizedPosition:Float = cast FlightRuntime.UNDEFINED;
    var eased:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.compare(count, 1.0, '<='))) { return cast 0.0; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(from, 'start'))) {
  (normalizedPosition = cast ((index / (count - 1.0)) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(from, 'end'))) {
  (normalizedPosition = cast ((((count - 1.0) - index) / (count - 1.0)) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(from, 'center'))) {
  var center:Dynamic = ((count - 1.0) / 2.0);
  (normalizedPosition = cast ((FlightRuntime.callProperty(HxMath, 'abs', cast ([(index - center)] : Array<Dynamic>)) / center) : Dynamic));
} else {
  var origin:Dynamic = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([from, (count - 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
  var maxDistance:Dynamic = FlightRuntime.callProperty(HxMath, 'max', cast ([origin, ((count - 1.0) - origin)] : Array<Dynamic>));
  (normalizedPosition = cast (FlightRuntime.select(FlightRuntime.compare(maxDistance, 0.0, '>'), function():Dynamic return cast (FlightRuntime.callProperty(HxMath, 'abs', cast ([(index - origin)] : Array<Dynamic>)) / maxDistance), function():Dynamic return cast 0.0) : Dynamic));
} } }
    eased = FlightRuntime.select(!FlightRuntime.strictEquals(staggerEase, FlightRuntime.UNDEFINED), function():Dynamic return cast FlightRuntime.callValue(staggerEase, cast ([normalizedPosition] : Array<Dynamic>)), function():Dynamic return cast normalizedPosition);
    return cast ((eased * each) * (count - 1.0));
    return cast null;
  }

  @:keep public static function createColorTween(manager:TweenManager, target:Dynamic, property:String, duration:Float, toColor:Float, ?options:TweenOptions):Tween<ColorComponents__colorTween> {
    var fromColor:Dynamic = cast FlightRuntime.UNDEFINED;
    var components:ColorComponents__colorTween = cast FlightRuntime.UNDEFINED;
    var tween:Dynamic = cast FlightRuntime.UNDEFINED;
    fromColor = FlightRuntime.coalesce(FlightRuntime.getIndex(target, property), function():Dynamic return cast 0.0);
    components = { b: (Std.int(fromColor) & Std.int(255.0)), g: (Std.int((Std.int(fromColor) >> Std.int(8.0))) & Std.int(255.0)), r: (Std.int((Std.int(fromColor) >> Std.int(16.0))) & Std.int(255.0)) };
    tween = FlightRuntime.callValue(createTween, cast ([manager, components, duration, { b: (Std.int(toColor) & Std.int(255.0)), g: (Std.int((Std.int(toColor) >> Std.int(8.0))) & Std.int(255.0)), r: (Std.int((Std.int(toColor) >> Std.int(16.0))) & Std.int(255.0)) }, options] : Array<Dynamic>));
    FlightRuntime.callValue(connectSignal, cast ([FlightRuntime.field(tween, 'onUpdate'), function() {
  FlightRuntime.setIndex(target, property, (Std.int((Std.int((Std.int((Std.int(FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.field(components, 'r')] : Array<Dynamic>))) & Std.int(255.0))) << Std.int(16.0))) | Std.int((Std.int((Std.int(FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.field(components, 'g')] : Array<Dynamic>))) & Std.int(255.0))) << Std.int(8.0))))) | Std.int((Std.int(FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.field(components, 'b')] : Array<Dynamic>))) & Std.int(255.0)))));
}] : Array<Dynamic>));
    return cast tween;
    return cast null;
  }

  @:keep public static function createTween<T>(managerOrTarget:Dynamic, targetOrDuration:Dynamic, durationOrProps:Dynamic, ?propsOrOptions:Dynamic, ?maybeOptions:TweenOptions):Tween<Dynamic> {
    var manager:TweenManager = cast FlightRuntime.UNDEFINED;
    var target:Dynamic = cast FlightRuntime.UNDEFINED;
    var duration:Float = cast FlightRuntime.UNDEFINED;
    var propertyMap:NumericProps<Dynamic> = cast FlightRuntime.UNDEFINED;
    var options:Null<TweenOptions> = cast FlightRuntime.UNDEFINED;
    var tween:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.callValue(TweenApi.isTweenManager__tween, cast ([managerOrTarget] : Array<Dynamic>)))) {
  (manager = cast (managerOrTarget : Dynamic));
  (target = cast ((cast targetOrDuration : Dynamic) : Dynamic));
  (duration = cast ((cast durationOrProps : Float) : Dynamic));
  (propertyMap = cast ((cast propsOrOptions : NumericProps<Dynamic>) : Dynamic));
  (options = cast (maybeOptions : Dynamic));
} else {
  (manager = cast (defaultManager : Dynamic));
  (target = cast ((cast managerOrTarget : Dynamic) : Dynamic));
  (duration = cast ((cast targetOrDuration : Float) : Dynamic));
  (propertyMap = cast ((cast durationOrProps : NumericProps<Dynamic>) : Dynamic));
  (options = cast ((cast propsOrOptions : Null<TweenOptions>) : Dynamic));
}
    tween = FlightRuntime.callValue(TweenApi.makeTween__tween, cast ([target, duration, propertyMap, options, FlightRuntime.field(manager, 'defaultEase')] : Array<Dynamic>));
    FlightRuntime.callValue(TweenApi.registerTween__tween, cast ([manager, tween, FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'overwrite'), function():Dynamic return cast true)] : Array<Dynamic>));
    return cast tween;
    return cast null;
  }

  @:keep public static function createTweenManager(?options:TweenManagerOptions):TweenManager {
    return cast { __brand: 'TweenManager', defaultEase: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'defaultEase'), function():Dynamic return cast easeOutExponential), tweens: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []) };
    return cast null;
  }

  @:keep public static function createTweenStagger<T>(manager:TweenManager, targets:Array<Dynamic>, duration:Float, propertyMap:NumericProps<Dynamic>, ?stagger:TweenStaggerOptions, ?options:TweenOptions):Array<Tween<Dynamic>> {
    var each:Dynamic = cast FlightRuntime.UNDEFINED;
    var from:Dynamic = cast FlightRuntime.UNDEFINED;
    var staggerEase:Dynamic = cast FlightRuntime.UNDEFINED;
    var baseDelay:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var tweens:Array<Tween<Dynamic>> = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(targets, 'length'), 0.0))) { return cast cast ([] : Array<Dynamic>); }
    each = FlightRuntime.coalesce(FlightRuntime.optionalField(stagger, 'each'), function():Dynamic return cast 0.1);
    from = FlightRuntime.coalesce(FlightRuntime.optionalField(stagger, 'from'), function():Dynamic return cast 'start');
    staggerEase = FlightRuntime.optionalField(stagger, 'staggerEase');
    baseDelay = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'delay'), function():Dynamic return cast 0.0);
    count = FlightRuntime.field(targets, 'length');
    tweens = cast ([] : Array<Dynamic>);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, count, '<'))) {
        var staggerOffset:Dynamic = FlightRuntime.callValue(TweenApi.computeStaggerDelay__tweenStagger, cast ([i, count, each, from, staggerEase] : Array<Dynamic>));
        var tween:Dynamic = FlightRuntime.callValue(createTween, cast ([manager, FlightRuntime.getIndex(targets, i), duration, propertyMap, FlightRuntime.mergeObjects([options, { delay: (baseDelay + staggerOffset) }])] : Array<Dynamic>));
        FlightRuntime.callProperty(tweens, 'push', cast ([tween] : Array<Dynamic>));
        i++;
      }
    }
    return cast tweens;
    return cast null;
  }

  @:keep public static function createTweenTimer(manager:TweenManager, duration:Float, ?options:TweenOptions):Tween<Dynamic> {
    return cast FlightRuntime.callValue(createTween, cast ([manager, {  }, duration, {  }, options] : Array<Dynamic>));
    return cast null;
  }

  public static final defaultManager:TweenManager = FlightRuntime.callValue(createTweenManager, cast ([] : Array<Dynamic>));

  @:keep public static function getActiveTweenCount(manager:TweenManager):Float {
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    count = 0.0;
    for (list in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'values', cast ([] : Array<Dynamic>)))) {
      (count = cast ((count + FlightRuntime.field(list, 'length')) : Dynamic));
    }
    return cast count;
    return cast null;
  }

  @:keep public static function getTweenProgress(tween:Tween<Dynamic>):Float {
    var activeElapsed:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.field(tween, 'complete'))) { return cast 1.0; }
    activeElapsed = (FlightRuntime.field(tween, 'elapsed') - FlightRuntime.field(tween, 'delay'));
    if (FlightRuntime.truthy(FlightRuntime.compare(activeElapsed, 0.0, '<='))) { return cast 0.0; }
    return cast FlightRuntime.callProperty(HxMath, 'min', cast ([(activeElapsed / FlightRuntime.field(tween, 'duration')), 1.0] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getTweensOf(manager:TweenManager, target:Dynamic):Array<Tween<Dynamic>> {
    return cast FlightRuntime.coalesce(FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'get', cast ([target] : Array<Dynamic>)), function():Dynamic return cast cast ([] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function hasTweensOf(manager:TweenManager, target:Dynamic):Bool {
    var list:Dynamic = cast FlightRuntime.UNDEFINED;
    list = FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'get', cast ([target] : Array<Dynamic>));
    return cast FlightRuntime.andValue(!FlightRuntime.strictEquals(list, FlightRuntime.UNDEFINED), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(list, 'length'), 0.0, '>'));
    return cast null;
  }

  @:keep public static function initializeTween<T>(tween:Tween<Dynamic>):Void {
    var target:Dynamic = cast FlightRuntime.UNDEFINED;
    var propertyMap:Dynamic = cast FlightRuntime.UNDEFINED;
    target = (cast FlightRuntime.field(tween, 'target') : Dynamic);
    propertyMap = (cast FlightRuntime.field(tween, 'propertyMap') : Dynamic);
    for (detail in FlightRuntime.iterable(FlightRuntime.field(tween, 'properties'))) {
      var start:Dynamic = FlightRuntime.coalesce(FlightRuntime.getIndex(target, FlightRuntime.field(detail, 'key')), function():Dynamic return cast 0.0);
      var end:Dynamic = FlightRuntime.coalesce(FlightRuntime.getIndex(propertyMap, FlightRuntime.field(detail, 'key')), function():Dynamic return cast 0.0);
      FlightRuntime.setField(detail, 'start', start);
      FlightRuntime.setField(detail, 'change', (end - start));
      if (FlightRuntime.truthy(FlightRuntime.field(tween, 'smartRotation'))) {
  var change:Dynamic = (((FlightRuntime.field(detail, 'change') % 360.0) + 360.0) % 360.0);
  if (FlightRuntime.truthy(FlightRuntime.compare(change, 180.0, '>'))) { (change = cast ((change - 360.0) : Dynamic)); }
  FlightRuntime.setField(detail, 'change', change);
}
    }
    FlightRuntime.setField(tween, 'initialized', true);
  }

  @:keep public static function invalidateTween(tween:Tween<Dynamic>):Void {
    FlightRuntime.setField(tween, 'initialized', false);
    FlightRuntime.setField(tween, 'complete', false);
    FlightRuntime.setField(tween, 'elapsed', 0.0);
  }

  @:keep public static function isTweenManager__tween(value:Dynamic):Bool {
    return cast FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.strictEquals(FlightRuntime.typeofValue(value), 'object'), function():Dynamic return cast !FlightRuntime.strictEquals(value, null)), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field((cast value : Dynamic), '__brand'), 'TweenManager'));
    return cast null;
  }

  @:keep public static function killTweensOfProperty(manager:TweenManager, key:String):Void {
    for (list in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'values', cast ([] : Array<Dynamic>)))) {
      for (tween in FlightRuntime.iterable(list)) {
        var p:Dynamic = (cast FlightRuntime.field(tween, 'propertyMap') : Dynamic);
        if (FlightRuntime.truthy(FlightRuntime.hasField(p, key))) { FlightRuntime.setField(tween, 'complete', true); }
      }
    }
  }

  @:keep public static function makeTween__tween<T>(target:Dynamic, duration:Float, propertyMap:NumericProps<Dynamic>, options:Null<TweenOptions>, defaultEase:EasingFunction):Tween<Dynamic> {
    var keys:Dynamic = cast FlightRuntime.UNDEFINED;
    var properties:Array<TweenPropertyDetail> = cast FlightRuntime.UNDEFINED;
    keys = FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Object'] : Array<Dynamic>)), 'keys', cast ([propertyMap] : Array<Dynamic>));
    properties = FlightRuntime.callProperty(keys, 'map', cast ([function(key:Dynamic) return { change: 0.0, key: key, start: 0.0 }] : Array<Dynamic>));
    return cast { complete: false, delay: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'delay'), function():Dynamic return cast 0.0), duration: duration, ease: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'ease'), function():Dynamic return cast defaultEase), elapsed: 0.0, initialized: false, onComplete: FlightRuntime.callValue(createSignal, cast ([] : Array<Dynamic>)), onRepeat: FlightRuntime.callValue(createSignal, cast ([] : Array<Dynamic>)), onUpdate: FlightRuntime.callValue(createSignal, cast ([] : Array<Dynamic>)), onYoyo: FlightRuntime.callValue(createSignal, cast ([] : Array<Dynamic>)), paused: false, properties: properties, propertyMap: propertyMap, reflect: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'reflect'), function():Dynamic return cast false), repeat: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'repeat'), function():Dynamic return cast 0.0), reverse: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'reverse'), function():Dynamic return cast false), smartRotation: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'smartRotation'), function():Dynamic return cast false), snapping: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'snapping'), function():Dynamic return cast false), target: target };
    return cast null;
  }

  @:keep public static function pauseAllTweens(manager:TweenManager):Void {
    for (list in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'values', cast ([] : Array<Dynamic>)))) {
      for (tween in FlightRuntime.iterable(list)) {
        FlightRuntime.setField(tween, 'paused', true);
      }
    }
  }

  @:keep public static function pauseTween(tween:Tween<Dynamic>):Void {
    FlightRuntime.setField(tween, 'paused', true);
  }

  @:keep public static function pauseTweens(manager:TweenManager, target:Dynamic):Void {
    var list:Dynamic = cast FlightRuntime.UNDEFINED;
    list = FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'get', cast ([target] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(list, FlightRuntime.UNDEFINED))) { return; }
    for (tween in FlightRuntime.iterable(list)) {
      FlightRuntime.setField(tween, 'paused', true);
    }
  }

  @:keep public static function registerTween__tween<T>(manager:TweenManager, tween:Tween<Dynamic>, overwrite:Bool):Void {
    var list:Dynamic = cast FlightRuntime.UNDEFINED;
    list = FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'get', cast ([FlightRuntime.field(tween, 'target')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(list, FlightRuntime.UNDEFINED))) {
  (list = cast (cast ([] : Array<Dynamic>) : Dynamic));
  FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'set', cast ([FlightRuntime.field(tween, 'target'), list] : Array<Dynamic>));
}
    if (FlightRuntime.truthy(overwrite)) {
  {
    var i:Dynamic = (FlightRuntime.field(list, 'length') - 1.0);
    while (FlightRuntime.truthy(FlightRuntime.compare(i, 0.0, '>='))) {
      var existing:Dynamic = FlightRuntime.getIndex(list, i);
      var existingMap:Dynamic = (cast FlightRuntime.field(existing, 'propertyMap') : Dynamic);
      var overlaps:Dynamic = false;
      for (detail in FlightRuntime.iterable(FlightRuntime.field(tween, 'properties'))) {
        if (FlightRuntime.truthy(FlightRuntime.hasField(existingMap, FlightRuntime.field(detail, 'key')))) {
  (overlaps = cast (true : Dynamic));
  break;
}
      }
      if (FlightRuntime.truthy(overlaps)) { FlightRuntime.setField(existing, 'complete', true); }
      i--;
    }
  }
}
    FlightRuntime.callProperty(list, 'push', cast ([tween] : Array<Dynamic>));
  }

  @:keep public static function resetAllTweens(manager:TweenManager):Void {
    FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'clear', cast ([] : Array<Dynamic>));
  }

  @:keep public static function restartTween(tween:Tween<Dynamic>, includeDelay:Dynamic = true):Void {
    FlightRuntime.setField(tween, 'initialized', false);
    FlightRuntime.setField(tween, 'complete', false);
    FlightRuntime.setField(tween, 'elapsed', FlightRuntime.select(includeDelay, function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.field(tween, 'delay')));
  }

  @:keep public static function resumeAllTweens(manager:TweenManager):Void {
    for (list in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'values', cast ([] : Array<Dynamic>)))) {
      for (tween in FlightRuntime.iterable(list)) {
        FlightRuntime.setField(tween, 'paused', false);
      }
    }
  }

  @:keep public static function resumeTween(tween:Tween<Dynamic>):Void {
    FlightRuntime.setField(tween, 'paused', false);
  }

  @:keep public static function resumeTweens(manager:TweenManager, target:Dynamic):Void {
    var list:Dynamic = cast FlightRuntime.UNDEFINED;
    list = FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'get', cast ([target] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(list, FlightRuntime.UNDEFINED))) { return; }
    for (tween in FlightRuntime.iterable(list)) {
      FlightRuntime.setField(tween, 'paused', false);
    }
  }

  @:keep public static function seekTween(tween:Tween<Dynamic>, timeSeconds:Float):Void {
    var maxElapsed:Dynamic = cast FlightRuntime.UNDEFINED;
    var clampedElapsed:Dynamic = cast FlightRuntime.UNDEFINED;
    var activeElapsed:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var effectiveT:Dynamic = cast FlightRuntime.UNDEFINED;
    var easedT:Dynamic = cast FlightRuntime.UNDEFINED;
    var writes:Array<{ var key:String; var value:Float; }> = cast FlightRuntime.UNDEFINED;
    var target:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.field(tween, 'initialized')))) { FlightRuntime.callValue(initializeTween, cast ([tween] : Array<Dynamic>)); }
    maxElapsed = (FlightRuntime.field(tween, 'delay') + FlightRuntime.field(tween, 'duration'));
    clampedElapsed = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([timeSeconds, maxElapsed] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.setField(tween, 'elapsed', clampedElapsed);
    activeElapsed = (clampedElapsed - FlightRuntime.field(tween, 'delay'));
    if (FlightRuntime.truthy(FlightRuntime.compare(activeElapsed, 0.0, '<='))) { return; }
    t = FlightRuntime.callProperty(HxMath, 'min', cast ([(activeElapsed / FlightRuntime.field(tween, 'duration')), 1.0] : Array<Dynamic>));
    effectiveT = FlightRuntime.select(FlightRuntime.field(tween, 'reverse'), function():Dynamic return cast (1.0 - t), function():Dynamic return cast t);
    easedT = FlightRuntime.callProperty(tween, 'ease', cast ([effectiveT] : Array<Dynamic>));
    writes = cast ([] : Array<Dynamic>);
    for (detail in FlightRuntime.iterable(FlightRuntime.field(tween, 'properties'))) {
      var value:Dynamic = (FlightRuntime.field(detail, 'start') + (FlightRuntime.field(detail, 'change') * easedT));
      if (FlightRuntime.truthy(FlightRuntime.field(tween, 'snapping'))) { (value = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([value] : Array<Dynamic>)) : Dynamic)); }
      FlightRuntime.callProperty(writes, 'push', cast ([{ key: FlightRuntime.field(detail, 'key'), value: value }] : Array<Dynamic>));
    }
    target = (cast FlightRuntime.field(tween, 'target') : Dynamic);
    for (__iteration0 in FlightRuntime.iterable(writes)) {
      var key:Dynamic = FlightRuntime.field(__iteration0, 'key');
      var value:Dynamic = FlightRuntime.field(__iteration0, 'value');
      FlightRuntime.setIndex(target, key, value);
    }
    FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(tween, 'onUpdate')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(t, 1.0, '>='), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.field(tween, 'complete'))))) {
  FlightRuntime.setField(tween, 'complete', true);
  FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(tween, 'onComplete')] : Array<Dynamic>));
}
  }

  @:keep public static function setTweenProgress(tween:Tween<Dynamic>, progress:Float):Void {
    var clamped:Dynamic = cast FlightRuntime.UNDEFINED;
    var targetElapsed:Dynamic = cast FlightRuntime.UNDEFINED;
    clamped = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([progress, 1.0] : Array<Dynamic>))] : Array<Dynamic>));
    targetElapsed = (FlightRuntime.field(tween, 'delay') + (clamped * FlightRuntime.field(tween, 'duration')));
    FlightRuntime.callValue(seekTween, cast ([tween, targetElapsed] : Array<Dynamic>));
  }

  @:keep public static function stopAllTweens(manager:TweenManager, ?options:StopTweenOptions):Void {
    for (list in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'values', cast ([] : Array<Dynamic>)))) {
      for (tween in FlightRuntime.iterable(list)) {
        FlightRuntime.callValue(stopTween, cast ([tween, options] : Array<Dynamic>));
      }
    }
  }

  @:keep public static function stopTween(tween:Tween<Dynamic>, ?options:StopTweenOptions):Void {
    var doComplete:Dynamic = cast FlightRuntime.UNDEFINED;
    var doSendEvent:Dynamic = cast FlightRuntime.UNDEFINED;
    doComplete = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'complete'), function():Dynamic return cast false);
    doSendEvent = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'sendEvent'), function():Dynamic return cast true);
    if (FlightRuntime.truthy(doComplete)) {
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.field(tween, 'initialized')))) { FlightRuntime.callValue(initializeTween, cast ([tween] : Array<Dynamic>)); }
  var effectiveT:Dynamic = FlightRuntime.select(FlightRuntime.field(tween, 'reverse'), function():Dynamic return cast 0.0, function():Dynamic return cast 1.0);
  var easedT:Dynamic = FlightRuntime.callProperty(tween, 'ease', cast ([effectiveT] : Array<Dynamic>));
  var t:Dynamic = (cast FlightRuntime.field(tween, 'target') : Dynamic);
  for (detail in FlightRuntime.iterable(FlightRuntime.field(tween, 'properties'))) {
    var value:Dynamic = (FlightRuntime.field(detail, 'start') + (FlightRuntime.field(detail, 'change') * easedT));
    if (FlightRuntime.truthy(FlightRuntime.field(tween, 'snapping'))) { (value = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([value] : Array<Dynamic>)) : Dynamic)); }
    FlightRuntime.setIndex(t, FlightRuntime.field(detail, 'key'), value);
  }
  if (FlightRuntime.truthy(doSendEvent)) { FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(tween, 'onComplete')] : Array<Dynamic>)); }
}
    FlightRuntime.setField(tween, 'complete', true);
  }

  @:keep public static function stopTweens(manager:TweenManager, target:Dynamic, ?propertyMap:NumericProps<Dynamic>, ?options:StopTweenOptions):Void {
    var list:Dynamic = cast FlightRuntime.UNDEFINED;
    list = FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'get', cast ([target] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(list, FlightRuntime.UNDEFINED))) { return; }
    for (tween in FlightRuntime.iterable(list)) {
      if (FlightRuntime.truthy(!FlightRuntime.strictEquals(propertyMap, FlightRuntime.UNDEFINED))) {
  var p:Dynamic = (cast propertyMap : Dynamic);
  var tweenMap:Dynamic = (cast FlightRuntime.field(tween, 'propertyMap') : Dynamic);
  var overlaps:Dynamic = false;
  for (key in FlightRuntime.iterable(FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Object'] : Array<Dynamic>)), 'keys', cast ([p] : Array<Dynamic>)))) {
    if (FlightRuntime.truthy(FlightRuntime.hasField(tweenMap, key))) {
  (overlaps = cast (true : Dynamic));
  break;
}
  }
  if (FlightRuntime.truthy(!FlightRuntime.truthy(overlaps))) { continue; }
}
      FlightRuntime.callValue(stopTween, cast ([tween, options] : Array<Dynamic>));
    }
  }

  @:keep public static function updateTween__updateTweens<T>(tween:Tween<Dynamic>, deltaTime:Float):Void {
    var activeElapsed:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var effectiveT:Dynamic = cast FlightRuntime.UNDEFINED;
    var easedT:Dynamic = cast FlightRuntime.UNDEFINED;
    var target:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.field(tween, 'paused'), function():Dynamic return cast FlightRuntime.field(tween, 'complete')))) { return; }
    FlightRuntime.setField(tween, 'elapsed', (FlightRuntime.field(tween, 'elapsed') + deltaTime));
    activeElapsed = (FlightRuntime.field(tween, 'elapsed') - FlightRuntime.field(tween, 'delay'));
    if (FlightRuntime.truthy(FlightRuntime.compare(activeElapsed, 0.0, '<='))) { return; }
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.field(tween, 'initialized')))) { FlightRuntime.callValue(initializeTween, cast ([tween] : Array<Dynamic>)); }
    t = FlightRuntime.callProperty(HxMath, 'min', cast ([(activeElapsed / FlightRuntime.field(tween, 'duration')), 1.0] : Array<Dynamic>));
    effectiveT = FlightRuntime.select(FlightRuntime.field(tween, 'reverse'), function():Dynamic return cast (1.0 - t), function():Dynamic return cast t);
    easedT = FlightRuntime.callProperty(tween, 'ease', cast ([effectiveT] : Array<Dynamic>));
    target = (cast FlightRuntime.field(tween, 'target') : Dynamic);
    for (detail in FlightRuntime.iterable(FlightRuntime.field(tween, 'properties'))) {
      var value:Dynamic = (FlightRuntime.field(detail, 'start') + (FlightRuntime.field(detail, 'change') * easedT));
      if (FlightRuntime.truthy(FlightRuntime.field(tween, 'snapping'))) { (value = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([value] : Array<Dynamic>)) : Dynamic)); }
      FlightRuntime.setIndex(target, FlightRuntime.field(detail, 'key'), value);
    }
    FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(tween, 'onUpdate')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(t, 1.0, '>='))) {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(tween, 'repeat'), 0.0))) {
  FlightRuntime.setField(tween, 'complete', true);
  FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(tween, 'onComplete')] : Array<Dynamic>));
} else {
  if (FlightRuntime.truthy(FlightRuntime.field(tween, 'reflect'))) {
  FlightRuntime.setField(tween, 'reverse', !FlightRuntime.truthy(FlightRuntime.field(tween, 'reverse')));
  FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(tween, 'onYoyo')] : Array<Dynamic>));
}
  FlightRuntime.setField(tween, 'elapsed', FlightRuntime.field(tween, 'delay'));
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(tween, 'repeat'), 0.0, '>'))) { FlightRuntime.incrementField(tween, 'repeat', -1, true); }
  FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(tween, 'onRepeat')] : Array<Dynamic>));
}
}
  }

  @:keep public static function updateTweens(manager:TweenManager, deltaTime:Float):Void {
    for (__iteration0 in FlightRuntime.iterable(FlightRuntime.field(manager, 'tweens'))) {
      var target:Dynamic = FlightRuntime.getIndex(__iteration0, 0.0);
      var list:Dynamic = FlightRuntime.getIndex(__iteration0, 1.0);
      var i:Dynamic = (FlightRuntime.field(list, 'length') - 1.0);
      while (FlightRuntime.truthy(FlightRuntime.compare(i, 0.0, '>='))) {
  if (FlightRuntime.truthy(FlightRuntime.field(FlightRuntime.getIndex(list, i), 'complete'))) {
  FlightRuntime.splice(list, Std.int(i), Std.int(1.0), []);
} else {
  FlightRuntime.callValue(TweenApi.updateTween__updateTweens, cast ([FlightRuntime.getIndex(list, i), deltaTime] : Array<Dynamic>));
}
  i--;
}
      if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(list, 'length'), 0.0))) { FlightRuntime.callProperty(FlightRuntime.field(manager, 'tweens'), 'delete', cast ([target] : Array<Dynamic>)); }
    }
  }
}
