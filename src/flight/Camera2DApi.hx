// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Geometry.createMatrix;
import flight.Geometry.createRectangle;
import flight.Geometry.createVector2;
import flight.Geometry.inverseMatrix;
import flight.Geometry.inverseMatrixTransformPointXY;
import flight.Geometry.matrixTransformBounds;
import flight.Geometry.matrixTransformPointXY;
import flight.Geometry.setTransformMatrix;
import flight.Geometry.translateMatrixByVectorXY;
import flight.Math.clamp;
import flight.Math.damp;
import flight.Types.Camera2D;
import flight.Types.Camera2DFollowOptions;
import flight.Types.Camera2DOptions;
import flight.Types.MatrixLike;
import flight.Types.RectangleLike;
import flight.Types.Vector2Like;

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.Camera2DApi")
class Camera2DApi {
  @:keep public static function createCamera2D(viewportWidth:Float, viewportHeight:Float, ?options:Camera2DOptions):Camera2D {
    return cast { rotation: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'rotation'), function():Dynamic return cast 0.0), viewportHeight: viewportHeight, viewportWidth: viewportWidth, x: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'x'), function():Dynamic return cast 0.0), y: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'y'), function():Dynamic return cast 0.0), zoom: FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'zoom'), function():Dynamic return cast 1.0) };
    return cast null;
  }

  @:keep public static function getCamera2DParallaxPoint(camera:Camera2D, factor:Float, out:Vector2Like):Void {
    FlightRuntime.callValue(getCamera2DViewMatrix, cast ([camera, Camera2DApi.scratchMatrix__parallax] : Array<Dynamic>));
    FlightRuntime.setField(out, 'x', ((FlightRuntime.field(Camera2DApi.scratchMatrix__parallax, 'tx') - (FlightRuntime.field(camera, 'viewportWidth') * 0.5)) * factor));
    FlightRuntime.setField(out, 'y', ((FlightRuntime.field(Camera2DApi.scratchMatrix__parallax, 'ty') - (FlightRuntime.field(camera, 'viewportHeight') * 0.5)) * factor));
  }

  @:keep public static function getCamera2DViewMatrix(camera:Camera2D, out:MatrixLike):Void {
    var zoom:Dynamic = cast FlightRuntime.UNDEFINED;
    zoom = FlightRuntime.field(camera, 'zoom');
    FlightRuntime.callValue(setTransformMatrix, cast ([out, zoom, zoom, -FlightRuntime.field(camera, 'rotation'), (FlightRuntime.field(camera, 'viewportWidth') * 0.5), (FlightRuntime.field(camera, 'viewportHeight') * 0.5)] : Array<Dynamic>));
    FlightRuntime.callValue(translateMatrixByVectorXY, cast ([out, out, -FlightRuntime.field(camera, 'x'), -FlightRuntime.field(camera, 'y')] : Array<Dynamic>));
  }

  @:keep public static function getCamera2DVisibleBounds(camera:Camera2D, out:RectangleLike):Void {
    FlightRuntime.callValue(getCamera2DViewMatrix, cast ([camera, Camera2DApi.scratchMatrix__visibleBounds] : Array<Dynamic>));
    FlightRuntime.callValue(inverseMatrix, cast ([Camera2DApi.scratchInverse__visibleBounds, Camera2DApi.scratchMatrix__visibleBounds] : Array<Dynamic>));
    FlightRuntime.callValue(matrixTransformBounds, cast ([out, Camera2DApi.scratchInverse__visibleBounds, 0.0, 0.0, FlightRuntime.field(camera, 'viewportWidth'), FlightRuntime.field(camera, 'viewportHeight')] : Array<Dynamic>));
  }

  @:keep public static function projectCamera2DPoint(camera:Camera2D, worldX:Float, worldY:Float, out:Vector2Like):Void {
    FlightRuntime.callValue(getCamera2DViewMatrix, cast ([camera, Camera2DApi.scratchMatrix__projection] : Array<Dynamic>));
    FlightRuntime.callValue(matrixTransformPointXY, cast ([out, Camera2DApi.scratchMatrix__projection, worldX, worldY] : Array<Dynamic>));
  }

  public static final scratchAfter__zoom:Dynamic = FlightRuntime.callValue(createVector2, cast ([] : Array<Dynamic>));

  public static final scratchBefore__zoom:Dynamic = FlightRuntime.callValue(createVector2, cast ([] : Array<Dynamic>));

  public static final scratchBounds__follow:Dynamic = FlightRuntime.callValue(createRectangle, cast ([] : Array<Dynamic>));

  public static final scratchInverse__visibleBounds:Dynamic = FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>));

  public static final scratchMatrix__parallax:Dynamic = FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>));

  public static final scratchMatrix__projection:Dynamic = FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>));

  public static final scratchMatrix__visibleBounds:Dynamic = FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>));

  @:keep public static function unprojectCamera2DPoint(camera:Camera2D, screenX:Float, screenY:Float, out:Vector2Like):Void {
    FlightRuntime.callValue(getCamera2DViewMatrix, cast ([camera, Camera2DApi.scratchMatrix__projection] : Array<Dynamic>));
    FlightRuntime.callValue(inverseMatrixTransformPointXY, cast ([out, Camera2DApi.scratchMatrix__projection, screenX, screenY] : Array<Dynamic>));
  }

  @:keep public static function updateCamera2DFollow(camera:Camera2D, targetX:Float, targetY:Float, deltaTime:Float, ?options:Camera2DFollowOptions):Void {
    var camX:Dynamic = cast FlightRuntime.UNDEFINED;
    var camY:Dynamic = cast FlightRuntime.UNDEFINED;
    var deadHalfW:Dynamic = cast FlightRuntime.UNDEFINED;
    var deadHalfH:Dynamic = cast FlightRuntime.UNDEFINED;
    var smoothTime:Dynamic = cast FlightRuntime.UNDEFINED;
    var worldBounds:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var goalX:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var goalY:Dynamic = cast FlightRuntime.UNDEFINED;
    var nextX:Float = cast FlightRuntime.UNDEFINED;
    var nextY:Float = cast FlightRuntime.UNDEFINED;
    camX = FlightRuntime.field(camera, 'x');
    camY = FlightRuntime.field(camera, 'y');
    deadHalfW = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'deadzoneHalfWidth'), function():Dynamic return cast 0.0);
    deadHalfH = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'deadzoneHalfHeight'), function():Dynamic return cast 0.0);
    smoothTime = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'smoothTime'), function():Dynamic return cast 0.0);
    worldBounds = FlightRuntime.optionalField(options, 'worldBounds');
    dx = (targetX - camX);
    goalX = camX;
    if (FlightRuntime.truthy(FlightRuntime.compare(dx, deadHalfW, '>'))) { (goalX = cast ((targetX - deadHalfW) : Dynamic)); } else { if (FlightRuntime.truthy(FlightRuntime.compare(dx, -deadHalfW, '<'))) { (goalX = cast ((targetX + deadHalfW) : Dynamic)); } }
    dy = (targetY - camY);
    goalY = camY;
    if (FlightRuntime.truthy(FlightRuntime.compare(dy, deadHalfH, '>'))) { (goalY = cast ((targetY - deadHalfH) : Dynamic)); } else { if (FlightRuntime.truthy(FlightRuntime.compare(dy, -deadHalfH, '<'))) { (goalY = cast ((targetY + deadHalfH) : Dynamic)); } }
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(smoothTime, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(deltaTime, 0.0, '>')))) {
  var lambda:Dynamic = (1.0 / smoothTime);
  (nextX = cast (FlightRuntime.callValue(damp, cast ([camX, goalX, lambda, deltaTime] : Array<Dynamic>)) : Dynamic));
  (nextY = cast (FlightRuntime.callValue(damp, cast ([camY, goalY, lambda, deltaTime] : Array<Dynamic>)) : Dynamic));
} else {
  (nextX = cast (goalX : Dynamic));
  (nextY = cast (goalY : Dynamic));
}
    if (FlightRuntime.truthy(worldBounds)) {
  FlightRuntime.callValue(getCamera2DVisibleBounds, cast ([camera, Camera2DApi.scratchBounds__follow] : Array<Dynamic>));
  var halfVisW:Dynamic = (FlightRuntime.field(Camera2DApi.scratchBounds__follow, 'width') * 0.5);
  var halfVisH:Dynamic = (FlightRuntime.field(Camera2DApi.scratchBounds__follow, 'height') * 0.5);
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(worldBounds, 'width'), FlightRuntime.field(Camera2DApi.scratchBounds__follow, 'width'), '<='))) {
  (nextX = cast ((FlightRuntime.field(worldBounds, 'x') + (FlightRuntime.field(worldBounds, 'width') * 0.5)) : Dynamic));
} else {
  (nextX = cast (FlightRuntime.callValue(clamp, cast ([nextX, (FlightRuntime.field(worldBounds, 'x') + halfVisW), ((FlightRuntime.field(worldBounds, 'x') + FlightRuntime.field(worldBounds, 'width')) - halfVisW)] : Array<Dynamic>)) : Dynamic));
}
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(worldBounds, 'height'), FlightRuntime.field(Camera2DApi.scratchBounds__follow, 'height'), '<='))) {
  (nextY = cast ((FlightRuntime.field(worldBounds, 'y') + (FlightRuntime.field(worldBounds, 'height') * 0.5)) : Dynamic));
} else {
  (nextY = cast (FlightRuntime.callValue(clamp, cast ([nextY, (FlightRuntime.field(worldBounds, 'y') + halfVisH), ((FlightRuntime.field(worldBounds, 'y') + FlightRuntime.field(worldBounds, 'height')) - halfVisH)] : Array<Dynamic>)) : Dynamic));
}
}
    FlightRuntime.setField(camera, 'x', nextX);
    FlightRuntime.setField(camera, 'y', nextY);
  }

  @:keep public static function zoomCamera2DAtScreenPoint(camera:Camera2D, screenX:Float, screenY:Float, zoom:Float):Void {
    FlightRuntime.callValue(unprojectCamera2DPoint, cast ([camera, screenX, screenY, Camera2DApi.scratchBefore__zoom] : Array<Dynamic>));
    FlightRuntime.setField(camera, 'zoom', zoom);
    FlightRuntime.callValue(unprojectCamera2DPoint, cast ([camera, screenX, screenY, Camera2DApi.scratchAfter__zoom] : Array<Dynamic>));
    FlightRuntime.setField(camera, 'x', (FlightRuntime.field(camera, 'x') + (FlightRuntime.field(Camera2DApi.scratchBefore__zoom, 'x') - FlightRuntime.field(Camera2DApi.scratchAfter__zoom, 'x'))));
    FlightRuntime.setField(camera, 'y', (FlightRuntime.field(camera, 'y') + (FlightRuntime.field(Camera2DApi.scratchBefore__zoom, 'y') - FlightRuntime.field(Camera2DApi.scratchAfter__zoom, 'y'))));
  }
}
