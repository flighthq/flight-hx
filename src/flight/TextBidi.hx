// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Types.BidiClass;
import flight.Types.BidiClassBackend;
import flight.Types.BidiDirection;
import flight.Types.BidiRun;

@:expose("flight.TextBidi")
class TextBidi {
  public static var _backend__bidiClassBackend:Null<BidiClassBackend> = FlightRuntime.explicitNull();

  public static final _classOrder__bidiClassBackend:Array<BidiClass> = cast (['L', 'R', 'AL', 'EN', 'ES', 'ET', 'AN', 'CS', 'NSM', 'BN', 'B', 'S', 'WS', 'ON', 'LRE', 'RLE', 'LRO', 'RLO', 'PDF', 'LRI', 'RLI', 'FSI', 'PDI'] : Array<Dynamic>);

  public static final AL__bidiClassBackend:Dynamic = 2.0;

  public static final AN__bidiClassBackend:Dynamic = 6.0;

  public static final B__bidiClassBackend:Dynamic = 10.0;

  public static final BN__bidiClassBackend:Dynamic = 9.0;

  public static function applyExplicitLevels__resolveBidiLevels(original:Array<BidiClass>, working:Array<BidiClass>, levelArray:Array<Float>, matchingPdi:Dynamic, paragraphLevel:Float):Void {
    var MAX_DEPTH:Dynamic = cast FlightRuntime.UNDEFINED;
    var stackLevel:Array<Float> = cast FlightRuntime.UNDEFINED;
    var stackOverride:Array<Null<BidiClass>> = cast FlightRuntime.UNDEFINED;
    var stackIsolate:Array<Bool> = cast FlightRuntime.UNDEFINED;
    var overflowIsolate:Dynamic = cast FlightRuntime.UNDEFINED;
    var overflowEmbedding:Dynamic = cast FlightRuntime.UNDEFINED;
    var validIsolate:Dynamic = cast FlightRuntime.UNDEFINED;
    MAX_DEPTH = 125.0;
    stackLevel = cast ([paragraphLevel] : Array<Dynamic>);
    stackOverride = cast ([null] : Array<Dynamic>);
    stackIsolate = cast ([false] : Array<Dynamic>);
    overflowIsolate = 0.0;
    overflowEmbedding = 0.0;
    validIsolate = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(original, 'length'), '<'))) {
        var t:Dynamic = FlightRuntime.getIndex(original, i);
        var top:Dynamic = (FlightRuntime.field(stackLevel, 'length') - 1.0);
        {
          var __switchValue = t;
          if (__switchValue == 'RLE' || __switchValue == 'LRE' || __switchValue == 'RLO' || __switchValue == 'LRO') {
            {
              FlightRuntime.setIndex(levelArray, i, FlightRuntime.getIndex(stackLevel, top));
              FlightRuntime.setIndex(working, i, 'BN');
              var newLevel:Dynamic = FlightRuntime.select(FlightRuntime.orValue(FlightRuntime.strictEquals(t, 'RLE'), function():Dynamic return cast FlightRuntime.strictEquals(t, 'RLO')), function():Dynamic return cast FlightRuntime.callValue(TextBidi.nextOdd__resolveBidiLevels, cast ([FlightRuntime.getIndex(stackLevel, top)] : Array<Dynamic>)), function():Dynamic return cast FlightRuntime.callValue(TextBidi.nextEven__resolveBidiLevels, cast ([FlightRuntime.getIndex(stackLevel, top)] : Array<Dynamic>)));
              if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(newLevel, MAX_DEPTH, '<='), function():Dynamic return cast FlightRuntime.strictEquals(overflowIsolate, 0.0)), function():Dynamic return cast FlightRuntime.strictEquals(overflowEmbedding, 0.0)))) {
  FlightRuntime.callProperty(stackLevel, 'push', cast ([newLevel] : Array<Dynamic>));
  FlightRuntime.callProperty(stackOverride, 'push', cast ([FlightRuntime.select(FlightRuntime.strictEquals(t, 'RLO'), function():Dynamic return cast 'R', function():Dynamic return cast FlightRuntime.select(FlightRuntime.strictEquals(t, 'LRO'), function():Dynamic return cast 'L', function():Dynamic return cast null))] : Array<Dynamic>));
  FlightRuntime.callProperty(stackIsolate, 'push', cast ([false] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(overflowIsolate, 0.0))) {
  overflowEmbedding++;
} }
            }
          }
          else if (__switchValue == 'RLI' || __switchValue == 'LRI' || __switchValue == 'FSI') {
            {
              FlightRuntime.setIndex(levelArray, i, FlightRuntime.getIndex(stackLevel, top));
              if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.getIndex(stackOverride, top), null))) { FlightRuntime.setIndex(working, i, (cast FlightRuntime.getIndex(stackOverride, top) : BidiClass)); }
              var asRtl:Dynamic = FlightRuntime.strictEquals(t, 'RLI');
              if (FlightRuntime.truthy(FlightRuntime.strictEquals(t, 'FSI'))) { (asRtl = cast (FlightRuntime.strictEquals(FlightRuntime.callValue(TextBidi.computeParagraphLevel__resolveBidiLevels, cast ([original, (i + 1.0), FlightRuntime.getIndex(matchingPdi, i)] : Array<Dynamic>)), 1.0) : Dynamic)); }
              var newLevel:Dynamic = FlightRuntime.select(asRtl, function():Dynamic return cast FlightRuntime.callValue(TextBidi.nextOdd__resolveBidiLevels, cast ([FlightRuntime.getIndex(stackLevel, top)] : Array<Dynamic>)), function():Dynamic return cast FlightRuntime.callValue(TextBidi.nextEven__resolveBidiLevels, cast ([FlightRuntime.getIndex(stackLevel, top)] : Array<Dynamic>)));
              if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(newLevel, MAX_DEPTH, '<='), function():Dynamic return cast FlightRuntime.strictEquals(overflowIsolate, 0.0)), function():Dynamic return cast FlightRuntime.strictEquals(overflowEmbedding, 0.0)))) {
  validIsolate++;
  FlightRuntime.callProperty(stackLevel, 'push', cast ([newLevel] : Array<Dynamic>));
  FlightRuntime.callProperty(stackOverride, 'push', cast ([null] : Array<Dynamic>));
  FlightRuntime.callProperty(stackIsolate, 'push', cast ([true] : Array<Dynamic>));
} else {
  overflowIsolate++;
}
            }
          }
          else if (__switchValue == 'PDI') {
            {
              if (FlightRuntime.truthy(FlightRuntime.compare(overflowIsolate, 0.0, '>'))) {
  overflowIsolate--;
} else { if (FlightRuntime.truthy(FlightRuntime.compare(validIsolate, 0.0, '>'))) {
  (overflowEmbedding = cast (0.0 : Dynamic));
  while (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.getIndex(stackIsolate, (FlightRuntime.field(stackLevel, 'length') - 1.0))))) {
  FlightRuntime.callProperty(stackLevel, 'pop', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(stackOverride, 'pop', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(stackIsolate, 'pop', cast ([] : Array<Dynamic>));
}
  FlightRuntime.callProperty(stackLevel, 'pop', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(stackOverride, 'pop', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(stackIsolate, 'pop', cast ([] : Array<Dynamic>));
  validIsolate--;
} }
              var newTop:Dynamic = (FlightRuntime.field(stackLevel, 'length') - 1.0);
              FlightRuntime.setIndex(levelArray, i, FlightRuntime.getIndex(stackLevel, newTop));
              if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.getIndex(stackOverride, newTop), null))) { FlightRuntime.setIndex(working, i, (cast FlightRuntime.getIndex(stackOverride, newTop) : BidiClass)); }
            }
          }
          else if (__switchValue == 'PDF') {
            {
              if (FlightRuntime.truthy(FlightRuntime.compare(overflowIsolate, 0.0, '>'))) {
} else { if (FlightRuntime.truthy(FlightRuntime.compare(overflowEmbedding, 0.0, '>'))) {
  overflowEmbedding--;
} else { if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.truthy(FlightRuntime.getIndex(stackIsolate, top)), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(stackLevel, 'length'), 2.0, '>=')))) {
  FlightRuntime.callProperty(stackLevel, 'pop', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(stackOverride, 'pop', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(stackIsolate, 'pop', cast ([] : Array<Dynamic>));
} } }
              FlightRuntime.setIndex(levelArray, i, FlightRuntime.getIndex(stackLevel, (FlightRuntime.field(stackLevel, 'length') - 1.0)));
              FlightRuntime.setIndex(working, i, 'BN');
            }
          }
          else if (__switchValue == 'B') {
            {
              FlightRuntime.setLength(stackLevel, 1.0);
              FlightRuntime.setLength(stackOverride, 1.0);
              FlightRuntime.setLength(stackIsolate, 1.0);
              (overflowIsolate = cast (0.0 : Dynamic));
              (overflowEmbedding = cast (0.0 : Dynamic));
              (validIsolate = cast (0.0 : Dynamic));
              FlightRuntime.setIndex(levelArray, i, paragraphLevel);
            }
          }
          else if (__switchValue == 'BN') {
            {
              FlightRuntime.setIndex(levelArray, i, FlightRuntime.getIndex(stackLevel, top));
            }
          }
          else  {
            {
              FlightRuntime.setIndex(levelArray, i, FlightRuntime.getIndex(stackLevel, top));
              if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.getIndex(stackOverride, top), null))) { FlightRuntime.setIndex(working, i, (cast FlightRuntime.getIndex(stackOverride, top) : BidiClass)); }
            }
          }
        }
        i++;
      }
    }
  }

  public static function applyLineReset__resolveBidiLevels(original:Array<BidiClass>, levelArray:Array<Float>, paragraphLevel:Float):Void {
    var length:Dynamic = cast FlightRuntime.UNDEFINED;
    length = FlightRuntime.field(original, 'length');
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, length, '<'))) {
        var t:Dynamic = FlightRuntime.getIndex(original, i);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(t, 'B'), function():Dynamic return cast FlightRuntime.strictEquals(t, 'S')))) {
  FlightRuntime.setIndex(levelArray, i, paragraphLevel);
  {
    var j:Dynamic = (i - 1.0);
    while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(j, 0.0, '>='), function():Dynamic return cast FlightRuntime.callValue(TextBidi.isResetType__resolveBidiLevels, cast ([FlightRuntime.getIndex(original, j)] : Array<Dynamic>))))) {
      FlightRuntime.setIndex(levelArray, j, paragraphLevel);
      j--;
    }
  }
}
        i++;
      }
    }
    {
      var j:Dynamic = (length - 1.0);
      while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(j, 0.0, '>='), function():Dynamic return cast FlightRuntime.callValue(TextBidi.isResetType__resolveBidiLevels, cast ([FlightRuntime.getIndex(original, j)] : Array<Dynamic>))))) {
        FlightRuntime.setIndex(levelArray, j, paragraphLevel);
        j--;
      }
    }
  }

  public static final CS__bidiClassBackend:Dynamic = 7.0;

  public static final EN__bidiClassBackend:Dynamic = 3.0;

  public static function computeParagraphLevel__resolveBidiLevels(types:Array<BidiClass>, start:Float, end:Float):Float {
    var isolateDepth:Dynamic = cast FlightRuntime.UNDEFINED;
    isolateDepth = 0.0;
    {
      var i:Dynamic = start;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, end, '<'))) {
        var t:Dynamic = FlightRuntime.getIndex(types, i);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(t, 'LRI'), function():Dynamic return cast FlightRuntime.strictEquals(t, 'RLI')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'FSI')))) {
  isolateDepth++;
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(t, 'PDI'))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(isolateDepth, 0.0, '>'))) { isolateDepth--; }
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(isolateDepth, 0.0))) {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(t, 'L'))) { return cast 0.0; }
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(t, 'R'), function():Dynamic return cast FlightRuntime.strictEquals(t, 'AL')))) { return cast 1.0; }
} } }
        i++;
      }
    }
    return cast 0.0;
    return cast null;
  }

  public static function createCompactBidiClassBackend():BidiClassBackend {
    return cast { getBidiClass: TextBidi.getCompactBidiClass__bidiClassBackend };
    return cast null;
  }

  public static final ES__bidiClassBackend:Dynamic = 4.0;

  public static final ET__bidiClassBackend:Dynamic = 5.0;

  public static final FSI__bidiClassBackend:Dynamic = 21.0;

  public static final L__bidiClassBackend:Dynamic = 0.0;

  public static final LRE__bidiClassBackend:Dynamic = 14.0;

  public static function getBidiClassBackend():BidiClassBackend {
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(TextBidi._backend__bidiClassBackend, null))) { (TextBidi._backend__bidiClassBackend = cast (FlightRuntime.callValue(createCompactBidiClassBackend, cast ([] : Array<Dynamic>)) : Dynamic)); }
    return cast TextBidi._backend__bidiClassBackend;
    return cast null;
  }

  public static function getBidiRuns(text:String, baseDirection:BidiDirection):Array<BidiRun> {
    var levels:Dynamic = cast FlightRuntime.UNDEFINED;
    var runs:Array<BidiRun> = cast FlightRuntime.UNDEFINED;
    var length:Dynamic = cast FlightRuntime.UNDEFINED;
    var start:Dynamic = cast FlightRuntime.UNDEFINED;
    levels = FlightRuntime.callValue(resolveBidiLevels, cast ([text, baseDirection] : Array<Dynamic>));
    runs = cast ([] : Array<Dynamic>);
    length = FlightRuntime.field(levels, 'length');
    start = 0.0;
    {
      var i:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, length, '<='))) {
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(i, length), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.getIndex(levels, i), FlightRuntime.getIndex(levels, start))))) {
  var level:Dynamic = FlightRuntime.getIndex(levels, start);
  FlightRuntime.callProperty(runs, 'push', cast ([{ start: start, end: i, level: level, direction: FlightRuntime.select(FlightRuntime.strictEquals((level % 2.0), 0.0), function():Dynamic return cast 'ltr', function():Dynamic return cast 'rtl') }] : Array<Dynamic>));
  (start = cast (i : Dynamic));
}
        i++;
      }
    }
    return cast runs;
    return cast null;
  }

  public static function getCompactBidiClass__bidiClassBackend(codepoint:Float):BidiClass {
    var lo:Dynamic = cast FlightRuntime.UNDEFINED;
    var hi:Dynamic = cast FlightRuntime.UNDEFINED;
    lo = 0.0;
    hi = (TextBidi._rangeCount__bidiClassBackend - 1.0);
    while (FlightRuntime.truthy(FlightRuntime.compare(lo, hi, '<='))) {
  var mid:Dynamic = (Std.int((lo + hi)) >> Std.int(1.0));
  var base:Dynamic = (mid * 3.0);
  var start:Dynamic = FlightRuntime.getIndex(TextBidi._ranges__bidiClassBackend, base);
  if (FlightRuntime.truthy(FlightRuntime.compare(codepoint, start, '<'))) {
  (hi = cast ((mid - 1.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.compare(codepoint, FlightRuntime.getIndex(TextBidi._ranges__bidiClassBackend, (base + 1.0)), '>'))) {
  (lo = cast ((mid + 1.0) : Dynamic));
} else {
  return cast FlightRuntime.getIndex(TextBidi._classOrder__bidiClassBackend, FlightRuntime.getIndex(TextBidi._ranges__bidiClassBackend, (base + 2.0)));
} }
}
    return cast 'L';
    return cast null;
  }

  public static function isNeutralOrIsolate__resolveBidiLevels(t:BidiClass):Bool {
    return cast FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(t, 'B'), function():Dynamic return cast FlightRuntime.strictEquals(t, 'S')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'WS')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'ON')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'FSI')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'LRI')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'RLI')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'PDI'));
    return cast null;
  }

  public static function isResetType__resolveBidiLevels(t:BidiClass):Bool {
    return cast FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(t, 'WS'), function():Dynamic return cast FlightRuntime.strictEquals(t, 'LRI')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'RLI')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'FSI')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'PDI')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'LRE')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'RLE')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'LRO')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'RLO')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'PDF')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'BN'));
    return cast null;
  }

  public static final LRI__bidiClassBackend:Dynamic = 19.0;

  public static final LRO__bidiClassBackend:Dynamic = 16.0;

  public static final NSM__bidiClassBackend:Dynamic = 8.0;

  public static final ON__bidiClassBackend:Dynamic = 13.0;

  public static function neutralDirection__resolveBidiLevels(t:BidiClass):String {
    return cast FlightRuntime.select(FlightRuntime.strictEquals(t, 'L'), function():Dynamic return cast 'L', function():Dynamic return cast 'R');
    return cast null;
  }

  public static function nextEven__resolveBidiLevels(level:Float):Float {
    return cast (Std.int((level + 2.0)) & Std.int(~Std.int(1.0)));
    return cast null;
  }

  public static function nextOdd__resolveBidiLevels(level:Float):Float {
    return cast (Std.int((level + 1.0)) | Std.int(1.0));
    return cast null;
  }

  public static final PDF__bidiClassBackend:Dynamic = 18.0;

  public static final PDI__bidiClassBackend:Dynamic = 22.0;

  public static function pairIsolates__resolveBidiLevels(types:Array<BidiClass>, matchingPdi:Dynamic, matchingInitiator:Dynamic):Void {
    var stack:Array<Float> = cast FlightRuntime.UNDEFINED;
    stack = cast ([] : Array<Dynamic>);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(types, 'length'), '<'))) {
        var t:Dynamic = FlightRuntime.getIndex(types, i);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(t, 'LRI'), function():Dynamic return cast FlightRuntime.strictEquals(t, 'RLI')), function():Dynamic return cast FlightRuntime.strictEquals(t, 'FSI')))) {
  FlightRuntime.callProperty(stack, 'push', cast ([i] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(t, 'PDI'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(stack, 'length'), 0.0, '>')))) {
  var initiator:Dynamic = (cast FlightRuntime.callProperty(stack, 'pop', cast ([] : Array<Dynamic>)) : Float);
  FlightRuntime.setIndex(matchingPdi, initiator, i);
  FlightRuntime.setIndex(matchingInitiator, i, initiator);
} }
        i++;
      }
    }
  }

  public static final R__bidiClassBackend:Dynamic = 1.0;

  public static final RLE__bidiClassBackend:Dynamic = 15.0;

  public static final RLI__bidiClassBackend:Dynamic = 20.0;

  public static function reorderBidiLine(levels:Dynamic, start:Float, end:Float, out:Array<Float>):Void {
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var highest:Dynamic = cast FlightRuntime.UNDEFINED;
    var lowestOdd:Dynamic = cast FlightRuntime.UNDEFINED;
    count = (end - start);
    FlightRuntime.setLength(out, count);
    if (FlightRuntime.truthy(FlightRuntime.compare(count, 0.0, '<='))) { return; }
    highest = 0.0;
    lowestOdd = 255.0;
    {
      var i:Dynamic = start;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, end, '<'))) {
        var level:Dynamic = FlightRuntime.getIndex(levels, i);
        FlightRuntime.setIndex(out, (i - start), i);
        if (FlightRuntime.truthy(FlightRuntime.compare(level, highest, '>'))) { (highest = cast (level : Dynamic)); }
        if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals((level % 2.0), 1.0), function():Dynamic return cast FlightRuntime.compare(level, lowestOdd, '<')))) { (lowestOdd = cast (level : Dynamic)); }
        i++;
      }
    }
    {
      var level:Dynamic = highest;
      while (FlightRuntime.truthy(FlightRuntime.compare(level, lowestOdd, '>='))) {
        {
          var k:Dynamic = 0.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(k, count, '<'))) {
            if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.getIndex(levels, FlightRuntime.getIndex(out, k)), level, '>='))) {
  var j:Dynamic = k;
  while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(j, count, '<'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(levels, FlightRuntime.getIndex(out, j)), level, '>=')))) { j++; }
  {
    var lo:Dynamic = k;
    var hi:Dynamic = (j - 1.0);
    while (FlightRuntime.truthy(FlightRuntime.compare(lo, hi, '<'))) {
      var tmp:Dynamic = FlightRuntime.getIndex(out, lo);
      FlightRuntime.setIndex(out, lo, FlightRuntime.getIndex(out, hi));
      FlightRuntime.setIndex(out, hi, tmp);
      ({ lo++; hi--; });
    }
  }
  (k = cast (j : Dynamic));
} else {
  k++;
}
          }
        }
        level--;
      }
    }
  }

  public static function resolveBidiLevels(text:String, baseDirection:BidiDirection):Dynamic {
    var length:Dynamic = cast FlightRuntime.UNDEFINED;
    var levels:Dynamic = cast FlightRuntime.UNDEFINED;
    var backend:Dynamic = cast FlightRuntime.UNDEFINED;
    var original:Array<BidiClass> = cast FlightRuntime.UNDEFINED;
    var paragraphLevel:Dynamic = cast FlightRuntime.UNDEFINED;
    var matchingPdi:Dynamic = cast FlightRuntime.UNDEFINED;
    var matchingInitiator:Dynamic = cast FlightRuntime.UNDEFINED;
    var working:Dynamic = cast FlightRuntime.UNDEFINED;
    var levelArray:Dynamic = cast FlightRuntime.UNDEFINED;
    length = FlightRuntime.field(text, 'length');
    levels = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Uint8Array'] : Array<Dynamic>)), [length]);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(length, 0.0))) { return cast levels; }
    backend = FlightRuntime.callValue(getBidiClassBackend, cast ([] : Array<Dynamic>));
    original = FlightRuntime.createArray(length);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, length, '<'))) {
        var codepoint:Dynamic = (cast FlightRuntime.codePointAt(text, i) : Float);
        var cls:Dynamic = FlightRuntime.callProperty(backend, 'getBidiClass', cast ([codepoint] : Array<Dynamic>));
        FlightRuntime.setIndex(original, i, cls);
        if (FlightRuntime.truthy(FlightRuntime.compare(codepoint, 65535.0, '>'))) {
  FlightRuntime.setIndex(original, (i + 1.0), cls);
  i++;
}
        i++;
      }
    }
    paragraphLevel = FlightRuntime.select(FlightRuntime.strictEquals(baseDirection, 'ltr'), function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.select(FlightRuntime.strictEquals(baseDirection, 'rtl'), function():Dynamic return cast 1.0, function():Dynamic return cast FlightRuntime.callValue(TextBidi.computeParagraphLevel__resolveBidiLevels, cast ([original, 0.0, length] : Array<Dynamic>))));
    matchingPdi = FlightRuntime.fill(FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Int32Array'] : Array<Dynamic>)), [length]), length, 0, null, 1);
    matchingInitiator = FlightRuntime.fill(FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Int32Array'] : Array<Dynamic>)), [length]), -1.0, 0, null, 1);
    FlightRuntime.callValue(TextBidi.pairIsolates__resolveBidiLevels, cast ([original, matchingPdi, matchingInitiator] : Array<Dynamic>));
    working = FlightRuntime.slice(original, 0, null);
    levelArray = FlightRuntime.createArray(length);
    FlightRuntime.callValue(TextBidi.applyExplicitLevels__resolveBidiLevels, cast ([original, working, levelArray, matchingPdi, paragraphLevel] : Array<Dynamic>));
    FlightRuntime.callValue(TextBidi.resolveIsolatingRunSequences__resolveBidiLevels, cast ([original, working, levelArray, matchingPdi, matchingInitiator, paragraphLevel] : Array<Dynamic>));
    FlightRuntime.callValue(TextBidi.applyLineReset__resolveBidiLevels, cast ([original, levelArray, paragraphLevel] : Array<Dynamic>));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, length, '<'))) {
        FlightRuntime.setIndex(levels, i, FlightRuntime.getIndex(levelArray, i));
        i++;
      }
    }
    return cast levels;
    return cast null;
  }

  public static function resolveIsolatingRunSequences__resolveBidiLevels(original:Array<BidiClass>, working:Array<BidiClass>, levelArray:Array<Float>, matchingPdi:Dynamic, matchingInitiator:Dynamic, paragraphLevel:Float):Void {
    var length:Dynamic = cast FlightRuntime.UNDEFINED;
    var kept:Array<Float> = cast FlightRuntime.UNDEFINED;
    var runs:Array<{ var indices:Array<Float>; var keptStart:Float; var keptEnd:Float; }> = cast FlightRuntime.UNDEFINED;
    var runStart:Dynamic = cast FlightRuntime.UNDEFINED;
    var runByFirst:Dynamic = cast FlightRuntime.UNDEFINED;
    length = FlightRuntime.field(original, 'length');
    kept = cast ([] : Array<Dynamic>);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, length, '<'))) {
        if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.getIndex(working, i), 'BN'))) { FlightRuntime.callProperty(kept, 'push', cast ([i] : Array<Dynamic>)); }
        i++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(kept, 'length'), 0.0))) { return; }
    runs = cast ([] : Array<Dynamic>);
    runStart = 0.0;
    {
      var k:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, FlightRuntime.field(kept, 'length'), '<='))) {
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(k, FlightRuntime.field(kept, 'length')), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.getIndex(levelArray, FlightRuntime.getIndex(kept, k)), FlightRuntime.getIndex(levelArray, FlightRuntime.getIndex(kept, runStart)))))) {
  var indices:Array<Float> = cast ([] : Array<Dynamic>);
  {
    var m:Dynamic = runStart;
    while (FlightRuntime.truthy(FlightRuntime.compare(m, k, '<'))) {
      FlightRuntime.callProperty(indices, 'push', cast ([FlightRuntime.getIndex(kept, m)] : Array<Dynamic>));
      m++;
    }
  }
  FlightRuntime.callProperty(runs, 'push', cast ([{ indices: indices, keptStart: runStart, keptEnd: k }] : Array<Dynamic>));
  (runStart = cast (k : Dynamic));
}
        k++;
      }
    }
    runByFirst = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []);
    {
      var r:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(r, FlightRuntime.field(runs, 'length'), '<'))) {
        FlightRuntime.callProperty(runByFirst, 'set', cast ([FlightRuntime.getIndex(FlightRuntime.field(FlightRuntime.getIndex(runs, r), 'indices'), 0.0), r] : Array<Dynamic>));
        r++;
      }
    }
    {
      var r:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(r, FlightRuntime.field(runs, 'length'), '<'))) {
        var firstIdx:Dynamic = FlightRuntime.getIndex(FlightRuntime.field(FlightRuntime.getIndex(runs, r), 'indices'), 0.0);
        if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(FlightRuntime.getIndex(original, firstIdx), 'PDI'), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.getIndex(matchingInitiator, firstIdx), -1.0)))) { r++; continue; }
        var sequence:Array<Float> = cast ([] : Array<Dynamic>);
        var keptStart:Dynamic = FlightRuntime.field(FlightRuntime.getIndex(runs, r), 'keptStart');
        var keptEnd:Dynamic = FlightRuntime.field(FlightRuntime.getIndex(runs, r), 'keptEnd');
        var current:Dynamic = r;
        {
          while (true) {
            var run:Dynamic = FlightRuntime.getIndex(runs, current);
            {
              var m:Dynamic = 0.0;
              while (FlightRuntime.truthy(FlightRuntime.compare(m, FlightRuntime.field(FlightRuntime.field(run, 'indices'), 'length'), '<'))) {
                FlightRuntime.callProperty(sequence, 'push', cast ([FlightRuntime.getIndex(FlightRuntime.field(run, 'indices'), m)] : Array<Dynamic>));
                m++;
              }
            }
            (keptEnd = cast (FlightRuntime.field(run, 'keptEnd') : Dynamic));
            var lastIdx:Dynamic = FlightRuntime.getIndex(FlightRuntime.field(run, 'indices'), (FlightRuntime.field(FlightRuntime.field(run, 'indices'), 'length') - 1.0));
            var lastType:Dynamic = FlightRuntime.getIndex(original, lastIdx);
            if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(lastType, 'LRI'), function():Dynamic return cast FlightRuntime.strictEquals(lastType, 'RLI')), function():Dynamic return cast FlightRuntime.strictEquals(lastType, 'FSI')), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(matchingPdi, lastIdx), length, '<')))) {
  var next:Dynamic = FlightRuntime.callProperty(runByFirst, 'get', cast ([FlightRuntime.getIndex(matchingPdi, lastIdx)] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(next, FlightRuntime.UNDEFINED))) { break; }
  (current = cast (next : Dynamic));
} else {
  break;
}
          }
        }
        FlightRuntime.callValue(TextBidi.resolveSequence__resolveBidiLevels, cast ([original, working, levelArray, kept, sequence, keptStart, keptEnd, matchingPdi, paragraphLevel] : Array<Dynamic>));
        r++;
      }
    }
  }

  public static function resolveSequence__resolveBidiLevels(original:Array<BidiClass>, working:Array<BidiClass>, levelArray:Array<Float>, kept:Array<Float>, sequence:Array<Float>, keptStart:Float, keptEnd:Float, matchingPdi:Dynamic, paragraphLevel:Float):Void {
    var seqLevel:Dynamic = cast FlightRuntime.UNDEFINED;
    var prevLevel:Dynamic = cast FlightRuntime.UNDEFINED;
    var sos:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastIdx:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastType:Dynamic = cast FlightRuntime.UNDEFINED;
    var endsUnmatchedIsolate:Dynamic = cast FlightRuntime.UNDEFINED;
    var nextLevel:Dynamic = cast FlightRuntime.UNDEFINED;
    var eos:Dynamic = cast FlightRuntime.UNDEFINED;
    var len:Dynamic = cast FlightRuntime.UNDEFINED;
    var ty:Array<BidiClass> = cast FlightRuntime.UNDEFINED;
    var prev:BidiClass = cast FlightRuntime.UNDEFINED;
    var strong:BidiClass = cast FlightRuntime.UNDEFINED;
    var embeddingDir:BidiClass = cast FlightRuntime.UNDEFINED;
    var even:Dynamic = cast FlightRuntime.UNDEFINED;
    seqLevel = FlightRuntime.getIndex(levelArray, FlightRuntime.getIndex(sequence, 0.0));
    prevLevel = FlightRuntime.select(FlightRuntime.compare(keptStart, 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(levelArray, FlightRuntime.getIndex(kept, (keptStart - 1.0))), function():Dynamic return cast paragraphLevel);
    sos = FlightRuntime.select(FlightRuntime.strictEquals((FlightRuntime.callProperty(HxMath, 'max', cast ([seqLevel, prevLevel] : Array<Dynamic>)) % 2.0), 1.0), function():Dynamic return cast 'R', function():Dynamic return cast 'L');
    lastIdx = FlightRuntime.getIndex(sequence, (FlightRuntime.field(sequence, 'length') - 1.0));
    lastType = FlightRuntime.getIndex(original, lastIdx);
    endsUnmatchedIsolate = FlightRuntime.andValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(lastType, 'LRI'), function():Dynamic return cast FlightRuntime.strictEquals(lastType, 'RLI')), function():Dynamic return cast FlightRuntime.strictEquals(lastType, 'FSI')), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(matchingPdi, lastIdx), FlightRuntime.field(original, 'length'), '>='));
    nextLevel = FlightRuntime.select(endsUnmatchedIsolate, function():Dynamic return cast paragraphLevel, function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(keptEnd, FlightRuntime.field(kept, 'length'), '<'), function():Dynamic return cast FlightRuntime.getIndex(levelArray, FlightRuntime.getIndex(kept, keptEnd)), function():Dynamic return cast paragraphLevel));
    eos = FlightRuntime.select(FlightRuntime.strictEquals((FlightRuntime.callProperty(HxMath, 'max', cast ([seqLevel, nextLevel] : Array<Dynamic>)) % 2.0), 1.0), function():Dynamic return cast 'R', function():Dynamic return cast 'L');
    len = FlightRuntime.field(sequence, 'length');
    ty = FlightRuntime.createArray(len);
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, len, '<'))) {
        FlightRuntime.setIndex(ty, k, FlightRuntime.getIndex(working, FlightRuntime.getIndex(sequence, k)));
        k++;
      }
    }
    prev = sos;
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, len, '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, k), 'NSM'))) {
  FlightRuntime.setIndex(ty, k, FlightRuntime.select(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(prev, 'LRI'), function():Dynamic return cast FlightRuntime.strictEquals(prev, 'RLI')), function():Dynamic return cast FlightRuntime.strictEquals(prev, 'FSI')), function():Dynamic return cast FlightRuntime.strictEquals(prev, 'PDI')), function():Dynamic return cast 'ON', function():Dynamic return cast prev));
}
        (prev = cast (FlightRuntime.getIndex(ty, k) : Dynamic));
        k++;
      }
    }
    strong = sos;
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, len, '<'))) {
        var c:Dynamic = FlightRuntime.getIndex(ty, k);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(c, 'L'), function():Dynamic return cast FlightRuntime.strictEquals(c, 'R')), function():Dynamic return cast FlightRuntime.strictEquals(c, 'AL')))) { (strong = cast (c : Dynamic)); } else { if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(c, 'EN'), function():Dynamic return cast FlightRuntime.strictEquals(strong, 'AL')))) { FlightRuntime.setIndex(ty, k, 'AN'); } }
        k++;
      }
    }
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, len, '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, k), 'AL'))) { FlightRuntime.setIndex(ty, k, 'R'); }
        k++;
      }
    }
    {
      var k:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, (len - 1.0), '<'))) {
        var c:Dynamic = FlightRuntime.getIndex(ty, k);
        if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.strictEquals(c, 'ES'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, (k - 1.0)), 'EN')), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, (k + 1.0)), 'EN')))) { FlightRuntime.setIndex(ty, k, 'EN'); } else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(c, 'CS'))) {
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, (k - 1.0)), 'EN'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, (k + 1.0)), 'EN')))) { FlightRuntime.setIndex(ty, k, 'EN'); } else { if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, (k - 1.0)), 'AN'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, (k + 1.0)), 'AN')))) { FlightRuntime.setIndex(ty, k, 'AN'); } }
} }
        k++;
      }
    }
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, len, '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, k), 'ET'))) {
  var j:Dynamic = k;
  while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(j, len, '<'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, j), 'ET')))) { j++; }
  var before:Dynamic = FlightRuntime.select(FlightRuntime.compare(k, 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(ty, (k - 1.0)), function():Dynamic return cast sos);
  var after:Dynamic = FlightRuntime.select(FlightRuntime.compare(j, len, '<'), function():Dynamic return cast FlightRuntime.getIndex(ty, j), function():Dynamic return cast eos);
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(before, 'EN'), function():Dynamic return cast FlightRuntime.strictEquals(after, 'EN')))) { {   var m:Dynamic = k;   while (FlightRuntime.truthy(FlightRuntime.compare(m, j, '<'))) {     FlightRuntime.setIndex(ty, m, 'EN');     m++;   } } }
  (k = cast (j : Dynamic));
} else {
  k++;
}
      }
    }
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, len, '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, k), 'ES'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, k), 'ET')), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(ty, k), 'CS')))) { FlightRuntime.setIndex(ty, k, 'ON'); }
        k++;
      }
    }
    (strong = cast (sos : Dynamic));
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, len, '<'))) {
        var c:Dynamic = FlightRuntime.getIndex(ty, k);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(c, 'L'), function():Dynamic return cast FlightRuntime.strictEquals(c, 'R')))) { (strong = cast (c : Dynamic)); } else { if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(c, 'EN'), function():Dynamic return cast FlightRuntime.strictEquals(strong, 'L')))) { FlightRuntime.setIndex(ty, k, 'L'); } }
        k++;
      }
    }
    embeddingDir = FlightRuntime.select(FlightRuntime.strictEquals((seqLevel % 2.0), 1.0), function():Dynamic return cast 'R', function():Dynamic return cast 'L');
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, len, '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.callValue(TextBidi.isNeutralOrIsolate__resolveBidiLevels, cast ([FlightRuntime.getIndex(ty, k)] : Array<Dynamic>)))) {
  var j:Dynamic = k;
  while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(j, len, '<'), function():Dynamic return cast FlightRuntime.callValue(TextBidi.isNeutralOrIsolate__resolveBidiLevels, cast ([FlightRuntime.getIndex(ty, j)] : Array<Dynamic>))))) { j++; }
  var before:Dynamic = FlightRuntime.select(FlightRuntime.compare(k, 0.0, '>'), function():Dynamic return cast FlightRuntime.callValue(TextBidi.neutralDirection__resolveBidiLevels, cast ([FlightRuntime.getIndex(ty, (k - 1.0))] : Array<Dynamic>)), function():Dynamic return cast sos);
  var after:Dynamic = FlightRuntime.select(FlightRuntime.compare(j, len, '<'), function():Dynamic return cast FlightRuntime.callValue(TextBidi.neutralDirection__resolveBidiLevels, cast ([FlightRuntime.getIndex(ty, j)] : Array<Dynamic>)), function():Dynamic return cast eos);
  var resolved:Dynamic = FlightRuntime.select(FlightRuntime.strictEquals(before, after), function():Dynamic return cast before, function():Dynamic return cast embeddingDir);
  {
    var m:Dynamic = k;
    while (FlightRuntime.truthy(FlightRuntime.compare(m, j, '<'))) {
      FlightRuntime.setIndex(ty, m, resolved);
      m++;
    }
  }
  (k = cast (j : Dynamic));
} else {
  k++;
}
      }
    }
    even = FlightRuntime.strictEquals((seqLevel % 2.0), 0.0);
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, len, '<'))) {
        var c:Dynamic = FlightRuntime.getIndex(ty, k);
        var lvl:Dynamic = seqLevel;
        if (FlightRuntime.truthy(even)) {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(c, 'R'))) { (lvl = cast ((seqLevel + 1.0) : Dynamic)); } else { if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(c, 'AN'), function():Dynamic return cast FlightRuntime.strictEquals(c, 'EN')))) { (lvl = cast ((seqLevel + 2.0) : Dynamic)); } }
} else { if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(c, 'L'), function():Dynamic return cast FlightRuntime.strictEquals(c, 'EN')), function():Dynamic return cast FlightRuntime.strictEquals(c, 'AN')))) {
  (lvl = cast ((seqLevel + 1.0) : Dynamic));
} }
        FlightRuntime.setIndex(levelArray, FlightRuntime.getIndex(sequence, k), lvl);
        k++;
      }
    }
  }

  public static final RLO__bidiClassBackend:Dynamic = 17.0;

  public static final S__bidiClassBackend:Dynamic = 11.0;

  public static final WS__bidiClassBackend:Dynamic = 12.0;

  public static final _ranges__bidiClassBackend:Array<Float> = cast ([0.0, 8.0, TextBidi.BN__bidiClassBackend, 9.0, 9.0, TextBidi.S__bidiClassBackend, 10.0, 10.0, TextBidi.B__bidiClassBackend, 11.0, 11.0, TextBidi.S__bidiClassBackend, 12.0, 12.0, TextBidi.WS__bidiClassBackend, 13.0, 13.0, TextBidi.B__bidiClassBackend, 14.0, 27.0, TextBidi.BN__bidiClassBackend, 28.0, 30.0, TextBidi.B__bidiClassBackend, 31.0, 31.0, TextBidi.S__bidiClassBackend, 32.0, 32.0, TextBidi.WS__bidiClassBackend, 33.0, 34.0, TextBidi.ON__bidiClassBackend, 35.0, 37.0, TextBidi.ET__bidiClassBackend, 38.0, 42.0, TextBidi.ON__bidiClassBackend, 43.0, 43.0, TextBidi.ES__bidiClassBackend, 44.0, 44.0, TextBidi.CS__bidiClassBackend, 45.0, 45.0, TextBidi.ES__bidiClassBackend, 46.0, 47.0, TextBidi.CS__bidiClassBackend, 48.0, 57.0, TextBidi.EN__bidiClassBackend, 58.0, 58.0, TextBidi.CS__bidiClassBackend, 59.0, 64.0, TextBidi.ON__bidiClassBackend, 65.0, 90.0, TextBidi.L__bidiClassBackend, 91.0, 96.0, TextBidi.ON__bidiClassBackend, 97.0, 122.0, TextBidi.L__bidiClassBackend, 123.0, 126.0, TextBidi.ON__bidiClassBackend, 127.0, 132.0, TextBidi.BN__bidiClassBackend, 133.0, 133.0, TextBidi.B__bidiClassBackend, 134.0, 159.0, TextBidi.BN__bidiClassBackend, 160.0, 160.0, TextBidi.CS__bidiClassBackend, 161.0, 161.0, TextBidi.ON__bidiClassBackend, 162.0, 165.0, TextBidi.ET__bidiClassBackend, 166.0, 169.0, TextBidi.ON__bidiClassBackend, 170.0, 170.0, TextBidi.L__bidiClassBackend, 171.0, 172.0, TextBidi.ON__bidiClassBackend, 173.0, 173.0, TextBidi.BN__bidiClassBackend, 174.0, 175.0, TextBidi.ON__bidiClassBackend, 176.0, 177.0, TextBidi.ET__bidiClassBackend, 178.0, 179.0, TextBidi.EN__bidiClassBackend, 180.0, 180.0, TextBidi.ON__bidiClassBackend, 181.0, 181.0, TextBidi.L__bidiClassBackend, 182.0, 184.0, TextBidi.ON__bidiClassBackend, 185.0, 185.0, TextBidi.EN__bidiClassBackend, 186.0, 186.0, TextBidi.L__bidiClassBackend, 187.0, 191.0, TextBidi.ON__bidiClassBackend, 192.0, 214.0, TextBidi.L__bidiClassBackend, 215.0, 215.0, TextBidi.ON__bidiClassBackend, 216.0, 246.0, TextBidi.L__bidiClassBackend, 247.0, 247.0, TextBidi.ON__bidiClassBackend, 248.0, 255.0, TextBidi.L__bidiClassBackend, 768.0, 879.0, TextBidi.NSM__bidiClassBackend, 1424.0, 1424.0, TextBidi.R__bidiClassBackend, 1425.0, 1469.0, TextBidi.NSM__bidiClassBackend, 1470.0, 1470.0, TextBidi.R__bidiClassBackend, 1471.0, 1471.0, TextBidi.NSM__bidiClassBackend, 1472.0, 1472.0, TextBidi.R__bidiClassBackend, 1473.0, 1474.0, TextBidi.NSM__bidiClassBackend, 1475.0, 1475.0, TextBidi.R__bidiClassBackend, 1476.0, 1477.0, TextBidi.NSM__bidiClassBackend, 1478.0, 1478.0, TextBidi.R__bidiClassBackend, 1479.0, 1479.0, TextBidi.NSM__bidiClassBackend, 1480.0, 1535.0, TextBidi.R__bidiClassBackend, 1536.0, 1541.0, TextBidi.AN__bidiClassBackend, 1542.0, 1543.0, TextBidi.ON__bidiClassBackend, 1544.0, 1544.0, TextBidi.AL__bidiClassBackend, 1545.0, 1546.0, TextBidi.ET__bidiClassBackend, 1547.0, 1547.0, TextBidi.AL__bidiClassBackend, 1548.0, 1548.0, TextBidi.CS__bidiClassBackend, 1549.0, 1551.0, TextBidi.AL__bidiClassBackend, 1552.0, 1562.0, TextBidi.NSM__bidiClassBackend, 1563.0, 1610.0, TextBidi.AL__bidiClassBackend, 1611.0, 1631.0, TextBidi.NSM__bidiClassBackend, 1632.0, 1641.0, TextBidi.AN__bidiClassBackend, 1642.0, 1642.0, TextBidi.ET__bidiClassBackend, 1643.0, 1644.0, TextBidi.AN__bidiClassBackend, 1645.0, 1647.0, TextBidi.AL__bidiClassBackend, 1648.0, 1648.0, TextBidi.NSM__bidiClassBackend, 1649.0, 1749.0, TextBidi.AL__bidiClassBackend, 1750.0, 1756.0, TextBidi.NSM__bidiClassBackend, 1757.0, 1758.0, TextBidi.AN__bidiClassBackend, 1759.0, 1764.0, TextBidi.NSM__bidiClassBackend, 1765.0, 1766.0, TextBidi.AL__bidiClassBackend, 1767.0, 1768.0, TextBidi.NSM__bidiClassBackend, 1769.0, 1769.0, TextBidi.ON__bidiClassBackend, 1770.0, 1773.0, TextBidi.NSM__bidiClassBackend, 1774.0, 1775.0, TextBidi.AL__bidiClassBackend, 1776.0, 1785.0, TextBidi.EN__bidiClassBackend, 1786.0, 1791.0, TextBidi.AL__bidiClassBackend, 1872.0, 1919.0, TextBidi.AL__bidiClassBackend, 2208.0, 2303.0, TextBidi.AL__bidiClassBackend, 5760.0, 5760.0, TextBidi.WS__bidiClassBackend, 8192.0, 8202.0, TextBidi.WS__bidiClassBackend, 8203.0, 8205.0, TextBidi.BN__bidiClassBackend, 8206.0, 8206.0, TextBidi.L__bidiClassBackend, 8207.0, 8207.0, TextBidi.R__bidiClassBackend, 8208.0, 8231.0, TextBidi.ON__bidiClassBackend, 8232.0, 8232.0, TextBidi.WS__bidiClassBackend, 8233.0, 8233.0, TextBidi.B__bidiClassBackend, 8234.0, 8234.0, TextBidi.LRE__bidiClassBackend, 8235.0, 8235.0, TextBidi.RLE__bidiClassBackend, 8236.0, 8236.0, TextBidi.PDF__bidiClassBackend, 8237.0, 8237.0, TextBidi.LRO__bidiClassBackend, 8238.0, 8238.0, TextBidi.RLO__bidiClassBackend, 8239.0, 8239.0, TextBidi.CS__bidiClassBackend, 8240.0, 8244.0, TextBidi.ET__bidiClassBackend, 8245.0, 8281.0, TextBidi.ON__bidiClassBackend, 8287.0, 8287.0, TextBidi.WS__bidiClassBackend, 8288.0, 8292.0, TextBidi.BN__bidiClassBackend, 8294.0, 8294.0, TextBidi.LRI__bidiClassBackend, 8295.0, 8295.0, TextBidi.RLI__bidiClassBackend, 8296.0, 8296.0, TextBidi.FSI__bidiClassBackend, 8297.0, 8297.0, TextBidi.PDI__bidiClassBackend, 12288.0, 12288.0, TextBidi.WS__bidiClassBackend, 64285.0, 64285.0, TextBidi.R__bidiClassBackend, 64286.0, 64286.0, TextBidi.NSM__bidiClassBackend, 64287.0, 64335.0, TextBidi.R__bidiClassBackend, 64336.0, 64975.0, TextBidi.AL__bidiClassBackend, 65008.0, 65023.0, TextBidi.AL__bidiClassBackend, 65136.0, 65278.0, TextBidi.AL__bidiClassBackend, 65279.0, 65279.0, TextBidi.BN__bidiClassBackend] : Array<Dynamic>);

  public static function setBidiClassBackend(backend:Null<BidiClassBackend>):Void {
    (TextBidi._backend__bidiClassBackend = cast (backend : Dynamic));
  }

  public static final _rangeCount__bidiClassBackend:Dynamic = (FlightRuntime.field(TextBidi._ranges__bidiClassBackend, 'length') / 3.0);
}
