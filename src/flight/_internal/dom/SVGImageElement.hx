// Maintained host-type declaration stub. Canvas/GL/WGPU adapters consume this
// host image source without inspecting SVG-specific members.
package flight._internal.dom;

typedef SVGImageElement = Dynamic;
