// Maintained host-type declaration stub. Replace this toolkit entry with a
// concrete adapter before a non-JavaScript MIDI implementation is enabled.
package flight._internal.dom;

typedef MIDIOptions = Dynamic;
