// Maintained host-type declaration stub. This is a live DOM collection rather
// than an Array; a portable DOM adapter must provide its concrete collection.
package flight._internal.dom;

typedef NodeListOf<T> = Dynamic;
