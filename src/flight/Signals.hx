// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Types.Signal;
import flight.Types.SignalConnectOptions;
import flight.Types.SignalData;

typedef SignalThrottleOptions = { @:optional var leading:Bool; @:optional var trailing:Bool; };

@:expose("flight.Signals")
class Signals {
  public static function cancelSignal<T>(signal:Signal<Dynamic>):Void {
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(signal, 'data'), null))) { FlightRuntime.setField(FlightRuntime.field(signal, 'data'), 'cancelled', true); }
  }

  public static function clearSignal<T>(signal:Signal<Dynamic>):Void {
    FlightRuntime.setField(signal, 'emit', (cast (cast nullSignalEmit : Dynamic) : Dynamic));
    FlightRuntime.setField(signal, 'data', null);
  }

  public static function connectSignal<T>(signal:Signal<Dynamic>, slot:Dynamic, ?options:SignalConnectOptions):Void {
    var priority:Dynamic = cast FlightRuntime.UNDEFINED;
    var repeat:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    priority = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'priority'), function():Dynamic return cast 0.0);
    repeat = !FlightRuntime.truthy(FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'once'), function():Dynamic return cast false));
    FlightRuntime.callValue(Signals.initSignal__slot, cast ([signal] : Array<Dynamic>));
    data = FlightRuntime.field(signal, 'data');
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(FlightRuntime.field(data, 'priorities'), 'length'), '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.compare(priority, FlightRuntime.getIndex(FlightRuntime.field(data, 'priorities'), i), '>'))) {
  FlightRuntime.splice(FlightRuntime.field(data, 'slots'), Std.int(i), Std.int(0.0), [slot]);
  FlightRuntime.splice(FlightRuntime.field(data, 'priorities'), Std.int(i), Std.int(0.0), [priority]);
  FlightRuntime.splice(FlightRuntime.field(data, 'repeat'), Std.int(i), Std.int(0.0), [repeat]);
  return;
}
        i++;
      }
    }
    FlightRuntime.callProperty(FlightRuntime.field(data, 'slots'), 'push', cast ([slot] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(data, 'priorities'), 'push', cast ([priority] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(data, 'repeat'), 'push', cast ([repeat] : Array<Dynamic>));
  }

  public static function connectSignalAtFrameRate(source:Signal<Dynamic>, fps:Float, slot:Dynamic):Dynamic {
    var period:Dynamic = cast FlightRuntime.UNDEFINED;
    var elapsed:Dynamic = cast FlightRuntime.UNDEFINED;
    var handler:Dynamic = cast FlightRuntime.UNDEFINED;
    period = (1000.0 / fps);
    elapsed = 0.0;
    handler = function(delta:Float) {
  (elapsed = cast ((elapsed + delta) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.compare(elapsed, period, '>='))) {
  FlightRuntime.callValue(slot, cast ([elapsed] : Array<Dynamic>));
  (elapsed = cast ((elapsed % period) : Dynamic));
}
};
    FlightRuntime.callValue(connectSignal, cast ([source, handler] : Array<Dynamic>));
    return cast function() return FlightRuntime.callValue(disconnectSignal, cast ([source, handler] : Array<Dynamic>));
    return cast null;
  }

  public static function connectSignalDebounced<T>(source:Signal<Dynamic>, delayMs:Float, slot:Dynamic, ?options:SignalThrottleOptions):Dynamic {
    var leading:Dynamic = cast FlightRuntime.UNDEFINED;
    var trailing:Dynamic = cast FlightRuntime.UNDEFINED;
    var timer:Null<Dynamic> = cast FlightRuntime.UNDEFINED;
    var lastArgs:Null<Array<Dynamic>> = cast FlightRuntime.UNDEFINED;
    var leadingFired:Dynamic = cast FlightRuntime.UNDEFINED;
    var clearTimer:Dynamic = cast FlightRuntime.UNDEFINED;
    var handler:Dynamic = cast FlightRuntime.UNDEFINED;
    leading = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'leading'), function():Dynamic return cast false);
    trailing = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'trailing'), function():Dynamic return cast true);
    timer = null;
    lastArgs = null;
    leadingFired = false;
    clearTimer = function() {
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(timer, null))) {
  FlightRuntime.clearTimeout(timer);
  (timer = cast (null : Dynamic));
}
};
    handler = (cast function(...args:Dynamic) {
  (lastArgs = cast (args : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(leading, function():Dynamic return cast FlightRuntime.strictEquals(timer, null)), function():Dynamic return cast !FlightRuntime.truthy(leadingFired)))) {
  (leadingFired = cast (true : Dynamic));
  FlightRuntime.apply(slot, FlightRuntime.concatArrays([FlightRuntime.toArray(args)]));
}
  FlightRuntime.callValue(clearTimer, cast ([] : Array<Dynamic>));
  (timer = cast (FlightRuntime.setTimeout(function() {
  (timer = cast (null : Dynamic));
  (leadingFired = cast (false : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.andValue(trailing, function():Dynamic return cast !FlightRuntime.strictEquals(lastArgs, null)))) {
  FlightRuntime.apply(slot, FlightRuntime.concatArrays([FlightRuntime.toArray(lastArgs)]));
  (lastArgs = cast (null : Dynamic));
}
}, delayMs) : Dynamic));
} : Dynamic);
    FlightRuntime.callValue(connectSignal, cast ([source, handler] : Array<Dynamic>));
    return cast function() {
  FlightRuntime.callValue(disconnectSignal, cast ([source, handler] : Array<Dynamic>));
  FlightRuntime.callValue(clearTimer, cast ([] : Array<Dynamic>));
};
    return cast null;
  }

  public static function connectSignalThrottled<T>(source:Signal<Dynamic>, intervalMs:Float, slot:Dynamic, ?options:SignalThrottleOptions):Dynamic {
    var leading:Dynamic = cast FlightRuntime.UNDEFINED;
    var trailing:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastFiredAt:Dynamic = cast FlightRuntime.UNDEFINED;
    var trailingTimer:Null<Dynamic> = cast FlightRuntime.UNDEFINED;
    var lastArgs:Null<Array<Dynamic>> = cast FlightRuntime.UNDEFINED;
    var clearTrailing:Dynamic = cast FlightRuntime.UNDEFINED;
    var handler:Dynamic = cast FlightRuntime.UNDEFINED;
    leading = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'leading'), function():Dynamic return cast true);
    trailing = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'trailing'), function():Dynamic return cast true);
    lastFiredAt = -HxMath.POSITIVE_INFINITY;
    trailingTimer = null;
    lastArgs = null;
    clearTrailing = function() {
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(trailingTimer, null))) {
  FlightRuntime.clearTimeout(trailingTimer);
  (trailingTimer = cast (null : Dynamic));
}
};
    handler = (cast function(...args:Dynamic) {
  var now:Dynamic = cast FlightRuntime.UNDEFINED;
  var remaining:Dynamic = cast FlightRuntime.UNDEFINED;
  now = FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Date'] : Array<Dynamic>)), 'now', cast ([] : Array<Dynamic>));
  remaining = (intervalMs - (now - lastFiredAt));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(remaining, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(remaining, intervalMs, '>')))) {
  FlightRuntime.callValue(clearTrailing, cast ([] : Array<Dynamic>));
  (lastFiredAt = cast (now : Dynamic));
  if (FlightRuntime.truthy(leading)) {
  FlightRuntime.apply(slot, FlightRuntime.concatArrays([FlightRuntime.toArray(args)]));
} else {
  (lastArgs = cast (args : Dynamic));
}
} else { if (FlightRuntime.truthy(trailing)) {
  FlightRuntime.callValue(clearTrailing, cast ([] : Array<Dynamic>));
  (lastArgs = cast (args : Dynamic));
  (trailingTimer = cast (FlightRuntime.setTimeout(function() {
  (lastFiredAt = cast (FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Date'] : Array<Dynamic>)), 'now', cast ([] : Array<Dynamic>)) : Dynamic));
  (trailingTimer = cast (null : Dynamic));
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(lastArgs, null))) {
  FlightRuntime.apply(slot, FlightRuntime.concatArrays([FlightRuntime.toArray(lastArgs)]));
  (lastArgs = cast (null : Dynamic));
}
}, remaining) : Dynamic));
} }
} : Dynamic);
    FlightRuntime.callValue(connectSignal, cast ([source, handler] : Array<Dynamic>));
    return cast function() {
  FlightRuntime.callValue(disconnectSignal, cast ([source, handler] : Array<Dynamic>));
  FlightRuntime.callValue(clearTrailing, cast ([] : Array<Dynamic>));
};
    return cast null;
  }

  public static function createSignal<T>():Signal<Dynamic> {
    return cast { emit: (cast (cast nullSignalEmit : Dynamic) : Dynamic), data: null };
    return cast null;
  }

  public static function disconnectSignal<T>(signal:Signal<Dynamic>, slot:Dynamic):Void {
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    data = FlightRuntime.field(signal, 'data');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(data, null))) { return; }
    i = FlightRuntime.field(FlightRuntime.field(data, 'slots'), 'length');
    while (FlightRuntime.truthy(FlightRuntime.compare(--i, 0.0, '>='))) {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.getIndex(FlightRuntime.field(data, 'slots'), i), slot))) {
  FlightRuntime.splice(FlightRuntime.field(data, 'slots'), Std.int(i), Std.int(1.0), []);
  FlightRuntime.splice(FlightRuntime.field(data, 'priorities'), Std.int(i), Std.int(1.0), []);
  FlightRuntime.splice(FlightRuntime.field(data, 'repeat'), Std.int(i), Std.int(1.0), []);
}
}
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(data, 'slots'), 'length'), 0.0))) {
  FlightRuntime.setField(signal, 'emit', (cast (cast nullSignalEmit : Dynamic) : Dynamic));
  FlightRuntime.setField(signal, 'data', null);
}
  }

  public static function emitSignal<T>(signal:Signal<Dynamic>, ...args:Dynamic):Void {
    FlightRuntime.apply((cast FlightRuntime.field(signal, 'emit') : Dynamic), FlightRuntime.concatArrays([FlightRuntime.toArray(args)]));
  }

  public static function hasSignalSlots<T>(signal:Signal<Dynamic>):Bool {
    return cast FlightRuntime.andValue(!FlightRuntime.strictEquals(FlightRuntime.field(signal, 'data'), null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(FlightRuntime.field(signal, 'data'), 'slots'), 'length'), 0.0, '>'));
    return cast null;
  }

  public static function initSignal__slot<T>(signal:Signal<Dynamic>):Void {
    var data:SignalData<Dynamic> = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(signal, 'data'), null))) { return; }
    data = { slots: cast ([] : Array<Dynamic>), priorities: cast ([] : Array<Dynamic>), repeat: cast ([] : Array<Dynamic>), cancelled: false };
    FlightRuntime.setField(signal, 'data', data);
    FlightRuntime.setField(signal, 'emit', FlightRuntime.callValue(Signals.makeDispatch__slot, cast ([data] : Array<Dynamic>)));
  }

  public static function isSlotConnected<T>(signal:Signal<Dynamic>, slot:Dynamic):Bool {
    return cast FlightRuntime.andValue(!FlightRuntime.strictEquals(FlightRuntime.field(signal, 'data'), null), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(signal, 'data'), 'slots'), 'indexOf', cast ([slot] : Array<Dynamic>)), -1.0));
    return cast null;
  }

  public static function makeDispatch__slot<T>(data:SignalData<Dynamic>):Dynamic {
    return cast (cast (cast function(...args:Dynamic) {
  var i:Dynamic = cast FlightRuntime.UNDEFINED;
  FlightRuntime.setField(data, 'cancelled', false);
  i = 0.0;
  while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(FlightRuntime.field(data, 'slots'), 'length'), '<'))) {
  FlightRuntime.apply(FlightRuntime.getIndex(FlightRuntime.field(data, 'slots'), i), FlightRuntime.concatArrays([FlightRuntime.toArray(args)]));
  if (FlightRuntime.truthy(FlightRuntime.field(data, 'cancelled'))) { break; }
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.getIndex(FlightRuntime.field(data, 'repeat'), i)))) {
  FlightRuntime.splice(FlightRuntime.field(data, 'slots'), Std.int(i), Std.int(1.0), []);
  FlightRuntime.splice(FlightRuntime.field(data, 'priorities'), Std.int(i), Std.int(1.0), []);
  FlightRuntime.splice(FlightRuntime.field(data, 'repeat'), Std.int(i), Std.int(1.0), []);
} else {
  i++;
}
}
} : Dynamic) : Dynamic);
    return cast null;
  }

  public static final nullSignalEmit:Dynamic = function() {

};
}
