// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Color.computeRgbHexString;
import flight.DisplayObjectApi.getDisplayObjectRuntime;
import flight.DisplayObjectCanvas.defaultCanvasBeginBitmapFill as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasBeginBitmapFill;
import flight.DisplayObjectCanvas.defaultCanvasBeginFill as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasBeginFill;
import flight.DisplayObjectCanvas.defaultCanvasBeginGradientFill as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasBeginGradientFill;
import flight.DisplayObjectCanvas.defaultCanvasCubicCurveTo as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasCubicCurveTo;
import flight.DisplayObjectCanvas.defaultCanvasCurveTo as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasCurveTo;
import flight.DisplayObjectCanvas.defaultCanvasDrawCircle as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasDrawCircle;
import flight.DisplayObjectCanvas.defaultCanvasDrawEllipse as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasDrawEllipse;
import flight.DisplayObjectCanvas.defaultCanvasDrawRectangle as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasDrawRectangle;
import flight.DisplayObjectCanvas.defaultCanvasDrawRoundRectangle as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasDrawRoundRectangle;
import flight.DisplayObjectCanvas.defaultCanvasEndFill as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasEndFill;
import flight.DisplayObjectCanvas.defaultCanvasLineStyle as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasLineStyle;
import flight.DisplayObjectCanvas.defaultCanvasLineTo as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasLineTo;
import flight.DisplayObjectCanvas.defaultCanvasMoveTo as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasMoveTo;
import flight.DisplayObjectCanvas.defaultCanvasShapeCommands as facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasShapeCommands;
import flight.DisplayObjectCanvas.mapCanvasScale9ShapeCommands;
import flight.DisplayObjectCanvas.registerCanvasShapeCommands as facadeDisplayObjectWgpuDisplayObjectCanvasRegisterCanvasShapeCommands;
import flight.DisplayObjectCanvas.renderCanvasShapeCommands;
import flight.Geometry.createMatrix;
import flight.Geometry.createRectangle;
import flight.Geometry.multiplyMatrix;
import flight.Image.createImageResource;
import flight.Image.hasImageResourcePixels;
import flight.Image.setImageResourceSource;
import flight.Log.logOnce;
import flight.NodeApi.computeNodeBoundsRectangle;
import flight.NodeApi.ensureNodeWorldBoundsRectangle;
import flight.NodeApi.ensureNodeWorldMatrix;
import flight.NodeApi.getNodeChildAt;
import flight.NodeApi.getNodeChildCount;
import flight.NodeApi.getNodeLocalBoundsRectangle;
import flight.NodeApi.getNodeLocalContentRevision;
import flight.NodeApi.getNodeWorldBoundsRectangle;
import flight.NodeApi.getNodeWorldMatrix;
import flight.PathApi.tessellatePath;
import flight.Render.computeDisplayObjectRenderTargetTransform;
import flight.Render.computeRenderCacheTransform;
import flight.Render.computeRenderTargetSize;
import flight.Render.copyAllRenderersFromRenderState;
import flight.Render.createRenderState;
import flight.Render.getRenderProxy2D;
import flight.Render.getRenderProxyCache;
import flight.Render.isRenderProxyVisible;
import flight.Render.noopRendererData;
import flight.Render.prepareDisplayObjectRender;
import flight.Render.registerRenderCacheRenderer;
import flight.Render.registerRenderer;
import flight.RenderWgpu.beginWgpuRenderPass;
import flight.RenderWgpu.bindWgpuImageResourceTexture;
import flight.RenderWgpu.createWgpuRenderStateRuntime;
import flight.RenderWgpu.createWgpuRenderTarget;
import flight.RenderWgpu.createWgpuTextureEntry;
import flight.RenderWgpu.destroyWgpuRenderTarget;
import flight.RenderWgpu.drawWgpuQuad;
import flight.RenderWgpu.drawWgpuQuadWithTransform;
import flight.RenderWgpu.drawWgpuRenderTargetResult;
import flight.RenderWgpu.endWgpuRenderPass;
import flight.RenderWgpu.getWgpuRenderStateRuntime;
import flight.RenderWgpu.registerWgpuMaterialRenderer;
import flight.RenderWgpu.resizeWgpuRenderTarget;
import flight.RenderWgpu.resolveWgpuMaterialRenderer;
import flight.RenderWgpu.resolveWgpuShader;
import flight.RenderWgpu.setWgpuRenderTransform2D;
import flight.RenderWgpu.updateWgpuTextureEntry;
import flight.ShapeApi.getShapeFillRegions;
import flight.Text.computeTextFormatFontString;
import flight.Text.getRichTextPasswordCharacter;
import flight.Text.getRichTextRuntime;
import flight.Text.getTextLabelRuntime;
import flight.TextInput.getTextInputCaretRectangle;
import flight.TextInput.getTextInputSelectionBeginIndex;
import flight.TextInput.getTextInputSelectionEndIndex;
import flight.TextInput.getTextInputSelectionRectangles;
import flight.TextInput.getTextInputState;
import flight.TextLayout.computeRichTextContent;
import flight.TextLayout.computeTextBoundsHeight;
import flight.TextLayout.computeTextBoundsOffsetX;
import flight.TextLayout.computeTextBoundsWidth;
import flight.TextLayout.computeTextLayout;
import flight.TextLayout.createTextFormatRange;
import flight.TextLayout.getRichTextContent;
import flight.TextLayout.getRichTextScrollYOffset;
import flight.TextLayout.getTextLayoutResult;
import flight.Types.BatchFormat;
import flight.Types.Bitmap;
import flight.Types.BlendMode;
import flight.Types.ColorTransform;
import flight.Types.DisplayObject;
import flight.Types.DisplayObjectClipHooks;
import flight.Types.DisplayObjectRenderer;
import flight.Types.EntityRuntimeKey;
import flight.Types.ImageResource;
import flight.Types.Kind;
import flight.Types.LogLevel;
import flight.Types.Material;
import flight.Types.MaterialData;
import flight.Types.Matrix;
import flight.Types.MatrixLike;
import flight.Types.ParticleEmitter;
import flight.Types.PathWinding;
import flight.Types.QuadBatch;
import flight.Types.QuadBatchRuntime;
import flight.Types.RectangleLike;
import flight.Types.RenderCache;
import flight.Types.RenderCacheKind;
import flight.Types.RenderCacheRefreshOptions;
import flight.Types.RenderProxy2D;
import flight.Types.RenderState;
import flight.Types.Renderable;
import flight.Types.RendererData;
import flight.Types.RichText;
import flight.Types.RichTextRuntime;
import flight.Types.Scale9Mapper;
import flight.Types.Scale9Shape;
import flight.Types.Shape;
import flight.Types.Spatial2DNode;
import flight.Types.Sprite;
import flight.Types.SpriteRenderer;
import flight.Types.TextFormat;
import flight.Types.TextLabel;
import flight.Types.TextLabelRuntime;
import flight.Types.TextLayoutResult;
import flight.Types.TextSelectionRectangle;
import flight.Types.Tilemap;
import flight.Types.Transform2DNode;
import flight.Types.Velocity2D;
import flight.Types.VelocityField;
import flight.Types.Video;
import flight.Types.WgpuClipContourPipelines;
import flight.Types.WgpuColorAdjustmentFlush;
import flight.Types.WgpuColorAdjustmentFold;
import flight.Types.WgpuMaterialRenderer;
import flight.Types.WgpuRenderState;
import flight.Types.WgpuRenderStateRuntime;
import flight.Types.WgpuRenderStats;
import flight.Types.WgpuRenderTarget;
import flight.Types.WgpuScissorRect;
import flight.Types.WgpuShapeMeshBuffers;
import flight.Types.WgpuShapeMeshPipeline;
import flight.Types.WgpuSpriteBatchBufferSlot;
import flight.Types.WgpuTextureEntry;
import flight.Types.WgpuVelocityContext;
import flight.Types.WgpuVelocityWriter;
import flight.Velocity.getVelocity;

typedef Mutable__wgpuRenderStats<T> = Dynamic;

typedef WgpuParticleResources__wgpuParticleEmitter = { var pipelines:Dynamic; var pipelineLayout:Dynamic; var module:Dynamic; var instanceBindGroupLayout:Dynamic; };

typedef WgpuQuadBatchResources = { var instanceBindGroupLayout:Dynamic; var materialBindGroupLayout:Dynamic; var basePipelineLayout:Dynamic; var materialPipelineLayout:Dynamic; var pipelines:Dynamic; };

typedef WgpuRichTextData__wgpuRichText = { var entry:Null<WgpuTextureEntry>; var w:Float; var h:Float; };

typedef WgpuRichTextOverlay = Dynamic;

typedef WgpuScale9ShapeData__wgpuScale9Shape = { var canvas:Dynamic; var ctx:Dynamic; var lastH:Float; var lastScaleX:Float; var lastScaleY:Float; var lastContentId:Float; var lastW:Float; var entry:Null<WgpuTextureEntry>; };

typedef WgpuShapeData__wgpuShape = { var canvas:Dynamic; var ctx:Dynamic; var image:ImageResource; var lastContentId:Float; var lastW:Float; var lastH:Float; var meshVersion:Float; var meshes:Null<Array<WgpuShapeMesh>>; var meshBuffers:WgpuShapeMeshBuffers; };

typedef WgpuShapeMesh = { var vertices:flight.internal.FlightFloat32Array; var indices:flight.internal.FlightUInt16Array; var color:Float; var alpha:Float; };

typedef WgpuTextLabelData__wgpuTextLabel = { var canvas:Dynamic; var ctx:Dynamic; var image:ImageResource; var lastContentId:Float; var lastPixelRatio:Float; var logW:Float; var logH:Float; var lastPW:Float; var lastPH:Float; };

typedef WgpuVelocityPipeline__wgpuVelocity = { var pipeline:Dynamic; var uniformBuffer:Dynamic; var bindGroup:Dynamic; var cursor:Float; var scratch:flight.internal.FlightFloat32Array; };

typedef WgpuVideoData__wgpuVideo = { var entry:Null<WgpuTextureEntry>; var w:Float; var h:Float; };

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.DisplayObjectWgpu")
class DisplayObjectWgpu {
  public static final _activeVelocityPasses__wgpuVelocity:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _bakeTransform__wgpuCache:Dynamic = (cast FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>)) : Matrix);

  public static final _bounds__wgpuCache:Dynamic = FlightRuntime.callValue(createRectangle, cast ([] : Array<Dynamic>));

  public static final _cacheStateScreen__wgpuCache:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _colorTransformModules__wgpuColorAdjustment:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _identity__wgpuCache:Dynamic = (cast FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>)) : Matrix);

  public static final _modules__wgpuDefaultMaterial:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static var _offscreenCanvas__wgpuRichText:Null<Dynamic> = FlightRuntime.explicitNull();

  public static var _offscreenCtx__wgpuRichText:Null<Dynamic> = FlightRuntime.explicitNull();

  public static final _particleResources__wgpuParticleEmitter:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _quadBatchResources__wgpuSpriteBatch:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _remappedCommands__wgpuScale9Shape:Array<Dynamic> = cast ([] : Array<Dynamic>);

  public static final _remappedPathData__wgpuScale9Shape:Array<Float> = cast ([] : Array<Dynamic>);

  public static final _renderCacheTargets__wgpuCache:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _renderTransform__wgpuCache:Dynamic = (cast FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>)) : Matrix);

  public static final _scratchVelocity__wgpuVelocity:Velocity2D = { x: 0.0, y: 0.0 };

  public static var _shapeMeshIndexScratch__wgpuShapeMesh:Dynamic = new flight.internal.FlightUInt16Array(0.0);

  public static final _stats__wgpuRenderStats:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _velocityPipelines__wgpuVelocity:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _velocityWriters__wgpuVelocity:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static var _webgpuTextInputOverlay__wgpuRichText:Null<WgpuRichTextOverlay> = FlightRuntime.explicitNull();

  public static final _yInvert__wgpuCache:Dynamic = (cast FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>)) : Matrix);

  public static final ADD_BLEND__wgpuSpriteBatch:Dynamic = { color: { srcFactor: 'one', dstFactor: 'one', operation: 'add' }, alpha: { srcFactor: 'one', dstFactor: 'one', operation: 'add' } };

  @:keep public static function acquireWgpuSpriteBatchBufferSlot__wgpuSpriteBatch(state:WgpuRenderState):WgpuSpriteBatchBufferSlot {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var pool:Dynamic = cast FlightRuntime.UNDEFINED;
    var slot:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    pool = FlightRuntime.field(runtime, 'spriteBatchBufferPool');
    slot = FlightRuntime.getIndex(pool, FlightRuntime.field(runtime, 'spriteBatchBufferCursor'));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(slot, FlightRuntime.UNDEFINED))) {
  (slot = cast ({ instanceBuffer: null, instanceCapacity: 0.0, materialBuffer: null, materialCapacity: 0.0 } : Dynamic));
  FlightRuntime.setIndex(pool, FlightRuntime.field(runtime, 'spriteBatchBufferCursor'), slot);
}
    FlightRuntime.incrementField(runtime, 'spriteBatchBufferCursor', 1, true);
    return cast slot;
    return cast null;
  }

  public static final CARET_COLOR__wgpuTextInput:Dynamic = '#000000';

  @:keep public static function areWgpuColorAdjustmentGuardsEnabled(state:WgpuRenderState):Bool {
    return cast !FlightRuntime.looseEquals(FlightRuntime.field(FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>)), 'wgpuColorAdjustmentGuard'), null);
    return cast null;
  }

  @:keep public static function buildWgpuScale9Mapper(bounds:RectangleLike, scale9Grid:RectangleLike, scaleX:Float, scaleY:Float):Null<Scale9Mapper> {
    var gx:Dynamic = cast FlightRuntime.UNDEFINED;
    var gy:Dynamic = cast FlightRuntime.UNDEFINED;
    var gw:Dynamic = cast FlightRuntime.UNDEFINED;
    var gh:Dynamic = cast FlightRuntime.UNDEFINED;
    var bw:Dynamic = cast FlightRuntime.UNDEFINED;
    var bh:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.compare(scaleX, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(scaleY, 0.0, '<=')), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(bounds, 'width'), 0.0, '<=')), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(bounds, 'height'), 0.0, '<=')))) { return cast null; }
    gx = FlightRuntime.field(scale9Grid, 'x');
    gy = FlightRuntime.field(scale9Grid, 'y');
    gw = FlightRuntime.field(scale9Grid, 'width');
    gh = FlightRuntime.field(scale9Grid, 'height');
    bw = FlightRuntime.field(bounds, 'width');
    bh = FlightRuntime.field(bounds, 'height');
    return cast { mapX: function(x:Float) return FlightRuntime.callValue(DisplayObjectWgpu.toScale9Position__wgpuScale9Mapper, cast ([x, gx, gw, bw, scaleX] : Array<Dynamic>)), mapY: function(y:Float) return FlightRuntime.callValue(DisplayObjectWgpu.toScale9Position__wgpuScale9Mapper, cast ([y, gy, gh, bh, scaleY] : Array<Dynamic>)) };
    return cast null;
  }

  public static final CARET_WIDTH__wgpuTextInput:Dynamic = 1.0;

  public static final caretRectangle__wgpuTextInput:Dynamic = { height: 0.0, lineIndex: 0.0, width: 0.0, x: 0.0, y: 0.0 };

  public static final CLIP_UNIFORM_BYTES__wgpuClipContours:Dynamic = 48.0;

  public static final CLIP_WGSL__wgpuClipContours:Dynamic = '\nstruct ClipUniforms { matrix : mat3x3f }\n@group(0) @binding(0) var<uniform> u : ClipUniforms;\n@vertex fn vs_main(@location(0) position : vec2f) -> @builtin(position) vec4f {\n  let p = u.matrix * vec3f(position, 1.0);\n  return vec4f(p.x, p.y, 0.0, 1.0);\n}\n@fragment fn fs_main() -> @location(0) vec4f { return vec4f(0.0); }\n';

  public static final COLOR_TRANSFORM_FLOATS__wgpuColorAdjustment:Dynamic = 8.0;

  public static final COLOR_TRANSFORM_WGSL__wgpuColorAdjustment:Dynamic = '\n@group(3) @binding(0) var<storage, read> ctData : array<f32>;\n\nstruct VertexOut {\n  @builtin(position) position : vec4f,\n  @location(0) uv : vec2f,\n  @location(1) alpha : f32,\n  @location(2) ctMult : vec4f,\n  @location(3) ctOff : vec4f,\n}\n\n@vertex\nfn vs_main(@builtin(vertex_index) vi : u32, @builtin(instance_index) ii : u32) -> VertexOut {\n  let bv = quadBaseVertex(vi, ii);\n  let b = ii * 8u;\n  let ctMult = vec4f(ctData[b + 0u], ctData[b + 1u], ctData[b + 2u], ctData[b + 3u]);\n  let ctOff = vec4f(ctData[b + 4u], ctData[b + 5u], ctData[b + 6u], ctData[b + 7u]);\n  return VertexOut(bv.position, bv.uv, bv.alpha, ctMult, ctOff);\n}\n\n@fragment\nfn fs_main(in : VertexOut) -> @location(0) vec4f {\n  var color = textureSample(tex, smp, in.uv);\n  if (color.a <= 0.0) { discard; }\n  color = color * clamp(in.alpha, 0.0, 1.0);\n  if (color.a > 0.0) {\n    color = vec4f(color.rgb / color.a, color.a);\n    color = clamp(color * in.ctMult + in.ctOff, vec4f(0.0), vec4f(1.0));\n    color = vec4f(color.rgb * color.a, color.a);\n  }\n  return color;\n}\n';

  public static final CT_MODE_NONE__wgpuColorAdjustment:Dynamic = 0.0;

  @:keep public static function computeWgpuScissorRect__wgpuClipRectangle(state:WgpuRenderState, rect:RectangleLike, transform:MatrixLike):WgpuScissorRect {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var x0:Dynamic = cast FlightRuntime.UNDEFINED;
    var y0:Dynamic = cast FlightRuntime.UNDEFINED;
    var x1:Dynamic = cast FlightRuntime.UNDEFINED;
    var y1:Dynamic = cast FlightRuntime.UNDEFINED;
    var x2:Dynamic = cast FlightRuntime.UNDEFINED;
    var y2:Dynamic = cast FlightRuntime.UNDEFINED;
    var x3:Dynamic = cast FlightRuntime.UNDEFINED;
    var y3:Dynamic = cast FlightRuntime.UNDEFINED;
    var viewport:Dynamic = cast FlightRuntime.UNDEFINED;
    var minX:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxX:Dynamic = cast FlightRuntime.UNDEFINED;
    var minY:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxY:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    x0 = (((FlightRuntime.field(transform, 'a') * FlightRuntime.field(rect, 'x')) + (FlightRuntime.field(transform, 'c') * FlightRuntime.field(rect, 'y'))) + FlightRuntime.field(transform, 'tx'));
    y0 = (((FlightRuntime.field(transform, 'b') * FlightRuntime.field(rect, 'x')) + (FlightRuntime.field(transform, 'd') * FlightRuntime.field(rect, 'y'))) + FlightRuntime.field(transform, 'ty'));
    x1 = (((FlightRuntime.field(transform, 'a') * (FlightRuntime.field(rect, 'x') + FlightRuntime.field(rect, 'width'))) + (FlightRuntime.field(transform, 'c') * FlightRuntime.field(rect, 'y'))) + FlightRuntime.field(transform, 'tx'));
    y1 = (((FlightRuntime.field(transform, 'b') * (FlightRuntime.field(rect, 'x') + FlightRuntime.field(rect, 'width'))) + (FlightRuntime.field(transform, 'd') * FlightRuntime.field(rect, 'y'))) + FlightRuntime.field(transform, 'ty'));
    x2 = (((FlightRuntime.field(transform, 'a') * FlightRuntime.field(rect, 'x')) + (FlightRuntime.field(transform, 'c') * (FlightRuntime.field(rect, 'y') + FlightRuntime.field(rect, 'height')))) + FlightRuntime.field(transform, 'tx'));
    y2 = (((FlightRuntime.field(transform, 'b') * FlightRuntime.field(rect, 'x')) + (FlightRuntime.field(transform, 'd') * (FlightRuntime.field(rect, 'y') + FlightRuntime.field(rect, 'height')))) + FlightRuntime.field(transform, 'ty'));
    x3 = (((FlightRuntime.field(transform, 'a') * (FlightRuntime.field(rect, 'x') + FlightRuntime.field(rect, 'width'))) + (FlightRuntime.field(transform, 'c') * (FlightRuntime.field(rect, 'y') + FlightRuntime.field(rect, 'height')))) + FlightRuntime.field(transform, 'tx'));
    y3 = (((FlightRuntime.field(transform, 'b') * (FlightRuntime.field(rect, 'x') + FlightRuntime.field(rect, 'width'))) + (FlightRuntime.field(transform, 'd') * (FlightRuntime.field(rect, 'y') + FlightRuntime.field(rect, 'height')))) + FlightRuntime.field(transform, 'ty'));
    viewport = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'renderTargetViewport'), function():Dynamic return cast FlightRuntime.field(state, 'canvas'));
    minX = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'floor', cast ([HxMath.min(HxMath.min(HxMath.min(x0, x1), x2), x3)] : Array<Dynamic>))] : Array<Dynamic>));
    maxX = FlightRuntime.callProperty(HxMath, 'min', cast ([FlightRuntime.field(viewport, 'width'), FlightRuntime.callProperty(HxMath, 'ceil', cast ([HxMath.max(HxMath.max(HxMath.max(x0, x1), x2), x3)] : Array<Dynamic>))] : Array<Dynamic>));
    minY = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'floor', cast ([HxMath.min(HxMath.min(HxMath.min(y0, y1), y2), y3)] : Array<Dynamic>))] : Array<Dynamic>));
    maxY = FlightRuntime.callProperty(HxMath, 'min', cast ([FlightRuntime.field(viewport, 'height'), FlightRuntime.callProperty(HxMath, 'ceil', cast ([HxMath.max(HxMath.max(HxMath.max(y0, y1), y2), y3)] : Array<Dynamic>))] : Array<Dynamic>));
    return cast { height: FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, (maxY - minY)] : Array<Dynamic>)), width: FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, (maxX - minX)] : Array<Dynamic>)), x: minX, y: minY };
    return cast null;
  }

  @:keep public static function createClipContourUniformBuffer__wgpuClipContours(state:WgpuRenderState, t:Matrix):Dynamic {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var viewport:Dynamic = cast FlightRuntime.UNDEFINED;
    var iw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ih:Dynamic = cast FlightRuntime.UNDEFINED;
    var m:Dynamic = cast FlightRuntime.UNDEFINED;
    var buffer:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    viewport = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'renderTargetViewport'), function():Dynamic return cast FlightRuntime.field(state, 'canvas'));
    iw = (2.0 / FlightRuntime.field(viewport, 'width'));
    ih = (2.0 / FlightRuntime.field(viewport, 'height'));
    m = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [12.0]);
    FlightRuntime.setIndex(m, 0.0, (FlightRuntime.field(t, 'a') * iw));
    FlightRuntime.setIndex(m, 1.0, (-FlightRuntime.field(t, 'b') * ih));
    FlightRuntime.setIndex(m, 2.0, 0.0);
    FlightRuntime.setIndex(m, 4.0, (FlightRuntime.field(t, 'c') * iw));
    FlightRuntime.setIndex(m, 5.0, (-FlightRuntime.field(t, 'd') * ih));
    FlightRuntime.setIndex(m, 6.0, 0.0);
    FlightRuntime.setIndex(m, 8.0, ((FlightRuntime.field(t, 'tx') * iw) - 1.0));
    FlightRuntime.setIndex(m, 9.0, ((-FlightRuntime.field(t, 'ty') * ih) + 1.0));
    FlightRuntime.setIndex(m, 10.0, 1.0);
    buffer = FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBuffer', cast ([{ size: DisplayObjectWgpu.CLIP_UNIFORM_BYTES__wgpuClipContours, usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'UNIFORM')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(state, 'device'), 'queue'), 'writeBuffer', cast ([buffer, 0.0, m] : Array<Dynamic>));
    return cast buffer;
    return cast null;
  }

  @:keep public static function createClipContourVertexBuffer__wgpuClipContours(state:WgpuRenderState, contours:Array<Array<Float>>):{ var vertexBuffer:Dynamic; var vertexCount:Float; } {
    var tris:Array<Float> = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var vertexCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var vertexBuffer:Dynamic = cast FlightRuntime.UNDEFINED;
    tris = cast ([] : Array<Dynamic>);
    {
      var c:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(c, FlightRuntime.field(contours, 'length'), '<'))) {
        var contour:Dynamic = FlightRuntime.getIndex(contours, c);
        var pointCount:Dynamic = (Std.int(FlightRuntime.field(contour, 'length')) >> Std.int(1.0));
        if (FlightRuntime.truthy(FlightRuntime.compare(pointCount, 3.0, '<'))) { c++; continue; }
        {
          var i:Dynamic = 1.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(i, (pointCount - 1.0), '<'))) {
            FlightRuntime.pushMany(tris, cast ([FlightRuntime.getIndex(contour, 0.0), FlightRuntime.getIndex(contour, 1.0), FlightRuntime.getIndex(contour, (i * 2.0)), FlightRuntime.getIndex(contour, ((i * 2.0) + 1.0)), FlightRuntime.getIndex(contour, ((i + 1.0) * 2.0)), FlightRuntime.getIndex(contour, (((i + 1.0) * 2.0) + 1.0))] : Array<Dynamic>));
            i++;
          }
        }
        c++;
      }
    }
    data = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [tris]);
    vertexCount = (Std.int(FlightRuntime.field(data, 'length')) >> Std.int(1.0));
    vertexBuffer = FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBuffer', cast ([{ size: FlightRuntime.callProperty(HxMath, 'max', cast ([4.0, FlightRuntime.field(data, 'byteLength')] : Array<Dynamic>)), usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'VERTEX')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(data, 'byteLength'), 0.0, '>'))) { FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(state, 'device'), 'queue'), 'writeBuffer', cast ([vertexBuffer, 0.0, data] : Array<Dynamic>)); }
    return cast { vertexBuffer: vertexBuffer, vertexCount: vertexCount };
    return cast null;
  }

  @:keep public static function createWgpuCacheState(screenState:WgpuRenderState):WgpuRenderState {
    var screenRuntime:Dynamic = cast FlightRuntime.UNDEFINED;
    var cacheState:Dynamic = cast FlightRuntime.UNDEFINED;
    var cacheRuntime:Dynamic = cast FlightRuntime.UNDEFINED;
    screenRuntime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([screenState] : Array<Dynamic>));
    cacheState = (cast FlightRuntime.callValue(createRenderState, cast ([{ allowSmoothing: FlightRuntime.field(screenState, 'allowSmoothing'), pixelRatio: FlightRuntime.field(screenState, 'pixelRatio'), renderTransform2D: FlightRuntime.callValue(createMatrix, cast ([] : Array<Dynamic>)), roundPixels: FlightRuntime.field(screenState, 'roundPixels'), sceneGraphSyncPolicy: FlightRuntime.field(screenState, 'sceneGraphSyncPolicy') }] : Array<Dynamic>)) : WgpuRenderState);
    cacheRuntime = FlightRuntime.callValue(createWgpuRenderStateRuntime, cast ([] : Array<Dynamic>));
    FlightRuntime.setIndex(cacheState, EntityRuntimeKey, cacheRuntime);
    FlightRuntime.callValue(copyAllRenderersFromRenderState, cast ([cacheState, screenState] : Array<Dynamic>));
    FlightRuntime.setField(cacheState, 'applyBlendMode', FlightRuntime.field(screenState, 'applyBlendMode'));
    FlightRuntime.setField((cast cacheState : { var canvas:Dynamic; }), 'canvas', FlightRuntime.field(screenState, 'canvas'));
    FlightRuntime.setField((cast cacheState : { var context:Dynamic; }), 'context', FlightRuntime.field(screenState, 'context'));
    FlightRuntime.setField((cast cacheState : { var device:Dynamic; }), 'device', FlightRuntime.field(screenState, 'device'));
    FlightRuntime.setField((cast cacheState : { var format:Dynamic; }), 'format', FlightRuntime.field(screenState, 'format'));
    FlightRuntime.setField(cacheRuntime, 'uniformBindGroupLayout', FlightRuntime.field(screenRuntime, 'uniformBindGroupLayout'));
    FlightRuntime.setField(cacheRuntime, 'textureBindGroupLayout', FlightRuntime.field(screenRuntime, 'textureBindGroupLayout'));
    FlightRuntime.setField(cacheRuntime, 'uniformBuffer', FlightRuntime.field(screenRuntime, 'uniformBuffer'));
    FlightRuntime.setField(cacheRuntime, 'uniformData', FlightRuntime.field(screenRuntime, 'uniformData'));
    FlightRuntime.setField(cacheRuntime, 'uniformDataU32', FlightRuntime.field(screenRuntime, 'uniformDataU32'));
    FlightRuntime.setField(cacheRuntime, 'uniformStride', FlightRuntime.field(screenRuntime, 'uniformStride'));
    FlightRuntime.setField(cacheRuntime, 'uniformBindGroup', FlightRuntime.field(screenRuntime, 'uniformBindGroup'));
    FlightRuntime.setField(cacheRuntime, 'matrixArray', FlightRuntime.field(screenRuntime, 'matrixArray'));
    FlightRuntime.setField(cacheRuntime, 'pipelineCache', FlightRuntime.field(screenRuntime, 'pipelineCache'));
    FlightRuntime.setField(cacheRuntime, 'linearSampler', FlightRuntime.field(screenRuntime, 'linearSampler'));
    FlightRuntime.setField(cacheRuntime, 'nearestSampler', FlightRuntime.field(screenRuntime, 'nearestSampler'));
    FlightRuntime.setField(cacheRuntime, 'textureCache', FlightRuntime.field(screenRuntime, 'textureCache'));
    FlightRuntime.setField(cacheRuntime, 'defaultBitmapShader', FlightRuntime.field(screenRuntime, 'defaultBitmapShader'));
    FlightRuntime.setField(cacheRuntime, 'particleInstanceBuffer', FlightRuntime.field(screenRuntime, 'particleInstanceBuffer'));
    FlightRuntime.setField(cacheRuntime, 'particleInstanceData', FlightRuntime.field(screenRuntime, 'particleInstanceData'));
    FlightRuntime.setField(cacheRuntime, 'particleInstanceCapacity', FlightRuntime.field(screenRuntime, 'particleInstanceCapacity'));
    FlightRuntime.setField(cacheRuntime, 'commandEncoder', FlightRuntime.field(screenRuntime, 'commandEncoder'));
    FlightRuntime.setField(cacheRuntime, 'renderPass', FlightRuntime.field(screenRuntime, 'renderPass'));
    FlightRuntime.setField(cacheRuntime, 'canvasTextureView', FlightRuntime.field(screenRuntime, 'canvasTextureView'));
    FlightRuntime.setField(cacheRuntime, 'canvasViewCleared', FlightRuntime.field(screenRuntime, 'canvasViewCleared'));
    FlightRuntime.setField(cacheRuntime, 'depthStencilTexture', FlightRuntime.field(screenRuntime, 'depthStencilTexture'));
    FlightRuntime.setField(cacheRuntime, 'depthStencilView', FlightRuntime.field(screenRuntime, 'depthStencilView'));
    FlightRuntime.setField(cacheRuntime, 'depthStencilWidth', FlightRuntime.field(screenRuntime, 'depthStencilWidth'));
    FlightRuntime.setField(cacheRuntime, 'depthStencilHeight', FlightRuntime.field(screenRuntime, 'depthStencilHeight'));
    FlightRuntime.setField(cacheRuntime, 'materialRendererMap', FlightRuntime.field(screenRuntime, 'materialRendererMap'));
    FlightRuntime.setField(cacheRuntime, 'uniformOffset', 0.0);
    FlightRuntime.setField(cacheRuntime, 'currentBlendMode', null);
    FlightRuntime.setField(cacheRuntime, 'currentMaskDepth', 0.0);
    FlightRuntime.setField(cacheRuntime, 'clipContourPipelines', FlightRuntime.UNDEFINED);
    FlightRuntime.setField(cacheRuntime, 'clipContourStack', cast ([] : Array<Dynamic>));
    FlightRuntime.setField(cacheRuntime, 'clipForms', cast ([] : Array<Dynamic>));
    FlightRuntime.setField(cacheRuntime, 'shapeMeshPipelines', FlightRuntime.UNDEFINED);
    FlightRuntime.setField(cacheRuntime, 'spriteBatchBlendMode', null);
    FlightRuntime.setField(cacheRuntime, 'spriteBatchMaterial', null);
    FlightRuntime.setField(cacheRuntime, 'spriteBatchMaterialRenderer', null);
    FlightRuntime.setField(cacheRuntime, 'spriteBatchMaterialFloats', 0.0);
    FlightRuntime.setField(cacheRuntime, 'spriteBatchCount', 0.0);
    FlightRuntime.setField(cacheRuntime, 'spriteBatchInstanceData', FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [0.0]));
    FlightRuntime.setField(cacheRuntime, 'spriteBatchMaterialData', FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [0.0]));
    FlightRuntime.setField(cacheRuntime, 'spriteBatchTexture', null);
    FlightRuntime.setField(cacheRuntime, 'wgpuColorAdjustmentFold', FlightRuntime.field(screenRuntime, 'wgpuColorAdjustmentFold'));
    FlightRuntime.setField(cacheRuntime, 'wgpuColorAdjustmentGuard', FlightRuntime.field(screenRuntime, 'wgpuColorAdjustmentGuard'));
    FlightRuntime.setField(cacheRuntime, 'spriteBatchBufferPool', cast ([] : Array<Dynamic>));
    FlightRuntime.setField(cacheRuntime, 'spriteBatchBufferCursor', 0.0);
    FlightRuntime.setField(cacheRuntime, 'maskWriteMode', false);
    FlightRuntime.setField(cacheRuntime, 'currentScissorRect', null);
    FlightRuntime.setField(cacheRuntime, 'scissorStack', cast ([] : Array<Dynamic>));
    FlightRuntime.setField(cacheRuntime, 'renderTargetViewport', null);
    FlightRuntime.setField(cacheRuntime, 'renderTargetStack', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(DisplayObjectWgpu._cacheStateScreen__wgpuCache, 'set', cast ([cacheState, screenState] : Array<Dynamic>));
    return cast cacheState;
    return cast null;
  }

  @:keep public static function createWgpuRendererData<T>(data:Dynamic):RendererData {
    return cast (cast (cast data : Dynamic) : RendererData);
    return cast null;
  }

  @:keep public static function createWgpuRichTextData(_state:RenderState, _source:Renderable):RendererData {
    return cast FlightRuntime.callValue(createWgpuRendererData, cast ([{ entry: null, h: 0.0, w: 0.0 }] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function createWgpuScale9ShapeData(_state:RenderState, _source:Renderable):RendererData {
    var canvas:Dynamic = cast FlightRuntime.UNDEFINED;
    var ctx:Dynamic = cast FlightRuntime.UNDEFINED;
    canvas = FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['document'] : Array<Dynamic>)), 'createElement', cast (['canvas'] : Array<Dynamic>));
    FlightRuntime.setField(canvas, 'width', 1.0);
    FlightRuntime.setField(canvas, 'height', 1.0);
    ctx = FlightRuntime.callProperty(canvas, 'getContext', cast (['2d'] : Array<Dynamic>));
    return cast FlightRuntime.callValue(createWgpuRendererData, cast ([{ canvas: canvas, ctx: ctx, lastH: 0.0, lastScaleX: -1.0, lastScaleY: -1.0, lastContentId: -1.0, lastW: 0.0, entry: null }] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function createWgpuShapeData__wgpuShape(_state:RenderState, _source:Renderable):RendererData {
    var canvas:Dynamic = cast FlightRuntime.UNDEFINED;
    var ctx:Dynamic = cast FlightRuntime.UNDEFINED;
    canvas = FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['document'] : Array<Dynamic>)), 'createElement', cast (['canvas'] : Array<Dynamic>));
    FlightRuntime.setField(canvas, 'width', 1.0);
    FlightRuntime.setField(canvas, 'height', 1.0);
    ctx = FlightRuntime.callProperty(canvas, 'getContext', cast (['2d'] : Array<Dynamic>));
    return cast FlightRuntime.callValue(createWgpuRendererData, cast ([{ canvas: canvas, ctx: ctx, image: FlightRuntime.callValue(createImageResource, cast ([canvas] : Array<Dynamic>)), lastContentId: -1.0, lastW: 0.0, lastH: 0.0, meshVersion: -1.0, meshes: null, meshBuffers: { vertexBuffer: null, vertexCapacity: 0.0, indexBuffer: null, indexCapacity: 0.0, uniformBuffer: null, bindGroup: null } }] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function createWgpuSpriteBatchBuffer__wgpuSpriteBatch(state:WgpuRenderState, size:Float):Dynamic {
    return cast FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBuffer', cast ([{ size: size, usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'STORAGE')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function createWgpuTextLabelData__wgpuTextLabel(_state:RenderState, _source:Renderable):RendererData {
    var canvas:Dynamic = cast FlightRuntime.UNDEFINED;
    var ctx:Dynamic = cast FlightRuntime.UNDEFINED;
    canvas = FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['document'] : Array<Dynamic>)), 'createElement', cast (['canvas'] : Array<Dynamic>));
    FlightRuntime.setField(canvas, 'width', 1.0);
    FlightRuntime.setField(canvas, 'height', 1.0);
    ctx = FlightRuntime.callProperty(canvas, 'getContext', cast (['2d'] : Array<Dynamic>));
    return cast FlightRuntime.callValue(createWgpuRendererData, cast ([{ canvas: canvas, ctx: ctx, image: FlightRuntime.callValue(createImageResource, cast ([canvas] : Array<Dynamic>)), lastContentId: -1.0, lastPixelRatio: 0.0, logW: 0.0, logH: 0.0, lastPW: 0.0, lastPH: 0.0 }] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function createWgpuVelocityTarget(state:WgpuRenderState, width:Float, height:Float):WgpuRenderTarget {
    return cast FlightRuntime.callValue(createWgpuRenderTarget, cast ([state, width, height, 'rgba16float'] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function createWgpuVideoData(_state:RenderState, _source:Renderable):RendererData {
    return cast FlightRuntime.callValue(createWgpuRendererData, cast ([{ entry: null, h: 0.0, w: 0.0 }] : Array<Dynamic>));
    return cast null;
  }

  public static final CT_MODE_PER_INSTANCE__wgpuColorAdjustment:Dynamic = 2.0;

  public static final CT_MODE_UNIFORM__wgpuColorAdjustment:Dynamic = 1.0;

  public static final DEFAULT_MATERIAL_WGSL__wgpuDefaultMaterial:Dynamic = '\nstruct VertexOut {\n  @builtin(position) position : vec4f,\n  @location(0) uv : vec2f,\n  @location(1) alpha : f32,\n}\n\n@vertex\nfn vs_main(@builtin(vertex_index) vi : u32, @builtin(instance_index) ii : u32) -> VertexOut {\n  let bv = quadBaseVertex(vi, ii);\n  return VertexOut(bv.position, bv.uv, bv.alpha);\n}\n\n@fragment\nfn fs_main(in : VertexOut) -> @location(0) vec4f {\n  var color = textureSample(tex, smp, in.uv);\n  if (color.a <= 0.0) { discard; }\n  return color * clamp(in.alpha, 0.0, 1.0);\n}\n';

  public static final defaultWgpuBeginBitmapFill:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasBeginBitmapFill;

  public static final defaultWgpuBeginFill:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasBeginFill;

  public static final defaultWgpuBeginGradientFill:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasBeginGradientFill;

  public static final defaultWgpuBitmapRenderer:DisplayObjectRenderer = { format: BatchFormat.Quad, createData: noopRendererData, submit: drawWgpuBitmap };

  public static final defaultWgpuCubicCurveTo:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasCubicCurveTo;

  public static final defaultWgpuCurveTo:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasCurveTo;

  public static final defaultWgpuDisplayObjectRenderer:DisplayObjectRenderer = { createData: noopRendererData, submit: drawWgpuDisplayObject };

  public static final defaultWgpuDisplayObjectVelocityWriter:WgpuVelocityWriter = function(ctx:Dynamic, node:Dynamic) {
  var spatial:Dynamic = cast FlightRuntime.UNDEFINED;
  var bounds:Dynamic = cast FlightRuntime.UNDEFINED;
  FlightRuntime.callValue(getVelocity, cast ([FlightRuntime.field(ctx, 'field'), node, DisplayObjectWgpu._scratchVelocity__wgpuVelocity] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(FlightRuntime.field(DisplayObjectWgpu._scratchVelocity__wgpuVelocity, 'x'), 0.0), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(DisplayObjectWgpu._scratchVelocity__wgpuVelocity, 'y'), 0.0)))) { return; }
  spatial = (cast (cast node : Dynamic) : Spatial2DNode<Dynamic>);
  FlightRuntime.callValue(ensureNodeWorldBoundsRectangle, cast ([spatial] : Array<Dynamic>));
  bounds = FlightRuntime.callValue(getNodeWorldBoundsRectangle, cast ([spatial] : Array<Dynamic>));
  FlightRuntime.callValue(drawWgpuVelocityQuad, cast ([ctx, FlightRuntime.field(bounds, 'x'), FlightRuntime.field(bounds, 'y'), FlightRuntime.field(bounds, 'width'), FlightRuntime.field(bounds, 'height'), FlightRuntime.field(DisplayObjectWgpu._scratchVelocity__wgpuVelocity, 'x'), FlightRuntime.field(DisplayObjectWgpu._scratchVelocity__wgpuVelocity, 'y')] : Array<Dynamic>));
};

  public static final defaultWgpuDrawCircle:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasDrawCircle;

  public static final defaultWgpuDrawEllipse:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasDrawEllipse;

  public static final defaultWgpuDrawRectangle:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasDrawRectangle;

  public static final defaultWgpuDrawRoundRectangle:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasDrawRoundRectangle;

  public static final defaultWgpuEndFill:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasEndFill;

  public static final defaultWgpuLineStyle:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasLineStyle;

  public static final defaultWgpuLineTo:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasLineTo;

  public static final defaultWgpuMaterialRenderer:WgpuMaterialRenderer = { instanceFloatCount: 0.0, getShaderModule: function(state:WgpuRenderState) {
  var cached:Dynamic = cast FlightRuntime.UNDEFINED;
  var module:Dynamic = cast FlightRuntime.UNDEFINED;
  cached = FlightRuntime.callProperty(DisplayObjectWgpu._modules__wgpuDefaultMaterial, 'get', cast ([FlightRuntime.field(state, 'device')] : Array<Dynamic>));
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(cached, FlightRuntime.UNDEFINED))) { return cast cached; }
  module = FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createShaderModule', cast ([{ code: (FlightRuntime.callValue(getWgpuQuadBatchPreludeWGSL, cast ([] : Array<Dynamic>)) + DisplayObjectWgpu.DEFAULT_MATERIAL_WGSL__wgpuDefaultMaterial) }] : Array<Dynamic>));
  FlightRuntime.callProperty(DisplayObjectWgpu._modules__wgpuDefaultMaterial, 'set', cast ([FlightRuntime.field(state, 'device'), module] : Array<Dynamic>));
  return cast module;
} };

  public static final defaultWgpuMoveTo:Dynamic = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasMoveTo;

  public static final defaultWgpuParticleEmitterRenderer:SpriteRenderer = { createData: noopRendererData, submit: function(state:WgpuRenderState, node:RenderProxy2D) {
  FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
  FlightRuntime.callValue(drawWgpuParticleEmitter, cast ([state, node] : Array<Dynamic>));
} };

  public static final defaultWgpuParticleEmitterVelocityWriter:WgpuVelocityWriter = function(ctx:Dynamic, node:Dynamic) {
  var emitter:Dynamic = cast FlightRuntime.UNDEFINED;
  var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
  var atlas:Dynamic = cast FlightRuntime.UNDEFINED;
  var ids:Dynamic = cast FlightRuntime.UNDEFINED;
  var particleCount:Dynamic = cast FlightRuntime.UNDEFINED;
  var transforms:Dynamic = cast FlightRuntime.UNDEFINED;
  var velocities:Dynamic = cast FlightRuntime.UNDEFINED;
  var worldSpace:Dynamic = cast FlightRuntime.UNDEFINED;
  var regions:Dynamic = cast FlightRuntime.UNDEFINED;
  var numRegions:Dynamic = cast FlightRuntime.UNDEFINED;
  var wa:Dynamic = cast FlightRuntime.UNDEFINED;
  var wb:Dynamic = cast FlightRuntime.UNDEFINED;
  var wc:Dynamic = cast FlightRuntime.UNDEFINED;
  var wd:Dynamic = cast FlightRuntime.UNDEFINED;
  var wtx:Dynamic = cast FlightRuntime.UNDEFINED;
  var wty:Dynamic = cast FlightRuntime.UNDEFINED;
  emitter = (cast (cast node : Dynamic) : ParticleEmitter);
  __destructure0 = FlightRuntime.field(emitter, 'data');
  atlas = FlightRuntime.field(__destructure0, 'atlas');
  ids = FlightRuntime.field(__destructure0, 'ids');
  particleCount = FlightRuntime.field(__destructure0, 'particleCount');
  transforms = FlightRuntime.field(__destructure0, 'transforms');
  velocities = FlightRuntime.field(__destructure0, 'velocities');
  worldSpace = FlightRuntime.field(__destructure0, 'worldSpace');
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(atlas, null), function():Dynamic return cast FlightRuntime.strictEquals(particleCount, 0.0)), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(velocities, 'length'), (particleCount * 2.0), '<')))) { return; }
  regions = FlightRuntime.field(atlas, 'regions');
  numRegions = FlightRuntime.field(regions, 'length');
  wa = 1.0;
  wb = 0.0;
  wc = 0.0;
  wd = 1.0;
  wtx = 0.0;
  wty = 0.0;
  if (FlightRuntime.truthy(!FlightRuntime.truthy(worldSpace))) {
  FlightRuntime.callValue(ensureNodeWorldMatrix, cast ([(cast (cast node : Dynamic) : Transform2DNode<Dynamic>)] : Array<Dynamic>));
  var transform:Dynamic = FlightRuntime.callValue(getNodeWorldMatrix, cast ([(cast (cast node : Dynamic) : Transform2DNode<Dynamic>)] : Array<Dynamic>));
  (wa = cast (FlightRuntime.field(transform, 'a') : Dynamic));
  (wb = cast (FlightRuntime.field(transform, 'b') : Dynamic));
  (wc = cast (FlightRuntime.field(transform, 'c') : Dynamic));
  (wd = cast (FlightRuntime.field(transform, 'd') : Dynamic));
  (wtx = cast (FlightRuntime.field(transform, 'tx') : Dynamic));
  (wty = cast (FlightRuntime.field(transform, 'ty') : Dynamic));
}
  {
    var i:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, particleCount, '<'))) {
      var velocityX:Dynamic = FlightRuntime.getIndex(velocities, (i * 2.0));
      var velocityY:Dynamic = FlightRuntime.getIndex(velocities, ((i * 2.0) + 1.0));
      if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(velocityX, 0.0), function():Dynamic return cast FlightRuntime.strictEquals(velocityY, 0.0)))) { i++; continue; }
      var id:Dynamic = FlightRuntime.getIndex(ids, i);
      if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(id, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(id, numRegions, '>=')))) { i++; continue; }
      var region:Dynamic = FlightRuntime.getIndex(regions, id);
      var rw:Dynamic = FlightRuntime.field(region, 'width');
      var rh:Dynamic = FlightRuntime.field(region, 'height');
      if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(rw, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(rh, 0.0, '<=')))) { i++; continue; }
      var tt:Dynamic = (i * 4.0);
      var px:Dynamic = FlightRuntime.getIndex(transforms, tt);
      var py:Dynamic = FlightRuntime.getIndex(transforms, (tt + 1.0));
      var rotation:Dynamic = FlightRuntime.getIndex(transforms, (tt + 2.0));
      var scale:Dynamic = FlightRuntime.getIndex(transforms, (tt + 3.0));
      var cosScale:Dynamic = (FlightRuntime.callProperty(HxMath, 'cos', cast ([rotation] : Array<Dynamic>)) * scale);
      var sinScale:Dynamic = (FlightRuntime.callProperty(HxMath, 'sin', cast ([rotation] : Array<Dynamic>)) * scale);
      var minX:Dynamic = HxMath.POSITIVE_INFINITY;
      var minY:Dynamic = HxMath.POSITIVE_INFINITY;
      var maxX:Dynamic = -HxMath.POSITIVE_INFINITY;
      var maxY:Dynamic = -HxMath.POSITIVE_INFINITY;
      {
        var c:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(c, 4.0, '<'))) {
          var lx:Dynamic = ((Std.int(c) & Std.int(1.0)) * rw);
          var ly:Dynamic = ((Std.int(c) >> Std.int(1.0)) * rh);
          var rx:Dynamic = (((cosScale * lx) - (sinScale * ly)) + px);
          var ry:Dynamic = (((sinScale * lx) + (cosScale * ly)) + py);
          var wx:Dynamic = FlightRuntime.select(worldSpace, function():Dynamic return cast rx, function():Dynamic return cast (((wa * rx) + (wc * ry)) + wtx));
          var wy:Dynamic = FlightRuntime.select(worldSpace, function():Dynamic return cast ry, function():Dynamic return cast (((wb * rx) + (wd * ry)) + wty));
          if (FlightRuntime.truthy(FlightRuntime.compare(wx, minX, '<'))) { (minX = cast (wx : Dynamic)); }
          if (FlightRuntime.truthy(FlightRuntime.compare(wx, maxX, '>'))) { (maxX = cast (wx : Dynamic)); }
          if (FlightRuntime.truthy(FlightRuntime.compare(wy, minY, '<'))) { (minY = cast (wy : Dynamic)); }
          if (FlightRuntime.truthy(FlightRuntime.compare(wy, maxY, '>'))) { (maxY = cast (wy : Dynamic)); }
          c++;
        }
      }
      FlightRuntime.callValue(drawWgpuVelocityQuad, cast ([ctx, minX, minY, (maxX - minX), (maxY - minY), velocityX, velocityY] : Array<Dynamic>));
      i++;
    }
  }
};

  public static final defaultWgpuQuadBatchRenderer:SpriteRenderer = { format: BatchFormat.Quad, createData: noopRendererData, submit: DisplayObjectWgpu.submitWgpuQuadBatch__wgpuQuadBatch };

  public static final defaultWgpuQuadBatchVelocityWriter:WgpuVelocityWriter = function(ctx:Dynamic, node:Dynamic) {
  var batch:Dynamic = cast FlightRuntime.UNDEFINED;
  var data:Dynamic = cast FlightRuntime.UNDEFINED;
  var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
  var instanceVelocities:Dynamic = cast FlightRuntime.UNDEFINED;
  var __destructure1:Dynamic = cast FlightRuntime.UNDEFINED;
  var atlas:Dynamic = cast FlightRuntime.UNDEFINED;
  var ids:Dynamic = cast FlightRuntime.UNDEFINED;
  var instanceCount:Dynamic = cast FlightRuntime.UNDEFINED;
  var transforms:Dynamic = cast FlightRuntime.UNDEFINED;
  var transformType:Dynamic = cast FlightRuntime.UNDEFINED;
  var spatial:Dynamic = cast FlightRuntime.UNDEFINED;
  var bounds:Dynamic = cast FlightRuntime.UNDEFINED;
  batch = (cast (cast node : Dynamic) : QuadBatch);
  data = FlightRuntime.field(batch, 'data');
  runtime = (cast FlightRuntime.getIndex((cast node : { @:optional var __EntityRuntimeKey:Dynamic; }), EntityRuntimeKey) : QuadBatchRuntime);
  instanceVelocities = FlightRuntime.field(runtime, 'instanceVelocities');
  __destructure1 = data;
  atlas = FlightRuntime.field(__destructure1, 'atlas');
  ids = FlightRuntime.field(__destructure1, 'ids');
  instanceCount = FlightRuntime.field(__destructure1, 'instanceCount');
  transforms = FlightRuntime.field(__destructure1, 'transforms');
  transformType = FlightRuntime.field(__destructure1, 'transformType');
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(!FlightRuntime.strictEquals(instanceVelocities, null), function():Dynamic return cast !FlightRuntime.strictEquals(atlas, null)), function():Dynamic return cast FlightRuntime.compare(instanceCount, 0.0, '>')))) {
  var regions:Dynamic = FlightRuntime.field(atlas, 'regions');
  var numRegions:Dynamic = FlightRuntime.field(regions, 'length');
  FlightRuntime.callValue(ensureNodeWorldMatrix, cast ([(cast (cast node : Dynamic) : Transform2DNode<Dynamic>)] : Array<Dynamic>));
  var transform:Dynamic = FlightRuntime.callValue(getNodeWorldMatrix, cast ([(cast (cast node : Dynamic) : Transform2DNode<Dynamic>)] : Array<Dynamic>));
  var pa:Dynamic = FlightRuntime.field(transform, 'a');
  var pb:Dynamic = FlightRuntime.field(transform, 'b');
  var pc:Dynamic = FlightRuntime.field(transform, 'c');
  var pd:Dynamic = FlightRuntime.field(transform, 'd');
  var ptx:Dynamic = FlightRuntime.field(transform, 'tx');
  var pty:Dynamic = FlightRuntime.field(transform, 'ty');
  var isVector2:Dynamic = FlightRuntime.strictEquals(transformType, 'vector2');
  {
    var i:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, instanceCount, '<'))) {
      var velocityX:Dynamic = FlightRuntime.getIndex(instanceVelocities, (i * 2.0));
      var velocityY:Dynamic = FlightRuntime.getIndex(instanceVelocities, ((i * 2.0) + 1.0));
      if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(velocityX, 0.0), function():Dynamic return cast FlightRuntime.strictEquals(velocityY, 0.0)))) { i++; continue; }
      var id:Dynamic = FlightRuntime.getIndex(ids, i);
      if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(id, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(id, numRegions, '>=')))) { i++; continue; }
      var region:Dynamic = FlightRuntime.getIndex(regions, id);
      var w:Dynamic = FlightRuntime.field(region, 'width');
      var h:Dynamic = FlightRuntime.field(region, 'height');
      if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(w, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(h, 0.0, '<=')))) { i++; continue; }
      var wa:Float = cast FlightRuntime.UNDEFINED;
      var wb:Float = cast FlightRuntime.UNDEFINED;
      var wc:Float = cast FlightRuntime.UNDEFINED;
      var wd:Float = cast FlightRuntime.UNDEFINED;
      var wtx:Float = cast FlightRuntime.UNDEFINED;
      var wty:Float = cast FlightRuntime.UNDEFINED;
      if (FlightRuntime.truthy(isVector2)) {
  var dx:Dynamic = FlightRuntime.getIndex(transforms, (i * 2.0));
  var dy:Dynamic = FlightRuntime.getIndex(transforms, ((i * 2.0) + 1.0));
  (wa = cast (pa : Dynamic));
  (wb = cast (pb : Dynamic));
  (wc = cast (pc : Dynamic));
  (wd = cast (pd : Dynamic));
  (wtx = cast ((((pa * dx) + (pc * dy)) + ptx) : Dynamic));
  (wty = cast ((((pb * dx) + (pd * dy)) + pty) : Dynamic));
} else {
  var o:Dynamic = (i * 6.0);
  var la:Dynamic = FlightRuntime.getIndex(transforms, o);
  var lb:Dynamic = FlightRuntime.getIndex(transforms, (o + 1.0));
  var lc:Dynamic = FlightRuntime.getIndex(transforms, (o + 2.0));
  var ld:Dynamic = FlightRuntime.getIndex(transforms, (o + 3.0));
  var ltx:Dynamic = FlightRuntime.getIndex(transforms, (o + 4.0));
  var lty:Dynamic = FlightRuntime.getIndex(transforms, (o + 5.0));
  (wa = cast (((pa * la) + (pc * lb)) : Dynamic));
  (wb = cast (((pb * la) + (pd * lb)) : Dynamic));
  (wc = cast (((pa * lc) + (pc * ld)) : Dynamic));
  (wd = cast (((pb * lc) + (pd * ld)) : Dynamic));
  (wtx = cast ((((pa * ltx) + (pc * lty)) + ptx) : Dynamic));
  (wty = cast ((((pb * ltx) + (pd * lty)) + pty) : Dynamic));
}
      var x0:Dynamic = wtx;
      var y0:Dynamic = wty;
      var x1:Dynamic = ((wa * w) + wtx);
      var y1:Dynamic = ((wb * w) + wty);
      var x2:Dynamic = ((wc * h) + wtx);
      var y2:Dynamic = ((wd * h) + wty);
      var x3:Dynamic = (((wa * w) + (wc * h)) + wtx);
      var y3:Dynamic = (((wb * w) + (wd * h)) + wty);
      var minX:Dynamic = HxMath.min(HxMath.min(HxMath.min(x0, x1), x2), x3);
      var minY:Dynamic = HxMath.min(HxMath.min(HxMath.min(y0, y1), y2), y3);
      var maxX:Dynamic = HxMath.max(HxMath.max(HxMath.max(x0, x1), x2), x3);
      var maxY:Dynamic = HxMath.max(HxMath.max(HxMath.max(y0, y1), y2), y3);
      FlightRuntime.callValue(drawWgpuVelocityQuad, cast ([ctx, minX, minY, (maxX - minX), (maxY - minY), velocityX, velocityY] : Array<Dynamic>));
      i++;
    }
  }
  return;
}
  FlightRuntime.callValue(getVelocity, cast ([FlightRuntime.field(ctx, 'field'), node, DisplayObjectWgpu._scratchVelocity__wgpuVelocity] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(FlightRuntime.field(DisplayObjectWgpu._scratchVelocity__wgpuVelocity, 'x'), 0.0), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(DisplayObjectWgpu._scratchVelocity__wgpuVelocity, 'y'), 0.0)))) { return; }
  spatial = (cast (cast node : Dynamic) : Spatial2DNode<Dynamic>);
  FlightRuntime.callValue(ensureNodeWorldBoundsRectangle, cast ([spatial] : Array<Dynamic>));
  bounds = FlightRuntime.callValue(getNodeWorldBoundsRectangle, cast ([spatial] : Array<Dynamic>));
  FlightRuntime.callValue(drawWgpuVelocityQuad, cast ([ctx, FlightRuntime.field(bounds, 'x'), FlightRuntime.field(bounds, 'y'), FlightRuntime.field(bounds, 'width'), FlightRuntime.field(bounds, 'height'), FlightRuntime.field(DisplayObjectWgpu._scratchVelocity__wgpuVelocity, 'x'), FlightRuntime.field(DisplayObjectWgpu._scratchVelocity__wgpuVelocity, 'y')] : Array<Dynamic>));
};

  public static final defaultWgpuRenderCacheRenderer:DisplayObjectRenderer = { createData: noopRendererData, submit: DisplayObjectWgpu.drawWgpuRenderCache__wgpuCache };

  public static final defaultWgpuRichTextRenderer:DisplayObjectRenderer = { createData: createWgpuRichTextData, destroyData: destroyWgpuRichTextData, submit: drawWgpuRichText };

  public static final defaultWgpuScale9ShapeRenderer:DisplayObjectRenderer = { createData: createWgpuScale9ShapeData, destroyData: destroyWgpuScale9ShapeData, submit: drawWgpuScale9Shape };

  public static final defaultWgpuShapeCommands:Array<Dynamic> = facadeDisplayObjectWgpuDisplayObjectCanvasDefaultCanvasShapeCommands;

  public static final defaultWgpuShapeRenderer:DisplayObjectRenderer = { format: BatchFormat.Quad, createData: DisplayObjectWgpu.createWgpuShapeData__wgpuShape, destroyData: DisplayObjectWgpu.destroyWgpuShapeData__wgpuShape, submit: drawWgpuShape };

  public static final defaultWgpuSpriteRenderer:SpriteRenderer = { format: BatchFormat.Quad, createData: noopRendererData, submit: DisplayObjectWgpu.submitWgpuSpriteNode__wgpuSpriteRenderer };

  public static final defaultWgpuTextLabelRenderer:DisplayObjectRenderer = { format: BatchFormat.Quad, createData: DisplayObjectWgpu.createWgpuTextLabelData__wgpuTextLabel, destroyData: DisplayObjectWgpu.destroyWgpuTextLabelData__wgpuTextLabel, submit: drawWgpuTextLabel };

  public static final defaultWgpuTilemapRenderer:SpriteRenderer = { format: BatchFormat.Quad, createData: noopRendererData, submit: DisplayObjectWgpu.submitWgpuTilemap__wgpuTilemap };

  public static final defaultWgpuVideoRenderer:DisplayObjectRenderer = { createData: createWgpuVideoData, destroyData: destroyWgpuVideoData, submit: drawWgpuVideo };

  public static final INSTANCE_FLOATS__wgpuParticleEmitter:Dynamic = 14.0;

  @:keep public static function destroyWgpuRichTextData(_state:RenderState, data:RendererData):Void {
    var richData:Dynamic = cast FlightRuntime.UNDEFINED;
    richData = FlightRuntime.callValue(getWgpuRendererData, cast ([data] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(richData, null))) { return; }
    FlightRuntime.callOptionalProperty(FlightRuntime.optionalField(FlightRuntime.field(richData, 'entry'), 'texture'), 'destroy', cast ([] : Array<Dynamic>));
  }

  @:keep public static function destroyWgpuScale9ShapeData(_state:RenderState, data:RendererData):Void {
    var shapeData:Dynamic = cast FlightRuntime.UNDEFINED;
    shapeData = FlightRuntime.callValue(getWgpuRendererData, cast ([data] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(shapeData, null))) { return; }
    FlightRuntime.callOptionalProperty(FlightRuntime.optionalField(FlightRuntime.field(shapeData, 'entry'), 'texture'), 'destroy', cast ([] : Array<Dynamic>));
  }

  @:keep public static function destroyWgpuShapeData__wgpuShape(state:WgpuRenderState, data:RendererData):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var shapeData:Dynamic = cast FlightRuntime.UNDEFINED;
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    shapeData = FlightRuntime.callValue(getWgpuRendererData, cast ([data] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(shapeData, null))) { return; }
    entry = FlightRuntime.callProperty(FlightRuntime.field(runtime, 'imageResourceTextureCache'), 'get', cast ([FlightRuntime.field(shapeData, 'image')] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(entry, FlightRuntime.UNDEFINED))) {
  FlightRuntime.callProperty(FlightRuntime.field(entry, 'texture'), 'destroy', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(FlightRuntime.field(runtime, 'imageResourceTextureCache'), 'delete', cast ([FlightRuntime.field(shapeData, 'image')] : Array<Dynamic>));
}
    b = FlightRuntime.field(shapeData, 'meshBuffers');
    FlightRuntime.callOptionalProperty(FlightRuntime.field(b, 'vertexBuffer'), 'destroy', cast ([] : Array<Dynamic>));
    FlightRuntime.callOptionalProperty(FlightRuntime.field(b, 'indexBuffer'), 'destroy', cast ([] : Array<Dynamic>));
    FlightRuntime.callOptionalProperty(FlightRuntime.field(b, 'uniformBuffer'), 'destroy', cast ([] : Array<Dynamic>));
    FlightRuntime.setField(b, 'vertexBuffer', null);
    FlightRuntime.setField(b, 'indexBuffer', null);
    FlightRuntime.setField(b, 'uniformBuffer', null);
    FlightRuntime.setField(b, 'bindGroup', null);
  }

  @:keep public static function destroyWgpuTextLabelData__wgpuTextLabel(state:WgpuRenderState, data:RendererData):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var textLabelData:Dynamic = cast FlightRuntime.UNDEFINED;
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    textLabelData = FlightRuntime.callValue(getWgpuRendererData, cast ([data] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(textLabelData, null))) { return; }
    entry = FlightRuntime.callProperty(FlightRuntime.field(runtime, 'imageResourceTextureCache'), 'get', cast ([FlightRuntime.field(textLabelData, 'image')] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(entry, FlightRuntime.UNDEFINED))) {
  FlightRuntime.callProperty(FlightRuntime.field(entry, 'texture'), 'destroy', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(FlightRuntime.field(runtime, 'imageResourceTextureCache'), 'delete', cast ([FlightRuntime.field(textLabelData, 'image')] : Array<Dynamic>));
}
  }

  @:keep public static function destroyWgpuVideoData(_state:RenderState, data:RendererData):Void {
    var videoData:Dynamic = cast FlightRuntime.UNDEFINED;
    videoData = FlightRuntime.callValue(getWgpuRendererData, cast ([data] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(videoData, null))) { return; }
    FlightRuntime.callOptionalProperty(FlightRuntime.optionalField(FlightRuntime.field(videoData, 'entry'), 'texture'), 'destroy', cast ([] : Array<Dynamic>));
  }

  @:keep public static function drawRichTextToCanvas__wgpuRichText(context:Dynamic, source:RichText, result:Dynamic, fieldW:Float, fieldH:Float, text:String):Void {
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var firstVisibleLine:Dynamic = cast FlightRuntime.UNDEFINED;
    var scrollYOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    var scrollXOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    data = FlightRuntime.field(source, 'data');
    firstVisibleLine = (FlightRuntime.field(data, 'scrollV') - 1.0);
    scrollYOffset = FlightRuntime.select(FlightRuntime.compare(firstVisibleLine, 0.0, '>'), function():Dynamic return cast FlightRuntime.callValue(getRichTextScrollYOffset, cast ([FlightRuntime.field(result, 'lineHeights'), firstVisibleLine] : Array<Dynamic>)), function():Dynamic return cast 0.0);
    scrollXOffset = FlightRuntime.field(data, 'scrollH');
    FlightRuntime.callProperty(context, 'save', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(context, 'beginPath', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(context, 'rect', cast ([0.0, 0.0, fieldW, fieldH] : Array<Dynamic>));
    FlightRuntime.callProperty(context, 'clip', cast ([] : Array<Dynamic>));
    FlightRuntime.setField(context, 'textBaseline', 'alphabetic');
    FlightRuntime.setField(context, 'textAlign', 'start');
    for (group in FlightRuntime.iterable(FlightRuntime.field(result, 'groups'))) {
      if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(group, 'lineIndex'), firstVisibleLine, '<'))) { continue; }
      FlightRuntime.setField(context, 'font', FlightRuntime.callValue(computeTextFormatFontString, cast ([FlightRuntime.field(group, 'format')] : Array<Dynamic>)));
      FlightRuntime.setField(context, 'fillStyle', FlightRuntime.callValue(computeRgbHexString, cast ([FlightRuntime.coalesce(FlightRuntime.field(FlightRuntime.field(group, 'format'), 'color'), function():Dynamic return cast FlightRuntime.field(data, 'textColor'))] : Array<Dynamic>)));
      var slice:Dynamic = FlightRuntime.substring(text, FlightRuntime.field(group, 'startIndex'), FlightRuntime.field(group, 'endIndex'));
      var x:Dynamic = (FlightRuntime.field(group, 'offsetX') - scrollXOffset);
      var y:Dynamic = ((FlightRuntime.field(group, 'offsetY') + FlightRuntime.field(group, 'ascent')) - scrollYOffset);
      FlightRuntime.callProperty(context, 'fillText', cast ([slice, x, y] : Array<Dynamic>));
      if (FlightRuntime.truthy(FlightRuntime.field(FlightRuntime.field(group, 'format'), 'underline'))) {
  var lineY:Dynamic = (y + FlightRuntime.field(group, 'descent'));
  FlightRuntime.setField(context, 'strokeStyle', FlightRuntime.callValue(computeRgbHexString, cast ([FlightRuntime.coalesce(FlightRuntime.field(FlightRuntime.field(group, 'format'), 'color'), function():Dynamic return cast FlightRuntime.field(data, 'textColor'))] : Array<Dynamic>)));
  FlightRuntime.setField(context, 'lineWidth', FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, (FlightRuntime.coalesce(FlightRuntime.field(FlightRuntime.field(group, 'format'), 'size'), function():Dynamic return cast 12.0) / 16.0)] : Array<Dynamic>)));
  FlightRuntime.callProperty(context, 'beginPath', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(context, 'moveTo', cast ([x, lineY] : Array<Dynamic>));
  FlightRuntime.callProperty(context, 'lineTo', cast ([(x + FlightRuntime.field(group, 'width')), lineY] : Array<Dynamic>));
  FlightRuntime.callProperty(context, 'stroke', cast ([] : Array<Dynamic>));
}
    }
    FlightRuntime.callProperty(context, 'restore', cast ([] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuBitmap(state:WgpuRenderState, renderProxy:RenderProxy2D):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var imageSource:Dynamic = cast FlightRuntime.UNDEFINED;
    var shader:Dynamic = cast FlightRuntime.UNDEFINED;
    var material:Dynamic = cast FlightRuntime.UNDEFINED;
    var materialRenderer:Dynamic = cast FlightRuntime.UNDEFINED;
    var sr:Dynamic = cast FlightRuntime.UNDEFINED;
    var iw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ih:Dynamic = cast FlightRuntime.UNDEFINED;
    var w:Float = cast FlightRuntime.UNDEFINED;
    var h:Float = cast FlightRuntime.UNDEFINED;
    var u0:Float = cast FlightRuntime.UNDEFINED;
    var v0:Float = cast FlightRuntime.UNDEFINED;
    var u1:Float = cast FlightRuntime.UNDEFINED;
    var v1:Float = cast FlightRuntime.UNDEFINED;
    var startCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var base:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    source = (cast FlightRuntime.field(renderProxy, 'source') : Bitmap);
    imageSource = FlightRuntime.field(FlightRuntime.field(source, 'data'), 'image');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(imageSource, null), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.callValue(hasImageResourcePixels, cast ([imageSource] : Array<Dynamic>)))))) { return; }
    shader = FlightRuntime.callValue(resolveWgpuShader, cast ([state, renderProxy] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(shader, null))) {
  FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
  FlightRuntime.callOptionalProperty(state, 'applyBlendMode', cast ([state, FlightRuntime.field(renderProxy, 'blendMode')] : Array<Dynamic>));
  FlightRuntime.callValue(bindWgpuImageResourceTexture, cast ([state, imageSource] : Array<Dynamic>));
  FlightRuntime.callProperty(shader, 'bind', cast ([state, renderProxy] : Array<Dynamic>));
  return;
}
    material = FlightRuntime.field(renderProxy, 'material');
    materialRenderer = FlightRuntime.callValue(resolveWgpuMaterialRenderer, cast ([state, material] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(materialRenderer, null))) { return; }
    FlightRuntime.callValue(ensureWgpuQuadBatchResources, cast ([state] : Array<Dynamic>));
    sr = FlightRuntime.coalesce(FlightRuntime.field(FlightRuntime.field(source, 'data'), 'sourceRectangle'), function():Dynamic return cast null);
    iw = (1.0 / FlightRuntime.orValue(FlightRuntime.field(imageSource, 'width'), function():Dynamic return cast 1.0));
    ih = (1.0 / FlightRuntime.orValue(FlightRuntime.field(imageSource, 'height'), function():Dynamic return cast 1.0));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sr, null))) {
  (w = cast (FlightRuntime.field(imageSource, 'width') : Dynamic));
  (h = cast (FlightRuntime.field(imageSource, 'height') : Dynamic));
  (u0 = cast (0.0 : Dynamic));
  (v0 = cast (0.0 : Dynamic));
  (u1 = cast (1.0 : Dynamic));
  (v1 = cast (1.0 : Dynamic));
} else {
  (w = cast (FlightRuntime.field(sr, 'width') : Dynamic));
  (h = cast (FlightRuntime.field(sr, 'height') : Dynamic));
  (u0 = cast ((FlightRuntime.field(sr, 'x') * iw) : Dynamic));
  (v0 = cast ((FlightRuntime.field(sr, 'y') * ih) : Dynamic));
  (u1 = cast (((FlightRuntime.field(sr, 'x') + FlightRuntime.field(sr, 'width')) * iw) : Dynamic));
  (v1 = cast (((FlightRuntime.field(sr, 'y') + FlightRuntime.field(sr, 'height')) * ih) : Dynamic));
}
    startCount = FlightRuntime.field(runtime, 'spriteBatchCount');
    base = FlightRuntime.callValue(prepareWgpuSpriteBatchWrite, cast ([state, imageSource, FlightRuntime.field(renderProxy, 'blendMode'), material, materialRenderer, 1.0] : Array<Dynamic>));
    d = FlightRuntime.field(runtime, 'spriteBatchInstanceData');
    t = FlightRuntime.field(renderProxy, 'transform2D');
    FlightRuntime.setIndex(d, base, FlightRuntime.field(t, 'a'));
    FlightRuntime.setIndex(d, (base + 1.0), FlightRuntime.field(t, 'b'));
    FlightRuntime.setIndex(d, (base + 2.0), FlightRuntime.field(t, 'c'));
    FlightRuntime.setIndex(d, (base + 3.0), FlightRuntime.field(t, 'd'));
    FlightRuntime.setIndex(d, (base + 4.0), FlightRuntime.field(t, 'tx'));
    FlightRuntime.setIndex(d, (base + 5.0), FlightRuntime.field(t, 'ty'));
    FlightRuntime.setIndex(d, (base + 6.0), w);
    FlightRuntime.setIndex(d, (base + 7.0), h);
    FlightRuntime.setIndex(d, (base + 8.0), u0);
    FlightRuntime.setIndex(d, (base + 9.0), v0);
    FlightRuntime.setIndex(d, (base + 10.0), u1);
    FlightRuntime.setIndex(d, (base + 11.0), v1);
    FlightRuntime.setIndex(d, (base + 12.0), FlightRuntime.field(renderProxy, 'alpha'));
    FlightRuntime.callValue(packWgpuSpriteBatchMaterialInstance, cast ([state, FlightRuntime.field(renderProxy, 'materialData'), startCount] : Array<Dynamic>));
    FlightRuntime.callValue(recordWgpuSpriteBatchColorTransform, cast ([state, FlightRuntime.field(renderProxy, 'colorTransform'), startCount] : Array<Dynamic>));
    FlightRuntime.incrementField(runtime, 'spriteBatchCount', 1, true);
  }

  @:keep public static function drawWgpuDisplayObject(_state:WgpuRenderState, _renderProxy:RenderProxy2D):Void {
  }

  @:keep public static function drawWgpuParticleEmitter(state:WgpuRenderState, renderProxy:RenderProxy2D):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure2:Dynamic = cast FlightRuntime.UNDEFINED;
    var atlas:Dynamic = cast FlightRuntime.UNDEFINED;
    var alphas:Dynamic = cast FlightRuntime.UNDEFINED;
    var colors:Dynamic = cast FlightRuntime.UNDEFINED;
    var ids:Dynamic = cast FlightRuntime.UNDEFINED;
    var particleCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var transforms:Dynamic = cast FlightRuntime.UNDEFINED;
    var resources:Dynamic = cast FlightRuntime.UNDEFINED;
    var textureEntry:Dynamic = cast FlightRuntime.UNDEFINED;
    var regions:Dynamic = cast FlightRuntime.UNDEFINED;
    var numRegions:Dynamic = cast FlightRuntime.UNDEFINED;
    var nodeAlpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var iw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ih:Dynamic = cast FlightRuntime.UNDEFINED;
    var instanceData:Dynamic = cast FlightRuntime.UNDEFINED;
    var drawCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var base:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure3:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    var floatBase:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure4:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformData:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformDataU32:Dynamic = cast FlightRuntime.UNDEFINED;
    var matrixArray:Dynamic = cast FlightRuntime.UNDEFINED;
    var viewport:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var iw2:Float = cast FlightRuntime.UNDEFINED;
    var ih2:Float = cast FlightRuntime.UNDEFINED;
    var instanceBindGroup:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    source = (cast FlightRuntime.field(renderProxy, 'source') : ParticleEmitter);
    __destructure2 = FlightRuntime.field(source, 'data');
    atlas = FlightRuntime.field(__destructure2, 'atlas');
    alphas = FlightRuntime.field(__destructure2, 'alphas');
    colors = FlightRuntime.field(__destructure2, 'colors');
    ids = FlightRuntime.field(__destructure2, 'ids');
    particleCount = FlightRuntime.field(__destructure2, 'particleCount');
    transforms = FlightRuntime.field(__destructure2, 'transforms');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(atlas, null), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(atlas, 'image'), null)), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.callValue(hasImageResourcePixels, cast ([FlightRuntime.field(atlas, 'image')] : Array<Dynamic>)))), function():Dynamic return cast FlightRuntime.strictEquals(particleCount, 0.0)))) { return; }
    resources = FlightRuntime.callValue(DisplayObjectWgpu.ensureParticleResources__wgpuParticleEmitter, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(DisplayObjectWgpu.ensureParticleInstanceBuffer__wgpuParticleEmitter, cast ([state, particleCount] : Array<Dynamic>));
    FlightRuntime.callOptionalProperty(state, 'applyBlendMode', cast ([state, FlightRuntime.field(renderProxy, 'blendMode')] : Array<Dynamic>));
    textureEntry = FlightRuntime.callValue(bindWgpuImageResourceTexture, cast ([state, FlightRuntime.field(atlas, 'image')] : Array<Dynamic>));
    regions = FlightRuntime.field(atlas, 'regions');
    numRegions = FlightRuntime.field(regions, 'length');
    nodeAlpha = FlightRuntime.field(renderProxy, 'alpha');
    iw = (1.0 / FlightRuntime.orValue(FlightRuntime.field(FlightRuntime.field(atlas, 'image'), 'width'), function():Dynamic return cast 1.0));
    ih = (1.0 / FlightRuntime.orValue(FlightRuntime.field(FlightRuntime.field(atlas, 'image'), 'height'), function():Dynamic return cast 1.0));
    instanceData = FlightRuntime.field(runtime, 'particleInstanceData');
    drawCount = 0.0;
    base = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, particleCount, '<'))) {
        var id:Dynamic = FlightRuntime.getIndex(ids, i);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(id, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(id, numRegions, '>=')))) { i++; continue; }
        var region:Dynamic = FlightRuntime.getIndex(regions, id);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(FlightRuntime.field(region, 'width'), 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(region, 'height'), 0.0, '<=')))) { i++; continue; }
        var tt:Dynamic = (i * 4.0);
        var px:Dynamic = FlightRuntime.getIndex(transforms, tt);
        var py:Dynamic = FlightRuntime.getIndex(transforms, (tt + 1.0));
        var rotation:Dynamic = FlightRuntime.getIndex(transforms, (tt + 2.0));
        var scale:Dynamic = FlightRuntime.getIndex(transforms, (tt + 3.0));
        var cosR:Dynamic = (FlightRuntime.callProperty(HxMath, 'cos', cast ([rotation] : Array<Dynamic>)) * scale);
        var sinR:Dynamic = (FlightRuntime.callProperty(HxMath, 'sin', cast ([rotation] : Array<Dynamic>)) * scale);
        var ct:Dynamic = (i * 3.0);
        var hasColors:Dynamic = FlightRuntime.andValue(!FlightRuntime.looseEquals(colors, null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(colors, 'length'), (ct + 2.0), '>'));
        FlightRuntime.setIndex(instanceData, (base + 0.0), px);
        FlightRuntime.setIndex(instanceData, (base + 1.0), py);
        FlightRuntime.setIndex(instanceData, (base + 2.0), cosR);
        FlightRuntime.setIndex(instanceData, (base + 3.0), sinR);
        FlightRuntime.setIndex(instanceData, (base + 4.0), FlightRuntime.select(hasColors, function():Dynamic return cast FlightRuntime.getIndex(colors, ct), function():Dynamic return cast 1.0));
        FlightRuntime.setIndex(instanceData, (base + 5.0), FlightRuntime.select(hasColors, function():Dynamic return cast FlightRuntime.getIndex(colors, (ct + 1.0)), function():Dynamic return cast 1.0));
        FlightRuntime.setIndex(instanceData, (base + 6.0), FlightRuntime.select(hasColors, function():Dynamic return cast FlightRuntime.getIndex(colors, (ct + 2.0)), function():Dynamic return cast 1.0));
        FlightRuntime.setIndex(instanceData, (base + 7.0), (nodeAlpha * FlightRuntime.getIndex(alphas, i)));
        FlightRuntime.setIndex(instanceData, (base + 8.0), (FlightRuntime.field(region, 'x') * iw));
        FlightRuntime.setIndex(instanceData, (base + 9.0), (FlightRuntime.field(region, 'y') * ih));
        FlightRuntime.setIndex(instanceData, (base + 10.0), ((FlightRuntime.field(region, 'x') + FlightRuntime.field(region, 'width')) * iw));
        FlightRuntime.setIndex(instanceData, (base + 11.0), ((FlightRuntime.field(region, 'y') + FlightRuntime.field(region, 'height')) * ih));
        FlightRuntime.setIndex(instanceData, (base + 12.0), FlightRuntime.field(region, 'width'));
        FlightRuntime.setIndex(instanceData, (base + 13.0), FlightRuntime.field(region, 'height'));
        (base = cast ((base + DisplayObjectWgpu.INSTANCE_FLOATS__wgpuParticleEmitter) : Dynamic));
        drawCount++;
        i++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(drawCount, 0.0))) { return; }
    __destructure3 = state;
    device = FlightRuntime.field(__destructure3, 'device');
    FlightRuntime.callProperty(FlightRuntime.field(device, 'queue'), 'writeBuffer', cast ([FlightRuntime.field(runtime, 'particleInstanceBuffer'), 0.0, FlightRuntime.field(instanceData, 'buffer'), 0.0, (drawCount * DisplayObjectWgpu.INSTANCE_STRIDE__wgpuParticleEmitter)] : Array<Dynamic>));
    uniformOffset = FlightRuntime.field(runtime, 'uniformOffset');
    floatBase = (Std.int(uniformOffset) >> Std.int(2.0));
    __destructure4 = runtime;
    uniformData = FlightRuntime.field(__destructure4, 'uniformData');
    uniformDataU32 = FlightRuntime.field(__destructure4, 'uniformDataU32');
    matrixArray = FlightRuntime.field(__destructure4, 'matrixArray');
    viewport = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'renderTargetViewport'), function():Dynamic return cast FlightRuntime.field(state, 'canvas'));
    t = FlightRuntime.field(renderProxy, 'transform2D');
    if (FlightRuntime.truthy(FlightRuntime.field(FlightRuntime.field(source, 'data'), 'worldSpace'))) {
  (iw2 = cast ((2.0 / FlightRuntime.field(viewport, 'width')) : Dynamic));
  (ih2 = cast ((2.0 / FlightRuntime.field(viewport, 'height')) : Dynamic));
  FlightRuntime.setIndex(matrixArray, 0.0, iw2);
  FlightRuntime.setIndex(matrixArray, 1.0, 0.0);
  FlightRuntime.setIndex(matrixArray, 2.0, 0.0);
  FlightRuntime.setIndex(matrixArray, 3.0, 0.0);
  FlightRuntime.setIndex(matrixArray, 4.0, -ih2);
  FlightRuntime.setIndex(matrixArray, 5.0, 0.0);
  FlightRuntime.setIndex(matrixArray, 6.0, -1.0);
  FlightRuntime.setIndex(matrixArray, 7.0, 1.0);
  FlightRuntime.setIndex(matrixArray, 8.0, 1.0);
} else {
  (iw2 = cast ((2.0 / FlightRuntime.field(viewport, 'width')) : Dynamic));
  (ih2 = cast ((2.0 / FlightRuntime.field(viewport, 'height')) : Dynamic));
  FlightRuntime.setIndex(matrixArray, 0.0, (FlightRuntime.field(t, 'a') * iw2));
  FlightRuntime.setIndex(matrixArray, 1.0, (-FlightRuntime.field(t, 'b') * ih2));
  FlightRuntime.setIndex(matrixArray, 2.0, 0.0);
  FlightRuntime.setIndex(matrixArray, 3.0, (FlightRuntime.field(t, 'c') * iw2));
  FlightRuntime.setIndex(matrixArray, 4.0, (-FlightRuntime.field(t, 'd') * ih2));
  FlightRuntime.setIndex(matrixArray, 5.0, 0.0);
  FlightRuntime.setIndex(matrixArray, 6.0, ((FlightRuntime.field(t, 'tx') * iw2) - 1.0));
  FlightRuntime.setIndex(matrixArray, 7.0, ((-FlightRuntime.field(t, 'ty') * ih2) + 1.0));
  FlightRuntime.setIndex(matrixArray, 8.0, 1.0);
}
    FlightRuntime.setIndex(uniformData, (floatBase + 0.0), FlightRuntime.getIndex(matrixArray, 0.0));
    FlightRuntime.setIndex(uniformData, (floatBase + 1.0), FlightRuntime.getIndex(matrixArray, 1.0));
    FlightRuntime.setIndex(uniformData, (floatBase + 2.0), FlightRuntime.getIndex(matrixArray, 2.0));
    FlightRuntime.setIndex(uniformData, (floatBase + 3.0), 0.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 4.0), FlightRuntime.getIndex(matrixArray, 3.0));
    FlightRuntime.setIndex(uniformData, (floatBase + 5.0), FlightRuntime.getIndex(matrixArray, 4.0));
    FlightRuntime.setIndex(uniformData, (floatBase + 6.0), FlightRuntime.getIndex(matrixArray, 5.0));
    FlightRuntime.setIndex(uniformData, (floatBase + 7.0), 0.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 8.0), FlightRuntime.getIndex(matrixArray, 6.0));
    FlightRuntime.setIndex(uniformData, (floatBase + 9.0), FlightRuntime.getIndex(matrixArray, 7.0));
    FlightRuntime.setIndex(uniformData, (floatBase + 10.0), FlightRuntime.getIndex(matrixArray, 8.0));
    FlightRuntime.setIndex(uniformData, (floatBase + 11.0), 0.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 12.0), 1.0);
    FlightRuntime.setIndex(uniformDataU32, (floatBase + 13.0), 0.0);
    {
      var k:Dynamic = 14.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, 32.0, '<'))) {
        FlightRuntime.setIndex(uniformData, (floatBase + k), 0.0);
        k++;
      }
    }
    FlightRuntime.setField(runtime, 'uniformOffset', (FlightRuntime.field(runtime, 'uniformOffset') + FlightRuntime.field(runtime, 'uniformStride')));
    instanceBindGroup = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(resources, 'instanceBindGroupLayout'), entries: cast ([{ binding: 0.0, resource: { buffer: FlightRuntime.field(runtime, 'particleInstanceBuffer') } }] : Array<Dynamic>) }] : Array<Dynamic>));
    pass = FlightRuntime.field(runtime, 'renderPass');
    FlightRuntime.callProperty(pass, 'setPipeline', cast ([FlightRuntime.callValue(DisplayObjectWgpu.getParticlePipeline__wgpuParticleEmitter, cast ([state, resources] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([0.0, FlightRuntime.field(runtime, 'uniformBindGroup'), cast ([uniformOffset] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([1.0, FlightRuntime.field(textureEntry, 'bindGroup')] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([2.0, instanceBindGroup] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'draw', cast ([6.0, drawCount, 0.0, 0.0] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuRenderCache__wgpuCache(state:WgpuRenderState, renderProxy:RenderProxy2D):Void {
    var cache:Dynamic = cast FlightRuntime.UNDEFINED;
    var target:Dynamic = cast FlightRuntime.UNDEFINED;
    cache = FlightRuntime.callValue(getRenderProxyCache, cast ([state, FlightRuntime.field(renderProxy, 'source')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(cache, null))) { return; }
    target = FlightRuntime.callProperty(FlightRuntime.callValue(DisplayObjectWgpu.getTargets__wgpuCache, cast ([state] : Array<Dynamic>)), 'get', cast ([cache] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(target, FlightRuntime.UNDEFINED))) { return; }
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuRenderTargetResult, cast ([state, renderProxy, target, DisplayObjectWgpu._identity__wgpuCache] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuRichText(state:WgpuRenderState, renderProxy:RenderProxy2D):Void {
    var overlay:Dynamic = cast FlightRuntime.UNDEFINED;
    overlay = FlightRuntime.select(FlightRuntime.andValue(!FlightRuntime.strictEquals(DisplayObjectWgpu._webgpuTextInputOverlay__wgpuRichText, null), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.callValue(getRichTextRuntime, cast ([(cast FlightRuntime.field(renderProxy, 'source') : RichText)] : Array<Dynamic>)), 'input'), null)), function():Dynamic return cast DisplayObjectWgpu._webgpuTextInputOverlay__wgpuRichText, function():Dynamic return cast FlightRuntime.UNDEFINED);
    FlightRuntime.callValue(drawWgpuRichTextWithOverlay, cast ([state, renderProxy, overlay] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuRichTextWithOverlay(state:WgpuRenderState, renderProxy:RenderProxy2D, ?overlay:WgpuRichTextOverlay):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var richTextRuntime:Dynamic = cast FlightRuntime.UNDEFINED;
    var content:Dynamic = cast FlightRuntime.UNDEFINED;
    var result:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxTexDim:Dynamic = cast FlightRuntime.UNDEFINED;
    var pixelRatio:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxLogical:Dynamic = cast FlightRuntime.UNDEFINED;
    var fieldW:Dynamic = cast FlightRuntime.UNDEFINED;
    var fieldH:Dynamic = cast FlightRuntime.UNDEFINED;
    var offCtx:Dynamic = cast FlightRuntime.UNDEFINED;
    var richData:Dynamic = cast FlightRuntime.UNDEFINED;
    var pw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ph:Dynamic = cast FlightRuntime.UNDEFINED;
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    var offsetX:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    source = (cast FlightRuntime.field(renderProxy, 'source') : RichText);
    data = FlightRuntime.field(source, 'data');
    richTextRuntime = (cast FlightRuntime.callValue(getRichTextRuntime, cast ([source] : Array<Dynamic>)) : RichTextRuntime);
    content = FlightRuntime.callValue(getRichTextContent, cast ([richTextRuntime] : Array<Dynamic>));
    FlightRuntime.callValue(computeRichTextContent, cast ([content, data, FlightRuntime.callValue(getRichTextPasswordCharacter, cast ([source] : Array<Dynamic>))] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(content, 'text'), 'length'), 0.0), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.field(data, 'background'))), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.field(data, 'border'))))) { return; }
    result = FlightRuntime.callValue(DisplayObjectWgpu.layoutRichText__wgpuRichText, cast ([source, richTextRuntime, FlightRuntime.field(content, 'text'), FlightRuntime.field(content, 'formatRanges'), state] : Array<Dynamic>));
    maxTexDim = FlightRuntime.field(FlightRuntime.field(FlightRuntime.field(state, 'device'), 'limits'), 'maxTextureDimension2D');
    pixelRatio = FlightRuntime.field(state, 'pixelRatio');
    maxLogical = FlightRuntime.callProperty(HxMath, 'floor', cast ([(maxTexDim / pixelRatio)] : Array<Dynamic>));
    fieldW = FlightRuntime.callProperty(HxMath, 'min', cast ([FlightRuntime.callProperty(HxMath, 'ceil', cast ([FlightRuntime.callValue(computeTextBoundsWidth, cast ([data, result] : Array<Dynamic>))] : Array<Dynamic>)), maxLogical] : Array<Dynamic>));
    fieldH = FlightRuntime.callProperty(HxMath, 'min', cast ([FlightRuntime.callProperty(HxMath, 'ceil', cast ([FlightRuntime.callValue(computeTextBoundsHeight, cast ([data, result] : Array<Dynamic>))] : Array<Dynamic>)), maxLogical] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(fieldW, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(fieldH, 0.0, '<=')))) { return; }
    offCtx = FlightRuntime.callValue(DisplayObjectWgpu.getOffscreenCanvas__wgpuRichText, cast ([fieldW, fieldH, pixelRatio] : Array<Dynamic>));
    FlightRuntime.callProperty(offCtx, 'setTransform', cast ([pixelRatio, 0.0, 0.0, pixelRatio, 0.0, 0.0] : Array<Dynamic>));
    FlightRuntime.callProperty(offCtx, 'clearRect', cast ([0.0, 0.0, fieldW, fieldH] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.field(data, 'background'))) {
  FlightRuntime.setField(offCtx, 'fillStyle', FlightRuntime.callValue(computeRgbHexString, cast ([FlightRuntime.field(data, 'backgroundColor')] : Array<Dynamic>)));
  FlightRuntime.callProperty(offCtx, 'fillRect', cast ([0.0, 0.0, fieldW, fieldH] : Array<Dynamic>));
}
    if (FlightRuntime.truthy(FlightRuntime.field(data, 'border'))) {
  FlightRuntime.setField(offCtx, 'strokeStyle', FlightRuntime.callValue(computeRgbHexString, cast ([FlightRuntime.field(data, 'borderColor')] : Array<Dynamic>)));
  FlightRuntime.setField(offCtx, 'lineWidth', 1.0);
  FlightRuntime.callProperty(offCtx, 'strokeRect', cast ([0.0, 0.0, fieldW, fieldH] : Array<Dynamic>));
}
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(content, 'text'), 'length'), 0.0, '>'))) {
  FlightRuntime.callValue(DisplayObjectWgpu.drawRichTextToCanvas__wgpuRichText, cast ([offCtx, source, result, fieldW, fieldH, FlightRuntime.field(content, 'text')] : Array<Dynamic>));
}
    FlightRuntime.callOptionalValue(overlay, cast ([offCtx, source, result, fieldW, fieldH, FlightRuntime.field(content, 'text')] : Array<Dynamic>));
    FlightRuntime.callOptionalProperty(state, 'applyBlendMode', cast ([state, FlightRuntime.field(renderProxy, 'blendMode')] : Array<Dynamic>));
    richData = FlightRuntime.callValue(getWgpuRendererData, cast ([FlightRuntime.field(renderProxy, 'rendererData')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(richData, null))) { return; }
    pw = FlightRuntime.field(DisplayObjectWgpu._offscreenCanvas__wgpuRichText, 'width');
    ph = FlightRuntime.field(DisplayObjectWgpu._offscreenCanvas__wgpuRichText, 'height');
    entry = FlightRuntime.field(richData, 'entry');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(entry, null), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(richData, 'w'), pw)), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(richData, 'h'), ph)))) {
  FlightRuntime.callOptionalProperty(FlightRuntime.optionalField(entry, 'texture'), 'destroy', cast ([] : Array<Dynamic>));
  (entry = cast (FlightRuntime.callValue(createWgpuTextureEntry, cast ([state, pw, ph, DisplayObjectWgpu._offscreenCanvas__wgpuRichText] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.setField(richData, 'entry', entry);
  FlightRuntime.setField(richData, 'w', pw);
  FlightRuntime.setField(richData, 'h', ph);
} else {
  FlightRuntime.callValue(updateWgpuTextureEntry, cast ([state, entry, DisplayObjectWgpu._offscreenCanvas__wgpuRichText] : Array<Dynamic>));
}
    offsetX = FlightRuntime.callValue(computeTextBoundsOffsetX, cast ([data, result] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuQuad, cast ([state, renderProxy, entry, offsetX, 0.0, (offsetX + fieldW), fieldH, 0.0, 0.0, 1.0, 1.0] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuScale9Shape(state:WgpuRenderState, renderProxy:RenderProxy2D):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var scale9Grid:Dynamic = cast FlightRuntime.UNDEFINED;
    var version:Dynamic = cast FlightRuntime.UNDEFINED;
    var bounds:Dynamic = cast FlightRuntime.UNDEFINED;
    var mapper:Dynamic = cast FlightRuntime.UNDEFINED;
    var shapeData:Dynamic = cast FlightRuntime.UNDEFINED;
    var w:Dynamic = cast FlightRuntime.UNDEFINED;
    var h:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var c:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    source = (cast FlightRuntime.field(renderProxy, 'source') : Scale9Shape);
    __destructure0 = FlightRuntime.field(source, 'data');
    commands = FlightRuntime.field(__destructure0, 'commands');
    scale9Grid = FlightRuntime.field(__destructure0, 'scale9Grid');
    version = FlightRuntime.callValue(getNodeLocalContentRevision, cast ([source] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(commands, 'length'), 0.0))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(renderProxy, 'rendererData'), null))) { return; }
    bounds = FlightRuntime.callValue(getNodeLocalBoundsRectangle, cast ([source] : Array<Dynamic>));
    mapper = FlightRuntime.callValue(buildWgpuScale9Mapper, cast ([bounds, scale9Grid, FlightRuntime.field(source, 'scaleX'), FlightRuntime.field(source, 'scaleY')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(mapper, null))) {
  FlightRuntime.callValue(drawWgpuShape, cast ([state, renderProxy] : Array<Dynamic>));
  return;
}
    shapeData = FlightRuntime.callValue(getWgpuRendererData, cast ([FlightRuntime.field(renderProxy, 'rendererData')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(shapeData, null))) { return; }
    w = FlightRuntime.callProperty(HxMath, 'ceil', cast ([(FlightRuntime.field(bounds, 'width') * FlightRuntime.field(source, 'scaleX'))] : Array<Dynamic>));
    h = FlightRuntime.callProperty(HxMath, 'ceil', cast ([(FlightRuntime.field(bounds, 'height') * FlightRuntime.field(source, 'scaleY'))] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(w, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(h, 0.0, '<=')))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(!FlightRuntime.strictEquals(version, FlightRuntime.field(shapeData, 'lastContentId')), function():Dynamic return cast !FlightRuntime.strictEquals(w, FlightRuntime.field(shapeData, 'lastW'))), function():Dynamic return cast !FlightRuntime.strictEquals(h, FlightRuntime.field(shapeData, 'lastH'))), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(source, 'scaleX'), FlightRuntime.field(shapeData, 'lastScaleX'))), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(source, 'scaleY'), FlightRuntime.field(shapeData, 'lastScaleY'))))) {
  FlightRuntime.setField(FlightRuntime.field(shapeData, 'canvas'), 'width', w);
  FlightRuntime.setField(FlightRuntime.field(shapeData, 'canvas'), 'height', h);
  var ctx:Dynamic = FlightRuntime.field(shapeData, 'ctx');
  FlightRuntime.callProperty(ctx, 'clearRect', cast ([0.0, 0.0, w, h] : Array<Dynamic>));
  FlightRuntime.callProperty(ctx, 'save', cast ([] : Array<Dynamic>));
  FlightRuntime.callValue(remapWgpuScale9Commands, cast ([DisplayObjectWgpu._remappedCommands__wgpuScale9Shape, commands, mapper] : Array<Dynamic>));
  FlightRuntime.callProperty(ctx, 'translate', cast ([-FlightRuntime.field(bounds, 'x'), -FlightRuntime.field(bounds, 'y')] : Array<Dynamic>));
  FlightRuntime.callValue(renderCanvasShapeCommands, cast ([ctx, DisplayObjectWgpu._remappedCommands__wgpuScale9Shape] : Array<Dynamic>));
  FlightRuntime.callProperty(ctx, 'restore', cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.field(shapeData, 'entry'), null), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(shapeData, 'lastW'), w)), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(shapeData, 'lastH'), h)))) {
  FlightRuntime.callOptionalProperty(FlightRuntime.optionalField(FlightRuntime.field(shapeData, 'entry'), 'texture'), 'destroy', cast ([] : Array<Dynamic>));
  FlightRuntime.setField(shapeData, 'entry', FlightRuntime.callValue(createWgpuTextureEntry, cast ([state, w, h, FlightRuntime.field(shapeData, 'canvas')] : Array<Dynamic>)));
} else {
  FlightRuntime.callValue(updateWgpuTextureEntry, cast ([state, FlightRuntime.field(shapeData, 'entry'), FlightRuntime.field(shapeData, 'canvas')] : Array<Dynamic>));
}
  FlightRuntime.setField(shapeData, 'lastH', h);
  FlightRuntime.setField(shapeData, 'lastScaleX', FlightRuntime.field(source, 'scaleX'));
  FlightRuntime.setField(shapeData, 'lastScaleY', FlightRuntime.field(source, 'scaleY'));
  FlightRuntime.setField(shapeData, 'lastContentId', version);
  FlightRuntime.setField(shapeData, 'lastW', w);
}
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(shapeData, 'entry'), null))) { return; }
    t = FlightRuntime.field(renderProxy, 'transform2D');
    a = FlightRuntime.select(!FlightRuntime.strictEquals(FlightRuntime.field(source, 'scaleX'), 0.0), function():Dynamic return cast (FlightRuntime.field(t, 'a') / FlightRuntime.field(source, 'scaleX')), function():Dynamic return cast FlightRuntime.field(t, 'a'));
    b = FlightRuntime.select(!FlightRuntime.strictEquals(FlightRuntime.field(source, 'scaleX'), 0.0), function():Dynamic return cast (FlightRuntime.field(t, 'b') / FlightRuntime.field(source, 'scaleX')), function():Dynamic return cast FlightRuntime.field(t, 'b'));
    c = FlightRuntime.select(!FlightRuntime.strictEquals(FlightRuntime.field(source, 'scaleY'), 0.0), function():Dynamic return cast (FlightRuntime.field(t, 'c') / FlightRuntime.field(source, 'scaleY')), function():Dynamic return cast FlightRuntime.field(t, 'c'));
    d = FlightRuntime.select(!FlightRuntime.strictEquals(FlightRuntime.field(source, 'scaleY'), 0.0), function():Dynamic return cast (FlightRuntime.field(t, 'd') / FlightRuntime.field(source, 'scaleY')), function():Dynamic return cast FlightRuntime.field(t, 'd'));
    FlightRuntime.callValue(drawWgpuQuadWithTransform, cast ([state, renderProxy, { a: a, b: b, c: c, d: d, tx: FlightRuntime.field(t, 'tx'), ty: FlightRuntime.field(t, 'ty') }, FlightRuntime.field(shapeData, 'entry'), 0.0, 0.0, w, h, 0.0, 0.0, 1.0, 1.0] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuScale9ShapeMask(state:WgpuRenderState, data:RenderProxy2D):Void {
    FlightRuntime.callValue(drawWgpuScale9Shape, cast ([state, data] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuShape(state:WgpuRenderState, renderProxy:RenderProxy2D):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var version:Dynamic = cast FlightRuntime.UNDEFINED;
    var regions:Dynamic = cast FlightRuntime.UNDEFINED;
    var material:Dynamic = cast FlightRuntime.UNDEFINED;
    var materialRenderer:Dynamic = cast FlightRuntime.UNDEFINED;
    var shapeData:Dynamic = cast FlightRuntime.UNDEFINED;
    var bounds:Dynamic = cast FlightRuntime.UNDEFINED;
    var w:Dynamic = cast FlightRuntime.UNDEFINED;
    var h:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var tx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ty:Dynamic = cast FlightRuntime.UNDEFINED;
    var startCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var base:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    source = (cast FlightRuntime.field(renderProxy, 'source') : Shape);
    __destructure0 = FlightRuntime.field(source, 'data');
    commands = FlightRuntime.field(__destructure0, 'commands');
    version = FlightRuntime.callValue(getNodeLocalContentRevision, cast ([source] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(commands, 'length'), 0.0))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(renderProxy, 'rendererData'), null))) { return; }
    regions = FlightRuntime.callValue(getShapeFillRegions, cast ([commands] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(regions, null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(regions, 'length'), 0.0, '>')))) {
  var meshData:Dynamic = FlightRuntime.callValue(getWgpuRendererData, cast ([FlightRuntime.field(renderProxy, 'rendererData')] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(meshData, null))) { return; }
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(meshData, 'meshVersion'), version))) {
  FlightRuntime.setField(meshData, 'meshes', FlightRuntime.callProperty(regions, 'map', cast ([function(region:Dynamic) {
  var mesh:Dynamic = cast FlightRuntime.UNDEFINED;
  mesh = FlightRuntime.callValue(tessellatePath, cast ([FlightRuntime.field(region, 'path')] : Array<Dynamic>));
  return cast { vertices: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [FlightRuntime.field(mesh, 'vertices')]), indices: new flight.internal.FlightUInt16Array(FlightRuntime.field(mesh, 'indices')), color: FlightRuntime.field(region, 'color'), alpha: FlightRuntime.field(region, 'alpha') };
}] : Array<Dynamic>)));
  FlightRuntime.setField(meshData, 'meshVersion', version);
}
  FlightRuntime.callValue(drawWgpuShapeMeshes, cast ([state, renderProxy, FlightRuntime.coalesce(FlightRuntime.field(meshData, 'meshes'), function():Dynamic return cast cast ([] : Array<Dynamic>)), FlightRuntime.field(meshData, 'meshBuffers')] : Array<Dynamic>));
  return;
}
    material = FlightRuntime.field(renderProxy, 'material');
    materialRenderer = FlightRuntime.callValue(resolveWgpuMaterialRenderer, cast ([state, material] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(materialRenderer, null))) { return; }
    shapeData = FlightRuntime.callValue(getWgpuRendererData, cast ([FlightRuntime.field(renderProxy, 'rendererData')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(shapeData, null))) { return; }
    bounds = FlightRuntime.callValue(getNodeLocalBoundsRectangle, cast ([source] : Array<Dynamic>));
    w = FlightRuntime.callProperty(HxMath, 'ceil', cast ([FlightRuntime.field(bounds, 'width')] : Array<Dynamic>));
    h = FlightRuntime.callProperty(HxMath, 'ceil', cast ([FlightRuntime.field(bounds, 'height')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(w, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(h, 0.0, '<=')))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(!FlightRuntime.strictEquals(version, FlightRuntime.field(shapeData, 'lastContentId')), function():Dynamic return cast !FlightRuntime.strictEquals(w, FlightRuntime.field(shapeData, 'lastW'))), function():Dynamic return cast !FlightRuntime.strictEquals(h, FlightRuntime.field(shapeData, 'lastH'))))) {
  FlightRuntime.setField(FlightRuntime.field(shapeData, 'canvas'), 'width', w);
  FlightRuntime.setField(FlightRuntime.field(shapeData, 'canvas'), 'height', h);
  var ctx:Dynamic = FlightRuntime.field(shapeData, 'ctx');
  FlightRuntime.callProperty(ctx, 'clearRect', cast ([0.0, 0.0, w, h] : Array<Dynamic>));
  FlightRuntime.callProperty(ctx, 'save', cast ([] : Array<Dynamic>));
  FlightRuntime.callProperty(ctx, 'translate', cast ([-FlightRuntime.field(bounds, 'x'), -FlightRuntime.field(bounds, 'y')] : Array<Dynamic>));
  FlightRuntime.callValue(renderCanvasShapeCommands, cast ([ctx, commands] : Array<Dynamic>));
  FlightRuntime.callProperty(ctx, 'restore', cast ([] : Array<Dynamic>));
  FlightRuntime.callValue(setImageResourceSource, cast ([FlightRuntime.field(shapeData, 'image'), FlightRuntime.field(shapeData, 'canvas')] : Array<Dynamic>));
  FlightRuntime.setField(shapeData, 'lastContentId', version);
  FlightRuntime.setField(shapeData, 'lastW', w);
  FlightRuntime.setField(shapeData, 'lastH', h);
}
    FlightRuntime.callValue(ensureWgpuQuadBatchResources, cast ([state] : Array<Dynamic>));
    t = FlightRuntime.field(renderProxy, 'transform2D');
    tx = ((FlightRuntime.field(t, 'tx') + (FlightRuntime.field(t, 'a') * FlightRuntime.field(bounds, 'x'))) + (FlightRuntime.field(t, 'c') * FlightRuntime.field(bounds, 'y')));
    ty = ((FlightRuntime.field(t, 'ty') + (FlightRuntime.field(t, 'b') * FlightRuntime.field(bounds, 'x'))) + (FlightRuntime.field(t, 'd') * FlightRuntime.field(bounds, 'y')));
    startCount = FlightRuntime.field(runtime, 'spriteBatchCount');
    base = FlightRuntime.callValue(prepareWgpuSpriteBatchWrite, cast ([state, FlightRuntime.field(shapeData, 'image'), FlightRuntime.field(renderProxy, 'blendMode'), material, materialRenderer, 1.0] : Array<Dynamic>));
    d = FlightRuntime.field(runtime, 'spriteBatchInstanceData');
    FlightRuntime.setIndex(d, base, FlightRuntime.field(t, 'a'));
    FlightRuntime.setIndex(d, (base + 1.0), FlightRuntime.field(t, 'b'));
    FlightRuntime.setIndex(d, (base + 2.0), FlightRuntime.field(t, 'c'));
    FlightRuntime.setIndex(d, (base + 3.0), FlightRuntime.field(t, 'd'));
    FlightRuntime.setIndex(d, (base + 4.0), tx);
    FlightRuntime.setIndex(d, (base + 5.0), ty);
    FlightRuntime.setIndex(d, (base + 6.0), w);
    FlightRuntime.setIndex(d, (base + 7.0), h);
    FlightRuntime.setIndex(d, (base + 8.0), 0.0);
    FlightRuntime.setIndex(d, (base + 9.0), 0.0);
    FlightRuntime.setIndex(d, (base + 10.0), 1.0);
    FlightRuntime.setIndex(d, (base + 11.0), 1.0);
    FlightRuntime.setIndex(d, (base + 12.0), FlightRuntime.field(renderProxy, 'alpha'));
    FlightRuntime.callValue(packWgpuSpriteBatchMaterialInstance, cast ([state, FlightRuntime.field(renderProxy, 'materialData'), startCount] : Array<Dynamic>));
    FlightRuntime.callValue(recordWgpuSpriteBatchColorTransform, cast ([state, FlightRuntime.field(renderProxy, 'colorTransform'), startCount] : Array<Dynamic>));
    FlightRuntime.incrementField(runtime, 'spriteBatchCount', 1, true);
  }

  @:keep public static function drawWgpuShapeMeshes(state:WgpuRenderState, renderProxy:RenderProxy2D, meshes:Array<WgpuShapeMesh>, buffers:WgpuShapeMeshBuffers):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipelineEntry:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var queue:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniform:Dynamic = cast FlightRuntime.UNDEFINED;
    var nodeAlpha:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(meshes, 'length'), 0.0))) { return; }
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    pass = FlightRuntime.field(runtime, 'renderPass');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(pass, null))) { return; }
    pipelineEntry = FlightRuntime.callValue(DisplayObjectWgpu.ensureShapeMeshPipeline__wgpuShapeMesh, cast ([state] : Array<Dynamic>));
    device = FlightRuntime.field(state, 'device');
    queue = FlightRuntime.field(device, 'queue');
    uniform = FlightRuntime.callValue(DisplayObjectWgpu.ensureShapeMeshUniform__wgpuShapeMesh, cast ([state, pipelineEntry, buffers] : Array<Dynamic>));
    FlightRuntime.callValue(DisplayObjectWgpu.shapeMeshMatrix__wgpuShapeMesh, cast ([state, renderProxy] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setPipeline', cast ([FlightRuntime.field(pipelineEntry, 'pipeline')] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([0.0, FlightRuntime.field(buffers, 'bindGroup')] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setStencilReference', cast ([FlightRuntime.field(runtime, 'currentMaskDepth')] : Array<Dynamic>));
    nodeAlpha = FlightRuntime.field(renderProxy, 'alpha');
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(meshes, 'length'), '<'))) {
        var mesh:Dynamic = FlightRuntime.getIndex(meshes, i);
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(mesh, 'indices'), 'length'), 0.0))) { i++; continue; }
        var a:Dynamic = (FlightRuntime.field(mesh, 'alpha') * nodeAlpha);
        if (FlightRuntime.truthy(FlightRuntime.compare(a, 0.0, '<='))) { i++; continue; }
        var vertexBuffer:Dynamic = FlightRuntime.callValue(DisplayObjectWgpu.ensureShapeMeshVertexBuffer__wgpuShapeMesh, cast ([state, buffers, FlightRuntime.field(FlightRuntime.field(mesh, 'vertices'), 'byteLength')] : Array<Dynamic>));
        var indexBuffer:Dynamic = FlightRuntime.callValue(DisplayObjectWgpu.ensureShapeMeshIndexBuffer__wgpuShapeMesh, cast ([state, buffers, FlightRuntime.field(FlightRuntime.field(mesh, 'indices'), 'byteLength')] : Array<Dynamic>));
        FlightRuntime.callProperty(queue, 'writeBuffer', cast ([vertexBuffer, 0.0, FlightRuntime.field(FlightRuntime.field(mesh, 'vertices'), 'buffer'), FlightRuntime.field(FlightRuntime.field(mesh, 'vertices'), 'byteOffset'), FlightRuntime.field(FlightRuntime.field(mesh, 'vertices'), 'byteLength')] : Array<Dynamic>));
        FlightRuntime.callValue(DisplayObjectWgpu.writeShapeMeshIndices__wgpuShapeMesh, cast ([queue, indexBuffer, FlightRuntime.field(mesh, 'indices')] : Array<Dynamic>));
        var r:Dynamic = ((Std.int((Std.int(FlightRuntime.field(mesh, 'color')) >> Std.int(16.0))) & Std.int(255.0)) / 255.0);
        var g:Dynamic = ((Std.int((Std.int(FlightRuntime.field(mesh, 'color')) >> Std.int(8.0))) & Std.int(255.0)) / 255.0);
        var b:Dynamic = ((Std.int(FlightRuntime.field(mesh, 'color')) & Std.int(255.0)) / 255.0);
        FlightRuntime.setIndex(uniform, 12.0, (r * a));
        FlightRuntime.setIndex(uniform, 13.0, (g * a));
        FlightRuntime.setIndex(uniform, 14.0, (b * a));
        FlightRuntime.setIndex(uniform, 15.0, a);
        FlightRuntime.callProperty(queue, 'writeBuffer', cast ([FlightRuntime.field(buffers, 'uniformBuffer'), 0.0, FlightRuntime.field(uniform, 'buffer'), FlightRuntime.field(uniform, 'byteOffset'), FlightRuntime.field(uniform, 'byteLength')] : Array<Dynamic>));
        FlightRuntime.callProperty(pass, 'setVertexBuffer', cast ([0.0, vertexBuffer] : Array<Dynamic>));
        FlightRuntime.callProperty(pass, 'setIndexBuffer', cast ([indexBuffer, 'uint16'] : Array<Dynamic>));
        FlightRuntime.callProperty(pass, 'drawIndexed', cast ([FlightRuntime.field(FlightRuntime.field(mesh, 'indices'), 'length')] : Array<Dynamic>));
        i++;
      }
    }
  }

  @:keep public static function drawWgpuTextInputOverlay(context:Dynamic, source:RichText, result:TextLayoutResult, fieldW:Float, fieldH:Float, _text:String):Void {
    var input:Dynamic = cast FlightRuntime.UNDEFINED;
    var firstVisibleLine:Dynamic = cast FlightRuntime.UNDEFINED;
    var scrollYOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    var scrollXOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    input = FlightRuntime.callValue(getTextInputState, cast ([source] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(input, null), function():Dynamic return cast FlightRuntime.andValue(!FlightRuntime.truthy(FlightRuntime.field(input, 'focused')), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.field(input, 'alwaysShowSelection')))))) { return; }
    firstVisibleLine = (FlightRuntime.field(FlightRuntime.field(source, 'data'), 'scrollV') - 1.0);
    scrollYOffset = FlightRuntime.select(FlightRuntime.compare(firstVisibleLine, 0.0, '>'), function():Dynamic return cast FlightRuntime.callValue(getRichTextScrollYOffset, cast ([FlightRuntime.field(result, 'lineHeights'), firstVisibleLine] : Array<Dynamic>)), function():Dynamic return cast 0.0);
    scrollXOffset = FlightRuntime.field(FlightRuntime.field(source, 'data'), 'scrollH');
    FlightRuntime.callProperty(context, 'save', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(context, 'beginPath', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(context, 'rect', cast ([0.0, 0.0, fieldW, fieldH] : Array<Dynamic>));
    FlightRuntime.callProperty(context, 'clip', cast ([] : Array<Dynamic>));
    FlightRuntime.callValue(getTextInputSelectionRectangles, cast ([DisplayObjectWgpu.selectionRectangles__wgpuTextInput, source, result] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(DisplayObjectWgpu.selectionRectangles__wgpuTextInput, 'length'), 0.0, '>'))) {
  FlightRuntime.setField(context, 'fillStyle', FlightRuntime.callValue(computeRgbHexString, cast ([FlightRuntime.field(input, 'selectionColor')] : Array<Dynamic>)));
  FlightRuntime.setField(context, 'globalAlpha', FlightRuntime.field(input, 'selectionAlpha'));
  for (rect in FlightRuntime.iterable(DisplayObjectWgpu.selectionRectangles__wgpuTextInput)) {
    FlightRuntime.callProperty(context, 'fillRect', cast ([(FlightRuntime.field(rect, 'x') - scrollXOffset), (FlightRuntime.field(rect, 'y') - scrollYOffset), FlightRuntime.field(rect, 'width'), FlightRuntime.field(rect, 'height')] : Array<Dynamic>));
  }
}
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.field(input, 'focused'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.callValue(getTextInputSelectionBeginIndex, cast ([source] : Array<Dynamic>)), FlightRuntime.callValue(getTextInputSelectionEndIndex, cast ([source] : Array<Dynamic>)))))) {
  FlightRuntime.callValue(getTextInputCaretRectangle, cast ([DisplayObjectWgpu.caretRectangle__wgpuTextInput, source, result] : Array<Dynamic>));
  FlightRuntime.setField(context, 'fillStyle', DisplayObjectWgpu.CARET_COLOR__wgpuTextInput);
  FlightRuntime.setField(context, 'globalAlpha', 1.0);
  FlightRuntime.callProperty(context, 'fillRect', cast ([(FlightRuntime.field(DisplayObjectWgpu.caretRectangle__wgpuTextInput, 'x') - scrollXOffset), (FlightRuntime.field(DisplayObjectWgpu.caretRectangle__wgpuTextInput, 'y') - scrollYOffset), DisplayObjectWgpu.CARET_WIDTH__wgpuTextInput, FlightRuntime.field(DisplayObjectWgpu.caretRectangle__wgpuTextInput, 'height')] : Array<Dynamic>));
}
    FlightRuntime.callProperty(context, 'restore', cast ([] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuTextLabel(state:WgpuRenderState, renderProxy:RenderProxy2D):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var text:Dynamic = cast FlightRuntime.UNDEFINED;
    var textFormat:Dynamic = cast FlightRuntime.UNDEFINED;
    var fieldWidth:Dynamic = cast FlightRuntime.UNDEFINED;
    var fieldHeight:Dynamic = cast FlightRuntime.UNDEFINED;
    var material:Dynamic = cast FlightRuntime.UNDEFINED;
    var materialRenderer:Dynamic = cast FlightRuntime.UNDEFINED;
    var textData:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxTexDim:Dynamic = cast FlightRuntime.UNDEFINED;
    var pixelRatio:Dynamic = cast FlightRuntime.UNDEFINED;
    var version:Dynamic = cast FlightRuntime.UNDEFINED;
    var startCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var base:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    source = (cast FlightRuntime.field(renderProxy, 'source') : TextLabel);
    __destructure0 = FlightRuntime.field(source, 'data');
    text = FlightRuntime.field(__destructure0, 'text');
    textFormat = FlightRuntime.field(__destructure0, 'textFormat');
    fieldWidth = FlightRuntime.field(__destructure0, 'width');
    fieldHeight = FlightRuntime.field(__destructure0, 'height');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(text, 'length'), 0.0))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(renderProxy, 'rendererData'), null))) { return; }
    material = FlightRuntime.field(renderProxy, 'material');
    materialRenderer = FlightRuntime.callValue(resolveWgpuMaterialRenderer, cast ([state, material] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(materialRenderer, null))) { return; }
    textData = FlightRuntime.callValue(getWgpuRendererData, cast ([FlightRuntime.field(renderProxy, 'rendererData')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(textData, null))) { return; }
    maxTexDim = FlightRuntime.field(FlightRuntime.field(FlightRuntime.field(state, 'device'), 'limits'), 'maxTextureDimension2D');
    pixelRatio = FlightRuntime.field(state, 'pixelRatio');
    version = FlightRuntime.callValue(getNodeLocalContentRevision, cast ([source] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(!FlightRuntime.strictEquals(version, FlightRuntime.field(textData, 'lastContentId')), function():Dynamic return cast !FlightRuntime.strictEquals(pixelRatio, FlightRuntime.field(textData, 'lastPixelRatio'))))) {
  var measure:Dynamic = function(t:String, format:TextFormat) {
  FlightRuntime.setField(FlightRuntime.field(textData, 'ctx'), 'font', FlightRuntime.callValue(computeTextFormatFontString, cast ([format] : Array<Dynamic>)));
  return cast FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime.field(textData, 'ctx'), 'measureText', cast ([t] : Array<Dynamic>)), 'width');
};
  var result:Dynamic = FlightRuntime.callValue(getTextLayoutResult, cast ([(cast FlightRuntime.callValue(getTextLabelRuntime, cast ([source] : Array<Dynamic>)) : TextLabelRuntime)] : Array<Dynamic>));
  FlightRuntime.callValue(computeTextLayout, cast ([result, { text: text, formatRanges: cast ([FlightRuntime.callValue(createTextFormatRange, cast ([textFormat, 0.0, FlightRuntime.field(text, 'length')] : Array<Dynamic>))] : Array<Dynamic>), width: fieldWidth, height: fieldHeight, measure: measure, verticalAlign: FlightRuntime.select(FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(source, 'data'), 'autoSize'), 'none'), function():Dynamic return cast FlightRuntime.field(FlightRuntime.field(source, 'data'), 'verticalAlign'), function():Dynamic return cast 'top') }] : Array<Dynamic>));
  FlightRuntime.setField(textData, 'lastContentId', version);
  FlightRuntime.setField(textData, 'lastPixelRatio', pixelRatio);
  FlightRuntime.setField(textData, 'logW', 0.0);
  FlightRuntime.setField(textData, 'logH', 0.0);
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(result, 'groups'), 'length'), 0.0))) { return; }
  var maxX:Dynamic = 0.0;
  var maxY:Dynamic = 0.0;
  for (group in FlightRuntime.iterable(FlightRuntime.field(result, 'groups'))) {
    var right:Dynamic = (FlightRuntime.field(group, 'offsetX') + FlightRuntime.field(group, 'width'));
    var bottom:Dynamic = ((FlightRuntime.field(group, 'offsetY') + FlightRuntime.field(group, 'ascent')) + FlightRuntime.field(group, 'descent'));
    if (FlightRuntime.truthy(FlightRuntime.compare(right, maxX, '>'))) { (maxX = cast (right : Dynamic)); }
    if (FlightRuntime.truthy(FlightRuntime.compare(bottom, maxY, '>'))) { (maxY = cast (bottom : Dynamic)); }
  }
  var maxLogical:Dynamic = FlightRuntime.callProperty(HxMath, 'floor', cast ([(maxTexDim / pixelRatio)] : Array<Dynamic>));
  var w:Dynamic = FlightRuntime.callProperty(HxMath, 'min', cast ([FlightRuntime.callProperty(HxMath, 'ceil', cast ([maxX] : Array<Dynamic>)), maxLogical] : Array<Dynamic>));
  var h:Dynamic = FlightRuntime.callProperty(HxMath, 'min', cast ([FlightRuntime.callProperty(HxMath, 'ceil', cast ([maxY] : Array<Dynamic>)), maxLogical] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(w, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(h, 0.0, '<=')))) { return; }
  var pw:Dynamic = FlightRuntime.callProperty(HxMath, 'ceil', cast ([(w * pixelRatio)] : Array<Dynamic>));
  var ph:Dynamic = FlightRuntime.callProperty(HxMath, 'ceil', cast ([(h * pixelRatio)] : Array<Dynamic>));
  FlightRuntime.setField(FlightRuntime.field(textData, 'canvas'), 'width', pw);
  FlightRuntime.setField(FlightRuntime.field(textData, 'canvas'), 'height', ph);
  var ctx:Dynamic = FlightRuntime.field(textData, 'ctx');
  FlightRuntime.callProperty(ctx, 'setTransform', cast ([pixelRatio, 0.0, 0.0, pixelRatio, 0.0, 0.0] : Array<Dynamic>));
  FlightRuntime.callProperty(ctx, 'clearRect', cast ([0.0, 0.0, w, h] : Array<Dynamic>));
  FlightRuntime.setField(ctx, 'textBaseline', 'alphabetic');
  FlightRuntime.setField(ctx, 'textAlign', 'start');
  for (group in FlightRuntime.iterable(FlightRuntime.field(result, 'groups'))) {
    FlightRuntime.setField(ctx, 'font', FlightRuntime.callValue(computeTextFormatFontString, cast ([FlightRuntime.field(group, 'format')] : Array<Dynamic>)));
    FlightRuntime.setField(ctx, 'fillStyle', FlightRuntime.callValue(computeRgbHexString, cast ([FlightRuntime.coalesce(FlightRuntime.field(FlightRuntime.field(group, 'format'), 'color'), function():Dynamic return cast 0.0)] : Array<Dynamic>)));
    var slice:Dynamic = FlightRuntime.substring(text, FlightRuntime.field(group, 'startIndex'), FlightRuntime.field(group, 'endIndex'));
    FlightRuntime.callProperty(ctx, 'fillText', cast ([slice, FlightRuntime.field(group, 'offsetX'), (FlightRuntime.field(group, 'offsetY') + (FlightRuntime.field(group, 'ascent') * 0.815))] : Array<Dynamic>));
  }
  FlightRuntime.callValue(setImageResourceSource, cast ([FlightRuntime.field(textData, 'image'), FlightRuntime.field(textData, 'canvas')] : Array<Dynamic>));
  FlightRuntime.setField(textData, 'logW', w);
  FlightRuntime.setField(textData, 'logH', h);
  FlightRuntime.setField(textData, 'lastPW', pw);
  FlightRuntime.setField(textData, 'lastPH', ph);
}
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(FlightRuntime.field(textData, 'logW'), 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(textData, 'logH'), 0.0, '<=')))) { return; }
    FlightRuntime.callValue(ensureWgpuQuadBatchResources, cast ([state] : Array<Dynamic>));
    startCount = FlightRuntime.field(runtime, 'spriteBatchCount');
    base = FlightRuntime.callValue(prepareWgpuSpriteBatchWrite, cast ([state, FlightRuntime.field(textData, 'image'), FlightRuntime.field(renderProxy, 'blendMode'), material, materialRenderer, 1.0] : Array<Dynamic>));
    d = FlightRuntime.field(runtime, 'spriteBatchInstanceData');
    t = FlightRuntime.field(renderProxy, 'transform2D');
    FlightRuntime.setIndex(d, base, FlightRuntime.field(t, 'a'));
    FlightRuntime.setIndex(d, (base + 1.0), FlightRuntime.field(t, 'b'));
    FlightRuntime.setIndex(d, (base + 2.0), FlightRuntime.field(t, 'c'));
    FlightRuntime.setIndex(d, (base + 3.0), FlightRuntime.field(t, 'd'));
    FlightRuntime.setIndex(d, (base + 4.0), FlightRuntime.field(t, 'tx'));
    FlightRuntime.setIndex(d, (base + 5.0), FlightRuntime.field(t, 'ty'));
    FlightRuntime.setIndex(d, (base + 6.0), FlightRuntime.field(textData, 'logW'));
    FlightRuntime.setIndex(d, (base + 7.0), FlightRuntime.field(textData, 'logH'));
    FlightRuntime.setIndex(d, (base + 8.0), 0.0);
    FlightRuntime.setIndex(d, (base + 9.0), 0.0);
    FlightRuntime.setIndex(d, (base + 10.0), 1.0);
    FlightRuntime.setIndex(d, (base + 11.0), 1.0);
    FlightRuntime.setIndex(d, (base + 12.0), FlightRuntime.field(renderProxy, 'alpha'));
    FlightRuntime.callValue(packWgpuSpriteBatchMaterialInstance, cast ([state, FlightRuntime.field(renderProxy, 'materialData'), startCount] : Array<Dynamic>));
    FlightRuntime.callValue(recordWgpuSpriteBatchColorTransform, cast ([state, FlightRuntime.field(renderProxy, 'colorTransform'), startCount] : Array<Dynamic>));
    FlightRuntime.incrementField(runtime, 'spriteBatchCount', 1, true);
  }

  @:keep public static function drawWgpuVelocityQuad(ctx:WgpuVelocityContext, x:Float, y:Float, width:Float, height:Float, velocityX:Float, velocityY:Float):Void {
    var active:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipX0:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipY0:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipWidth:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipHeight:Dynamic = cast FlightRuntime.UNDEFINED;
    var slot:Dynamic = cast FlightRuntime.UNDEFINED;
    var scratch:Dynamic = cast FlightRuntime.UNDEFINED;
    active = FlightRuntime.callProperty(DisplayObjectWgpu._activeVelocityPasses__wgpuVelocity, 'get', cast ([FlightRuntime.field(ctx, 'state')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(active, FlightRuntime.UNDEFINED))) { return; }
    pipeline = FlightRuntime.field(active, 'pipeline');
    clipX0 = (((x / FlightRuntime.field(ctx, 'width')) * 2.0) - 1.0);
    clipY0 = (1.0 - ((y / FlightRuntime.field(ctx, 'height')) * 2.0));
    clipWidth = ((width / FlightRuntime.field(ctx, 'width')) * 2.0);
    clipHeight = -((height / FlightRuntime.field(ctx, 'height')) * 2.0);
    slot = FlightRuntime.field(pipeline, 'cursor');
    FlightRuntime.setField(pipeline, 'cursor', ((slot + DisplayObjectWgpu.UNIFORM_STRIDE__wgpuVelocity) % (DisplayObjectWgpu.UNIFORM_SLOTS__wgpuVelocity * DisplayObjectWgpu.UNIFORM_STRIDE__wgpuVelocity)));
    scratch = FlightRuntime.field(pipeline, 'scratch');
    FlightRuntime.setIndex(scratch, 0.0, clipX0);
    FlightRuntime.setIndex(scratch, 1.0, clipY0);
    FlightRuntime.setIndex(scratch, 2.0, clipWidth);
    FlightRuntime.setIndex(scratch, 3.0, clipHeight);
    FlightRuntime.setIndex(scratch, 4.0, (velocityX * FlightRuntime.field(ctx, 'pixelRatio')));
    FlightRuntime.setIndex(scratch, 5.0, (velocityY * FlightRuntime.field(ctx, 'pixelRatio')));
    FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(FlightRuntime.field(ctx, 'state'), 'device'), 'queue'), 'writeBuffer', cast ([FlightRuntime.field(pipeline, 'uniformBuffer'), slot, FlightRuntime.field(scratch, 'buffer'), 0.0, DisplayObjectWgpu.UNIFORM_BYTES__wgpuVelocity] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(active, 'pass'), 'setBindGroup', cast ([0.0, FlightRuntime.field(pipeline, 'bindGroup'), cast ([slot] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(active, 'pass'), 'draw', cast ([6.0] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuVideo(state:WgpuRenderState, renderProxy:RenderProxy2D):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var element:Dynamic = cast FlightRuntime.UNDEFINED;
    var vw:Dynamic = cast FlightRuntime.UNDEFINED;
    var vh:Dynamic = cast FlightRuntime.UNDEFINED;
    var videoData:Dynamic = cast FlightRuntime.UNDEFINED;
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    source = (cast FlightRuntime.field(renderProxy, 'source') : Video);
    element = FlightRuntime.optionalField(FlightRuntime.field(FlightRuntime.field(source, 'data'), 'source'), 'element');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(element, FlightRuntime.UNDEFINED), function():Dynamic return cast FlightRuntime.strictEquals(element, null)), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(element, 'readyState'), 2.0, '<')))) { return; }
    vw = FlightRuntime.field(element, 'videoWidth');
    vh = FlightRuntime.field(element, 'videoHeight');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(vw, 0.0), function():Dynamic return cast FlightRuntime.strictEquals(vh, 0.0)))) { return; }
    FlightRuntime.callOptionalProperty(state, 'applyBlendMode', cast ([state, FlightRuntime.field(renderProxy, 'blendMode')] : Array<Dynamic>));
    videoData = FlightRuntime.callValue(getWgpuRendererData, cast ([FlightRuntime.field(renderProxy, 'rendererData')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(videoData, null))) { return; }
    entry = FlightRuntime.field(videoData, 'entry');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(entry, null), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(videoData, 'w'), vw)), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(videoData, 'h'), vh)))) {
  FlightRuntime.callOptionalProperty(FlightRuntime.optionalField(entry, 'texture'), 'destroy', cast ([] : Array<Dynamic>));
  var device:Dynamic = FlightRuntime.field(state, 'device');
  var __destructure0:Dynamic = runtime;
  var textureBindGroupLayout:Dynamic = FlightRuntime.field(__destructure0, 'textureBindGroupLayout');
  var linearSampler:Dynamic = FlightRuntime.field(__destructure0, 'linearSampler');
  var texture:Dynamic = FlightRuntime.callProperty(device, 'createTexture', cast ([{ format: 'rgba8unorm', size: cast ([vw, vh, 1.0] : Array<Dynamic>), usage: (Std.int((Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUTextureUsage'] : Array<Dynamic>)), 'COPY_DST')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUTextureUsage'] : Array<Dynamic>)), 'RENDER_ATTACHMENT')))) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUTextureUsage'] : Array<Dynamic>)), 'TEXTURE_BINDING'))) }] : Array<Dynamic>));
  var view:Dynamic = FlightRuntime.callProperty(texture, 'createView', cast ([] : Array<Dynamic>));
  var bindGroup:Dynamic = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ entries: cast ([{ binding: 0.0, resource: view }, { binding: 1.0, resource: linearSampler }] : Array<Dynamic>), layout: textureBindGroupLayout }] : Array<Dynamic>));
  (entry = cast ({ bindGroup: bindGroup, texture: texture, view: view } : Dynamic));
  FlightRuntime.setField(videoData, 'entry', entry);
  FlightRuntime.setField(videoData, 'w', vw);
  FlightRuntime.setField(videoData, 'h', vh);
}
    FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(state, 'device'), 'queue'), 'copyExternalImageToTexture', cast ([{ source: element, flipY: false }, { premultipliedAlpha: false, texture: FlightRuntime.field(entry, 'texture') }, cast ([vw, vh] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuQuad, cast ([state, renderProxy, entry, 0.0, 0.0, vw, vh, 0.0, 0.0, 1.0, 1.0] : Array<Dynamic>));
  }

  @:keep public static function enableWgpuClipSupport(state:WgpuRenderState):Void {
    FlightRuntime.setField(state, 'displayObjectClipHooks', DisplayObjectWgpu.webgpuClipHooks__wgpuClip);
  }

  @:keep public static function enableWgpuColorAdjustment(state:WgpuRenderState):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    FlightRuntime.setField(runtime, 'wgpuColorAdjustmentFold', DisplayObjectWgpu.wgpuColorAdjustmentFold__wgpuColorAdjustment);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'spriteBatchColorTransformMode'), FlightRuntime.UNDEFINED))) { FlightRuntime.setField(runtime, 'spriteBatchColorTransformMode', DisplayObjectWgpu.CT_MODE_NONE__wgpuColorAdjustment); }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'spriteBatchColorTransformData'), FlightRuntime.UNDEFINED))) {
  FlightRuntime.setField(runtime, 'spriteBatchColorTransformData', FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [(DisplayObjectWgpu.COLOR_TRANSFORM_FLOATS__wgpuColorAdjustment * 256.0)]));
}
  }

  @:keep public static function enableWgpuColorAdjustmentGuards(state:WgpuRenderState):Void {
    FlightRuntime.setField(FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>)), 'wgpuColorAdjustmentGuard', DisplayObjectWgpu.warnWgpuColorAdjustmentNotEnabled__enableWgpuColorAdjustmentGuards);
  }

  @:keep public static function enableWgpuRenderCache(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerRenderCacheRenderer, cast ([state, defaultWgpuRenderCacheRenderer] : Array<Dynamic>));
  }

  @:keep public static function enableWgpuTextInput():Void {
    FlightRuntime.callValue(registerWgpuTextInputOverlay, cast ([drawWgpuTextInputOverlay] : Array<Dynamic>));
  }

  @:keep public static function ensureClipContourPipelines__wgpuClipContours(state:WgpuRenderState):WgpuClipContourPipelines {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var format:Dynamic = cast FlightRuntime.UNDEFINED;
    var cache:Dynamic = cast FlightRuntime.UNDEFINED;
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var module:Dynamic = cast FlightRuntime.UNDEFINED;
    var bindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var layout:Dynamic = cast FlightRuntime.UNDEFINED;
    var vertexBuffers:Array<Dynamic> = cast FlightRuntime.UNDEFINED;
    var make:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipelines:WgpuClipContourPipelines = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    format = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'currentColorFormat'), function():Dynamic return cast FlightRuntime.field(state, 'format'));
    cache = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'clipContourPipelines'), function():Dynamic return cast FlightRuntime.setField(runtime, 'clipContourPipelines', FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), [])));
    existing = FlightRuntime.callProperty(cache, 'get', cast ([format] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED))) { return cast existing; }
    device = FlightRuntime.field(state, 'device');
    module = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: DisplayObjectWgpu.CLIP_WGSL__wgpuClipContours }] : Array<Dynamic>));
    bindGroupLayout = FlightRuntime.callProperty(device, 'createBindGroupLayout', cast ([{ entries: cast ([{ binding: 0.0, visibility: FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'VERTEX'), buffer: { type: 'uniform' } }] : Array<Dynamic>) }] : Array<Dynamic>));
    layout = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([bindGroupLayout] : Array<Dynamic>) }] : Array<Dynamic>));
    vertexBuffers = cast ([{ arrayStride: 8.0, attributes: cast ([{ shaderLocation: 0.0, offset: 0.0, format: 'float32x2' }] : Array<Dynamic>) }] : Array<Dynamic>);
    make = function(passOp:Dynamic) return FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: layout, vertex: { module: module, entryPoint: 'vs_main', buffers: vertexBuffers }, fragment: { module: module, entryPoint: 'fs_main', targets: cast ([{ format: format, writeMask: 0.0 }] : Array<Dynamic>) }, primitive: { topology: 'triangle-list', cullMode: 'none' }, depthStencil: { format: 'depth24plus-stencil8', depthWriteEnabled: false, depthCompare: 'always', stencilFront: { compare: 'equal', passOp: passOp, failOp: 'keep', depthFailOp: 'keep' }, stencilBack: { compare: 'equal', passOp: passOp, failOp: 'keep', depthFailOp: 'keep' }, stencilReadMask: 255.0, stencilWriteMask: 255.0 } }] : Array<Dynamic>));
    pipelines = { write: FlightRuntime.callValue(make, cast (['increment-clamp'] : Array<Dynamic>)), erase: FlightRuntime.callValue(make, cast (['decrement-clamp'] : Array<Dynamic>)), bindGroupLayout: bindGroupLayout };
    FlightRuntime.callProperty(cache, 'set', cast ([format, pipelines] : Array<Dynamic>));
    return cast pipelines;
    return cast null;
  }

  @:keep public static function ensureParticleInstanceBuffer__wgpuParticleEmitter(state:WgpuRenderState, count:Float):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var needed:Dynamic = cast FlightRuntime.UNDEFINED;
    var newCapacity:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    needed = (count * DisplayObjectWgpu.INSTANCE_STRIDE__wgpuParticleEmitter);
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(FlightRuntime.field(runtime, 'particleInstanceCapacity'), needed, '>='), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'particleInstanceBuffer'), null)))) { return; }
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'particleInstanceBuffer'), null))) {
  FlightRuntime.callProperty(FlightRuntime.coalesce(FlightRuntime.field(runtime, 'retiredBuffers'), function():Dynamic return cast FlightRuntime.setField(runtime, 'retiredBuffers', cast ([] : Array<Dynamic>))), 'push', cast ([FlightRuntime.field(runtime, 'particleInstanceBuffer')] : Array<Dynamic>));
}
    newCapacity = FlightRuntime.callProperty(HxMath, 'max', cast ([needed, (FlightRuntime.orValue(FlightRuntime.field(runtime, 'particleInstanceCapacity'), function():Dynamic return cast 0.0) * 2.0)] : Array<Dynamic>));
    FlightRuntime.setField(runtime, 'particleInstanceBuffer', FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBuffer', cast ([{ size: FlightRuntime.callProperty(HxMath, 'max', cast ([newCapacity, DisplayObjectWgpu.INSTANCE_STRIDE__wgpuParticleEmitter] : Array<Dynamic>)), usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'STORAGE')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>)));
    FlightRuntime.setField(runtime, 'particleInstanceCapacity', newCapacity);
    FlightRuntime.setField(runtime, 'particleInstanceData', FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [(newCapacity / 4.0)]));
  }

  @:keep public static function ensureParticleResources__wgpuParticleEmitter(state:WgpuRenderState):WgpuParticleResources__wgpuParticleEmitter {
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure1:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformBindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var textureBindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var instanceBindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipelineLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var module:Dynamic = cast FlightRuntime.UNDEFINED;
    var resources:WgpuParticleResources__wgpuParticleEmitter = cast FlightRuntime.UNDEFINED;
    existing = FlightRuntime.callProperty(DisplayObjectWgpu._particleResources__wgpuParticleEmitter, 'get', cast ([FlightRuntime.field(state, 'device')] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED))) { return cast existing; }
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    __destructure0 = state;
    device = FlightRuntime.field(__destructure0, 'device');
    __destructure1 = runtime;
    uniformBindGroupLayout = FlightRuntime.field(__destructure1, 'uniformBindGroupLayout');
    textureBindGroupLayout = FlightRuntime.field(__destructure1, 'textureBindGroupLayout');
    instanceBindGroupLayout = FlightRuntime.callProperty(device, 'createBindGroupLayout', cast ([{ entries: cast ([{ binding: 0.0, visibility: FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'VERTEX'), buffer: { type: 'read-only-storage' } }] : Array<Dynamic>) }] : Array<Dynamic>));
    pipelineLayout = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([uniformBindGroupLayout, textureBindGroupLayout, instanceBindGroupLayout] : Array<Dynamic>) }] : Array<Dynamic>));
    module = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: DisplayObjectWgpu.PARTICLE_SHADER_SRC__wgpuParticleEmitter }] : Array<Dynamic>));
    resources = { pipelines: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []), pipelineLayout: pipelineLayout, module: module, instanceBindGroupLayout: instanceBindGroupLayout };
    FlightRuntime.callProperty(DisplayObjectWgpu._particleResources__wgpuParticleEmitter, 'set', cast ([device, resources] : Array<Dynamic>));
    return cast resources;
    return cast null;
  }

  @:keep public static function ensureShapeMeshIndexBuffer__wgpuShapeMesh(state:WgpuRenderState, buffers:WgpuShapeMeshBuffers, byteLength:Float):Dynamic {
    var size:Dynamic = cast FlightRuntime.UNDEFINED;
    size = FlightRuntime.callProperty(HxMath, 'max', cast ([4.0, (Std.int((byteLength + 3.0)) & Std.int(~Std.int(3.0)))] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.field(buffers, 'indexBuffer'), null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(buffers, 'indexCapacity'), size, '<')))) {
  FlightRuntime.callOptionalProperty(FlightRuntime.field(buffers, 'indexBuffer'), 'destroy', cast ([] : Array<Dynamic>));
  FlightRuntime.setField(buffers, 'indexBuffer', FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBuffer', cast ([{ size: size, usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'INDEX')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>)));
  FlightRuntime.setField(buffers, 'indexCapacity', size);
}
    return cast FlightRuntime.field(buffers, 'indexBuffer');
    return cast null;
  }

  @:keep public static function ensureShapeMeshPipeline__wgpuShapeMesh(state:WgpuRenderState):WgpuShapeMeshPipeline {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var format:Dynamic = cast FlightRuntime.UNDEFINED;
    var cache:Dynamic = cast FlightRuntime.UNDEFINED;
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var module:Dynamic = cast FlightRuntime.UNDEFINED;
    var bindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var layout:Dynamic = cast FlightRuntime.UNDEFINED;
    var vertexBuffers:Array<Dynamic> = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var entry:WgpuShapeMeshPipeline = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    format = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'currentColorFormat'), function():Dynamic return cast FlightRuntime.field(state, 'format'));
    cache = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'shapeMeshPipelines'), function():Dynamic return cast FlightRuntime.setField(runtime, 'shapeMeshPipelines', FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), [])));
    existing = FlightRuntime.callProperty(cache, 'get', cast ([format] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED))) { return cast existing; }
    device = FlightRuntime.field(state, 'device');
    module = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: DisplayObjectWgpu.SHAPE_MESH_WGSL__wgpuShapeMesh }] : Array<Dynamic>));
    bindGroupLayout = FlightRuntime.callProperty(device, 'createBindGroupLayout', cast ([{ entries: cast ([{ binding: 0.0, visibility: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'VERTEX')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'FRAGMENT'))), buffer: { type: 'uniform' } }] : Array<Dynamic>) }] : Array<Dynamic>));
    layout = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([bindGroupLayout] : Array<Dynamic>) }] : Array<Dynamic>));
    vertexBuffers = cast ([{ arrayStride: 8.0, attributes: cast ([{ shaderLocation: 0.0, offset: 0.0, format: 'float32x2' }] : Array<Dynamic>) }] : Array<Dynamic>);
    pipeline = FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: layout, vertex: { module: module, entryPoint: 'vs_main', buffers: vertexBuffers }, fragment: { module: module, entryPoint: 'fs_main', targets: cast ([{ format: format, blend: { color: { srcFactor: 'one', dstFactor: 'one-minus-src-alpha', operation: 'add' }, alpha: { srcFactor: 'one', dstFactor: 'one-minus-src-alpha', operation: 'add' } } }] : Array<Dynamic>) }, primitive: { topology: 'triangle-list', cullMode: 'none' }, depthStencil: { format: 'depth24plus-stencil8', depthWriteEnabled: false, depthCompare: 'always', stencilFront: { compare: 'equal', passOp: 'keep', failOp: 'keep', depthFailOp: 'keep' }, stencilBack: { compare: 'equal', passOp: 'keep', failOp: 'keep', depthFailOp: 'keep' }, stencilReadMask: 255.0, stencilWriteMask: 0.0 } }] : Array<Dynamic>));
    entry = { pipeline: pipeline, bindGroupLayout: bindGroupLayout };
    FlightRuntime.callProperty(cache, 'set', cast ([format, entry] : Array<Dynamic>));
    return cast entry;
    return cast null;
  }

  @:keep public static function ensureShapeMeshUniform__wgpuShapeMesh(state:WgpuRenderState, pipelineEntry:WgpuShapeMeshPipeline, buffers:WgpuShapeMeshBuffers):flight.internal.FlightFloat32Array {
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(buffers, 'uniformBuffer'), null))) {
  FlightRuntime.setField(buffers, 'uniformBuffer', FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBuffer', cast ([{ size: DisplayObjectWgpu.SHAPE_MESH_UNIFORM_BYTES__wgpuShapeMesh, usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'UNIFORM')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>)));
  FlightRuntime.setField(buffers, 'bindGroup', FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBindGroup', cast ([{ layout: FlightRuntime.field(pipelineEntry, 'bindGroupLayout'), entries: cast ([{ binding: 0.0, resource: { buffer: FlightRuntime.field(buffers, 'uniformBuffer') } }] : Array<Dynamic>) }] : Array<Dynamic>)));
}
    return cast DisplayObjectWgpu._shapeMeshUniformScratch__wgpuShapeMesh;
    return cast null;
  }

  @:keep public static function ensureShapeMeshVertexBuffer__wgpuShapeMesh(state:WgpuRenderState, buffers:WgpuShapeMeshBuffers, byteLength:Float):Dynamic {
    var size:Dynamic = cast FlightRuntime.UNDEFINED;
    size = FlightRuntime.callProperty(HxMath, 'max', cast ([8.0, byteLength] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.field(buffers, 'vertexBuffer'), null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(buffers, 'vertexCapacity'), size, '<')))) {
  FlightRuntime.callOptionalProperty(FlightRuntime.field(buffers, 'vertexBuffer'), 'destroy', cast ([] : Array<Dynamic>));
  FlightRuntime.setField(buffers, 'vertexBuffer', FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBuffer', cast ([{ size: size, usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'VERTEX')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>)));
  FlightRuntime.setField(buffers, 'vertexCapacity', size);
}
    return cast FlightRuntime.field(buffers, 'vertexBuffer');
    return cast null;
  }

  @:keep public static function ensureWgpuColorTransformCapacity__wgpuColorAdjustment(runtime:WgpuRenderStateRuntime, floatsNeeded:Float):Void {
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var newSize:Dynamic = cast FlightRuntime.UNDEFINED;
    var grown:Dynamic = cast FlightRuntime.UNDEFINED;
    existing = FlightRuntime.field(runtime, 'spriteBatchColorTransformData');
    if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED), function():Dynamic return cast FlightRuntime.compare(floatsNeeded, FlightRuntime.field(existing, 'length'), '<=')))) { return; }
    newSize = HxMath.max(HxMath.max(floatsNeeded, (FlightRuntime.coalesce(FlightRuntime.optionalField(existing, 'length'), function():Dynamic return cast 0.0) * 2.0)), (DisplayObjectWgpu.COLOR_TRANSFORM_FLOATS__wgpuColorAdjustment * 256.0));
    grown = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [newSize]);
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED))) { FlightRuntime.callProperty(grown, 'set', cast ([existing] : Array<Dynamic>)); }
    FlightRuntime.setField(runtime, 'spriteBatchColorTransformData', grown);
  }

  @:keep public static function ensureWgpuQuadBatchResources(state:WgpuRenderState):WgpuQuadBatchResources {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure1:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformBindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var textureBindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var instanceBindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var materialBindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var basePipelineLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var materialPipelineLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var resources:WgpuQuadBatchResources = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    existing = FlightRuntime.callProperty(DisplayObjectWgpu._quadBatchResources__wgpuSpriteBatch, 'get', cast ([FlightRuntime.field(state, 'device')] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED))) { return cast existing; }
    __destructure0 = state;
    device = FlightRuntime.field(__destructure0, 'device');
    __destructure1 = runtime;
    uniformBindGroupLayout = FlightRuntime.field(__destructure1, 'uniformBindGroupLayout');
    textureBindGroupLayout = FlightRuntime.field(__destructure1, 'textureBindGroupLayout');
    instanceBindGroupLayout = FlightRuntime.callProperty(device, 'createBindGroupLayout', cast ([{ entries: cast ([{ binding: 0.0, visibility: FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'VERTEX'), buffer: { type: 'read-only-storage' } }] : Array<Dynamic>) }] : Array<Dynamic>));
    materialBindGroupLayout = FlightRuntime.callProperty(device, 'createBindGroupLayout', cast ([{ entries: cast ([{ binding: 0.0, visibility: FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'VERTEX'), buffer: { type: 'read-only-storage' } }] : Array<Dynamic>) }] : Array<Dynamic>));
    basePipelineLayout = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([uniformBindGroupLayout, textureBindGroupLayout, instanceBindGroupLayout] : Array<Dynamic>) }] : Array<Dynamic>));
    materialPipelineLayout = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([uniformBindGroupLayout, textureBindGroupLayout, instanceBindGroupLayout, materialBindGroupLayout] : Array<Dynamic>) }] : Array<Dynamic>));
    resources = { instanceBindGroupLayout: instanceBindGroupLayout, materialBindGroupLayout: materialBindGroupLayout, basePipelineLayout: basePipelineLayout, materialPipelineLayout: materialPipelineLayout, pipelines: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []) };
    FlightRuntime.callProperty(DisplayObjectWgpu._quadBatchResources__wgpuSpriteBatch, 'set', cast ([device, resources] : Array<Dynamic>));
    return cast resources;
    return cast null;
  }

  @:keep public static function ensureWgpuRenderCacheTarget(state:WgpuRenderState, cache:RenderCache, width:Float, height:Float):WgpuRenderTarget {
    var targets:Dynamic = cast FlightRuntime.UNDEFINED;
    var target:Dynamic = cast FlightRuntime.UNDEFINED;
    targets = FlightRuntime.callValue(DisplayObjectWgpu.getTargets__wgpuCache, cast ([state] : Array<Dynamic>));
    target = FlightRuntime.callProperty(targets, 'get', cast ([cache] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(target, FlightRuntime.UNDEFINED))) {
  (target = cast (FlightRuntime.callValue(createWgpuRenderTarget, cast ([state, width, height] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(targets, 'set', cast ([cache, target] : Array<Dynamic>));
} else {
  FlightRuntime.callValue(resizeWgpuRenderTarget, cast ([state, target, width, height] : Array<Dynamic>));
}
    return cast target;
    return cast null;
  }

  @:keep public static function ensureWgpuRenderStatsMutable__wgpuRenderStats(state:WgpuRenderState):Mutable__wgpuRenderStats<WgpuRenderStats> {
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    entry = FlightRuntime.callProperty(DisplayObjectWgpu._stats__wgpuRenderStats, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(entry, FlightRuntime.UNDEFINED))) {
  (entry = cast ({ batchFlushCount: 0.0, drawCallCount: 0.0, instanceCount: 0.0, textureUploadCount: 0.0 } : Dynamic));
  FlightRuntime.callProperty(DisplayObjectWgpu._stats__wgpuRenderStats, 'set', cast ([state, entry] : Array<Dynamic>));
}
    return cast entry;
    return cast null;
  }

  @:keep public static function ensureWgpuVelocityPipeline__wgpuVelocity(state:WgpuRenderState):WgpuVelocityPipeline__wgpuVelocity {
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var module:Dynamic = cast FlightRuntime.UNDEFINED;
    var bindGroupLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var layout:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformBuffer:Dynamic = cast FlightRuntime.UNDEFINED;
    var bindGroup:Dynamic = cast FlightRuntime.UNDEFINED;
    var entry:WgpuVelocityPipeline__wgpuVelocity = cast FlightRuntime.UNDEFINED;
    existing = FlightRuntime.callProperty(DisplayObjectWgpu._velocityPipelines__wgpuVelocity, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED))) { return cast existing; }
    device = FlightRuntime.field(state, 'device');
    module = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: DisplayObjectWgpu.VELOCITY_WGSL__wgpuVelocity }] : Array<Dynamic>));
    bindGroupLayout = FlightRuntime.callProperty(device, 'createBindGroupLayout', cast ([{ entries: cast ([{ binding: 0.0, visibility: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'VERTEX')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'FRAGMENT'))), buffer: { type: 'uniform', hasDynamicOffset: true } }] : Array<Dynamic>) }] : Array<Dynamic>));
    layout = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([bindGroupLayout] : Array<Dynamic>) }] : Array<Dynamic>));
    pipeline = FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: layout, vertex: { module: module, entryPoint: 'vs_main' }, fragment: { module: module, entryPoint: 'fs_main', targets: cast ([{ format: 'rgba16float' }] : Array<Dynamic>) }, primitive: { topology: 'triangle-list' } }] : Array<Dynamic>));
    uniformBuffer = FlightRuntime.callProperty(device, 'createBuffer', cast ([{ size: (DisplayObjectWgpu.UNIFORM_SLOTS__wgpuVelocity * DisplayObjectWgpu.UNIFORM_STRIDE__wgpuVelocity), usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'UNIFORM')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>));
    bindGroup = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: bindGroupLayout, entries: cast ([{ binding: 0.0, resource: { buffer: uniformBuffer, size: DisplayObjectWgpu.UNIFORM_BYTES__wgpuVelocity } }] : Array<Dynamic>) }] : Array<Dynamic>));
    entry = { pipeline: pipeline, uniformBuffer: uniformBuffer, bindGroup: bindGroup, cursor: 0.0, scratch: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [(DisplayObjectWgpu.UNIFORM_BYTES__wgpuVelocity / 4.0)]) };
    FlightRuntime.callProperty(DisplayObjectWgpu._velocityPipelines__wgpuVelocity, 'set', cast ([state, entry] : Array<Dynamic>));
    return cast entry;
    return cast null;
  }

  @:keep public static function equalsRecordedColorTransform__wgpuColorAdjustment(a:Null<ColorTransform>, b:Null<ColorTransform>):Bool {
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(a, b))) { return cast true; }
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(a, null), function():Dynamic return cast FlightRuntime.strictEquals(b, null)))) { return cast false; }
    return cast FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.strictEquals(FlightRuntime.field(a, 'redMultiplier'), FlightRuntime.field(b, 'redMultiplier')), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(a, 'greenMultiplier'), FlightRuntime.field(b, 'greenMultiplier'))), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(a, 'blueMultiplier'), FlightRuntime.field(b, 'blueMultiplier'))), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(a, 'alphaMultiplier'), FlightRuntime.field(b, 'alphaMultiplier'))), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(a, 'redOffset'), FlightRuntime.field(b, 'redOffset'))), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(a, 'greenOffset'), FlightRuntime.field(b, 'greenOffset'))), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(a, 'blueOffset'), FlightRuntime.field(b, 'blueOffset'))), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(a, 'alphaOffset'), FlightRuntime.field(b, 'alphaOffset')));
    return cast null;
  }

  @:keep public static function fillWgpuSpriteBatchUniformColorTransform__wgpuColorAdjustment(runtime:WgpuRenderStateRuntime, colorTransform:ColorTransform, count:Float):Void {
    FlightRuntime.callValue(DisplayObjectWgpu.ensureWgpuColorTransformCapacity__wgpuColorAdjustment, cast ([runtime, (count * DisplayObjectWgpu.COLOR_TRANSFORM_FLOATS__wgpuColorAdjustment)] : Array<Dynamic>));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, count, '<'))) {
        FlightRuntime.callValue(DisplayObjectWgpu.writeWgpuColorTransformInstance__wgpuColorAdjustment, cast ([runtime, colorTransform, i] : Array<Dynamic>));
        i++;
      }
    }
  }

  @:keep public static function flushWgpuSpriteBatch(state:WgpuRenderState):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var texture:Dynamic = cast FlightRuntime.UNDEFINED;
    var blendMode:Dynamic = cast FlightRuntime.UNDEFINED;
    var renderer:Dynamic = cast FlightRuntime.UNDEFINED;
    var ctFlush:Dynamic = cast FlightRuntime.UNDEFINED;
    var group3Floats:Dynamic = cast FlightRuntime.UNDEFINED;
    var group3Data:Dynamic = cast FlightRuntime.UNDEFINED;
    var resources:Dynamic = cast FlightRuntime.UNDEFINED;
    var slot:Dynamic = cast FlightRuntime.UNDEFINED;
    var instanceBytes:Dynamic = cast FlightRuntime.UNDEFINED;
    var textureEntry:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    var instanceBindGroup:Dynamic = cast FlightRuntime.UNDEFINED;
    var module:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    count = FlightRuntime.field(runtime, 'spriteBatchCount');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(count, 0.0), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null)))) {
  FlightRuntime.callValue(DisplayObjectWgpu.resetWgpuSpriteBatch__wgpuSpriteBatch, cast ([state] : Array<Dynamic>));
  return;
}
    texture = FlightRuntime.field(runtime, 'spriteBatchTexture');
    blendMode = FlightRuntime.field(runtime, 'spriteBatchBlendMode');
    renderer = FlightRuntime.field(runtime, 'spriteBatchMaterialRenderer');
    ctFlush = FlightRuntime.coalesce(FlightRuntime.callOptionalProperty(FlightRuntime.field(runtime, 'wgpuColorAdjustmentFold'), 'resolveFlush', cast ([state, count] : Array<Dynamic>)), function():Dynamic return cast null);
    group3Floats = FlightRuntime.select(!FlightRuntime.strictEquals(ctFlush, null), function():Dynamic return cast FlightRuntime.field(ctFlush, 'floats'), function():Dynamic return cast FlightRuntime.field(runtime, 'spriteBatchMaterialFloats'));
    group3Data = FlightRuntime.select(!FlightRuntime.strictEquals(ctFlush, null), function():Dynamic return cast FlightRuntime.field(ctFlush, 'data'), function():Dynamic return cast FlightRuntime.field(runtime, 'spriteBatchMaterialData'));
    FlightRuntime.callValue(DisplayObjectWgpu.resetWgpuSpriteBatch__wgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    resources = FlightRuntime.callValue(ensureWgpuQuadBatchResources, cast ([state] : Array<Dynamic>));
    slot = FlightRuntime.callValue(DisplayObjectWgpu.acquireWgpuSpriteBatchBufferSlot__wgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    instanceBytes = (count * DisplayObjectWgpu.SPRITE_INSTANCE_STRIDE__wgpuSpriteBatch);
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.field(slot, 'instanceBuffer'), null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(slot, 'instanceCapacity'), instanceBytes, '<')))) {
  var capacity:Dynamic = HxMath.max(HxMath.max(instanceBytes, (FlightRuntime.field(slot, 'instanceCapacity') * 2.0)), (DisplayObjectWgpu.SPRITE_INSTANCE_STRIDE__wgpuSpriteBatch * 256.0));
  FlightRuntime.setField(slot, 'instanceBuffer', FlightRuntime.callValue(DisplayObjectWgpu.createWgpuSpriteBatchBuffer__wgpuSpriteBatch, cast ([state, capacity] : Array<Dynamic>)));
  FlightRuntime.setField(slot, 'instanceCapacity', capacity);
}
    FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(state, 'device'), 'queue'), 'writeBuffer', cast ([FlightRuntime.field(slot, 'instanceBuffer'), 0.0, FlightRuntime.field(FlightRuntime.field(runtime, 'spriteBatchInstanceData'), 'buffer'), 0.0, instanceBytes] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(group3Floats, 0.0, '>'))) {
  var group3Bytes:Dynamic = ((count * group3Floats) * 4.0);
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.field(slot, 'materialBuffer'), null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(slot, 'materialCapacity'), group3Bytes, '<')))) {
  var capacity:Dynamic = HxMath.max(HxMath.max(group3Bytes, (FlightRuntime.field(slot, 'materialCapacity') * 2.0)), ((group3Floats * 4.0) * 256.0));
  FlightRuntime.setField(slot, 'materialBuffer', FlightRuntime.callValue(DisplayObjectWgpu.createWgpuSpriteBatchBuffer__wgpuSpriteBatch, cast ([state, capacity] : Array<Dynamic>)));
  FlightRuntime.setField(slot, 'materialCapacity', capacity);
}
  FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(state, 'device'), 'queue'), 'writeBuffer', cast ([FlightRuntime.field(slot, 'materialBuffer'), 0.0, FlightRuntime.field(group3Data, 'buffer'), 0.0, group3Bytes] : Array<Dynamic>));
}
    FlightRuntime.callOptionalProperty(state, 'applyBlendMode', cast ([state, blendMode] : Array<Dynamic>));
    textureEntry = FlightRuntime.callValue(bindWgpuImageResourceTexture, cast ([state, texture] : Array<Dynamic>));
    uniformOffset = FlightRuntime.callValue(DisplayObjectWgpu.writeWgpuSpriteBatchUniforms__wgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    instanceBindGroup = FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBindGroup', cast ([{ layout: FlightRuntime.field(resources, 'instanceBindGroupLayout'), entries: cast ([{ binding: 0.0, resource: { buffer: FlightRuntime.field(slot, 'instanceBuffer') } }] : Array<Dynamic>) }] : Array<Dynamic>));
    module = FlightRuntime.select(!FlightRuntime.strictEquals(ctFlush, null), function():Dynamic return cast FlightRuntime.field(ctFlush, 'module'), function():Dynamic return cast FlightRuntime.callProperty(renderer, 'getShaderModule', cast ([state] : Array<Dynamic>)));
    pipeline = FlightRuntime.callValue(getWgpuQuadBatchPipeline, cast ([state, resources, module, FlightRuntime.compare(group3Floats, 0.0, '>'), blendMode] : Array<Dynamic>));
    pass = FlightRuntime.field(runtime, 'renderPass');
    FlightRuntime.callProperty(pass, 'setPipeline', cast ([pipeline] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([0.0, FlightRuntime.field(runtime, 'uniformBindGroup'), cast ([uniformOffset] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([1.0, FlightRuntime.field(textureEntry, 'bindGroup')] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([2.0, instanceBindGroup] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(group3Floats, 0.0, '>'))) {
  var materialBindGroup:Dynamic = FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBindGroup', cast ([{ layout: FlightRuntime.field(resources, 'materialBindGroupLayout'), entries: cast ([{ binding: 0.0, resource: { buffer: FlightRuntime.field(slot, 'materialBuffer') } }] : Array<Dynamic>) }] : Array<Dynamic>));
  FlightRuntime.callProperty(pass, 'setBindGroup', cast ([3.0, materialBindGroup] : Array<Dynamic>));
}
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(runtime, 'currentMaskDepth'), 0.0, '>'))) { FlightRuntime.callProperty(pass, 'setStencilReference', cast ([FlightRuntime.field(runtime, 'currentMaskDepth')] : Array<Dynamic>)); }
    FlightRuntime.callProperty(pass, 'draw', cast ([6.0, count, 0.0, 0.0] : Array<Dynamic>));
  }

  @:keep public static function getOffscreenCanvas__wgpuRichText(width:Float, height:Float, pixelRatio:Float = 1.0):Dynamic {
    var pw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ph:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(!FlightRuntime.truthy(DisplayObjectWgpu._offscreenCanvas__wgpuRichText))) {
  (DisplayObjectWgpu._offscreenCanvas__wgpuRichText = cast (FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['document'] : Array<Dynamic>)), 'createElement', cast (['canvas'] : Array<Dynamic>)) : Dynamic));
  (DisplayObjectWgpu._offscreenCtx__wgpuRichText = cast (FlightRuntime.callProperty(DisplayObjectWgpu._offscreenCanvas__wgpuRichText, 'getContext', cast (['2d'] : Array<Dynamic>)) : Dynamic));
}
    pw = FlightRuntime.callProperty(HxMath, 'ceil', cast ([(width * pixelRatio)] : Array<Dynamic>));
    ph = FlightRuntime.callProperty(HxMath, 'ceil', cast ([(height * pixelRatio)] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(DisplayObjectWgpu._offscreenCanvas__wgpuRichText, 'width'), pw))) { FlightRuntime.setField(DisplayObjectWgpu._offscreenCanvas__wgpuRichText, 'width', pw); }
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(DisplayObjectWgpu._offscreenCanvas__wgpuRichText, 'height'), ph))) { FlightRuntime.setField(DisplayObjectWgpu._offscreenCanvas__wgpuRichText, 'height', ph); }
    return cast DisplayObjectWgpu._offscreenCtx__wgpuRichText;
    return cast null;
  }

  @:keep public static function getParticlePipeline__wgpuParticleEmitter(state:WgpuRenderState, resources:WgpuParticleResources__wgpuParticleEmitter):Dynamic {
    var format:Dynamic = cast FlightRuntime.UNDEFINED;
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    format = FlightRuntime.coalesce(FlightRuntime.field(FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>)), 'currentColorFormat'), function():Dynamic return cast FlightRuntime.field(state, 'format'));
    existing = FlightRuntime.callProperty(FlightRuntime.field(resources, 'pipelines'), 'get', cast ([format] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED))) { return cast existing; }
    pipeline = FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createRenderPipeline', cast ([{ layout: FlightRuntime.field(resources, 'pipelineLayout'), vertex: { module: FlightRuntime.field(resources, 'module'), entryPoint: 'vs_main' }, fragment: { module: FlightRuntime.field(resources, 'module'), entryPoint: 'fs_main', targets: cast ([{ format: format, blend: { color: { srcFactor: 'one', dstFactor: 'one-minus-src-alpha', operation: 'add' }, alpha: { srcFactor: 'one', dstFactor: 'one-minus-src-alpha', operation: 'add' } } }] : Array<Dynamic>) }, depthStencil: { format: 'depth24plus-stencil8', depthWriteEnabled: false, depthCompare: 'always', stencilFront: { compare: 'always', passOp: 'keep', failOp: 'keep', depthFailOp: 'keep' }, stencilBack: { compare: 'always', passOp: 'keep', failOp: 'keep', depthFailOp: 'keep' }, stencilReadMask: 255.0, stencilWriteMask: 0.0 }, primitive: { topology: 'triangle-list' } }] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(resources, 'pipelines'), 'set', cast ([format, pipeline] : Array<Dynamic>));
    return cast pipeline;
    return cast null;
  }

  @:keep public static function getTargets__wgpuCache(state:WgpuRenderState):Dynamic {
    var targets:Dynamic = cast FlightRuntime.UNDEFINED;
    targets = FlightRuntime.callProperty(DisplayObjectWgpu._renderCacheTargets__wgpuCache, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(targets, FlightRuntime.UNDEFINED))) {
  (targets = cast (FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []) : Dynamic));
  FlightRuntime.callProperty(DisplayObjectWgpu._renderCacheTargets__wgpuCache, 'set', cast ([state, targets] : Array<Dynamic>));
}
    return cast targets;
    return cast null;
  }

  @:keep public static function getWgpuQuadBatchPipeline(state:WgpuRenderState, resources:WgpuQuadBatchResources, module:Dynamic, hasMaterialData:Bool, blendMode:Null<BlendMode>):Dynamic {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var perModule:Dynamic = cast FlightRuntime.UNDEFINED;
    var stencilMode:Dynamic = cast FlightRuntime.UNDEFINED;
    var format:Dynamic = cast FlightRuntime.UNDEFINED;
    var key:Dynamic = cast FlightRuntime.UNDEFINED;
    var cached:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure2:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var blend:Dynamic = cast FlightRuntime.UNDEFINED;
    var isMaskWrite:Dynamic = cast FlightRuntime.UNDEFINED;
    var stencilFace:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    perModule = FlightRuntime.callProperty(FlightRuntime.field(resources, 'pipelines'), 'get', cast ([module] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(perModule, FlightRuntime.UNDEFINED))) {
  (perModule = cast (FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []) : Dynamic));
  FlightRuntime.callProperty(FlightRuntime.field(resources, 'pipelines'), 'set', cast ([module, perModule] : Array<Dynamic>));
}
    stencilMode = FlightRuntime.select(FlightRuntime.field(runtime, 'maskWriteMode'), function():Dynamic return cast 'maskwrite', function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(runtime, 'currentMaskDepth'), 0.0, '>'), function():Dynamic return cast 'masked', function():Dynamic return cast 'normal'));
    format = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'currentColorFormat'), function():Dynamic return cast FlightRuntime.field(state, 'format'));
    key = '' + Std.string(FlightRuntime.coalesce(blendMode, function():Dynamic return cast 'null')) + '-' + Std.string(stencilMode) + '-' + Std.string(format) + '';
    cached = FlightRuntime.callProperty(perModule, 'get', cast ([key] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(cached, FlightRuntime.UNDEFINED))) { return cast cached; }
    __destructure2 = state;
    device = FlightRuntime.field(__destructure2, 'device');
    blend = FlightRuntime.select(FlightRuntime.strictEquals(blendMode, Types.BlendModeValue.Add), function():Dynamic return cast DisplayObjectWgpu.ADD_BLEND__wgpuSpriteBatch, function():Dynamic return cast DisplayObjectWgpu.NORMAL_BLEND__wgpuSpriteBatch);
    isMaskWrite = FlightRuntime.strictEquals(stencilMode, 'maskwrite');
    if (FlightRuntime.truthy(isMaskWrite)) {
  (stencilFace = cast ({ compare: 'always', passOp: 'replace', failOp: 'keep', depthFailOp: 'keep' } : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(stencilMode, 'masked'))) {
  (stencilFace = cast ({ compare: 'equal', passOp: 'keep', failOp: 'keep', depthFailOp: 'keep' } : Dynamic));
} else {
  (stencilFace = cast ({ compare: 'always', passOp: 'keep', failOp: 'keep', depthFailOp: 'keep' } : Dynamic));
} }
    pipeline = FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: FlightRuntime.select(hasMaterialData, function():Dynamic return cast FlightRuntime.field(resources, 'materialPipelineLayout'), function():Dynamic return cast FlightRuntime.field(resources, 'basePipelineLayout')), vertex: { module: module, entryPoint: 'vs_main' }, fragment: { module: module, entryPoint: 'fs_main', targets: cast ([{ format: format, blend: FlightRuntime.select(isMaskWrite, function():Dynamic return cast FlightRuntime.UNDEFINED, function():Dynamic return cast blend), writeMask: FlightRuntime.select(isMaskWrite, function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUColorWrite'] : Array<Dynamic>)), 'ALL')) }] : Array<Dynamic>) }, depthStencil: { format: 'depth24plus-stencil8', depthWriteEnabled: false, depthCompare: 'always', stencilFront: stencilFace, stencilBack: stencilFace, stencilReadMask: 255.0, stencilWriteMask: FlightRuntime.select(isMaskWrite, function():Dynamic return cast 255.0, function():Dynamic return cast 0.0) }, primitive: { topology: 'triangle-list' } }] : Array<Dynamic>));
    FlightRuntime.callProperty(perModule, 'set', cast ([key, pipeline] : Array<Dynamic>));
    return cast pipeline;
    return cast null;
  }

  @:keep public static function getWgpuQuadBatchPreludeWGSL():String {
    return cast DisplayObjectWgpu.QUAD_BATCH_PRELUDE_WGSL__wgpuSpriteBatch;
    return cast null;
  }

  @:keep public static function getWgpuRenderCacheTarget(state:WgpuRenderState, cache:RenderCache):Null<WgpuRenderTarget> {
    return cast FlightRuntime.coalesce(FlightRuntime.callProperty(FlightRuntime.callValue(DisplayObjectWgpu.getTargets__wgpuCache, cast ([state] : Array<Dynamic>)), 'get', cast ([cache] : Array<Dynamic>)), function():Dynamic return cast null);
    return cast null;
  }

  @:keep public static function getWgpuRendererData<T>(data:Null<RendererData>):Null<Dynamic> {
    return cast (cast (cast data : Dynamic) : Null<Dynamic>);
    return cast null;
  }

  @:keep public static function getWgpuRenderStats(state:WgpuRenderState):WgpuRenderStats {
    return cast FlightRuntime.callValue(DisplayObjectWgpu.ensureWgpuRenderStatsMutable__wgpuRenderStats, cast ([state] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getWgpuSpriteBatchColorTransformModule__wgpuColorAdjustment(state:WgpuRenderState):Dynamic {
    var cached:Dynamic = cast FlightRuntime.UNDEFINED;
    var module:Dynamic = cast FlightRuntime.UNDEFINED;
    cached = FlightRuntime.callProperty(DisplayObjectWgpu._colorTransformModules__wgpuColorAdjustment, 'get', cast ([FlightRuntime.field(state, 'device')] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(cached, FlightRuntime.UNDEFINED))) { return cast cached; }
    module = FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createShaderModule', cast ([{ code: (FlightRuntime.callValue(getWgpuQuadBatchPreludeWGSL, cast ([] : Array<Dynamic>)) + DisplayObjectWgpu.COLOR_TRANSFORM_WGSL__wgpuColorAdjustment) }] : Array<Dynamic>));
    FlightRuntime.callProperty(DisplayObjectWgpu._colorTransformModules__wgpuColorAdjustment, 'set', cast ([FlightRuntime.field(state, 'device'), module] : Array<Dynamic>));
    return cast module;
    return cast null;
  }

  @:keep public static function getWgpuVelocityWriter(state:WgpuRenderState, kind:Kind):Null<WgpuVelocityWriter> {
    return cast FlightRuntime.coalesce(FlightRuntime.callOptionalProperty(FlightRuntime.callProperty(DisplayObjectWgpu._velocityWriters__wgpuVelocity, 'get', cast ([state] : Array<Dynamic>)), 'get', cast ([kind] : Array<Dynamic>)), function():Dynamic return cast null);
    return cast null;
  }

  public static final INSTANCE_STRIDE__wgpuParticleEmitter:Dynamic = (DisplayObjectWgpu.INSTANCE_FLOATS__wgpuParticleEmitter * 4.0);

  public static final INSTANCE_STRIDE_FLOATS__wgpuQuadBatch:Dynamic = 13.0;

  public static final INSTANCE_STRIDE_FLOATS__wgpuTilemap:Dynamic = 13.0;

  public static final NORMAL_BLEND__wgpuSpriteBatch:Dynamic = { color: { srcFactor: 'one', dstFactor: 'one-minus-src-alpha', operation: 'add' }, alpha: { srcFactor: 'one', dstFactor: 'one-minus-src-alpha', operation: 'add' } };

  @:keep public static function intersectWgpuScissorRect__wgpuClipRectangle(a:Null<WgpuScissorRect>, b:WgpuScissorRect):WgpuScissorRect {
    var x:Dynamic = cast FlightRuntime.UNDEFINED;
    var y:Dynamic = cast FlightRuntime.UNDEFINED;
    var right:Dynamic = cast FlightRuntime.UNDEFINED;
    var bottom:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(a, null))) { return cast { height: FlightRuntime.field(b, 'height'), width: FlightRuntime.field(b, 'width'), x: FlightRuntime.field(b, 'x'), y: FlightRuntime.field(b, 'y') }; }
    x = FlightRuntime.callProperty(HxMath, 'max', cast ([FlightRuntime.field(a, 'x'), FlightRuntime.field(b, 'x')] : Array<Dynamic>));
    y = FlightRuntime.callProperty(HxMath, 'max', cast ([FlightRuntime.field(a, 'y'), FlightRuntime.field(b, 'y')] : Array<Dynamic>));
    right = FlightRuntime.callProperty(HxMath, 'min', cast ([(FlightRuntime.field(a, 'x') + FlightRuntime.field(a, 'width')), (FlightRuntime.field(b, 'x') + FlightRuntime.field(b, 'width'))] : Array<Dynamic>));
    bottom = FlightRuntime.callProperty(HxMath, 'min', cast ([(FlightRuntime.field(a, 'y') + FlightRuntime.field(a, 'height')), (FlightRuntime.field(b, 'y') + FlightRuntime.field(b, 'height'))] : Array<Dynamic>));
    return cast { height: FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, (bottom - y)] : Array<Dynamic>)), width: FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, (right - x)] : Array<Dynamic>)), x: x, y: y };
    return cast null;
  }

  @:keep public static function layoutRichText__wgpuRichText(source:RichText, richTextRuntime:RichTextRuntime, text:String, formatRanges:Dynamic, state:WgpuRenderState):Dynamic {
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxTexDim:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxLogical:Dynamic = cast FlightRuntime.UNDEFINED;
    var measure:Dynamic = cast FlightRuntime.UNDEFINED;
    var result:Dynamic = cast FlightRuntime.UNDEFINED;
    data = FlightRuntime.field(source, 'data');
    maxTexDim = FlightRuntime.field(FlightRuntime.field(FlightRuntime.field(state, 'device'), 'limits'), 'maxTextureDimension2D');
    maxLogical = FlightRuntime.callProperty(HxMath, 'floor', cast ([(maxTexDim / FlightRuntime.field(state, 'pixelRatio'))] : Array<Dynamic>));
    measure = function(value:String, format:TextFormat) {
  var context:Dynamic = cast FlightRuntime.UNDEFINED;
  context = FlightRuntime.callValue(DisplayObjectWgpu.getOffscreenCanvas__wgpuRichText, cast ([1.0, 1.0] : Array<Dynamic>));
  FlightRuntime.setField(context, 'font', FlightRuntime.callValue(computeTextFormatFontString, cast ([format] : Array<Dynamic>)));
  return cast FlightRuntime.field(FlightRuntime.callProperty(context, 'measureText', cast ([value] : Array<Dynamic>)), 'width');
};
    result = FlightRuntime.callValue(getTextLayoutResult, cast ([(cast richTextRuntime : TextLabelRuntime)] : Array<Dynamic>));
    FlightRuntime.callValue(computeTextLayout, cast ([result, { formatRanges: formatRanges, height: FlightRuntime.callProperty(HxMath, 'min', cast ([FlightRuntime.field(data, 'height'), maxLogical] : Array<Dynamic>)), measure: measure, multiline: FlightRuntime.field(data, 'multiline'), text: text, verticalAlign: FlightRuntime.select(FlightRuntime.strictEquals(FlightRuntime.field(data, 'autoSize'), 'none'), function():Dynamic return cast FlightRuntime.field(data, 'verticalAlign'), function():Dynamic return cast 'top'), width: FlightRuntime.callProperty(HxMath, 'min', cast ([FlightRuntime.select(FlightRuntime.field(data, 'wordWrap'), function():Dynamic return cast FlightRuntime.field(data, 'width'), function():Dynamic return cast 10000.0), maxLogical] : Array<Dynamic>)), wordWrap: FlightRuntime.field(data, 'wordWrap') }] : Array<Dynamic>));
    return cast result;
    return cast null;
  }

  public static final PARTICLE_SHADER_SRC__wgpuParticleEmitter:Dynamic = '\nstruct Uniforms {\n  matrix : mat3x3f,\n  alpha : f32,\n  hasColorTransform : u32,\n  _pad0 : f32,\n  _pad1 : f32,\n  colorMultiplier : vec4f,\n  colorOffset : vec4f,\n  x0 : f32, y0 : f32, x1 : f32, y1 : f32,\n  u0 : f32, v0 : f32, u1 : f32, v1 : f32,\n}\n\nstruct InstanceData {\n  px : f32, py : f32,\n  cosScale : f32, sinScale : f32,\n  r : f32, g : f32, b : f32, alpha : f32,\n  u0 : f32, v0 : f32, u1 : f32, v1 : f32,\n  width : f32, height : f32,\n}\n\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n@group(2) @binding(0) var<storage, read> instances : array<InstanceData>;\n\nstruct VertexOut {\n  @builtin(position) position : vec4f,\n  @location(0) uv : vec2f,\n  @location(1) color : vec4f,\n}\n\n@vertex\nfn vs_main(\n  @builtin(vertex_index) vi : u32,\n  @builtin(instance_index) ii : u32,\n) -> VertexOut {\n  let inst = instances[ii];\n  let xi = (vi == 1u || vi == 2u || vi == 4u);\n  let yi = (vi == 2u || vi == 4u || vi == 5u);\n  let lx = select(0.0, inst.width, xi);\n  let ly = select(0.0, inst.height, yi);\n  // Rotate and translate in world space\n  let rx = inst.cosScale * lx - inst.sinScale * ly + inst.px;\n  let ry = inst.sinScale * lx + inst.cosScale * ly + inst.py;\n  let p = uni.matrix * vec3f(rx, ry, 1.0);\n  let u = select(inst.u0, inst.u1, xi);\n  let v = select(inst.v0, inst.v1, yi);\n  var out : VertexOut;\n  out.position = vec4f(p.x, p.y, 0.0, 1.0);\n  out.uv = vec2f(u, v);\n  out.color = vec4f(inst.r, inst.g, inst.b, inst.alpha);\n  return out;\n}\n\n@fragment\nfn fs_main(in : VertexOut) -> @location(0) vec4f {\n  let tex_color = textureSample(tex, smp, in.uv);\n  let out_color = vec4f(tex_color.rgb * in.color.rgb, tex_color.a) * in.color.a;\n  if (out_color.a <= 0.0) { discard; }\n  return out_color;\n}\n';

  @:keep public static function packWgpuSpriteBatchMaterialInstance(state:WgpuRenderState, materialData:Null<MaterialData>, instanceIndex:Float):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var floats:Dynamic = cast FlightRuntime.UNDEFINED;
    var renderer:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    floats = FlightRuntime.field(runtime, 'spriteBatchMaterialFloats');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(floats, 0.0))) { return; }
    renderer = FlightRuntime.field(runtime, 'spriteBatchMaterialRenderer');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(renderer, null), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(renderer, 'packInstance'), FlightRuntime.UNDEFINED)))) { return; }
    FlightRuntime.callProperty(renderer, 'packInstance', cast ([state, FlightRuntime.field(runtime, 'spriteBatchMaterial'), materialData, FlightRuntime.field(runtime, 'spriteBatchMaterialData'), (instanceIndex * floats)] : Array<Dynamic>));
  }

  public static final QUAD_BATCH_PRELUDE_WGSL__wgpuSpriteBatch:Dynamic = '\nstruct Uniforms {\n  matrix : mat3x3f,\n}\n\nstruct InstanceData {\n  a : f32, b : f32, c : f32, d : f32,\n  tx : f32, ty : f32,\n  width : f32, height : f32,\n  u0 : f32, v0 : f32, u1 : f32, v1 : f32,\n  alpha : f32,\n}\n\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n@group(2) @binding(0) var<storage, read> instances : array<InstanceData>;\n\nstruct BaseVertex {\n  position : vec4f,\n  uv : vec2f,\n  alpha : f32,\n}\n\nfn quadBaseVertex(vi : u32, ii : u32) -> BaseVertex {\n  let inst = instances[ii];\n  let xi = (vi == 1u || vi == 2u || vi == 4u);\n  let yi = (vi == 2u || vi == 4u || vi == 5u);\n  let lx = select(0.0, inst.width, xi);\n  let ly = select(0.0, inst.height, yi);\n  let wx = inst.a * lx + inst.c * ly + inst.tx;\n  let wy = inst.b * lx + inst.d * ly + inst.ty;\n  let p = uni.matrix * vec3f(wx, wy, 1.0);\n  var bv : BaseVertex;\n  bv.position = vec4f(p.x, p.y, 0.0, 1.0);\n  bv.uv = vec2f(select(inst.u0, inst.u1, xi), select(inst.v0, inst.v1, yi));\n  bv.alpha = inst.alpha;\n  return bv;\n}\n';

  @:keep public static function popOneWgpuClip__wgpuClip(state:WgpuRenderState):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var form:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    form = FlightRuntime.callProperty(FlightRuntime.field(runtime, 'clipForms'), 'pop', cast ([] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(form, 'contour'))) { FlightRuntime.callValue(popWgpuClipContours, cast ([state] : Array<Dynamic>)); } else { FlightRuntime.callValue(popWgpuClipRectangle, cast ([state] : Array<Dynamic>)); }
  }

  @:keep public static function popWgpuClipContours(state:WgpuRenderState):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    entry = FlightRuntime.callProperty(FlightRuntime.field(runtime, 'clipContourStack'), 'pop', cast ([] : Array<Dynamic>));
    FlightRuntime.setField(runtime, 'currentMaskDepth', FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, (FlightRuntime.field(runtime, 'currentMaskDepth') - 1.0)] : Array<Dynamic>)));
    pass = FlightRuntime.field(runtime, 'renderPass');
    if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(pass, null), function():Dynamic return cast !FlightRuntime.strictEquals(entry, FlightRuntime.UNDEFINED)))) {
  var pipelines:Dynamic = FlightRuntime.callValue(DisplayObjectWgpu.ensureClipContourPipelines__wgpuClipContours, cast ([state] : Array<Dynamic>));
  FlightRuntime.callProperty(pass, 'setPipeline', cast ([FlightRuntime.field(pipelines, 'erase')] : Array<Dynamic>));
  FlightRuntime.callProperty(pass, 'setBindGroup', cast ([0.0, FlightRuntime.field(entry, 'bindGroup')] : Array<Dynamic>));
  FlightRuntime.callProperty(pass, 'setVertexBuffer', cast ([0.0, FlightRuntime.field(entry, 'vertexBuffer')] : Array<Dynamic>));
  FlightRuntime.callProperty(pass, 'setStencilReference', cast ([(FlightRuntime.field(entry, 'depth') + 1.0)] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(entry, 'vertexCount'), 0.0, '>'))) { FlightRuntime.callProperty(pass, 'draw', cast ([FlightRuntime.field(entry, 'vertexCount')] : Array<Dynamic>)); }
}
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(entry, FlightRuntime.UNDEFINED))) {
  FlightRuntime.pushMany(FlightRuntime.coalesce(FlightRuntime.field(runtime, 'retiredBuffers'), function():Dynamic return cast FlightRuntime.setField(runtime, 'retiredBuffers', cast ([] : Array<Dynamic>))), cast ([FlightRuntime.field(entry, 'vertexBuffer'), FlightRuntime.field(entry, 'uniformBuffer')] : Array<Dynamic>));
}
  }

  @:keep public static function popWgpuClipRectangle(state:WgpuRenderState):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var stack:Dynamic = cast FlightRuntime.UNDEFINED;
    var previous:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    stack = FlightRuntime.field(runtime, 'scissorStack');
    FlightRuntime.callProperty(stack, 'pop', cast ([] : Array<Dynamic>));
    previous = FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(stack, 'length'), 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(stack, (FlightRuntime.field(stack, 'length') - 1.0)), function():Dynamic return cast null);
    FlightRuntime.setField(runtime, 'currentScissorRect', previous);
    pass = FlightRuntime.field(runtime, 'renderPass');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(pass, null))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(previous, null))) {
  var viewport:Dynamic = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'renderTargetViewport'), function():Dynamic return cast FlightRuntime.field(state, 'canvas'));
  FlightRuntime.callProperty(pass, 'setScissorRect', cast ([0.0, 0.0, FlightRuntime.field(viewport, 'width'), FlightRuntime.field(viewport, 'height')] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(FlightRuntime.field(previous, 'width'), 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(previous, 'height'), 0.0, '<=')))) {
  FlightRuntime.callProperty(pass, 'setScissorRect', cast ([0.0, 0.0, 1.0, 1.0] : Array<Dynamic>));
} else {
  FlightRuntime.callProperty(pass, 'setScissorRect', cast ([FlightRuntime.field(previous, 'x'), FlightRuntime.field(previous, 'y'), FlightRuntime.field(previous, 'width'), FlightRuntime.field(previous, 'height')] : Array<Dynamic>));
} }
  }

  @:keep public static function prepareWgpuSpriteBatchWrite(state:WgpuRenderState, texture:ImageResource, blendMode:Null<BlendMode>, material:Null<Material>, materialRenderer:WgpuMaterialRenderer, maxInstances:Float):Float {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var floats:Dynamic = cast FlightRuntime.UNDEFINED;
    var needed:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(!FlightRuntime.strictEquals(texture, FlightRuntime.field(runtime, 'spriteBatchTexture')), function():Dynamic return cast !FlightRuntime.strictEquals(blendMode, FlightRuntime.field(runtime, 'spriteBatchBlendMode'))), function():Dynamic return cast !FlightRuntime.strictEquals(material, FlightRuntime.field(runtime, 'spriteBatchMaterial'))))) {
  FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
}
    FlightRuntime.setField(runtime, 'spriteBatchTexture', texture);
    FlightRuntime.setField(runtime, 'spriteBatchBlendMode', blendMode);
    FlightRuntime.setField(runtime, 'spriteBatchMaterial', material);
    FlightRuntime.setField(runtime, 'spriteBatchMaterialRenderer', materialRenderer);
    floats = FlightRuntime.field(materialRenderer, 'instanceFloatCount');
    FlightRuntime.setField(runtime, 'spriteBatchMaterialFloats', floats);
    needed = ((FlightRuntime.field(runtime, 'spriteBatchCount') + maxInstances) * SPRITE_INSTANCE_FLOATS);
    if (FlightRuntime.truthy(FlightRuntime.compare(needed, FlightRuntime.field(FlightRuntime.field(runtime, 'spriteBatchInstanceData'), 'length'), '>'))) {
  var newSize:Dynamic = HxMath.max(HxMath.max(needed, (FlightRuntime.field(FlightRuntime.field(runtime, 'spriteBatchInstanceData'), 'length') * 2.0)), (SPRITE_INSTANCE_FLOATS * 256.0));
  FlightRuntime.setField(runtime, 'spriteBatchInstanceData', FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [newSize]));
}
    if (FlightRuntime.truthy(FlightRuntime.compare(floats, 0.0, '>'))) {
  var materialNeeded:Dynamic = ((FlightRuntime.field(runtime, 'spriteBatchCount') + maxInstances) * floats);
  if (FlightRuntime.truthy(FlightRuntime.compare(materialNeeded, FlightRuntime.field(FlightRuntime.field(runtime, 'spriteBatchMaterialData'), 'length'), '>'))) {
  var newSize:Dynamic = HxMath.max(HxMath.max(materialNeeded, (FlightRuntime.field(FlightRuntime.field(runtime, 'spriteBatchMaterialData'), 'length') * 2.0)), (floats * 256.0));
  FlightRuntime.setField(runtime, 'spriteBatchMaterialData', FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [newSize]));
}
}
    return cast (FlightRuntime.field(runtime, 'spriteBatchCount') * SPRITE_INSTANCE_FLOATS);
    return cast null;
  }

  @:keep public static function promoteWgpuSpriteBatchColorTransformToPerInstance__wgpuColorAdjustment(runtime:WgpuRenderStateRuntime, instanceCount:Float, fill:Null<ColorTransform>):Void {
    FlightRuntime.setField(runtime, 'spriteBatchColorTransformMode', DisplayObjectWgpu.CT_MODE_PER_INSTANCE__wgpuColorAdjustment);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, instanceCount, '<'))) {
        FlightRuntime.callValue(DisplayObjectWgpu.writeWgpuColorTransformInstance__wgpuColorAdjustment, cast ([runtime, fill, i] : Array<Dynamic>));
        i++;
      }
    }
  }

  @:keep public static function pushWgpuClipContours(state:WgpuRenderState, contours:Array<Array<Float>>, winding:PathWinding, worldTransform:Matrix):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var depth:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipelines:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var vertexBuffer:Dynamic = cast FlightRuntime.UNDEFINED;
    var vertexCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformBuffer:Dynamic = cast FlightRuntime.UNDEFINED;
    var bindGroup:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    FlightRuntime.voidValue(winding);
    device = FlightRuntime.field(state, 'device');
    depth = FlightRuntime.field(runtime, 'currentMaskDepth');
    pipelines = FlightRuntime.callValue(DisplayObjectWgpu.ensureClipContourPipelines__wgpuClipContours, cast ([state] : Array<Dynamic>));
    __destructure0 = FlightRuntime.callValue(DisplayObjectWgpu.createClipContourVertexBuffer__wgpuClipContours, cast ([state, contours] : Array<Dynamic>));
    vertexBuffer = FlightRuntime.field(__destructure0, 'vertexBuffer');
    vertexCount = FlightRuntime.field(__destructure0, 'vertexCount');
    uniformBuffer = FlightRuntime.callValue(DisplayObjectWgpu.createClipContourUniformBuffer__wgpuClipContours, cast ([state, worldTransform] : Array<Dynamic>));
    bindGroup = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(pipelines, 'bindGroupLayout'), entries: cast ([{ binding: 0.0, resource: { buffer: uniformBuffer } }] : Array<Dynamic>) }] : Array<Dynamic>));
    pass = FlightRuntime.field(runtime, 'renderPass');
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(pass, null))) {
  FlightRuntime.callProperty(pass, 'setPipeline', cast ([FlightRuntime.field(pipelines, 'write')] : Array<Dynamic>));
  FlightRuntime.callProperty(pass, 'setBindGroup', cast ([0.0, bindGroup] : Array<Dynamic>));
  FlightRuntime.callProperty(pass, 'setVertexBuffer', cast ([0.0, vertexBuffer] : Array<Dynamic>));
  FlightRuntime.callProperty(pass, 'setStencilReference', cast ([depth] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.compare(vertexCount, 0.0, '>'))) { FlightRuntime.callProperty(pass, 'draw', cast ([vertexCount] : Array<Dynamic>)); }
}
    FlightRuntime.callProperty(FlightRuntime.field(runtime, 'clipContourStack'), 'push', cast ([{ vertexBuffer: vertexBuffer, vertexCount: vertexCount, uniformBuffer: uniformBuffer, bindGroup: bindGroup, depth: depth }] : Array<Dynamic>));
    FlightRuntime.setField(runtime, 'currentMaskDepth', (depth + 1.0));
  }

  @:keep public static function pushWgpuClipRectangle(state:WgpuRenderState, rect:RectangleLike, transform:MatrixLike):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var next:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    next = FlightRuntime.callValue(DisplayObjectWgpu.intersectWgpuScissorRect__wgpuClipRectangle, cast ([FlightRuntime.coalesce(FlightRuntime.field(runtime, 'currentScissorRect'), function():Dynamic return cast null), FlightRuntime.callValue(DisplayObjectWgpu.computeWgpuScissorRect__wgpuClipRectangle, cast ([state, rect, transform] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.setField(runtime, 'currentScissorRect', next);
    FlightRuntime.callProperty(FlightRuntime.field(runtime, 'scissorStack'), 'push', cast ([next] : Array<Dynamic>));
    pass = FlightRuntime.field(runtime, 'renderPass');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(pass, null))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(FlightRuntime.field(next, 'width'), 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(next, 'height'), 0.0, '<=')))) {
  FlightRuntime.callProperty(pass, 'setScissorRect', cast ([0.0, 0.0, 1.0, 1.0] : Array<Dynamic>));
} else {
  FlightRuntime.callProperty(pass, 'setScissorRect', cast ([FlightRuntime.field(next, 'x'), FlightRuntime.field(next, 'y'), FlightRuntime.field(next, 'width'), FlightRuntime.field(next, 'height')] : Array<Dynamic>));
}
  }

  public static final selectionRectangles__wgpuTextInput:Array<TextSelectionRectangle> = cast ([] : Array<Dynamic>);

  @:keep public static function recordWgpuBatchFlush(state:WgpuRenderState, instances:Float):Void {
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    entry = FlightRuntime.callProperty(DisplayObjectWgpu._stats__wgpuRenderStats, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(entry, FlightRuntime.UNDEFINED))) { return; }
    FlightRuntime.incrementField(entry, 'drawCallCount', 1, true);
    FlightRuntime.setField(entry, 'instanceCount', (FlightRuntime.field(entry, 'instanceCount') + instances));
    FlightRuntime.incrementField(entry, 'batchFlushCount', 1, true);
  }

  @:keep public static function recordWgpuColorAdjustment__wgpuColorAdjustment(runtime:WgpuRenderStateRuntime, colorTransform:Null<ColorTransform>, instanceIndex:Float):Void {
    var mode:Dynamic = cast FlightRuntime.UNDEFINED;
    var tint:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'spriteBatchColorTransformData'), FlightRuntime.UNDEFINED))) {
  FlightRuntime.setField(runtime, 'spriteBatchColorTransformData', FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [(DisplayObjectWgpu.COLOR_TRANSFORM_FLOATS__wgpuColorAdjustment * 256.0)]));
}
    mode = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'spriteBatchColorTransformMode'), function():Dynamic return cast DisplayObjectWgpu.CT_MODE_NONE__wgpuColorAdjustment);
    tint = FlightRuntime.coalesce(colorTransform, function():Dynamic return cast null);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(mode, DisplayObjectWgpu.CT_MODE_NONE__wgpuColorAdjustment))) {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(tint, null))) { return; }
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(instanceIndex, 0.0))) {
  FlightRuntime.setField(runtime, 'spriteBatchColorTransformMode', DisplayObjectWgpu.CT_MODE_UNIFORM__wgpuColorAdjustment);
  FlightRuntime.setField(runtime, 'spriteBatchUniformColorTransform', tint);
  return;
}
  FlightRuntime.callValue(DisplayObjectWgpu.promoteWgpuSpriteBatchColorTransformToPerInstance__wgpuColorAdjustment, cast ([runtime, instanceIndex, null] : Array<Dynamic>));
  FlightRuntime.callValue(DisplayObjectWgpu.writeWgpuColorTransformInstance__wgpuColorAdjustment, cast ([runtime, tint, instanceIndex] : Array<Dynamic>));
  return;
}
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(mode, DisplayObjectWgpu.CT_MODE_UNIFORM__wgpuColorAdjustment))) {
  var uniform:Dynamic = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'spriteBatchUniformColorTransform'), function():Dynamic return cast null);
  if (FlightRuntime.truthy(FlightRuntime.callValue(DisplayObjectWgpu.equalsRecordedColorTransform__wgpuColorAdjustment, cast ([tint, uniform] : Array<Dynamic>)))) { return; }
  FlightRuntime.callValue(DisplayObjectWgpu.promoteWgpuSpriteBatchColorTransformToPerInstance__wgpuColorAdjustment, cast ([runtime, instanceIndex, uniform] : Array<Dynamic>));
  FlightRuntime.callValue(DisplayObjectWgpu.writeWgpuColorTransformInstance__wgpuColorAdjustment, cast ([runtime, tint, instanceIndex] : Array<Dynamic>));
  return;
}
    FlightRuntime.callValue(DisplayObjectWgpu.writeWgpuColorTransformInstance__wgpuColorAdjustment, cast ([runtime, tint, instanceIndex] : Array<Dynamic>));
  }

  @:keep public static function recordWgpuSpriteBatchColorTransform(state:WgpuRenderState, colorTransform:Null<ColorTransform>, instanceIndex:Float):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var fold:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    fold = FlightRuntime.field(runtime, 'wgpuColorAdjustmentFold');
    if (FlightRuntime.truthy(!FlightRuntime.looseEquals(fold, null))) {
  FlightRuntime.callProperty(fold, 'record', cast ([runtime, colorTransform, instanceIndex] : Array<Dynamic>));
  return;
}
    if (FlightRuntime.truthy(!FlightRuntime.looseEquals(colorTransform, null))) { FlightRuntime.callOptionalProperty(runtime, 'wgpuColorAdjustmentGuard', cast ([state, colorTransform] : Array<Dynamic>)); }
  }

  @:keep public static function recordWgpuTextureUpload(state:WgpuRenderState):Void {
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    entry = FlightRuntime.callProperty(DisplayObjectWgpu._stats__wgpuRenderStats, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(entry, FlightRuntime.UNDEFINED))) { return; }
    FlightRuntime.incrementField(entry, 'textureUploadCount', 1, true);
  }

  @:keep public static function refreshWgpuRenderCache(cacheState:WgpuRenderState, cache:RenderCache, source:DisplayObject, ?options:RenderCacheRefreshOptions):Bool {
    var screenState:Dynamic = cast FlightRuntime.UNDEFINED;
    var cacheRuntime:Dynamic = cast FlightRuntime.UNDEFINED;
    var screenRuntime:Dynamic = cast FlightRuntime.UNDEFINED;
    var padding:Dynamic = cast FlightRuntime.UNDEFINED;
    var minWidth:Dynamic = cast FlightRuntime.UNDEFINED;
    var minHeight:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var width:Dynamic = cast FlightRuntime.UNDEFINED;
    var height:Dynamic = cast FlightRuntime.UNDEFINED;
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var resized:Dynamic = cast FlightRuntime.UNDEFINED;
    var target:Dynamic = cast FlightRuntime.UNDEFINED;
    var dirty:Dynamic = cast FlightRuntime.UNDEFINED;
    screenState = FlightRuntime.coalesce(FlightRuntime.callProperty(DisplayObjectWgpu._cacheStateScreen__wgpuCache, 'get', cast ([cacheState] : Array<Dynamic>)), function():Dynamic return cast cacheState);
    cacheRuntime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([cacheState] : Array<Dynamic>));
    screenRuntime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([screenState] : Array<Dynamic>));
    FlightRuntime.setField(cacheRuntime, 'commandEncoder', FlightRuntime.field(screenRuntime, 'commandEncoder'));
    FlightRuntime.setField(cacheRuntime, 'renderPass', FlightRuntime.field(screenRuntime, 'renderPass'));
    FlightRuntime.setField(cacheRuntime, 'canvasTextureView', FlightRuntime.field(screenRuntime, 'canvasTextureView'));
    FlightRuntime.setField(cacheRuntime, 'canvasViewCleared', FlightRuntime.field(screenRuntime, 'canvasViewCleared'));
    FlightRuntime.setField(cacheRuntime, 'depthStencilTexture', FlightRuntime.field(screenRuntime, 'depthStencilTexture'));
    FlightRuntime.setField(cacheRuntime, 'depthStencilView', FlightRuntime.field(screenRuntime, 'depthStencilView'));
    FlightRuntime.setField(cacheRuntime, 'depthStencilWidth', FlightRuntime.field(screenRuntime, 'depthStencilWidth'));
    FlightRuntime.setField(cacheRuntime, 'depthStencilHeight', FlightRuntime.field(screenRuntime, 'depthStencilHeight'));
    FlightRuntime.setField(cacheRuntime, 'uniformOffset', FlightRuntime.field(screenRuntime, 'uniformOffset'));
    padding = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'padding'), function():Dynamic return cast 0.0);
    minWidth = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'minWidth'), function():Dynamic return cast 1.0);
    minHeight = FlightRuntime.coalesce(FlightRuntime.optionalField(options, 'minHeight'), function():Dynamic return cast 1.0);
    FlightRuntime.callValue(computeNodeBoundsRectangle, cast ([DisplayObjectWgpu._bounds__wgpuCache, source, source] : Array<Dynamic>));
    __destructure0 = FlightRuntime.callValue(computeRenderTargetSize, cast ([DisplayObjectWgpu._bounds__wgpuCache, padding, minWidth, minHeight] : Array<Dynamic>));
    width = FlightRuntime.field(__destructure0, 'width');
    height = FlightRuntime.field(__destructure0, 'height');
    existing = FlightRuntime.callValue(getWgpuRenderCacheTarget, cast ([screenState, cache] : Array<Dynamic>));
    resized = FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(existing, null), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(existing, 'width'), width)), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(existing, 'height'), height));
    target = FlightRuntime.callValue(ensureWgpuRenderCacheTarget, cast ([screenState, cache, width, height] : Array<Dynamic>));
    FlightRuntime.callValue(computeDisplayObjectRenderTargetTransform, cast ([DisplayObjectWgpu._renderTransform__wgpuCache, source, DisplayObjectWgpu._bounds__wgpuCache, padding, padding] : Array<Dynamic>));
    FlightRuntime.callValue(computeRenderCacheTransform, cast ([FlightRuntime.field(cache, 'transform'), DisplayObjectWgpu._bounds__wgpuCache, padding, padding] : Array<Dynamic>));
    FlightRuntime.setField(DisplayObjectWgpu._yInvert__wgpuCache, 'd', -1.0);
    FlightRuntime.setField(DisplayObjectWgpu._yInvert__wgpuCache, 'ty', FlightRuntime.field(target, 'height'));
    FlightRuntime.callValue(multiplyMatrix, cast ([DisplayObjectWgpu._bakeTransform__wgpuCache, DisplayObjectWgpu._yInvert__wgpuCache, DisplayObjectWgpu._renderTransform__wgpuCache] : Array<Dynamic>));
    FlightRuntime.setField(cacheRuntime, 'spriteBatchBufferCursor', 0.0);
    FlightRuntime.callValue(beginWgpuRenderPass, cast ([cacheState, target] : Array<Dynamic>));
    FlightRuntime.callValue(setWgpuRenderTransform2D, cast ([cacheState, DisplayObjectWgpu._bakeTransform__wgpuCache] : Array<Dynamic>));
    dirty = FlightRuntime.callValue(prepareDisplayObjectRender, cast ([cacheState, source] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(dirty, function():Dynamic return cast resized))) {
  FlightRuntime.callValue(renderWgpuDisplayObject, cast ([cacheState, source] : Array<Dynamic>));
}
    FlightRuntime.callValue(endWgpuRenderPass, cast ([cacheState] : Array<Dynamic>));
    FlightRuntime.setField(screenRuntime, 'commandEncoder', FlightRuntime.field(cacheRuntime, 'commandEncoder'));
    FlightRuntime.setField(screenRuntime, 'renderPass', FlightRuntime.field(cacheRuntime, 'renderPass'));
    FlightRuntime.setField(screenRuntime, 'canvasTextureView', FlightRuntime.field(cacheRuntime, 'canvasTextureView'));
    FlightRuntime.setField(screenRuntime, 'canvasViewCleared', FlightRuntime.field(cacheRuntime, 'canvasViewCleared'));
    FlightRuntime.setField(screenRuntime, 'currentBlendMode', null);
    FlightRuntime.setField(screenRuntime, 'uniformOffset', FlightRuntime.field(cacheRuntime, 'uniformOffset'));
    return cast FlightRuntime.orValue(dirty, function():Dynamic return cast resized);
    return cast null;
  }

  @:keep public static function registerDefaultWgpuMaterial(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerWgpuMaterialRenderer, cast ([state, Types.DefaultMaterialKind, defaultWgpuMaterialRenderer] : Array<Dynamic>));
  }

  @:keep public static function registerWgpuDisplayObjectRenderers(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerDefaultWgpuMaterial, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.BitmapKind, defaultWgpuBitmapRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.DisplayObjectKind, defaultWgpuDisplayObjectRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.ParticleEmitterKind, defaultWgpuParticleEmitterRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.QuadBatchKind, defaultWgpuQuadBatchRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.RichTextKind, defaultWgpuRichTextRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.Scale9ShapeKind, defaultWgpuScale9ShapeRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.ShapeKind, defaultWgpuShapeRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.SpriteKind, defaultWgpuSpriteRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.TextLabelKind, defaultWgpuTextLabelRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.TilemapKind, defaultWgpuTilemapRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.RenderCacheKindValue, defaultWgpuRenderCacheRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.VideoKind, defaultWgpuVideoRenderer] : Array<Dynamic>));
  }

  @:keep public static function registerWgpuShapeCommands(commands:Array<Dynamic>):Void {
    FlightRuntime.callValue(facadeDisplayObjectWgpuDisplayObjectCanvasRegisterCanvasShapeCommands, cast ([commands] : Array<Dynamic>));
  }

  @:keep public static function registerWgpuSpriteRenderers(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerDefaultWgpuMaterial, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.ParticleEmitterKind, defaultWgpuParticleEmitterRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.QuadBatchKind, defaultWgpuQuadBatchRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.SpriteKind, defaultWgpuSpriteRenderer] : Array<Dynamic>));
    FlightRuntime.callValue(registerRenderer, cast ([state, Types.TilemapKind, defaultWgpuTilemapRenderer] : Array<Dynamic>));
  }

  @:keep public static function registerWgpuTextInputOverlay(overlay:WgpuRichTextOverlay):Void {
    (DisplayObjectWgpu._webgpuTextInputOverlay__wgpuRichText = cast (overlay : Dynamic));
  }

  @:keep public static function registerWgpuVelocityWriter(state:WgpuRenderState, kind:Kind, writer:WgpuVelocityWriter):Void {
    var writers:Dynamic = cast FlightRuntime.UNDEFINED;
    writers = FlightRuntime.callProperty(DisplayObjectWgpu._velocityWriters__wgpuVelocity, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(writers, FlightRuntime.UNDEFINED))) {
  (writers = cast (FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []) : Dynamic));
  FlightRuntime.callProperty(DisplayObjectWgpu._velocityWriters__wgpuVelocity, 'set', cast ([state, writers] : Array<Dynamic>));
}
    FlightRuntime.callProperty(writers, 'set', cast ([kind, writer] : Array<Dynamic>));
  }

  @:keep public static function releaseWgpuRenderCache(state:WgpuRenderState, cache:RenderCache):Void {
    var targets:Dynamic = cast FlightRuntime.UNDEFINED;
    var target:Dynamic = cast FlightRuntime.UNDEFINED;
    targets = FlightRuntime.callValue(DisplayObjectWgpu.getTargets__wgpuCache, cast ([state] : Array<Dynamic>));
    target = FlightRuntime.callProperty(targets, 'get', cast ([cache] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(target, FlightRuntime.UNDEFINED))) { return; }
    FlightRuntime.callValue(destroyWgpuRenderTarget, cast ([state, target] : Array<Dynamic>));
    FlightRuntime.callProperty(targets, 'delete', cast ([cache] : Array<Dynamic>));
  }

  @:keep public static function remapWgpuScale9Commands(out:Array<Dynamic>, source:Array<Dynamic>, mapper:Dynamic):Void {
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.callValue(mapCanvasScale9ShapeCommands, cast ([out, source, mapper] : Array<Dynamic>));
    i = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(out, 'length'), '<'))) {
  var key:Dynamic = (cast FlightRuntime.getIndex(out, i) : String);
  var argCount:Dynamic = (cast FlightRuntime.getIndex(out, (i + 1.0)) : Float);
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(key, 'drawPath'))) {
  var pathData:Dynamic = (cast FlightRuntime.getIndex(out, (i + 3.0)) : Array<Float>);
  FlightRuntime.setLength(DisplayObjectWgpu._remappedPathData__wgpuScale9Shape, FlightRuntime.field(pathData, 'length'));
  {
    var k:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(k, FlightRuntime.field(pathData, 'length'), '<'))) {
      FlightRuntime.setIndex(DisplayObjectWgpu._remappedPathData__wgpuScale9Shape, k, FlightRuntime.getIndex(pathData, k));
      k++;
    }
  }
  FlightRuntime.setIndex(out, (i + 3.0), DisplayObjectWgpu._remappedPathData__wgpuScale9Shape);
}
  (i = cast ((i + (argCount + 2.0)) : Dynamic));
}
  }

  @:keep public static function renderWgpuDisplayObject(state:WgpuRenderState, source:DisplayObject):Void {
    var tempStack:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipHooks:Dynamic = cast FlightRuntime.UNDEFINED;
    var stackLength:Dynamic = cast FlightRuntime.UNDEFINED;
    tempStack = FlightRuntime.field(FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>)), 'tempStack');
    clipHooks = FlightRuntime.field(state, 'displayObjectClipHooks');
    stackLength = 1.0;
    FlightRuntime.setIndex(tempStack, 0.0, source);
    while (FlightRuntime.truthy(FlightRuntime.compare(stackLength, 0.0, '>'))) {
  var current:Dynamic = (cast FlightRuntime.getIndex(tempStack, --stackLength) : DisplayObject);
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.field(current, 'enabled')))) { continue; }
  var data:Dynamic = FlightRuntime.callValue(getRenderProxy2D, cast ([state, current] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(data, FlightRuntime.UNDEFINED))) { continue; }
  FlightRuntime.callOptionalProperty(clipHooks, 'popClip', cast ([state, data, current] : Array<Dynamic>));
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(isRenderProxyVisible, cast ([data] : Array<Dynamic>))))) { continue; }
  FlightRuntime.callOptionalProperty(clipHooks, 'pushClip', cast ([state, data, current] : Array<Dynamic>));
  FlightRuntime.callOptionalProperty(FlightRuntime.field(data, 'renderer'), 'submit', cast ([state, data] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.field(data, 'traverseChildren'))) {
  var children:Dynamic = FlightRuntime.field(FlightRuntime.callValue(getDisplayObjectRuntime, cast ([current] : Array<Dynamic>)), 'children');
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(children, null))) {
  {
    var i:Dynamic = (FlightRuntime.field(children, 'length') - 1.0);
    while (FlightRuntime.truthy(FlightRuntime.compare(i, 0.0, '>='))) {
      FlightRuntime.setIndex(tempStack, stackLength++, (cast FlightRuntime.getIndex(children, i) : DisplayObject));
      i--;
    }
  }
}
}
}
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
    FlightRuntime.callOptionalProperty(clipHooks, 'finalize', cast ([state] : Array<Dynamic>));
  }

  @:keep public static function renderWgpuSprite(state:WgpuRenderState, source:DisplayObject):Void {
    var tempStack:Dynamic = cast FlightRuntime.UNDEFINED;
    var stackLength:Dynamic = cast FlightRuntime.UNDEFINED;
    tempStack = FlightRuntime.field(FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>)), 'tempStack');
    stackLength = 1.0;
    FlightRuntime.setIndex(tempStack, 0.0, source);
    while (FlightRuntime.truthy(FlightRuntime.compare(stackLength, 0.0, '>'))) {
  var current:Dynamic = (cast FlightRuntime.getIndex(tempStack, --stackLength) : DisplayObject);
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.field(current, 'enabled')))) { continue; }
  var data:Dynamic = FlightRuntime.callValue(getRenderProxy2D, cast ([state, current] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(data, FlightRuntime.UNDEFINED), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.callValue(isRenderProxyVisible, cast ([data] : Array<Dynamic>)))))) { continue; }
  FlightRuntime.callOptionalProperty(FlightRuntime.field(data, 'renderer'), 'submit', cast ([state, data] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.field(data, 'traverseChildren'))) {
  var children:Dynamic = FlightRuntime.field(FlightRuntime.callValue(getDisplayObjectRuntime, cast ([current] : Array<Dynamic>)), 'children');
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(children, null))) {
  {
    var i:Dynamic = (FlightRuntime.field(children, 'length') - 1.0);
    while (FlightRuntime.truthy(FlightRuntime.compare(i, 0.0, '>='))) {
      FlightRuntime.setIndex(tempStack, stackLength++, (cast FlightRuntime.getIndex(children, i) : DisplayObject));
      i--;
    }
  }
}
}
}
    FlightRuntime.callValue(flushWgpuSpriteBatch, cast ([state] : Array<Dynamic>));
  }

  @:keep public static function renderWgpuVelocity<Traits>(state:WgpuRenderState, root:Transform2DNode<Traits>, field:VelocityField, target:WgpuRenderTarget):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    var ctx:WgpuVelocityContext = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'commandEncoder'), null))) {
  throw FlightRuntime.error('No active command encoder — call renderWgpuBackground before renderWgpuVelocity.');
}
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) {
  FlightRuntime.callProperty(FlightRuntime.field(runtime, 'renderPass'), 'end', cast ([] : Array<Dynamic>));
  FlightRuntime.setField(runtime, 'renderPass', null);
}
    pipeline = FlightRuntime.callValue(DisplayObjectWgpu.ensureWgpuVelocityPipeline__wgpuVelocity, cast ([state] : Array<Dynamic>));
    FlightRuntime.setField(pipeline, 'cursor', 0.0);
    pass = FlightRuntime.callProperty(FlightRuntime.field(runtime, 'commandEncoder'), 'beginRenderPass', cast ([{ colorAttachments: cast ([{ view: FlightRuntime.field(target, 'view'), loadOp: 'clear', storeOp: 'store', clearValue: { r: 0.0, g: 0.0, b: 0.0, a: 0.0 } }] : Array<Dynamic>) }] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setViewport', cast ([0.0, 0.0, FlightRuntime.field(target, 'width'), FlightRuntime.field(target, 'height'), 0.0, 1.0] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setPipeline', cast ([FlightRuntime.field(pipeline, 'pipeline')] : Array<Dynamic>));
    FlightRuntime.callProperty(DisplayObjectWgpu._activeVelocityPasses__wgpuVelocity, 'set', cast ([state, { pass: pass, pipeline: pipeline }] : Array<Dynamic>));
    ctx = { state: state, field: field, width: FlightRuntime.field(target, 'width'), height: FlightRuntime.field(target, 'height'), pixelRatio: FlightRuntime.field(state, 'pixelRatio') };
    FlightRuntime.callValue(DisplayObjectWgpu.visitWgpuVelocity__wgpuVelocity, cast ([ctx, root] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'end', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(DisplayObjectWgpu._activeVelocityPasses__wgpuVelocity, 'delete', cast ([state] : Array<Dynamic>));
  }

  @:keep public static function resetWgpuRenderStats(state:WgpuRenderState):Void {
    var entry:Dynamic = cast FlightRuntime.UNDEFINED;
    entry = FlightRuntime.callValue(DisplayObjectWgpu.ensureWgpuRenderStatsMutable__wgpuRenderStats, cast ([state] : Array<Dynamic>));
    FlightRuntime.setField(entry, 'batchFlushCount', 0.0);
    FlightRuntime.setField(entry, 'drawCallCount', 0.0);
    FlightRuntime.setField(entry, 'instanceCount', 0.0);
    FlightRuntime.setField(entry, 'textureUploadCount', 0.0);
  }

  @:keep public static function resetWgpuSpriteBatch__wgpuSpriteBatch(state:WgpuRenderState):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    FlightRuntime.setField(runtime, 'spriteBatchCount', 0.0);
    FlightRuntime.setField(runtime, 'spriteBatchTexture', null);
    FlightRuntime.setField(runtime, 'spriteBatchBlendMode', null);
    FlightRuntime.setField(runtime, 'spriteBatchMaterial', null);
    FlightRuntime.setField(runtime, 'spriteBatchMaterialRenderer', null);
    FlightRuntime.setField(runtime, 'spriteBatchMaterialFloats', 0.0);
  }

  @:keep public static function resetWgpuSpriteBatchBufferPool(state:WgpuRenderState):Void {
    FlightRuntime.setField(FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>)), 'spriteBatchBufferCursor', 0.0);
  }

  @:keep public static function resolveWgpuColorAdjustmentFlush__wgpuColorAdjustment(state:WgpuRenderState, count:Float):Null<WgpuColorAdjustmentFlush> {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var ctMode:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    ctMode = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'spriteBatchColorTransformMode'), function():Dynamic return cast DisplayObjectWgpu.CT_MODE_NONE__wgpuColorAdjustment);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(ctMode, DisplayObjectWgpu.CT_MODE_NONE__wgpuColorAdjustment))) { return cast null; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(ctMode, DisplayObjectWgpu.CT_MODE_UNIFORM__wgpuColorAdjustment))) {
  FlightRuntime.callValue(DisplayObjectWgpu.fillWgpuSpriteBatchUniformColorTransform__wgpuColorAdjustment, cast ([runtime, FlightRuntime.field(runtime, 'spriteBatchUniformColorTransform'), count] : Array<Dynamic>));
}
    FlightRuntime.setField(runtime, 'spriteBatchColorTransformMode', DisplayObjectWgpu.CT_MODE_NONE__wgpuColorAdjustment);
    FlightRuntime.setField(runtime, 'spriteBatchUniformColorTransform', null);
    return cast { data: FlightRuntime.field(runtime, 'spriteBatchColorTransformData'), floats: DisplayObjectWgpu.COLOR_TRANSFORM_FLOATS__wgpuColorAdjustment, module: FlightRuntime.callValue(DisplayObjectWgpu.getWgpuSpriteBatchColorTransformModule__wgpuColorAdjustment, cast ([state] : Array<Dynamic>)) };
    return cast null;
  }

  public static final SHAPE_MESH_UNIFORM_BYTES__wgpuShapeMesh:Dynamic = 64.0;

  public static final SHAPE_MESH_UNIFORM_FLOATS__wgpuShapeMesh:Dynamic = (DisplayObjectWgpu.SHAPE_MESH_UNIFORM_BYTES__wgpuShapeMesh / 4.0);

  public static final _shapeMeshUniformScratch__wgpuShapeMesh:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [DisplayObjectWgpu.SHAPE_MESH_UNIFORM_FLOATS__wgpuShapeMesh]);

  public static final SHAPE_MESH_WGSL__wgpuShapeMesh:Dynamic = '\nstruct ShapeMeshUniforms { matrix : mat3x3f, color : vec4f }\n@group(0) @binding(0) var<uniform> u : ShapeMeshUniforms;\n@vertex fn vs_main(@location(0) position : vec2f) -> @builtin(position) vec4f {\n  let p = u.matrix * vec3f(position, 1.0);\n  return vec4f(p.x, p.y, 0.0, 1.0);\n}\n@fragment fn fs_main() -> @location(0) vec4f { return u.color; }\n';

  @:keep public static function shapeMeshMatrix__wgpuShapeMesh(state:WgpuRenderState, renderProxy:RenderProxy2D):flight.internal.FlightFloat32Array {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var viewport:Dynamic = cast FlightRuntime.UNDEFINED;
    var iw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ih:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var m:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    viewport = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'renderTargetViewport'), function():Dynamic return cast FlightRuntime.field(state, 'canvas'));
    iw = (2.0 / FlightRuntime.orValue(FlightRuntime.field(viewport, 'width'), function():Dynamic return cast 1.0));
    ih = (2.0 / FlightRuntime.orValue(FlightRuntime.field(viewport, 'height'), function():Dynamic return cast 1.0));
    t = FlightRuntime.field(renderProxy, 'transform2D');
    m = DisplayObjectWgpu._shapeMeshUniformScratch__wgpuShapeMesh;
    FlightRuntime.setIndex(m, 0.0, (FlightRuntime.field(t, 'a') * iw));
    FlightRuntime.setIndex(m, 1.0, (-FlightRuntime.field(t, 'b') * ih));
    FlightRuntime.setIndex(m, 2.0, 0.0);
    FlightRuntime.setIndex(m, 3.0, 0.0);
    FlightRuntime.setIndex(m, 4.0, (FlightRuntime.field(t, 'c') * iw));
    FlightRuntime.setIndex(m, 5.0, (-FlightRuntime.field(t, 'd') * ih));
    FlightRuntime.setIndex(m, 6.0, 0.0);
    FlightRuntime.setIndex(m, 7.0, 0.0);
    FlightRuntime.setIndex(m, 8.0, ((FlightRuntime.field(t, 'tx') * iw) - 1.0));
    FlightRuntime.setIndex(m, 9.0, ((-FlightRuntime.field(t, 'ty') * ih) + 1.0));
    FlightRuntime.setIndex(m, 10.0, 1.0);
    FlightRuntime.setIndex(m, 11.0, 0.0);
    return cast m;
    return cast null;
  }

  public static final SPRITE_INSTANCE_FLOATS:Dynamic = 13.0;

  public static final SPRITE_INSTANCE_STRIDE__wgpuSpriteBatch:Dynamic = (SPRITE_INSTANCE_FLOATS * 4.0);

  @:keep public static function submitWgpuQuadBatch__wgpuQuadBatch(state:WgpuRenderState, quadBatch:RenderProxy2D):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var atlas:Dynamic = cast FlightRuntime.UNDEFINED;
    var instanceCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var ids:Dynamic = cast FlightRuntime.UNDEFINED;
    var transforms:Dynamic = cast FlightRuntime.UNDEFINED;
    var material:Dynamic = cast FlightRuntime.UNDEFINED;
    var materialRenderer:Dynamic = cast FlightRuntime.UNDEFINED;
    var nodeMaterialData:Dynamic = cast FlightRuntime.UNDEFINED;
    var perQuadColorTransform:Dynamic = cast FlightRuntime.UNDEFINED;
    var nodeColorTransform:Dynamic = cast FlightRuntime.UNDEFINED;
    var startCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var base:Dynamic = cast FlightRuntime.UNDEFINED;
    var regions:Dynamic = cast FlightRuntime.UNDEFINED;
    var numRegions:Dynamic = cast FlightRuntime.UNDEFINED;
    var iw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ih:Dynamic = cast FlightRuntime.UNDEFINED;
    var instanceData:Dynamic = cast FlightRuntime.UNDEFINED;
    var isVector2:Dynamic = cast FlightRuntime.UNDEFINED;
    var pt:Dynamic = cast FlightRuntime.UNDEFINED;
    var pa:Dynamic = cast FlightRuntime.UNDEFINED;
    var pb:Dynamic = cast FlightRuntime.UNDEFINED;
    var pc:Dynamic = cast FlightRuntime.UNDEFINED;
    var pd:Dynamic = cast FlightRuntime.UNDEFINED;
    var ptx:Dynamic = cast FlightRuntime.UNDEFINED;
    var pty:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var writeBase:Dynamic = cast FlightRuntime.UNDEFINED;
    var drawCount:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    source = (cast FlightRuntime.field(quadBatch, 'source') : QuadBatch);
    data = FlightRuntime.field(source, 'data');
    __destructure0 = data;
    atlas = FlightRuntime.field(__destructure0, 'atlas');
    instanceCount = FlightRuntime.field(__destructure0, 'instanceCount');
    ids = FlightRuntime.field(__destructure0, 'ids');
    transforms = FlightRuntime.field(__destructure0, 'transforms');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(atlas, null), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(atlas, 'image'), null)), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.callValue(hasImageResourcePixels, cast ([FlightRuntime.field(atlas, 'image')] : Array<Dynamic>)))), function():Dynamic return cast FlightRuntime.strictEquals(instanceCount, 0.0)))) { return; }
    material = FlightRuntime.field(quadBatch, 'material');
    materialRenderer = FlightRuntime.callValue(resolveWgpuMaterialRenderer, cast ([state, material] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(materialRenderer, null))) { return; }
    nodeMaterialData = FlightRuntime.field(quadBatch, 'materialData');
    perQuadColorTransform = FlightRuntime.field(data, 'materialData');
    nodeColorTransform = FlightRuntime.field(quadBatch, 'colorTransform');
    startCount = FlightRuntime.field(runtime, 'spriteBatchCount');
    base = FlightRuntime.callValue(prepareWgpuSpriteBatchWrite, cast ([state, FlightRuntime.field(atlas, 'image'), FlightRuntime.field(quadBatch, 'blendMode'), material, materialRenderer, instanceCount] : Array<Dynamic>));
    regions = FlightRuntime.field(atlas, 'regions');
    numRegions = FlightRuntime.field(regions, 'length');
    iw = (1.0 / FlightRuntime.orValue(FlightRuntime.field(FlightRuntime.field(atlas, 'image'), 'width'), function():Dynamic return cast 1.0));
    ih = (1.0 / FlightRuntime.orValue(FlightRuntime.field(FlightRuntime.field(atlas, 'image'), 'height'), function():Dynamic return cast 1.0));
    instanceData = FlightRuntime.field(runtime, 'spriteBatchInstanceData');
    isVector2 = FlightRuntime.strictEquals(FlightRuntime.field(data, 'transformType'), 'vector2');
    pt = FlightRuntime.field(quadBatch, 'transform2D');
    pa = FlightRuntime.field(pt, 'a');
    pb = FlightRuntime.field(pt, 'b');
    pc = FlightRuntime.field(pt, 'c');
    pd = FlightRuntime.field(pt, 'd');
    ptx = FlightRuntime.field(pt, 'tx');
    pty = FlightRuntime.field(pt, 'ty');
    alpha = FlightRuntime.field(quadBatch, 'alpha');
    writeBase = base;
    drawCount = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, instanceCount, '<'))) {
        var id:Dynamic = FlightRuntime.getIndex(ids, i);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(id, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(id, numRegions, '>=')))) { i++; continue; }
        var region:Dynamic = FlightRuntime.getIndex(regions, id);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(FlightRuntime.field(region, 'width'), 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(region, 'height'), 0.0, '<=')))) { i++; continue; }
        if (FlightRuntime.truthy(isVector2)) {
  var dx:Dynamic = FlightRuntime.getIndex(transforms, (i * 2.0));
  var dy:Dynamic = FlightRuntime.getIndex(transforms, ((i * 2.0) + 1.0));
  FlightRuntime.setIndex(instanceData, writeBase, pa);
  FlightRuntime.setIndex(instanceData, (writeBase + 1.0), pb);
  FlightRuntime.setIndex(instanceData, (writeBase + 2.0), pc);
  FlightRuntime.setIndex(instanceData, (writeBase + 3.0), pd);
  FlightRuntime.setIndex(instanceData, (writeBase + 4.0), (((pa * dx) + (pc * dy)) + ptx));
  FlightRuntime.setIndex(instanceData, (writeBase + 5.0), (((pb * dx) + (pd * dy)) + pty));
} else {
  var offset:Dynamic = (i * 6.0);
  var la:Dynamic = FlightRuntime.getIndex(transforms, offset);
  var lb:Dynamic = FlightRuntime.getIndex(transforms, (offset + 1.0));
  var lc:Dynamic = FlightRuntime.getIndex(transforms, (offset + 2.0));
  var ld:Dynamic = FlightRuntime.getIndex(transforms, (offset + 3.0));
  var ltx:Dynamic = FlightRuntime.getIndex(transforms, (offset + 4.0));
  var lty:Dynamic = FlightRuntime.getIndex(transforms, (offset + 5.0));
  FlightRuntime.setIndex(instanceData, writeBase, ((pa * la) + (pc * lb)));
  FlightRuntime.setIndex(instanceData, (writeBase + 1.0), ((pb * la) + (pd * lb)));
  FlightRuntime.setIndex(instanceData, (writeBase + 2.0), ((pa * lc) + (pc * ld)));
  FlightRuntime.setIndex(instanceData, (writeBase + 3.0), ((pb * lc) + (pd * ld)));
  FlightRuntime.setIndex(instanceData, (writeBase + 4.0), (((pa * ltx) + (pc * lty)) + ptx));
  FlightRuntime.setIndex(instanceData, (writeBase + 5.0), (((pb * ltx) + (pd * lty)) + pty));
}
        FlightRuntime.setIndex(instanceData, (writeBase + 6.0), FlightRuntime.field(region, 'width'));
        FlightRuntime.setIndex(instanceData, (writeBase + 7.0), FlightRuntime.field(region, 'height'));
        FlightRuntime.setIndex(instanceData, (writeBase + 8.0), (FlightRuntime.field(region, 'x') * iw));
        FlightRuntime.setIndex(instanceData, (writeBase + 9.0), (FlightRuntime.field(region, 'y') * ih));
        FlightRuntime.setIndex(instanceData, (writeBase + 10.0), ((FlightRuntime.field(region, 'x') + FlightRuntime.field(region, 'width')) * iw));
        FlightRuntime.setIndex(instanceData, (writeBase + 11.0), ((FlightRuntime.field(region, 'y') + FlightRuntime.field(region, 'height')) * ih));
        FlightRuntime.setIndex(instanceData, (writeBase + 12.0), alpha);
        FlightRuntime.callValue(packWgpuSpriteBatchMaterialInstance, cast ([state, nodeMaterialData, (startCount + drawCount)] : Array<Dynamic>));
        var colorTransform:Dynamic = FlightRuntime.coalesce((cast FlightRuntime.optionalIndex(perQuadColorTransform, i) : Null<ColorTransform>), function():Dynamic return cast nodeColorTransform);
        FlightRuntime.callValue(recordWgpuSpriteBatchColorTransform, cast ([state, colorTransform, (startCount + drawCount)] : Array<Dynamic>));
        (writeBase = cast ((writeBase + DisplayObjectWgpu.INSTANCE_STRIDE_FLOATS__wgpuQuadBatch) : Dynamic));
        drawCount++;
        i++;
      }
    }
    FlightRuntime.setField(runtime, 'spriteBatchCount', (FlightRuntime.field(runtime, 'spriteBatchCount') + drawCount));
  }

  @:keep public static function submitWgpuSpriteNode__wgpuSpriteRenderer(state:WgpuRenderState, spriteNode:RenderProxy2D):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var atlas:Dynamic = cast FlightRuntime.UNDEFINED;
    var id:Dynamic = cast FlightRuntime.UNDEFINED;
    var regions:Dynamic = cast FlightRuntime.UNDEFINED;
    var region:Dynamic = cast FlightRuntime.UNDEFINED;
    var iw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ih:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var material:Dynamic = cast FlightRuntime.UNDEFINED;
    var materialRenderer:Dynamic = cast FlightRuntime.UNDEFINED;
    var base:Dynamic = cast FlightRuntime.UNDEFINED;
    var instanceIndex:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    source = (cast FlightRuntime.field(spriteNode, 'source') : Sprite);
    __destructure0 = FlightRuntime.field(source, 'data');
    atlas = FlightRuntime.field(__destructure0, 'atlas');
    id = FlightRuntime.field(__destructure0, 'id');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(atlas, null), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(atlas, 'image'), null)), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.callValue(hasImageResourcePixels, cast ([FlightRuntime.field(atlas, 'image')] : Array<Dynamic>)))))) { return; }
    regions = FlightRuntime.field(atlas, 'regions');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(id, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(id, FlightRuntime.field(regions, 'length'), '>=')))) { return; }
    region = FlightRuntime.getIndex(regions, id);
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(FlightRuntime.field(region, 'width'), 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(region, 'height'), 0.0, '<=')))) { return; }
    iw = (1.0 / FlightRuntime.orValue(FlightRuntime.field(FlightRuntime.field(atlas, 'image'), 'width'), function():Dynamic return cast 1.0));
    ih = (1.0 / FlightRuntime.orValue(FlightRuntime.field(FlightRuntime.field(atlas, 'image'), 'height'), function():Dynamic return cast 1.0));
    t = FlightRuntime.field(spriteNode, 'transform2D');
    material = FlightRuntime.field(spriteNode, 'material');
    materialRenderer = FlightRuntime.callValue(resolveWgpuMaterialRenderer, cast ([state, material] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(materialRenderer, null))) { return; }
    base = FlightRuntime.callValue(prepareWgpuSpriteBatchWrite, cast ([state, FlightRuntime.field(atlas, 'image'), FlightRuntime.field(spriteNode, 'blendMode'), material, materialRenderer, 1.0] : Array<Dynamic>));
    instanceIndex = FlightRuntime.field(runtime, 'spriteBatchCount');
    d = FlightRuntime.field(runtime, 'spriteBatchInstanceData');
    FlightRuntime.setIndex(d, base, FlightRuntime.field(t, 'a'));
    FlightRuntime.setIndex(d, (base + 1.0), FlightRuntime.field(t, 'b'));
    FlightRuntime.setIndex(d, (base + 2.0), FlightRuntime.field(t, 'c'));
    FlightRuntime.setIndex(d, (base + 3.0), FlightRuntime.field(t, 'd'));
    FlightRuntime.setIndex(d, (base + 4.0), FlightRuntime.field(t, 'tx'));
    FlightRuntime.setIndex(d, (base + 5.0), FlightRuntime.field(t, 'ty'));
    FlightRuntime.setIndex(d, (base + 6.0), FlightRuntime.field(region, 'width'));
    FlightRuntime.setIndex(d, (base + 7.0), FlightRuntime.field(region, 'height'));
    FlightRuntime.setIndex(d, (base + 8.0), (FlightRuntime.field(region, 'x') * iw));
    FlightRuntime.setIndex(d, (base + 9.0), (FlightRuntime.field(region, 'y') * ih));
    FlightRuntime.setIndex(d, (base + 10.0), ((FlightRuntime.field(region, 'x') + FlightRuntime.field(region, 'width')) * iw));
    FlightRuntime.setIndex(d, (base + 11.0), ((FlightRuntime.field(region, 'y') + FlightRuntime.field(region, 'height')) * ih));
    FlightRuntime.setIndex(d, (base + 12.0), FlightRuntime.field(spriteNode, 'alpha'));
    FlightRuntime.callValue(packWgpuSpriteBatchMaterialInstance, cast ([state, FlightRuntime.field(spriteNode, 'materialData'), instanceIndex] : Array<Dynamic>));
    FlightRuntime.callValue(recordWgpuSpriteBatchColorTransform, cast ([state, FlightRuntime.field(spriteNode, 'colorTransform'), instanceIndex] : Array<Dynamic>));
    FlightRuntime.incrementField(runtime, 'spriteBatchCount', 1, true);
  }

  @:keep public static function submitWgpuTilemap__wgpuTilemap(state:WgpuRenderState, tilemapNode:RenderProxy2D):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var columns:Dynamic = cast FlightRuntime.UNDEFINED;
    var rows:Dynamic = cast FlightRuntime.UNDEFINED;
    var tileset:Dynamic = cast FlightRuntime.UNDEFINED;
    var tiles:Dynamic = cast FlightRuntime.UNDEFINED;
    var atlas:Dynamic = cast FlightRuntime.UNDEFINED;
    var material:Dynamic = cast FlightRuntime.UNDEFINED;
    var materialRenderer:Dynamic = cast FlightRuntime.UNDEFINED;
    var nodeMaterialData:Dynamic = cast FlightRuntime.UNDEFINED;
    var perTileColorTransform:Dynamic = cast FlightRuntime.UNDEFINED;
    var nodeColorTransform:Dynamic = cast FlightRuntime.UNDEFINED;
    var startCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var base:Dynamic = cast FlightRuntime.UNDEFINED;
    var regions:Dynamic = cast FlightRuntime.UNDEFINED;
    var numRegions:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure1:Dynamic = cast FlightRuntime.UNDEFINED;
    var tileHeight:Dynamic = cast FlightRuntime.UNDEFINED;
    var tileWidth:Dynamic = cast FlightRuntime.UNDEFINED;
    var iw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ih:Dynamic = cast FlightRuntime.UNDEFINED;
    var instanceData:Dynamic = cast FlightRuntime.UNDEFINED;
    var pt:Dynamic = cast FlightRuntime.UNDEFINED;
    var pa:Dynamic = cast FlightRuntime.UNDEFINED;
    var pb:Dynamic = cast FlightRuntime.UNDEFINED;
    var pc:Dynamic = cast FlightRuntime.UNDEFINED;
    var pd:Dynamic = cast FlightRuntime.UNDEFINED;
    var ptx:Dynamic = cast FlightRuntime.UNDEFINED;
    var pty:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var writeBase:Dynamic = cast FlightRuntime.UNDEFINED;
    var drawCount:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) { return; }
    source = (cast FlightRuntime.field(tilemapNode, 'source') : Tilemap);
    __destructure0 = FlightRuntime.field(source, 'data');
    columns = FlightRuntime.field(__destructure0, 'columns');
    rows = FlightRuntime.field(__destructure0, 'rows');
    tileset = FlightRuntime.field(__destructure0, 'tileset');
    tiles = FlightRuntime.field(__destructure0, 'tiles');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(tileset, null))) { return; }
    atlas = FlightRuntime.field(tileset, 'atlas');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(atlas, null), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(atlas, 'image'), null)), function():Dynamic return cast !FlightRuntime.truthy(FlightRuntime.callValue(hasImageResourcePixels, cast ([FlightRuntime.field(atlas, 'image')] : Array<Dynamic>)))))) { return; }
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(columns, 0.0), function():Dynamic return cast FlightRuntime.strictEquals(rows, 0.0)))) { return; }
    material = FlightRuntime.field(tilemapNode, 'material');
    materialRenderer = FlightRuntime.callValue(resolveWgpuMaterialRenderer, cast ([state, material] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(materialRenderer, null))) { return; }
    nodeMaterialData = FlightRuntime.field(tilemapNode, 'materialData');
    perTileColorTransform = FlightRuntime.field(FlightRuntime.field(source, 'data'), 'materialData');
    nodeColorTransform = FlightRuntime.field(tilemapNode, 'colorTransform');
    startCount = FlightRuntime.field(runtime, 'spriteBatchCount');
    base = FlightRuntime.callValue(prepareWgpuSpriteBatchWrite, cast ([state, FlightRuntime.field(atlas, 'image'), FlightRuntime.field(tilemapNode, 'blendMode'), material, materialRenderer, (columns * rows)] : Array<Dynamic>));
    regions = FlightRuntime.field(atlas, 'regions');
    numRegions = FlightRuntime.field(regions, 'length');
    __destructure1 = tileset;
    tileHeight = FlightRuntime.field(__destructure1, 'tileHeight');
    tileWidth = FlightRuntime.field(__destructure1, 'tileWidth');
    iw = (1.0 / FlightRuntime.orValue(FlightRuntime.field(FlightRuntime.field(atlas, 'image'), 'width'), function():Dynamic return cast 1.0));
    ih = (1.0 / FlightRuntime.orValue(FlightRuntime.field(FlightRuntime.field(atlas, 'image'), 'height'), function():Dynamic return cast 1.0));
    instanceData = FlightRuntime.field(runtime, 'spriteBatchInstanceData');
    pt = FlightRuntime.field(tilemapNode, 'transform2D');
    pa = FlightRuntime.field(pt, 'a');
    pb = FlightRuntime.field(pt, 'b');
    pc = FlightRuntime.field(pt, 'c');
    pd = FlightRuntime.field(pt, 'd');
    ptx = FlightRuntime.field(pt, 'tx');
    pty = FlightRuntime.field(pt, 'ty');
    alpha = FlightRuntime.field(tilemapNode, 'alpha');
    writeBase = base;
    drawCount = 0.0;
    {
      var row:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(row, rows, '<'))) {
        {
          var col:Dynamic = 0.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(col, columns, '<'))) {
            var id:Dynamic = FlightRuntime.getIndex(tiles, ((row * columns) + col));
            if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(id, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(id, numRegions, '>=')))) { col++; continue; }
            var region:Dynamic = FlightRuntime.getIndex(regions, id);
            if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(FlightRuntime.field(region, 'width'), 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(region, 'height'), 0.0, '<=')))) { col++; continue; }
            var dx:Dynamic = (col * tileWidth);
            var dy:Dynamic = (row * tileHeight);
            FlightRuntime.setIndex(instanceData, writeBase, pa);
            FlightRuntime.setIndex(instanceData, (writeBase + 1.0), pb);
            FlightRuntime.setIndex(instanceData, (writeBase + 2.0), pc);
            FlightRuntime.setIndex(instanceData, (writeBase + 3.0), pd);
            FlightRuntime.setIndex(instanceData, (writeBase + 4.0), (((pa * dx) + (pc * dy)) + ptx));
            FlightRuntime.setIndex(instanceData, (writeBase + 5.0), (((pb * dx) + (pd * dy)) + pty));
            FlightRuntime.setIndex(instanceData, (writeBase + 6.0), tileWidth);
            FlightRuntime.setIndex(instanceData, (writeBase + 7.0), tileHeight);
            FlightRuntime.setIndex(instanceData, (writeBase + 8.0), (FlightRuntime.field(region, 'x') * iw));
            FlightRuntime.setIndex(instanceData, (writeBase + 9.0), (FlightRuntime.field(region, 'y') * ih));
            FlightRuntime.setIndex(instanceData, (writeBase + 10.0), ((FlightRuntime.field(region, 'x') + FlightRuntime.field(region, 'width')) * iw));
            FlightRuntime.setIndex(instanceData, (writeBase + 11.0), ((FlightRuntime.field(region, 'y') + FlightRuntime.field(region, 'height')) * ih));
            FlightRuntime.setIndex(instanceData, (writeBase + 12.0), alpha);
            FlightRuntime.callValue(packWgpuSpriteBatchMaterialInstance, cast ([state, nodeMaterialData, (startCount + drawCount)] : Array<Dynamic>));
            var colorTransform:Dynamic = FlightRuntime.coalesce((cast FlightRuntime.optionalIndex(perTileColorTransform, ((row * columns) + col)) : Null<ColorTransform>), function():Dynamic return cast nodeColorTransform);
            FlightRuntime.callValue(recordWgpuSpriteBatchColorTransform, cast ([state, colorTransform, (startCount + drawCount)] : Array<Dynamic>));
            (writeBase = cast ((writeBase + DisplayObjectWgpu.INSTANCE_STRIDE_FLOATS__wgpuTilemap) : Dynamic));
            drawCount++;
            col++;
          }
        }
        row++;
      }
    }
    FlightRuntime.setField(runtime, 'spriteBatchCount', (FlightRuntime.field(runtime, 'spriteBatchCount') + drawCount));
  }

  @:keep public static function toScale9Position__wgpuScale9Mapper(pos:Float, scale9Start:Float, scale9Center:Float, unscaledSize:Float, scale:Float):Float {
    var scale9End:Dynamic = cast FlightRuntime.UNDEFINED;
    var size:Dynamic = cast FlightRuntime.UNDEFINED;
    var center:Dynamic = cast FlightRuntime.UNDEFINED;
    scale9End = ((unscaledSize - scale9Center) - scale9Start);
    size = (unscaledSize * scale);
    center = ((size - scale9Start) - scale9End);
    if (FlightRuntime.truthy(FlightRuntime.compare(pos, scale9Start, '<='))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(center, 0.0, '<'))) {
  return cast ((pos * ((scale9Start + scale9End) + center)) / (scale9Start + scale9End));
}
  return cast pos;
}
    if (FlightRuntime.truthy(FlightRuntime.compare(pos, (scale9Start + scale9Center), '>='))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(center, 0.0, '<'))) {
  return cast (((scale9Start + ((pos - scale9Start) - scale9Center)) * ((scale9Start + scale9End) + center)) / (scale9Start + scale9End));
}
  return cast ((scale9Start + center) + ((pos - scale9Start) - scale9Center));
}
    if (FlightRuntime.truthy(FlightRuntime.compare(center, 0.0, '<'))) {
  return cast ((scale9Start * ((scale9Start + scale9End) + center)) / (scale9Start + scale9End));
}
    return cast (scale9Start + ((center * (pos - scale9Start)) / scale9Center));
    return cast null;
  }

  public static final UNIFORM_BYTES__wgpuVelocity:Dynamic = 32.0;

  public static final UNIFORM_SLOTS__wgpuVelocity:Dynamic = 1024.0;

  public static final UNIFORM_STRIDE__wgpuVelocity:Dynamic = 256.0;

  public static final VELOCITY_WGSL__wgpuVelocity:Dynamic = '\nstruct Uniforms {\n  clipRect : vec4f,\n  velocity : vec2f,\n  _pad : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n\n@vertex\nfn vs_main(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {\n  var corners = array<vec2f, 6>(\n    vec2f(0.0, 0.0), vec2f(1.0, 0.0), vec2f(0.0, 1.0),\n    vec2f(0.0, 1.0), vec2f(1.0, 0.0), vec2f(1.0, 1.0),\n  );\n  let clip = uni.clipRect.xy + corners[vi] * uni.clipRect.zw;\n  return vec4f(clip, 0.0, 1.0);\n}\n\n@fragment\nfn fs_main() -> @location(0) vec4f {\n  return vec4f(uni.velocity, 0.0, 1.0);\n}';

  @:keep public static function visitWgpuVelocity__wgpuVelocity<Traits>(ctx:WgpuVelocityContext, node:Transform2DNode<Traits>):Void {
    var writer:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    writer = FlightRuntime.callValue(getWgpuVelocityWriter, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(node, 'kind')] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(writer, null))) { FlightRuntime.callValue(writer, cast ([ctx, node] : Array<Dynamic>)); }
    count = FlightRuntime.callValue(getNodeChildCount, cast ([node] : Array<Dynamic>));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, count, '<'))) {
        var child:Dynamic = FlightRuntime.callValue(getNodeChildAt, cast ([node, i] : Array<Dynamic>));
        if (FlightRuntime.truthy(!FlightRuntime.strictEquals(child, null))) { FlightRuntime.callValue(DisplayObjectWgpu.visitWgpuVelocity__wgpuVelocity, cast ([ctx, (cast (cast child : Dynamic) : Transform2DNode<Traits>)] : Array<Dynamic>)); }
        i++;
      }
    }
  }

  @:keep public static function warnWgpuColorAdjustmentNotEnabled__enableWgpuColorAdjustmentGuards():Void {
    FlightRuntime.callValue(logOnce, cast (['displayobject-wgpu:color-adjustment-not-enabled', LogLevel.Warn, { message: 'recordWgpuSpriteBatchColorTransform: color transform present but WGPU color adjustment not enabled — call enableWgpuColorAdjustment(state)' }, 'displayobject-wgpu'] : Array<Dynamic>));
  }

  public static final webgpuClipHooks__wgpuClip:DisplayObjectClipHooks = { finalize: function(state:WgpuRenderState) {
  var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
  runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
  while (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(runtime, 'clipForms'), 'length'), 0.0, '>'))) { FlightRuntime.callValue(DisplayObjectWgpu.popOneWgpuClip__wgpuClip, cast ([state] : Array<Dynamic>)); }
}, popClip: function(state:WgpuRenderState, data:RenderProxy2D, source:DisplayObject) {
  var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
  var target:Dynamic = cast FlightRuntime.UNDEFINED;
  runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
  target = (FlightRuntime.field(data, 'clipDepth') - FlightRuntime.select(!FlightRuntime.looseEquals(FlightRuntime.field(source, 'clip'), null), function():Dynamic return cast 1.0, function():Dynamic return cast 0.0));
  while (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(runtime, 'clipForms'), 'length'), target, '>'))) { FlightRuntime.callValue(DisplayObjectWgpu.popOneWgpuClip__wgpuClip, cast ([state] : Array<Dynamic>)); }
}, pushClip: function(state:WgpuRenderState, data:RenderProxy2D, source:DisplayObject) {
  var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
  var clip:Dynamic = cast FlightRuntime.UNDEFINED;
  runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
  clip = FlightRuntime.field(source, 'clip');
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(clip, null))) { return; }
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(clip, 'contours'), null))) {
  FlightRuntime.callValue(pushWgpuClipRectangle, cast ([state, FlightRuntime.field(clip, 'rect'), FlightRuntime.field(data, 'transform2D')] : Array<Dynamic>));
  FlightRuntime.callProperty(FlightRuntime.field(runtime, 'clipForms'), 'push', cast (['rect'] : Array<Dynamic>));
} else {
  FlightRuntime.callValue(pushWgpuClipContours, cast ([state, FlightRuntime.field(clip, 'contours'), FlightRuntime.field(clip, 'winding'), FlightRuntime.field(data, 'transform2D')] : Array<Dynamic>));
  FlightRuntime.callProperty(FlightRuntime.field(runtime, 'clipForms'), 'push', cast (['contour'] : Array<Dynamic>));
}
} };

  public static final wgpuColorAdjustmentFold__wgpuColorAdjustment:WgpuColorAdjustmentFold = { record: DisplayObjectWgpu.recordWgpuColorAdjustment__wgpuColorAdjustment, resolveFlush: DisplayObjectWgpu.resolveWgpuColorAdjustmentFlush__wgpuColorAdjustment };

  @:keep public static function writeShapeMeshIndices__wgpuShapeMesh(queue:Dynamic, indexBuffer:Dynamic, indices:flight.internal.FlightUInt16Array):Void {
    var byteLength:Dynamic = cast FlightRuntime.UNDEFINED;
    var paddedBytes:Dynamic = cast FlightRuntime.UNDEFINED;
    var wordCount:Dynamic = cast FlightRuntime.UNDEFINED;
    byteLength = FlightRuntime.field(indices, 'byteLength');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals((Std.int(byteLength) & Std.int(3.0)), 0.0))) {
  FlightRuntime.callProperty(queue, 'writeBuffer', cast ([indexBuffer, 0.0, FlightRuntime.field(indices, 'buffer'), FlightRuntime.field(indices, 'byteOffset'), byteLength] : Array<Dynamic>));
  return;
}
    paddedBytes = (Std.int((byteLength + 3.0)) & Std.int(~Std.int(3.0)));
    wordCount = (Std.int(paddedBytes) >> Std.int(1.0));
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(DisplayObjectWgpu._shapeMeshIndexScratch__wgpuShapeMesh, 'length'), wordCount, '<'))) { (DisplayObjectWgpu._shapeMeshIndexScratch__wgpuShapeMesh = cast (new flight.internal.FlightUInt16Array(wordCount) : Dynamic)); }
    FlightRuntime.callProperty(DisplayObjectWgpu._shapeMeshIndexScratch__wgpuShapeMesh, 'set', cast ([indices] : Array<Dynamic>));
    FlightRuntime.callProperty(queue, 'writeBuffer', cast ([indexBuffer, 0.0, FlightRuntime.field(DisplayObjectWgpu._shapeMeshIndexScratch__wgpuShapeMesh, 'buffer'), 0.0, paddedBytes] : Array<Dynamic>));
  }

  @:keep public static function writeWgpuColorTransformInstance__wgpuColorAdjustment(runtime:WgpuRenderStateRuntime, colorTransform:Null<ColorTransform>, instanceIndex:Float):Void {
    var offset:Dynamic = cast FlightRuntime.UNDEFINED;
    var out:Dynamic = cast FlightRuntime.UNDEFINED;
    offset = (instanceIndex * DisplayObjectWgpu.COLOR_TRANSFORM_FLOATS__wgpuColorAdjustment);
    FlightRuntime.callValue(DisplayObjectWgpu.ensureWgpuColorTransformCapacity__wgpuColorAdjustment, cast ([runtime, (offset + DisplayObjectWgpu.COLOR_TRANSFORM_FLOATS__wgpuColorAdjustment)] : Array<Dynamic>));
    out = FlightRuntime.field(runtime, 'spriteBatchColorTransformData');
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(colorTransform, null))) {
  FlightRuntime.setIndex(out, offset, FlightRuntime.field(colorTransform, 'redMultiplier'));
  FlightRuntime.setIndex(out, (offset + 1.0), FlightRuntime.field(colorTransform, 'greenMultiplier'));
  FlightRuntime.setIndex(out, (offset + 2.0), FlightRuntime.field(colorTransform, 'blueMultiplier'));
  FlightRuntime.setIndex(out, (offset + 3.0), FlightRuntime.field(colorTransform, 'alphaMultiplier'));
  FlightRuntime.setIndex(out, (offset + 4.0), (FlightRuntime.field(colorTransform, 'redOffset') / 255.0));
  FlightRuntime.setIndex(out, (offset + 5.0), (FlightRuntime.field(colorTransform, 'greenOffset') / 255.0));
  FlightRuntime.setIndex(out, (offset + 6.0), (FlightRuntime.field(colorTransform, 'blueOffset') / 255.0));
  FlightRuntime.setIndex(out, (offset + 7.0), (FlightRuntime.field(colorTransform, 'alphaOffset') / 255.0));
} else {
  FlightRuntime.setIndex(out, offset, 1.0);
  FlightRuntime.setIndex(out, (offset + 1.0), 1.0);
  FlightRuntime.setIndex(out, (offset + 2.0), 1.0);
  FlightRuntime.setIndex(out, (offset + 3.0), 1.0);
  FlightRuntime.setIndex(out, (offset + 4.0), 0.0);
  FlightRuntime.setIndex(out, (offset + 5.0), 0.0);
  FlightRuntime.setIndex(out, (offset + 6.0), 0.0);
  FlightRuntime.setIndex(out, (offset + 7.0), 0.0);
}
  }

  @:keep public static function writeWgpuSpriteBatchUniforms__wgpuSpriteBatch(state:WgpuRenderState):Float {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    var floatBase:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure3:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformData:Dynamic = cast FlightRuntime.UNDEFINED;
    var viewport:Dynamic = cast FlightRuntime.UNDEFINED;
    var iw:Dynamic = cast FlightRuntime.UNDEFINED;
    var ih:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    uniformOffset = FlightRuntime.field(runtime, 'uniformOffset');
    floatBase = (Std.int(uniformOffset) >> Std.int(2.0));
    __destructure3 = runtime;
    uniformData = FlightRuntime.field(__destructure3, 'uniformData');
    viewport = FlightRuntime.coalesce(FlightRuntime.field(runtime, 'renderTargetViewport'), function():Dynamic return cast FlightRuntime.field(state, 'canvas'));
    iw = (2.0 / FlightRuntime.field(viewport, 'width'));
    ih = (2.0 / FlightRuntime.field(viewport, 'height'));
    FlightRuntime.setIndex(uniformData, (floatBase + 0.0), iw);
    FlightRuntime.setIndex(uniformData, (floatBase + 1.0), 0.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 2.0), 0.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 3.0), 0.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 4.0), 0.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 5.0), -ih);
    FlightRuntime.setIndex(uniformData, (floatBase + 6.0), 0.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 7.0), 0.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 8.0), -1.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 9.0), 1.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 10.0), 1.0);
    FlightRuntime.setIndex(uniformData, (floatBase + 11.0), 0.0);
    FlightRuntime.setField(runtime, 'uniformOffset', (FlightRuntime.field(runtime, 'uniformOffset') + FlightRuntime.field(runtime, 'uniformStride')));
    return cast uniformOffset;
    return cast null;
  }
}
