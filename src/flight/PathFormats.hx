// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.PathApi.appendPathArcTo;
import flight.PathApi.appendPathClose;
import flight.PathApi.appendPathCubicCurveTo;
import flight.PathApi.appendPathCurveTo;
import flight.PathApi.appendPathLineTo;
import flight.PathApi.appendPathMoveTo;
import flight.PathApi.createPath;
import flight.PathApi.forEachPathSegment;
import flight.Types.Path;

@:expose("flight.PathFormats")
class PathFormats {
  public static function appendSvgPathData(path:Path, d:String):Bool {
    var length:Dynamic = cast FlightRuntime.UNDEFINED;
    var pos:Dynamic = cast FlightRuntime.UNDEFINED;
    var currentX:Dynamic = cast FlightRuntime.UNDEFINED;
    var currentY:Dynamic = cast FlightRuntime.UNDEFINED;
    var startX:Dynamic = cast FlightRuntime.UNDEFINED;
    var startY:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastControl2X:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastControl2Y:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastQuadControlX:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastQuadControlY:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastKind:Dynamic = cast FlightRuntime.UNDEFINED;
    var skipSeparators:Void->Void = cast FlightRuntime.UNDEFINED;
    var readNumber:Void->Null<Float> = cast FlightRuntime.UNDEFINED;
    var readFlag:Void->Null<Float> = cast FlightRuntime.UNDEFINED;
    length = FlightRuntime.field(d, 'length');
    pos = 0.0;
    currentX = 0.0;
    currentY = 0.0;
    startX = 0.0;
    startY = 0.0;
    lastControl2X = 0.0;
    lastControl2Y = 0.0;
    lastQuadControlX = 0.0;
    lastQuadControlY = 0.0;
    lastKind = '';
    skipSeparators = function skipSeparators():Void {
  while (FlightRuntime.truthy(FlightRuntime.compare(pos, length, '<'))) {
  var c:Dynamic = FlightRuntime.charCodeAt(d, pos);
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(c, 32.0), function():Dynamic return cast FlightRuntime.strictEquals(c, 9.0)), function():Dynamic return cast FlightRuntime.strictEquals(c, 10.0)), function():Dynamic return cast FlightRuntime.strictEquals(c, 13.0)), function():Dynamic return cast FlightRuntime.strictEquals(c, 12.0)), function():Dynamic return cast FlightRuntime.strictEquals(c, 44.0)))) { pos++; } else { break; }
}
};
    readNumber = function readNumber():Null<Float> {
  var start:Dynamic = cast FlightRuntime.UNDEFINED;
  var sawDigit:Dynamic = cast FlightRuntime.UNDEFINED;
  FlightRuntime.callValue(skipSeparators, cast ([] : Array<Dynamic>));
  start = pos;
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(pos, length, '<'), function():Dynamic return cast FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.getIndex(d, pos), '+'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(d, pos), '-'))))) { pos++; }
  sawDigit = false;
  while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(pos, length, '<'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(d, pos), '0', '>=')), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(d, pos), '9', '<=')))) {
  pos++;
  (sawDigit = cast (true : Dynamic));
}
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(pos, length, '<'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(d, pos), '.')))) {
  pos++;
  while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(pos, length, '<'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(d, pos), '0', '>=')), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(d, pos), '9', '<=')))) {
  pos++;
  (sawDigit = cast (true : Dynamic));
}
}
  if (FlightRuntime.truthy(!FlightRuntime.truthy(sawDigit))) {
  (pos = cast (start : Dynamic));
  return cast null;
}
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(pos, length, '<'), function():Dynamic return cast FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.getIndex(d, pos), 'e'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(d, pos), 'E'))))) {
  var expStart:Dynamic = pos;
  pos++;
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(pos, length, '<'), function():Dynamic return cast FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.getIndex(d, pos), '+'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(d, pos), '-'))))) { pos++; }
  var expDigit:Dynamic = false;
  while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(pos, length, '<'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(d, pos), '0', '>=')), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(d, pos), '9', '<=')))) {
  pos++;
  (expDigit = cast (true : Dynamic));
}
  if (FlightRuntime.truthy(!FlightRuntime.truthy(expDigit))) { (pos = cast (expStart : Dynamic)); }
}
  return cast FlightRuntime.callProperty(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Number'] : Array<Dynamic>)), 'parseFloat', cast ([FlightRuntime.slice(d, start, pos)] : Array<Dynamic>));
};
    readFlag = function readFlag():Null<Float> {
  FlightRuntime.callValue(skipSeparators, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(pos, length, '<'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(d, pos), '0')))) {
  pos++;
  return cast 0.0;
}
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(pos, length, '<'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(d, pos), '1')))) {
  pos++;
  return cast 1.0;
}
  return cast null;
};
    while (FlightRuntime.truthy(true)) {
  FlightRuntime.callValue(skipSeparators, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.compare(pos, length, '>='))) { break; }
  var commandLetter:Dynamic = FlightRuntime.getIndex(d, pos);
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(PathFormats.isSvgCommandLetter__svgPathData, cast ([commandLetter] : Array<Dynamic>))))) { return cast false; }
  pos++;
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.strictEquals(lastKind, ''), function():Dynamic return cast !FlightRuntime.strictEquals(commandLetter, 'M')), function():Dynamic return cast !FlightRuntime.strictEquals(commandLetter, 'm')))) { return cast false; }
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(commandLetter, 'Z'), function():Dynamic return cast FlightRuntime.strictEquals(commandLetter, 'z')))) {
  FlightRuntime.callValue(appendPathClose, cast ([path] : Array<Dynamic>));
  (currentX = cast (startX : Dynamic));
  (currentY = cast (startY : Dynamic));
  (lastKind = cast ('Z' : Dynamic));
  continue;
}
  var active:Dynamic = commandLetter;
  var first:Dynamic = true;
  while (FlightRuntime.truthy(true)) {
  if (FlightRuntime.truthy(!FlightRuntime.truthy(first))) {
  FlightRuntime.callValue(skipSeparators, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.compare(pos, length, '>='))) { break; }
  if (FlightRuntime.truthy(FlightRuntime.callValue(PathFormats.isSvgCommandLetter__svgPathData, cast ([FlightRuntime.getIndex(d, pos)] : Array<Dynamic>)))) { break; }
}
  var relative:Dynamic = FlightRuntime.compare(active, 'a', '>=');
  var upper:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast FlightRuntime.callProperty(active, 'toUpperCase', cast ([] : Array<Dynamic>)), function():Dynamic return cast active);
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(upper, 'M'))) {
  var nx:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var ny:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(nx, null), function():Dynamic return cast FlightRuntime.strictEquals(ny, null)))) { return cast false; }
  (currentX = cast (FlightRuntime.select(relative, function():Dynamic return cast (currentX + nx), function():Dynamic return cast nx) : Dynamic));
  (currentY = cast (FlightRuntime.select(relative, function():Dynamic return cast (currentY + ny), function():Dynamic return cast ny) : Dynamic));
  (startX = cast (currentX : Dynamic));
  (startY = cast (currentY : Dynamic));
  FlightRuntime.callValue(appendPathMoveTo, cast ([path, currentX, currentY] : Array<Dynamic>));
  (lastKind = cast ('M' : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(upper, 'L'))) {
  var nx:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var ny:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(nx, null), function():Dynamic return cast FlightRuntime.strictEquals(ny, null)))) { return cast false; }
  (currentX = cast (FlightRuntime.select(relative, function():Dynamic return cast (currentX + nx), function():Dynamic return cast nx) : Dynamic));
  (currentY = cast (FlightRuntime.select(relative, function():Dynamic return cast (currentY + ny), function():Dynamic return cast ny) : Dynamic));
  FlightRuntime.callValue(appendPathLineTo, cast ([path, currentX, currentY] : Array<Dynamic>));
  (lastKind = cast ('L' : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(upper, 'H'))) {
  var nx:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(nx, null))) { return cast false; }
  (currentX = cast (FlightRuntime.select(relative, function():Dynamic return cast (currentX + nx), function():Dynamic return cast nx) : Dynamic));
  FlightRuntime.callValue(appendPathLineTo, cast ([path, currentX, currentY] : Array<Dynamic>));
  (lastKind = cast ('L' : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(upper, 'V'))) {
  var ny:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(ny, null))) { return cast false; }
  (currentY = cast (FlightRuntime.select(relative, function():Dynamic return cast (currentY + ny), function():Dynamic return cast ny) : Dynamic));
  FlightRuntime.callValue(appendPathLineTo, cast ([path, currentX, currentY] : Array<Dynamic>));
  (lastKind = cast ('L' : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(upper, 'C'))) {
  var x1:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var y1:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var x2:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var y2:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var x:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var y:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(x1, null), function():Dynamic return cast FlightRuntime.strictEquals(y1, null)), function():Dynamic return cast FlightRuntime.strictEquals(x2, null)), function():Dynamic return cast FlightRuntime.strictEquals(y2, null)), function():Dynamic return cast FlightRuntime.strictEquals(x, null)), function():Dynamic return cast FlightRuntime.strictEquals(y, null)))) { return cast false; }
  var c1x:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentX + x1), function():Dynamic return cast x1);
  var c1y:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentY + y1), function():Dynamic return cast y1);
  var c2x:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentX + x2), function():Dynamic return cast x2);
  var c2y:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentY + y2), function():Dynamic return cast y2);
  var ax:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentX + x), function():Dynamic return cast x);
  var ay:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentY + y), function():Dynamic return cast y);
  FlightRuntime.callValue(appendPathCubicCurveTo, cast ([path, c1x, c1y, c2x, c2y, ax, ay] : Array<Dynamic>));
  (lastControl2X = cast (c2x : Dynamic));
  (lastControl2Y = cast (c2y : Dynamic));
  (currentX = cast (ax : Dynamic));
  (currentY = cast (ay : Dynamic));
  (lastKind = cast ('C' : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(upper, 'S'))) {
  var x2:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var y2:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var x:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var y:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(x2, null), function():Dynamic return cast FlightRuntime.strictEquals(y2, null)), function():Dynamic return cast FlightRuntime.strictEquals(x, null)), function():Dynamic return cast FlightRuntime.strictEquals(y, null)))) { return cast false; }
  var reflect:Dynamic = FlightRuntime.orValue(FlightRuntime.strictEquals(lastKind, 'C'), function():Dynamic return cast FlightRuntime.strictEquals(lastKind, 'S'));
  var c1x:Dynamic = FlightRuntime.select(reflect, function():Dynamic return cast ((2.0 * currentX) - lastControl2X), function():Dynamic return cast currentX);
  var c1y:Dynamic = FlightRuntime.select(reflect, function():Dynamic return cast ((2.0 * currentY) - lastControl2Y), function():Dynamic return cast currentY);
  var c2x:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentX + x2), function():Dynamic return cast x2);
  var c2y:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentY + y2), function():Dynamic return cast y2);
  var ax:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentX + x), function():Dynamic return cast x);
  var ay:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentY + y), function():Dynamic return cast y);
  FlightRuntime.callValue(appendPathCubicCurveTo, cast ([path, c1x, c1y, c2x, c2y, ax, ay] : Array<Dynamic>));
  (lastControl2X = cast (c2x : Dynamic));
  (lastControl2Y = cast (c2y : Dynamic));
  (currentX = cast (ax : Dynamic));
  (currentY = cast (ay : Dynamic));
  (lastKind = cast ('S' : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(upper, 'Q'))) {
  var x1:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var y1:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var x:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var y:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(x1, null), function():Dynamic return cast FlightRuntime.strictEquals(y1, null)), function():Dynamic return cast FlightRuntime.strictEquals(x, null)), function():Dynamic return cast FlightRuntime.strictEquals(y, null)))) { return cast false; }
  var cx:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentX + x1), function():Dynamic return cast x1);
  var cy:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentY + y1), function():Dynamic return cast y1);
  var ax:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentX + x), function():Dynamic return cast x);
  var ay:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentY + y), function():Dynamic return cast y);
  FlightRuntime.callValue(appendPathCurveTo, cast ([path, cx, cy, ax, ay] : Array<Dynamic>));
  (lastQuadControlX = cast (cx : Dynamic));
  (lastQuadControlY = cast (cy : Dynamic));
  (currentX = cast (ax : Dynamic));
  (currentY = cast (ay : Dynamic));
  (lastKind = cast ('Q' : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(upper, 'T'))) {
  var x:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var y:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(x, null), function():Dynamic return cast FlightRuntime.strictEquals(y, null)))) { return cast false; }
  var reflect:Dynamic = FlightRuntime.orValue(FlightRuntime.strictEquals(lastKind, 'Q'), function():Dynamic return cast FlightRuntime.strictEquals(lastKind, 'T'));
  var cx:Dynamic = FlightRuntime.select(reflect, function():Dynamic return cast ((2.0 * currentX) - lastQuadControlX), function():Dynamic return cast currentX);
  var cy:Dynamic = FlightRuntime.select(reflect, function():Dynamic return cast ((2.0 * currentY) - lastQuadControlY), function():Dynamic return cast currentY);
  var ax:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentX + x), function():Dynamic return cast x);
  var ay:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentY + y), function():Dynamic return cast y);
  FlightRuntime.callValue(appendPathCurveTo, cast ([path, cx, cy, ax, ay] : Array<Dynamic>));
  (lastQuadControlX = cast (cx : Dynamic));
  (lastQuadControlY = cast (cy : Dynamic));
  (currentX = cast (ax : Dynamic));
  (currentY = cast (ay : Dynamic));
  (lastKind = cast ('T' : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(upper, 'A'))) {
  var rx:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var ry:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var rotationDegrees:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var largeArc:Dynamic = FlightRuntime.callValue(readFlag, cast ([] : Array<Dynamic>));
  var sweep:Dynamic = FlightRuntime.callValue(readFlag, cast ([] : Array<Dynamic>));
  var x:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  var y:Dynamic = FlightRuntime.callValue(readNumber, cast ([] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(rx, null), function():Dynamic return cast FlightRuntime.strictEquals(ry, null)), function():Dynamic return cast FlightRuntime.strictEquals(rotationDegrees, null)), function():Dynamic return cast FlightRuntime.strictEquals(largeArc, null)), function():Dynamic return cast FlightRuntime.strictEquals(sweep, null)), function():Dynamic return cast FlightRuntime.strictEquals(x, null)), function():Dynamic return cast FlightRuntime.strictEquals(y, null)))) {
  return cast false;
}
  var ax:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentX + x), function():Dynamic return cast x);
  var ay:Dynamic = FlightRuntime.select(relative, function():Dynamic return cast (currentY + y), function():Dynamic return cast y);
  FlightRuntime.callValue(appendPathArcTo, cast ([path, rx, ry, ((rotationDegrees * HxMath.PI) / 180.0), FlightRuntime.strictEquals(largeArc, 1.0), FlightRuntime.strictEquals(sweep, 1.0), ax, ay] : Array<Dynamic>));
  (currentX = cast (ax : Dynamic));
  (currentY = cast (ay : Dynamic));
  (lastKind = cast ('A' : Dynamic));
} else {
  return cast false;
} } } } } } } } }
  (first = cast (false : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(active, 'M'))) { (active = cast ('L' : Dynamic)); } else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(active, 'm'))) { (active = cast ('l' : Dynamic)); } }
}
}
    return cast true;
    return cast null;
  }

  public static function formatSvgNumber__svgPathData(value:Float, ?precision:Float):String {
    var factor:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(precision, FlightRuntime.UNDEFINED))) { return cast Std.string(value); }
    factor = HxMath.pow(10.0, precision);
    return cast Std.string((FlightRuntime.callProperty(HxMath, 'round', cast ([(value * factor)] : Array<Dynamic>)) / factor));
    return cast null;
  }

  public static function formatSvgPathData(path:Path, ?options:{ @:optional var precision:Float; }):String {
    var precision:Dynamic = cast FlightRuntime.UNDEFINED;
    var parts:Array<String> = cast FlightRuntime.UNDEFINED;
    precision = FlightRuntime.optionalField(options, 'precision');
    parts = cast ([] : Array<Dynamic>);
    FlightRuntime.callValue(forEachPathSegment, cast ([path, function(segment:Dynamic) {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'moveTo'))) {
  FlightRuntime.callProperty(parts, 'push', cast (['M' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'x'), precision] : Array<Dynamic>))) + ' ' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'y'), precision] : Array<Dynamic>))) + ''] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'lineTo'))) {
  FlightRuntime.callProperty(parts, 'push', cast (['L' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'x'), precision] : Array<Dynamic>))) + ' ' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'y'), precision] : Array<Dynamic>))) + ''] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'curveTo'))) {
  FlightRuntime.callProperty(parts, 'push', cast ([('Q' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'controlX'), precision] : Array<Dynamic>))) + ' ' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'controlY'), precision] : Array<Dynamic>))) + ' ' + '' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'x'), precision] : Array<Dynamic>))) + ' ' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'y'), precision] : Array<Dynamic>))) + '')] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'cubicCurveTo'))) {
  FlightRuntime.callProperty(parts, 'push', cast ([(('C' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'control1X'), precision] : Array<Dynamic>))) + ' ' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'control1Y'), precision] : Array<Dynamic>))) + ' ' + '' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'control2X'), precision] : Array<Dynamic>))) + ' ' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'control2Y'), precision] : Array<Dynamic>))) + ' ') + '' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'x'), precision] : Array<Dynamic>))) + ' ' + Std.string(FlightRuntime.callValue(PathFormats.formatSvgNumber__svgPathData, cast ([FlightRuntime.field(segment, 'y'), precision] : Array<Dynamic>))) + '')] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'close'))) {
  FlightRuntime.callProperty(parts, 'push', cast (['Z'] : Array<Dynamic>));
} } } } }
}] : Array<Dynamic>));
    return cast FlightRuntime.join(parts, '');
    return cast null;
  }

  public static function isSvgCommandLetter__svgPathData(c:String):Bool {
    return cast !FlightRuntime.strictEquals(FlightRuntime.callProperty('MmLlHhVvCcSsQqTtAaZz', 'indexOf', cast ([c] : Array<Dynamic>)), -1.0);
    return cast null;
  }

  public static function parseSvgPathData(d:String):Null<Path> {
    var path:Dynamic = cast FlightRuntime.UNDEFINED;
    path = FlightRuntime.callValue(createPath, cast ([] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(appendSvgPathData, cast ([path, d] : Array<Dynamic>))))) { return cast null; }
    return cast path;
    return cast null;
  }
}
