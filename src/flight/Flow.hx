// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Types.FlowStack;
import flight.Types.FlowState;

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.Flow")
class Flow {
  @:keep public static function clearFlowStack(stack:FlowStack):Void {
    var states:Dynamic = cast FlightRuntime.UNDEFINED;
    states = FlightRuntime.field(stack, 'states');
    {
      var i:Dynamic = (FlightRuntime.field(states, 'length') - 1.0);
      while (FlightRuntime.truthy(FlightRuntime.compare(i, 0.0, '>='))) {
        FlightRuntime.callOptionalProperty(FlightRuntime.getIndex(states, i), 'onExit', cast ([] : Array<Dynamic>));
        i--;
      }
    }
    FlightRuntime.setLength(states, 0.0);
  }

  @:keep public static function createFlowStack():FlowStack {
    return cast { states: cast ([] : Array<Dynamic>) };
    return cast null;
  }

  @:keep public static function getActiveFlowState(stack:FlowStack):Null<FlowState> {
    var states:Dynamic = cast FlightRuntime.UNDEFINED;
    states = FlightRuntime.field(stack, 'states');
    return cast FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(states, 'length'), 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(states, (FlightRuntime.field(states, 'length') - 1.0)), function():Dynamic return cast null);
    return cast null;
  }

  @:keep public static function getFlowStackDepth(stack:FlowStack):Float {
    return cast FlightRuntime.field(FlightRuntime.field(stack, 'states'), 'length');
    return cast null;
  }

  @:keep public static function getFlowStackVisibleStates(stack:FlowStack, out:Array<FlowState>):Void {
    var states:Dynamic = cast FlightRuntime.UNDEFINED;
    var top:Dynamic = cast FlightRuntime.UNDEFINED;
    var lowest:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.setLength(out, 0.0);
    states = FlightRuntime.field(stack, 'states');
    top = (FlightRuntime.field(states, 'length') - 1.0);
    if (FlightRuntime.truthy(FlightRuntime.compare(top, 0.0, '<'))) {
  return;
}
    lowest = top;
    while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(lowest, 0.0, '>'), function():Dynamic return cast FlightRuntime.field(FlightRuntime.getIndex(states, lowest), 'renderBelow')))) {
  lowest--;
}
    {
      var i:Dynamic = lowest;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, top, '<='))) {
        FlightRuntime.callProperty(out, 'push', cast ([FlightRuntime.getIndex(states, i)] : Array<Dynamic>));
        i++;
      }
    }
  }

  @:keep public static function popFlowState(stack:FlowStack):Null<FlowState> {
    var states:Dynamic = cast FlightRuntime.UNDEFINED;
    var popped:Dynamic = cast FlightRuntime.UNDEFINED;
    var revealed:Dynamic = cast FlightRuntime.UNDEFINED;
    states = FlightRuntime.field(stack, 'states');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(states, 'length'), 0.0))) {
  return cast null;
}
    popped = (cast FlightRuntime.callProperty(states, 'pop', cast ([] : Array<Dynamic>)) : FlowState);
    FlightRuntime.callOptionalProperty(popped, 'onExit', cast ([] : Array<Dynamic>));
    revealed = FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(states, 'length'), 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(states, (FlightRuntime.field(states, 'length') - 1.0)), function():Dynamic return cast null);
    FlightRuntime.callOptionalProperty(revealed, 'onResume', cast ([] : Array<Dynamic>));
    return cast popped;
    return cast null;
  }

  @:keep public static function pushFlowState(stack:FlowStack, state:FlowState):Void {
    var states:Dynamic = cast FlightRuntime.UNDEFINED;
    var previousTop:Dynamic = cast FlightRuntime.UNDEFINED;
    states = FlightRuntime.field(stack, 'states');
    previousTop = FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(states, 'length'), 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(states, (FlightRuntime.field(states, 'length') - 1.0)), function():Dynamic return cast null);
    FlightRuntime.callOptionalProperty(previousTop, 'onPause', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(states, 'push', cast ([state] : Array<Dynamic>));
    FlightRuntime.callOptionalProperty(state, 'onEnter', cast ([] : Array<Dynamic>));
  }

  @:keep public static function replaceFlowState(stack:FlowStack, state:FlowState):Void {
    var states:Dynamic = cast FlightRuntime.UNDEFINED;
    states = FlightRuntime.field(stack, 'states');
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(states, 'length'), 0.0, '>'))) {
  var previousTop:Dynamic = (cast FlightRuntime.callProperty(states, 'pop', cast ([] : Array<Dynamic>)) : FlowState);
  FlightRuntime.callOptionalProperty(previousTop, 'onExit', cast ([] : Array<Dynamic>));
}
    FlightRuntime.callProperty(states, 'push', cast ([state] : Array<Dynamic>));
    FlightRuntime.callOptionalProperty(state, 'onEnter', cast ([] : Array<Dynamic>));
  }

  @:keep public static function updateFlowStack(stack:FlowStack, deltaTime:Float):Void {
    var states:Dynamic = cast FlightRuntime.UNDEFINED;
    var index:Dynamic = cast FlightRuntime.UNDEFINED;
    states = FlightRuntime.field(stack, 'states');
    index = (FlightRuntime.field(states, 'length') - 1.0);
    if (FlightRuntime.truthy(FlightRuntime.compare(index, 0.0, '<'))) {
  return;
}
    FlightRuntime.callOptionalProperty(FlightRuntime.getIndex(states, index), 'onUpdate', cast ([deltaTime] : Array<Dynamic>));
    while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(index, 0.0, '>'), function():Dynamic return cast FlightRuntime.field(FlightRuntime.getIndex(states, index), 'updateBelow')))) {
  index--;
  FlightRuntime.callOptionalProperty(FlightRuntime.getIndex(states, index), 'onUpdate', cast ([deltaTime] : Array<Dynamic>));
}
  }
}
