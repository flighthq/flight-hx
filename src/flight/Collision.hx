// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Geometry.createVector2;
import flight.Geometry.normalizeVector2;
import flight.Types.CollisionAabb;
import flight.Types.CollisionCircle;
import flight.Types.CollisionManifold;
import flight.Types.CollisionObb;
import flight.Types.CollisionPolygon;
import flight.Types.CollisionSegment;
import flight.Types.CollisionShape;
import flight.Types.CollisionShapeKind;

@:expose("flight.Collision")
class Collision {
  public static function accumulatePolygonAxes__shapeCollision(sx:Dynamic, sn:Float, ax:Dynamic, an:Float, bx:Dynamic, bn:Float, out:CollisionManifold):Bool {
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, sn, '<'))) {
        var j:Dynamic = ((i + 1.0) % sn);
        var x0:Dynamic = FlightRuntime.getIndex(sx, (Std.int(i) << Std.int(1.0)));
        var y0:Dynamic = FlightRuntime.getIndex(sx, ((Std.int(i) << Std.int(1.0)) + 1.0));
        var x1:Dynamic = FlightRuntime.getIndex(sx, (Std.int(j) << Std.int(1.0)));
        var y1:Dynamic = FlightRuntime.getIndex(sx, ((Std.int(j) << Std.int(1.0)) + 1.0));
        FlightRuntime.setField(Collision.scratchAxis__shapeCollision, 'x', (y1 - y0));
        FlightRuntime.setField(Collision.scratchAxis__shapeCollision, 'y', -(x1 - x0));
        var len:Dynamic = FlightRuntime.callValue(normalizeVector2, cast ([Collision.scratchAxis__shapeCollision, Collision.scratchAxis__shapeCollision] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.compare(len, Collision.EPS__shapeCollision, '<'))) { i++; continue; }
        var axisX:Dynamic = FlightRuntime.field(Collision.scratchAxis__shapeCollision, 'x');
        var axisY:Dynamic = FlightRuntime.field(Collision.scratchAxis__shapeCollision, 'y');
        var overlap:Dynamic = FlightRuntime.callValue(Collision.polygonAxisOverlap__shapeCollision, cast ([axisX, axisY, ax, an, bx, bn] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.compare(overlap, 0.0, '<='))) {
  FlightRuntime.callValue(clearCollisionManifold, cast ([out] : Array<Dynamic>));
  return cast false;
}
        if (FlightRuntime.truthy(FlightRuntime.compare(overlap, FlightRuntime.field(Collision.minOverlapAxis__shapeCollision, 'overlap'), '<'))) {
  FlightRuntime.setField(Collision.minOverlapAxis__shapeCollision, 'overlap', overlap);
  FlightRuntime.setField(Collision.minOverlapAxis__shapeCollision, 'x', axisX);
  FlightRuntime.setField(Collision.minOverlapAxis__shapeCollision, 'y', axisY);
}
        i++;
      }
    }
    return cast true;
    return cast null;
  }

  public static function circleAabbOverlap__shapeCollision(cx:Float, cy:Float, radius:Float, minX:Float, minY:Float, maxX:Float, maxY:Float, out:CollisionManifold):Bool {
    var closestX:Dynamic = cast FlightRuntime.UNDEFINED;
    var closestY:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var distSquared:Dynamic = cast FlightRuntime.UNDEFINED;
    var left:Dynamic = cast FlightRuntime.UNDEFINED;
    var right:Dynamic = cast FlightRuntime.UNDEFINED;
    var bottom:Dynamic = cast FlightRuntime.UNDEFINED;
    var top:Dynamic = cast FlightRuntime.UNDEFINED;
    var min:Dynamic = cast FlightRuntime.UNDEFINED;
    var normalX:Dynamic = cast FlightRuntime.UNDEFINED;
    var normalY:Dynamic = cast FlightRuntime.UNDEFINED;
    closestX = FlightRuntime.select(FlightRuntime.compare(cx, minX, '<'), function():Dynamic return cast minX, function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(cx, maxX, '>'), function():Dynamic return cast maxX, function():Dynamic return cast cx));
    closestY = FlightRuntime.select(FlightRuntime.compare(cy, minY, '<'), function():Dynamic return cast minY, function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(cy, maxY, '>'), function():Dynamic return cast maxY, function():Dynamic return cast cy));
    dx = (cx - closestX);
    dy = (cy - closestY);
    distSquared = ((dx * dx) + (dy * dy));
    if (FlightRuntime.truthy(FlightRuntime.compare(distSquared, (Collision.EPS__shapeCollision * Collision.EPS__shapeCollision), '>'))) {
  var dist:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([distSquared] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.compare(dist, radius, '>='))) {
  FlightRuntime.callValue(clearCollisionManifold, cast ([out] : Array<Dynamic>));
  return cast false;
}
  var inv:Dynamic = (1.0 / dist);
  FlightRuntime.setField(out, 'normalX', (dx * inv));
  FlightRuntime.setField(out, 'normalY', (dy * inv));
  FlightRuntime.setField(out, 'depth', (radius - dist));
  FlightRuntime.setField(out, 'overlapping', true);
  return cast true;
}
    left = (cx - minX);
    right = (maxX - cx);
    bottom = (cy - minY);
    top = (maxY - cy);
    min = left;
    normalX = -1.0;
    normalY = 0.0;
    if (FlightRuntime.truthy(FlightRuntime.compare(right, min, '<'))) {
  (min = cast (right : Dynamic));
  (normalX = cast (1.0 : Dynamic));
  (normalY = cast (0.0 : Dynamic));
}
    if (FlightRuntime.truthy(FlightRuntime.compare(bottom, min, '<'))) {
  (min = cast (bottom : Dynamic));
  (normalX = cast (0.0 : Dynamic));
  (normalY = cast (-1.0 : Dynamic));
}
    if (FlightRuntime.truthy(FlightRuntime.compare(top, min, '<'))) {
  (min = cast (top : Dynamic));
  (normalX = cast (0.0 : Dynamic));
  (normalY = cast (1.0 : Dynamic));
}
    FlightRuntime.setField(out, 'normalX', normalX);
    FlightRuntime.setField(out, 'normalY', normalY);
    FlightRuntime.setField(out, 'depth', (min + radius));
    FlightRuntime.setField(out, 'overlapping', true);
    return cast true;
    return cast null;
  }

  public static function circlePolygonAxisOverlap__shapeCollision(axisX:Float, axisY:Float, cx:Float, cy:Float, radius:Float, px:Dynamic, pn:Float):Float {
    var minP:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxP:Dynamic = cast FlightRuntime.UNDEFINED;
    var c:Dynamic = cast FlightRuntime.UNDEFINED;
    var cMin:Dynamic = cast FlightRuntime.UNDEFINED;
    var cMax:Dynamic = cast FlightRuntime.UNDEFINED;
    var penLow:Dynamic = cast FlightRuntime.UNDEFINED;
    var penHigh:Dynamic = cast FlightRuntime.UNDEFINED;
    minP = HxMath.POSITIVE_INFINITY;
    maxP = -HxMath.POSITIVE_INFINITY;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, pn, '<'))) {
        var d:Dynamic = ((FlightRuntime.getIndex(px, (Std.int(i) << Std.int(1.0))) * axisX) + (FlightRuntime.getIndex(px, ((Std.int(i) << Std.int(1.0)) + 1.0)) * axisY));
        if (FlightRuntime.truthy(FlightRuntime.compare(d, minP, '<'))) { (minP = cast (d : Dynamic)); }
        if (FlightRuntime.truthy(FlightRuntime.compare(d, maxP, '>'))) { (maxP = cast (d : Dynamic)); }
        i++;
      }
    }
    c = ((cx * axisX) + (cy * axisY));
    cMin = (c - radius);
    cMax = (c + radius);
    penLow = (maxP - cMin);
    penHigh = (cMax - minP);
    return cast FlightRuntime.select(FlightRuntime.compare(penLow, penHigh, '<'), function():Dynamic return cast penLow, function():Dynamic return cast penHigh);
    return cast null;
  }

  public static function clearCollisionManifold(out:CollisionManifold):Void {
    FlightRuntime.setField(out, 'overlapping', false);
    FlightRuntime.setField(out, 'normalX', 0.0);
    FlightRuntime.setField(out, 'normalY', 0.0);
    FlightRuntime.setField(out, 'depth', 0.0);
  }

  public static final clipRange__segmentCollision:Dynamic = { t0: 0.0, t1: 1.0 };

  public static function clipSegmentSlab__segmentCollision(p:Float, q:Float):Bool {
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([p] : Array<Dynamic>)), Collision.EPS__segmentCollision, '<'))) {
  return cast FlightRuntime.compare(q, 0.0, '>=');
}
    r = (q / p);
    if (FlightRuntime.truthy(FlightRuntime.compare(p, 0.0, '<'))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(r, FlightRuntime.field(Collision.clipRange__segmentCollision, 't1'), '>'))) { return cast false; }
  if (FlightRuntime.truthy(FlightRuntime.compare(r, FlightRuntime.field(Collision.clipRange__segmentCollision, 't0'), '>'))) { FlightRuntime.setField(Collision.clipRange__segmentCollision, 't0', r); }
} else {
  if (FlightRuntime.truthy(FlightRuntime.compare(r, FlightRuntime.field(Collision.clipRange__segmentCollision, 't0'), '<'))) { return cast false; }
  if (FlightRuntime.truthy(FlightRuntime.compare(r, FlightRuntime.field(Collision.clipRange__segmentCollision, 't1'), '<'))) { FlightRuntime.setField(Collision.clipRange__segmentCollision, 't1', r); }
}
    return cast true;
    return cast null;
  }

  public static function createCollisionManifold():CollisionManifold {
    return cast { overlapping: false, normalX: 0.0, normalY: 0.0, depth: 0.0 };
    return cast null;
  }

  public static final EPS__pointContainment:Dynamic = 1e-9;

  public static final EPS__segmentCollision:Dynamic = 1e-9;

  public static final EPS__shapeCollision:Dynamic = 1e-9;

  public static function getCollisionShapeContainsPoint(shape:CollisionShape, x:Float, y:Float):Bool {
    {
      var __switchValue = FlightRuntime.field(shape, 'kind');
      if (__switchValue == 'circle') {
        {
          var dx:Dynamic = (x - FlightRuntime.field(shape, 'x'));
          var dy:Dynamic = (y - FlightRuntime.field(shape, 'y'));
          return cast FlightRuntime.compare(((dx * dx) + (dy * dy)), (FlightRuntime.field(shape, 'radius') * FlightRuntime.field(shape, 'radius')), '<=');
        }
      }
      else if (__switchValue == 'aabb') {
        return cast FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(x, FlightRuntime.field(shape, 'minX'), '>='), function():Dynamic return cast FlightRuntime.compare(x, FlightRuntime.field(shape, 'maxX'), '<=')), function():Dynamic return cast FlightRuntime.compare(y, FlightRuntime.field(shape, 'minY'), '>=')), function():Dynamic return cast FlightRuntime.compare(y, FlightRuntime.field(shape, 'maxY'), '<='));
      }
      else if (__switchValue == 'obb') {
        {
          var cos:Dynamic = FlightRuntime.callProperty(HxMath, 'cos', cast ([FlightRuntime.field(shape, 'rotation')] : Array<Dynamic>));
          var sin:Dynamic = FlightRuntime.callProperty(HxMath, 'sin', cast ([FlightRuntime.field(shape, 'rotation')] : Array<Dynamic>));
          var dx:Dynamic = (x - FlightRuntime.field(shape, 'x'));
          var dy:Dynamic = (y - FlightRuntime.field(shape, 'y'));
          var localX:Dynamic = ((dx * cos) + (dy * sin));
          var localY:Dynamic = ((-dx * sin) + (dy * cos));
          return cast FlightRuntime.andValue(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([localX] : Array<Dynamic>)), FlightRuntime.field(shape, 'halfW'), '<='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([localY] : Array<Dynamic>)), FlightRuntime.field(shape, 'halfH'), '<='));
        }
      }
      else if (__switchValue == 'polygon') {
        return cast FlightRuntime.callValue(Collision.isPointInConvexPolygon__pointContainment, cast ([x, y, FlightRuntime.field(shape, 'points'), (Std.int(FlightRuntime.field(FlightRuntime.field(shape, 'points'), 'length')) >> Std.int(1.0))] : Array<Dynamic>));
      }
      else if (__switchValue == 'segment') {
        {
          var dx:Dynamic = (FlightRuntime.field(shape, 'x1') - FlightRuntime.field(shape, 'x0'));
          var dy:Dynamic = (FlightRuntime.field(shape, 'y1') - FlightRuntime.field(shape, 'y0'));
          var lengthSquared:Dynamic = ((dx * dx) + (dy * dy));
          var t:Dynamic = 0.0;
          if (FlightRuntime.truthy(FlightRuntime.compare(lengthSquared, Collision.EPS__pointContainment, '>'))) {
  (t = cast (((((x - FlightRuntime.field(shape, 'x0')) * dx) + ((y - FlightRuntime.field(shape, 'y0')) * dy)) / lengthSquared) : Dynamic));
  (t = cast (FlightRuntime.select(FlightRuntime.compare(t, 0.0, '<'), function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(t, 1.0, '>'), function():Dynamic return cast 1.0, function():Dynamic return cast t)) : Dynamic));
}
          var closestX:Dynamic = (FlightRuntime.field(shape, 'x0') + (t * dx));
          var closestY:Dynamic = (FlightRuntime.field(shape, 'y0') + (t * dy));
          var ddx:Dynamic = (x - closestX);
          var ddy:Dynamic = (y - closestY);
          return cast FlightRuntime.compare(((ddx * ddx) + (ddy * ddy)), Collision.EPS__pointContainment, '<=');
        }
      }
      else if (__switchValue == 'point') {
        {
          var dx:Dynamic = (x - FlightRuntime.field(shape, 'x'));
          var dy:Dynamic = (y - FlightRuntime.field(shape, 'y'));
          return cast FlightRuntime.compare(((dx * dx) + (dy * dy)), Collision.EPS__pointContainment, '<=');
        }
      }
      else  {
        return cast false;
      }
    }
    return cast null;
  }

  public static function isPointInConvexPolygon__pointContainment(x:Float, y:Float, px:Array<Float>, pn:Float):Bool {
    var positive:Dynamic = cast FlightRuntime.UNDEFINED;
    var negative:Dynamic = cast FlightRuntime.UNDEFINED;
    positive = false;
    negative = false;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, pn, '<'))) {
        var j:Dynamic = ((i + 1.0) % pn);
        var x0:Dynamic = FlightRuntime.getIndex(px, (Std.int(i) << Std.int(1.0)));
        var y0:Dynamic = FlightRuntime.getIndex(px, ((Std.int(i) << Std.int(1.0)) + 1.0));
        var x1:Dynamic = FlightRuntime.getIndex(px, (Std.int(j) << Std.int(1.0)));
        var y1:Dynamic = FlightRuntime.getIndex(px, ((Std.int(j) << Std.int(1.0)) + 1.0));
        var cross:Dynamic = (((x1 - x0) * (y - y0)) - ((y1 - y0) * (x - x0)));
        if (FlightRuntime.truthy(FlightRuntime.compare(cross, Collision.EPS__pointContainment, '>'))) { (positive = cast (true : Dynamic)); } else { if (FlightRuntime.truthy(FlightRuntime.compare(cross, -Collision.EPS__pointContainment, '<'))) { (negative = cast (true : Dynamic)); } }
        if (FlightRuntime.truthy(FlightRuntime.andValue(positive, function():Dynamic return cast negative))) { return cast false; }
        i++;
      }
    }
    return cast true;
    return cast null;
  }

  public static function isPointInConvexPolygon__segmentCollision(x:Float, y:Float, px:Array<Float>, pn:Float):Bool {
    var positive:Dynamic = cast FlightRuntime.UNDEFINED;
    var negative:Dynamic = cast FlightRuntime.UNDEFINED;
    positive = false;
    negative = false;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, pn, '<'))) {
        var j:Dynamic = ((i + 1.0) % pn);
        var x0:Dynamic = FlightRuntime.getIndex(px, (Std.int(i) << Std.int(1.0)));
        var y0:Dynamic = FlightRuntime.getIndex(px, ((Std.int(i) << Std.int(1.0)) + 1.0));
        var x1:Dynamic = FlightRuntime.getIndex(px, (Std.int(j) << Std.int(1.0)));
        var y1:Dynamic = FlightRuntime.getIndex(px, ((Std.int(j) << Std.int(1.0)) + 1.0));
        var cross:Dynamic = (((x1 - x0) * (y - y0)) - ((y1 - y0) * (x - x0)));
        if (FlightRuntime.truthy(FlightRuntime.compare(cross, Collision.EPS__segmentCollision, '>'))) { (positive = cast (true : Dynamic)); } else { if (FlightRuntime.truthy(FlightRuntime.compare(cross, -Collision.EPS__segmentCollision, '<'))) { (negative = cast (true : Dynamic)); } }
        if (FlightRuntime.truthy(FlightRuntime.andValue(positive, function():Dynamic return cast negative))) { return cast false; }
        i++;
      }
    }
    return cast true;
    return cast null;
  }

  public static function isSegmentOverlappingBox__segmentCollision(x0:Float, y0:Float, x1:Float, y1:Float, minX:Float, minY:Float, maxX:Float, maxY:Float):Bool {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (x1 - x0);
    dy = (y1 - y0);
    FlightRuntime.setField(Collision.clipRange__segmentCollision, 't0', 0.0);
    FlightRuntime.setField(Collision.clipRange__segmentCollision, 't1', 1.0);
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Collision.clipSegmentSlab__segmentCollision, cast ([-dx, (x0 - minX)] : Array<Dynamic>))))) { return cast false; }
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Collision.clipSegmentSlab__segmentCollision, cast ([dx, (maxX - x0)] : Array<Dynamic>))))) { return cast false; }
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Collision.clipSegmentSlab__segmentCollision, cast ([-dy, (y0 - minY)] : Array<Dynamic>))))) { return cast false; }
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Collision.clipSegmentSlab__segmentCollision, cast ([dy, (maxY - y0)] : Array<Dynamic>))))) { return cast false; }
    return cast FlightRuntime.compare(FlightRuntime.field(Collision.clipRange__segmentCollision, 't0'), FlightRuntime.field(Collision.clipRange__segmentCollision, 't1'), '<=');
    return cast null;
  }

  public static function isSegmentsIntersecting__segmentCollision(ax0:Float, ay0:Float, ax1:Float, ay1:Float, bx0:Float, by0:Float, bx1:Float, by1:Float):Bool {
    var d1x:Dynamic = cast FlightRuntime.UNDEFINED;
    var d1y:Dynamic = cast FlightRuntime.UNDEFINED;
    var d2x:Dynamic = cast FlightRuntime.UNDEFINED;
    var d2y:Dynamic = cast FlightRuntime.UNDEFINED;
    var denom:Dynamic = cast FlightRuntime.UNDEFINED;
    var ex:Dynamic = cast FlightRuntime.UNDEFINED;
    var ey:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var u:Dynamic = cast FlightRuntime.UNDEFINED;
    d1x = (ax1 - ax0);
    d1y = (ay1 - ay0);
    d2x = (bx1 - bx0);
    d2y = (by1 - by0);
    denom = ((d1x * d2y) - (d1y * d2x));
    ex = (bx0 - ax0);
    ey = (by0 - ay0);
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([denom] : Array<Dynamic>)), Collision.EPS__segmentCollision, '<'))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([((ex * d1y) - (ey * d1x))] : Array<Dynamic>)), Collision.EPS__segmentCollision, '>'))) { return cast false; }
  var lengthSquared:Dynamic = ((d1x * d1x) + (d1y * d1y));
  if (FlightRuntime.truthy(FlightRuntime.compare(lengthSquared, Collision.EPS__segmentCollision, '<'))) {
  var bLengthSquared:Dynamic = ((d2x * d2x) + (d2y * d2y));
  if (FlightRuntime.truthy(FlightRuntime.compare(bLengthSquared, Collision.EPS__segmentCollision, '<'))) {
  return cast FlightRuntime.andValue(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([ex] : Array<Dynamic>)), Collision.EPS__segmentCollision, '<'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([ey] : Array<Dynamic>)), Collision.EPS__segmentCollision, '<'));
}
  var tb:Dynamic = ((((ax0 - bx0) * d2x) + ((ay0 - by0) * d2y)) / bLengthSquared);
  (tb = cast (FlightRuntime.select(FlightRuntime.compare(tb, 0.0, '<'), function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(tb, 1.0, '>'), function():Dynamic return cast 1.0, function():Dynamic return cast tb)) : Dynamic));
  var qx:Dynamic = (bx0 + (tb * d2x));
  var qy:Dynamic = (by0 + (tb * d2y));
  return cast FlightRuntime.compare((((ax0 - qx) * (ax0 - qx)) + ((ay0 - qy) * (ay0 - qy))), Collision.EPS__segmentCollision, '<');
}
  var t0:Dynamic = (((ex * d1x) + (ey * d1y)) / lengthSquared);
  var t1:Dynamic = ((((bx1 - ax0) * d1x) + ((by1 - ay0) * d1y)) / lengthSquared);
  var lo:Dynamic = FlightRuntime.select(FlightRuntime.compare(t0, t1, '<'), function():Dynamic return cast t0, function():Dynamic return cast t1);
  var hi:Dynamic = FlightRuntime.select(FlightRuntime.compare(t0, t1, '<'), function():Dynamic return cast t1, function():Dynamic return cast t0);
  return cast FlightRuntime.andValue(FlightRuntime.compare(hi, -Collision.EPS__segmentCollision, '>='), function():Dynamic return cast FlightRuntime.compare(lo, (1.0 + Collision.EPS__segmentCollision), '<='));
}
    t = (((ex * d2y) - (ey * d2x)) / denom);
    u = (((ex * d1y) - (ey * d1x)) / denom);
    return cast FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(t, -Collision.EPS__segmentCollision, '>='), function():Dynamic return cast FlightRuntime.compare(t, (1.0 + Collision.EPS__segmentCollision), '<=')), function():Dynamic return cast FlightRuntime.compare(u, -Collision.EPS__segmentCollision, '>=')), function():Dynamic return cast FlightRuntime.compare(u, (1.0 + Collision.EPS__segmentCollision), '<='));
    return cast null;
  }

  public static final minOverlapAxis__shapeCollision:Dynamic = { overlap: HxMath.POSITIVE_INFINITY, x: 0.0, y: 0.0 };

  public static function polygonAxisOverlap__shapeCollision(axisX:Float, axisY:Float, ax:Dynamic, an:Float, bx:Dynamic, bn:Float):Float {
    var minA:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxA:Dynamic = cast FlightRuntime.UNDEFINED;
    var minB:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxB:Dynamic = cast FlightRuntime.UNDEFINED;
    var penLow:Dynamic = cast FlightRuntime.UNDEFINED;
    var penHigh:Dynamic = cast FlightRuntime.UNDEFINED;
    minA = HxMath.POSITIVE_INFINITY;
    maxA = -HxMath.POSITIVE_INFINITY;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, an, '<'))) {
        var d:Dynamic = ((FlightRuntime.getIndex(ax, (Std.int(i) << Std.int(1.0))) * axisX) + (FlightRuntime.getIndex(ax, ((Std.int(i) << Std.int(1.0)) + 1.0)) * axisY));
        if (FlightRuntime.truthy(FlightRuntime.compare(d, minA, '<'))) { (minA = cast (d : Dynamic)); }
        if (FlightRuntime.truthy(FlightRuntime.compare(d, maxA, '>'))) { (maxA = cast (d : Dynamic)); }
        i++;
      }
    }
    minB = HxMath.POSITIVE_INFINITY;
    maxB = -HxMath.POSITIVE_INFINITY;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, bn, '<'))) {
        var d:Dynamic = ((FlightRuntime.getIndex(bx, (Std.int(i) << Std.int(1.0))) * axisX) + (FlightRuntime.getIndex(bx, ((Std.int(i) << Std.int(1.0)) + 1.0)) * axisY));
        if (FlightRuntime.truthy(FlightRuntime.compare(d, minB, '<'))) { (minB = cast (d : Dynamic)); }
        if (FlightRuntime.truthy(FlightRuntime.compare(d, maxB, '>'))) { (maxB = cast (d : Dynamic)); }
        i++;
      }
    }
    penLow = (maxA - minB);
    penHigh = (maxB - minA);
    return cast FlightRuntime.select(FlightRuntime.compare(penLow, penHigh, '<'), function():Dynamic return cast penLow, function():Dynamic return cast penHigh);
    return cast null;
  }

  public static function satCircleConvexOverlap__shapeCollision(cx:Float, cy:Float, radius:Float, px:Dynamic, pn:Float, out:CollisionManifold):Bool {
    var minOverlap:Dynamic = cast FlightRuntime.UNDEFINED;
    var normalX:Dynamic = cast FlightRuntime.UNDEFINED;
    var normalY:Dynamic = cast FlightRuntime.UNDEFINED;
    var nearestX:Dynamic = cast FlightRuntime.UNDEFINED;
    var nearestY:Dynamic = cast FlightRuntime.UNDEFINED;
    var nearestDistSquared:Dynamic = cast FlightRuntime.UNDEFINED;
    var vertexAxisLen:Dynamic = cast FlightRuntime.UNDEFINED;
    var centroidX:Dynamic = cast FlightRuntime.UNDEFINED;
    var centroidY:Dynamic = cast FlightRuntime.UNDEFINED;
    minOverlap = HxMath.POSITIVE_INFINITY;
    normalX = 0.0;
    normalY = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, pn, '<'))) {
        var j:Dynamic = ((i + 1.0) % pn);
        var x0:Dynamic = FlightRuntime.getIndex(px, (Std.int(i) << Std.int(1.0)));
        var y0:Dynamic = FlightRuntime.getIndex(px, ((Std.int(i) << Std.int(1.0)) + 1.0));
        var x1:Dynamic = FlightRuntime.getIndex(px, (Std.int(j) << Std.int(1.0)));
        var y1:Dynamic = FlightRuntime.getIndex(px, ((Std.int(j) << Std.int(1.0)) + 1.0));
        FlightRuntime.setField(Collision.scratchAxis__shapeCollision, 'x', (y1 - y0));
        FlightRuntime.setField(Collision.scratchAxis__shapeCollision, 'y', -(x1 - x0));
        var len:Dynamic = FlightRuntime.callValue(normalizeVector2, cast ([Collision.scratchAxis__shapeCollision, Collision.scratchAxis__shapeCollision] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.compare(len, Collision.EPS__shapeCollision, '<'))) { i++; continue; }
        var axisX:Dynamic = FlightRuntime.field(Collision.scratchAxis__shapeCollision, 'x');
        var axisY:Dynamic = FlightRuntime.field(Collision.scratchAxis__shapeCollision, 'y');
        var overlap:Dynamic = FlightRuntime.callValue(Collision.circlePolygonAxisOverlap__shapeCollision, cast ([axisX, axisY, cx, cy, radius, px, pn] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.compare(overlap, 0.0, '<='))) {
  FlightRuntime.callValue(clearCollisionManifold, cast ([out] : Array<Dynamic>));
  return cast false;
}
        if (FlightRuntime.truthy(FlightRuntime.compare(overlap, minOverlap, '<'))) {
  (minOverlap = cast (overlap : Dynamic));
  (normalX = cast (axisX : Dynamic));
  (normalY = cast (axisY : Dynamic));
}
        i++;
      }
    }
    nearestX = 0.0;
    nearestY = 0.0;
    nearestDistSquared = HxMath.POSITIVE_INFINITY;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, pn, '<'))) {
        var vx:Dynamic = FlightRuntime.getIndex(px, (Std.int(i) << Std.int(1.0)));
        var vy:Dynamic = FlightRuntime.getIndex(px, ((Std.int(i) << Std.int(1.0)) + 1.0));
        var ddx:Dynamic = (cx - vx);
        var ddy:Dynamic = (cy - vy);
        var d2:Dynamic = ((ddx * ddx) + (ddy * ddy));
        if (FlightRuntime.truthy(FlightRuntime.compare(d2, nearestDistSquared, '<'))) {
  (nearestDistSquared = cast (d2 : Dynamic));
  (nearestX = cast (vx : Dynamic));
  (nearestY = cast (vy : Dynamic));
}
        i++;
      }
    }
    FlightRuntime.setField(Collision.scratchAxis__shapeCollision, 'x', (cx - nearestX));
    FlightRuntime.setField(Collision.scratchAxis__shapeCollision, 'y', (cy - nearestY));
    vertexAxisLen = FlightRuntime.callValue(normalizeVector2, cast ([Collision.scratchAxis__shapeCollision, Collision.scratchAxis__shapeCollision] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(vertexAxisLen, Collision.EPS__shapeCollision, '>'))) {
  var axisX:Dynamic = FlightRuntime.field(Collision.scratchAxis__shapeCollision, 'x');
  var axisY:Dynamic = FlightRuntime.field(Collision.scratchAxis__shapeCollision, 'y');
  var overlap:Dynamic = FlightRuntime.callValue(Collision.circlePolygonAxisOverlap__shapeCollision, cast ([axisX, axisY, cx, cy, radius, px, pn] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.compare(overlap, 0.0, '<='))) {
  FlightRuntime.callValue(clearCollisionManifold, cast ([out] : Array<Dynamic>));
  return cast false;
}
  if (FlightRuntime.truthy(FlightRuntime.compare(overlap, minOverlap, '<'))) {
  (minOverlap = cast (overlap : Dynamic));
  (normalX = cast (axisX : Dynamic));
  (normalY = cast (axisY : Dynamic));
}
}
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(minOverlap, HxMath.POSITIVE_INFINITY))) {
  FlightRuntime.callValue(clearCollisionManifold, cast ([out] : Array<Dynamic>));
  return cast false;
}
    centroidX = 0.0;
    centroidY = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, pn, '<'))) {
        (centroidX = cast ((centroidX + FlightRuntime.getIndex(px, (Std.int(i) << Std.int(1.0)))) : Dynamic));
        (centroidY = cast ((centroidY + FlightRuntime.getIndex(px, ((Std.int(i) << Std.int(1.0)) + 1.0))) : Dynamic));
        i++;
      }
    }
    (centroidX = cast ((centroidX / pn) : Dynamic));
    (centroidY = cast ((centroidY / pn) : Dynamic));
    if (FlightRuntime.truthy(FlightRuntime.compare(((normalX * (cx - centroidX)) + (normalY * (cy - centroidY))), 0.0, '<'))) {
  (normalX = cast (-normalX : Dynamic));
  (normalY = cast (-normalY : Dynamic));
}
    FlightRuntime.setField(out, 'normalX', normalX);
    FlightRuntime.setField(out, 'normalY', normalY);
    FlightRuntime.setField(out, 'depth', minOverlap);
    FlightRuntime.setField(out, 'overlapping', true);
    return cast true;
    return cast null;
  }

  public static function satConvexOverlap__shapeCollision(ax:Dynamic, an:Float, bx:Dynamic, bn:Float, out:CollisionManifold):Bool {
    var aCentroidX:Dynamic = cast FlightRuntime.UNDEFINED;
    var aCentroidY:Dynamic = cast FlightRuntime.UNDEFINED;
    var bCentroidX:Dynamic = cast FlightRuntime.UNDEFINED;
    var bCentroidY:Dynamic = cast FlightRuntime.UNDEFINED;
    var normalX:Dynamic = cast FlightRuntime.UNDEFINED;
    var normalY:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.setField(Collision.minOverlapAxis__shapeCollision, 'overlap', HxMath.POSITIVE_INFINITY);
    FlightRuntime.setField(Collision.minOverlapAxis__shapeCollision, 'x', 0.0);
    FlightRuntime.setField(Collision.minOverlapAxis__shapeCollision, 'y', 0.0);
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Collision.accumulatePolygonAxes__shapeCollision, cast ([ax, an, ax, an, bx, bn, out] : Array<Dynamic>))))) { return cast false; }
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Collision.accumulatePolygonAxes__shapeCollision, cast ([bx, bn, ax, an, bx, bn, out] : Array<Dynamic>))))) { return cast false; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(Collision.minOverlapAxis__shapeCollision, 'overlap'), HxMath.POSITIVE_INFINITY))) {
  FlightRuntime.callValue(clearCollisionManifold, cast ([out] : Array<Dynamic>));
  return cast false;
}
    aCentroidX = 0.0;
    aCentroidY = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, an, '<'))) {
        (aCentroidX = cast ((aCentroidX + FlightRuntime.getIndex(ax, (Std.int(i) << Std.int(1.0)))) : Dynamic));
        (aCentroidY = cast ((aCentroidY + FlightRuntime.getIndex(ax, ((Std.int(i) << Std.int(1.0)) + 1.0))) : Dynamic));
        i++;
      }
    }
    (aCentroidX = cast ((aCentroidX / an) : Dynamic));
    (aCentroidY = cast ((aCentroidY / an) : Dynamic));
    bCentroidX = 0.0;
    bCentroidY = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, bn, '<'))) {
        (bCentroidX = cast ((bCentroidX + FlightRuntime.getIndex(bx, (Std.int(i) << Std.int(1.0)))) : Dynamic));
        (bCentroidY = cast ((bCentroidY + FlightRuntime.getIndex(bx, ((Std.int(i) << Std.int(1.0)) + 1.0))) : Dynamic));
        i++;
      }
    }
    (bCentroidX = cast ((bCentroidX / bn) : Dynamic));
    (bCentroidY = cast ((bCentroidY / bn) : Dynamic));
    normalX = FlightRuntime.field(Collision.minOverlapAxis__shapeCollision, 'x');
    normalY = FlightRuntime.field(Collision.minOverlapAxis__shapeCollision, 'y');
    if (FlightRuntime.truthy(FlightRuntime.compare(((normalX * (aCentroidX - bCentroidX)) + (normalY * (aCentroidY - bCentroidY))), 0.0, '<'))) {
  (normalX = cast (-normalX : Dynamic));
  (normalY = cast (-normalY : Dynamic));
}
    FlightRuntime.setField(out, 'normalX', normalX);
    FlightRuntime.setField(out, 'normalY', normalY);
    FlightRuntime.setField(out, 'depth', FlightRuntime.field(Collision.minOverlapAxis__shapeCollision, 'overlap'));
    FlightRuntime.setField(out, 'overlapping', true);
    return cast true;
    return cast null;
  }

  public static final scratchA__shapeCollision:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float64Array'] : Array<Dynamic>)), [8.0]);

  public static final scratchAxis__shapeCollision:Dynamic = FlightRuntime.callValue(createVector2, cast ([] : Array<Dynamic>));

  public static final scratchB__shapeCollision:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float64Array'] : Array<Dynamic>)), [8.0]);

  public static function shapeKindRank__testCollision(kind:CollisionShapeKind):Float {
    {
      var __switchValue = kind;
      if (__switchValue == 'circle') {
        return cast 0.0;
      }
      else if (__switchValue == 'aabb') {
        return cast 1.0;
      }
      else if (__switchValue == 'obb') {
        return cast 2.0;
      }
      else if (__switchValue == 'polygon') {
        return cast 3.0;
      }
      else  {
        return cast -1.0;
      }
    }
    return cast null;
  }

  public static function testAabbAabbCollision(a:CollisionAabb, b:CollisionAabb, out:CollisionManifold):Bool {
    var aMinX:Dynamic = cast FlightRuntime.UNDEFINED;
    var aMinY:Dynamic = cast FlightRuntime.UNDEFINED;
    var aMaxX:Dynamic = cast FlightRuntime.UNDEFINED;
    var aMaxY:Dynamic = cast FlightRuntime.UNDEFINED;
    var bMinX:Dynamic = cast FlightRuntime.UNDEFINED;
    var bMinY:Dynamic = cast FlightRuntime.UNDEFINED;
    var bMaxX:Dynamic = cast FlightRuntime.UNDEFINED;
    var bMaxY:Dynamic = cast FlightRuntime.UNDEFINED;
    var penLeftX:Dynamic = cast FlightRuntime.UNDEFINED;
    var penRightX:Dynamic = cast FlightRuntime.UNDEFINED;
    var overlapX:Dynamic = cast FlightRuntime.UNDEFINED;
    var penDownY:Dynamic = cast FlightRuntime.UNDEFINED;
    var penUpY:Dynamic = cast FlightRuntime.UNDEFINED;
    var overlapY:Dynamic = cast FlightRuntime.UNDEFINED;
    aMinX = FlightRuntime.field(a, 'minX');
    aMinY = FlightRuntime.field(a, 'minY');
    aMaxX = FlightRuntime.field(a, 'maxX');
    aMaxY = FlightRuntime.field(a, 'maxY');
    bMinX = FlightRuntime.field(b, 'minX');
    bMinY = FlightRuntime.field(b, 'minY');
    bMaxX = FlightRuntime.field(b, 'maxX');
    bMaxY = FlightRuntime.field(b, 'maxY');
    penLeftX = (aMaxX - bMinX);
    penRightX = (bMaxX - aMinX);
    overlapX = FlightRuntime.select(FlightRuntime.compare(penLeftX, penRightX, '<'), function():Dynamic return cast penLeftX, function():Dynamic return cast penRightX);
    penDownY = (aMaxY - bMinY);
    penUpY = (bMaxY - aMinY);
    overlapY = FlightRuntime.select(FlightRuntime.compare(penDownY, penUpY, '<'), function():Dynamic return cast penDownY, function():Dynamic return cast penUpY);
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(overlapX, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(overlapY, 0.0, '<=')))) {
  FlightRuntime.callValue(clearCollisionManifold, cast ([out] : Array<Dynamic>));
  return cast false;
}
    if (FlightRuntime.truthy(FlightRuntime.compare(overlapX, overlapY, '<'))) {
  FlightRuntime.setField(out, 'normalX', FlightRuntime.select(FlightRuntime.compare(penLeftX, penRightX, '<'), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0));
  FlightRuntime.setField(out, 'normalY', 0.0);
  FlightRuntime.setField(out, 'depth', overlapX);
} else {
  FlightRuntime.setField(out, 'normalX', 0.0);
  FlightRuntime.setField(out, 'normalY', FlightRuntime.select(FlightRuntime.compare(penDownY, penUpY, '<'), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0));
  FlightRuntime.setField(out, 'depth', overlapY);
}
    FlightRuntime.setField(out, 'overlapping', true);
    return cast true;
    return cast null;
  }

  public static function testAabbObbCollision(a:CollisionAabb, b:CollisionObb, out:CollisionManifold):Bool {
    FlightRuntime.callValue(Collision.writeAabbVertices__shapeCollision, cast ([a, Collision.scratchA__shapeCollision] : Array<Dynamic>));
    FlightRuntime.callValue(Collision.writeObbVertices__shapeCollision, cast ([b, Collision.scratchB__shapeCollision] : Array<Dynamic>));
    return cast FlightRuntime.callValue(Collision.satConvexOverlap__shapeCollision, cast ([Collision.scratchA__shapeCollision, 4.0, Collision.scratchB__shapeCollision, 4.0, out] : Array<Dynamic>));
    return cast null;
  }

  public static function testAabbPolygonCollision(a:CollisionAabb, b:CollisionPolygon, out:CollisionManifold):Bool {
    var bPoints:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.callValue(Collision.writeAabbVertices__shapeCollision, cast ([a, Collision.scratchA__shapeCollision] : Array<Dynamic>));
    bPoints = FlightRuntime.field(b, 'points');
    return cast FlightRuntime.callValue(Collision.satConvexOverlap__shapeCollision, cast ([Collision.scratchA__shapeCollision, 4.0, bPoints, (Std.int(FlightRuntime.field(bPoints, 'length')) >> Std.int(1.0)), out] : Array<Dynamic>));
    return cast null;
  }

  public static function testCircleAabbCollision(a:CollisionCircle, b:CollisionAabb, out:CollisionManifold):Bool {
    return cast FlightRuntime.callValue(Collision.circleAabbOverlap__shapeCollision, cast ([FlightRuntime.field(a, 'x'), FlightRuntime.field(a, 'y'), FlightRuntime.field(a, 'radius'), FlightRuntime.field(b, 'minX'), FlightRuntime.field(b, 'minY'), FlightRuntime.field(b, 'maxX'), FlightRuntime.field(b, 'maxY'), out] : Array<Dynamic>));
    return cast null;
  }

  public static function testCircleCircleCollision(a:CollisionCircle, b:CollisionCircle, out:CollisionManifold):Bool {
    var ax:Dynamic = cast FlightRuntime.UNDEFINED;
    var ay:Dynamic = cast FlightRuntime.UNDEFINED;
    var bx:Dynamic = cast FlightRuntime.UNDEFINED;
    var by:Dynamic = cast FlightRuntime.UNDEFINED;
    var radiusSum:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var distSquared:Dynamic = cast FlightRuntime.UNDEFINED;
    var dist:Dynamic = cast FlightRuntime.UNDEFINED;
    ax = FlightRuntime.field(a, 'x');
    ay = FlightRuntime.field(a, 'y');
    bx = FlightRuntime.field(b, 'x');
    by = FlightRuntime.field(b, 'y');
    radiusSum = (FlightRuntime.field(a, 'radius') + FlightRuntime.field(b, 'radius'));
    dx = (ax - bx);
    dy = (ay - by);
    distSquared = ((dx * dx) + (dy * dy));
    if (FlightRuntime.truthy(FlightRuntime.compare(distSquared, (radiusSum * radiusSum), '>='))) {
  FlightRuntime.callValue(clearCollisionManifold, cast ([out] : Array<Dynamic>));
  return cast false;
}
    dist = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([distSquared] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(dist, Collision.EPS__shapeCollision, '>'))) {
  var inv:Dynamic = (1.0 / dist);
  FlightRuntime.setField(out, 'normalX', (dx * inv));
  FlightRuntime.setField(out, 'normalY', (dy * inv));
  FlightRuntime.setField(out, 'depth', (radiusSum - dist));
} else {
  FlightRuntime.setField(out, 'normalX', 1.0);
  FlightRuntime.setField(out, 'normalY', 0.0);
  FlightRuntime.setField(out, 'depth', radiusSum);
}
    FlightRuntime.setField(out, 'overlapping', true);
    return cast true;
    return cast null;
  }

  public static function testCircleObbCollision(a:CollisionCircle, b:CollisionObb, out:CollisionManifold):Bool {
    var cx:Dynamic = cast FlightRuntime.UNDEFINED;
    var cy:Dynamic = cast FlightRuntime.UNDEFINED;
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    var halfW:Dynamic = cast FlightRuntime.UNDEFINED;
    var halfH:Dynamic = cast FlightRuntime.UNDEFINED;
    var cos:Dynamic = cast FlightRuntime.UNDEFINED;
    var sin:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var localX:Dynamic = cast FlightRuntime.UNDEFINED;
    var localY:Dynamic = cast FlightRuntime.UNDEFINED;
    var localNormalX:Dynamic = cast FlightRuntime.UNDEFINED;
    var localNormalY:Dynamic = cast FlightRuntime.UNDEFINED;
    cx = FlightRuntime.field(a, 'x');
    cy = FlightRuntime.field(a, 'y');
    radius = FlightRuntime.field(a, 'radius');
    halfW = FlightRuntime.field(b, 'halfW');
    halfH = FlightRuntime.field(b, 'halfH');
    cos = FlightRuntime.callProperty(HxMath, 'cos', cast ([FlightRuntime.field(b, 'rotation')] : Array<Dynamic>));
    sin = FlightRuntime.callProperty(HxMath, 'sin', cast ([FlightRuntime.field(b, 'rotation')] : Array<Dynamic>));
    dx = (cx - FlightRuntime.field(b, 'x'));
    dy = (cy - FlightRuntime.field(b, 'y'));
    localX = ((dx * cos) + (dy * sin));
    localY = ((-dx * sin) + (dy * cos));
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Collision.circleAabbOverlap__shapeCollision, cast ([localX, localY, radius, -halfW, -halfH, halfW, halfH, out] : Array<Dynamic>))))) {
  return cast false;
}
    localNormalX = FlightRuntime.field(out, 'normalX');
    localNormalY = FlightRuntime.field(out, 'normalY');
    FlightRuntime.setField(out, 'normalX', ((localNormalX * cos) - (localNormalY * sin)));
    FlightRuntime.setField(out, 'normalY', ((localNormalX * sin) + (localNormalY * cos)));
    return cast true;
    return cast null;
  }

  public static function testCirclePolygonCollision(a:CollisionCircle, b:CollisionPolygon, out:CollisionManifold):Bool {
    var points:Dynamic = cast FlightRuntime.UNDEFINED;
    points = FlightRuntime.field(b, 'points');
    return cast FlightRuntime.callValue(Collision.satCircleConvexOverlap__shapeCollision, cast ([FlightRuntime.field(a, 'x'), FlightRuntime.field(a, 'y'), FlightRuntime.field(a, 'radius'), points, (Std.int(FlightRuntime.field(points, 'length')) >> Std.int(1.0)), out] : Array<Dynamic>));
    return cast null;
  }

  public static function testCollision(a:CollisionShape, b:CollisionShape, out:CollisionManifold):Bool {
    var rankA:Dynamic = cast FlightRuntime.UNDEFINED;
    var rankB:Dynamic = cast FlightRuntime.UNDEFINED;
    var swapped:Dynamic = cast FlightRuntime.UNDEFINED;
    var lo:Dynamic = cast FlightRuntime.UNDEFINED;
    var hi:Dynamic = cast FlightRuntime.UNDEFINED;
    var overlapping:Dynamic = cast FlightRuntime.UNDEFINED;
    rankA = FlightRuntime.callValue(Collision.shapeKindRank__testCollision, cast ([FlightRuntime.field(a, 'kind')] : Array<Dynamic>));
    rankB = FlightRuntime.callValue(Collision.shapeKindRank__testCollision, cast ([FlightRuntime.field(b, 'kind')] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(rankA, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(rankB, 0.0, '<')))) {
  FlightRuntime.callValue(clearCollisionManifold, cast ([out] : Array<Dynamic>));
  return cast false;
}
    swapped = FlightRuntime.compare(rankA, rankB, '>');
    lo = FlightRuntime.select(swapped, function():Dynamic return cast b, function():Dynamic return cast a);
    hi = FlightRuntime.select(swapped, function():Dynamic return cast a, function():Dynamic return cast b);
    overlapping = false;
    {
      var __switchValue = FlightRuntime.field(lo, 'kind');
      if (__switchValue == 'circle') {
        {
          var __switchValue = FlightRuntime.field(hi, 'kind');
          if (__switchValue == 'circle') {
            (overlapping = cast (FlightRuntime.callValue(testCircleCircleCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
          }
          else if (__switchValue == 'aabb') {
            (overlapping = cast (FlightRuntime.callValue(testCircleAabbCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
          }
          else if (__switchValue == 'obb') {
            (overlapping = cast (FlightRuntime.callValue(testCircleObbCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
          }
          else if (__switchValue == 'polygon') {
            (overlapping = cast (FlightRuntime.callValue(testCirclePolygonCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
          }
        }
      }
      else if (__switchValue == 'aabb') {
        {
          var __switchValue = FlightRuntime.field(hi, 'kind');
          if (__switchValue == 'aabb') {
            (overlapping = cast (FlightRuntime.callValue(testAabbAabbCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
          }
          else if (__switchValue == 'obb') {
            (overlapping = cast (FlightRuntime.callValue(testAabbObbCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
          }
          else if (__switchValue == 'polygon') {
            (overlapping = cast (FlightRuntime.callValue(testAabbPolygonCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
          }
        }
      }
      else if (__switchValue == 'obb') {
        {
          var __switchValue = FlightRuntime.field(hi, 'kind');
          if (__switchValue == 'obb') {
            (overlapping = cast (FlightRuntime.callValue(testObbObbCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
          }
          else if (__switchValue == 'polygon') {
            (overlapping = cast (FlightRuntime.callValue(testObbPolygonCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
          }
        }
      }
      else if (__switchValue == 'polygon') {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(hi, 'kind'), 'polygon'))) {
  (overlapping = cast (FlightRuntime.callValue(testPolygonPolygonCollision, cast ([lo, hi, out] : Array<Dynamic>)) : Dynamic));
}
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.andValue(overlapping, function():Dynamic return cast swapped))) {
  FlightRuntime.setField(out, 'normalX', -FlightRuntime.field(out, 'normalX'));
  FlightRuntime.setField(out, 'normalY', -FlightRuntime.field(out, 'normalY'));
}
    return cast overlapping;
    return cast null;
  }

  public static function testObbObbCollision(a:CollisionObb, b:CollisionObb, out:CollisionManifold):Bool {
    FlightRuntime.callValue(Collision.writeObbVertices__shapeCollision, cast ([a, Collision.scratchA__shapeCollision] : Array<Dynamic>));
    FlightRuntime.callValue(Collision.writeObbVertices__shapeCollision, cast ([b, Collision.scratchB__shapeCollision] : Array<Dynamic>));
    return cast FlightRuntime.callValue(Collision.satConvexOverlap__shapeCollision, cast ([Collision.scratchA__shapeCollision, 4.0, Collision.scratchB__shapeCollision, 4.0, out] : Array<Dynamic>));
    return cast null;
  }

  public static function testObbPolygonCollision(a:CollisionObb, b:CollisionPolygon, out:CollisionManifold):Bool {
    var bPoints:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.callValue(Collision.writeObbVertices__shapeCollision, cast ([a, Collision.scratchA__shapeCollision] : Array<Dynamic>));
    bPoints = FlightRuntime.field(b, 'points');
    return cast FlightRuntime.callValue(Collision.satConvexOverlap__shapeCollision, cast ([Collision.scratchA__shapeCollision, 4.0, bPoints, (Std.int(FlightRuntime.field(bPoints, 'length')) >> Std.int(1.0)), out] : Array<Dynamic>));
    return cast null;
  }

  public static function testPolygonPolygonCollision(a:CollisionPolygon, b:CollisionPolygon, out:CollisionManifold):Bool {
    var aPoints:Dynamic = cast FlightRuntime.UNDEFINED;
    var bPoints:Dynamic = cast FlightRuntime.UNDEFINED;
    aPoints = FlightRuntime.field(a, 'points');
    bPoints = FlightRuntime.field(b, 'points');
    return cast FlightRuntime.callValue(Collision.satConvexOverlap__shapeCollision, cast ([aPoints, (Std.int(FlightRuntime.field(aPoints, 'length')) >> Std.int(1.0)), bPoints, (Std.int(FlightRuntime.field(bPoints, 'length')) >> Std.int(1.0)), out] : Array<Dynamic>));
    return cast null;
  }

  public static function testSegmentAabbCollision(a:CollisionSegment, b:CollisionAabb):Bool {
    return cast FlightRuntime.callValue(Collision.isSegmentOverlappingBox__segmentCollision, cast ([FlightRuntime.field(a, 'x0'), FlightRuntime.field(a, 'y0'), FlightRuntime.field(a, 'x1'), FlightRuntime.field(a, 'y1'), FlightRuntime.field(b, 'minX'), FlightRuntime.field(b, 'minY'), FlightRuntime.field(b, 'maxX'), FlightRuntime.field(b, 'maxY')] : Array<Dynamic>));
    return cast null;
  }

  public static function testSegmentCircleCollision(a:CollisionSegment, b:CollisionCircle):Bool {
    var x0:Dynamic = cast FlightRuntime.UNDEFINED;
    var y0:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var lengthSquared:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    var closestX:Dynamic = cast FlightRuntime.UNDEFINED;
    var closestY:Dynamic = cast FlightRuntime.UNDEFINED;
    var ddx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ddy:Dynamic = cast FlightRuntime.UNDEFINED;
    x0 = FlightRuntime.field(a, 'x0');
    y0 = FlightRuntime.field(a, 'y0');
    dx = (FlightRuntime.field(a, 'x1') - x0);
    dy = (FlightRuntime.field(a, 'y1') - y0);
    lengthSquared = ((dx * dx) + (dy * dy));
    t = 0.0;
    if (FlightRuntime.truthy(FlightRuntime.compare(lengthSquared, Collision.EPS__segmentCollision, '>'))) {
  (t = cast (((((FlightRuntime.field(b, 'x') - x0) * dx) + ((FlightRuntime.field(b, 'y') - y0) * dy)) / lengthSquared) : Dynamic));
  (t = cast (FlightRuntime.select(FlightRuntime.compare(t, 0.0, '<'), function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(t, 1.0, '>'), function():Dynamic return cast 1.0, function():Dynamic return cast t)) : Dynamic));
}
    closestX = (x0 + (t * dx));
    closestY = (y0 + (t * dy));
    ddx = (FlightRuntime.field(b, 'x') - closestX);
    ddy = (FlightRuntime.field(b, 'y') - closestY);
    return cast FlightRuntime.compare(((ddx * ddx) + (ddy * ddy)), (FlightRuntime.field(b, 'radius') * FlightRuntime.field(b, 'radius')), '<=');
    return cast null;
  }

  public static function testSegmentObbCollision(a:CollisionSegment, b:CollisionObb):Bool {
    var cos:Dynamic = cast FlightRuntime.UNDEFINED;
    var sin:Dynamic = cast FlightRuntime.UNDEFINED;
    var d0x:Dynamic = cast FlightRuntime.UNDEFINED;
    var d0y:Dynamic = cast FlightRuntime.UNDEFINED;
    var d1x:Dynamic = cast FlightRuntime.UNDEFINED;
    var d1y:Dynamic = cast FlightRuntime.UNDEFINED;
    var localX0:Dynamic = cast FlightRuntime.UNDEFINED;
    var localY0:Dynamic = cast FlightRuntime.UNDEFINED;
    var localX1:Dynamic = cast FlightRuntime.UNDEFINED;
    var localY1:Dynamic = cast FlightRuntime.UNDEFINED;
    cos = FlightRuntime.callProperty(HxMath, 'cos', cast ([FlightRuntime.field(b, 'rotation')] : Array<Dynamic>));
    sin = FlightRuntime.callProperty(HxMath, 'sin', cast ([FlightRuntime.field(b, 'rotation')] : Array<Dynamic>));
    d0x = (FlightRuntime.field(a, 'x0') - FlightRuntime.field(b, 'x'));
    d0y = (FlightRuntime.field(a, 'y0') - FlightRuntime.field(b, 'y'));
    d1x = (FlightRuntime.field(a, 'x1') - FlightRuntime.field(b, 'x'));
    d1y = (FlightRuntime.field(a, 'y1') - FlightRuntime.field(b, 'y'));
    localX0 = ((d0x * cos) + (d0y * sin));
    localY0 = ((-d0x * sin) + (d0y * cos));
    localX1 = ((d1x * cos) + (d1y * sin));
    localY1 = ((-d1x * sin) + (d1y * cos));
    return cast FlightRuntime.callValue(Collision.isSegmentOverlappingBox__segmentCollision, cast ([localX0, localY0, localX1, localY1, -FlightRuntime.field(b, 'halfW'), -FlightRuntime.field(b, 'halfH'), FlightRuntime.field(b, 'halfW'), FlightRuntime.field(b, 'halfH')] : Array<Dynamic>));
    return cast null;
  }

  public static function testSegmentPolygonCollision(a:CollisionSegment, b:CollisionPolygon):Bool {
    var points:Dynamic = cast FlightRuntime.UNDEFINED;
    var pn:Dynamic = cast FlightRuntime.UNDEFINED;
    points = FlightRuntime.field(b, 'points');
    pn = (Std.int(FlightRuntime.field(points, 'length')) >> Std.int(1.0));
    if (FlightRuntime.truthy(FlightRuntime.callValue(Collision.isPointInConvexPolygon__segmentCollision, cast ([FlightRuntime.field(a, 'x0'), FlightRuntime.field(a, 'y0'), points, pn] : Array<Dynamic>)))) { return cast true; }
    if (FlightRuntime.truthy(FlightRuntime.callValue(Collision.isPointInConvexPolygon__segmentCollision, cast ([FlightRuntime.field(a, 'x1'), FlightRuntime.field(a, 'y1'), points, pn] : Array<Dynamic>)))) { return cast true; }
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, pn, '<'))) {
        var j:Dynamic = ((i + 1.0) % pn);
        if (FlightRuntime.truthy(FlightRuntime.callValue(Collision.isSegmentsIntersecting__segmentCollision, cast ([FlightRuntime.field(a, 'x0'), FlightRuntime.field(a, 'y0'), FlightRuntime.field(a, 'x1'), FlightRuntime.field(a, 'y1'), FlightRuntime.getIndex(points, (Std.int(i) << Std.int(1.0))), FlightRuntime.getIndex(points, ((Std.int(i) << Std.int(1.0)) + 1.0)), FlightRuntime.getIndex(points, (Std.int(j) << Std.int(1.0))), FlightRuntime.getIndex(points, ((Std.int(j) << Std.int(1.0)) + 1.0))] : Array<Dynamic>)))) {
  return cast true;
}
        i++;
      }
    }
    return cast false;
    return cast null;
  }

  public static function testSegmentSegmentCollision(a:CollisionSegment, b:CollisionSegment):Bool {
    return cast FlightRuntime.callValue(Collision.isSegmentsIntersecting__segmentCollision, cast ([FlightRuntime.field(a, 'x0'), FlightRuntime.field(a, 'y0'), FlightRuntime.field(a, 'x1'), FlightRuntime.field(a, 'y1'), FlightRuntime.field(b, 'x0'), FlightRuntime.field(b, 'y0'), FlightRuntime.field(b, 'x1'), FlightRuntime.field(b, 'y1')] : Array<Dynamic>));
    return cast null;
  }

  public static function writeAabbVertices__shapeCollision(aabb:CollisionAabb, out:Dynamic):Void {
    var minX:Dynamic = cast FlightRuntime.UNDEFINED;
    var minY:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxX:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxY:Dynamic = cast FlightRuntime.UNDEFINED;
    minX = FlightRuntime.field(aabb, 'minX');
    minY = FlightRuntime.field(aabb, 'minY');
    maxX = FlightRuntime.field(aabb, 'maxX');
    maxY = FlightRuntime.field(aabb, 'maxY');
    FlightRuntime.setIndex(out, 0.0, minX);
    FlightRuntime.setIndex(out, 1.0, minY);
    FlightRuntime.setIndex(out, 2.0, maxX);
    FlightRuntime.setIndex(out, 3.0, minY);
    FlightRuntime.setIndex(out, 4.0, maxX);
    FlightRuntime.setIndex(out, 5.0, maxY);
    FlightRuntime.setIndex(out, 6.0, minX);
    FlightRuntime.setIndex(out, 7.0, maxY);
  }

  public static function writeObbVertices__shapeCollision(obb:CollisionObb, out:Dynamic):Void {
    var cx:Dynamic = cast FlightRuntime.UNDEFINED;
    var cy:Dynamic = cast FlightRuntime.UNDEFINED;
    var halfW:Dynamic = cast FlightRuntime.UNDEFINED;
    var halfH:Dynamic = cast FlightRuntime.UNDEFINED;
    var cos:Dynamic = cast FlightRuntime.UNDEFINED;
    var sin:Dynamic = cast FlightRuntime.UNDEFINED;
    var wx:Dynamic = cast FlightRuntime.UNDEFINED;
    var wy:Dynamic = cast FlightRuntime.UNDEFINED;
    var hx:Dynamic = cast FlightRuntime.UNDEFINED;
    var hy:Dynamic = cast FlightRuntime.UNDEFINED;
    cx = FlightRuntime.field(obb, 'x');
    cy = FlightRuntime.field(obb, 'y');
    halfW = FlightRuntime.field(obb, 'halfW');
    halfH = FlightRuntime.field(obb, 'halfH');
    cos = FlightRuntime.callProperty(HxMath, 'cos', cast ([FlightRuntime.field(obb, 'rotation')] : Array<Dynamic>));
    sin = FlightRuntime.callProperty(HxMath, 'sin', cast ([FlightRuntime.field(obb, 'rotation')] : Array<Dynamic>));
    wx = (cos * halfW);
    wy = (sin * halfW);
    hx = (-sin * halfH);
    hy = (cos * halfH);
    FlightRuntime.setIndex(out, 0.0, ((cx - wx) - hx));
    FlightRuntime.setIndex(out, 1.0, ((cy - wy) - hy));
    FlightRuntime.setIndex(out, 2.0, ((cx + wx) - hx));
    FlightRuntime.setIndex(out, 3.0, ((cy + wy) - hy));
    FlightRuntime.setIndex(out, 4.0, ((cx + wx) + hx));
    FlightRuntime.setIndex(out, 5.0, ((cy + wy) + hy));
    FlightRuntime.setIndex(out, 6.0, ((cx - wx) + hx));
    FlightRuntime.setIndex(out, 7.0, ((cy - wy) + hy));
  }
}
