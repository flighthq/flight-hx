// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Signals.createSignal;
import flight.Signals.emitSignal;
import flight.Types.AnimationChannel;
import flight.Types.AnimationClip;
import flight.Types.AnimationInterpolation;
import flight.Types.AnimationLoopMode;
import flight.Types.AnimationPlayer;
import flight.Types.AnimationTrack;
import flight.Types.AnimationTrackValidationDiagnostic;

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.Animation")
class Animation {
  @:keep public static function advanceAnimationPlayer(player:AnimationPlayer, dt:Float):Void {
    var duration:Dynamic = cast FlightRuntime.UNDEFINED;
    var time:Dynamic = cast FlightRuntime.UNDEFINED;
    var looped:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.field(player, 'playing')))) { return; }
    duration = FlightRuntime.field(FlightRuntime.field(player, 'clip'), 'duration');
    if (FlightRuntime.truthy(FlightRuntime.compare(duration, 0.0, '<='))) {
  FlightRuntime.setField(player, 'time', 0.0);
  return;
}
    time = (FlightRuntime.field(player, 'time') + (dt * FlightRuntime.field(player, 'speed')));
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.field(player, 'loop')))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(time, duration, '>='))) {
  FlightRuntime.setField(player, 'time', duration);
  FlightRuntime.setField(player, 'playing', false);
  FlightRuntime.callValue(Animation.emitAnimationPlayerFinished__animationPlayer, cast ([player] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.compare(time, 0.0, '<'))) {
  FlightRuntime.setField(player, 'time', 0.0);
  FlightRuntime.setField(player, 'playing', false);
  FlightRuntime.callValue(Animation.emitAnimationPlayerFinished__animationPlayer, cast ([player] : Array<Dynamic>));
} else {
  FlightRuntime.setField(player, 'time', time);
} }
  return;
}
    looped = false;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(player, 'loopMode'), Types.AnimationLoopModePingPong))) {
  {
    while (true) {
      if (FlightRuntime.truthy(FlightRuntime.compare(time, duration, '>'))) {
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Animation.consumeAnimationPlayerLoop__animationPlayer, cast ([player] : Array<Dynamic>))))) {
  FlightRuntime.callValue(Animation.finishAnimationPlayerAt__animationPlayer, cast ([player, duration] : Array<Dynamic>));
  return;
}
  (time = cast (((2.0 * duration) - time) : Dynamic));
  FlightRuntime.setField(player, 'speed', -FlightRuntime.field(player, 'speed'));
  (looped = cast (true : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.compare(time, 0.0, '<'))) {
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Animation.consumeAnimationPlayerLoop__animationPlayer, cast ([player] : Array<Dynamic>))))) {
  FlightRuntime.callValue(Animation.finishAnimationPlayerAt__animationPlayer, cast ([player, 0.0] : Array<Dynamic>));
  return;
}
  (time = cast (-time : Dynamic));
  FlightRuntime.setField(player, 'speed', -FlightRuntime.field(player, 'speed'));
  (looped = cast (true : Dynamic));
} else {
  break;
} }
    }
  }
} else {
  while (FlightRuntime.truthy(FlightRuntime.compare(time, duration, '>='))) {
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Animation.consumeAnimationPlayerLoop__animationPlayer, cast ([player] : Array<Dynamic>))))) {
  FlightRuntime.callValue(Animation.finishAnimationPlayerAt__animationPlayer, cast ([player, duration] : Array<Dynamic>));
  return;
}
  (time = cast ((time - duration) : Dynamic));
  (looped = cast (true : Dynamic));
}
  while (FlightRuntime.truthy(FlightRuntime.compare(time, 0.0, '<'))) {
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(Animation.consumeAnimationPlayerLoop__animationPlayer, cast ([player] : Array<Dynamic>))))) {
  FlightRuntime.callValue(Animation.finishAnimationPlayerAt__animationPlayer, cast ([player, 0.0] : Array<Dynamic>));
  return;
}
  (time = cast ((time + duration) : Dynamic));
  (looped = cast (true : Dynamic));
}
}
    FlightRuntime.setField(player, 'time', time);
    if (FlightRuntime.truthy(looped)) { FlightRuntime.callValue(Animation.emitAnimationPlayerLooped__animationPlayer, cast ([player] : Array<Dynamic>)); }
  }

  @:keep public static function cloneAnimationClip(clip:AnimationClip):AnimationClip {
    var channels:Array<AnimationChannel> = cast FlightRuntime.UNDEFINED;
    channels = cast ([] : Array<Dynamic>);
    for (channel in FlightRuntime.iterable(FlightRuntime.field(clip, 'channels'))) {
      FlightRuntime.callProperty(channels, 'push', cast ([FlightRuntime.callValue(createAnimationChannel, cast ([FlightRuntime.callValue(cloneAnimationTrack, cast ([FlightRuntime.field(channel, 'track')] : Array<Dynamic>)), FlightRuntime.field(channel, 'targetRef')] : Array<Dynamic>))] : Array<Dynamic>));
    }
    return cast { channels: channels, duration: FlightRuntime.field(clip, 'duration') };
    return cast null;
  }

  @:keep public static function cloneAnimationPlayer(player:AnimationPlayer):AnimationPlayer {
    return cast { clip: FlightRuntime.field(player, 'clip'), loop: FlightRuntime.field(player, 'loop'), loopMode: FlightRuntime.field(player, 'loopMode'), onFinished: null, onLooped: null, playing: FlightRuntime.field(player, 'playing'), repeatCount: FlightRuntime.field(player, 'repeatCount'), speed: FlightRuntime.field(player, 'speed'), time: FlightRuntime.field(player, 'time') };
    return cast null;
  }

  @:keep public static function cloneAnimationTrack(track:AnimationTrack):AnimationTrack {
    return cast { components: FlightRuntime.field(track, 'components'), easing: FlightRuntime.field(track, 'easing'), interpolation: FlightRuntime.field(track, 'interpolation'), quaternion: FlightRuntime.field(track, 'quaternion'), times: FlightRuntime.callValue(Animation.cloneNumberBuffer__animationTrack, cast ([FlightRuntime.field(track, 'times')] : Array<Dynamic>)), values: FlightRuntime.callValue(Animation.cloneNumberBuffer__animationTrack, cast ([FlightRuntime.field(track, 'values')] : Array<Dynamic>)) };
    return cast null;
  }

  @:keep public static function cloneNumberBuffer__animationTrack(src:Dynamic):Dynamic {
    var out:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.isInstanceOf(src, FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>))))) { return cast FlightRuntime.slice(src, 0, null); }
    out = FlightRuntime.createArray(FlightRuntime.field(src, 'length'));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(src, 'length'), '<'))) {
        FlightRuntime.setIndex(out, i, FlightRuntime.getIndex(src, i));
        i++;
      }
    }
    return cast out;
    return cast null;
  }

  @:keep public static function computeChannelsDuration__animationClip(channels:Array<AnimationChannel>):Float {
    var max:Dynamic = cast FlightRuntime.UNDEFINED;
    max = 0.0;
    for (channel in FlightRuntime.iterable(channels)) {
      var times:Dynamic = FlightRuntime.field(FlightRuntime.field(channel, 'track'), 'times');
      var last:Dynamic = FlightRuntime.field(times, 'length');
      if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(last, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.getIndex(times, (last - 1.0)), max, '>')))) { (max = cast (FlightRuntime.getIndex(times, (last - 1.0)) : Dynamic)); }
    }
    return cast max;
    return cast null;
  }

  @:keep public static function consumeAnimationPlayerLoop__animationPlayer(player:AnimationPlayer):Bool {
    var rc:Dynamic = cast FlightRuntime.UNDEFINED;
    rc = FlightRuntime.field(player, 'repeatCount');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(rc, FlightRuntime.UNDEFINED), function():Dynamic return cast FlightRuntime.compare(rc, 0.0, '<')))) { return cast true; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(rc, 0.0))) { return cast false; }
    FlightRuntime.setField(player, 'repeatCount', (rc - 1.0));
    return cast true;
    return cast null;
  }

  @:keep public static function copyKeyframeValue__animationTrack(out:Dynamic, track:AnimationTrack, k:Float):Void {
    var off:Dynamic = cast FlightRuntime.UNDEFINED;
    off = FlightRuntime.callValue(Animation.keyframeValueOffset__animationTrack, cast ([track, k] : Array<Dynamic>));
    {
      var c:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(c, FlightRuntime.field(track, 'components'), '<'))) {
        FlightRuntime.setIndex(out, c, FlightRuntime.getIndex(FlightRuntime.field(track, 'values'), (off + c)));
        c++;
      }
    }
  }

  @:keep public static function createAnimationChannel(track:AnimationTrack, targetRef:Dynamic):AnimationChannel {
    return cast { targetRef: targetRef, track: track };
    return cast null;
  }

  @:keep public static function createAnimationClip(channels:Array<AnimationChannel>, ?duration:Float):AnimationClip {
    return cast { channels: channels, duration: FlightRuntime.coalesce(duration, function():Dynamic return cast FlightRuntime.callValue(Animation.computeChannelsDuration__animationClip, cast ([channels] : Array<Dynamic>))) };
    return cast null;
  }

  @:keep public static function createAnimationPlayer(clip:AnimationClip, ?opts:{ @:optional var loop:Bool; @:optional var loopMode:AnimationLoopMode; @:optional var playing:Bool; @:optional var repeatCount:Float; @:optional var speed:Float; @:optional var time:Float; }):AnimationPlayer {
    return cast { clip: clip, loop: FlightRuntime.coalesce(FlightRuntime.optionalField(opts, 'loop'), function():Dynamic return cast true), loopMode: FlightRuntime.coalesce(FlightRuntime.optionalField(opts, 'loopMode'), function():Dynamic return cast Types.AnimationLoopModeRepeat), onFinished: null, onLooped: null, playing: FlightRuntime.coalesce(FlightRuntime.optionalField(opts, 'playing'), function():Dynamic return cast true), repeatCount: FlightRuntime.coalesce(FlightRuntime.optionalField(opts, 'repeatCount'), function():Dynamic return cast -1.0), speed: FlightRuntime.coalesce(FlightRuntime.optionalField(opts, 'speed'), function():Dynamic return cast 1.0), time: FlightRuntime.coalesce(FlightRuntime.optionalField(opts, 'time'), function():Dynamic return cast 0.0) };
    return cast null;
  }

  @:keep public static function createAnimationTrack(opts:{ var times:Dynamic; var values:Dynamic; @:optional var components:Float; @:optional var interpolation:AnimationInterpolation; @:optional var quaternion:Bool; @:optional var easing:Dynamic; }):AnimationTrack {
    return cast { components: FlightRuntime.coalesce(FlightRuntime.field(opts, 'components'), function():Dynamic return cast 1.0), easing: FlightRuntime.coalesce(FlightRuntime.field(opts, 'easing'), function():Dynamic return cast null), interpolation: FlightRuntime.coalesce(FlightRuntime.field(opts, 'interpolation'), function():Dynamic return cast Types.AnimationInterpolationLinear), quaternion: FlightRuntime.coalesce(FlightRuntime.field(opts, 'quaternion'), function():Dynamic return cast false), times: FlightRuntime.field(opts, 'times'), values: FlightRuntime.field(opts, 'values') };
    return cast null;
  }

  @:keep public static function emitAnimationPlayerFinished__animationPlayer(player:AnimationPlayer):Void {
    if (FlightRuntime.truthy(!FlightRuntime.looseEquals(FlightRuntime.field(player, 'onFinished'), null))) { FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(player, 'onFinished')] : Array<Dynamic>)); }
  }

  @:keep public static function emitAnimationPlayerLooped__animationPlayer(player:AnimationPlayer):Void {
    if (FlightRuntime.truthy(!FlightRuntime.looseEquals(FlightRuntime.field(player, 'onLooped'), null))) { FlightRuntime.callValue(emitSignal, cast ([FlightRuntime.field(player, 'onLooped')] : Array<Dynamic>)); }
  }

  @:keep public static function enableAnimationPlayerSignals(player:AnimationPlayer):Void {
    if (FlightRuntime.truthy(FlightRuntime.looseEquals(FlightRuntime.field(player, 'onFinished'), null))) { FlightRuntime.setField(player, 'onFinished', FlightRuntime.callValue(createSignal, cast ([] : Array<Dynamic>))); }
    if (FlightRuntime.truthy(FlightRuntime.looseEquals(FlightRuntime.field(player, 'onLooped'), null))) { FlightRuntime.setField(player, 'onLooped', FlightRuntime.callValue(createSignal, cast ([] : Array<Dynamic>))); }
  }

  @:keep public static function finishAnimationPlayerAt__animationPlayer(player:AnimationPlayer, time:Float):Void {
    FlightRuntime.setField(player, 'time', time);
    FlightRuntime.setField(player, 'playing', false);
    FlightRuntime.callValue(Animation.emitAnimationPlayerFinished__animationPlayer, cast ([player] : Array<Dynamic>));
  }

  @:keep public static function getAnimationClipDuration(clip:AnimationClip):Float {
    return cast FlightRuntime.field(clip, 'duration');
    return cast null;
  }

  @:keep public static function getAnimationPlayerNormalizedTime(player:AnimationPlayer):Float {
    var duration:Dynamic = cast FlightRuntime.UNDEFINED;
    var n:Dynamic = cast FlightRuntime.UNDEFINED;
    duration = FlightRuntime.field(FlightRuntime.field(player, 'clip'), 'duration');
    if (FlightRuntime.truthy(FlightRuntime.compare(duration, 0.0, '<='))) { return cast 0.0; }
    n = (FlightRuntime.field(player, 'time') / duration);
    return cast FlightRuntime.select(FlightRuntime.compare(n, 0.0, '<'), function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(n, 1.0, '>'), function():Dynamic return cast 1.0, function():Dynamic return cast n));
    return cast null;
  }

  @:keep public static function keyframeStride__animationTrack(track:AnimationTrack):Float {
    return cast FlightRuntime.select(FlightRuntime.strictEquals(FlightRuntime.field(track, 'interpolation'), 'Cubic'), function():Dynamic return cast (FlightRuntime.field(track, 'components') * 3.0), function():Dynamic return cast FlightRuntime.field(track, 'components'));
    return cast null;
  }

  @:keep public static function keyframeValueOffset__animationTrack(track:AnimationTrack, k:Float):Float {
    var stride:Dynamic = cast FlightRuntime.UNDEFINED;
    stride = FlightRuntime.callValue(Animation.keyframeStride__animationTrack, cast ([track] : Array<Dynamic>));
    return cast FlightRuntime.select(FlightRuntime.strictEquals(FlightRuntime.field(track, 'interpolation'), 'Cubic'), function():Dynamic return cast ((k * stride) + FlightRuntime.field(track, 'components')), function():Dynamic return cast (k * stride));
    return cast null;
  }

  @:keep public static function normalizeFlatQuaternion__animationTrack(out:Dynamic):Void {
    var x:Dynamic = cast FlightRuntime.UNDEFINED;
    var y:Dynamic = cast FlightRuntime.UNDEFINED;
    var z:Dynamic = cast FlightRuntime.UNDEFINED;
    var w:Dynamic = cast FlightRuntime.UNDEFINED;
    var len:Dynamic = cast FlightRuntime.UNDEFINED;
    x = FlightRuntime.getIndex(out, 0.0);
    y = FlightRuntime.getIndex(out, 1.0);
    z = FlightRuntime.getIndex(out, 2.0);
    w = FlightRuntime.getIndex(out, 3.0);
    len = FlightRuntime.hypot(x, y, z, w);
    if (FlightRuntime.truthy(FlightRuntime.compare(len, 0.0, '>'))) {
  var inv:Dynamic = (1.0 / len);
  FlightRuntime.setIndex(out, 0.0, (x * inv));
  FlightRuntime.setIndex(out, 1.0, (y * inv));
  FlightRuntime.setIndex(out, 2.0, (z * inv));
  FlightRuntime.setIndex(out, 3.0, (w * inv));
}
  }

  @:keep public static function playAnimationPlayer(player:AnimationPlayer):Void {
    FlightRuntime.setField(player, 'playing', true);
  }

  @:keep public static function sampleAnimationClip(out:Dynamic, clip:AnimationClip, time:Float, visit:Dynamic):Void {
    var channels:Dynamic = cast FlightRuntime.UNDEFINED;
    channels = FlightRuntime.field(clip, 'channels');
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(channels, 'length'), '<'))) {
        var channel:Dynamic = FlightRuntime.getIndex(channels, i);
        FlightRuntime.callValue(sampleAnimationTrack, cast ([out, FlightRuntime.field(channel, 'track'), time] : Array<Dynamic>));
        FlightRuntime.callValue(visit, cast ([out, channel, i] : Array<Dynamic>));
        i++;
      }
    }
  }

  @:keep public static function sampleAnimationTrack(out:Dynamic, track:AnimationTrack, t:Float):Void {
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var components:Dynamic = cast FlightRuntime.UNDEFINED;
    var times:Dynamic = cast FlightRuntime.UNDEFINED;
    var values:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var lo:Dynamic = cast FlightRuntime.UNDEFINED;
    var hi:Dynamic = cast FlightRuntime.UNDEFINED;
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    var t0:Dynamic = cast FlightRuntime.UNDEFINED;
    var dt:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var oi:Dynamic = cast FlightRuntime.UNDEFINED;
    var oj:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure0 = track;
    components = FlightRuntime.field(__destructure0, 'components');
    times = FlightRuntime.field(__destructure0, 'times');
    values = FlightRuntime.field(__destructure0, 'values');
    count = FlightRuntime.field(times, 'length');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(count, 0.0))) {
  {
    var c:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(c, components, '<'))) {
      FlightRuntime.setIndex(out, c, 0.0);
      c++;
    }
  }
  return;
}
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(count, 1.0), function():Dynamic return cast FlightRuntime.compare(t, FlightRuntime.getIndex(times, 0.0), '<=')))) {
  FlightRuntime.callValue(Animation.copyKeyframeValue__animationTrack, cast ([out, track, 0.0] : Array<Dynamic>));
  return;
}
    if (FlightRuntime.truthy(FlightRuntime.compare(t, FlightRuntime.getIndex(times, (count - 1.0)), '>='))) {
  FlightRuntime.callValue(Animation.copyKeyframeValue__animationTrack, cast ([out, track, (count - 1.0)] : Array<Dynamic>));
  return;
}
    lo = 0.0;
    hi = (count - 1.0);
    while (FlightRuntime.truthy(FlightRuntime.compare(lo, hi, '<'))) {
  var mid:Dynamic = (Std.int(((lo + hi) + 1.0)) >> Std.int(1.0));
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.getIndex(times, mid), t, '<='))) { (lo = cast (mid : Dynamic)); } else { (hi = cast ((mid - 1.0) : Dynamic)); }
}
    i = lo;
    t0 = FlightRuntime.getIndex(times, i);
    dt = (FlightRuntime.getIndex(times, (i + 1.0)) - t0);
    alpha = FlightRuntime.select(FlightRuntime.compare(dt, 0.0, '>'), function():Dynamic return cast ((t - t0) / dt), function():Dynamic return cast 0.0);
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(track, 'easing'), null))) { (alpha = cast (FlightRuntime.callProperty(track, 'easing', cast ([alpha] : Array<Dynamic>)) : Dynamic)); }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(track, 'interpolation'), 'Step'))) {
  FlightRuntime.callValue(Animation.copyKeyframeValue__animationTrack, cast ([out, track, i] : Array<Dynamic>));
  return;
}
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(track, 'interpolation'), 'Cubic'))) {
  FlightRuntime.callValue(Animation.sampleCubicSegment__animationTrack, cast ([out, track, i, alpha, dt] : Array<Dynamic>));
  return;
}
    oi = FlightRuntime.callValue(Animation.keyframeValueOffset__animationTrack, cast ([track, i] : Array<Dynamic>));
    oj = FlightRuntime.callValue(Animation.keyframeValueOffset__animationTrack, cast ([track, (i + 1.0)] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.field(track, 'quaternion'), function():Dynamic return cast FlightRuntime.strictEquals(components, 4.0)))) {
  FlightRuntime.callValue(Animation.slerpFlatQuaternion__animationTrack, cast ([out, values, oi, oj, alpha] : Array<Dynamic>));
  return;
}
    {
      var c:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(c, components, '<'))) {
        var a:Dynamic = FlightRuntime.getIndex(values, (oi + c));
        FlightRuntime.setIndex(out, c, (a + ((FlightRuntime.getIndex(values, (oj + c)) - a) * alpha)));
        c++;
      }
    }
  }

  @:keep public static function sampleCubicSegment__animationTrack(out:Dynamic, track:AnimationTrack, i:Float, alpha:Float, dt:Float):Void {
    var __destructure3:Dynamic = cast FlightRuntime.UNDEFINED;
    var components:Dynamic = cast FlightRuntime.UNDEFINED;
    var values:Dynamic = cast FlightRuntime.UNDEFINED;
    var stride:Dynamic = cast FlightRuntime.UNDEFINED;
    var a2:Dynamic = cast FlightRuntime.UNDEFINED;
    var a3:Dynamic = cast FlightRuntime.UNDEFINED;
    var h00:Dynamic = cast FlightRuntime.UNDEFINED;
    var h10:Dynamic = cast FlightRuntime.UNDEFINED;
    var h01:Dynamic = cast FlightRuntime.UNDEFINED;
    var h11:Dynamic = cast FlightRuntime.UNDEFINED;
    var base0:Dynamic = cast FlightRuntime.UNDEFINED;
    var base1:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure3 = track;
    components = FlightRuntime.field(__destructure3, 'components');
    values = FlightRuntime.field(__destructure3, 'values');
    stride = (components * 3.0);
    a2 = (alpha * alpha);
    a3 = (a2 * alpha);
    h00 = (((2.0 * a3) - (3.0 * a2)) + 1.0);
    h10 = ((a3 - (2.0 * a2)) + alpha);
    h01 = ((-2.0 * a3) + (3.0 * a2));
    h11 = (a3 - a2);
    base0 = (i * stride);
    base1 = ((i + 1.0) * stride);
    {
      var c:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(c, components, '<'))) {
        var p0:Dynamic = FlightRuntime.getIndex(values, ((base0 + components) + c));
        var m0:Dynamic = FlightRuntime.getIndex(values, ((base0 + (components * 2.0)) + c));
        var p1:Dynamic = FlightRuntime.getIndex(values, ((base1 + components) + c));
        var m1:Dynamic = FlightRuntime.getIndex(values, (base1 + c));
        FlightRuntime.setIndex(out, c, ((((h00 * p0) + ((h10 * dt) * m0)) + (h01 * p1)) + ((h11 * dt) * m1)));
        c++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.field(track, 'quaternion'), function():Dynamic return cast FlightRuntime.strictEquals(components, 4.0)))) { FlightRuntime.callValue(Animation.normalizeFlatQuaternion__animationTrack, cast ([out] : Array<Dynamic>)); }
  }

  @:keep public static function seekAnimationPlayer(player:AnimationPlayer, time:Float):Void {
    var duration:Dynamic = cast FlightRuntime.UNDEFINED;
    duration = FlightRuntime.field(FlightRuntime.field(player, 'clip'), 'duration');
    FlightRuntime.setField(player, 'time', FlightRuntime.select(FlightRuntime.compare(time, 0.0, '<'), function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(time, duration, '>'), function():Dynamic return cast duration, function():Dynamic return cast time)));
  }

  @:keep public static function slerpFlatQuaternion__animationTrack(out:Dynamic, values:Dynamic, oa:Float, ob:Float, alpha:Float):Void {
    var ax:Dynamic = cast FlightRuntime.UNDEFINED;
    var ay:Dynamic = cast FlightRuntime.UNDEFINED;
    var az:Dynamic = cast FlightRuntime.UNDEFINED;
    var aw:Dynamic = cast FlightRuntime.UNDEFINED;
    var bx:Dynamic = cast FlightRuntime.UNDEFINED;
    var by:Dynamic = cast FlightRuntime.UNDEFINED;
    var bz:Dynamic = cast FlightRuntime.UNDEFINED;
    var bw:Dynamic = cast FlightRuntime.UNDEFINED;
    var cosom:Dynamic = cast FlightRuntime.UNDEFINED;
    var scale0:Float = cast FlightRuntime.UNDEFINED;
    var scale1:Float = cast FlightRuntime.UNDEFINED;
    ax = FlightRuntime.getIndex(values, oa);
    ay = FlightRuntime.getIndex(values, (oa + 1.0));
    az = FlightRuntime.getIndex(values, (oa + 2.0));
    aw = FlightRuntime.getIndex(values, (oa + 3.0));
    bx = FlightRuntime.getIndex(values, ob);
    by = FlightRuntime.getIndex(values, (ob + 1.0));
    bz = FlightRuntime.getIndex(values, (ob + 2.0));
    bw = FlightRuntime.getIndex(values, (ob + 3.0));
    cosom = ((((ax * bx) + (ay * by)) + (az * bz)) + (aw * bw));
    if (FlightRuntime.truthy(FlightRuntime.compare(cosom, 0.0, '<'))) {
  (cosom = cast (-cosom : Dynamic));
  (bx = cast (-bx : Dynamic));
  (by = cast (-by : Dynamic));
  (bz = cast (-bz : Dynamic));
  (bw = cast (-bw : Dynamic));
}
    if (FlightRuntime.truthy(FlightRuntime.compare((1.0 - cosom), 0.000001, '>'))) {
  var omega:Dynamic = FlightRuntime.callProperty(HxMath, 'acos', cast ([cosom] : Array<Dynamic>));
  var sinom:Dynamic = FlightRuntime.callProperty(HxMath, 'sin', cast ([omega] : Array<Dynamic>));
  (scale0 = cast ((FlightRuntime.callProperty(HxMath, 'sin', cast ([((1.0 - alpha) * omega)] : Array<Dynamic>)) / sinom) : Dynamic));
  (scale1 = cast ((FlightRuntime.callProperty(HxMath, 'sin', cast ([(alpha * omega)] : Array<Dynamic>)) / sinom) : Dynamic));
} else {
  (scale0 = cast ((1.0 - alpha) : Dynamic));
  (scale1 = cast (alpha : Dynamic));
}
    FlightRuntime.setIndex(out, 0.0, ((scale0 * ax) + (scale1 * bx)));
    FlightRuntime.setIndex(out, 1.0, ((scale0 * ay) + (scale1 * by)));
    FlightRuntime.setIndex(out, 2.0, ((scale0 * az) + (scale1 * bz)));
    FlightRuntime.setIndex(out, 3.0, ((scale0 * aw) + (scale1 * bw)));
  }

  @:keep public static function stopAnimationPlayer(player:AnimationPlayer):Void {
    FlightRuntime.setField(player, 'playing', false);
    FlightRuntime.setField(player, 'time', 0.0);
  }

  @:keep public static function trimAnimationTrack(track:AnimationTrack, startTime:Float, endTime:Float):AnimationTrack {
    var __destructure1:Dynamic = cast FlightRuntime.UNDEFINED;
    var components:Dynamic = cast FlightRuntime.UNDEFINED;
    var times:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var stride:Dynamic = cast FlightRuntime.UNDEFINED;
    var outTimes:Array<Float> = cast FlightRuntime.UNDEFINED;
    var outValues:Array<Float> = cast FlightRuntime.UNDEFINED;
    __destructure1 = track;
    components = FlightRuntime.field(__destructure1, 'components');
    times = FlightRuntime.field(__destructure1, 'times');
    count = FlightRuntime.field(times, 'length');
    stride = FlightRuntime.callValue(Animation.keyframeStride__animationTrack, cast ([track] : Array<Dynamic>));
    outTimes = cast ([] : Array<Dynamic>);
    outValues = cast ([] : Array<Dynamic>);
    {
      var k:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, count, '<'))) {
        var time:Dynamic = FlightRuntime.getIndex(times, k);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(time, startTime, '<'), function():Dynamic return cast FlightRuntime.compare(time, endTime, '>')))) { k++; continue; }
        FlightRuntime.callProperty(outTimes, 'push', cast ([(time - startTime)] : Array<Dynamic>));
        var off:Dynamic = (k * stride);
        {
          var c:Dynamic = 0.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(c, stride, '<'))) {
            FlightRuntime.callProperty(outValues, 'push', cast ([FlightRuntime.getIndex(FlightRuntime.field(track, 'values'), (off + c))] : Array<Dynamic>));
            c++;
          }
        }
        k++;
      }
    }
    return cast { components: components, easing: FlightRuntime.field(track, 'easing'), interpolation: FlightRuntime.field(track, 'interpolation'), quaternion: FlightRuntime.field(track, 'quaternion'), times: outTimes, values: outValues };
    return cast null;
  }

  @:keep public static function validateAnimationTrack(track:AnimationTrack):Null<Array<AnimationTrackValidationDiagnostic>> {
    var diagnostics:Array<AnimationTrackValidationDiagnostic> = cast FlightRuntime.UNDEFINED;
    var __destructure2:Dynamic = cast FlightRuntime.UNDEFINED;
    var times:Dynamic = cast FlightRuntime.UNDEFINED;
    var values:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var expected:Dynamic = cast FlightRuntime.UNDEFINED;
    diagnostics = cast ([] : Array<Dynamic>);
    __destructure2 = track;
    times = FlightRuntime.field(__destructure2, 'times');
    values = FlightRuntime.field(__destructure2, 'values');
    count = FlightRuntime.field(times, 'length');
    {
      var k:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, count, '<'))) {
        if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.getIndex(times, k), FlightRuntime.getIndex(times, (k - 1.0)), '<='))) {
  FlightRuntime.callProperty(diagnostics, 'push', cast ([{ code: 'nonAscendingTimes', index: k, message: 'times[' + Std.string(k) + '] (' + Std.string(FlightRuntime.getIndex(times, k)) + ') is not greater than times[' + Std.string((k - 1.0)) + '] (' + Std.string(FlightRuntime.getIndex(times, (k - 1.0))) + '); times must be strictly ascending.' }] : Array<Dynamic>));
}
        k++;
      }
    }
    expected = (count * FlightRuntime.callValue(Animation.keyframeStride__animationTrack, cast ([track] : Array<Dynamic>)));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(values, 'length'), expected))) {
  FlightRuntime.callProperty(diagnostics, 'push', cast ([{ code: 'valuesLengthMismatch', index: null, message: 'values.length (' + Std.string(FlightRuntime.field(values, 'length')) + ') must equal keyCount * componentsPerKeyframe (' + Std.string(expected) + ').' }] : Array<Dynamic>));
}
    return cast FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(diagnostics, 'length'), 0.0, '>'), function():Dynamic return cast diagnostics, function():Dynamic return cast null);
    return cast null;
  }
}
