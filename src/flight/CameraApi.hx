// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.EntityApi.createEntity;
import flight.Geometry.createFrustum;
import flight.Geometry.createMatrix4;
import flight.Geometry.createVector2;
import flight.Geometry.createVector3;
import flight.Geometry.inverseMatrix4;
import flight.Geometry.isFrustumContainingPoint;
import flight.Geometry.isFrustumIntersectingAabb;
import flight.Geometry.isFrustumIntersectingSphere;
import flight.Geometry.multiplyMatrix4;
import flight.Geometry.normalizeVector3;
import flight.Geometry.setFrustumFromMatrix4;
import flight.Geometry.setMatrix4LookAt;
import flight.Geometry.setOrthographicMatrix4;
import flight.Geometry.setPerspectiveMatrix4;
import flight.Geometry.subtractVector3;
import flight.Types.AabbLike;
import flight.Types.BoundingSphereLike;
import flight.Types.Camera;
import flight.Types.FrustumLike;
import flight.Types.Matrix4Like;
import flight.Types.OrthographicProjection;
import flight.Types.PerspectiveProjection;
import flight.Types.PlaneLike;
import flight.Types.Projection;
import flight.Types.Ray3DLike;
import flight.Types.Vector3Like;

typedef CameraOptions = { var far:Float; var near:Float; var projection:Projection; };

typedef OrthographicProjectionOptions = { var halfHeight:Float; var halfWidth:Float; };

typedef PerspectiveProjectionOptions = { @:optional var aspect:Float; var fovY:Float; };

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.CameraApi")
class CameraApi {
  public static final __scratchDir__picking:Dynamic = FlightRuntime.callValue(createVector3, cast ([] : Array<Dynamic>));

  public static final __scratchFar__picking:Dynamic = FlightRuntime.callValue(createVector3, cast ([] : Array<Dynamic>));

  public static final __scratchFrustum__culling:Dynamic = FlightRuntime.callValue(createFrustum, cast ([] : Array<Dynamic>));

  public static final __scratchInverse__camera:Dynamic = FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>));

  public static final __scratchInverseVP__frustumCorners:Dynamic = FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>));

  public static final __scratchInverseVP__picking:Dynamic = FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>));

  public static final __scratchNdc__intersection:Vector3Like = { x: 0.0, y: 0.0, z: 0.0 };

  public static final __scratchNear__picking:Dynamic = FlightRuntime.callValue(createVector3, cast ([] : Array<Dynamic>));

  public static final __scratchProjection__camera:Dynamic = FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>));

  public static final __scratchViewProjection__camera:Dynamic = FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>));

  public static final __scratchViewProjection__culling:Dynamic = FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>));

  public static final __scratchViewProjection__frustumCorners:Dynamic = FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>));

  public static final __scratchViewProjection__picking:Dynamic = FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>));

  public static final _eye__shadowCamera:Vector3Like = { x: 0.0, y: 0.0, z: 0.0 };

  public static final _target__shadowCamera:Vector3Like = { x: 0.0, y: 0.0, z: 0.0 };

  public static final _upY__shadowCamera:Vector3Like = { x: 0.0, y: 1.0, z: 0.0 };

  public static final _upZ__shadowCamera:Vector3Like = { x: 0.0, y: 0.0, z: 1.0 };

  @:keep public static function configureDirectionalShadowCamera(camera:Camera, lightDirection:Vector3Like, sceneBounds:AabbLike):Void {
    var min:Dynamic = cast FlightRuntime.UNDEFINED;
    var max:Dynamic = cast FlightRuntime.UNDEFINED;
    var cx:Dynamic = cast FlightRuntime.UNDEFINED;
    var cy:Dynamic = cast FlightRuntime.UNDEFINED;
    var cz:Dynamic = cast FlightRuntime.UNDEFINED;
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    var dl:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var dz:Dynamic = cast FlightRuntime.UNDEFINED;
    var distance:Dynamic = cast FlightRuntime.UNDEFINED;
    var up:Dynamic = cast FlightRuntime.UNDEFINED;
    min = FlightRuntime.field(sceneBounds, 'min');
    max = FlightRuntime.field(sceneBounds, 'max');
    cx = ((FlightRuntime.field(min, 'x') + FlightRuntime.field(max, 'x')) * 0.5);
    cy = ((FlightRuntime.field(min, 'y') + FlightRuntime.field(max, 'y')) * 0.5);
    cz = ((FlightRuntime.field(min, 'z') + FlightRuntime.field(max, 'z')) * 0.5);
    radius = FlightRuntime.hypot((FlightRuntime.field(max, 'x') - cx), (FlightRuntime.field(max, 'y') - cy), (FlightRuntime.field(max, 'z') - cz));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(radius, 0.0))) { (radius = cast (1.0 : Dynamic)); }
    dl = FlightRuntime.orValue(FlightRuntime.hypot(FlightRuntime.field(lightDirection, 'x'), FlightRuntime.field(lightDirection, 'y'), FlightRuntime.field(lightDirection, 'z')), function():Dynamic return cast 1.0);
    dx = (FlightRuntime.field(lightDirection, 'x') / dl);
    dy = (FlightRuntime.field(lightDirection, 'y') / dl);
    dz = (FlightRuntime.field(lightDirection, 'z') / dl);
    distance = (radius * 2.0);
    FlightRuntime.setField(CameraApi._eye__shadowCamera, 'x', (cx - (dx * distance)));
    FlightRuntime.setField(CameraApi._eye__shadowCamera, 'y', (cy - (dy * distance)));
    FlightRuntime.setField(CameraApi._eye__shadowCamera, 'z', (cz - (dz * distance)));
    FlightRuntime.setField(CameraApi._target__shadowCamera, 'x', cx);
    FlightRuntime.setField(CameraApi._target__shadowCamera, 'y', cy);
    FlightRuntime.setField(CameraApi._target__shadowCamera, 'z', cz);
    up = FlightRuntime.select(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([dy] : Array<Dynamic>)), 0.99, '>'), function():Dynamic return cast CameraApi._upZ__shadowCamera, function():Dynamic return cast CameraApi._upY__shadowCamera);
    FlightRuntime.callValue(setCameraViewMatrix4FromLookAt, cast ([camera, CameraApi._eye__shadowCamera, CameraApi._target__shadowCamera, up] : Array<Dynamic>));
    FlightRuntime.setField(camera, 'near', radius);
    FlightRuntime.setField(camera, 'far', (radius * 3.0));
    FlightRuntime.setField(camera, 'projection', FlightRuntime.callValue(createOrthographicProjection, cast ([{ halfHeight: radius, halfWidth: radius }] : Array<Dynamic>)));
  }

  @:keep public static function createCamera(opts:CameraOptions):Camera {
    return cast FlightRuntime.callValue(createEntity, cast ([{ far: FlightRuntime.field(opts, 'far'), inverseViewProjection: FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>)), jitter: FlightRuntime.callValue(createVector2, cast ([0.0, 0.0] : Array<Dynamic>)), near: FlightRuntime.field(opts, 'near'), projection: FlightRuntime.field(opts, 'projection'), view: FlightRuntime.callValue(createMatrix4, cast ([] : Array<Dynamic>)) }] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function createOrthographicProjection(opts:OrthographicProjectionOptions):OrthographicProjection {
    return cast { halfHeight: FlightRuntime.field(opts, 'halfHeight'), halfWidth: FlightRuntime.field(opts, 'halfWidth'), kind: 'orthographic' };
    return cast null;
  }

  @:keep public static function createPerspectiveProjection(opts:PerspectiveProjectionOptions):PerspectiveProjection {
    return cast { aspect: FlightRuntime.coalesce(FlightRuntime.field(opts, 'aspect'), function():Dynamic return cast 1.0), fovY: FlightRuntime.field(opts, 'fovY'), kind: 'perspective' };
    return cast null;
  }

  @:keep public static function getCameraForward(out:Vector3Like, camera:Camera):Void {
    var m:Dynamic = cast FlightRuntime.UNDEFINED;
    m = FlightRuntime.field(FlightRuntime.field(camera, 'view'), 'm');
    FlightRuntime.setField(out, 'x', -FlightRuntime.getIndex(m, 2.0));
    FlightRuntime.setField(out, 'y', -FlightRuntime.getIndex(m, 6.0));
    FlightRuntime.setField(out, 'z', -FlightRuntime.getIndex(m, 10.0));
  }

  @:keep public static function getCameraFrustum(out:FrustumLike, camera:Camera, aspect:Float):Void {
    FlightRuntime.callValue(getCameraViewProjectionMatrix4, cast ([CameraApi.__scratchViewProjection__culling, camera, aspect] : Array<Dynamic>));
    FlightRuntime.callValue(setFrustumFromMatrix4, cast ([out, CameraApi.__scratchViewProjection__culling] : Array<Dynamic>));
  }

  @:keep public static function getCameraFrustumCorners(out:Array<Vector3Like>, camera:Camera, aspect:Float):Bool {
    var m:Dynamic = cast FlightRuntime.UNDEFINED;
    var ndcCorners:Array<Array<Float>> = cast FlightRuntime.UNDEFINED;
    var results:Array<Array<Float>> = cast FlightRuntime.UNDEFINED;
    FlightRuntime.callValue(getCameraViewProjectionMatrix4, cast ([CameraApi.__scratchViewProjection__frustumCorners, camera, aspect] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(inverseMatrix4, cast ([CameraApi.__scratchInverseVP__frustumCorners, CameraApi.__scratchViewProjection__frustumCorners] : Array<Dynamic>))))) {
  return cast false;
}
    m = FlightRuntime.field(CameraApi.__scratchInverseVP__frustumCorners, 'm');
    ndcCorners = cast ([cast ([-1.0, -1.0, -1.0] : Array<Dynamic>), cast ([1.0, -1.0, -1.0] : Array<Dynamic>), cast ([-1.0, 1.0, -1.0] : Array<Dynamic>), cast ([1.0, 1.0, -1.0] : Array<Dynamic>), cast ([-1.0, -1.0, 1.0] : Array<Dynamic>), cast ([1.0, -1.0, 1.0] : Array<Dynamic>), cast ([-1.0, 1.0, 1.0] : Array<Dynamic>), cast ([1.0, 1.0, 1.0] : Array<Dynamic>)] : Array<Dynamic>);
    results = cast ([] : Array<Dynamic>);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, 8.0, '<'))) {
        var __destructure0:Dynamic = FlightRuntime.getIndex(ndcCorners, i);
        var nx:Dynamic = FlightRuntime.getIndex(__destructure0, 0.0);
        var ny:Dynamic = FlightRuntime.getIndex(__destructure0, 1.0);
        var nz:Dynamic = FlightRuntime.getIndex(__destructure0, 2.0);
        var wx:Dynamic = ((((FlightRuntime.getIndex(m, 0.0) * nx) + (FlightRuntime.getIndex(m, 4.0) * ny)) + (FlightRuntime.getIndex(m, 8.0) * nz)) + FlightRuntime.getIndex(m, 12.0));
        var wy:Dynamic = ((((FlightRuntime.getIndex(m, 1.0) * nx) + (FlightRuntime.getIndex(m, 5.0) * ny)) + (FlightRuntime.getIndex(m, 9.0) * nz)) + FlightRuntime.getIndex(m, 13.0));
        var wz:Dynamic = ((((FlightRuntime.getIndex(m, 2.0) * nx) + (FlightRuntime.getIndex(m, 6.0) * ny)) + (FlightRuntime.getIndex(m, 10.0) * nz)) + FlightRuntime.getIndex(m, 14.0));
        var ww:Dynamic = ((((FlightRuntime.getIndex(m, 3.0) * nx) + (FlightRuntime.getIndex(m, 7.0) * ny)) + (FlightRuntime.getIndex(m, 11.0) * nz)) + FlightRuntime.getIndex(m, 15.0));
        if (FlightRuntime.truthy(!FlightRuntime.strictEquals(ww, 0.0))) {
  var invW:Dynamic = (1.0 / ww);
  (wx = cast ((wx * invW) : Dynamic));
  (wy = cast ((wy * invW) : Dynamic));
  (wz = cast ((wz * invW) : Dynamic));
}
        FlightRuntime.callProperty(results, 'push', cast ([cast ([wx, wy, wz] : Array<Dynamic>)] : Array<Dynamic>));
        i++;
      }
    }
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, 8.0, '<'))) {
        FlightRuntime.setField(FlightRuntime.getIndex(out, i), 'x', FlightRuntime.getIndex(FlightRuntime.getIndex(results, i), 0.0));
        FlightRuntime.setField(FlightRuntime.getIndex(out, i), 'y', FlightRuntime.getIndex(FlightRuntime.getIndex(results, i), 1.0));
        FlightRuntime.setField(FlightRuntime.getIndex(out, i), 'z', FlightRuntime.getIndex(FlightRuntime.getIndex(results, i), 2.0));
        i++;
      }
    }
    return cast true;
    return cast null;
  }

  @:keep public static function getCameraInverseViewProjectionMatrix4(out:Matrix4Like, camera:Camera, aspect:Float):Bool {
    FlightRuntime.callValue(getCameraViewProjectionMatrix4, cast ([CameraApi.__scratchViewProjection__camera, camera, aspect] : Array<Dynamic>));
    return cast FlightRuntime.callValue(inverseMatrix4, cast ([out, CameraApi.__scratchViewProjection__camera] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getCameraLinearDepth(camera:Camera, ndcZ:Float):Float {
    var near:Dynamic = cast FlightRuntime.UNDEFINED;
    var far:Dynamic = cast FlightRuntime.UNDEFINED;
    var range:Dynamic = cast FlightRuntime.UNDEFINED;
    var denominator:Dynamic = cast FlightRuntime.UNDEFINED;
    near = FlightRuntime.field(camera, 'near');
    far = FlightRuntime.field(camera, 'far');
    range = (far - near);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(range, 0.0))) {
  return cast 0.0;
}
    denominator = ((ndcZ * range) - (far + near));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(denominator, 0.0))) {
  return cast 0.0;
}
    return cast (((2.0 * far) * near) / denominator);
    return cast null;
  }

  @:keep public static function getCameraPosition(out:Vector3Like, camera:Camera):Void {
    var m:Dynamic = cast FlightRuntime.UNDEFINED;
    var m00:Dynamic = cast FlightRuntime.UNDEFINED;
    var m01:Dynamic = cast FlightRuntime.UNDEFINED;
    var m02:Dynamic = cast FlightRuntime.UNDEFINED;
    var m10:Dynamic = cast FlightRuntime.UNDEFINED;
    var m11:Dynamic = cast FlightRuntime.UNDEFINED;
    var m12:Dynamic = cast FlightRuntime.UNDEFINED;
    var m20:Dynamic = cast FlightRuntime.UNDEFINED;
    var m21:Dynamic = cast FlightRuntime.UNDEFINED;
    var m22:Dynamic = cast FlightRuntime.UNDEFINED;
    var tx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ty:Dynamic = cast FlightRuntime.UNDEFINED;
    var tz:Dynamic = cast FlightRuntime.UNDEFINED;
    m = FlightRuntime.field(FlightRuntime.field(camera, 'view'), 'm');
    m00 = FlightRuntime.getIndex(m, 0.0);
    m01 = FlightRuntime.getIndex(m, 1.0);
    m02 = FlightRuntime.getIndex(m, 2.0);
    m10 = FlightRuntime.getIndex(m, 4.0);
    m11 = FlightRuntime.getIndex(m, 5.0);
    m12 = FlightRuntime.getIndex(m, 6.0);
    m20 = FlightRuntime.getIndex(m, 8.0);
    m21 = FlightRuntime.getIndex(m, 9.0);
    m22 = FlightRuntime.getIndex(m, 10.0);
    tx = FlightRuntime.getIndex(m, 12.0);
    ty = FlightRuntime.getIndex(m, 13.0);
    tz = FlightRuntime.getIndex(m, 14.0);
    FlightRuntime.setField(out, 'x', -(((m00 * tx) + (m01 * ty)) + (m02 * tz)));
    FlightRuntime.setField(out, 'y', -(((m10 * tx) + (m11 * ty)) + (m12 * tz)));
    FlightRuntime.setField(out, 'z', -(((m20 * tx) + (m21 * ty)) + (m22 * tz)));
  }

  @:keep public static function getCameraRayThroughBoundingSphere(out:Ray3DLike, camera:Camera, sphere:BoundingSphereLike, aspect:Float):Bool {
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(sphere, 'radius'), 0.0, '<'))) {
  return cast false;
}
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(getCameraWorldToScreen, cast ([CameraApi.__scratchNdc__intersection, camera, FlightRuntime.field(sphere, 'center'), aspect] : Array<Dynamic>))))) {
  return cast false;
}
    return cast FlightRuntime.callValue(getCameraScreenToWorldRay, cast ([out, camera, FlightRuntime.field(CameraApi.__scratchNdc__intersection, 'x'), FlightRuntime.field(CameraApi.__scratchNdc__intersection, 'y'), aspect] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getCameraRight(out:Vector3Like, camera:Camera):Void {
    var m:Dynamic = cast FlightRuntime.UNDEFINED;
    m = FlightRuntime.field(FlightRuntime.field(camera, 'view'), 'm');
    FlightRuntime.setField(out, 'x', FlightRuntime.getIndex(m, 0.0));
    FlightRuntime.setField(out, 'y', FlightRuntime.getIndex(m, 4.0));
    FlightRuntime.setField(out, 'z', FlightRuntime.getIndex(m, 8.0));
  }

  @:keep public static function getCameraScreenToWorldRay(out:Ray3DLike, camera:Camera, ndcX:Float, ndcY:Float, aspect:Float):Bool {
    var m:Dynamic = cast FlightRuntime.UNDEFINED;
    var nx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ny:Dynamic = cast FlightRuntime.UNDEFINED;
    var nearX:Dynamic = cast FlightRuntime.UNDEFINED;
    var nearY:Dynamic = cast FlightRuntime.UNDEFINED;
    var nearZ:Dynamic = cast FlightRuntime.UNDEFINED;
    var nearW:Dynamic = cast FlightRuntime.UNDEFINED;
    var farX:Dynamic = cast FlightRuntime.UNDEFINED;
    var farY:Dynamic = cast FlightRuntime.UNDEFINED;
    var farZ:Dynamic = cast FlightRuntime.UNDEFINED;
    var farW:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.callValue(getCameraViewProjectionMatrix4, cast ([CameraApi.__scratchViewProjection__picking, camera, aspect] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(inverseMatrix4, cast ([CameraApi.__scratchInverseVP__picking, CameraApi.__scratchViewProjection__picking] : Array<Dynamic>))))) {
  return cast false;
}
    m = FlightRuntime.field(CameraApi.__scratchInverseVP__picking, 'm');
    nx = ndcX;
    ny = ndcY;
    nearX = ((((FlightRuntime.getIndex(m, 0.0) * nx) + (FlightRuntime.getIndex(m, 4.0) * ny)) + (FlightRuntime.getIndex(m, 8.0) * -1.0)) + FlightRuntime.getIndex(m, 12.0));
    nearY = ((((FlightRuntime.getIndex(m, 1.0) * nx) + (FlightRuntime.getIndex(m, 5.0) * ny)) + (FlightRuntime.getIndex(m, 9.0) * -1.0)) + FlightRuntime.getIndex(m, 13.0));
    nearZ = ((((FlightRuntime.getIndex(m, 2.0) * nx) + (FlightRuntime.getIndex(m, 6.0) * ny)) + (FlightRuntime.getIndex(m, 10.0) * -1.0)) + FlightRuntime.getIndex(m, 14.0));
    nearW = ((((FlightRuntime.getIndex(m, 3.0) * nx) + (FlightRuntime.getIndex(m, 7.0) * ny)) + (FlightRuntime.getIndex(m, 11.0) * -1.0)) + FlightRuntime.getIndex(m, 15.0));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(nearW, 0.0))) {
  var invW:Dynamic = (1.0 / nearW);
  (nearX = cast ((nearX * invW) : Dynamic));
  (nearY = cast ((nearY * invW) : Dynamic));
  (nearZ = cast ((nearZ * invW) : Dynamic));
}
    farX = ((((FlightRuntime.getIndex(m, 0.0) * nx) + (FlightRuntime.getIndex(m, 4.0) * ny)) + FlightRuntime.getIndex(m, 8.0)) + FlightRuntime.getIndex(m, 12.0));
    farY = ((((FlightRuntime.getIndex(m, 1.0) * nx) + (FlightRuntime.getIndex(m, 5.0) * ny)) + FlightRuntime.getIndex(m, 9.0)) + FlightRuntime.getIndex(m, 13.0));
    farZ = ((((FlightRuntime.getIndex(m, 2.0) * nx) + (FlightRuntime.getIndex(m, 6.0) * ny)) + FlightRuntime.getIndex(m, 10.0)) + FlightRuntime.getIndex(m, 14.0));
    farW = ((((FlightRuntime.getIndex(m, 3.0) * nx) + (FlightRuntime.getIndex(m, 7.0) * ny)) + FlightRuntime.getIndex(m, 11.0)) + FlightRuntime.getIndex(m, 15.0));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(farW, 0.0))) {
  var invW:Dynamic = (1.0 / farW);
  (farX = cast ((farX * invW) : Dynamic));
  (farY = cast ((farY * invW) : Dynamic));
  (farZ = cast ((farZ * invW) : Dynamic));
}
    FlightRuntime.setField(CameraApi.__scratchNear__picking, 'x', nearX);
    FlightRuntime.setField(CameraApi.__scratchNear__picking, 'y', nearY);
    FlightRuntime.setField(CameraApi.__scratchNear__picking, 'z', nearZ);
    FlightRuntime.setField(CameraApi.__scratchFar__picking, 'x', farX);
    FlightRuntime.setField(CameraApi.__scratchFar__picking, 'y', farY);
    FlightRuntime.setField(CameraApi.__scratchFar__picking, 'z', farZ);
    FlightRuntime.callValue(subtractVector3, cast ([CameraApi.__scratchDir__picking, CameraApi.__scratchFar__picking, CameraApi.__scratchNear__picking] : Array<Dynamic>));
    FlightRuntime.callValue(normalizeVector3, cast ([CameraApi.__scratchDir__picking, CameraApi.__scratchDir__picking] : Array<Dynamic>));
    FlightRuntime.setField(FlightRuntime.field(out, 'origin'), 'x', nearX);
    FlightRuntime.setField(FlightRuntime.field(out, 'origin'), 'y', nearY);
    FlightRuntime.setField(FlightRuntime.field(out, 'origin'), 'z', nearZ);
    FlightRuntime.setField(FlightRuntime.field(out, 'direction'), 'x', FlightRuntime.field(CameraApi.__scratchDir__picking, 'x'));
    FlightRuntime.setField(FlightRuntime.field(out, 'direction'), 'y', FlightRuntime.field(CameraApi.__scratchDir__picking, 'y'));
    FlightRuntime.setField(FlightRuntime.field(out, 'direction'), 'z', FlightRuntime.field(CameraApi.__scratchDir__picking, 'z'));
    return cast true;
    return cast null;
  }

  @:keep public static function getCameraUp(out:Vector3Like, camera:Camera):Void {
    var m:Dynamic = cast FlightRuntime.UNDEFINED;
    m = FlightRuntime.field(FlightRuntime.field(camera, 'view'), 'm');
    FlightRuntime.setField(out, 'x', FlightRuntime.getIndex(m, 1.0));
    FlightRuntime.setField(out, 'y', FlightRuntime.getIndex(m, 5.0));
    FlightRuntime.setField(out, 'z', FlightRuntime.getIndex(m, 9.0));
  }

  @:keep public static function getCameraViewProjectionMatrix4(out:Matrix4Like, camera:Camera, aspect:Float):Void {
    FlightRuntime.callValue(setProjectionMatrix4, cast ([CameraApi.__scratchProjection__camera, FlightRuntime.field(camera, 'projection'), aspect, FlightRuntime.field(camera, 'near'), FlightRuntime.field(camera, 'far')] : Array<Dynamic>));
    FlightRuntime.callValue(multiplyMatrix4, cast ([out, CameraApi.__scratchProjection__camera, FlightRuntime.field(camera, 'view')] : Array<Dynamic>));
  }

  @:keep public static function getCameraViewSpaceZ(camera:Camera, ndcZ:Float):Float {
    return cast -FlightRuntime.callValue(getCameraLinearDepth, cast ([camera, ndcZ] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getCameraWorldToScreen(out:Vector3Like, camera:Camera, worldPoint:Vector3Like, aspect:Float):Bool {
    var m:Dynamic = cast FlightRuntime.UNDEFINED;
    var wx:Dynamic = cast FlightRuntime.UNDEFINED;
    var wy:Dynamic = cast FlightRuntime.UNDEFINED;
    var wz:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipX:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipY:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipZ:Dynamic = cast FlightRuntime.UNDEFINED;
    var clipW:Dynamic = cast FlightRuntime.UNDEFINED;
    var invW:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.callValue(getCameraViewProjectionMatrix4, cast ([CameraApi.__scratchViewProjection__picking, camera, aspect] : Array<Dynamic>));
    m = FlightRuntime.field(CameraApi.__scratchViewProjection__picking, 'm');
    wx = FlightRuntime.field(worldPoint, 'x');
    wy = FlightRuntime.field(worldPoint, 'y');
    wz = FlightRuntime.field(worldPoint, 'z');
    clipX = ((((FlightRuntime.getIndex(m, 0.0) * wx) + (FlightRuntime.getIndex(m, 4.0) * wy)) + (FlightRuntime.getIndex(m, 8.0) * wz)) + FlightRuntime.getIndex(m, 12.0));
    clipY = ((((FlightRuntime.getIndex(m, 1.0) * wx) + (FlightRuntime.getIndex(m, 5.0) * wy)) + (FlightRuntime.getIndex(m, 9.0) * wz)) + FlightRuntime.getIndex(m, 13.0));
    clipZ = ((((FlightRuntime.getIndex(m, 2.0) * wx) + (FlightRuntime.getIndex(m, 6.0) * wy)) + (FlightRuntime.getIndex(m, 10.0) * wz)) + FlightRuntime.getIndex(m, 14.0));
    clipW = ((((FlightRuntime.getIndex(m, 3.0) * wx) + (FlightRuntime.getIndex(m, 7.0) * wy)) + (FlightRuntime.getIndex(m, 11.0) * wz)) + FlightRuntime.getIndex(m, 15.0));
    if (FlightRuntime.truthy(FlightRuntime.compare(clipW, 0.0, '<='))) {
  return cast false;
}
    invW = (1.0 / clipW);
    FlightRuntime.setField(out, 'x', (clipX * invW));
    FlightRuntime.setField(out, 'y', (clipY * invW));
    FlightRuntime.setField(out, 'z', (clipZ * invW));
    return cast true;
    return cast null;
  }

  @:keep public static function intersectCameraRayWithPlane(out:Vector3Like, ray:Ray3DLike, plane:PlaneLike):Bool {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var dz:Dynamic = cast FlightRuntime.UNDEFINED;
    var ox:Dynamic = cast FlightRuntime.UNDEFINED;
    var oy:Dynamic = cast FlightRuntime.UNDEFINED;
    var oz:Dynamic = cast FlightRuntime.UNDEFINED;
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var c:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    var denom:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = FlightRuntime.field(FlightRuntime.field(ray, 'direction'), 'x');
    dy = FlightRuntime.field(FlightRuntime.field(ray, 'direction'), 'y');
    dz = FlightRuntime.field(FlightRuntime.field(ray, 'direction'), 'z');
    ox = FlightRuntime.field(FlightRuntime.field(ray, 'origin'), 'x');
    oy = FlightRuntime.field(FlightRuntime.field(ray, 'origin'), 'y');
    oz = FlightRuntime.field(FlightRuntime.field(ray, 'origin'), 'z');
    a = FlightRuntime.field(plane, 'a');
    b = FlightRuntime.field(plane, 'b');
    c = FlightRuntime.field(plane, 'c');
    d = FlightRuntime.field(plane, 'd');
    denom = (((a * dx) + (b * dy)) + (c * dz));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(denom, 0.0))) {
  return cast false;
}
    t = (-((((a * ox) + (b * oy)) + (c * oz)) + d) / denom);
    if (FlightRuntime.truthy(FlightRuntime.compare(t, 0.0, '<'))) {
  return cast false;
}
    FlightRuntime.setField(out, 'x', (ox + (t * dx)));
    FlightRuntime.setField(out, 'y', (oy + (t * dy)));
    FlightRuntime.setField(out, 'z', (oz + (t * dz)));
    return cast true;
    return cast null;
  }

  @:keep public static function isBoxInCameraFrustum(camera:Camera, aabb:AabbLike, aspect:Float):Bool {
    FlightRuntime.callValue(getCameraFrustum, cast ([CameraApi.__scratchFrustum__culling, camera, aspect] : Array<Dynamic>));
    return cast FlightRuntime.callValue(isFrustumIntersectingAabb, cast ([CameraApi.__scratchFrustum__culling, aabb] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function isOrthographicProjection(projection:Projection):Bool {
    return cast FlightRuntime.strictEquals(FlightRuntime.field(projection, 'kind'), 'orthographic');
    return cast null;
  }

  @:keep public static function isPerspectiveProjection(projection:Projection):Bool {
    return cast FlightRuntime.strictEquals(FlightRuntime.field(projection, 'kind'), 'perspective');
    return cast null;
  }

  @:keep public static function isPointInCameraFrustum(camera:Camera, point:Vector3Like, aspect:Float):Bool {
    FlightRuntime.callValue(getCameraFrustum, cast ([CameraApi.__scratchFrustum__culling, camera, aspect] : Array<Dynamic>));
    return cast FlightRuntime.callValue(isFrustumContainingPoint, cast ([CameraApi.__scratchFrustum__culling, point] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function isSphereInCameraFrustum(camera:Camera, sphere:BoundingSphereLike, aspect:Float):Bool {
    FlightRuntime.callValue(getCameraFrustum, cast ([CameraApi.__scratchFrustum__culling, camera, aspect] : Array<Dynamic>));
    return cast FlightRuntime.callValue(isFrustumIntersectingSphere, cast ([CameraApi.__scratchFrustum__culling, sphere] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function setCameraJitter(camera:Camera, x:Float, y:Float):Void {
    FlightRuntime.setField(FlightRuntime.field(camera, 'jitter'), 'x', x);
    FlightRuntime.setField(FlightRuntime.field(camera, 'jitter'), 'y', y);
  }

  @:keep public static function setCameraViewMatrix4FromLookAt(camera:Camera, eye:Vector3Like, target:Vector3Like, up:Vector3Like):Void {
    FlightRuntime.callValue(setMatrix4LookAt, cast ([FlightRuntime.field(camera, 'view'), eye, target, up] : Array<Dynamic>));
  }

  @:keep public static function setCameraViewMatrix4FromMatrix4(camera:Camera, view:Matrix4Like):Void {
    FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(camera, 'view'), 'm'), 'set', cast ([FlightRuntime.field(view, 'm')] : Array<Dynamic>));
  }

  @:keep public static function setProjectionMatrix4(out:Matrix4Like, projection:Projection, aspect:Float, near:Float, far:Float):Void {
    var halfWidth:Dynamic = cast FlightRuntime.UNDEFINED;
    var halfHeight:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(projection, 'kind'), 'perspective'))) {
  var tanHalfFovY:Dynamic = FlightRuntime.callProperty(HxMath, 'tan', cast ([(FlightRuntime.field(projection, 'fovY') * 0.5)] : Array<Dynamic>));
  FlightRuntime.callValue(setPerspectiveMatrix4, cast ([out, tanHalfFovY, aspect, near, far] : Array<Dynamic>));
  return;
}
    halfWidth = FlightRuntime.field(projection, 'halfWidth');
    halfHeight = FlightRuntime.field(projection, 'halfHeight');
    FlightRuntime.callValue(setOrthographicMatrix4, cast ([out, -halfWidth, halfWidth, -halfHeight, halfHeight, near, far] : Array<Dynamic>));
  }

  @:keep public static function updateCameraInverseViewProjection(camera:Camera, aspect:Float):Bool {
    var ok:Dynamic = cast FlightRuntime.UNDEFINED;
    ok = FlightRuntime.callValue(getCameraInverseViewProjectionMatrix4, cast ([CameraApi.__scratchInverse__camera, camera, aspect] : Array<Dynamic>));
    if (FlightRuntime.truthy(ok)) {
  FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(camera, 'inverseViewProjection'), 'm'), 'set', cast ([FlightRuntime.field(CameraApi.__scratchInverse__camera, 'm')] : Array<Dynamic>));
}
    return cast ok;
    return cast null;
  }
}
