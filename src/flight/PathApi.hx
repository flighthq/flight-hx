// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Types.MatrixLike;
import flight.Types.Path;
import flight.Types.PathCommand;
import flight.Types.PathMesh;
import flight.Types.PathMeshTyped;
import flight.Types.PathSegment;
import flight.Types.PathWinding;
import flight.Types.RectangleLike;
import flight.Types.StrokeStyle;
import flight.Types.Vector2Like;

typedef DashSegment__strokePath = { var closed:Bool; var points:Array<Float>; };

typedef StrokeSubpath__strokePath = { var closed:Bool; var points:Array<Float>; };

typedef Subpath__reversePath = { var closed:Bool; var points:Array<SubpathPoint__reversePath>; };

typedef SubpathPoint__reversePath = Dynamic;

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.PathApi")
class PathApi {
  @:keep public static function acquirePathMesh(path:Path, tolerance:Dynamic = 0.25):PathMesh {
    var mesh:Dynamic = cast FlightRuntime.UNDEFINED;
    var fresh:Dynamic = cast FlightRuntime.UNDEFINED;
    mesh = FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(PathApi.pathMeshPool__pathMeshPool, 'length'), 0.0, '>'), function():Dynamic return cast FlightRuntime.callProperty(PathApi.pathMeshPool__pathMeshPool, 'pop', cast ([] : Array<Dynamic>)), function():Dynamic return cast { vertices: cast ([] : Array<Dynamic>), indices: cast ([] : Array<Dynamic>) });
    FlightRuntime.setLength(FlightRuntime.field(mesh, 'vertices'), 0.0);
    FlightRuntime.setLength(FlightRuntime.field(mesh, 'indices'), 0.0);
    fresh = FlightRuntime.callValue(tessellatePath, cast ([path, tolerance] : Array<Dynamic>));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(FlightRuntime.field(fresh, 'vertices'), 'length'), '<'))) {
        FlightRuntime.setIndex(FlightRuntime.field(mesh, 'vertices'), i, FlightRuntime.getIndex(FlightRuntime.field(fresh, 'vertices'), i));
        i++;
      }
    }
    FlightRuntime.setLength(FlightRuntime.field(mesh, 'vertices'), FlightRuntime.field(FlightRuntime.field(fresh, 'vertices'), 'length'));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(FlightRuntime.field(fresh, 'indices'), 'length'), '<'))) {
        FlightRuntime.setIndex(FlightRuntime.field(mesh, 'indices'), i, FlightRuntime.getIndex(FlightRuntime.field(fresh, 'indices'), i));
        i++;
      }
    }
    FlightRuntime.setLength(FlightRuntime.field(mesh, 'indices'), FlightRuntime.field(FlightRuntime.field(fresh, 'indices'), 'length'));
    return cast mesh;
    return cast null;
  }

  @:keep public static function acquirePathMeshTyped(path:Path, tolerance:Dynamic = 0.25):PathMeshTyped {
    var fresh:Dynamic = cast FlightRuntime.UNDEFINED;
    var mesh:Dynamic = cast FlightRuntime.UNDEFINED;
    fresh = FlightRuntime.callValue(tessellatePathTyped, cast ([path, tolerance] : Array<Dynamic>));
    mesh = FlightRuntime.select(FlightRuntime.compare(FlightRuntime.field(PathApi.typedPool__pathMeshPool, 'length'), 0.0, '>'), function():Dynamic return cast FlightRuntime.callProperty(PathApi.typedPool__pathMeshPool, 'pop', cast ([] : Array<Dynamic>)), function():Dynamic return cast { vertices: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [0.0]), indices: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Uint32Array'] : Array<Dynamic>)), [0.0]) });
    FlightRuntime.setField(mesh, 'vertices', FlightRuntime.field(fresh, 'vertices'));
    FlightRuntime.setField(mesh, 'indices', FlightRuntime.field(fresh, 'indices'));
    return cast mesh;
    return cast null;
  }

  @:keep public static function addArcPoints__strokePath(cx:Float, cy:Float, r:Float, startAngle:Float, endAngle:Float, ccw:Bool, tolerance:Float, out:Array<Float>):Void {
    var ratio:Dynamic = cast FlightRuntime.UNDEFINED;
    var n:Dynamic = cast FlightRuntime.UNDEFINED;
    var delta:Dynamic = cast FlightRuntime.UNDEFINED;
    var steps:Dynamic = cast FlightRuntime.UNDEFINED;
    var stepAngle:Dynamic = cast FlightRuntime.UNDEFINED;
    ratio = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, (tolerance / r)] : Array<Dynamic>))] : Array<Dynamic>));
    n = FlightRuntime.callProperty(HxMath, 'max', cast ([4.0, FlightRuntime.callProperty(HxMath, 'ceil', cast ([(HxMath.PI / FlightRuntime.callProperty(HxMath, 'acos', cast ([(1.0 - ratio)] : Array<Dynamic>)))] : Array<Dynamic>))] : Array<Dynamic>));
    delta = (endAngle - startAngle);
    if (FlightRuntime.truthy(ccw)) {
  if (FlightRuntime.truthy(FlightRuntime.compare(delta, 0.0, '>'))) { (delta = cast ((delta - (HxMath.PI * 2.0)) : Dynamic)); }
} else {
  if (FlightRuntime.truthy(FlightRuntime.compare(delta, 0.0, '<'))) { (delta = cast ((delta + (HxMath.PI * 2.0)) : Dynamic)); }
}
    steps = FlightRuntime.callProperty(HxMath, 'ceil', cast ([(FlightRuntime.callProperty(HxMath, 'abs', cast ([delta] : Array<Dynamic>)) / ((HxMath.PI * 2.0) / n))] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(steps, 1.0, '<='))) { return; }
    stepAngle = (delta / steps);
    {
      var i:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, steps, '<'))) {
        var angle:Dynamic = (startAngle + (i * stepAngle));
        FlightRuntime.pushMany(out, cast ([(cx + (FlightRuntime.callProperty(HxMath, 'cos', cast ([angle] : Array<Dynamic>)) * r)), (cy + (FlightRuntime.callProperty(HxMath, 'sin', cast ([angle] : Array<Dynamic>)) * r))] : Array<Dynamic>));
        i++;
      }
    }
  }

  @:keep public static function addCap__strokePath(px:Float, py:Float, nx:Float, ny:Float, edx:Float, edy:Float, halfWidth:Float, cap:String, left:Array<Float>, right:Array<Float>, tolerance:Float, isStart:Bool):Void {
    var lx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ly:Dynamic = cast FlightRuntime.UNDEFINED;
    var rx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ry:Dynamic = cast FlightRuntime.UNDEFINED;
    lx = (px + (nx * halfWidth));
    ly = (py + (ny * halfWidth));
    rx = (px - (nx * halfWidth));
    ry = (py - (ny * halfWidth));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(cap, 'butt'))) {
  FlightRuntime.pushMany(left, cast ([lx, ly] : Array<Dynamic>));
  FlightRuntime.pushMany(right, cast ([rx, ry] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(cap, 'square'))) {
  FlightRuntime.pushMany(left, cast ([(lx + (edx * halfWidth)), (ly + (edy * halfWidth))] : Array<Dynamic>));
  FlightRuntime.pushMany(right, cast ([(rx + (edx * halfWidth)), (ry + (edy * halfWidth))] : Array<Dynamic>));
} else {
  if (FlightRuntime.truthy(isStart)) {
  FlightRuntime.pushMany(left, cast ([lx, ly] : Array<Dynamic>));
  FlightRuntime.pushMany(right, cast ([rx, ry] : Array<Dynamic>));
  var startAngle:Dynamic = FlightRuntime.callProperty(HxMath, 'atan2', cast ([-ny, -nx] : Array<Dynamic>));
  var endAngle:Dynamic = FlightRuntime.callProperty(HxMath, 'atan2', cast ([ny, nx] : Array<Dynamic>));
  FlightRuntime.callValue(PathApi.addArcPoints__strokePath, cast ([px, py, halfWidth, startAngle, endAngle, false, tolerance, right] : Array<Dynamic>));
} else {
  FlightRuntime.pushMany(left, cast ([lx, ly] : Array<Dynamic>));
  FlightRuntime.callValue(PathApi.addArcPoints__strokePath, cast ([px, py, halfWidth, FlightRuntime.callProperty(HxMath, 'atan2', cast ([ny, nx] : Array<Dynamic>)), FlightRuntime.callProperty(HxMath, 'atan2', cast ([-ny, -nx] : Array<Dynamic>)), true, tolerance, left] : Array<Dynamic>));
  FlightRuntime.pushMany(right, cast ([rx, ry] : Array<Dynamic>));
}
} }
  }

  @:keep public static function addJoin__strokePath(px:Float, py:Float, nx0:Float, ny0:Float, nx1:Float, ny1:Float, halfWidth:Float, join:String, miterLimit:Float, left:Array<Float>, right:Array<Float>, tolerance:Float):Void {
    var lx0:Dynamic = cast FlightRuntime.UNDEFINED;
    var ly0:Dynamic = cast FlightRuntime.UNDEFINED;
    var rx0:Dynamic = cast FlightRuntime.UNDEFINED;
    var ry0:Dynamic = cast FlightRuntime.UNDEFINED;
    var lx1:Dynamic = cast FlightRuntime.UNDEFINED;
    var ly1:Dynamic = cast FlightRuntime.UNDEFINED;
    var rx1:Dynamic = cast FlightRuntime.UNDEFINED;
    var ry1:Dynamic = cast FlightRuntime.UNDEFINED;
    lx0 = (px + (nx0 * halfWidth));
    ly0 = (py + (ny0 * halfWidth));
    rx0 = (px - (nx0 * halfWidth));
    ry0 = (py - (ny0 * halfWidth));
    lx1 = (px + (nx1 * halfWidth));
    ly1 = (py + (ny1 * halfWidth));
    rx1 = (px - (nx1 * halfWidth));
    ry1 = (py - (ny1 * halfWidth));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(join, 'miter'))) {
  var cross:Dynamic = ((nx0 * ny1) - (ny0 * nx1));
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([cross] : Array<Dynamic>)), 1e-8, '<'))) {
  FlightRuntime.pushMany(left, cast ([lx0, ly0] : Array<Dynamic>));
  FlightRuntime.pushMany(right, cast ([rx0, ry0] : Array<Dynamic>));
} else {
  var dx:Dynamic = (lx1 - lx0);
  var dy:Dynamic = (ly1 - ly0);
  var t:Dynamic = (((dx * ny1) - (dy * nx1)) / cross);
  var mx:Dynamic = (lx0 + (t * nx0));
  var my:Dynamic = (ly0 + (t * ny0));
  var miterLen:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([(((mx - px) * (mx - px)) + ((my - py) * (my - py)))] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.compare(miterLen, (halfWidth * miterLimit), '<='))) {
  FlightRuntime.pushMany(left, cast ([mx, my] : Array<Dynamic>));
  var rmx:Dynamic = ((px * 2.0) - mx);
  var rmy:Dynamic = ((py * 2.0) - my);
  FlightRuntime.pushMany(right, cast ([rmx, rmy] : Array<Dynamic>));
} else {
  FlightRuntime.pushMany(left, cast ([lx0, ly0, lx1, ly1] : Array<Dynamic>));
  FlightRuntime.pushMany(right, cast ([rx0, ry0, rx1, ry1] : Array<Dynamic>));
}
}
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(join, 'round'))) {
  FlightRuntime.pushMany(left, cast ([lx0, ly0] : Array<Dynamic>));
  FlightRuntime.callValue(PathApi.addArcPoints__strokePath, cast ([px, py, halfWidth, FlightRuntime.callProperty(HxMath, 'atan2', cast ([ny0, nx0] : Array<Dynamic>)), FlightRuntime.callProperty(HxMath, 'atan2', cast ([ny1, nx1] : Array<Dynamic>)), true, tolerance, left] : Array<Dynamic>));
  FlightRuntime.pushMany(left, cast ([lx1, ly1] : Array<Dynamic>));
  FlightRuntime.pushMany(right, cast ([rx0, ry0] : Array<Dynamic>));
  FlightRuntime.callValue(PathApi.addArcPoints__strokePath, cast ([px, py, halfWidth, FlightRuntime.callProperty(HxMath, 'atan2', cast ([-ny0, -nx0] : Array<Dynamic>)), FlightRuntime.callProperty(HxMath, 'atan2', cast ([-ny1, -nx1] : Array<Dynamic>)), false, tolerance, right] : Array<Dynamic>));
  FlightRuntime.pushMany(right, cast ([rx1, ry1] : Array<Dynamic>));
} else {
  FlightRuntime.pushMany(left, cast ([lx0, ly0, lx1, ly1] : Array<Dynamic>));
  FlightRuntime.pushMany(right, cast ([rx0, ry0, rx1, ry1] : Array<Dynamic>));
} }
  }

  @:keep public static function appendArcCubics__path(path:Path, cx:Float, cy:Float, rx:Float, ry:Float, xAxisRotation:Float, theta1:Float, dtheta:Float):Void {
    var nSegs:Dynamic = cast FlightRuntime.UNDEFINED;
    var dt:Dynamic = cast FlightRuntime.UNDEFINED;
    var cos_u3C6_:Dynamic = cast FlightRuntime.UNDEFINED;
    var sin_u3C6_:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(dtheta, 0.0))) { return; }
    nSegs = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'ceil', cast ([(FlightRuntime.callProperty(HxMath, 'abs', cast ([dtheta] : Array<Dynamic>)) / (HxMath.PI / 2.0))] : Array<Dynamic>))] : Array<Dynamic>));
    dt = (dtheta / nSegs);
    cos_u3C6_ = FlightRuntime.callProperty(HxMath, 'cos', cast ([xAxisRotation] : Array<Dynamic>));
    sin_u3C6_ = FlightRuntime.callProperty(HxMath, 'sin', cast ([xAxisRotation] : Array<Dynamic>));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, nSegs, '<'))) {
        var t1:Dynamic = (theta1 + (i * dt));
        var t2:Dynamic = (t1 + dt);
        var cos1:Dynamic = FlightRuntime.callProperty(HxMath, 'cos', cast ([t1] : Array<Dynamic>));
        var sin1:Dynamic = FlightRuntime.callProperty(HxMath, 'sin', cast ([t1] : Array<Dynamic>));
        var cos2:Dynamic = FlightRuntime.callProperty(HxMath, 'cos', cast ([t2] : Array<Dynamic>));
        var sin2:Dynamic = FlightRuntime.callProperty(HxMath, 'sin', cast ([t2] : Array<Dynamic>));
        var alpha:Dynamic = ((4.0 / 3.0) * FlightRuntime.callProperty(HxMath, 'tan', cast ([(dt / 4.0)] : Array<Dynamic>)));
        var dx1:Dynamic = ((-rx * sin1) * alpha);
        var dy1:Dynamic = ((ry * cos1) * alpha);
        var dx2:Dynamic = ((rx * sin2) * alpha);
        var dy2:Dynamic = ((-ry * cos2) * alpha);
        var p1x:Dynamic = ((cx + ((cos_u3C6_ * rx) * cos1)) - ((sin_u3C6_ * ry) * sin1));
        var p1y:Dynamic = ((cy + ((sin_u3C6_ * rx) * cos1)) + ((cos_u3C6_ * ry) * sin1));
        var p2x:Dynamic = ((cx + ((cos_u3C6_ * rx) * cos2)) - ((sin_u3C6_ * ry) * sin2));
        var p2y:Dynamic = ((cy + ((sin_u3C6_ * rx) * cos2)) + ((cos_u3C6_ * ry) * sin2));
        var c1x:Dynamic = ((p1x + (cos_u3C6_ * dx1)) - (sin_u3C6_ * dy1));
        var c1y:Dynamic = ((p1y + (sin_u3C6_ * dx1)) + (cos_u3C6_ * dy1));
        var c2x:Dynamic = ((p2x + (cos_u3C6_ * dx2)) - (sin_u3C6_ * dy2));
        var c2y:Dynamic = ((p2y + (sin_u3C6_ * dx2)) + (cos_u3C6_ * dy2));
        FlightRuntime.callValue(appendPathCubicCurveTo, cast ([path, c1x, c1y, c2x, c2y, p2x, p2y] : Array<Dynamic>));
        i++;
      }
    }
  }

  @:keep public static function appendCornerArc__path(path:Path, cx:Float, cy:Float, radius:Float, startAngle:Float, endAngle:Float):Void {
    var k:Dynamic = cast FlightRuntime.UNDEFINED;
    var cosStart:Dynamic = cast FlightRuntime.UNDEFINED;
    var sinStart:Dynamic = cast FlightRuntime.UNDEFINED;
    var cosEnd:Dynamic = cast FlightRuntime.UNDEFINED;
    var sinEnd:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.compare(radius, 0.0, '<='))) { return; }
    k = (radius * PathApi.KAPPA__path);
    cosStart = FlightRuntime.callProperty(HxMath, 'cos', cast ([startAngle] : Array<Dynamic>));
    sinStart = FlightRuntime.callProperty(HxMath, 'sin', cast ([startAngle] : Array<Dynamic>));
    cosEnd = FlightRuntime.callProperty(HxMath, 'cos', cast ([endAngle] : Array<Dynamic>));
    sinEnd = FlightRuntime.callProperty(HxMath, 'sin', cast ([endAngle] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathCubicCurveTo, cast ([path, ((cx + (cosStart * radius)) - (sinStart * k)), ((cy + (sinStart * radius)) + (cosStart * k)), ((cx + (cosEnd * radius)) + (sinEnd * k)), ((cy + (sinEnd * radius)) - (cosEnd * k)), (cx + (cosEnd * radius)), (cy + (sinEnd * radius))] : Array<Dynamic>));
  }

  @:keep public static function appendPathArc(path:Path, cx:Float, cy:Float, radius:Float, startAngle:Float, endAngle:Float, anticlockwise:Dynamic = false, connectToCurrent:Dynamic = false):Void {
    var sweep:Dynamic = cast FlightRuntime.UNDEFINED;
    var arcStartX:Dynamic = cast FlightRuntime.UNDEFINED;
    var arcStartY:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.compare(radius, 0.0, '<='))) { return; }
    sweep = (endAngle - startAngle);
    if (FlightRuntime.truthy(anticlockwise)) {
  if (FlightRuntime.truthy(FlightRuntime.compare(sweep, 0.0, '>'))) { (sweep = cast ((sweep - (HxMath.PI * 2.0)) : Dynamic)); }
} else {
  if (FlightRuntime.truthy(FlightRuntime.compare(sweep, 0.0, '<'))) { (sweep = cast ((sweep + (HxMath.PI * 2.0)) : Dynamic)); }
}
    arcStartX = (cx + (FlightRuntime.callProperty(HxMath, 'cos', cast ([startAngle] : Array<Dynamic>)) * radius));
    arcStartY = (cy + (FlightRuntime.callProperty(HxMath, 'sin', cast ([startAngle] : Array<Dynamic>)) * radius));
    if (FlightRuntime.truthy(connectToCurrent)) {
  FlightRuntime.callValue(appendPathLineTo, cast ([path, arcStartX, arcStartY] : Array<Dynamic>));
} else {
  FlightRuntime.callValue(appendPathMoveTo, cast ([path, arcStartX, arcStartY] : Array<Dynamic>));
}
    FlightRuntime.callValue(PathApi.appendArcCubics__path, cast ([path, cx, cy, radius, radius, 0.0, startAngle, sweep] : Array<Dynamic>));
  }

  @:keep public static function appendPathArcTo(path:Path, radiusX:Float, radiusY:Float, xAxisRotation:Float, largeArc:Bool, sweep:Bool, endX:Float, endY:Float):Void {
    var x1:Dynamic = cast FlightRuntime.UNDEFINED;
    var y1:Dynamic = cast FlightRuntime.UNDEFINED;
    var x2:Dynamic = cast FlightRuntime.UNDEFINED;
    var y2:Dynamic = cast FlightRuntime.UNDEFINED;
    var rx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ry:Dynamic = cast FlightRuntime.UNDEFINED;
    var cos_u3C6_:Dynamic = cast FlightRuntime.UNDEFINED;
    var sin_u3C6_:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var x1p:Dynamic = cast FlightRuntime.UNDEFINED;
    var y1p:Dynamic = cast FlightRuntime.UNDEFINED;
    var x1pSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var y1pSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var rxSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var rySq:Dynamic = cast FlightRuntime.UNDEFINED;
    var lambda:Dynamic = cast FlightRuntime.UNDEFINED;
    var rxSq2:Dynamic = cast FlightRuntime.UNDEFINED;
    var rySq2:Dynamic = cast FlightRuntime.UNDEFINED;
    var num:Dynamic = cast FlightRuntime.UNDEFINED;
    var den:Dynamic = cast FlightRuntime.UNDEFINED;
    var sq:Dynamic = cast FlightRuntime.UNDEFINED;
    var sign:Dynamic = cast FlightRuntime.UNDEFINED;
    var cxp:Dynamic = cast FlightRuntime.UNDEFINED;
    var cyp:Dynamic = cast FlightRuntime.UNDEFINED;
    var cx:Dynamic = cast FlightRuntime.UNDEFINED;
    var cy:Dynamic = cast FlightRuntime.UNDEFINED;
    var ux:Dynamic = cast FlightRuntime.UNDEFINED;
    var uy:Dynamic = cast FlightRuntime.UNDEFINED;
    var vx:Dynamic = cast FlightRuntime.UNDEFINED;
    var vy:Dynamic = cast FlightRuntime.UNDEFINED;
    var theta1:Dynamic = cast FlightRuntime.UNDEFINED;
    var dtheta:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(radiusX, 0.0), function():Dynamic return cast FlightRuntime.strictEquals(radiusY, 0.0)))) {
  FlightRuntime.callValue(appendPathLineTo, cast ([path, endX, endY] : Array<Dynamic>));
  return;
}
    x1 = 0.0;
    y1 = 0.0;
    {
      var last:Dynamic = FlightRuntime.callValue(getPathLastPoint, cast ([path] : Array<Dynamic>));
      if (FlightRuntime.truthy(!FlightRuntime.strictEquals(last, null))) {
  (x1 = cast (FlightRuntime.getIndex(last, 0.0) : Dynamic));
  (y1 = cast (FlightRuntime.getIndex(last, 1.0) : Dynamic));
}
    }
    x2 = endX;
    y2 = endY;
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(x1, x2), function():Dynamic return cast FlightRuntime.strictEquals(y1, y2)))) { return; }
    rx = FlightRuntime.callProperty(HxMath, 'abs', cast ([radiusX] : Array<Dynamic>));
    ry = FlightRuntime.callProperty(HxMath, 'abs', cast ([radiusY] : Array<Dynamic>));
    cos_u3C6_ = FlightRuntime.callProperty(HxMath, 'cos', cast ([xAxisRotation] : Array<Dynamic>));
    sin_u3C6_ = FlightRuntime.callProperty(HxMath, 'sin', cast ([xAxisRotation] : Array<Dynamic>));
    dx = ((x1 - x2) / 2.0);
    dy = ((y1 - y2) / 2.0);
    x1p = ((cos_u3C6_ * dx) + (sin_u3C6_ * dy));
    y1p = ((-sin_u3C6_ * dx) + (cos_u3C6_ * dy));
    x1pSq = (x1p * x1p);
    y1pSq = (y1p * y1p);
    rxSq = (rx * rx);
    rySq = (ry * ry);
    lambda = ((x1pSq / rxSq) + (y1pSq / rySq));
    if (FlightRuntime.truthy(FlightRuntime.compare(lambda, 1.0, '>'))) {
  var sqrtLambda:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([lambda] : Array<Dynamic>));
  (rx = cast ((rx * sqrtLambda) : Dynamic));
  (ry = cast ((ry * sqrtLambda) : Dynamic));
}
    rxSq2 = (rx * rx);
    rySq2 = (ry * ry);
    num = (((rxSq2 * rySq2) - (rxSq2 * y1pSq)) - (rySq2 * x1pSq));
    den = ((rxSq2 * y1pSq) + (rySq2 * x1pSq));
    sq = FlightRuntime.select(FlightRuntime.compare(den, 0.0, '<='), function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.callProperty(HxMath, 'sqrt', cast ([FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, (num / den)] : Array<Dynamic>))] : Array<Dynamic>)));
    sign = FlightRuntime.select(FlightRuntime.strictEquals(largeArc, sweep), function():Dynamic return cast -1.0, function():Dynamic return cast 1.0);
    cxp = (((sign * sq) * (rx * y1p)) / ry);
    cyp = (((sign * sq) * (-ry * x1p)) / rx);
    cx = (((cos_u3C6_ * cxp) - (sin_u3C6_ * cyp)) + ((x1 + x2) / 2.0));
    cy = (((sin_u3C6_ * cxp) + (cos_u3C6_ * cyp)) + ((y1 + y2) / 2.0));
    ux = ((x1p - cxp) / rx);
    uy = ((y1p - cyp) / ry);
    vx = ((-x1p - cxp) / rx);
    vy = ((-y1p - cyp) / ry);
    theta1 = FlightRuntime.callValue(PathApi.vectorAngle__path, cast ([1.0, 0.0, ux, uy] : Array<Dynamic>));
    dtheta = FlightRuntime.callValue(PathApi.vectorAngle__path, cast ([ux, uy, vx, vy] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.truthy(sweep), function():Dynamic return cast FlightRuntime.compare(dtheta, 0.0, '>')))) { (dtheta = cast ((dtheta - (HxMath.PI * 2.0)) : Dynamic)); }
    if (FlightRuntime.truthy(FlightRuntime.andValue(sweep, function():Dynamic return cast FlightRuntime.compare(dtheta, 0.0, '<')))) { (dtheta = cast ((dtheta + (HxMath.PI * 2.0)) : Dynamic)); }
    FlightRuntime.callValue(PathApi.appendArcCubics__path, cast ([path, cx, cy, rx, ry, xAxisRotation, theta1, dtheta] : Array<Dynamic>));
  }

  @:keep public static function appendPathCircle(path:Path, cx:Float, cy:Float, radius:Float):Void {
    FlightRuntime.callValue(appendPathEllipse, cast ([path, cx, cy, radius, radius] : Array<Dynamic>));
  }

  @:keep public static function appendPathClose(path:Path):Void {
    FlightRuntime.callProperty(FlightRuntime.field(path, 'commands'), 'push', cast ([Types.PathCommandValue.CLOSE] : Array<Dynamic>));
  }

  @:keep public static function appendPathCubicCurveTo(path:Path, control1X:Float, control1Y:Float, control2X:Float, control2Y:Float, anchorX:Float, anchorY:Float):Void {
    FlightRuntime.callProperty(FlightRuntime.field(path, 'commands'), 'push', cast ([Types.PathCommandValue.CUBIC_CURVE_TO] : Array<Dynamic>));
    FlightRuntime.pushMany(FlightRuntime.field(path, 'data'), cast ([control1X, control1Y, control2X, control2Y, anchorX, anchorY] : Array<Dynamic>));
  }

  @:keep public static function appendPathCurveTo(path:Path, controlX:Float, controlY:Float, anchorX:Float, anchorY:Float):Void {
    FlightRuntime.callProperty(FlightRuntime.field(path, 'commands'), 'push', cast ([Types.PathCommandValue.CURVE_TO] : Array<Dynamic>));
    FlightRuntime.pushMany(FlightRuntime.field(path, 'data'), cast ([controlX, controlY, anchorX, anchorY] : Array<Dynamic>));
  }

  @:keep public static function appendPathEllipse(path:Path, cx:Float, cy:Float, radiusX:Float, radiusY:Float):Void {
    var kx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ky:Dynamic = cast FlightRuntime.UNDEFINED;
    kx = (radiusX * PathApi.KAPPA__path);
    ky = (radiusY * PathApi.KAPPA__path);
    FlightRuntime.callValue(appendPathMoveTo, cast ([path, (cx + radiusX), cy] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathCubicCurveTo, cast ([path, (cx + radiusX), (cy - ky), (cx + kx), (cy - radiusY), cx, (cy - radiusY)] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathCubicCurveTo, cast ([path, (cx - kx), (cy - radiusY), (cx - radiusX), (cy - ky), (cx - radiusX), cy] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathCubicCurveTo, cast ([path, (cx - radiusX), (cy + ky), (cx - kx), (cy + radiusY), cx, (cy + radiusY)] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathCubicCurveTo, cast ([path, (cx + kx), (cy + radiusY), (cx + radiusX), (cy + ky), (cx + radiusX), cy] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathClose, cast ([path] : Array<Dynamic>));
  }

  @:keep public static function appendPathLineTo(path:Path, x:Float, y:Float):Void {
    FlightRuntime.callProperty(FlightRuntime.field(path, 'commands'), 'push', cast ([Types.PathCommandValue.LINE_TO] : Array<Dynamic>));
    FlightRuntime.pushMany(FlightRuntime.field(path, 'data'), cast ([x, y] : Array<Dynamic>));
  }

  @:keep public static function appendPathMoveTo(path:Path, x:Float, y:Float):Void {
    FlightRuntime.callProperty(FlightRuntime.field(path, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
    FlightRuntime.pushMany(FlightRuntime.field(path, 'data'), cast ([x, y] : Array<Dynamic>));
  }

  @:keep public static function appendPathPolygon(path:Path, points:Array<Float>):Void {
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(points, 'length'), 6.0, '<'))) { return; }
    FlightRuntime.callValue(appendPathMoveTo, cast ([path, FlightRuntime.getIndex(points, 0.0), FlightRuntime.getIndex(points, 1.0)] : Array<Dynamic>));
    {
      var i:Dynamic = 2.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(points, 'length'), '<'))) {
        FlightRuntime.callValue(appendPathLineTo, cast ([path, FlightRuntime.getIndex(points, i), FlightRuntime.getIndex(points, (i + 1.0))] : Array<Dynamic>));
        (i = cast ((i + 2.0) : Dynamic));
      }
    }
    FlightRuntime.callValue(appendPathClose, cast ([path] : Array<Dynamic>));
  }

  @:keep public static function appendPathPolyline(path:Path, points:Array<Float>):Void {
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(points, 'length'), 4.0, '<'))) { return; }
    FlightRuntime.callValue(appendPathMoveTo, cast ([path, FlightRuntime.getIndex(points, 0.0), FlightRuntime.getIndex(points, 1.0)] : Array<Dynamic>));
    {
      var i:Dynamic = 2.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(points, 'length'), '<'))) {
        FlightRuntime.callValue(appendPathLineTo, cast ([path, FlightRuntime.getIndex(points, i), FlightRuntime.getIndex(points, (i + 1.0))] : Array<Dynamic>));
        (i = cast ((i + 2.0) : Dynamic));
      }
    }
  }

  @:keep public static function appendPathRectangle(path:Path, x:Float, y:Float, width:Float, height:Float):Void {
    FlightRuntime.callValue(appendPathMoveTo, cast ([path, x, y] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathLineTo, cast ([path, (x + width), y] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathLineTo, cast ([path, (x + width), (y + height)] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathLineTo, cast ([path, x, (y + height)] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathClose, cast ([path] : Array<Dynamic>));
  }

  @:keep public static function appendPathRoundRectangle(path:Path, x:Float, y:Float, width:Float, height:Float, radius:Dynamic):Void {
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var rtl:Dynamic = cast FlightRuntime.UNDEFINED;
    var rtr:Dynamic = cast FlightRuntime.UNDEFINED;
    var rbr:Dynamic = cast FlightRuntime.UNDEFINED;
    var rbl:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure0 = FlightRuntime.callValue(PathApi.normalizeCornerRadii__path, cast ([radius, width, height] : Array<Dynamic>));
    rtl = FlightRuntime.getIndex(__destructure0, 0.0);
    rtr = FlightRuntime.getIndex(__destructure0, 1.0);
    rbr = FlightRuntime.getIndex(__destructure0, 2.0);
    rbl = FlightRuntime.getIndex(__destructure0, 3.0);
    FlightRuntime.callValue(appendPathMoveTo, cast ([path, (x + rtl), y] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathLineTo, cast ([path, ((x + width) - rtr), y] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.appendCornerArc__path, cast ([path, ((x + width) - rtr), (y + rtr), rtr, (-HxMath.PI / 2.0), 0.0] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathLineTo, cast ([path, (x + width), ((y + height) - rbr)] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.appendCornerArc__path, cast ([path, ((x + width) - rbr), ((y + height) - rbr), rbr, 0.0, (HxMath.PI / 2.0)] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathLineTo, cast ([path, (x + rbl), (y + height)] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.appendCornerArc__path, cast ([path, (x + rbl), ((y + height) - rbl), rbl, (HxMath.PI / 2.0), HxMath.PI] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathLineTo, cast ([path, x, (y + rtl)] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.appendCornerArc__path, cast ([path, (x + rtl), (y + rtl), rtl, HxMath.PI, ((HxMath.PI * 3.0) / 2.0)] : Array<Dynamic>));
    FlightRuntime.callValue(appendPathClose, cast ([path] : Array<Dynamic>));
  }

  @:keep public static function applyDash__strokePath(pts:Array<Float>, closed:Bool, dash:Array<Float>, dashOffset:Float):Array<DashSegment__strokePath> {
    var result:Array<DashSegment__strokePath> = cast FlightRuntime.UNDEFINED;
    var totalDashLength:Dynamic = cast FlightRuntime.UNDEFINED;
    var offset:Dynamic = cast FlightRuntime.UNDEFINED;
    var dashIndex:Dynamic = cast FlightRuntime.UNDEFINED;
    var remaining:Dynamic = cast FlightRuntime.UNDEFINED;
    var isOn:Dynamic = cast FlightRuntime.UNDEFINED;
    var current:Null<Array<Float>> = cast FlightRuntime.UNDEFINED;
    var n:Dynamic = cast FlightRuntime.UNDEFINED;
    result = cast ([] : Array<Dynamic>);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(dash, 'length'), 0.0))) {
  FlightRuntime.callProperty(result, 'push', cast ([{ points: (cast pts : Array<Float>), closed: closed }] : Array<Dynamic>));
  return cast result;
}
    totalDashLength = FlightRuntime.reduce(dash, function(s:Dynamic, d:Dynamic) return (s + d), 0.0);
    if (FlightRuntime.truthy(FlightRuntime.compare(totalDashLength, 0.0, '<='))) {
  FlightRuntime.callProperty(result, 'push', cast ([{ points: (cast pts : Array<Float>), closed: closed }] : Array<Dynamic>));
  return cast result;
}
    offset = (((dashOffset % totalDashLength) + totalDashLength) % totalDashLength);
    dashIndex = 0.0;
    remaining = 0.0;
    isOn = true;
    {
      var acc:Dynamic = 0.0;
      {
        var i:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(dash, 'length'), '<'))) {
          if (FlightRuntime.truthy(FlightRuntime.compare((acc + FlightRuntime.getIndex(dash, i)), offset, '>'))) {
  (dashIndex = cast (i : Dynamic));
  (remaining = cast ((FlightRuntime.getIndex(dash, i) - (offset - acc)) : Dynamic));
  (isOn = cast (FlightRuntime.strictEquals((i % 2.0), 0.0) : Dynamic));
  break;
}
          (acc = cast ((acc + FlightRuntime.getIndex(dash, i)) : Dynamic));
          i++;
        }
      }
    }
    current = null;
    n = (Std.int(FlightRuntime.field(pts, 'length')) >> Std.int(1.0));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, (n - 1.0), '<'))) {
        var x0:Dynamic = FlightRuntime.getIndex(pts, (i * 2.0));
        var y0:Dynamic = FlightRuntime.getIndex(pts, ((i * 2.0) + 1.0));
        var x1:Dynamic = FlightRuntime.getIndex(pts, ((i + 1.0) * 2.0));
        var y1:Dynamic = FlightRuntime.getIndex(pts, (((i + 1.0) * 2.0) + 1.0));
        var dx:Dynamic = (x1 - x0);
        var dy:Dynamic = (y1 - y0);
        var segLen:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.andValue(isOn, function():Dynamic return cast FlightRuntime.strictEquals(current, null)))) {
  (current = cast (cast ([x0, y0] : Array<Dynamic>) : Dynamic));
}
        var consumed:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(consumed, segLen, '<'))) {
  var step:Dynamic = FlightRuntime.callProperty(HxMath, 'min', cast ([remaining, (segLen - consumed)] : Array<Dynamic>));
  var t:Dynamic = ((consumed + step) / segLen);
  var ix:Dynamic = (x0 + (t * dx));
  var iy:Dynamic = (y0 + (t * dy));
  if (FlightRuntime.truthy(isOn)) {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(current, null))) { (current = cast (cast ([(x0 + ((consumed / segLen) * dx)), (y0 + ((consumed / segLen) * dy))] : Array<Dynamic>) : Dynamic)); }
  FlightRuntime.pushMany(current, cast ([ix, iy] : Array<Dynamic>));
} else {
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(current, null))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(current, 'length'), 4.0, '>='))) { FlightRuntime.callProperty(result, 'push', cast ([{ points: current, closed: false }] : Array<Dynamic>)); }
  (current = cast (null : Dynamic));
}
  if (FlightRuntime.truthy(FlightRuntime.compare(step, remaining, '>='))) {
  (current = cast (cast ([ix, iy] : Array<Dynamic>) : Dynamic));
}
}
  (consumed = cast ((consumed + step) : Dynamic));
  (remaining = cast ((remaining - step) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.compare(remaining, 1e-10, '<='))) {
  (dashIndex = cast (((dashIndex + 1.0) % FlightRuntime.field(dash, 'length')) : Dynamic));
  (remaining = cast (FlightRuntime.getIndex(dash, dashIndex) : Dynamic));
  (isOn = cast (FlightRuntime.strictEquals((dashIndex % 2.0), 0.0) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.andValue(isOn, function():Dynamic return cast FlightRuntime.strictEquals(current, null)))) {
  (current = cast (cast ([ix, iy] : Array<Dynamic>) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.truthy(isOn), function():Dynamic return cast !FlightRuntime.strictEquals(current, null)))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(current, 'length'), 4.0, '>='))) { FlightRuntime.callProperty(result, 'push', cast ([{ points: current, closed: false }] : Array<Dynamic>)); }
  (current = cast (null : Dynamic));
} }
}
}
        i++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(current, null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(current, 'length'), 4.0, '>=')))) {
  FlightRuntime.callProperty(result, 'push', cast ([{ points: current, closed: false }] : Array<Dynamic>));
}
    return cast result;
    return cast null;
  }

  @:keep public static function applyDashToContour__dashPath(pts:Array<Float>, dash:Array<Float>, dashOffset:Float, totalDashLength:Float, out:Path):Void {
    var n:Dynamic = cast FlightRuntime.UNDEFINED;
    var offset:Dynamic = cast FlightRuntime.UNDEFINED;
    var dashIndex:Dynamic = cast FlightRuntime.UNDEFINED;
    var remaining:Dynamic = cast FlightRuntime.UNDEFINED;
    var isOn:Dynamic = cast FlightRuntime.UNDEFINED;
    var segStarted:Dynamic = cast FlightRuntime.UNDEFINED;
    n = (Std.int(FlightRuntime.field(pts, 'length')) >> Std.int(1.0));
    if (FlightRuntime.truthy(FlightRuntime.compare(n, 2.0, '<'))) { return; }
    offset = (((dashOffset % totalDashLength) + totalDashLength) % totalDashLength);
    dashIndex = 0.0;
    remaining = 0.0;
    isOn = true;
    {
      var acc:Dynamic = 0.0;
      {
        var i:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(dash, 'length'), '<'))) {
          if (FlightRuntime.truthy(FlightRuntime.compare((acc + FlightRuntime.getIndex(dash, i)), offset, '>'))) {
  (dashIndex = cast (i : Dynamic));
  (remaining = cast ((FlightRuntime.getIndex(dash, i) - (offset - acc)) : Dynamic));
  (isOn = cast (FlightRuntime.strictEquals((i % 2.0), 0.0) : Dynamic));
  break;
}
          (acc = cast ((acc + FlightRuntime.getIndex(dash, i)) : Dynamic));
          i++;
        }
      }
    }
    segStarted = false;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, (n - 1.0), '<'))) {
        var x0:Dynamic = FlightRuntime.getIndex(pts, (i * 2.0));
        var y0:Dynamic = FlightRuntime.getIndex(pts, ((i * 2.0) + 1.0));
        var x1:Dynamic = FlightRuntime.getIndex(pts, ((i + 1.0) * 2.0));
        var y1:Dynamic = FlightRuntime.getIndex(pts, (((i + 1.0) * 2.0) + 1.0));
        var dx:Dynamic = (x1 - x0);
        var dy:Dynamic = (y1 - y0);
        var segLen:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.andValue(isOn, function():Dynamic return cast !FlightRuntime.truthy(segStarted)))) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([x0, y0] : Array<Dynamic>));
  (segStarted = cast (true : Dynamic));
}
        var consumed:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(consumed, segLen, '<'))) {
  var step:Dynamic = FlightRuntime.callProperty(HxMath, 'min', cast ([remaining, (segLen - consumed)] : Array<Dynamic>));
  var t:Dynamic = FlightRuntime.select(FlightRuntime.compare(segLen, 0.0, '>'), function():Dynamic return cast ((consumed + step) / segLen), function():Dynamic return cast 0.0);
  var ix:Dynamic = (x0 + (t * dx));
  var iy:Dynamic = (y0 + (t * dy));
  if (FlightRuntime.truthy(isOn)) {
  if (FlightRuntime.truthy(!FlightRuntime.truthy(segStarted))) {
  var tStart:Dynamic = FlightRuntime.select(FlightRuntime.compare(segLen, 0.0, '>'), function():Dynamic return cast (consumed / segLen), function():Dynamic return cast 0.0);
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([(x0 + (tStart * dx)), (y0 + (tStart * dy))] : Array<Dynamic>));
  (segStarted = cast (true : Dynamic));
}
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.LINE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([ix, iy] : Array<Dynamic>));
}
  (consumed = cast ((consumed + step) : Dynamic));
  (remaining = cast ((remaining - step) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.compare(remaining, 1e-10, '<='))) {
  (dashIndex = cast (((dashIndex + 1.0) % FlightRuntime.field(dash, 'length')) : Dynamic));
  (remaining = cast (FlightRuntime.getIndex(dash, dashIndex) : Dynamic));
  var wasOn:Dynamic = isOn;
  (isOn = cast (FlightRuntime.strictEquals((dashIndex % 2.0), 0.0) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.andValue(wasOn, function():Dynamic return cast !FlightRuntime.truthy(isOn)))) {
  (segStarted = cast (false : Dynamic));
}
  if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.truthy(wasOn), function():Dynamic return cast isOn))) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([ix, iy] : Array<Dynamic>));
  (segStarted = cast (true : Dynamic));
}
}
}
        i++;
      }
    }
  }

  @:keep public static function chordDistSq__containsPathPoint(px:Float, py:Float, x0:Float, y0:Float, x1:Float, y1:Float):Float {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var lenSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var cross:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (x1 - x0);
    dy = (y1 - y0);
    lenSq = ((dx * dx) + (dy * dy));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(lenSq, 0.0))) {
  var ax:Dynamic = (px - x0);
  var ay:Dynamic = (py - y0);
  return cast ((ax * ax) + (ay * ay));
}
    cross = ((dx * (y0 - py)) - (dy * (x0 - px)));
    return cast ((cross * cross) / lenSq);
    return cast null;
  }

  @:keep public static function chordLengthParameterize__fitPathCurves(pts:Array<Float>, first:Float, last:Float):Array<Float> {
    var u:Array<Float> = cast FlightRuntime.UNDEFINED;
    var total:Dynamic = cast FlightRuntime.UNDEFINED;
    u = cast ([0.0] : Array<Dynamic>);
    {
      var i:Dynamic = (first + 1.0);
      while (FlightRuntime.truthy(FlightRuntime.compare(i, last, '<='))) {
        var dx:Dynamic = (FlightRuntime.getIndex(pts, (i * 2.0)) - FlightRuntime.getIndex(pts, ((i - 1.0) * 2.0)));
        var dy:Dynamic = (FlightRuntime.getIndex(pts, ((i * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, (((i - 1.0) * 2.0) + 1.0)));
        FlightRuntime.callProperty(u, 'push', cast ([(FlightRuntime.getIndex(u, (FlightRuntime.field(u, 'length') - 1.0)) + FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>)))] : Array<Dynamic>));
        i++;
      }
    }
    total = FlightRuntime.getIndex(u, (FlightRuntime.field(u, 'length') - 1.0));
    if (FlightRuntime.truthy(FlightRuntime.compare(total, 0.0, '>'))) {
  {
    var i:Dynamic = 1.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(u, 'length'), '<'))) {
      FlightRuntime.setIndex(u, i, (FlightRuntime.getIndex(u, i) / total));
      i++;
    }
  }
}
    return cast u;
    return cast null;
  }

  @:keep public static function cleanPath(source:Path, tolerance:Float, out:Path, flattenTolerance:Dynamic = 0.25):Void {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var winding:Dynamic = cast FlightRuntime.UNDEFINED;
    var toleranceSq:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([source, flattenTolerance] : Array<Dynamic>));
    winding = FlightRuntime.field(source, 'winding');
    toleranceSq = (tolerance * tolerance);
    FlightRuntime.setLength(FlightRuntime.field(out, 'commands'), 0.0);
    FlightRuntime.setLength(FlightRuntime.field(out, 'data'), 0.0);
    FlightRuntime.setField(out, 'winding', winding);
    for (contour in FlightRuntime.iterable(contours)) {
      var n:Dynamic = (Std.int(FlightRuntime.field(contour, 'length')) >> Std.int(1.0));
      if (FlightRuntime.truthy(FlightRuntime.compare(n, 2.0, '<'))) { continue; }
      var closed:Dynamic = FlightRuntime.andValue(FlightRuntime.compare(n, 3.0, '>='), function():Dynamic return cast FlightRuntime.callValue(PathApi.withinTolerance__cleanPath, cast ([FlightRuntime.getIndex(contour, 0.0), FlightRuntime.getIndex(contour, 1.0), FlightRuntime.getIndex(contour, ((n * 2.0) - 2.0)), FlightRuntime.getIndex(contour, ((n * 2.0) - 1.0)), toleranceSq] : Array<Dynamic>)));
      var count:Dynamic = FlightRuntime.select(closed, function():Dynamic return cast (n - 1.0), function():Dynamic return cast n);
      var kept:Array<Float> = cast ([] : Array<Dynamic>);
      {
        var i:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, count, '<'))) {
          FlightRuntime.callValue(PathApi.pushCleanVertex__cleanPath, cast ([kept, FlightRuntime.getIndex(contour, (i * 2.0)), FlightRuntime.getIndex(contour, ((i * 2.0) + 1.0)), toleranceSq] : Array<Dynamic>));
          i++;
        }
      }
      if (FlightRuntime.truthy(closed)) { FlightRuntime.callValue(PathApi.collapseClosedSeam__cleanPath, cast ([kept, toleranceSq] : Array<Dynamic>)); }
      var keptCount:Dynamic = (Std.int(FlightRuntime.field(kept, 'length')) >> Std.int(1.0));
      if (FlightRuntime.truthy(FlightRuntime.select(closed, function():Dynamic return cast FlightRuntime.compare(keptCount, 3.0, '<'), function():Dynamic return cast FlightRuntime.compare(keptCount, 2.0, '<')))) { continue; }
      FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
      FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(kept, 0.0), FlightRuntime.getIndex(kept, 1.0)] : Array<Dynamic>));
      {
        var i:Dynamic = 1.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, keptCount, '<'))) {
          FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.LINE_TO] : Array<Dynamic>));
          FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(kept, (i * 2.0)), FlightRuntime.getIndex(kept, ((i * 2.0) + 1.0))] : Array<Dynamic>));
          i++;
        }
      }
      if (FlightRuntime.truthy(closed)) { FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.CLOSE] : Array<Dynamic>)); }
    }
  }

  @:keep public static function clonePath(source:Path):Path {
    return cast FlightRuntime.callValue(copyPath, cast ([source] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function collapseClosedSeam__cleanPath(kept:Array<Float>, toleranceSq:Float):Void {
    var changed:Dynamic = cast FlightRuntime.UNDEFINED;
    changed = true;
    while (FlightRuntime.truthy(FlightRuntime.andValue(changed, function():Dynamic return cast FlightRuntime.compare((Std.int(FlightRuntime.field(kept, 'length')) >> Std.int(1.0)), 3.0, '>')))) {
  (changed = cast (false : Dynamic));
  var last:Dynamic = (Std.int(FlightRuntime.field(kept, 'length')) >> Std.int(1.0));
  if (FlightRuntime.truthy(FlightRuntime.callValue(PathApi.isMiddleRemovable__cleanPath, cast ([kept, (last - 2.0), (last - 1.0), 0.0, toleranceSq] : Array<Dynamic>)))) {
  FlightRuntime.setLength(kept, (kept.length - 2.0));
  (changed = cast (true : Dynamic));
  continue;
}
  if (FlightRuntime.truthy(FlightRuntime.callValue(PathApi.isMiddleRemovable__cleanPath, cast ([kept, (last - 1.0), 0.0, 1.0, toleranceSq] : Array<Dynamic>)))) {
  FlightRuntime.copyWithin(kept, 0.0, 2.0);
  FlightRuntime.setLength(kept, (kept.length - 2.0));
  (changed = cast (true : Dynamic));
}
}
  }

  @:keep public static function computeCenterTangent__fitPathCurves(pts:Array<Float>, idx:Float):Array<Float> {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var len:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (FlightRuntime.getIndex(pts, ((idx - 1.0) * 2.0)) - FlightRuntime.getIndex(pts, ((idx + 1.0) * 2.0)));
    dy = (FlightRuntime.getIndex(pts, (((idx - 1.0) * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, (((idx + 1.0) * 2.0) + 1.0)));
    len = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>));
    return cast FlightRuntime.select(FlightRuntime.compare(len, 0.0, '>'), function():Dynamic return cast cast ([(dx / len), (dy / len)] : Array<Dynamic>), function():Dynamic return cast cast ([1.0, 0.0] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeLeftTangent__fitPathCurves(pts:Array<Float>, idx:Float):Array<Float> {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var len:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (FlightRuntime.getIndex(pts, ((idx + 1.0) * 2.0)) - FlightRuntime.getIndex(pts, (idx * 2.0)));
    dy = (FlightRuntime.getIndex(pts, (((idx + 1.0) * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, ((idx * 2.0) + 1.0)));
    len = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>));
    return cast FlightRuntime.select(FlightRuntime.compare(len, 0.0, '>'), function():Dynamic return cast cast ([(dx / len), (dy / len)] : Array<Dynamic>), function():Dynamic return cast cast ([1.0, 0.0] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeMaxError__fitPathCurves(pts:Array<Float>, first:Float, last:Float, bezier:Array<Float>, u:Array<Float>):Array<Float> {
    var maxDist:Dynamic = cast FlightRuntime.UNDEFINED;
    var splitPoint:Dynamic = cast FlightRuntime.UNDEFINED;
    maxDist = 0.0;
    splitPoint = (Std.int(((last - first) + 1.0)) >> Std.int(1.0));
    {
      var i:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, (last - first), '<'))) {
        var t:Dynamic = FlightRuntime.getIndex(u, i);
        var mt:Dynamic = (1.0 - t);
        var bx:Dynamic = ((((((mt * mt) * mt) * FlightRuntime.getIndex(bezier, 0.0)) + ((((3.0 * mt) * mt) * t) * FlightRuntime.getIndex(bezier, 2.0))) + ((((3.0 * mt) * t) * t) * FlightRuntime.getIndex(bezier, 4.0))) + (((t * t) * t) * FlightRuntime.getIndex(bezier, 6.0)));
        var by:Dynamic = ((((((mt * mt) * mt) * FlightRuntime.getIndex(bezier, 1.0)) + ((((3.0 * mt) * mt) * t) * FlightRuntime.getIndex(bezier, 3.0))) + ((((3.0 * mt) * t) * t) * FlightRuntime.getIndex(bezier, 5.0))) + (((t * t) * t) * FlightRuntime.getIndex(bezier, 7.0)));
        var dx:Dynamic = (FlightRuntime.getIndex(pts, ((first + i) * 2.0)) - bx);
        var dy:Dynamic = (FlightRuntime.getIndex(pts, (((first + i) * 2.0) + 1.0)) - by);
        var distSq:Dynamic = ((dx * dx) + (dy * dy));
        if (FlightRuntime.truthy(FlightRuntime.compare(distSq, maxDist, '>='))) {
  (maxDist = cast (distSq : Dynamic));
  (splitPoint = cast ((first + i) : Dynamic));
}
        i++;
      }
    }
    return cast cast ([maxDist, splitPoint] : Array<Dynamic>);
    return cast null;
  }

  @:keep public static function computePathWindingNumber__containsPathPoint(path:Path, px:Float, py:Float, tolerance:Float):Float {
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var toleranceSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var windingNumber:Dynamic = cast FlightRuntime.UNDEFINED;
    var x:Dynamic = cast FlightRuntime.UNDEFINED;
    var y:Dynamic = cast FlightRuntime.UNDEFINED;
    var contourStartX:Dynamic = cast FlightRuntime.UNDEFINED;
    var contourStartY:Dynamic = cast FlightRuntime.UNDEFINED;
    var hasContour:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastX:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastY:Dynamic = cast FlightRuntime.UNDEFINED;
    var di:Dynamic = cast FlightRuntime.UNDEFINED;
    var flushContour:Dynamic = cast FlightRuntime.UNDEFINED;
    commands = FlightRuntime.field(path, 'commands');
    data = FlightRuntime.field(path, 'data');
    toleranceSq = (tolerance * tolerance);
    windingNumber = 0.0;
    x = 0.0;
    y = 0.0;
    contourStartX = 0.0;
    contourStartY = 0.0;
    hasContour = false;
    lastX = 0.0;
    lastY = 0.0;
    di = 0.0;
    flushContour = function() {
  if (FlightRuntime.truthy(hasContour)) {
  (windingNumber = cast ((windingNumber + FlightRuntime.callValue(PathApi.countSegmentCrossings__containsPathPoint, cast ([px, py, lastX, lastY, contourStartX, contourStartY] : Array<Dynamic>))) : Dynamic));
}
};
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(commands, 'length'), '<'))) {
        var command:Dynamic = FlightRuntime.getIndex(commands, ci);
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.MOVE_TO))) {
  FlightRuntime.callValue(flushContour, cast ([] : Array<Dynamic>));
  (x = cast (FlightRuntime.getIndex(data, di) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 1.0)) : Dynamic));
  (di = cast ((di + 2.0) : Dynamic));
  (contourStartX = cast (x : Dynamic));
  (contourStartY = cast (y : Dynamic));
  (lastX = cast (x : Dynamic));
  (lastY = cast (y : Dynamic));
  (hasContour = cast (true : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_MOVE_TO))) {
  FlightRuntime.callValue(flushContour, cast ([] : Array<Dynamic>));
  (x = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
  (di = cast ((di + 4.0) : Dynamic));
  (contourStartX = cast (x : Dynamic));
  (contourStartY = cast (y : Dynamic));
  (lastX = cast (x : Dynamic));
  (lastY = cast (y : Dynamic));
  (hasContour = cast (true : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.LINE_TO))) {
  var nx:Dynamic = FlightRuntime.getIndex(data, di);
  var ny:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  (di = cast ((di + 2.0) : Dynamic));
  if (FlightRuntime.truthy(hasContour)) {
  (windingNumber = cast ((windingNumber + FlightRuntime.callValue(PathApi.countSegmentCrossings__containsPathPoint, cast ([px, py, lastX, lastY, nx, ny] : Array<Dynamic>))) : Dynamic));
}
  (lastX = cast (nx : Dynamic));
  (lastY = cast (ny : Dynamic));
  (x = cast (nx : Dynamic));
  (y = cast (ny : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_LINE_TO))) {
  var nx:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var ny:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  (di = cast ((di + 4.0) : Dynamic));
  if (FlightRuntime.truthy(hasContour)) {
  (windingNumber = cast ((windingNumber + FlightRuntime.callValue(PathApi.countSegmentCrossings__containsPathPoint, cast ([px, py, lastX, lastY, nx, ny] : Array<Dynamic>))) : Dynamic));
}
  (lastX = cast (nx : Dynamic));
  (lastY = cast (ny : Dynamic));
  (x = cast (nx : Dynamic));
  (y = cast (ny : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CURVE_TO))) {
  var cx:Dynamic = FlightRuntime.getIndex(data, di);
  var cy:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  var ax:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var ay:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  (di = cast ((di + 4.0) : Dynamic));
  if (FlightRuntime.truthy(hasContour)) {
  (windingNumber = cast ((windingNumber + FlightRuntime.callValue(PathApi.flattenQuadraticWindingNumber__containsPathPoint, cast ([px, py, lastX, lastY, cx, cy, ax, ay, toleranceSq, 0.0] : Array<Dynamic>))) : Dynamic));
}
  (lastX = cast (ax : Dynamic));
  (lastY = cast (ay : Dynamic));
  (x = cast (ax : Dynamic));
  (y = cast (ay : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CUBIC_CURVE_TO))) {
  var c1x:Dynamic = FlightRuntime.getIndex(data, di);
  var c1y:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  var c2x:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var c2y:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  var ax:Dynamic = FlightRuntime.getIndex(data, (di + 4.0));
  var ay:Dynamic = FlightRuntime.getIndex(data, (di + 5.0));
  (di = cast ((di + 6.0) : Dynamic));
  if (FlightRuntime.truthy(hasContour)) {
  (windingNumber = cast ((windingNumber + FlightRuntime.callValue(PathApi.flattenCubicWindingNumber__containsPathPoint, cast ([px, py, lastX, lastY, c1x, c1y, c2x, c2y, ax, ay, toleranceSq, 0.0] : Array<Dynamic>))) : Dynamic));
}
  (lastX = cast (ax : Dynamic));
  (lastY = cast (ay : Dynamic));
  (x = cast (ax : Dynamic));
  (y = cast (ay : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CLOSE))) {
  if (FlightRuntime.truthy(hasContour)) {
  (windingNumber = cast ((windingNumber + FlightRuntime.callValue(PathApi.countSegmentCrossings__containsPathPoint, cast ([px, py, lastX, lastY, contourStartX, contourStartY] : Array<Dynamic>))) : Dynamic));
  (lastX = cast (contourStartX : Dynamic));
  (lastY = cast (contourStartY : Dynamic));
  (x = cast (contourStartX : Dynamic));
  (y = cast (contourStartY : Dynamic));
  (hasContour = cast (false : Dynamic));
}
} } } } } } }
        ci++;
      }
    }
    FlightRuntime.callValue(flushContour, cast ([] : Array<Dynamic>));
    return cast FlightRuntime.callProperty(HxMath, 'abs', cast ([windingNumber] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function computeRightTangent__fitPathCurves(pts:Array<Float>, idx:Float):Array<Float> {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var len:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (FlightRuntime.getIndex(pts, ((idx - 1.0) * 2.0)) - FlightRuntime.getIndex(pts, (idx * 2.0)));
    dy = (FlightRuntime.getIndex(pts, (((idx - 1.0) * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, ((idx * 2.0) + 1.0)));
    len = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>));
    return cast FlightRuntime.select(FlightRuntime.compare(len, 0.0, '>'), function():Dynamic return cast cast ([(dx / len), (dy / len)] : Array<Dynamic>), function():Dynamic return cast cast ([-1.0, 0.0] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function containsPathPoint(path:Path, px:Float, py:Float, tolerance:Dynamic = 0.25):Bool {
    var winding:Dynamic = cast FlightRuntime.UNDEFINED;
    winding = FlightRuntime.callValue(PathApi.computePathWindingNumber__containsPathPoint, cast ([path, px, py, tolerance] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(path, 'winding'), 'evenOdd'))) { return cast !FlightRuntime.strictEquals((Std.int(winding) & Std.int(1.0)), 0.0); }
    return cast !FlightRuntime.strictEquals(winding, 0.0);
    return cast null;
  }

  @:keep public static function contourLength__getPathContourLengths(contour:Array<Float>):Float {
    var len:Dynamic = cast FlightRuntime.UNDEFINED;
    len = 0.0;
    {
      var i:Dynamic = 2.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(contour, 'length'), '<'))) {
        var dx:Dynamic = (FlightRuntime.getIndex(contour, i) - FlightRuntime.getIndex(contour, (i - 2.0)));
        var dy:Dynamic = (FlightRuntime.getIndex(contour, (i + 1.0)) - FlightRuntime.getIndex(contour, (i - 1.0)));
        (len = cast ((len + FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>))) : Dynamic));
        (i = cast ((i + 2.0) : Dynamic));
      }
    }
    return cast len;
    return cast null;
  }

  @:keep public static function contourLength__getPathLength(contour:Array<Float>):Float {
    var len:Dynamic = cast FlightRuntime.UNDEFINED;
    len = 0.0;
    {
      var i:Dynamic = 2.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(contour, 'length'), '<'))) {
        var dx:Dynamic = (FlightRuntime.getIndex(contour, i) - FlightRuntime.getIndex(contour, (i - 2.0)));
        var dy:Dynamic = (FlightRuntime.getIndex(contour, (i + 1.0)) - FlightRuntime.getIndex(contour, (i - 1.0)));
        (len = cast ((len + FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>))) : Dynamic));
        (i = cast ((i + 2.0) : Dynamic));
      }
    }
    return cast len;
    return cast null;
  }

  @:keep public static function copyCommands__dashPath(source:Path, out:Path):Void {
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(FlightRuntime.field(source, 'commands'), 'length'), '<'))) {
        FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([FlightRuntime.getIndex(FlightRuntime.field(source, 'commands'), i)] : Array<Dynamic>));
        i++;
      }
    }
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(FlightRuntime.field(source, 'data'), 'length'), '<'))) {
        FlightRuntime.callProperty(FlightRuntime.field(out, 'data'), 'push', cast ([FlightRuntime.getIndex(FlightRuntime.field(source, 'data'), i)] : Array<Dynamic>));
        i++;
      }
    }
  }

  @:keep public static function copyPath(source:Path, ?out:Path):Path {
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(out, FlightRuntime.UNDEFINED))) {
  return cast { commands: FlightRuntime.slice(FlightRuntime.field(source, 'commands'), 0, null), data: FlightRuntime.slice(FlightRuntime.field(source, 'data'), 0, null), winding: FlightRuntime.field(source, 'winding') };
}
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(out, source))) {
  FlightRuntime.setLength(FlightRuntime.field(out, 'commands'), 0.0);
  {
    var i:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(FlightRuntime.field(source, 'commands'), 'length'), '<'))) {
      FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([FlightRuntime.getIndex(FlightRuntime.field(source, 'commands'), i)] : Array<Dynamic>));
      i++;
    }
  }
  FlightRuntime.setLength(FlightRuntime.field(out, 'data'), 0.0);
  {
    var i:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(FlightRuntime.field(source, 'data'), 'length'), '<'))) {
      FlightRuntime.callProperty(FlightRuntime.field(out, 'data'), 'push', cast ([FlightRuntime.getIndex(FlightRuntime.field(source, 'data'), i)] : Array<Dynamic>));
      i++;
    }
  }
  FlightRuntime.setField(out, 'winding', FlightRuntime.field(source, 'winding'));
}
    return cast out;
    return cast null;
  }

  @:keep public static function countSegmentCrossings__containsPathPoint(px:Float, py:Float, x0:Float, y0:Float, x1:Float, y1:Float):Float {
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.andValue(FlightRuntime.compare(y0, py, '<='), function():Dynamic return cast FlightRuntime.compare(y1, py, '>')), function():Dynamic return cast FlightRuntime.andValue(FlightRuntime.compare(y1, py, '<='), function():Dynamic return cast FlightRuntime.compare(y0, py, '>'))))) {
  var crossX:Dynamic = (x0 + (((py - y0) * (x1 - x0)) / (y1 - y0)));
  if (FlightRuntime.truthy(FlightRuntime.compare(px, crossX, '<'))) {
  return cast FlightRuntime.select(FlightRuntime.compare(y1, y0, '>'), function():Dynamic return cast 1.0, function():Dynamic return cast -1.0);
}
}
    return cast 0.0;
    return cast null;
  }

  @:keep public static function createPath(winding:PathWinding = 'nonZero'):Path {
    return cast { commands: cast ([] : Array<Dynamic>), data: cast ([] : Array<Dynamic>), winding: winding };
    return cast null;
  }

  @:keep public static function cubicExtremumRoots__getPathBounds(p0:Float, p1:Float, p2:Float, p3:Float, cb:Dynamic):Void {
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var c:Dynamic = cast FlightRuntime.UNDEFINED;
    var discriminant:Dynamic = cast FlightRuntime.UNDEFINED;
    var sqrtD:Dynamic = cast FlightRuntime.UNDEFINED;
    var t1:Dynamic = cast FlightRuntime.UNDEFINED;
    var t2:Dynamic = cast FlightRuntime.UNDEFINED;
    a = (((-p0 + (3.0 * p1)) - (3.0 * p2)) + p3);
    b = (2.0 * ((p0 - (2.0 * p1)) + p2));
    c = (p1 - p0);
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([a] : Array<Dynamic>)), 1e-12, '<'))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([b] : Array<Dynamic>)), 1e-12, '<'))) { return; }
  var t:Dynamic = (-c / b);
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(t, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(t, 1.0, '<')))) { FlightRuntime.callValue(cb, cast ([t] : Array<Dynamic>)); }
  return;
}
    discriminant = ((b * b) - ((4.0 * a) * c));
    if (FlightRuntime.truthy(FlightRuntime.compare(discriminant, 0.0, '<'))) { return; }
    sqrtD = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([discriminant] : Array<Dynamic>));
    t1 = ((-b + sqrtD) / (2.0 * a));
    t2 = ((-b - sqrtD) / (2.0 * a));
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(t1, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(t1, 1.0, '<')))) { FlightRuntime.callValue(cb, cast ([t1] : Array<Dynamic>)); }
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(t2, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(t2, 1.0, '<')), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([(t2 - t1)] : Array<Dynamic>)), 1e-12, '>')))) { FlightRuntime.callValue(cb, cast ([t2] : Array<Dynamic>)); }
  }

  @:keep public static function dashPath(source:Path, dash:Array<Float>, dashOffset:Float, out:Path, tolerance:Dynamic = 0.25):Void {
    var totalDashLength:Dynamic = cast FlightRuntime.UNDEFINED;
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.setLength(FlightRuntime.field(out, 'commands'), 0.0);
    FlightRuntime.setLength(FlightRuntime.field(out, 'data'), 0.0);
    FlightRuntime.setField(out, 'winding', FlightRuntime.field(source, 'winding'));
    totalDashLength = FlightRuntime.callValue(PathApi.dashTotal__dashPath, cast ([dash] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(totalDashLength, 0.0, '<='))) {
  FlightRuntime.callValue(PathApi.copyCommands__dashPath, cast ([source, out] : Array<Dynamic>));
  return;
}
    contours = FlightRuntime.callValue(flattenPath, cast ([source, tolerance] : Array<Dynamic>));
    for (contour in FlightRuntime.iterable(contours)) {
      FlightRuntime.callValue(PathApi.applyDashToContour__dashPath, cast ([contour, dash, dashOffset, totalDashLength, out] : Array<Dynamic>));
    }
  }

  @:keep public static function dashTotal__dashPath(dash:Array<Float>):Float {
    var total:Dynamic = cast FlightRuntime.UNDEFINED;
    total = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(dash, 'length'), '<'))) {
        (total = cast ((total + FlightRuntime.getIndex(dash, i)) : Dynamic));
        i++;
      }
    }
    return cast total;
    return cast null;
  }

  @:keep public static function decimatePath(source:Path, tolerance:Float, out:Path, flattenTolerance:Dynamic = 0.25):Void {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([source, flattenTolerance] : Array<Dynamic>));
    FlightRuntime.setLength(FlightRuntime.field(out, 'commands'), 0.0);
    FlightRuntime.setLength(FlightRuntime.field(out, 'data'), 0.0);
    FlightRuntime.setField(out, 'winding', FlightRuntime.field(source, 'winding'));
    for (contour in FlightRuntime.iterable(contours)) {
      var n:Dynamic = (Std.int(FlightRuntime.field(contour, 'length')) >> Std.int(1.0));
      if (FlightRuntime.truthy(FlightRuntime.compare(n, 2.0, '<'))) { continue; }
      var closed:Dynamic = FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(n, 3.0, '>='), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(contour, 0.0), FlightRuntime.getIndex(contour, (FlightRuntime.field(contour, 'length') - 2.0)))), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(contour, 1.0), FlightRuntime.getIndex(contour, (FlightRuntime.field(contour, 'length') - 1.0))));
      var last:Dynamic = FlightRuntime.select(closed, function():Dynamic return cast (n - 1.0), function():Dynamic return cast n);
      var keep:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Uint8Array'] : Array<Dynamic>)), [last]);
      FlightRuntime.setIndex(keep, 0.0, 1.0);
      FlightRuntime.setIndex(keep, (last - 1.0), 1.0);
      FlightRuntime.callValue(PathApi.douglasPeucker__decimatePath, cast ([contour, 0.0, (last - 1.0), (tolerance * tolerance), keep] : Array<Dynamic>));
      var first:Dynamic = true;
      {
        var i:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(i, last, '<'))) {
          if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.getIndex(keep, i)))) { i++; continue; }
          if (FlightRuntime.truthy(first)) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
  (first = cast (false : Dynamic));
} else {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.LINE_TO] : Array<Dynamic>));
}
          FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(contour, (i * 2.0)), FlightRuntime.getIndex(contour, ((i * 2.0) + 1.0))] : Array<Dynamic>));
          i++;
        }
      }
      if (FlightRuntime.truthy(closed)) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.CLOSE] : Array<Dynamic>));
}
    }
  }

  @:keep public static function decodeSubpaths__reversePath(path:Path):Array<Subpath__reversePath> {
    var subpaths:Array<Subpath__reversePath> = cast FlightRuntime.UNDEFINED;
    var current:Null<Subpath__reversePath> = cast FlightRuntime.UNDEFINED;
    var ensureCurrent:Dynamic = cast FlightRuntime.UNDEFINED;
    subpaths = cast ([] : Array<Dynamic>);
    current = null;
    ensureCurrent = function() {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(current, null))) {
  (current = cast ({ points: cast ([{ x: 0.0, y: 0.0, kind: 'move' }] : Array<Dynamic>), closed: false } : Dynamic));
  FlightRuntime.callProperty(subpaths, 'push', cast ([current] : Array<Dynamic>));
}
  return cast current;
};
    FlightRuntime.callValue(forEachPathSegment, cast ([path, function(segment:Dynamic) {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'moveTo'))) {
  (current = cast ({ points: cast ([{ x: FlightRuntime.field(segment, 'x'), y: FlightRuntime.field(segment, 'y'), kind: 'move' }] : Array<Dynamic>), closed: false } : Dynamic));
  FlightRuntime.callProperty(subpaths, 'push', cast ([current] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'lineTo'))) {
  FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.callValue(ensureCurrent, cast ([] : Array<Dynamic>)), 'points'), 'push', cast ([{ x: FlightRuntime.field(segment, 'x'), y: FlightRuntime.field(segment, 'y'), kind: 'line' }] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'curveTo'))) {
  FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.callValue(ensureCurrent, cast ([] : Array<Dynamic>)), 'points'), 'push', cast ([{ x: FlightRuntime.field(segment, 'x'), y: FlightRuntime.field(segment, 'y'), kind: 'quad', cx: FlightRuntime.field(segment, 'controlX'), cy: FlightRuntime.field(segment, 'controlY') }] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'cubicCurveTo'))) {
  FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.callValue(ensureCurrent, cast ([] : Array<Dynamic>)), 'points'), 'push', cast ([{ x: FlightRuntime.field(segment, 'x'), y: FlightRuntime.field(segment, 'y'), kind: 'cubic', c1x: FlightRuntime.field(segment, 'control1X'), c1y: FlightRuntime.field(segment, 'control1Y'), c2x: FlightRuntime.field(segment, 'control2X'), c2y: FlightRuntime.field(segment, 'control2Y') }] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(segment, 'kind'), 'close'))) {
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(current, null))) { FlightRuntime.setField(current, 'closed', true); }
} } } } }
}] : Array<Dynamic>));
    return cast subpaths;
    return cast null;
  }

  @:keep public static function decodeSubpaths__strokePath(path:Path, tolerance:Float):Array<StrokeSubpath__strokePath> {
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var toleranceSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var subpaths:Array<StrokeSubpath__strokePath> = cast FlightRuntime.UNDEFINED;
    var current:Null<StrokeSubpath__strokePath> = cast FlightRuntime.UNDEFINED;
    var x:Dynamic = cast FlightRuntime.UNDEFINED;
    var y:Dynamic = cast FlightRuntime.UNDEFINED;
    var contourStartX:Dynamic = cast FlightRuntime.UNDEFINED;
    var contourStartY:Dynamic = cast FlightRuntime.UNDEFINED;
    var di:Dynamic = cast FlightRuntime.UNDEFINED;
    var ensureCurrent:Dynamic = cast FlightRuntime.UNDEFINED;
    commands = FlightRuntime.field(path, 'commands');
    data = FlightRuntime.field(path, 'data');
    toleranceSq = (tolerance * tolerance);
    subpaths = cast ([] : Array<Dynamic>);
    current = null;
    x = 0.0;
    y = 0.0;
    contourStartX = 0.0;
    contourStartY = 0.0;
    di = 0.0;
    ensureCurrent = function() {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(current, null))) {
  (current = cast ({ points: cast ([0.0, 0.0] : Array<Dynamic>), closed: false } : Dynamic));
  FlightRuntime.callProperty(subpaths, 'push', cast ([current] : Array<Dynamic>));
  (x = cast (0.0 : Dynamic));
  (y = cast (0.0 : Dynamic));
}
  return cast current;
};
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(commands, 'length'), '<'))) {
        var command:Dynamic = FlightRuntime.getIndex(commands, ci);
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.MOVE_TO))) {
  (x = cast (FlightRuntime.getIndex(data, di) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 1.0)) : Dynamic));
  (di = cast ((di + 2.0) : Dynamic));
  (contourStartX = cast (x : Dynamic));
  (contourStartY = cast (y : Dynamic));
  (current = cast ({ points: cast ([x, y] : Array<Dynamic>), closed: false } : Dynamic));
  FlightRuntime.callProperty(subpaths, 'push', cast ([current] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_MOVE_TO))) {
  (x = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
  (di = cast ((di + 4.0) : Dynamic));
  (contourStartX = cast (x : Dynamic));
  (contourStartY = cast (y : Dynamic));
  (current = cast ({ points: cast ([x, y] : Array<Dynamic>), closed: false } : Dynamic));
  FlightRuntime.callProperty(subpaths, 'push', cast ([current] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.LINE_TO))) {
  var sp:Dynamic = FlightRuntime.callValue(ensureCurrent, cast ([] : Array<Dynamic>));
  (x = cast (FlightRuntime.getIndex(data, di) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 1.0)) : Dynamic));
  (di = cast ((di + 2.0) : Dynamic));
  FlightRuntime.pushMany(FlightRuntime.field(sp, 'points'), cast ([x, y] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_LINE_TO))) {
  var sp:Dynamic = FlightRuntime.callValue(ensureCurrent, cast ([] : Array<Dynamic>));
  (x = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
  (di = cast ((di + 4.0) : Dynamic));
  FlightRuntime.pushMany(FlightRuntime.field(sp, 'points'), cast ([x, y] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CURVE_TO))) {
  var sp:Dynamic = FlightRuntime.callValue(ensureCurrent, cast ([] : Array<Dynamic>));
  FlightRuntime.callValue(PathApi.flattenQuadratic__strokePath, cast ([FlightRuntime.field(sp, 'points'), x, y, FlightRuntime.getIndex(data, di), FlightRuntime.getIndex(data, (di + 1.0)), FlightRuntime.getIndex(data, (di + 2.0)), FlightRuntime.getIndex(data, (di + 3.0)), toleranceSq, 0.0] : Array<Dynamic>));
  (x = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
  (di = cast ((di + 4.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CUBIC_CURVE_TO))) {
  var sp:Dynamic = FlightRuntime.callValue(ensureCurrent, cast ([] : Array<Dynamic>));
  FlightRuntime.callValue(PathApi.flattenCubic__strokePath, cast ([FlightRuntime.field(sp, 'points'), x, y, FlightRuntime.getIndex(data, di), FlightRuntime.getIndex(data, (di + 1.0)), FlightRuntime.getIndex(data, (di + 2.0)), FlightRuntime.getIndex(data, (di + 3.0)), FlightRuntime.getIndex(data, (di + 4.0)), FlightRuntime.getIndex(data, (di + 5.0)), toleranceSq, 0.0] : Array<Dynamic>));
  (x = cast (FlightRuntime.getIndex(data, (di + 4.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 5.0)) : Dynamic));
  (di = cast ((di + 6.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CLOSE))) {
  if (FlightRuntime.truthy(!FlightRuntime.strictEquals(current, null))) {
  FlightRuntime.setField(current, 'closed', true);
  (x = cast (contourStartX : Dynamic));
  (y = cast (contourStartY : Dynamic));
  (current = cast (null : Dynamic));
}
} } } } } } }
        ci++;
      }
    }
    return cast subpaths;
    return cast null;
  }

  @:keep public static function distanceToChordSq__flattenPath(px:Float, py:Float, x0:Float, y0:Float, x1:Float, y1:Float):Float {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var lengthSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var cross:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (x1 - x0);
    dy = (y1 - y0);
    lengthSq = ((dx * dx) + (dy * dy));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(lengthSq, 0.0))) {
  var ax:Dynamic = (px - x0);
  var ay:Dynamic = (py - y0);
  return cast ((ax * ax) + (ay * ay));
}
    cross = ((dx * (y0 - py)) - (dy * (x0 - px)));
    return cast ((cross * cross) / lengthSq);
    return cast null;
  }

  @:keep public static function distChordSq__strokePath(px:Float, py:Float, x0:Float, y0:Float, x1:Float, y1:Float):Float {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var lenSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var cross:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (x1 - x0);
    dy = (y1 - y0);
    lenSq = ((dx * dx) + (dy * dy));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(lenSq, 0.0))) {
  var ax:Dynamic = (px - x0);
  var ay:Dynamic = (py - y0);
  return cast ((ax * ax) + (ay * ay));
}
    cross = ((dx * (y0 - py)) - (dy * (x0 - px)));
    return cast ((cross * cross) / lenSq);
    return cast null;
  }

  @:keep public static function douglasPeucker__decimatePath(pts:Array<Float>, first:Float, last:Float, toleranceSq:Float, keep:Dynamic):Void {
    var x0:Dynamic = cast FlightRuntime.UNDEFINED;
    var y0:Dynamic = cast FlightRuntime.UNDEFINED;
    var x1:Dynamic = cast FlightRuntime.UNDEFINED;
    var y1:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var lenSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxDistSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxIdx:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.compare((last - first), 2.0, '<'))) { return; }
    x0 = FlightRuntime.getIndex(pts, (first * 2.0));
    y0 = FlightRuntime.getIndex(pts, ((first * 2.0) + 1.0));
    x1 = FlightRuntime.getIndex(pts, (last * 2.0));
    y1 = FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0));
    dx = (x1 - x0);
    dy = (y1 - y0);
    lenSq = ((dx * dx) + (dy * dy));
    maxDistSq = 0.0;
    maxIdx = first;
    {
      var i:Dynamic = (first + 1.0);
      while (FlightRuntime.truthy(FlightRuntime.compare(i, last, '<'))) {
        var px:Dynamic = FlightRuntime.getIndex(pts, (i * 2.0));
        var py:Dynamic = FlightRuntime.getIndex(pts, ((i * 2.0) + 1.0));
        var distSq:Float = cast FlightRuntime.UNDEFINED;
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(lenSq, 0.0))) {
  var ax:Dynamic = (px - x0);
  var ay:Dynamic = (py - y0);
  (distSq = cast (((ax * ax) + (ay * ay)) : Dynamic));
} else {
  var cross:Dynamic = ((dx * (y0 - py)) - (dy * (x0 - px)));
  (distSq = cast (((cross * cross) / lenSq) : Dynamic));
}
        if (FlightRuntime.truthy(FlightRuntime.compare(distSq, maxDistSq, '>'))) {
  (maxDistSq = cast (distSq : Dynamic));
  (maxIdx = cast (i : Dynamic));
}
        i++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.compare(maxDistSq, toleranceSq, '>'))) {
  FlightRuntime.setIndex(keep, maxIdx, 1.0);
  FlightRuntime.callValue(PathApi.douglasPeucker__decimatePath, cast ([pts, first, maxIdx, toleranceSq, keep] : Array<Dynamic>));
  FlightRuntime.callValue(PathApi.douglasPeucker__decimatePath, cast ([pts, maxIdx, last, toleranceSq, keep] : Array<Dynamic>));
}
  }

  @:keep public static function encodeReversedSubpath__reversePath(subpath:Subpath__reversePath, out:Path):Void {
    var pts:Dynamic = cast FlightRuntime.UNDEFINED;
    var last:Dynamic = cast FlightRuntime.UNDEFINED;
    pts = FlightRuntime.field(subpath, 'points');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(pts, 'length'), 0.0))) { return; }
    last = FlightRuntime.getIndex(pts, (FlightRuntime.field(pts, 'length') - 1.0));
    FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
    FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.field(last, 'x'), FlightRuntime.field(last, 'y')] : Array<Dynamic>));
    {
      var i:Dynamic = (FlightRuntime.field(pts, 'length') - 1.0);
      while (FlightRuntime.truthy(FlightRuntime.compare(i, 1.0, '>='))) {
        var from:Dynamic = FlightRuntime.getIndex(pts, i);
        var to:Dynamic = FlightRuntime.getIndex(pts, (i - 1.0));
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.field(from, 'kind'), 'line'), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(from, 'kind'), 'move')))) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.LINE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.field(to, 'x'), FlightRuntime.field(to, 'y')] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(from, 'kind'), 'quad'))) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.CURVE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.field(from, 'cx'), FlightRuntime.field(from, 'cy'), FlightRuntime.field(to, 'x'), FlightRuntime.field(to, 'y')] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(from, 'kind'), 'cubic'))) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.CUBIC_CURVE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.field(from, 'c2x'), FlightRuntime.field(from, 'c2y'), FlightRuntime.field(from, 'c1x'), FlightRuntime.field(from, 'c1y'), FlightRuntime.field(to, 'x'), FlightRuntime.field(to, 'y')] : Array<Dynamic>));
} } }
        i--;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.field(subpath, 'closed'))) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.CLOSE] : Array<Dynamic>));
}
  }

  @:keep public static function ensureContour__flattenPath(contours:Array<Array<Float>>, contour:Null<Array<Float>>):Array<Float> {
    var started:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(contour, null))) { return cast contour; }
    started = cast ([0.0, 0.0] : Array<Dynamic>);
    FlightRuntime.callProperty(contours, 'push', cast ([started] : Array<Dynamic>));
    return cast started;
    return cast null;
  }

  @:keep public static function evalCubic__getPathBounds(p0:Float, p1:Float, p2:Float, p3:Float, t:Float):Float {
    var mt:Dynamic = cast FlightRuntime.UNDEFINED;
    mt = (1.0 - t);
    return cast ((((((mt * mt) * mt) * p0) + ((((3.0 * mt) * mt) * t) * p1)) + ((((3.0 * mt) * t) * t) * p2)) + (((t * t) * t) * p3));
    return cast null;
  }

  @:keep public static function evalQuadratic__getPathBounds(p0:Float, p1:Float, p2:Float, t:Float):Float {
    var mt:Dynamic = cast FlightRuntime.UNDEFINED;
    mt = (1.0 - t);
    return cast ((((mt * mt) * p0) + (((2.0 * mt) * t) * p1)) + ((t * t) * p2));
    return cast null;
  }

  @:keep public static function expandCubicBounds__getPathBounds(x0:Float, y0:Float, c1x:Float, c1y:Float, c2x:Float, c2y:Float, x3:Float, y3:Float, expand:Dynamic):Void {
    FlightRuntime.callValue(expand, cast ([x3, y3] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.cubicExtremumRoots__getPathBounds, cast ([x0, c1x, c2x, x3, function(t:Dynamic) {
  FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(PathApi.evalCubic__getPathBounds, cast ([x0, c1x, c2x, x3, t] : Array<Dynamic>)), FlightRuntime.callValue(PathApi.evalCubic__getPathBounds, cast ([y0, c1y, c2y, y3, t] : Array<Dynamic>))] : Array<Dynamic>));
}] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.cubicExtremumRoots__getPathBounds, cast ([y0, c1y, c2y, y3, function(t:Dynamic) {
  FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(PathApi.evalCubic__getPathBounds, cast ([x0, c1x, c2x, x3, t] : Array<Dynamic>)), FlightRuntime.callValue(PathApi.evalCubic__getPathBounds, cast ([y0, c1y, c2y, y3, t] : Array<Dynamic>))] : Array<Dynamic>));
}] : Array<Dynamic>));
  }

  @:keep public static function expandQuadraticBounds__getPathBounds(x0:Float, y0:Float, cx:Float, cy:Float, x2:Float, y2:Float, expand:Dynamic):Void {
    var tx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ty:Dynamic = cast FlightRuntime.UNDEFINED;
    FlightRuntime.callValue(expand, cast ([x2, y2] : Array<Dynamic>));
    tx = FlightRuntime.callValue(PathApi.quadraticExtremumT__getPathBounds, cast ([x0, cx, x2] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(tx, null))) { FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(PathApi.evalQuadratic__getPathBounds, cast ([x0, cx, x2, tx] : Array<Dynamic>)), FlightRuntime.callValue(PathApi.evalQuadratic__getPathBounds, cast ([y0, cy, y2, tx] : Array<Dynamic>))] : Array<Dynamic>)); }
    ty = FlightRuntime.callValue(PathApi.quadraticExtremumT__getPathBounds, cast ([y0, cy, y2] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(ty, null))) { FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(PathApi.evalQuadratic__getPathBounds, cast ([x0, cx, x2, ty] : Array<Dynamic>)), FlightRuntime.callValue(PathApi.evalQuadratic__getPathBounds, cast ([y0, cy, y2, ty] : Array<Dynamic>))] : Array<Dynamic>)); }
  }

  @:keep public static function findCorners__fitPathCurves(pts:Array<Float>, n:Float):Array<Float> {
    var corners:Dynamic = cast FlightRuntime.UNDEFINED;
    corners = cast ([0.0] : Array<Dynamic>);
    {
      var i:Dynamic = 1.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, (n - 1.0), '<'))) {
        var dx0:Dynamic = (FlightRuntime.getIndex(pts, (i * 2.0)) - FlightRuntime.getIndex(pts, ((i - 1.0) * 2.0)));
        var dy0:Dynamic = (FlightRuntime.getIndex(pts, ((i * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, (((i - 1.0) * 2.0) + 1.0)));
        var dx1:Dynamic = (FlightRuntime.getIndex(pts, ((i + 1.0) * 2.0)) - FlightRuntime.getIndex(pts, (i * 2.0)));
        var dy1:Dynamic = (FlightRuntime.getIndex(pts, (((i + 1.0) * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, ((i * 2.0) + 1.0)));
        var len0:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx0 * dx0) + (dy0 * dy0))] : Array<Dynamic>));
        var len1:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx1 * dx1) + (dy1 * dy1))] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(len0, 0.0), function():Dynamic return cast FlightRuntime.strictEquals(len1, 0.0)))) { i++; continue; }
        var dot:Dynamic = (((dx0 * dx1) + (dy0 * dy1)) / (len0 * len1));
        if (FlightRuntime.truthy(FlightRuntime.compare(dot, 0.5, '<'))) { FlightRuntime.callProperty(corners, 'push', cast ([i] : Array<Dynamic>)); }
        i++;
      }
    }
    FlightRuntime.callProperty(corners, 'push', cast ([(n - 1.0)] : Array<Dynamic>));
    return cast corners;
    return cast null;
  }

  @:keep public static function fitCubic__fitPathCurves(pts:Array<Float>, first:Float, last:Float, tHat1:Array<Float>, tHat2:Array<Float>, toleranceSq:Float, out:Path):Void {
    var nPts:Dynamic = cast FlightRuntime.UNDEFINED;
    var u:Dynamic = cast FlightRuntime.UNDEFINED;
    var MAX_ITERATIONS:Dynamic = cast FlightRuntime.UNDEFINED;
    nPts = ((last - first) + 1.0);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(nPts, 2.0))) {
  var dist:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([(HxMath.pow((FlightRuntime.getIndex(pts, (last * 2.0)) - FlightRuntime.getIndex(pts, (first * 2.0))), 2.0) + HxMath.pow((FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, ((first * 2.0) + 1.0))), 2.0))] : Array<Dynamic>));
  var d:Dynamic = (dist / 3.0);
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.CUBIC_CURVE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([(FlightRuntime.getIndex(pts, (first * 2.0)) + (FlightRuntime.getIndex(tHat1, 0.0) * d)), (FlightRuntime.getIndex(pts, ((first * 2.0) + 1.0)) + (FlightRuntime.getIndex(tHat1, 1.0) * d)), (FlightRuntime.getIndex(pts, (last * 2.0)) + (FlightRuntime.getIndex(tHat2, 0.0) * d)), (FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0)) + (FlightRuntime.getIndex(tHat2, 1.0) * d)), FlightRuntime.getIndex(pts, (last * 2.0)), FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0))] : Array<Dynamic>));
  return;
}
    u = FlightRuntime.callValue(PathApi.chordLengthParameterize__fitPathCurves, cast ([pts, first, last] : Array<Dynamic>));
    MAX_ITERATIONS = 4.0;
    {
      var iter:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(iter, MAX_ITERATIONS, '<='))) {
        var bezier:Dynamic = FlightRuntime.callValue(PathApi.generateBezier__fitPathCurves, cast ([pts, first, last, u, tHat1, tHat2] : Array<Dynamic>));
        var __destructure0:Dynamic = FlightRuntime.callValue(PathApi.computeMaxError__fitPathCurves, cast ([pts, first, last, bezier, u] : Array<Dynamic>));
        var maxErr:Dynamic = FlightRuntime.getIndex(__destructure0, 0.0);
        var splitPoint:Dynamic = FlightRuntime.getIndex(__destructure0, 1.0);
        if (FlightRuntime.truthy(FlightRuntime.compare(maxErr, toleranceSq, '<'))) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.CUBIC_CURVE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(bezier, 2.0), FlightRuntime.getIndex(bezier, 3.0), FlightRuntime.getIndex(bezier, 4.0), FlightRuntime.getIndex(bezier, 5.0), FlightRuntime.getIndex(bezier, 6.0), FlightRuntime.getIndex(bezier, 7.0)] : Array<Dynamic>));
  return;
}
        if (FlightRuntime.truthy(FlightRuntime.compare(iter, MAX_ITERATIONS, '<'))) {
  (u = cast (FlightRuntime.callValue(PathApi.reparameterize__fitPathCurves, cast ([pts, first, last, u, bezier] : Array<Dynamic>)) : Dynamic));
} else {
  var tHatCenter:Dynamic = FlightRuntime.callValue(PathApi.computeCenterTangent__fitPathCurves, cast ([pts, splitPoint] : Array<Dynamic>));
  FlightRuntime.callValue(PathApi.fitCubic__fitPathCurves, cast ([pts, first, splitPoint, tHat1, cast ([-FlightRuntime.getIndex(tHatCenter, 0.0), -FlightRuntime.getIndex(tHatCenter, 1.0)] : Array<Dynamic>), toleranceSq, out] : Array<Dynamic>));
  FlightRuntime.callValue(PathApi.fitCubic__fitPathCurves, cast ([pts, splitPoint, last, tHatCenter, tHat2, toleranceSq, out] : Array<Dynamic>));
}
        iter++;
      }
    }
  }

  @:keep public static function fitPathCurves(source:Path, tolerance:Float, out:Path, flattenTolerance:Dynamic = 0.25):Void {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var toleranceSq:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([source, flattenTolerance] : Array<Dynamic>));
    FlightRuntime.setLength(FlightRuntime.field(out, 'commands'), 0.0);
    FlightRuntime.setLength(FlightRuntime.field(out, 'data'), 0.0);
    FlightRuntime.setField(out, 'winding', FlightRuntime.field(source, 'winding'));
    toleranceSq = (tolerance * tolerance);
    for (contour in FlightRuntime.iterable(contours)) {
      var n:Dynamic = (Std.int(FlightRuntime.field(contour, 'length')) >> Std.int(1.0));
      if (FlightRuntime.truthy(FlightRuntime.compare(n, 2.0, '<'))) { continue; }
      var closed:Dynamic = FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(n, 3.0, '>='), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(contour, 0.0), FlightRuntime.getIndex(contour, (FlightRuntime.field(contour, 'length') - 2.0)))), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(contour, 1.0), FlightRuntime.getIndex(contour, (FlightRuntime.field(contour, 'length') - 1.0))));
      var pts:Dynamic = FlightRuntime.select(closed, function():Dynamic return cast FlightRuntime.slice(contour, 0.0, ((n - 1.0) * 2.0)), function():Dynamic return cast contour);
      var pn:Dynamic = (Std.int(FlightRuntime.field(pts, 'length')) >> Std.int(1.0));
      if (FlightRuntime.truthy(FlightRuntime.compare(pn, 2.0, '<'))) { continue; }
      if (FlightRuntime.truthy(FlightRuntime.strictEquals(pn, 2.0))) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(pts, 0.0), FlightRuntime.getIndex(pts, 1.0)] : Array<Dynamic>));
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.LINE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(pts, 2.0), FlightRuntime.getIndex(pts, 3.0)] : Array<Dynamic>));
  if (FlightRuntime.truthy(closed)) { FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.CLOSE] : Array<Dynamic>)); }
  continue;
}
      var corners:Dynamic = FlightRuntime.callValue(PathApi.findCorners__fitPathCurves, cast ([pts, pn] : Array<Dynamic>));
      FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
      FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(pts, 0.0), FlightRuntime.getIndex(pts, 1.0)] : Array<Dynamic>));
      {
        var ci:Dynamic = 0.0;
        while (FlightRuntime.truthy(FlightRuntime.compare(ci, (FlightRuntime.field(corners, 'length') - 1.0), '<'))) {
          var first:Dynamic = FlightRuntime.getIndex(corners, ci);
          var last:Dynamic = FlightRuntime.getIndex(corners, (ci + 1.0));
          if (FlightRuntime.truthy(FlightRuntime.compare((last - first), 2.0, '<'))) {
  FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.LINE_TO] : Array<Dynamic>));
  FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(pts, (last * 2.0)), FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0))] : Array<Dynamic>));
  ci++;
  continue;
}
          var tHat1:Dynamic = FlightRuntime.callValue(PathApi.computeLeftTangent__fitPathCurves, cast ([pts, first] : Array<Dynamic>));
          var tHat2:Dynamic = FlightRuntime.callValue(PathApi.computeRightTangent__fitPathCurves, cast ([pts, last] : Array<Dynamic>));
          FlightRuntime.callValue(PathApi.fitCubic__fitPathCurves, cast ([pts, first, last, tHat1, tHat2, toleranceSq, out] : Array<Dynamic>));
          ci++;
        }
      }
      if (FlightRuntime.truthy(closed)) { FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.CLOSE] : Array<Dynamic>)); }
    }
  }

  @:keep public static function flattenCubic__flattenPath(out:Array<Float>, x0:Float, y0:Float, c1x:Float, c1y:Float, c2x:Float, c2y:Float, x1:Float, y1:Float, toleranceSq:Float, depth:Float):Void {
    var d1:Dynamic = cast FlightRuntime.UNDEFINED;
    var d2:Dynamic = cast FlightRuntime.UNDEFINED;
    var x01:Dynamic = cast FlightRuntime.UNDEFINED;
    var y01:Dynamic = cast FlightRuntime.UNDEFINED;
    var x12:Dynamic = cast FlightRuntime.UNDEFINED;
    var y12:Dynamic = cast FlightRuntime.UNDEFINED;
    var x23:Dynamic = cast FlightRuntime.UNDEFINED;
    var y23:Dynamic = cast FlightRuntime.UNDEFINED;
    var x012:Dynamic = cast FlightRuntime.UNDEFINED;
    var y012:Dynamic = cast FlightRuntime.UNDEFINED;
    var x123:Dynamic = cast FlightRuntime.UNDEFINED;
    var y123:Dynamic = cast FlightRuntime.UNDEFINED;
    var xm:Dynamic = cast FlightRuntime.UNDEFINED;
    var ym:Dynamic = cast FlightRuntime.UNDEFINED;
    d1 = FlightRuntime.callValue(PathApi.distanceToChordSq__flattenPath, cast ([c1x, c1y, x0, y0, x1, y1] : Array<Dynamic>));
    d2 = FlightRuntime.callValue(PathApi.distanceToChordSq__flattenPath, cast ([c2x, c2y, x0, y0, x1, y1] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(depth, PathApi.MAX_SUBDIVISION_DEPTH__flattenPath, '>='), function():Dynamic return cast FlightRuntime.andValue(FlightRuntime.compare(d1, toleranceSq, '<='), function():Dynamic return cast FlightRuntime.compare(d2, toleranceSq, '<='))))) {
  FlightRuntime.pushMany(out, cast ([x1, y1] : Array<Dynamic>));
  return;
}
    x01 = ((x0 + c1x) / 2.0);
    y01 = ((y0 + c1y) / 2.0);
    x12 = ((c1x + c2x) / 2.0);
    y12 = ((c1y + c2y) / 2.0);
    x23 = ((c2x + x1) / 2.0);
    y23 = ((c2y + y1) / 2.0);
    x012 = ((x01 + x12) / 2.0);
    y012 = ((y01 + y12) / 2.0);
    x123 = ((x12 + x23) / 2.0);
    y123 = ((y12 + y23) / 2.0);
    xm = ((x012 + x123) / 2.0);
    ym = ((y012 + y123) / 2.0);
    FlightRuntime.callValue(PathApi.flattenCubic__flattenPath, cast ([out, x0, y0, x01, y01, x012, y012, xm, ym, toleranceSq, (depth + 1.0)] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.flattenCubic__flattenPath, cast ([out, xm, ym, x123, y123, x23, y23, x1, y1, toleranceSq, (depth + 1.0)] : Array<Dynamic>));
  }

  @:keep public static function flattenCubic__strokePath(out:Array<Float>, x0:Float, y0:Float, c1x:Float, c1y:Float, c2x:Float, c2y:Float, x1:Float, y1:Float, toleranceSq:Float, depth:Float):Void {
    var dxc1:Dynamic = cast FlightRuntime.UNDEFINED;
    var dxc2:Dynamic = cast FlightRuntime.UNDEFINED;
    var x01:Dynamic = cast FlightRuntime.UNDEFINED;
    var y01:Dynamic = cast FlightRuntime.UNDEFINED;
    var x12:Dynamic = cast FlightRuntime.UNDEFINED;
    var y12:Dynamic = cast FlightRuntime.UNDEFINED;
    var x23:Dynamic = cast FlightRuntime.UNDEFINED;
    var y23:Dynamic = cast FlightRuntime.UNDEFINED;
    var x012:Dynamic = cast FlightRuntime.UNDEFINED;
    var y012:Dynamic = cast FlightRuntime.UNDEFINED;
    var x123:Dynamic = cast FlightRuntime.UNDEFINED;
    var y123:Dynamic = cast FlightRuntime.UNDEFINED;
    var xm:Dynamic = cast FlightRuntime.UNDEFINED;
    var ym:Dynamic = cast FlightRuntime.UNDEFINED;
    dxc1 = FlightRuntime.callValue(PathApi.distChordSq__strokePath, cast ([c1x, c1y, x0, y0, x1, y1] : Array<Dynamic>));
    dxc2 = FlightRuntime.callValue(PathApi.distChordSq__strokePath, cast ([c2x, c2y, x0, y0, x1, y1] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(depth, PathApi.MAX_SUBDIVISION_DEPTH__strokePath, '>='), function():Dynamic return cast FlightRuntime.andValue(FlightRuntime.compare(dxc1, toleranceSq, '<='), function():Dynamic return cast FlightRuntime.compare(dxc2, toleranceSq, '<='))))) {
  FlightRuntime.pushMany(out, cast ([x1, y1] : Array<Dynamic>));
  return;
}
    x01 = ((x0 + c1x) / 2.0);
    y01 = ((y0 + c1y) / 2.0);
    x12 = ((c1x + c2x) / 2.0);
    y12 = ((c1y + c2y) / 2.0);
    x23 = ((c2x + x1) / 2.0);
    y23 = ((c2y + y1) / 2.0);
    x012 = ((x01 + x12) / 2.0);
    y012 = ((y01 + y12) / 2.0);
    x123 = ((x12 + x23) / 2.0);
    y123 = ((y12 + y23) / 2.0);
    xm = ((x012 + x123) / 2.0);
    ym = ((y012 + y123) / 2.0);
    FlightRuntime.callValue(PathApi.flattenCubic__strokePath, cast ([out, x0, y0, x01, y01, x012, y012, xm, ym, toleranceSq, (depth + 1.0)] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.flattenCubic__strokePath, cast ([out, xm, ym, x123, y123, x23, y23, x1, y1, toleranceSq, (depth + 1.0)] : Array<Dynamic>));
  }

  @:keep public static function flattenCubicWindingNumber__containsPathPoint(px:Float, py:Float, x0:Float, y0:Float, c1x:Float, c1y:Float, c2x:Float, c2y:Float, x1:Float, y1:Float, toleranceSq:Float, depth:Float):Float {
    var d1:Dynamic = cast FlightRuntime.UNDEFINED;
    var d2:Dynamic = cast FlightRuntime.UNDEFINED;
    var x01:Dynamic = cast FlightRuntime.UNDEFINED;
    var y01:Dynamic = cast FlightRuntime.UNDEFINED;
    var x12:Dynamic = cast FlightRuntime.UNDEFINED;
    var y12:Dynamic = cast FlightRuntime.UNDEFINED;
    var x23:Dynamic = cast FlightRuntime.UNDEFINED;
    var y23:Dynamic = cast FlightRuntime.UNDEFINED;
    var x012:Dynamic = cast FlightRuntime.UNDEFINED;
    var y012:Dynamic = cast FlightRuntime.UNDEFINED;
    var x123:Dynamic = cast FlightRuntime.UNDEFINED;
    var y123:Dynamic = cast FlightRuntime.UNDEFINED;
    var xm:Dynamic = cast FlightRuntime.UNDEFINED;
    var ym:Dynamic = cast FlightRuntime.UNDEFINED;
    d1 = FlightRuntime.callValue(PathApi.chordDistSq__containsPathPoint, cast ([c1x, c1y, x0, y0, x1, y1] : Array<Dynamic>));
    d2 = FlightRuntime.callValue(PathApi.chordDistSq__containsPathPoint, cast ([c2x, c2y, x0, y0, x1, y1] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(depth, PathApi.MAX_SUBDIVISION_DEPTH__containsPathPoint, '>='), function():Dynamic return cast FlightRuntime.andValue(FlightRuntime.compare(d1, toleranceSq, '<='), function():Dynamic return cast FlightRuntime.compare(d2, toleranceSq, '<='))))) {
  return cast FlightRuntime.callValue(PathApi.countSegmentCrossings__containsPathPoint, cast ([px, py, x0, y0, x1, y1] : Array<Dynamic>));
}
    x01 = ((x0 + c1x) / 2.0);
    y01 = ((y0 + c1y) / 2.0);
    x12 = ((c1x + c2x) / 2.0);
    y12 = ((c1y + c2y) / 2.0);
    x23 = ((c2x + x1) / 2.0);
    y23 = ((c2y + y1) / 2.0);
    x012 = ((x01 + x12) / 2.0);
    y012 = ((y01 + y12) / 2.0);
    x123 = ((x12 + x23) / 2.0);
    y123 = ((y12 + y23) / 2.0);
    xm = ((x012 + x123) / 2.0);
    ym = ((y012 + y123) / 2.0);
    return cast (FlightRuntime.callValue(PathApi.flattenCubicWindingNumber__containsPathPoint, cast ([px, py, x0, y0, x01, y01, x012, y012, xm, ym, toleranceSq, (depth + 1.0)] : Array<Dynamic>)) + FlightRuntime.callValue(PathApi.flattenCubicWindingNumber__containsPathPoint, cast ([px, py, xm, ym, x123, y123, x23, y23, x1, y1, toleranceSq, (depth + 1.0)] : Array<Dynamic>)));
    return cast null;
  }

  @:keep public static function flattenPath(path:Path, tolerance:Dynamic = 0.25):Array<Array<Float>> {
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var toleranceSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var contours:Array<Array<Float>> = cast FlightRuntime.UNDEFINED;
    var contour:Null<Array<Float>> = cast FlightRuntime.UNDEFINED;
    var x:Dynamic = cast FlightRuntime.UNDEFINED;
    var y:Dynamic = cast FlightRuntime.UNDEFINED;
    var contourStartX:Dynamic = cast FlightRuntime.UNDEFINED;
    var contourStartY:Dynamic = cast FlightRuntime.UNDEFINED;
    var di:Dynamic = cast FlightRuntime.UNDEFINED;
    commands = FlightRuntime.field(path, 'commands');
    data = FlightRuntime.field(path, 'data');
    toleranceSq = (tolerance * tolerance);
    contours = cast ([] : Array<Dynamic>);
    contour = null;
    x = 0.0;
    y = 0.0;
    contourStartX = 0.0;
    contourStartY = 0.0;
    di = 0.0;
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(commands, 'length'), '<'))) {
        var command:Dynamic = FlightRuntime.getIndex(commands, ci);
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.MOVE_TO))) {
  (x = cast (FlightRuntime.getIndex(data, di) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 1.0)) : Dynamic));
  (di = cast ((di + 2.0) : Dynamic));
  (contourStartX = cast (x : Dynamic));
  (contourStartY = cast (y : Dynamic));
  (contour = cast (cast ([x, y] : Array<Dynamic>) : Dynamic));
  FlightRuntime.callProperty(contours, 'push', cast ([contour] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_MOVE_TO))) {
  (x = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
  (di = cast ((di + 4.0) : Dynamic));
  (contourStartX = cast (x : Dynamic));
  (contourStartY = cast (y : Dynamic));
  (contour = cast (cast ([x, y] : Array<Dynamic>) : Dynamic));
  FlightRuntime.callProperty(contours, 'push', cast ([contour] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.LINE_TO))) {
  (contour = cast (FlightRuntime.callValue(PathApi.ensureContour__flattenPath, cast ([contours, contour] : Array<Dynamic>)) : Dynamic));
  (x = cast (FlightRuntime.getIndex(data, di) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 1.0)) : Dynamic));
  (di = cast ((di + 2.0) : Dynamic));
  FlightRuntime.pushMany(contour, cast ([x, y] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_LINE_TO))) {
  (contour = cast (FlightRuntime.callValue(PathApi.ensureContour__flattenPath, cast ([contours, contour] : Array<Dynamic>)) : Dynamic));
  (x = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
  (di = cast ((di + 4.0) : Dynamic));
  FlightRuntime.pushMany(contour, cast ([x, y] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CURVE_TO))) {
  (contour = cast (FlightRuntime.callValue(PathApi.ensureContour__flattenPath, cast ([contours, contour] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callValue(PathApi.flattenQuadratic__flattenPath, cast ([contour, x, y, FlightRuntime.getIndex(data, di), FlightRuntime.getIndex(data, (di + 1.0)), FlightRuntime.getIndex(data, (di + 2.0)), FlightRuntime.getIndex(data, (di + 3.0)), toleranceSq, 0.0] : Array<Dynamic>));
  (x = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
  (di = cast ((di + 4.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CUBIC_CURVE_TO))) {
  (contour = cast (FlightRuntime.callValue(PathApi.ensureContour__flattenPath, cast ([contours, contour] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callValue(PathApi.flattenCubic__flattenPath, cast ([contour, x, y, FlightRuntime.getIndex(data, di), FlightRuntime.getIndex(data, (di + 1.0)), FlightRuntime.getIndex(data, (di + 2.0)), FlightRuntime.getIndex(data, (di + 3.0)), FlightRuntime.getIndex(data, (di + 4.0)), FlightRuntime.getIndex(data, (di + 5.0)), toleranceSq, 0.0] : Array<Dynamic>));
  (x = cast (FlightRuntime.getIndex(data, (di + 4.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 5.0)) : Dynamic));
  (di = cast ((di + 6.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CLOSE))) {
  if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(contour, null), function():Dynamic return cast FlightRuntime.orValue(!FlightRuntime.strictEquals(x, contourStartX), function():Dynamic return cast !FlightRuntime.strictEquals(y, contourStartY))))) {
  FlightRuntime.pushMany(contour, cast ([contourStartX, contourStartY] : Array<Dynamic>));
}
  (x = cast (contourStartX : Dynamic));
  (y = cast (contourStartY : Dynamic));
  (contour = cast (null : Dynamic));
} } } } } } }
        ci++;
      }
    }
    return cast contours;
    return cast null;
  }

  @:keep public static function flattenQuadratic__flattenPath(out:Array<Float>, x0:Float, y0:Float, cx:Float, cy:Float, x1:Float, y1:Float, toleranceSq:Float, depth:Float):Void {
    var x01:Dynamic = cast FlightRuntime.UNDEFINED;
    var y01:Dynamic = cast FlightRuntime.UNDEFINED;
    var x12:Dynamic = cast FlightRuntime.UNDEFINED;
    var y12:Dynamic = cast FlightRuntime.UNDEFINED;
    var xm:Dynamic = cast FlightRuntime.UNDEFINED;
    var ym:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(depth, PathApi.MAX_SUBDIVISION_DEPTH__flattenPath, '>='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callValue(PathApi.distanceToChordSq__flattenPath, cast ([cx, cy, x0, y0, x1, y1] : Array<Dynamic>)), toleranceSq, '<=')))) {
  FlightRuntime.pushMany(out, cast ([x1, y1] : Array<Dynamic>));
  return;
}
    x01 = ((x0 + cx) / 2.0);
    y01 = ((y0 + cy) / 2.0);
    x12 = ((cx + x1) / 2.0);
    y12 = ((cy + y1) / 2.0);
    xm = ((x01 + x12) / 2.0);
    ym = ((y01 + y12) / 2.0);
    FlightRuntime.callValue(PathApi.flattenQuadratic__flattenPath, cast ([out, x0, y0, x01, y01, xm, ym, toleranceSq, (depth + 1.0)] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.flattenQuadratic__flattenPath, cast ([out, xm, ym, x12, y12, x1, y1, toleranceSq, (depth + 1.0)] : Array<Dynamic>));
  }

  @:keep public static function flattenQuadratic__strokePath(out:Array<Float>, x0:Float, y0:Float, cx:Float, cy:Float, x1:Float, y1:Float, toleranceSq:Float, depth:Float):Void {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var lengthSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var distSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var x01:Dynamic = cast FlightRuntime.UNDEFINED;
    var y01:Dynamic = cast FlightRuntime.UNDEFINED;
    var x12:Dynamic = cast FlightRuntime.UNDEFINED;
    var y12:Dynamic = cast FlightRuntime.UNDEFINED;
    var xm:Dynamic = cast FlightRuntime.UNDEFINED;
    var ym:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (x1 - x0);
    dy = (y1 - y0);
    lengthSq = ((dx * dx) + (dy * dy));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(lengthSq, 0.0))) {
  var ax:Dynamic = (cx - x0);
  var ay:Dynamic = (cy - y0);
  (distSq = cast (((ax * ax) + (ay * ay)) : Dynamic));
} else {
  var cross:Dynamic = ((dx * (y0 - cy)) - (dy * (x0 - cx)));
  (distSq = cast (((cross * cross) / lengthSq) : Dynamic));
}
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(depth, PathApi.MAX_SUBDIVISION_DEPTH__strokePath, '>='), function():Dynamic return cast FlightRuntime.compare(distSq, toleranceSq, '<=')))) {
  FlightRuntime.pushMany(out, cast ([x1, y1] : Array<Dynamic>));
  return;
}
    x01 = ((x0 + cx) / 2.0);
    y01 = ((y0 + cy) / 2.0);
    x12 = ((cx + x1) / 2.0);
    y12 = ((cy + y1) / 2.0);
    xm = ((x01 + x12) / 2.0);
    ym = ((y01 + y12) / 2.0);
    FlightRuntime.callValue(PathApi.flattenQuadratic__strokePath, cast ([out, x0, y0, x01, y01, xm, ym, toleranceSq, (depth + 1.0)] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.flattenQuadratic__strokePath, cast ([out, xm, ym, x12, y12, x1, y1, toleranceSq, (depth + 1.0)] : Array<Dynamic>));
  }

  @:keep public static function flattenQuadraticWindingNumber__containsPathPoint(px:Float, py:Float, x0:Float, y0:Float, cx:Float, cy:Float, x1:Float, y1:Float, toleranceSq:Float, depth:Float):Float {
    var mx01:Dynamic = cast FlightRuntime.UNDEFINED;
    var my01:Dynamic = cast FlightRuntime.UNDEFINED;
    var mx12:Dynamic = cast FlightRuntime.UNDEFINED;
    var my12:Dynamic = cast FlightRuntime.UNDEFINED;
    var mx:Dynamic = cast FlightRuntime.UNDEFINED;
    var my:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(depth, PathApi.MAX_SUBDIVISION_DEPTH__containsPathPoint, '>='), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.callValue(PathApi.chordDistSq__containsPathPoint, cast ([cx, cy, x0, y0, x1, y1] : Array<Dynamic>)), toleranceSq, '<=')))) {
  return cast FlightRuntime.callValue(PathApi.countSegmentCrossings__containsPathPoint, cast ([px, py, x0, y0, x1, y1] : Array<Dynamic>));
}
    mx01 = ((x0 + cx) / 2.0);
    my01 = ((y0 + cy) / 2.0);
    mx12 = ((cx + x1) / 2.0);
    my12 = ((cy + y1) / 2.0);
    mx = ((mx01 + mx12) / 2.0);
    my = ((my01 + my12) / 2.0);
    return cast (FlightRuntime.callValue(PathApi.flattenQuadraticWindingNumber__containsPathPoint, cast ([px, py, x0, y0, mx01, my01, mx, my, toleranceSq, (depth + 1.0)] : Array<Dynamic>)) + FlightRuntime.callValue(PathApi.flattenQuadraticWindingNumber__containsPathPoint, cast ([px, py, mx, my, mx12, my12, x1, y1, toleranceSq, (depth + 1.0)] : Array<Dynamic>)));
    return cast null;
  }

  @:keep public static function forEachPathSegment(path:Path, visitor:Dynamic):Void {
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var di:Dynamic = cast FlightRuntime.UNDEFINED;
    commands = FlightRuntime.field(path, 'commands');
    data = FlightRuntime.field(path, 'data');
    di = 0.0;
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(commands, 'length'), '<'))) {
        var command:Dynamic = FlightRuntime.getIndex(commands, ci);
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.MOVE_TO))) {
  var x:Dynamic = FlightRuntime.getIndex(data, di);
  var y:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  (di = cast ((di + 2.0) : Dynamic));
  FlightRuntime.callValue(visitor, cast ([{ kind: 'moveTo', x: x, y: y }] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_MOVE_TO))) {
  var x:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var y:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  (di = cast ((di + 4.0) : Dynamic));
  FlightRuntime.callValue(visitor, cast ([{ kind: 'moveTo', x: x, y: y }] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.LINE_TO))) {
  var x:Dynamic = FlightRuntime.getIndex(data, di);
  var y:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  (di = cast ((di + 2.0) : Dynamic));
  FlightRuntime.callValue(visitor, cast ([{ kind: 'lineTo', x: x, y: y }] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_LINE_TO))) {
  var x:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var y:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  (di = cast ((di + 4.0) : Dynamic));
  FlightRuntime.callValue(visitor, cast ([{ kind: 'lineTo', x: x, y: y }] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CURVE_TO))) {
  var controlX:Dynamic = FlightRuntime.getIndex(data, di);
  var controlY:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  var x:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var y:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  (di = cast ((di + 4.0) : Dynamic));
  FlightRuntime.callValue(visitor, cast ([{ kind: 'curveTo', controlX: controlX, controlY: controlY, x: x, y: y }] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CUBIC_CURVE_TO))) {
  var control1X:Dynamic = FlightRuntime.getIndex(data, di);
  var control1Y:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  var control2X:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var control2Y:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  var x:Dynamic = FlightRuntime.getIndex(data, (di + 4.0));
  var y:Dynamic = FlightRuntime.getIndex(data, (di + 5.0));
  (di = cast ((di + 6.0) : Dynamic));
  FlightRuntime.callValue(visitor, cast ([{ kind: 'cubicCurveTo', control1X: control1X, control1Y: control1Y, control2X: control2X, control2Y: control2Y, x: x, y: y }] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CLOSE))) {
  FlightRuntime.callValue(visitor, cast ([{ kind: 'close' }] : Array<Dynamic>));
} } } } } } }
        ci++;
      }
    }
  }

  @:keep public static function generateBezier__fitPathCurves(pts:Array<Float>, first:Float, last:Float, u:Array<Float>, tHat1:Array<Float>, tHat2:Array<Float>):Array<Float> {
    var nPts:Dynamic = cast FlightRuntime.UNDEFINED;
    var c00:Dynamic = cast FlightRuntime.UNDEFINED;
    var c01:Dynamic = cast FlightRuntime.UNDEFINED;
    var c11:Dynamic = cast FlightRuntime.UNDEFINED;
    var x0:Dynamic = cast FlightRuntime.UNDEFINED;
    var x1:Dynamic = cast FlightRuntime.UNDEFINED;
    var det:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha1:Float = cast FlightRuntime.UNDEFINED;
    var alpha2:Float = cast FlightRuntime.UNDEFINED;
    var segLength:Dynamic = cast FlightRuntime.UNDEFINED;
    var epsilon:Dynamic = cast FlightRuntime.UNDEFINED;
    nPts = ((last - first) + 1.0);
    c00 = 0.0;
    c01 = 0.0;
    c11 = 0.0;
    x0 = 0.0;
    x1 = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, nPts, '<'))) {
        var t:Dynamic = FlightRuntime.getIndex(u, i);
        var b1:Dynamic = (((3.0 * t) * (1.0 - t)) * (1.0 - t));
        var b2:Dynamic = (((3.0 * t) * t) * (1.0 - t));
        var a1x:Dynamic = (FlightRuntime.getIndex(tHat1, 0.0) * b1);
        var a1y:Dynamic = (FlightRuntime.getIndex(tHat1, 1.0) * b1);
        var a2x:Dynamic = (FlightRuntime.getIndex(tHat2, 0.0) * b2);
        var a2y:Dynamic = (FlightRuntime.getIndex(tHat2, 1.0) * b2);
        (c00 = cast ((c00 + ((a1x * a1x) + (a1y * a1y))) : Dynamic));
        (c01 = cast ((c01 + ((a1x * a2x) + (a1y * a2y))) : Dynamic));
        (c11 = cast ((c11 + ((a2x * a2x) + (a2y * a2y))) : Dynamic));
        var b0:Dynamic = (((1.0 - t) * (1.0 - t)) * (1.0 - t));
        var b3:Dynamic = ((t * t) * t);
        var tmpx:Dynamic = (FlightRuntime.getIndex(pts, ((first + i) * 2.0)) - ((((FlightRuntime.getIndex(pts, (first * 2.0)) * b0) + (FlightRuntime.getIndex(pts, (first * 2.0)) * b1)) + (FlightRuntime.getIndex(pts, (last * 2.0)) * b2)) + (FlightRuntime.getIndex(pts, (last * 2.0)) * b3)));
        var tmpy:Dynamic = (FlightRuntime.getIndex(pts, (((first + i) * 2.0) + 1.0)) - ((((FlightRuntime.getIndex(pts, ((first * 2.0) + 1.0)) * b0) + (FlightRuntime.getIndex(pts, ((first * 2.0) + 1.0)) * b1)) + (FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0)) * b2)) + (FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0)) * b3)));
        (x0 = cast ((x0 + ((a1x * tmpx) + (a1y * tmpy))) : Dynamic));
        (x1 = cast ((x1 + ((a2x * tmpx) + (a2y * tmpy))) : Dynamic));
        i++;
      }
    }
    det = ((c00 * c11) - (c01 * c01));
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([det] : Array<Dynamic>)), 1e-12, '<'))) {
  var dist:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([(HxMath.pow((FlightRuntime.getIndex(pts, (last * 2.0)) - FlightRuntime.getIndex(pts, (first * 2.0))), 2.0) + HxMath.pow((FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, ((first * 2.0) + 1.0))), 2.0))] : Array<Dynamic>));
  (alpha1 = cast ((alpha2 = cast ((dist / 3.0) : Dynamic)) : Dynamic));
} else {
  (alpha1 = cast ((((c11 * x0) - (c01 * x1)) / det) : Dynamic));
  (alpha2 = cast ((((c00 * x1) - (c01 * x0)) / det) : Dynamic));
}
    segLength = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([(HxMath.pow((FlightRuntime.getIndex(pts, (last * 2.0)) - FlightRuntime.getIndex(pts, (first * 2.0))), 2.0) + HxMath.pow((FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, ((first * 2.0) + 1.0))), 2.0))] : Array<Dynamic>));
    epsilon = (0.000001 * segLength);
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(alpha1, epsilon, '<'), function():Dynamic return cast FlightRuntime.compare(alpha2, epsilon, '<')))) {
  (alpha1 = cast ((alpha2 = cast ((segLength / 3.0) : Dynamic)) : Dynamic));
}
    return cast cast ([FlightRuntime.getIndex(pts, (first * 2.0)), FlightRuntime.getIndex(pts, ((first * 2.0) + 1.0)), (FlightRuntime.getIndex(pts, (first * 2.0)) + (FlightRuntime.getIndex(tHat1, 0.0) * alpha1)), (FlightRuntime.getIndex(pts, ((first * 2.0) + 1.0)) + (FlightRuntime.getIndex(tHat1, 1.0) * alpha1)), (FlightRuntime.getIndex(pts, (last * 2.0)) + (FlightRuntime.getIndex(tHat2, 0.0) * alpha2)), (FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0)) + (FlightRuntime.getIndex(tHat2, 1.0) * alpha2)), FlightRuntime.getIndex(pts, (last * 2.0)), FlightRuntime.getIndex(pts, ((last * 2.0) + 1.0))] : Array<Dynamic>);
    return cast null;
  }

  @:keep public static function getCubicBezierPoint(x0:Float, y0:Float, c1x:Float, c1y:Float, c2x:Float, c2y:Float, x1:Float, y1:Float, t:Float, out:Vector2Like):Vector2Like {
    var u:Dynamic = cast FlightRuntime.UNDEFINED;
    var u2:Dynamic = cast FlightRuntime.UNDEFINED;
    var u3:Dynamic = cast FlightRuntime.UNDEFINED;
    var t2:Dynamic = cast FlightRuntime.UNDEFINED;
    var t3:Dynamic = cast FlightRuntime.UNDEFINED;
    u = (1.0 - t);
    u2 = (u * u);
    u3 = (u2 * u);
    t2 = (t * t);
    t3 = (t2 * t);
    FlightRuntime.setField(out, 'x', ((((u3 * x0) + (((3.0 * u2) * t) * c1x)) + (((3.0 * u) * t2) * c2x)) + (t3 * x1)));
    FlightRuntime.setField(out, 'y', ((((u3 * y0) + (((3.0 * u2) * t) * c1y)) + (((3.0 * u) * t2) * c2y)) + (t3 * y1)));
    return cast out;
    return cast null;
  }

  @:keep public static function getCubicBezierTangent(x0:Float, y0:Float, c1x:Float, c1y:Float, c2x:Float, c2y:Float, x1:Float, y1:Float, t:Float, out:Vector2Like):Vector2Like {
    var u:Dynamic = cast FlightRuntime.UNDEFINED;
    var u2:Dynamic = cast FlightRuntime.UNDEFINED;
    var t2:Dynamic = cast FlightRuntime.UNDEFINED;
    u = (1.0 - t);
    u2 = (u * u);
    t2 = (t * t);
    FlightRuntime.setField(out, 'x', (3.0 * (((u2 * (c1x - x0)) + (((2.0 * u) * t) * (c2x - c1x))) + (t2 * (x1 - c2x)))));
    FlightRuntime.setField(out, 'y', (3.0 * (((u2 * (c1y - y0)) + (((2.0 * u) * t) * (c2y - c1y))) + (t2 * (y1 - c2y)))));
    return cast out;
    return cast null;
  }

  @:keep public static function getPathBounds(path:Path, out:RectangleLike):Bool {
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var minX:Dynamic = cast FlightRuntime.UNDEFINED;
    var minY:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxX:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxY:Dynamic = cast FlightRuntime.UNDEFINED;
    var x:Dynamic = cast FlightRuntime.UNDEFINED;
    var y:Dynamic = cast FlightRuntime.UNDEFINED;
    var di:Dynamic = cast FlightRuntime.UNDEFINED;
    var expand:Dynamic = cast FlightRuntime.UNDEFINED;
    commands = FlightRuntime.field(path, 'commands');
    data = FlightRuntime.field(path, 'data');
    minX = HxMath.POSITIVE_INFINITY;
    minY = HxMath.POSITIVE_INFINITY;
    maxX = -HxMath.POSITIVE_INFINITY;
    maxY = -HxMath.POSITIVE_INFINITY;
    x = 0.0;
    y = 0.0;
    di = 0.0;
    expand = function(px:Float, py:Float) {
  if (FlightRuntime.truthy(FlightRuntime.compare(px, minX, '<'))) { (minX = cast (px : Dynamic)); }
  if (FlightRuntime.truthy(FlightRuntime.compare(px, maxX, '>'))) { (maxX = cast (px : Dynamic)); }
  if (FlightRuntime.truthy(FlightRuntime.compare(py, minY, '<'))) { (minY = cast (py : Dynamic)); }
  if (FlightRuntime.truthy(FlightRuntime.compare(py, maxY, '>'))) { (maxY = cast (py : Dynamic)); }
};
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(commands, 'length'), '<'))) {
        var command:Dynamic = FlightRuntime.getIndex(commands, ci);
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.MOVE_TO))) {
  (x = cast (FlightRuntime.getIndex(data, di) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 1.0)) : Dynamic));
  (di = cast ((di + 2.0) : Dynamic));
  FlightRuntime.callValue(expand, cast ([x, y] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_MOVE_TO))) {
  (x = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
  (di = cast ((di + 4.0) : Dynamic));
  FlightRuntime.callValue(expand, cast ([x, y] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.LINE_TO))) {
  var nx:Dynamic = FlightRuntime.getIndex(data, di);
  var ny:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  (di = cast ((di + 2.0) : Dynamic));
  FlightRuntime.callValue(expand, cast ([nx, ny] : Array<Dynamic>));
  (x = cast (nx : Dynamic));
  (y = cast (ny : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_LINE_TO))) {
  var nx:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var ny:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  (di = cast ((di + 4.0) : Dynamic));
  FlightRuntime.callValue(expand, cast ([nx, ny] : Array<Dynamic>));
  (x = cast (nx : Dynamic));
  (y = cast (ny : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CURVE_TO))) {
  var cx:Dynamic = FlightRuntime.getIndex(data, di);
  var cy:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  var ax:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var ay:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  (di = cast ((di + 4.0) : Dynamic));
  FlightRuntime.callValue(PathApi.expandQuadraticBounds__getPathBounds, cast ([x, y, cx, cy, ax, ay, expand] : Array<Dynamic>));
  (x = cast (ax : Dynamic));
  (y = cast (ay : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CUBIC_CURVE_TO))) {
  var c1x:Dynamic = FlightRuntime.getIndex(data, di);
  var c1y:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  var c2x:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var c2y:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  var ax:Dynamic = FlightRuntime.getIndex(data, (di + 4.0));
  var ay:Dynamic = FlightRuntime.getIndex(data, (di + 5.0));
  (di = cast ((di + 6.0) : Dynamic));
  FlightRuntime.callValue(PathApi.expandCubicBounds__getPathBounds, cast ([x, y, c1x, c1y, c2x, c2y, ax, ay, expand] : Array<Dynamic>));
  (x = cast (ax : Dynamic));
  (y = cast (ay : Dynamic));
} } } } } }
        ci++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(minX, HxMath.POSITIVE_INFINITY))) {
  FlightRuntime.setField(out, 'x', 0.0);
  FlightRuntime.setField(out, 'y', 0.0);
  FlightRuntime.setField(out, 'width', 0.0);
  FlightRuntime.setField(out, 'height', 0.0);
  return cast false;
}
    FlightRuntime.setField(out, 'x', minX);
    FlightRuntime.setField(out, 'y', minY);
    FlightRuntime.setField(out, 'width', (maxX - minX));
    FlightRuntime.setField(out, 'height', (maxY - minY));
    return cast true;
    return cast null;
  }

  @:keep public static function getPathContourLengths(path:Path, tolerance:Dynamic = 0.25):Array<Float> {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var lengths:Array<Float> = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([path, tolerance] : Array<Dynamic>));
    lengths = cast ([] : Array<Dynamic>);
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(contours, 'length'), '<'))) {
        FlightRuntime.callProperty(lengths, 'push', cast ([FlightRuntime.callValue(PathApi.contourLength__getPathContourLengths, cast ([FlightRuntime.getIndex(contours, ci)] : Array<Dynamic>))] : Array<Dynamic>));
        ci++;
      }
    }
    return cast lengths;
    return cast null;
  }

  @:keep public static function getPathContourOrientation(path:Path, tolerance:Dynamic = 0.25):String {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var area:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([path, tolerance] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(contours, 'length'), 0.0))) { return cast 'degenerate'; }
    area = FlightRuntime.callValue(PathApi.shoelaceArea__getPathSignedArea, cast ([FlightRuntime.getIndex(contours, 0.0)] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.compare(area, 0.0, '>'))) { return cast 'ccw'; }
    if (FlightRuntime.truthy(FlightRuntime.compare(area, 0.0, '<'))) { return cast 'cw'; }
    return cast 'degenerate';
    return cast null;
  }

  @:keep public static function getPathLastPoint(path:Path):Null<Array<Float>> {
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    data = FlightRuntime.field(path, 'data');
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(data, 'length'), 2.0, '<'))) { return cast null; }
    return cast cast ([FlightRuntime.getIndex(data, (FlightRuntime.field(data, 'length') - 2.0)), FlightRuntime.getIndex(data, (FlightRuntime.field(data, 'length') - 1.0))] : Array<Dynamic>);
    return cast null;
  }

  @:keep public static function getPathLength(path:Path, tolerance:Dynamic = 0.25):Float {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var total:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([path, tolerance] : Array<Dynamic>));
    total = 0.0;
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(contours, 'length'), '<'))) {
        (total = cast ((total + FlightRuntime.callValue(PathApi.contourLength__getPathLength, cast ([FlightRuntime.getIndex(contours, ci)] : Array<Dynamic>))) : Dynamic));
        ci++;
      }
    }
    return cast total;
    return cast null;
  }

  @:keep public static function getPathNearestPoint(path:Path, px:Float, py:Float, out:Vector2Like, tolerance:Dynamic = 0.25):Float {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var bestDistSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var bestX:Dynamic = cast FlightRuntime.UNDEFINED;
    var bestY:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([path, tolerance] : Array<Dynamic>));
    bestDistSq = HxMath.POSITIVE_INFINITY;
    bestX = 0.0;
    bestY = 0.0;
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(contours, 'length'), '<'))) {
        var contour:Dynamic = FlightRuntime.getIndex(contours, ci);
        {
          var i:Dynamic = 2.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(contour, 'length'), '<'))) {
            var ax:Dynamic = FlightRuntime.getIndex(contour, (i - 2.0));
            var ay:Dynamic = FlightRuntime.getIndex(contour, (i - 1.0));
            var bx:Dynamic = FlightRuntime.getIndex(contour, i);
            var by:Dynamic = FlightRuntime.getIndex(contour, (i + 1.0));
            var dx:Dynamic = (bx - ax);
            var dy:Dynamic = (by - ay);
            var lenSq:Dynamic = ((dx * dx) + (dy * dy));
            var t:Float = cast FlightRuntime.UNDEFINED;
            if (FlightRuntime.truthy(FlightRuntime.strictEquals(lenSq, 0.0))) {
  (t = cast (0.0 : Dynamic));
} else {
  (t = cast (((((px - ax) * dx) + ((py - ay) * dy)) / lenSq) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.compare(t, 0.0, '<'))) { (t = cast (0.0 : Dynamic)); } else { if (FlightRuntime.truthy(FlightRuntime.compare(t, 1.0, '>'))) { (t = cast (1.0 : Dynamic)); } }
}
            var cx:Dynamic = (ax + (t * dx));
            var cy:Dynamic = (ay + (t * dy));
            var distSq:Dynamic = (((px - cx) * (px - cx)) + ((py - cy) * (py - cy)));
            if (FlightRuntime.truthy(FlightRuntime.compare(distSq, bestDistSq, '<'))) {
  (bestDistSq = cast (distSq : Dynamic));
  (bestX = cast (cx : Dynamic));
  (bestY = cast (cy : Dynamic));
}
            (i = cast ((i + 2.0) : Dynamic));
          }
        }
        ci++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(bestDistSq, HxMath.POSITIVE_INFINITY))) { return cast -1.0; }
    FlightRuntime.setField(out, 'x', bestX);
    FlightRuntime.setField(out, 'y', bestY);
    return cast FlightRuntime.callProperty(HxMath, 'sqrt', cast ([bestDistSq] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getPathPointAtDistance(path:Path, distance:Float, out:Vector2Like, tolerance:Dynamic = 0.25):Bool {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([path, tolerance] : Array<Dynamic>));
    return cast FlightRuntime.callValue(PathApi.samplePathPoint__getPathPointAtDistance, cast ([contours, distance, out] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getPathPositionAtDistance(path:Path, distance:Float, pointOut:Vector2Like, tangentOut:Vector2Like, tolerance:Dynamic = 0.25):Bool {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var hasPoint:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([path, tolerance] : Array<Dynamic>));
    hasPoint = FlightRuntime.callValue(PathApi.samplePathPoint__getPathPointAtDistance, cast ([contours, distance, pointOut] : Array<Dynamic>));
    FlightRuntime.callValue(PathApi.samplePathTangent__getPathPointAtDistance, cast ([contours, distance, tangentOut] : Array<Dynamic>));
    return cast hasPoint;
    return cast null;
  }

  @:keep public static function getPathSegmentPointAtParameter(path:Path, segmentIndex:Float, t:Float, out:Vector2Like):Bool {
    return cast FlightRuntime.callValue(PathApi.walkPathSegment__getPathSegmentAtParameter, cast ([path, segmentIndex, t, out, false] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getPathSegmentTangentAtParameter(path:Path, segmentIndex:Float, t:Float, out:Vector2Like):Bool {
    return cast FlightRuntime.callValue(PathApi.walkPathSegment__getPathSegmentAtParameter, cast ([path, segmentIndex, t, out, true] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getPathSignedArea(path:Path, tolerance:Dynamic = 0.25):Float {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var total:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([path, tolerance] : Array<Dynamic>));
    total = 0.0;
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(contours, 'length'), '<'))) {
        (total = cast ((total + FlightRuntime.callValue(PathApi.shoelaceArea__getPathSignedArea, cast ([FlightRuntime.getIndex(contours, ci)] : Array<Dynamic>))) : Dynamic));
        ci++;
      }
    }
    return cast total;
    return cast null;
  }

  @:keep public static function getPathTangentAtDistance(path:Path, distance:Float, out:Vector2Like, tolerance:Dynamic = 0.25):Bool {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([path, tolerance] : Array<Dynamic>));
    return cast FlightRuntime.callValue(PathApi.samplePathTangent__getPathPointAtDistance, cast ([contours, distance, out] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function getQuadraticBezierPoint(x0:Float, y0:Float, cx:Float, cy:Float, x1:Float, y1:Float, t:Float, out:Vector2Like):Vector2Like {
    var u:Dynamic = cast FlightRuntime.UNDEFINED;
    u = (1.0 - t);
    FlightRuntime.setField(out, 'x', ((((u * u) * x0) + (((2.0 * u) * t) * cx)) + ((t * t) * x1)));
    FlightRuntime.setField(out, 'y', ((((u * u) * y0) + (((2.0 * u) * t) * cy)) + ((t * t) * y1)));
    return cast out;
    return cast null;
  }

  @:keep public static function getQuadraticBezierTangent(x0:Float, y0:Float, cx:Float, cy:Float, x1:Float, y1:Float, t:Float, out:Vector2Like):Vector2Like {
    var u:Dynamic = cast FlightRuntime.UNDEFINED;
    u = (1.0 - t);
    FlightRuntime.setField(out, 'x', (2.0 * ((u * (cx - x0)) + (t * (x1 - cx)))));
    FlightRuntime.setField(out, 'y', (2.0 * ((u * (cy - y0)) + (t * (y1 - cy)))));
    return cast out;
    return cast null;
  }

  @:keep public static function isEar__tessellatePath(contour:Array<Float>, ring:Array<Float>, a:Float, b:Float, c:Float):Bool {
    var ax:Dynamic = cast FlightRuntime.UNDEFINED;
    var ay:Dynamic = cast FlightRuntime.UNDEFINED;
    var bx:Dynamic = cast FlightRuntime.UNDEFINED;
    var by:Dynamic = cast FlightRuntime.UNDEFINED;
    var cx:Dynamic = cast FlightRuntime.UNDEFINED;
    var cy:Dynamic = cast FlightRuntime.UNDEFINED;
    ax = FlightRuntime.getIndex(contour, (a * 2.0));
    ay = FlightRuntime.getIndex(contour, ((a * 2.0) + 1.0));
    bx = FlightRuntime.getIndex(contour, (b * 2.0));
    by = FlightRuntime.getIndex(contour, ((b * 2.0) + 1.0));
    cx = FlightRuntime.getIndex(contour, (c * 2.0));
    cy = FlightRuntime.getIndex(contour, ((c * 2.0) + 1.0));
    if (FlightRuntime.truthy(FlightRuntime.compare((((bx - ax) * (cy - by)) - ((by - ay) * (cx - bx))), 0.0, '<='))) { return cast false; }
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(ring, 'length'), '<'))) {
        var p:Dynamic = FlightRuntime.getIndex(ring, i);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(p, a), function():Dynamic return cast FlightRuntime.strictEquals(p, b)), function():Dynamic return cast FlightRuntime.strictEquals(p, c)))) { i++; continue; }
        if (FlightRuntime.truthy(FlightRuntime.callValue(PathApi.isPointInTriangle__tessellatePath, cast ([FlightRuntime.getIndex(contour, (p * 2.0)), FlightRuntime.getIndex(contour, ((p * 2.0) + 1.0)), ax, ay, bx, by, cx, cy] : Array<Dynamic>)))) { return cast false; }
        i++;
      }
    }
    return cast true;
    return cast null;
  }

  @:keep public static function isMiddleRemovable__cleanPath(kept:Array<Float>, prev:Float, mid:Float, next:Float, toleranceSq:Float):Bool {
    var px:Dynamic = cast FlightRuntime.UNDEFINED;
    var py:Dynamic = cast FlightRuntime.UNDEFINED;
    var mx:Dynamic = cast FlightRuntime.UNDEFINED;
    var my:Dynamic = cast FlightRuntime.UNDEFINED;
    var sx:Dynamic = cast FlightRuntime.UNDEFINED;
    var sy:Dynamic = cast FlightRuntime.UNDEFINED;
    px = FlightRuntime.getIndex(kept, (prev * 2.0));
    py = FlightRuntime.getIndex(kept, ((prev * 2.0) + 1.0));
    mx = FlightRuntime.getIndex(kept, (mid * 2.0));
    my = FlightRuntime.getIndex(kept, ((mid * 2.0) + 1.0));
    sx = FlightRuntime.getIndex(kept, (next * 2.0));
    sy = FlightRuntime.getIndex(kept, ((next * 2.0) + 1.0));
    return cast FlightRuntime.callValue(PathApi.isRedundantMiddle__cleanPath, cast ([px, py, mx, my, sx, sy, toleranceSq] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function isPointInTriangle__tessellatePath(px:Float, py:Float, ax:Float, ay:Float, bx:Float, by:Float, cx:Float, cy:Float):Bool {
    var d1:Dynamic = cast FlightRuntime.UNDEFINED;
    var d2:Dynamic = cast FlightRuntime.UNDEFINED;
    var d3:Dynamic = cast FlightRuntime.UNDEFINED;
    var hasNegative:Dynamic = cast FlightRuntime.UNDEFINED;
    var hasPositive:Dynamic = cast FlightRuntime.UNDEFINED;
    d1 = (((px - bx) * (ay - by)) - ((ax - bx) * (py - by)));
    d2 = (((px - cx) * (by - cy)) - ((bx - cx) * (py - cy)));
    d3 = (((px - ax) * (cy - ay)) - ((cx - ax) * (py - ay)));
    hasNegative = FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.compare(d1, 0.0, '<'), function():Dynamic return cast FlightRuntime.compare(d2, 0.0, '<')), function():Dynamic return cast FlightRuntime.compare(d3, 0.0, '<'));
    hasPositive = FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.compare(d1, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(d2, 0.0, '>')), function():Dynamic return cast FlightRuntime.compare(d3, 0.0, '>'));
    return cast !FlightRuntime.truthy(FlightRuntime.andValue(hasNegative, function():Dynamic return cast hasPositive));
    return cast null;
  }

  @:keep public static function isRedundantMiddle__cleanPath(px:Float, py:Float, mx:Float, my:Float, sx:Float, sy:Float, toleranceSq:Float):Bool {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var lengthSq:Dynamic = cast FlightRuntime.UNDEFINED;
    var cross:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (sx - px);
    dy = (sy - py);
    lengthSq = ((dx * dx) + (dy * dy));
    if (FlightRuntime.truthy(FlightRuntime.compare(lengthSq, toleranceSq, '<='))) { return cast true; }
    cross = ((dx * (py - my)) - (dy * (px - mx)));
    return cast FlightRuntime.compare(((cross * cross) / lengthSq), toleranceSq, '<=');
    return cast null;
  }

  public static final KAPPA__path:Dynamic = 0.5522847498308936;

  public static final MAX_SUBDIVISION_DEPTH__containsPathPoint:Dynamic = 16.0;

  public static final MAX_SUBDIVISION_DEPTH__flattenPath:Dynamic = 16.0;

  public static final MAX_SUBDIVISION_DEPTH__strokePath:Dynamic = 16.0;

  @:keep public static function newtonRaphsonRootFind__fitPathCurves(bezier:Array<Float>, px:Float, py:Float, u:Float):Float {
    var mt:Dynamic = cast FlightRuntime.UNDEFINED;
    var qx:Dynamic = cast FlightRuntime.UNDEFINED;
    var qy:Dynamic = cast FlightRuntime.UNDEFINED;
    var q1x:Dynamic = cast FlightRuntime.UNDEFINED;
    var q1y:Dynamic = cast FlightRuntime.UNDEFINED;
    var num:Dynamic = cast FlightRuntime.UNDEFINED;
    var q2x:Dynamic = cast FlightRuntime.UNDEFINED;
    var q2y:Dynamic = cast FlightRuntime.UNDEFINED;
    var den:Dynamic = cast FlightRuntime.UNDEFINED;
    mt = (1.0 - u);
    qx = ((((((mt * mt) * mt) * FlightRuntime.getIndex(bezier, 0.0)) + ((((3.0 * mt) * mt) * u) * FlightRuntime.getIndex(bezier, 2.0))) + ((((3.0 * mt) * u) * u) * FlightRuntime.getIndex(bezier, 4.0))) + (((u * u) * u) * FlightRuntime.getIndex(bezier, 6.0)));
    qy = ((((((mt * mt) * mt) * FlightRuntime.getIndex(bezier, 1.0)) + ((((3.0 * mt) * mt) * u) * FlightRuntime.getIndex(bezier, 3.0))) + ((((3.0 * mt) * u) * u) * FlightRuntime.getIndex(bezier, 5.0))) + (((u * u) * u) * FlightRuntime.getIndex(bezier, 7.0)));
    q1x = (((((3.0 * mt) * mt) * (FlightRuntime.getIndex(bezier, 2.0) - FlightRuntime.getIndex(bezier, 0.0))) + (((6.0 * mt) * u) * (FlightRuntime.getIndex(bezier, 4.0) - FlightRuntime.getIndex(bezier, 2.0)))) + (((3.0 * u) * u) * (FlightRuntime.getIndex(bezier, 6.0) - FlightRuntime.getIndex(bezier, 4.0))));
    q1y = (((((3.0 * mt) * mt) * (FlightRuntime.getIndex(bezier, 3.0) - FlightRuntime.getIndex(bezier, 1.0))) + (((6.0 * mt) * u) * (FlightRuntime.getIndex(bezier, 5.0) - FlightRuntime.getIndex(bezier, 3.0)))) + (((3.0 * u) * u) * (FlightRuntime.getIndex(bezier, 7.0) - FlightRuntime.getIndex(bezier, 5.0))));
    num = (((qx - px) * q1x) + ((qy - py) * q1y));
    q2x = (((6.0 * mt) * ((FlightRuntime.getIndex(bezier, 4.0) - (2.0 * FlightRuntime.getIndex(bezier, 2.0))) + FlightRuntime.getIndex(bezier, 0.0))) + ((6.0 * u) * ((FlightRuntime.getIndex(bezier, 6.0) - (2.0 * FlightRuntime.getIndex(bezier, 4.0))) + FlightRuntime.getIndex(bezier, 2.0))));
    q2y = (((6.0 * mt) * ((FlightRuntime.getIndex(bezier, 5.0) - (2.0 * FlightRuntime.getIndex(bezier, 3.0))) + FlightRuntime.getIndex(bezier, 1.0))) + ((6.0 * u) * ((FlightRuntime.getIndex(bezier, 7.0) - (2.0 * FlightRuntime.getIndex(bezier, 5.0))) + FlightRuntime.getIndex(bezier, 3.0))));
    den = ((((q1x * q1x) + (q1y * q1y)) + ((qx - px) * q2x)) + ((qy - py) * q2y));
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([den] : Array<Dynamic>)), 1e-12, '<'))) { return cast u; }
    return cast FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, (u - (num / den))] : Array<Dynamic>))] : Array<Dynamic>));
    return cast null;
  }

  @:keep public static function normalizeCornerRadii__path(radius:Dynamic, width:Float, height:Float):Array<Float> {
    var __destructure1:Dynamic = cast FlightRuntime.UNDEFINED;
    var rtl:Dynamic = cast FlightRuntime.UNDEFINED;
    var rtr:Dynamic = cast FlightRuntime.UNDEFINED;
    var rbr:Dynamic = cast FlightRuntime.UNDEFINED;
    var rbl:Dynamic = cast FlightRuntime.UNDEFINED;
    var halfW:Dynamic = cast FlightRuntime.UNDEFINED;
    var halfH:Dynamic = cast FlightRuntime.UNDEFINED;
    var clampTL:Dynamic = cast FlightRuntime.UNDEFINED;
    var clampTR:Dynamic = cast FlightRuntime.UNDEFINED;
    var clampBR:Dynamic = cast FlightRuntime.UNDEFINED;
    var clampBL:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure1 = FlightRuntime.select(FlightRuntime.strictEquals(FlightRuntime.typeofValue(radius), 'number'), function():Dynamic return cast cast ([radius, radius, radius, radius] : Array<Dynamic>), function():Dynamic return cast radius);
    rtl = FlightRuntime.getIndex(__destructure1, 0.0);
    rtr = FlightRuntime.getIndex(__destructure1, 1.0);
    rbr = FlightRuntime.getIndex(__destructure1, 2.0);
    rbl = FlightRuntime.getIndex(__destructure1, 3.0);
    halfW = (FlightRuntime.callProperty(HxMath, 'abs', cast ([width] : Array<Dynamic>)) / 2.0);
    halfH = (FlightRuntime.callProperty(HxMath, 'abs', cast ([height] : Array<Dynamic>)) / 2.0);
    clampTL = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, HxMath.min(HxMath.min(rtl, halfW), halfH)] : Array<Dynamic>));
    clampTR = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, HxMath.min(HxMath.min(rtr, halfW), halfH)] : Array<Dynamic>));
    clampBR = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, HxMath.min(HxMath.min(rbr, halfW), halfH)] : Array<Dynamic>));
    clampBL = FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, HxMath.min(HxMath.min(rbl, halfW), halfH)] : Array<Dynamic>));
    return cast cast ([clampTL, clampTR, clampBR, clampBL] : Array<Dynamic>);
    return cast null;
  }

  public static final pathMeshPool__pathMeshPool:Array<PathMesh> = cast ([] : Array<Dynamic>);

  public static final POOL_HIGH_WATER__pathMeshPool:Dynamic = 64.0;

  @:keep public static function pushCleanVertex__cleanPath(kept:Array<Float>, x:Float, y:Float, toleranceSq:Float):Void {
    var k:Dynamic = cast FlightRuntime.UNDEFINED;
    k = FlightRuntime.field(kept, 'length');
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(k, 2.0, '>='), function():Dynamic return cast FlightRuntime.callValue(PathApi.withinTolerance__cleanPath, cast ([FlightRuntime.getIndex(kept, (k - 2.0)), FlightRuntime.getIndex(kept, (k - 1.0)), x, y, toleranceSq] : Array<Dynamic>))))) { return; }
    while (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(kept, 'length'), 4.0, '>='))) {
  var m:Dynamic = FlightRuntime.field(kept, 'length');
  if (FlightRuntime.truthy(!FlightRuntime.truthy(FlightRuntime.callValue(PathApi.isRedundantMiddle__cleanPath, cast ([FlightRuntime.getIndex(kept, (m - 4.0)), FlightRuntime.getIndex(kept, (m - 3.0)), FlightRuntime.getIndex(kept, (m - 2.0)), FlightRuntime.getIndex(kept, (m - 1.0)), x, y, toleranceSq] : Array<Dynamic>))))) { break; }
  FlightRuntime.setLength(kept, (kept.length - 2.0));
}
    FlightRuntime.pushMany(kept, cast ([x, y] : Array<Dynamic>));
  }

  @:keep public static function quadraticExtremumT__getPathBounds(p0:Float, p1:Float, p2:Float):Null<Float> {
    var denom:Dynamic = cast FlightRuntime.UNDEFINED;
    var t:Dynamic = cast FlightRuntime.UNDEFINED;
    denom = ((p0 - (2.0 * p1)) + p2);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(denom, 0.0))) { return cast null; }
    t = ((p0 - p1) / denom);
    return cast FlightRuntime.select(FlightRuntime.andValue(FlightRuntime.compare(t, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(t, 1.0, '<')), function():Dynamic return cast t, function():Dynamic return cast null);
    return cast null;
  }

  @:keep public static function releasePathMesh(mesh:PathMesh):Void {
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(PathApi.pathMeshPool__pathMeshPool, 'length'), PathApi.POOL_HIGH_WATER__pathMeshPool, '<'))) {
  FlightRuntime.callProperty(PathApi.pathMeshPool__pathMeshPool, 'push', cast ([mesh] : Array<Dynamic>));
}
  }

  @:keep public static function releasePathMeshTyped(mesh:PathMeshTyped):Void {
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(PathApi.typedPool__pathMeshPool, 'length'), PathApi.POOL_HIGH_WATER__pathMeshPool, '<'))) {
  FlightRuntime.callProperty(PathApi.typedPool__pathMeshPool, 'push', cast ([mesh] : Array<Dynamic>));
}
  }

  @:keep public static function reparameterize__fitPathCurves(pts:Array<Float>, first:Float, last:Float, u:Array<Float>, bezier:Array<Float>):Array<Float> {
    var uPrime:Array<Float> = cast FlightRuntime.UNDEFINED;
    uPrime = cast ([] : Array<Dynamic>);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, (last - first), '<='))) {
        FlightRuntime.callProperty(uPrime, 'push', cast ([FlightRuntime.callValue(PathApi.newtonRaphsonRootFind__fitPathCurves, cast ([bezier, FlightRuntime.getIndex(pts, ((first + i) * 2.0)), FlightRuntime.getIndex(pts, (((first + i) * 2.0) + 1.0)), FlightRuntime.getIndex(u, i)] : Array<Dynamic>))] : Array<Dynamic>));
        i++;
      }
    }
    return cast uPrime;
    return cast null;
  }

  @:keep public static function reversePath(source:Path, out:Path):Void {
    var subpaths:Dynamic = cast FlightRuntime.UNDEFINED;
    subpaths = FlightRuntime.callValue(PathApi.decodeSubpaths__reversePath, cast ([source] : Array<Dynamic>));
    FlightRuntime.setLength(FlightRuntime.field(out, 'commands'), 0.0);
    FlightRuntime.setLength(FlightRuntime.field(out, 'data'), 0.0);
    FlightRuntime.setField(out, 'winding', FlightRuntime.field(source, 'winding'));
    for (subpath in FlightRuntime.iterable(subpaths)) {
      FlightRuntime.callValue(PathApi.encodeReversedSubpath__reversePath, cast ([subpath, out] : Array<Dynamic>));
    }
  }

  @:keep public static function samplePathPoint__getPathPointAtDistance(contours:Array<Array<Float>>, distance:Float, out:Vector2Like):Bool {
    var remaining:Dynamic = cast FlightRuntime.UNDEFINED;
    var last:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(contours, 'length'), 0.0))) { return cast false; }
    remaining = distance;
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(contours, 'length'), '<'))) {
        var contour:Dynamic = FlightRuntime.getIndex(contours, ci);
        if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(contour, 'length'), 2.0, '<'))) { ci++; continue; }
        if (FlightRuntime.truthy(FlightRuntime.compare(remaining, 0.0, '<='))) {
  FlightRuntime.setField(out, 'x', FlightRuntime.getIndex(contour, 0.0));
  FlightRuntime.setField(out, 'y', FlightRuntime.getIndex(contour, 1.0));
  return cast true;
}
        {
          var i:Dynamic = 2.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(contour, 'length'), '<'))) {
            var dx:Dynamic = (FlightRuntime.getIndex(contour, i) - FlightRuntime.getIndex(contour, (i - 2.0)));
            var dy:Dynamic = (FlightRuntime.getIndex(contour, (i + 1.0)) - FlightRuntime.getIndex(contour, (i - 1.0)));
            var segLen:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>));
            if (FlightRuntime.truthy(FlightRuntime.compare(remaining, segLen, '<='))) {
  var t:Dynamic = FlightRuntime.select(FlightRuntime.compare(segLen, 0.0, '>'), function():Dynamic return cast (remaining / segLen), function():Dynamic return cast 0.0);
  FlightRuntime.setField(out, 'x', (FlightRuntime.getIndex(contour, (i - 2.0)) + (t * dx)));
  FlightRuntime.setField(out, 'y', (FlightRuntime.getIndex(contour, (i - 1.0)) + (t * dy)));
  return cast true;
}
            (remaining = cast ((remaining - segLen) : Dynamic));
            (i = cast ((i + 2.0) : Dynamic));
          }
        }
        ci++;
      }
    }
    last = FlightRuntime.getIndex(contours, (FlightRuntime.field(contours, 'length') - 1.0));
    FlightRuntime.setField(out, 'x', FlightRuntime.getIndex(last, (FlightRuntime.field(last, 'length') - 2.0)));
    FlightRuntime.setField(out, 'y', FlightRuntime.getIndex(last, (FlightRuntime.field(last, 'length') - 1.0)));
    return cast true;
    return cast null;
  }

  @:keep public static function samplePathTangent__getPathPointAtDistance(contours:Array<Array<Float>>, distance:Float, out:Vector2Like):Bool {
    var remaining:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastTx:Dynamic = cast FlightRuntime.UNDEFINED;
    var lastTy:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(contours, 'length'), 0.0))) {
  FlightRuntime.setField(out, 'x', 1.0);
  FlightRuntime.setField(out, 'y', 0.0);
  return cast false;
}
    remaining = distance;
    lastTx = 1.0;
    lastTy = 0.0;
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(contours, 'length'), '<'))) {
        var contour:Dynamic = FlightRuntime.getIndex(contours, ci);
        if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(contour, 'length'), 4.0, '<'))) { ci++; continue; }
        {
          var i:Dynamic = 2.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(contour, 'length'), '<'))) {
            var dx:Dynamic = (FlightRuntime.getIndex(contour, i) - FlightRuntime.getIndex(contour, (i - 2.0)));
            var dy:Dynamic = (FlightRuntime.getIndex(contour, (i + 1.0)) - FlightRuntime.getIndex(contour, (i - 1.0)));
            var segLen:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>));
            if (FlightRuntime.truthy(FlightRuntime.compare(segLen, 0.0, '>'))) {
  var invLen:Dynamic = (1.0 / segLen);
  (lastTx = cast ((dx * invLen) : Dynamic));
  (lastTy = cast ((dy * invLen) : Dynamic));
}
            if (FlightRuntime.truthy(FlightRuntime.compare(remaining, segLen, '<='))) {
  FlightRuntime.setField(out, 'x', lastTx);
  FlightRuntime.setField(out, 'y', lastTy);
  return cast true;
}
            (remaining = cast ((remaining - segLen) : Dynamic));
            (i = cast ((i + 2.0) : Dynamic));
          }
        }
        ci++;
      }
    }
    FlightRuntime.setField(out, 'x', lastTx);
    FlightRuntime.setField(out, 'y', lastTy);
    return cast true;
    return cast null;
  }

  @:keep public static function shoelaceArea__getPathSignedArea(contour:Array<Float>):Float {
    var n:Dynamic = cast FlightRuntime.UNDEFINED;
    var area:Dynamic = cast FlightRuntime.UNDEFINED;
    n = (Std.int(FlightRuntime.field(contour, 'length')) >> Std.int(1.0));
    if (FlightRuntime.truthy(FlightRuntime.compare(n, 3.0, '<'))) { return cast 0.0; }
    area = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, n, '<'))) {
        var j:Dynamic = ((i + 1.0) % n);
        (area = cast ((area + (FlightRuntime.getIndex(contour, (i * 2.0)) * FlightRuntime.getIndex(contour, ((j * 2.0) + 1.0)))) : Dynamic));
        (area = cast ((area - (FlightRuntime.getIndex(contour, (j * 2.0)) * FlightRuntime.getIndex(contour, ((i * 2.0) + 1.0)))) : Dynamic));
        i++;
      }
    }
    return cast (area / 2.0);
    return cast null;
  }

  @:keep public static function strokePath(path:Path, style:StrokeStyle, tolerance:Dynamic = 0.25):Path {
    var width:Dynamic = cast FlightRuntime.UNDEFINED;
    var join:Dynamic = cast FlightRuntime.UNDEFINED;
    var cap:Dynamic = cast FlightRuntime.UNDEFINED;
    var miterLimit:Dynamic = cast FlightRuntime.UNDEFINED;
    var halfWidth:Dynamic = cast FlightRuntime.UNDEFINED;
    var result:Path = cast FlightRuntime.UNDEFINED;
    var subpaths:Dynamic = cast FlightRuntime.UNDEFINED;
    var dash:Dynamic = cast FlightRuntime.UNDEFINED;
    var dashOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    width = FlightRuntime.coalesce(FlightRuntime.field(style, 'width'), function():Dynamic return cast 1.0);
    join = FlightRuntime.coalesce(FlightRuntime.field(style, 'join'), function():Dynamic return cast 'miter');
    cap = FlightRuntime.coalesce(FlightRuntime.field(style, 'cap'), function():Dynamic return cast 'butt');
    miterLimit = FlightRuntime.coalesce(FlightRuntime.field(style, 'miterLimit'), function():Dynamic return cast 4.0);
    halfWidth = (width / 2.0);
    result = { commands: cast ([] : Array<Dynamic>), data: cast ([] : Array<Dynamic>), winding: 'nonZero' };
    subpaths = FlightRuntime.callValue(PathApi.decodeSubpaths__strokePath, cast ([path, tolerance] : Array<Dynamic>));
    dash = FlightRuntime.select(FlightRuntime.andValue(FlightRuntime.field(style, 'dash'), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(style, 'dash'), 'length'), 0.0, '>')), function():Dynamic return cast FlightRuntime.field(style, 'dash'), function():Dynamic return cast null);
    dashOffset = FlightRuntime.coalesce(FlightRuntime.field(style, 'dashOffset'), function():Dynamic return cast 0.0);
    for (subpath in FlightRuntime.iterable(subpaths)) {
      if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(subpath, 'points'), 'length'), 2.0, '<'))) { continue; }
      var segments:Dynamic = FlightRuntime.select(dash, function():Dynamic return cast FlightRuntime.callValue(PathApi.applyDash__strokePath, cast ([FlightRuntime.field(subpath, 'points'), FlightRuntime.field(subpath, 'closed'), dash, dashOffset] : Array<Dynamic>)), function():Dynamic return cast cast ([{ points: FlightRuntime.field(subpath, 'points'), closed: FlightRuntime.field(subpath, 'closed') }] : Array<Dynamic>));
      for (seg in FlightRuntime.iterable(segments)) {
        if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(seg, 'points'), 'length'), 2.0, '<'))) { continue; }
        FlightRuntime.callValue(PathApi.strokeSubpath__strokePath, cast ([FlightRuntime.field(seg, 'points'), FlightRuntime.field(seg, 'closed'), halfWidth, join, cap, miterLimit, result, tolerance] : Array<Dynamic>));
      }
    }
    return cast result;
    return cast null;
  }

  @:keep public static function strokeSubpath__strokePath(pts:Array<Float>, closed:Bool, halfWidth:Float, join:String, cap:String, miterLimit:Float, out:Path, tolerance:Float):Void {
    var n:Dynamic = cast FlightRuntime.UNDEFINED;
    var left:Array<Float> = cast FlightRuntime.UNDEFINED;
    var right:Array<Float> = cast FlightRuntime.UNDEFINED;
    var normals:Dynamic = cast FlightRuntime.UNDEFINED;
    n = (Std.int(FlightRuntime.field(pts, 'length')) >> Std.int(1.0));
    if (FlightRuntime.truthy(FlightRuntime.compare(n, 2.0, '<'))) { return; }
    left = cast ([] : Array<Dynamic>);
    right = cast ([] : Array<Dynamic>);
    normals = FlightRuntime.createArray(((n - 1.0) * 2.0));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, (n - 1.0), '<'))) {
        var dx:Dynamic = (FlightRuntime.getIndex(pts, ((i + 1.0) * 2.0)) - FlightRuntime.getIndex(pts, (i * 2.0)));
        var dy:Dynamic = (FlightRuntime.getIndex(pts, (((i + 1.0) * 2.0) + 1.0)) - FlightRuntime.getIndex(pts, ((i * 2.0) + 1.0)));
        var len:Dynamic = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((dx * dx) + (dy * dy))] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.compare(len, 0.0, '>'))) {
  FlightRuntime.setIndex(normals, (i * 2.0), (-dy / len));
  FlightRuntime.setIndex(normals, ((i * 2.0) + 1.0), (dx / len));
} else {
  FlightRuntime.setIndex(normals, (i * 2.0), FlightRuntime.select(FlightRuntime.compare(i, 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(normals, ((i - 1.0) * 2.0)), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(normals, ((i * 2.0) + 1.0), FlightRuntime.select(FlightRuntime.compare(i, 0.0, '>'), function():Dynamic return cast FlightRuntime.getIndex(normals, (((i - 1.0) * 2.0) + 1.0)), function():Dynamic return cast 1.0));
}
        i++;
      }
    }
    if (FlightRuntime.truthy(closed)) {
  {
    var i:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, (n - 1.0), '<'))) {
      var prev:Dynamic = (((i + n) - 2.0) % (n - 1.0));
      var curr:Dynamic = i;
      var nx0:Dynamic = FlightRuntime.getIndex(normals, (prev * 2.0));
      var ny0:Dynamic = FlightRuntime.getIndex(normals, ((prev * 2.0) + 1.0));
      var nx1:Dynamic = FlightRuntime.getIndex(normals, (curr * 2.0));
      var ny1:Dynamic = FlightRuntime.getIndex(normals, ((curr * 2.0) + 1.0));
      FlightRuntime.callValue(PathApi.addJoin__strokePath, cast ([FlightRuntime.getIndex(pts, (i * 2.0)), FlightRuntime.getIndex(pts, ((i * 2.0) + 1.0)), nx0, ny0, nx1, ny1, halfWidth, join, miterLimit, left, right, tolerance] : Array<Dynamic>));
      i++;
    }
  }
} else {
  var sn0x:Dynamic = FlightRuntime.getIndex(normals, 0.0);
  var sn0y:Dynamic = FlightRuntime.getIndex(normals, 1.0);
  FlightRuntime.callValue(PathApi.addCap__strokePath, cast ([FlightRuntime.getIndex(pts, 0.0), FlightRuntime.getIndex(pts, 1.0), sn0x, sn0y, -sn0y, sn0x, halfWidth, cap, left, right, tolerance, true] : Array<Dynamic>));
  {
    var i:Dynamic = 1.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, (n - 1.0), '<'))) {
      var nx0:Dynamic = FlightRuntime.getIndex(normals, ((i - 1.0) * 2.0));
      var ny0:Dynamic = FlightRuntime.getIndex(normals, (((i - 1.0) * 2.0) + 1.0));
      var nx1:Dynamic = FlightRuntime.getIndex(normals, (i * 2.0));
      var ny1:Dynamic = FlightRuntime.getIndex(normals, ((i * 2.0) + 1.0));
      FlightRuntime.callValue(PathApi.addJoin__strokePath, cast ([FlightRuntime.getIndex(pts, (i * 2.0)), FlightRuntime.getIndex(pts, ((i * 2.0) + 1.0)), nx0, ny0, nx1, ny1, halfWidth, join, miterLimit, left, right, tolerance] : Array<Dynamic>));
      i++;
    }
  }
  var snLx:Dynamic = FlightRuntime.getIndex(normals, ((n - 2.0) * 2.0));
  var snLy:Dynamic = FlightRuntime.getIndex(normals, (((n - 2.0) * 2.0) + 1.0));
  FlightRuntime.callValue(PathApi.addCap__strokePath, cast ([FlightRuntime.getIndex(pts, ((n - 1.0) * 2.0)), FlightRuntime.getIndex(pts, (((n - 1.0) * 2.0) + 1.0)), snLx, snLy, snLy, -snLx, halfWidth, cap, left, right, tolerance, false] : Array<Dynamic>));
}
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(left, 'length'), 4.0, '<'))) { return; }
    FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.MOVE_TO] : Array<Dynamic>));
    FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(left, 0.0), FlightRuntime.getIndex(left, 1.0)] : Array<Dynamic>));
    {
      var i:Dynamic = 2.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(left, 'length'), '<'))) {
        FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.LINE_TO] : Array<Dynamic>));
        FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(left, i), FlightRuntime.getIndex(left, (i + 1.0))] : Array<Dynamic>));
        (i = cast ((i + 2.0) : Dynamic));
      }
    }
    {
      var i:Dynamic = (FlightRuntime.field(right, 'length') - 2.0);
      while (FlightRuntime.truthy(FlightRuntime.compare(i, 0.0, '>='))) {
        FlightRuntime.callProperty(FlightRuntime.field(out, 'commands'), 'push', cast ([Types.PathCommandValue.LINE_TO] : Array<Dynamic>));
        FlightRuntime.pushMany(FlightRuntime.field(out, 'data'), cast ([FlightRuntime.getIndex(right, i), FlightRuntime.getIndex(right, (i + 1.0))] : Array<Dynamic>));
        (i = cast ((i - 2.0) : Dynamic));
      }
    }
    FlightRuntime.callValue(appendPathClose, cast ([out] : Array<Dynamic>));
  }

  @:keep public static function tessellateContour__tessellatePath(source:Array<Float>, vertices:Array<Float>, indices:Array<Float>):Void {
    var pts:Array<Float> = cast FlightRuntime.UNDEFINED;
    var contour:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var base:Dynamic = cast FlightRuntime.UNDEFINED;
    var twiceArea:Dynamic = cast FlightRuntime.UNDEFINED;
    var ring:Array<Float> = cast FlightRuntime.UNDEFINED;
    var guard:Dynamic = cast FlightRuntime.UNDEFINED;
    pts = cast ([] : Array<Dynamic>);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(source, 'length'), '<'))) {
        var x:Dynamic = FlightRuntime.getIndex(source, i);
        var y:Dynamic = FlightRuntime.getIndex(source, (i + 1.0));
        if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(FlightRuntime.field(pts, 'length'), 2.0, '>='), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(pts, (FlightRuntime.field(pts, 'length') - 2.0)), x)), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(pts, (FlightRuntime.field(pts, 'length') - 1.0)), y)))) { (i = cast ((i + 2.0) : Dynamic)); continue; }
        FlightRuntime.pushMany(pts, cast ([x, y] : Array<Dynamic>));
        (i = cast ((i + 2.0) : Dynamic));
      }
    }
    contour = pts;
    count = (Std.int(FlightRuntime.field(contour, 'length')) >> Std.int(1.0));
    if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(count, 2.0, '>='), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(contour, 0.0), FlightRuntime.getIndex(contour, ((count - 1.0) * 2.0)))), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.getIndex(contour, 1.0), FlightRuntime.getIndex(contour, (((count - 1.0) * 2.0) + 1.0)))))) {
  (count = cast ((count - 1.0) : Dynamic));
}
    if (FlightRuntime.truthy(FlightRuntime.compare(count, 3.0, '<'))) { return; }
    base = (Std.int(FlightRuntime.field(vertices, 'length')) >> Std.int(1.0));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, count, '<'))) {
        FlightRuntime.pushMany(vertices, cast ([FlightRuntime.getIndex(contour, (i * 2.0)), FlightRuntime.getIndex(contour, ((i * 2.0) + 1.0))] : Array<Dynamic>));
        i++;
      }
    }
    twiceArea = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, count, '<'))) {
        var j:Dynamic = ((i + 1.0) % count);
        (twiceArea = cast ((twiceArea + ((FlightRuntime.getIndex(contour, (i * 2.0)) * FlightRuntime.getIndex(contour, ((j * 2.0) + 1.0))) - (FlightRuntime.getIndex(contour, (j * 2.0)) * FlightRuntime.getIndex(contour, ((i * 2.0) + 1.0))))) : Dynamic));
        i++;
      }
    }
    ring = cast ([] : Array<Dynamic>);
    if (FlightRuntime.truthy(FlightRuntime.compare(twiceArea, 0.0, '<'))) {
  {
    var i:Dynamic = (count - 1.0);
    while (FlightRuntime.truthy(FlightRuntime.compare(i, 0.0, '>='))) {
      FlightRuntime.callProperty(ring, 'push', cast ([i] : Array<Dynamic>));
      i--;
    }
  }
} else {
  {
    var i:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, count, '<'))) {
      FlightRuntime.callProperty(ring, 'push', cast ([i] : Array<Dynamic>));
      i++;
    }
  }
}
    guard = (FlightRuntime.field(ring, 'length') * FlightRuntime.field(ring, 'length'));
    while (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(FlightRuntime.field(ring, 'length'), 3.0, '>'), function():Dynamic return cast FlightRuntime.compare(guard--, 0.0, '>')))) {
  var clipped:Dynamic = false;
  {
    var i:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(ring, 'length'), '<'))) {
      var a:Dynamic = FlightRuntime.getIndex(ring, (((i + FlightRuntime.field(ring, 'length')) - 1.0) % FlightRuntime.field(ring, 'length')));
      var b:Dynamic = FlightRuntime.getIndex(ring, i);
      var c:Dynamic = FlightRuntime.getIndex(ring, ((i + 1.0) % FlightRuntime.field(ring, 'length')));
      if (FlightRuntime.truthy(FlightRuntime.callValue(PathApi.isEar__tessellatePath, cast ([contour, ring, a, b, c] : Array<Dynamic>)))) {
  FlightRuntime.pushMany(indices, cast ([(base + a), (base + b), (base + c)] : Array<Dynamic>));
  FlightRuntime.splice(ring, Std.int(i), Std.int(1.0), []);
  (clipped = cast (true : Dynamic));
  break;
}
      i++;
    }
  }
  if (FlightRuntime.truthy(!FlightRuntime.truthy(clipped))) { break; }
}
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(ring, 'length'), 3.0))) {
  FlightRuntime.pushMany(indices, cast ([(base + FlightRuntime.getIndex(ring, 0.0)), (base + FlightRuntime.getIndex(ring, 1.0)), (base + FlightRuntime.getIndex(ring, 2.0))] : Array<Dynamic>));
}
  }

  @:keep public static function tessellatePath(path:Path, tolerance:Dynamic = 0.25):PathMesh {
    var contours:Dynamic = cast FlightRuntime.UNDEFINED;
    var vertices:Array<Float> = cast FlightRuntime.UNDEFINED;
    var indices:Array<Float> = cast FlightRuntime.UNDEFINED;
    contours = FlightRuntime.callValue(flattenPath, cast ([path, tolerance] : Array<Dynamic>));
    vertices = cast ([] : Array<Dynamic>);
    indices = cast ([] : Array<Dynamic>);
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(contours, 'length'), '<'))) {
        FlightRuntime.callValue(PathApi.tessellateContour__tessellatePath, cast ([FlightRuntime.getIndex(contours, i), vertices, indices] : Array<Dynamic>));
        i++;
      }
    }
    return cast { vertices: vertices, indices: indices };
    return cast null;
  }

  @:keep public static function tessellatePathTyped(path:Path, tolerance:Dynamic = 0.25):PathMeshTyped {
    var mesh:Dynamic = cast FlightRuntime.UNDEFINED;
    mesh = FlightRuntime.callValue(tessellatePath, cast ([path, tolerance] : Array<Dynamic>));
    return cast { vertices: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [FlightRuntime.field(mesh, 'vertices')]), indices: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Uint32Array'] : Array<Dynamic>)), [FlightRuntime.field(mesh, 'indices')]) };
    return cast null;
  }

  @:keep public static function transformPath(source:Path, matrix:MatrixLike, out:Path):Void {
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var c:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    var tx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ty:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var di:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure0 = matrix;
    a = FlightRuntime.field(__destructure0, 'a');
    b = FlightRuntime.field(__destructure0, 'b');
    c = FlightRuntime.field(__destructure0, 'c');
    d = FlightRuntime.field(__destructure0, 'd');
    tx = FlightRuntime.field(__destructure0, 'tx');
    ty = FlightRuntime.field(__destructure0, 'ty');
    FlightRuntime.callValue(copyPath, cast ([source, out] : Array<Dynamic>));
    data = FlightRuntime.field(out, 'data');
    commands = FlightRuntime.field(source, 'commands');
    di = 0.0;
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(commands, 'length'), '<'))) {
        var command:Dynamic = FlightRuntime.getIndex(commands, ci);
        if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(command, Types.PathCommandValue.MOVE_TO), function():Dynamic return cast FlightRuntime.strictEquals(command, Types.PathCommandValue.LINE_TO)))) {
  var x:Dynamic = FlightRuntime.getIndex(data, di);
  var y:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  FlightRuntime.setIndex(data, di, (((a * x) + (c * y)) + tx));
  FlightRuntime.setIndex(data, (di + 1.0), (((b * x) + (d * y)) + ty));
  (di = cast ((di + 2.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CURVE_TO))) {
  {
    var k:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(k, 4.0, '<'))) {
      var x:Dynamic = FlightRuntime.getIndex(data, (di + k));
      var y:Dynamic = FlightRuntime.getIndex(data, ((di + k) + 1.0));
      FlightRuntime.setIndex(data, (di + k), (((a * x) + (c * y)) + tx));
      FlightRuntime.setIndex(data, ((di + k) + 1.0), (((b * x) + (d * y)) + ty));
      (k = cast ((k + 2.0) : Dynamic));
    }
  }
  (di = cast ((di + 4.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CUBIC_CURVE_TO))) {
  {
    var k:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(k, 6.0, '<'))) {
      var x:Dynamic = FlightRuntime.getIndex(data, (di + k));
      var y:Dynamic = FlightRuntime.getIndex(data, ((di + k) + 1.0));
      FlightRuntime.setIndex(data, (di + k), (((a * x) + (c * y)) + tx));
      FlightRuntime.setIndex(data, ((di + k) + 1.0), (((b * x) + (d * y)) + ty));
      (k = cast ((k + 2.0) : Dynamic));
    }
  }
  (di = cast ((di + 6.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_MOVE_TO), function():Dynamic return cast FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_LINE_TO)))) {
  var x:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var y:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  FlightRuntime.setIndex(data, (di + 2.0), (((a * x) + (c * y)) + tx));
  FlightRuntime.setIndex(data, (di + 3.0), (((b * x) + (d * y)) + ty));
  (di = cast ((di + 4.0) : Dynamic));
} } } }
        ci++;
      }
    }
  }

  @:keep public static function translatePath(source:Path, dx:Float, dy:Float, out:Path):Void {
    FlightRuntime.callValue(transformPath, cast ([source, { a: 1.0, b: 0.0, c: 0.0, d: 1.0, tx: dx, ty: dy }, out] : Array<Dynamic>));
  }

  public static final typedPool__pathMeshPool:Array<PathMeshTyped> = cast ([] : Array<Dynamic>);

  @:keep public static function vectorAngle__path(ux:Float, uy:Float, vx:Float, vy:Float):Float {
    var dot:Dynamic = cast FlightRuntime.UNDEFINED;
    var lenU:Dynamic = cast FlightRuntime.UNDEFINED;
    var lenV:Dynamic = cast FlightRuntime.UNDEFINED;
    var cosAngle:Dynamic = cast FlightRuntime.UNDEFINED;
    var angle:Dynamic = cast FlightRuntime.UNDEFINED;
    dot = ((ux * vx) + (uy * vy));
    lenU = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((ux * ux) + (uy * uy))] : Array<Dynamic>));
    lenV = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((vx * vx) + (vy * vy))] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(lenU, 0.0), function():Dynamic return cast FlightRuntime.strictEquals(lenV, 0.0)))) { return cast 0.0; }
    cosAngle = FlightRuntime.callProperty(HxMath, 'max', cast ([-1.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, (dot / (lenU * lenV))] : Array<Dynamic>))] : Array<Dynamic>));
    angle = FlightRuntime.callProperty(HxMath, 'acos', cast ([cosAngle] : Array<Dynamic>));
    return cast FlightRuntime.select(FlightRuntime.compare(((ux * vy) - (uy * vx)), 0.0, '<'), function():Dynamic return cast -angle, function():Dynamic return cast angle);
    return cast null;
  }

  @:keep public static function walkPathSegment__getPathSegmentAtParameter(path:Path, segmentIndex:Float, t:Float, out:Vector2Like, wantTangent:Bool):Bool {
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var currentSegment:Dynamic = cast FlightRuntime.UNDEFINED;
    var x:Dynamic = cast FlightRuntime.UNDEFINED;
    var y:Dynamic = cast FlightRuntime.UNDEFINED;
    var di:Dynamic = cast FlightRuntime.UNDEFINED;
    commands = FlightRuntime.field(path, 'commands');
    data = FlightRuntime.field(path, 'data');
    currentSegment = 0.0;
    x = 0.0;
    y = 0.0;
    di = 0.0;
    {
      var ci:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(ci, FlightRuntime.field(commands, 'length'), '<'))) {
        var command:Dynamic = FlightRuntime.getIndex(commands, ci);
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.MOVE_TO))) {
  (x = cast (FlightRuntime.getIndex(data, di) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 1.0)) : Dynamic));
  (di = cast ((di + 2.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_MOVE_TO))) {
  (x = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
  (y = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
  (di = cast ((di + 4.0) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.LINE_TO))) {
  var x1:Dynamic = FlightRuntime.getIndex(data, di);
  var y1:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  (di = cast ((di + 2.0) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(currentSegment, segmentIndex))) {
  if (FlightRuntime.truthy(wantTangent)) {
  FlightRuntime.setField(out, 'x', (x1 - x));
  FlightRuntime.setField(out, 'y', (y1 - y));
} else {
  FlightRuntime.setField(out, 'x', (x + (t * (x1 - x))));
  FlightRuntime.setField(out, 'y', (y + (t * (y1 - y))));
}
  return cast true;
}
  (x = cast (x1 : Dynamic));
  (y = cast (y1 : Dynamic));
  currentSegment++;
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.WIDE_LINE_TO))) {
  var x1:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var y1:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  (di = cast ((di + 4.0) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(currentSegment, segmentIndex))) {
  if (FlightRuntime.truthy(wantTangent)) {
  FlightRuntime.setField(out, 'x', (x1 - x));
  FlightRuntime.setField(out, 'y', (y1 - y));
} else {
  FlightRuntime.setField(out, 'x', (x + (t * (x1 - x))));
  FlightRuntime.setField(out, 'y', (y + (t * (y1 - y))));
}
  return cast true;
}
  (x = cast (x1 : Dynamic));
  (y = cast (y1 : Dynamic));
  currentSegment++;
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CURVE_TO))) {
  var cx:Dynamic = FlightRuntime.getIndex(data, di);
  var cy:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  var x1:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var y1:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  (di = cast ((di + 4.0) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(currentSegment, segmentIndex))) {
  if (FlightRuntime.truthy(wantTangent)) {
  FlightRuntime.callValue(getQuadraticBezierTangent, cast ([x, y, cx, cy, x1, y1, t, out] : Array<Dynamic>));
} else {
  FlightRuntime.callValue(getQuadraticBezierPoint, cast ([x, y, cx, cy, x1, y1, t, out] : Array<Dynamic>));
}
  return cast true;
}
  (x = cast (x1 : Dynamic));
  (y = cast (y1 : Dynamic));
  currentSegment++;
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CUBIC_CURVE_TO))) {
  var c1x:Dynamic = FlightRuntime.getIndex(data, di);
  var c1y:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
  var c2x:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
  var c2y:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
  var x1:Dynamic = FlightRuntime.getIndex(data, (di + 4.0));
  var y1:Dynamic = FlightRuntime.getIndex(data, (di + 5.0));
  (di = cast ((di + 6.0) : Dynamic));
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(currentSegment, segmentIndex))) {
  if (FlightRuntime.truthy(wantTangent)) {
  FlightRuntime.callValue(getCubicBezierTangent, cast ([x, y, c1x, c1y, c2x, c2y, x1, y1, t, out] : Array<Dynamic>));
} else {
  FlightRuntime.callValue(getCubicBezierPoint, cast ([x, y, c1x, c1y, c2x, c2y, x1, y1, t, out] : Array<Dynamic>));
}
  return cast true;
}
  (x = cast (x1 : Dynamic));
  (y = cast (y1 : Dynamic));
  currentSegment++;
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(command, Types.PathCommandValue.CLOSE))) {
} } } } } } }
        ci++;
      }
    }
    return cast false;
    return cast null;
  }

  @:keep public static function withinTolerance__cleanPath(ax:Float, ay:Float, bx:Float, by:Float, toleranceSq:Float):Bool {
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    dx = (ax - bx);
    dy = (ay - by);
    return cast FlightRuntime.compare(((dx * dx) + (dy * dy)), toleranceSq, '<=');
    return cast null;
  }
}
