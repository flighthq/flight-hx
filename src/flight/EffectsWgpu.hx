// Generated by flight-hx. Do not edit.
package flight;

import Math as HxMath;
import flight.internal.FlightRuntime;
import flight.Adjustments.bakeColorLutForRun;
import flight.Adjustments.createColorLutCache;
import flight.Adjustments.fuseColorMatrices;
import flight.Adjustments.getAdjustmentColorMatrix;
import flight.Adjustments.isColorLutAdjustment;
import flight.Effects.computeBloomBlurRadius;
import flight.Effects.computeBloomIntensity;
import flight.Effects.computeBloomThreshold;
import flight.Effects.computeBoxBlurPassRadius;
import flight.RenderWgpu.acquireWgpuRenderTarget;
import flight.RenderWgpu.beginWgpuRenderPass;
import flight.RenderWgpu.createWgpuRenderTarget;
import flight.RenderWgpu.createWgpuRenderTargetPool;
import flight.RenderWgpu.destroyWgpuRenderTarget;
import flight.RenderWgpu.destroyWgpuRenderTargetPool;
import flight.RenderWgpu.endWgpuRenderPass;
import flight.RenderWgpu.getWgpuRenderStateRuntime;
import flight.RenderWgpu.releaseWgpuRenderTarget;
import flight.RenderWgpu.resizeWgpuRenderTarget;
import flight.Types.Adjustment;
import flight.Types.BevelEffect;
import flight.Types.BloomEffect;
import flight.Types.BlurEffect;
import flight.Types.BokehDepthOfFieldEffect;
import flight.Types.CameraMotionBlurEffect;
import flight.Types.ChromaticAberrationEffect;
import flight.Types.ColorLut;
import flight.Types.ConvolutionEffect;
import flight.Types.CrtEffect;
import flight.Types.DirectionalBlurEffect;
import flight.Types.DisplacementEffect;
import flight.Types.DitherEffect;
import flight.Types.DropShadowEffect;
import flight.Types.FilmGrainEffect;
import flight.Types.FxaaEffect;
import flight.Types.GlitchEffect;
import flight.Types.GodRaysEffect;
import flight.Types.GradientBevelEffect;
import flight.Types.GradientGlowEffect;
import flight.Types.HalftoneEffect;
import flight.Types.InnerGlowEffect;
import flight.Types.InnerShadowEffect;
import flight.Types.KuwaharaEffect;
import flight.Types.LensDirtEffect;
import flight.Types.LensDistortionEffect;
import flight.Types.LensFlareEffect;
import flight.Types.MedianEffect;
import flight.Types.MotionBlurEffect;
import flight.Types.OuterGlowEffect;
import flight.Types.OutlineEffect;
import flight.Types.PixelateEffect;
import flight.Types.PosterizeEffect;
import flight.Types.RadialBlurEffect;
import flight.Types.RenderEffect;
import flight.Types.RenderEffectPipelineOptions;
import flight.Types.ScanlinesEffect;
import flight.Types.ScreenSpaceFogEffect;
import flight.Types.SharpenEffect;
import flight.Types.SketchEffect;
import flight.Types.SmaaEffect;
import flight.Types.SsaoEffect;
import flight.Types.SsrEffect;
import flight.Types.TaaEffect;
import flight.Types.TiltShiftEffect;
import flight.Types.ToneMapEffect;
import flight.Types.VignetteEffect;
import flight.Types.WgpuColorLutTextureCache;
import flight.Types.WgpuRenderEffectPipeline;
import flight.Types.WgpuRenderEffectRunner;
import flight.Types.WgpuRenderState;
import flight.Types.WgpuRenderTarget;
import flight.Types.WgpuRenderTargetPool;
import flight.Types.WhiteBalanceEffect;

typedef BevelCompositeParams__wgpuBevelEffect = { var offsetX:Float; var offsetY:Float; var highlightColor:Float; var highlightAlpha:Float; var shadowColor:Float; var shadowAlpha:Float; var intensity:Float; var clipMode:Float; };

typedef BoxBlurEdgeColor__wgpuEffectBoxBlur = Array<Float>;

typedef WgpuDualSourceEffectPipeline = WgpuEffectPipeline;

typedef WgpuEffectBlendMode = String;

typedef WgpuEffectPassState__wgpuEffectPass = { var uniformBuffer:Dynamic; var uniformData:flight.internal.FlightFloat32Array; var uniformDataI32:Dynamic; var uniformOffset:Float; var uniformStride:Float; var uniformSlots:Float; var uniformBGLayout:Dynamic; var uniformBG:Dynamic; var textureBGLayout:Dynamic; var textureBGs:Dynamic; var sampler:Dynamic; var format:Dynamic; };

typedef WgpuEffectPipeline = { var pipeline:Dynamic; var blendMode:WgpuEffectBlendMode; @:optional var compileForFormat:Dynamic; @:optional var variants:Dynamic; };

#if js
@:build(jsasync.JSAsync.build())
#end
@:expose("flight.EffectsWgpu")
class EffectsWgpu {
  public static final _compositePipelines__wgpuBloomEffect:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _motionBlurPipelines__wgpuMotionBlurEffect:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  @:keep public static function _noopSetUniforms__wgpuTaaEffect():Void {
  }

  public static final _pipelines__wgpuEffectProgramCache:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final _registries__wgpuRenderEffectRegistry:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  @:keep public static function acquireUniformSlot__wgpuEffectPass(fs:WgpuEffectPassState__wgpuEffectPass):Float {
    var offset:Dynamic = cast FlightRuntime.UNDEFINED;
    offset = FlightRuntime.field(fs, 'uniformOffset');
    FlightRuntime.setField(fs, 'uniformOffset', ((offset + FlightRuntime.field(fs, 'uniformStride')) % (FlightRuntime.field(fs, 'uniformSlots') * FlightRuntime.field(fs, 'uniformStride'))));
    return cast offset;
    return cast null;
  }

  @:keep public static function applyBevelEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, pool:WgpuRenderTargetPool, effect:BevelEffect):Void {
    var src:Dynamic = cast FlightRuntime.UNDEFINED;
    var dst:Dynamic = cast FlightRuntime.UNDEFINED;
    var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
    var tinted:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurred:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurTemp:Dynamic = cast FlightRuntime.UNDEFINED;
    var angle:Dynamic = cast FlightRuntime.UNDEFINED;
    var distance:Dynamic = cast FlightRuntime.UNDEFINED;
    var offsetX:Dynamic = cast FlightRuntime.UNDEFINED;
    var offsetY:Dynamic = cast FlightRuntime.UNDEFINED;
    var shadowColor:Dynamic = cast FlightRuntime.UNDEFINED;
    var shadowAlpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var highlightColor:Dynamic = cast FlightRuntime.UNDEFINED;
    var highlightAlpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var quality:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceMode:Dynamic = cast FlightRuntime.UNDEFINED;
    var bevelType:Dynamic = cast FlightRuntime.UNDEFINED;
    src = (cast source : WgpuRenderTarget);
    dst = (cast dest : WgpuRenderTarget);
    descriptor = { width: FlightRuntime.field(source, 'width'), height: FlightRuntime.field(source, 'height'), format: FlightRuntime.field(source, 'format') };
    tinted = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    blurred = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    blurTemp = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    angle = ((FlightRuntime.coalesce(FlightRuntime.field(effect, 'angle'), function():Dynamic return cast 45.0) * HxMath.PI) / 180.0);
    distance = FlightRuntime.coalesce(FlightRuntime.field(effect, 'distance'), function():Dynamic return cast 4.0);
    offsetX = FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.callProperty(HxMath, 'cos', cast ([angle] : Array<Dynamic>)) * distance)] : Array<Dynamic>));
    offsetY = FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.callProperty(HxMath, 'sin', cast ([angle] : Array<Dynamic>)) * distance)] : Array<Dynamic>));
    shadowColor = FlightRuntime.coalesce(FlightRuntime.field(effect, 'shadowColor'), function():Dynamic return cast 0.0);
    shadowAlpha = FlightRuntime.coalesce(FlightRuntime.field(effect, 'shadowAlpha'), function():Dynamic return cast 1.0);
    highlightColor = FlightRuntime.coalesce(FlightRuntime.field(effect, 'highlightColor'), function():Dynamic return cast 16777215.0);
    highlightAlpha = FlightRuntime.coalesce(FlightRuntime.field(effect, 'highlightAlpha'), function():Dynamic return cast 1.0);
    strength = FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 1.0);
    quality = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'quality'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    sourceMode = FlightRuntime.coalesce(FlightRuntime.field(effect, 'sourceMode'), function():Dynamic return cast 'draw');
    bevelType = FlightRuntime.coalesce(FlightRuntime.field(effect, 'bevelType'), function():Dynamic return cast 'inner');
    FlightRuntime.callValue(applyWgpuEffectTintPass, cast ([state, src, tinted, 16777215.0, 1.0, 1.0] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectBoxBlur, cast ([state, tinted, blurred, blurTemp, { blurX: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurX'), function():Dynamic return cast 4.0), blurY: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurY'), function():Dynamic return cast 4.0), passes: quality }] : Array<Dynamic>));
    FlightRuntime.callValue(clearWgpuEffectTarget, cast ([state, dst] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'draw'))) { FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, src, dst] : Array<Dynamic>)); }
    FlightRuntime.callValue(EffectsWgpu.applyWgpuBevelCompositePass__wgpuBevelEffect, cast ([state, blurred, src, dst, { offsetX: (offsetX / FlightRuntime.field(source, 'width')), offsetY: (-offsetY / FlightRuntime.field(source, 'height')), highlightColor: highlightColor, highlightAlpha: highlightAlpha, shadowColor: shadowColor, shadowAlpha: shadowAlpha, intensity: strength, clipMode: FlightRuntime.select(FlightRuntime.strictEquals(bevelType, 'inner'), function():Dynamic return cast 1.0, function():Dynamic return cast FlightRuntime.select(FlightRuntime.strictEquals(bevelType, 'outer'), function():Dynamic return cast 2.0, function():Dynamic return cast 0.0)) }] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'knockout'))) { FlightRuntime.callValue(applyWgpuEffectErasePass, cast ([state, src, dst] : Array<Dynamic>)); }
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, tinted] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, blurred] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, blurTemp] : Array<Dynamic>));
  }

  @:keep public static function applyBloomEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, pool:WgpuRenderTargetPool, effect:BloomEffect):Void {
    var threshold:Dynamic = cast FlightRuntime.UNDEFINED;
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
    var bright:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurred:Dynamic = cast FlightRuntime.UNDEFINED;
    var temp:Dynamic = cast FlightRuntime.UNDEFINED;
    var brightPipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var compositePipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    threshold = FlightRuntime.callValue(computeBloomThreshold, cast ([effect] : Array<Dynamic>));
    intensity = FlightRuntime.callValue(computeBloomIntensity, cast ([effect] : Array<Dynamic>));
    radius = FlightRuntime.callValue(computeBloomBlurRadius, cast ([effect] : Array<Dynamic>));
    descriptor = { width: FlightRuntime.field(source, 'width'), height: FlightRuntime.field(source, 'height'), format: FlightRuntime.field(source, 'format') };
    bright = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    blurred = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    temp = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    brightPipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'bloom.bright', EffectsWgpu.BLOOM_BRIGHT_FRAGMENT_WGSL__wgpuBloomEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), bright, brightPipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, threshold);
}] : Array<Dynamic>));
    FlightRuntime.callValue(applyGaussianBlurToWgpu, cast ([state, bright, blurred, temp, { blurX: radius, blurY: radius }] : Array<Dynamic>));
    compositePipeline = FlightRuntime.callValue(EffectsWgpu.getBloomCompositePipeline__wgpuBloomEffect, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuDualSourceEffectPass, cast ([state, (cast source : WgpuRenderTarget), blurred, (cast dest : WgpuRenderTarget), compositePipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
}] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, bright] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, blurred] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, temp] : Array<Dynamic>));
  }

  @:keep public static function applyBlurEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, temp:WgpuRenderTarget, effect:BlurEffect):Void {
    FlightRuntime.callValue(applyGaussianBlurToWgpu, cast ([state, source, dest, temp, { blurX: FlightRuntime.field(effect, 'blurX'), blurY: FlightRuntime.field(effect, 'blurY') }] : Array<Dynamic>));
  }

  @:keep public static function applyBokehDepthOfFieldEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:BokehDepthOfFieldEffect):Void {
    var maxBlur:Dynamic = cast FlightRuntime.UNDEFINED;
    var width:Dynamic = cast FlightRuntime.UNDEFINED;
    var height:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    maxBlur = FlightRuntime.coalesce(FlightRuntime.field(effect, 'maxBlur'), function():Dynamic return cast 4.0);
    width = FlightRuntime.field(source, 'width');
    height = FlightRuntime.field(source, 'height');
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'lens.bokehDoF', EffectsWgpu.BOKEH_DOF_FRAGMENT_WGSL__wgpuBokehDepthOfFieldEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, maxBlur);
  FlightRuntime.setIndex(f32, 2.0, width);
  FlightRuntime.setIndex(f32, 3.0, height);
}] : Array<Dynamic>));
  }

  @:keep public static function applyBoxBlurPass__wgpuEffectBoxBlur(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, radius:Float, dirX:Float, dirY:Float, edgeColor:Null<BoxBlurEdgeColor__wgpuEffectBoxBlur>):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(EffectsWgpu.getBoxBlurPipeline__wgpuEffectBoxBlur, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, source, dest, pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, (1.0 / FlightRuntime.field(source, 'width')));
  FlightRuntime.setIndex(f32, 1.0, (1.0 / FlightRuntime.field(source, 'height')));
  FlightRuntime.setIndex(f32, 2.0, dirX);
  FlightRuntime.setIndex(f32, 3.0, dirY);
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(edgeColor, FlightRuntime.UNDEFINED))) {
  FlightRuntime.setIndex(f32, 4.0, 0.0);
  FlightRuntime.setIndex(f32, 5.0, 0.0);
  FlightRuntime.setIndex(f32, 6.0, 0.0);
  FlightRuntime.setIndex(f32, 7.0, 0.0);
  FlightRuntime.setIndex(f32, 9.0, 0.0);
} else {
  FlightRuntime.setIndex(f32, 4.0, FlightRuntime.getIndex(edgeColor, 0.0));
  FlightRuntime.setIndex(f32, 5.0, FlightRuntime.getIndex(edgeColor, 1.0));
  FlightRuntime.setIndex(f32, 6.0, FlightRuntime.getIndex(edgeColor, 2.0));
  FlightRuntime.setIndex(f32, 7.0, FlightRuntime.getIndex(edgeColor, 3.0));
  FlightRuntime.setIndex(f32, 9.0, 1.0);
}
  FlightRuntime.setIndex(f32, 8.0, radius);
}] : Array<Dynamic>));
  }

  @:keep public static function applyCameraMotionBlurEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:CameraMotionBlurEffect):Void {
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var samples:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 0.5);
    samples = FlightRuntime.coalesce(FlightRuntime.field(effect, 'samples'), function():Dynamic return cast 16.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'motion.cameraMotionBlur', EffectsWgpu.CAMERA_MOTION_BLUR_FRAGMENT_WGSL__wgpuCameraMotionBlurEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
  FlightRuntime.setIndex(f32, 1.0, samples);
}] : Array<Dynamic>));
  }

  @:keep public static function applyChromaticAberrationEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:ChromaticAberrationEffect):Void {
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var radial:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 0.005);
    radial = FlightRuntime.coalesce(FlightRuntime.field(effect, 'radial'), function():Dynamic return cast true);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'lens.chromaticAberration', EffectsWgpu.CHROMATIC_ABERRATION_FRAGMENT_WGSL__wgpuChromaticAberrationEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
  FlightRuntime.setIndex(f32, 1.0, FlightRuntime.select(radial, function():Dynamic return cast 1.0, function():Dynamic return cast 0.0));
}] : Array<Dynamic>));
  }

  @:keep public static function applyColorLutPassToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, lut:ColorLut, cache:WgpuColorLutTextureCache):Void {
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var fs:Dynamic = cast FlightRuntime.UNDEFINED;
    var texture:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var lutBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var slotOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure0 = state;
    device = FlightRuntime.field(__destructure0, 'device');
    fs = FlightRuntime.callValue(getWgpuEffectPassState, cast ([state] : Array<Dynamic>));
    texture = FlightRuntime.callValue(EffectsWgpu.uploadLutTexture__wgpuColorLutPass, cast ([state, lut, cache] : Array<Dynamic>));
    sourceBG = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(fs, 'textureBGLayout'), entries: cast ([{ binding: 0.0, resource: FlightRuntime.field((cast source : WgpuRenderTarget), 'view') }, { binding: 1.0, resource: FlightRuntime.field(fs, 'sampler') }] : Array<Dynamic>) }] : Array<Dynamic>));
    lutBG = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.callValue(EffectsWgpu.getLutBindGroupLayout__wgpuColorLutPass, cast ([state] : Array<Dynamic>)), entries: cast ([{ binding: 0.0, resource: FlightRuntime.callProperty(texture, 'createView', cast ([{ dimension: '3d' }] : Array<Dynamic>)) }, { binding: 1.0, resource: FlightRuntime.field(fs, 'sampler') }] : Array<Dynamic>) }] : Array<Dynamic>));
    pipeline = FlightRuntime.callValue(EffectsWgpu.getLutPipeline__wgpuColorLutPass, cast ([state, FlightRuntime.field((cast dest : WgpuRenderTarget), 'format')] : Array<Dynamic>));
    slotOffset = FlightRuntime.callProperty(fs, 'acquireSlot', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(fs, 'writeSlot', cast ([slotOffset, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, FlightRuntime.field(lut, 'size'));
}] : Array<Dynamic>));
    pass = FlightRuntime.callProperty(fs, 'beginPass', cast ([(cast dest : WgpuRenderTarget), 'load'] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setPipeline', cast ([FlightRuntime.field(pipeline, 'pipeline')] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([0.0, FlightRuntime.field(fs, 'uniformBG'), cast ([slotOffset] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([1.0, sourceBG] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([2.0, lutBG] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'draw', cast ([6.0] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'end', cast ([] : Array<Dynamic>));
  }

  @:keep public static function applyColorMatrixPassToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, matrix:Array<Float>):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'adjustment.colorMatrix', EffectsWgpu.COLOR_MATRIX_FRAGMENT_WGSL__wgpuColorMatrixPass, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 0.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 1.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 1.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 2.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 3.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 4.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 5.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 5.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 6.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 6.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 7.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 7.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 8.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 8.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 10.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 9.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 11.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 10.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 12.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 11.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 13.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 12.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 15.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 13.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 16.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 14.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 17.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 15.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 18.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 16.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 4.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 17.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 9.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 18.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 14.0), function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 19.0, FlightRuntime.coalesce(FlightRuntime.getIndex(matrix, 19.0), function():Dynamic return cast 0.0));
}] : Array<Dynamic>));
  }

  @:keep public static function applyConvolutionEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:ConvolutionEffect):Void {
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var matrix:Dynamic = cast FlightRuntime.UNDEFINED;
    var matrixX:Dynamic = cast FlightRuntime.UNDEFINED;
    var matrixY:Dynamic = cast FlightRuntime.UNDEFINED;
    var bias:Dynamic = cast FlightRuntime.UNDEFINED;
    var clampEdge:Dynamic = cast FlightRuntime.UNDEFINED;
    var preserveAlpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var edgeColor:Dynamic = cast FlightRuntime.UNDEFINED;
    var divisor:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure0 = effect;
    matrix = FlightRuntime.field(__destructure0, 'matrix');
    matrixX = FlightRuntime.field(__destructure0, 'matrixX');
    matrixY = FlightRuntime.field(__destructure0, 'matrixY');
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(matrixX, 0.0, '<='), function():Dynamic return cast FlightRuntime.compare(matrixY, 0.0, '<=')))) { throw FlightRuntime.error('Convolution matrix dimensions must be positive'); }
    if (FlightRuntime.truthy(FlightRuntime.compare((matrixX * matrixY), MAX_CONVOLUTION_EFFECT_WGPU_KERNEL_SIZE, '>'))) { throw FlightRuntime.error('Convolution kernel exceeds the WebGPU maximum of 7×7 (' + Std.string(matrixX) + '×' + Std.string(matrixY) + ' given)'); }
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(matrix, 'length'), (matrixX * matrixY), '<'))) { throw FlightRuntime.error('Convolution matrix does not match its declared dimensions'); }
    bias = FlightRuntime.coalesce(FlightRuntime.field(effect, 'bias'), function():Dynamic return cast 0.0);
    clampEdge = FlightRuntime.coalesce(FlightRuntime.field(effect, 'clamp'), function():Dynamic return cast true);
    preserveAlpha = FlightRuntime.coalesce(FlightRuntime.field(effect, 'preserveAlpha'), function():Dynamic return cast true);
    edgeColor = FlightRuntime.coalesce(FlightRuntime.field(effect, 'color'), function():Dynamic return cast 0.0);
    divisor = FlightRuntime.coalesce(FlightRuntime.field(effect, 'divisor'), function():Dynamic return cast FlightRuntime.callValue(EffectsWgpu.getAutoDivisor__wgpuConvolutionEffect, cast ([matrix, (matrixX * matrixY)] : Array<Dynamic>)));
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.convolution', EffectsWgpu.CONVOLUTION_WGSL__wgpuConvolutionEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic, i32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, (1.0 / FlightRuntime.field(source, 'width')));
  FlightRuntime.setIndex(f32, 1.0, (1.0 / FlightRuntime.field(source, 'height')));
  FlightRuntime.setIndex(i32, 2.0, matrixX);
  FlightRuntime.setIndex(i32, 3.0, matrixY);
  FlightRuntime.setIndex(f32, 4.0, divisor);
  FlightRuntime.setIndex(f32, 5.0, bias);
  FlightRuntime.setIndex(i32, 6.0, FlightRuntime.select(clampEdge, function():Dynamic return cast 1.0, function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(i32, 7.0, FlightRuntime.select(preserveAlpha, function():Dynamic return cast 1.0, function():Dynamic return cast 0.0));
  FlightRuntime.setIndex(f32, 8.0, ((Std.int((Std.int(edgeColor) >> Std.int(16.0))) & Std.int(255.0)) / 255.0));
  FlightRuntime.setIndex(f32, 9.0, ((Std.int((Std.int(edgeColor) >> Std.int(8.0))) & Std.int(255.0)) / 255.0));
  FlightRuntime.setIndex(f32, 10.0, ((Std.int(edgeColor) & Std.int(255.0)) / 255.0));
  FlightRuntime.setIndex(f32, 11.0, ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(edgeColor), Std.int(24.0))) & Std.int(255.0)) / 255.0));
  {
    var i:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, (matrixX * matrixY), '<'))) {
      FlightRuntime.setIndex(f32, (12.0 + i), FlightRuntime.getIndex(matrix, i));
      i++;
    }
  }
}] : Array<Dynamic>));
  }

  @:keep public static function applyCrtEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:CrtEffect):Void {
    var curvature:Dynamic = cast FlightRuntime.UNDEFINED;
    var scanlineIntensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var vignette:Dynamic = cast FlightRuntime.UNDEFINED;
    var aberration:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    curvature = FlightRuntime.coalesce(FlightRuntime.field(effect, 'curvature'), function():Dynamic return cast 0.1);
    scanlineIntensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'scanlineIntensity'), function():Dynamic return cast 0.3);
    vignette = FlightRuntime.coalesce(FlightRuntime.field(effect, 'vignette'), function():Dynamic return cast 0.3);
    aberration = FlightRuntime.coalesce(FlightRuntime.field(effect, 'aberration'), function():Dynamic return cast 0.005);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.crt', EffectsWgpu.CRT_FRAGMENT_WGSL__wgpuCrtEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, curvature);
  FlightRuntime.setIndex(f32, 1.0, scanlineIntensity);
  FlightRuntime.setIndex(f32, 2.0, vignette);
  FlightRuntime.setIndex(f32, 3.0, aberration);
  FlightRuntime.setIndex(f32, 4.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 5.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyDirectionalBlurEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:DirectionalBlurEffect):Void {
    var angle:Dynamic = cast FlightRuntime.UNDEFINED;
    var length:Dynamic = cast FlightRuntime.UNDEFINED;
    var samples:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    angle = FlightRuntime.coalesce(FlightRuntime.field(effect, 'angle'), function():Dynamic return cast 0.0);
    length = FlightRuntime.coalesce(FlightRuntime.field(effect, 'length'), function():Dynamic return cast 8.0);
    samples = FlightRuntime.coalesce(FlightRuntime.field(effect, 'samples'), function():Dynamic return cast 16.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'motion.directionalBlur', EffectsWgpu.DIRECTIONAL_BLUR_FRAGMENT_WGSL__wgpuDirectionalBlurEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, angle);
  FlightRuntime.setIndex(f32, 1.0, length);
  FlightRuntime.setIndex(f32, 2.0, samples);
  FlightRuntime.setIndex(f32, 4.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 5.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyDisplacementEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:DisplacementEffect):Void {
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var frequency:Dynamic = cast FlightRuntime.UNDEFINED;
    var seed:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 8.0);
    frequency = FlightRuntime.coalesce(FlightRuntime.field(effect, 'frequency'), function():Dynamic return cast 12.0);
    seed = FlightRuntime.coalesce(FlightRuntime.field(effect, 'seed'), function():Dynamic return cast 0.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'lens.displacement', EffectsWgpu.DISPLACEMENT_FRAGMENT_WGSL__wgpuDisplacementEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
  FlightRuntime.setIndex(f32, 1.0, frequency);
  FlightRuntime.setIndex(f32, 2.0, seed);
  FlightRuntime.setIndex(f32, 4.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 5.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyDitherEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:DitherEffect):Void {
    var levels:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    levels = FlightRuntime.coalesce(FlightRuntime.field(effect, 'levels'), function():Dynamic return cast 4.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.dither', EffectsWgpu.DITHER_FRAGMENT_WGSL__wgpuDitherEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, FlightRuntime.callProperty(HxMath, 'max', cast ([2.0, levels] : Array<Dynamic>)));
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyDropShadowEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, pool:WgpuRenderTargetPool, effect:DropShadowEffect):Void {
    var src:Dynamic = cast FlightRuntime.UNDEFINED;
    var dst:Dynamic = cast FlightRuntime.UNDEFINED;
    var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
    var mask:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurred:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurTemp:Dynamic = cast FlightRuntime.UNDEFINED;
    var angle:Dynamic = cast FlightRuntime.UNDEFINED;
    var distance:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var color:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var quality:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceMode:Dynamic = cast FlightRuntime.UNDEFINED;
    var tintStrength:Dynamic = cast FlightRuntime.UNDEFINED;
    var shadowPasses:Dynamic = cast FlightRuntime.UNDEFINED;
    src = (cast source : WgpuRenderTarget);
    dst = (cast dest : WgpuRenderTarget);
    descriptor = { width: FlightRuntime.field(source, 'width'), height: FlightRuntime.field(source, 'height'), format: FlightRuntime.field(source, 'format') };
    mask = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    blurred = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    blurTemp = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    angle = ((FlightRuntime.coalesce(FlightRuntime.field(effect, 'angle'), function():Dynamic return cast 45.0) * HxMath.PI) / 180.0);
    distance = FlightRuntime.coalesce(FlightRuntime.field(effect, 'distance'), function():Dynamic return cast 4.0);
    dx = (FlightRuntime.callProperty(HxMath, 'cos', cast ([angle] : Array<Dynamic>)) * distance);
    dy = (FlightRuntime.callProperty(HxMath, 'sin', cast ([angle] : Array<Dynamic>)) * distance);
    color = FlightRuntime.coalesce(FlightRuntime.field(effect, 'color'), function():Dynamic return cast 0.0);
    alpha = FlightRuntime.coalesce(FlightRuntime.field(effect, 'alpha'), function():Dynamic return cast 1.0);
    strength = FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 1.0);
    quality = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'quality'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    sourceMode = FlightRuntime.coalesce(FlightRuntime.field(effect, 'sourceMode'), function():Dynamic return cast 'draw');
    tintStrength = FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, strength] : Array<Dynamic>));
    shadowPasses = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'floor', cast ([strength] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectTintPass, cast ([state, src, mask, color, alpha, tintStrength] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectBoxBlur, cast ([state, mask, blurred, blurTemp, { blurX: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurX'), function():Dynamic return cast 4.0), blurY: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurY'), function():Dynamic return cast 4.0), passes: quality }] : Array<Dynamic>));
    FlightRuntime.callValue(clearWgpuEffectTarget, cast ([state, dst] : Array<Dynamic>));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, shadowPasses, '<'))) {
        FlightRuntime.callValue(applyWgpuEffectBlitOffsetPass, cast ([state, blurred, dst, dx, dy] : Array<Dynamic>));
        i++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'knockout'))) {
  FlightRuntime.callValue(applyWgpuEffectErasePass, cast ([state, src, dst] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'draw'))) {
  FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, src, dst] : Array<Dynamic>));
} }
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, mask] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, blurred] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, blurTemp] : Array<Dynamic>));
  }

  @:keep public static function applyFilmGrainEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:FilmGrainEffect):Void {
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var size:Dynamic = cast FlightRuntime.UNDEFINED;
    var seed:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 0.1);
    size = FlightRuntime.coalesce(FlightRuntime.field(effect, 'size'), function():Dynamic return cast 1.0);
    seed = FlightRuntime.coalesce(FlightRuntime.field(effect, 'seed'), function():Dynamic return cast 0.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.filmGrain', EffectsWgpu.FILM_GRAIN_FRAGMENT_WGSL__wgpuFilmGrainEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
  FlightRuntime.setIndex(f32, 1.0, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0001, size] : Array<Dynamic>)));
  FlightRuntime.setIndex(f32, 2.0, seed);
}] : Array<Dynamic>));
  }

  @:keep public static function applyFxaaEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:FxaaEffect):Void {
    var edgeThreshold:Dynamic = cast FlightRuntime.UNDEFINED;
    var width:Dynamic = cast FlightRuntime.UNDEFINED;
    var height:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    edgeThreshold = FlightRuntime.coalesce(FlightRuntime.field(effect, 'edgeThreshold'), function():Dynamic return cast 0.0312);
    width = FlightRuntime.field(source, 'width');
    height = FlightRuntime.field(source, 'height');
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'antialiasing.fxaa', EffectsWgpu.FXAA_FRAGMENT_WGSL__wgpuFxaaEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, width);
  FlightRuntime.setIndex(f32, 1.0, height);
  FlightRuntime.setIndex(f32, 2.0, edgeThreshold);
}] : Array<Dynamic>));
  }

  @:keep public static function applyGaussianBlurToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, temp:WgpuRenderTarget, options:{ @:optional var blurX:Float; @:optional var blurY:Float; }):Void {
    var sigmaX:Dynamic = cast FlightRuntime.UNDEFINED;
    var sigmaY:Dynamic = cast FlightRuntime.UNDEFINED;
    var radiusX:Dynamic = cast FlightRuntime.UNDEFINED;
    var radiusY:Dynamic = cast FlightRuntime.UNDEFINED;
    sigmaX = FlightRuntime.coalesce(FlightRuntime.field(options, 'blurX'), function():Dynamic return cast 4.0);
    sigmaY = FlightRuntime.coalesce(FlightRuntime.field(options, 'blurY'), function():Dynamic return cast 4.0);
    radiusX = FlightRuntime.select(FlightRuntime.compare(sigmaX, 0.0, '>'), function():Dynamic return cast FlightRuntime.callProperty(HxMath, 'ceil', cast ([(sigmaX * 3.0)] : Array<Dynamic>)), function():Dynamic return cast 0.0);
    radiusY = FlightRuntime.select(FlightRuntime.compare(sigmaY, 0.0, '>'), function():Dynamic return cast FlightRuntime.callProperty(HxMath, 'ceil', cast ([(sigmaY * 3.0)] : Array<Dynamic>)), function():Dynamic return cast 0.0);
    FlightRuntime.callValue(EffectsWgpu.applyWgpuGaussianBlurPass__wgpuBlurEffect, cast ([state, source, temp, sigmaX, radiusX, 1.0, 0.0] : Array<Dynamic>));
    FlightRuntime.callValue(EffectsWgpu.applyWgpuGaussianBlurPass__wgpuBlurEffect, cast ([state, temp, dest, sigmaY, radiusY, 0.0, 1.0] : Array<Dynamic>));
  }

  @:keep public static function applyGlitchEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:GlitchEffect):Void {
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var blockSize:Dynamic = cast FlightRuntime.UNDEFINED;
    var colorShift:Dynamic = cast FlightRuntime.UNDEFINED;
    var seed:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 0.5);
    blockSize = FlightRuntime.coalesce(FlightRuntime.field(effect, 'blockSize'), function():Dynamic return cast 24.0);
    colorShift = FlightRuntime.coalesce(FlightRuntime.field(effect, 'colorShift'), function():Dynamic return cast 8.0);
    seed = FlightRuntime.coalesce(FlightRuntime.field(effect, 'seed'), function():Dynamic return cast 0.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.glitch', EffectsWgpu.GLITCH_FRAGMENT_WGSL__wgpuGlitchEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
  FlightRuntime.setIndex(f32, 1.0, blockSize);
  FlightRuntime.setIndex(f32, 2.0, colorShift);
  FlightRuntime.setIndex(f32, 3.0, seed);
  FlightRuntime.setIndex(f32, 4.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 5.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyGodRaysEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:GodRaysEffect):Void {
    var centerX:Dynamic = cast FlightRuntime.UNDEFINED;
    var centerY:Dynamic = cast FlightRuntime.UNDEFINED;
    var density:Dynamic = cast FlightRuntime.UNDEFINED;
    var decay:Dynamic = cast FlightRuntime.UNDEFINED;
    var weight:Dynamic = cast FlightRuntime.UNDEFINED;
    var exposure:Dynamic = cast FlightRuntime.UNDEFINED;
    var samples:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    centerX = FlightRuntime.coalesce(FlightRuntime.field(effect, 'centerX'), function():Dynamic return cast 0.5);
    centerY = FlightRuntime.coalesce(FlightRuntime.field(effect, 'centerY'), function():Dynamic return cast 0.5);
    density = FlightRuntime.coalesce(FlightRuntime.field(effect, 'density'), function():Dynamic return cast 0.96);
    decay = FlightRuntime.coalesce(FlightRuntime.field(effect, 'decay'), function():Dynamic return cast 0.93);
    weight = FlightRuntime.coalesce(FlightRuntime.field(effect, 'weight'), function():Dynamic return cast 0.4);
    exposure = FlightRuntime.coalesce(FlightRuntime.field(effect, 'exposure'), function():Dynamic return cast 0.6);
    samples = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'samples'), function():Dynamic return cast 64.0)] : Array<Dynamic>))] : Array<Dynamic>));
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'atmospheric.godRays.' + Std.string(samples) + '', FlightRuntime.callValue(EffectsWgpu.buildGodRaysFragment__wgpuGodRaysEffect, cast ([samples] : Array<Dynamic>)), 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, centerX);
  FlightRuntime.setIndex(f32, 1.0, centerY);
  FlightRuntime.setIndex(f32, 2.0, density);
  FlightRuntime.setIndex(f32, 3.0, decay);
  FlightRuntime.setIndex(f32, 4.0, weight);
  FlightRuntime.setIndex(f32, 5.0, exposure);
}] : Array<Dynamic>));
  }

  @:keep public static function applyGradientBevelEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, pool:WgpuRenderTargetPool, effect:GradientBevelEffect):Void {
    var src:Dynamic = cast FlightRuntime.UNDEFINED;
    var dst:Dynamic = cast FlightRuntime.UNDEFINED;
    var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
    var s0:Dynamic = cast FlightRuntime.UNDEFINED;
    var s1:Dynamic = cast FlightRuntime.UNDEFINED;
    var s2:Dynamic = cast FlightRuntime.UNDEFINED;
    var angle:Dynamic = cast FlightRuntime.UNDEFINED;
    var distance:Dynamic = cast FlightRuntime.UNDEFINED;
    var quality:Dynamic = cast FlightRuntime.UNDEFINED;
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceMode:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var fs:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var encodePipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var encodeSlot:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurredBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var encodePass:Dynamic = cast FlightRuntime.UNDEFINED;
    var rampTexture:Dynamic = cast FlightRuntime.UNDEFINED;
    var rampBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var encodedBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var applyPipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var applySlot:Dynamic = cast FlightRuntime.UNDEFINED;
    var applyPass:Dynamic = cast FlightRuntime.UNDEFINED;
    src = (cast source : WgpuRenderTarget);
    dst = (cast dest : WgpuRenderTarget);
    descriptor = { width: FlightRuntime.field(source, 'width'), height: FlightRuntime.field(source, 'height'), format: FlightRuntime.field(source, 'format') };
    s0 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    s1 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    s2 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    angle = ((FlightRuntime.coalesce(FlightRuntime.field(effect, 'angle'), function():Dynamic return cast 45.0) * HxMath.PI) / 180.0);
    distance = FlightRuntime.coalesce(FlightRuntime.field(effect, 'distance'), function():Dynamic return cast 4.0);
    quality = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'quality'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    strength = FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 1.0);
    sourceMode = FlightRuntime.coalesce(FlightRuntime.field(effect, 'sourceMode'), function():Dynamic return cast 'draw');
    __destructure0 = state;
    device = FlightRuntime.field(__destructure0, 'device');
    fs = FlightRuntime.callValue(getWgpuEffectPassState, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectTintPass, cast ([state, src, s0, 16777215.0, 1.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, strength] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectBoxBlur, cast ([state, s0, s1, s2, { blurX: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurX'), function():Dynamic return cast 4.0), blurY: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurY'), function():Dynamic return cast 4.0), passes: quality }] : Array<Dynamic>));
    dx = ((FlightRuntime.callProperty(HxMath, 'cos', cast ([angle] : Array<Dynamic>)) * distance) / FlightRuntime.field(s1, 'width'));
    dy = -((FlightRuntime.callProperty(HxMath, 'sin', cast ([angle] : Array<Dynamic>)) * distance) / FlightRuntime.field(s1, 'height'));
    encodePipeline = FlightRuntime.callValue(EffectsWgpu.getEncodePipeline__wgpuGradientBevelEffect, cast ([state] : Array<Dynamic>));
    encodeSlot = FlightRuntime.callProperty(fs, 'acquireSlot', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(fs, 'writeSlot', cast ([encodeSlot, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, dx);
  FlightRuntime.setIndex(f32, 1.0, dy);
}] : Array<Dynamic>));
    blurredBG = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(fs, 'textureBGLayout'), entries: cast ([{ binding: 0.0, resource: FlightRuntime.field(s1, 'view') }, { binding: 1.0, resource: FlightRuntime.field(fs, 'sampler') }] : Array<Dynamic>) }] : Array<Dynamic>));
    encodePass = FlightRuntime.callProperty(fs, 'beginPass', cast ([s0, 'load'] : Array<Dynamic>));
    FlightRuntime.callProperty(encodePass, 'setPipeline', cast ([FlightRuntime.field(encodePipeline, 'pipeline')] : Array<Dynamic>));
    FlightRuntime.callProperty(encodePass, 'setBindGroup', cast ([0.0, FlightRuntime.field(fs, 'uniformBG'), cast ([encodeSlot] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callProperty(encodePass, 'setBindGroup', cast ([1.0, blurredBG] : Array<Dynamic>));
    FlightRuntime.callProperty(encodePass, 'draw', cast ([6.0] : Array<Dynamic>));
    FlightRuntime.callProperty(encodePass, 'end', cast ([] : Array<Dynamic>));
    rampTexture = FlightRuntime.callValue(getWgpuEffectGradientRampTexture, cast ([state, FlightRuntime.field(effect, 'colors'), FlightRuntime.field(effect, 'alphas'), FlightRuntime.field(effect, 'ratios')] : Array<Dynamic>));
    rampBG = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(fs, 'textureBGLayout'), entries: cast ([{ binding: 0.0, resource: FlightRuntime.callProperty(rampTexture, 'createView', cast ([] : Array<Dynamic>)) }, { binding: 1.0, resource: FlightRuntime.field(fs, 'sampler') }] : Array<Dynamic>) }] : Array<Dynamic>));
    encodedBG = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(fs, 'textureBGLayout'), entries: cast ([{ binding: 0.0, resource: FlightRuntime.field(s0, 'view') }, { binding: 1.0, resource: FlightRuntime.field(fs, 'sampler') }] : Array<Dynamic>) }] : Array<Dynamic>));
    sourceBG = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(fs, 'textureBGLayout'), entries: cast ([{ binding: 0.0, resource: FlightRuntime.field(src, 'view') }, { binding: 1.0, resource: FlightRuntime.field(fs, 'sampler') }] : Array<Dynamic>) }] : Array<Dynamic>));
    applyPipeline = FlightRuntime.callValue(EffectsWgpu.getApplyPipeline__wgpuGradientBevelEffect, cast ([state] : Array<Dynamic>));
    applySlot = FlightRuntime.callProperty(fs, 'acquireSlot', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(fs, 'writeSlot', cast ([applySlot, function() {

}] : Array<Dynamic>));
    applyPass = FlightRuntime.callProperty(fs, 'beginPass', cast ([s1, 'load'] : Array<Dynamic>));
    FlightRuntime.callProperty(applyPass, 'setPipeline', cast ([FlightRuntime.field(applyPipeline, 'pipeline')] : Array<Dynamic>));
    FlightRuntime.callProperty(applyPass, 'setBindGroup', cast ([0.0, FlightRuntime.field(fs, 'uniformBG'), cast ([applySlot] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callProperty(applyPass, 'setBindGroup', cast ([1.0, encodedBG] : Array<Dynamic>));
    FlightRuntime.callProperty(applyPass, 'setBindGroup', cast ([2.0, rampBG] : Array<Dynamic>));
    FlightRuntime.callProperty(applyPass, 'setBindGroup', cast ([3.0, sourceBG] : Array<Dynamic>));
    FlightRuntime.callProperty(applyPass, 'draw', cast ([6.0] : Array<Dynamic>));
    FlightRuntime.callProperty(applyPass, 'end', cast ([] : Array<Dynamic>));
    FlightRuntime.callValue(clearWgpuEffectTarget, cast ([state, dst] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'draw'))) { FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, src, dst] : Array<Dynamic>)); }
    FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, s1, dst] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'knockout'))) { FlightRuntime.callValue(applyWgpuEffectErasePass, cast ([state, src, dst] : Array<Dynamic>)); }
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s0] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s1] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s2] : Array<Dynamic>));
  }

  @:keep public static function applyGradientGlowEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, pool:WgpuRenderTargetPool, effect:GradientGlowEffect):Void {
    var src:Dynamic = cast FlightRuntime.UNDEFINED;
    var dst:Dynamic = cast FlightRuntime.UNDEFINED;
    var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
    var s0:Dynamic = cast FlightRuntime.UNDEFINED;
    var s1:Dynamic = cast FlightRuntime.UNDEFINED;
    var s2:Dynamic = cast FlightRuntime.UNDEFINED;
    var quality:Dynamic = cast FlightRuntime.UNDEFINED;
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceMode:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var fs:Dynamic = cast FlightRuntime.UNDEFINED;
    var rampTexture:Dynamic = cast FlightRuntime.UNDEFINED;
    var rampBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurredBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var slotOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    src = (cast source : WgpuRenderTarget);
    dst = (cast dest : WgpuRenderTarget);
    descriptor = { width: FlightRuntime.field(source, 'width'), height: FlightRuntime.field(source, 'height'), format: FlightRuntime.field(source, 'format') };
    s0 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    s1 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    s2 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    quality = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'quality'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    strength = FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 1.0);
    sourceMode = FlightRuntime.coalesce(FlightRuntime.field(effect, 'sourceMode'), function():Dynamic return cast 'draw');
    __destructure0 = state;
    device = FlightRuntime.field(__destructure0, 'device');
    fs = FlightRuntime.callValue(getWgpuEffectPassState, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectTintPass, cast ([state, src, s0, 16777215.0, 1.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, strength] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectBoxBlur, cast ([state, s0, s1, s2, { blurX: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurX'), function():Dynamic return cast 6.0), blurY: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurY'), function():Dynamic return cast 6.0), passes: quality }] : Array<Dynamic>));
    rampTexture = FlightRuntime.callValue(getWgpuEffectGradientRampTexture, cast ([state, FlightRuntime.field(effect, 'colors'), FlightRuntime.field(effect, 'alphas'), FlightRuntime.field(effect, 'ratios')] : Array<Dynamic>));
    rampBG = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(fs, 'textureBGLayout'), entries: cast ([{ binding: 0.0, resource: FlightRuntime.callProperty(rampTexture, 'createView', cast ([] : Array<Dynamic>)) }, { binding: 1.0, resource: FlightRuntime.field(fs, 'sampler') }] : Array<Dynamic>) }] : Array<Dynamic>));
    blurredBG = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(fs, 'textureBGLayout'), entries: cast ([{ binding: 0.0, resource: FlightRuntime.field(s1, 'view') }, { binding: 1.0, resource: FlightRuntime.field(fs, 'sampler') }] : Array<Dynamic>) }] : Array<Dynamic>));
    pipeline = FlightRuntime.callValue(EffectsWgpu.getLookupPipeline__wgpuGradientGlowEffect, cast ([state] : Array<Dynamic>));
    slotOffset = FlightRuntime.callProperty(fs, 'acquireSlot', cast ([] : Array<Dynamic>));
    FlightRuntime.callProperty(fs, 'writeSlot', cast ([slotOffset, function() {

}] : Array<Dynamic>));
    pass = FlightRuntime.callProperty(fs, 'beginPass', cast ([s0, 'load'] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setPipeline', cast ([FlightRuntime.field(pipeline, 'pipeline')] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([0.0, FlightRuntime.field(fs, 'uniformBG'), cast ([slotOffset] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([1.0, blurredBG] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([2.0, rampBG] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'draw', cast ([6.0] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'end', cast ([] : Array<Dynamic>));
    FlightRuntime.callValue(clearWgpuEffectTarget, cast ([state, dst] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, s0, dst] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'knockout'))) {
  FlightRuntime.callValue(applyWgpuEffectErasePass, cast ([state, src, dst] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'draw'))) {
  FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, src, dst] : Array<Dynamic>));
} }
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s0] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s1] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s2] : Array<Dynamic>));
  }

  @:keep public static function applyHalftoneEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:HalftoneEffect):Void {
    var scale:Dynamic = cast FlightRuntime.UNDEFINED;
    var angle:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    scale = FlightRuntime.coalesce(FlightRuntime.field(effect, 'scale'), function():Dynamic return cast 6.0);
    angle = FlightRuntime.coalesce(FlightRuntime.field(effect, 'angle'), function():Dynamic return cast 0.4);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.halftone', EffectsWgpu.HALFTONE_FRAGMENT_WGSL__wgpuHalftoneEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, scale] : Array<Dynamic>)));
  FlightRuntime.setIndex(f32, 1.0, angle);
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyInnerGlowEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, pool:WgpuRenderTargetPool, effect:InnerGlowEffect):Void {
    var src:Dynamic = cast FlightRuntime.UNDEFINED;
    var dst:Dynamic = cast FlightRuntime.UNDEFINED;
    var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
    var s0:Dynamic = cast FlightRuntime.UNDEFINED;
    var s1:Dynamic = cast FlightRuntime.UNDEFINED;
    var s2:Dynamic = cast FlightRuntime.UNDEFINED;
    var color:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var quality:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceMode:Dynamic = cast FlightRuntime.UNDEFINED;
    src = (cast source : WgpuRenderTarget);
    dst = (cast dest : WgpuRenderTarget);
    descriptor = { width: FlightRuntime.field(source, 'width'), height: FlightRuntime.field(source, 'height'), format: FlightRuntime.field(source, 'format') };
    s0 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    s1 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    s2 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    color = FlightRuntime.coalesce(FlightRuntime.field(effect, 'color'), function():Dynamic return cast 16711680.0);
    alpha = FlightRuntime.coalesce(FlightRuntime.field(effect, 'alpha'), function():Dynamic return cast 1.0);
    strength = FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 1.0);
    quality = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'quality'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    sourceMode = FlightRuntime.coalesce(FlightRuntime.field(effect, 'sourceMode'), function():Dynamic return cast 'draw');
    FlightRuntime.callValue(applyWgpuEffectInvertTintPass, cast ([state, src, s0, color, alpha, strength] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectBoxBlur, cast ([state, s0, s1, s2, { blurX: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurX'), function():Dynamic return cast 6.0), blurY: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurY'), function():Dynamic return cast 6.0), edgeColor: FlightRuntime.callValue(EffectsWgpu.getInvertTintEdgeColor__wgpuInnerGlowEffect, cast ([color, alpha, strength] : Array<Dynamic>)), passes: quality }] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectInnerClipPass, cast ([state, s1, src, s0] : Array<Dynamic>));
    FlightRuntime.callValue(clearWgpuEffectTarget, cast ([state, dst] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'draw'))) {
  FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, src, dst] : Array<Dynamic>));
}
    FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, s0, dst] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s0] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s1] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s2] : Array<Dynamic>));
  }

  @:keep public static function applyInnerShadowEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, pool:WgpuRenderTargetPool, effect:InnerShadowEffect):Void {
    var src:Dynamic = cast FlightRuntime.UNDEFINED;
    var dst:Dynamic = cast FlightRuntime.UNDEFINED;
    var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
    var s0:Dynamic = cast FlightRuntime.UNDEFINED;
    var s1:Dynamic = cast FlightRuntime.UNDEFINED;
    var s2:Dynamic = cast FlightRuntime.UNDEFINED;
    var angle:Dynamic = cast FlightRuntime.UNDEFINED;
    var distance:Dynamic = cast FlightRuntime.UNDEFINED;
    var dx:Dynamic = cast FlightRuntime.UNDEFINED;
    var dy:Dynamic = cast FlightRuntime.UNDEFINED;
    var color:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var quality:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceMode:Dynamic = cast FlightRuntime.UNDEFINED;
    src = (cast source : WgpuRenderTarget);
    dst = (cast dest : WgpuRenderTarget);
    descriptor = { width: FlightRuntime.field(source, 'width'), height: FlightRuntime.field(source, 'height'), format: FlightRuntime.field(source, 'format') };
    s0 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    s1 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    s2 = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    angle = ((FlightRuntime.coalesce(FlightRuntime.field(effect, 'angle'), function():Dynamic return cast 45.0) * HxMath.PI) / 180.0);
    distance = FlightRuntime.coalesce(FlightRuntime.field(effect, 'distance'), function():Dynamic return cast 4.0);
    dx = (FlightRuntime.callProperty(HxMath, 'cos', cast ([angle] : Array<Dynamic>)) * distance);
    dy = (FlightRuntime.callProperty(HxMath, 'sin', cast ([angle] : Array<Dynamic>)) * distance);
    color = FlightRuntime.coalesce(FlightRuntime.field(effect, 'color'), function():Dynamic return cast 0.0);
    alpha = FlightRuntime.coalesce(FlightRuntime.field(effect, 'alpha'), function():Dynamic return cast 1.0);
    strength = FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 1.0);
    quality = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'quality'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    sourceMode = FlightRuntime.coalesce(FlightRuntime.field(effect, 'sourceMode'), function():Dynamic return cast 'draw');
    FlightRuntime.callValue(applyWgpuEffectInvertTintPass, cast ([state, src, s0, color, alpha, strength] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectBoxBlur, cast ([state, s0, s1, s2, { blurX: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurX'), function():Dynamic return cast 4.0), blurY: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurY'), function():Dynamic return cast 4.0), edgeColor: FlightRuntime.callValue(EffectsWgpu.getInvertTintEdgeColor__wgpuInnerShadowEffect, cast ([color, alpha, strength] : Array<Dynamic>)), passes: quality }] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectBlitOffsetPass, cast ([state, s1, s0, dx, dy] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectInnerClipPass, cast ([state, s0, src, s1] : Array<Dynamic>));
    FlightRuntime.callValue(clearWgpuEffectTarget, cast ([state, dst] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'draw'))) {
  FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, src, dst] : Array<Dynamic>));
}
    FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, s1, dst] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s0] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s1] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, s2] : Array<Dynamic>));
  }

  @:keep public static function applyKuwaharaEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:KuwaharaEffect):Void {
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    radius = FlightRuntime.coalesce(FlightRuntime.field(effect, 'radius'), function():Dynamic return cast 3.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.kuwahara', EffectsWgpu.KUWAHARA_FRAGMENT_WGSL__wgpuKuwaharaEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, radius] : Array<Dynamic>)));
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyLensDirtEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:LensDirtEffect):Void {
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var threshold:Dynamic = cast FlightRuntime.UNDEFINED;
    var seed:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 1.0);
    threshold = FlightRuntime.coalesce(FlightRuntime.field(effect, 'threshold'), function():Dynamic return cast 0.55);
    seed = FlightRuntime.coalesce(FlightRuntime.field(effect, 'seed'), function():Dynamic return cast 0.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'lens.lensDirt', EffectsWgpu.LENS_DIRT_FRAGMENT_WGSL__wgpuLensDirtEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
  FlightRuntime.setIndex(f32, 1.0, threshold);
  FlightRuntime.setIndex(f32, 2.0, seed);
}] : Array<Dynamic>));
  }

  @:keep public static function applyLensDistortionEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:LensDistortionEffect):Void {
    var amount:Dynamic = cast FlightRuntime.UNDEFINED;
    var scale:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    amount = FlightRuntime.coalesce(FlightRuntime.field(effect, 'amount'), function():Dynamic return cast 0.2);
    scale = FlightRuntime.coalesce(FlightRuntime.field(effect, 'scale'), function():Dynamic return cast 1.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'lens.lensDistortion', EffectsWgpu.LENS_DISTORTION_FRAGMENT_WGSL__wgpuLensDistortionEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, amount);
  FlightRuntime.setIndex(f32, 1.0, scale);
}] : Array<Dynamic>));
  }

  @:keep public static function applyLensFlareEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:LensFlareEffect):Void {
    var threshold:Dynamic = cast FlightRuntime.UNDEFINED;
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var ghosts:Dynamic = cast FlightRuntime.UNDEFINED;
    var halo:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    threshold = FlightRuntime.coalesce(FlightRuntime.field(effect, 'threshold'), function():Dynamic return cast 0.8);
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 1.0);
    ghosts = FlightRuntime.coalesce(FlightRuntime.field(effect, 'ghosts'), function():Dynamic return cast 4.0);
    halo = FlightRuntime.coalesce(FlightRuntime.field(effect, 'halo'), function():Dynamic return cast 0.5);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'lens.lensFlare', EffectsWgpu.LENS_FLARE_FRAGMENT_WGSL__wgpuLensFlareEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, threshold);
  FlightRuntime.setIndex(f32, 1.0, intensity);
  FlightRuntime.setIndex(f32, 2.0, ghosts);
  FlightRuntime.setIndex(f32, 3.0, halo);
}] : Array<Dynamic>));
  }

  @:keep public static function applyMedianEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:MedianEffect):Void {
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    radius = FlightRuntime.callProperty(HxMath, 'min', cast ([MAX_MEDIAN_EFFECT_WGPU_RADIUS, FlightRuntime.callProperty(HxMath, 'max', cast ([0.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'radius'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>))] : Array<Dynamic>));
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.median', EffectsWgpu.MEDIAN_WGSL__wgpuMedianEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic, i32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, (1.0 / FlightRuntime.field(source, 'width')));
  FlightRuntime.setIndex(f32, 1.0, (1.0 / FlightRuntime.field(source, 'height')));
  FlightRuntime.setIndex(i32, 2.0, radius);
}] : Array<Dynamic>));
  }

  @:keep public static function applyMotionBlurEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, velocityTexture:Null<Dynamic>, effect:MotionBlurEffect):Void {
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var samples:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    var velocitySource:Dynamic = cast FlightRuntime.UNDEFINED;
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 1.0);
    samples = FlightRuntime.coalesce(FlightRuntime.field(effect, 'samples'), function():Dynamic return cast 16.0);
    pipeline = FlightRuntime.callValue(EffectsWgpu.getMotionBlurPipeline__wgpuMotionBlurEffect, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(velocityTexture, null))) {
  FlightRuntime.callValue(drawWgpuDualSourceEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
  FlightRuntime.setIndex(f32, 1.0, samples);
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
  FlightRuntime.setIndex(f32, 4.0, 0.0);
}] : Array<Dynamic>));
  return;
}
    velocitySource = (cast { view: FlightRuntime.callProperty(velocityTexture, 'createView', cast ([] : Array<Dynamic>)) } : WgpuRenderTarget);
    FlightRuntime.callValue(drawWgpuDualSourceEffectPass, cast ([state, (cast source : WgpuRenderTarget), velocitySource, (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
  FlightRuntime.setIndex(f32, 1.0, samples);
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
  FlightRuntime.setIndex(f32, 4.0, 1.0);
}] : Array<Dynamic>));
  }

  @:keep public static function applyOuterGlowEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, pool:WgpuRenderTargetPool, effect:OuterGlowEffect):Void {
    var src:Dynamic = cast FlightRuntime.UNDEFINED;
    var dst:Dynamic = cast FlightRuntime.UNDEFINED;
    var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
    var mask:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurred:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurTemp:Dynamic = cast FlightRuntime.UNDEFINED;
    var color:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var quality:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceMode:Dynamic = cast FlightRuntime.UNDEFINED;
    var tintStrength:Dynamic = cast FlightRuntime.UNDEFINED;
    var glowPasses:Dynamic = cast FlightRuntime.UNDEFINED;
    src = (cast source : WgpuRenderTarget);
    dst = (cast dest : WgpuRenderTarget);
    descriptor = { width: FlightRuntime.field(source, 'width'), height: FlightRuntime.field(source, 'height'), format: FlightRuntime.field(source, 'format') };
    mask = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    blurred = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    blurTemp = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, pool, descriptor] : Array<Dynamic>));
    color = FlightRuntime.coalesce(FlightRuntime.field(effect, 'color'), function():Dynamic return cast 16711680.0);
    alpha = FlightRuntime.coalesce(FlightRuntime.field(effect, 'alpha'), function():Dynamic return cast 1.0);
    strength = FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 1.0);
    quality = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(effect, 'quality'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    sourceMode = FlightRuntime.coalesce(FlightRuntime.field(effect, 'sourceMode'), function():Dynamic return cast 'draw');
    tintStrength = FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, strength] : Array<Dynamic>));
    glowPasses = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'floor', cast ([strength] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectTintPass, cast ([state, src, mask, color, alpha, tintStrength] : Array<Dynamic>));
    FlightRuntime.callValue(applyWgpuEffectBoxBlur, cast ([state, mask, blurred, blurTemp, { blurX: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurX'), function():Dynamic return cast 6.0), blurY: FlightRuntime.coalesce(FlightRuntime.field(effect, 'blurY'), function():Dynamic return cast 6.0), passes: quality }] : Array<Dynamic>));
    FlightRuntime.callValue(clearWgpuEffectTarget, cast ([state, dst] : Array<Dynamic>));
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, glowPasses, '<'))) {
        FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, blurred, dst] : Array<Dynamic>));
        i++;
      }
    }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'knockout'))) {
  FlightRuntime.callValue(applyWgpuEffectErasePass, cast ([state, src, dst] : Array<Dynamic>));
} else { if (FlightRuntime.truthy(FlightRuntime.strictEquals(sourceMode, 'draw'))) {
  FlightRuntime.callValue(applyWgpuEffectBlitPass, cast ([state, src, dst] : Array<Dynamic>));
} }
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, mask] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, blurred] : Array<Dynamic>));
    FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([pool, blurTemp] : Array<Dynamic>));
  }

  @:keep public static function applyOutlineEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:OutlineEffect):Void {
    var threshold:Dynamic = cast FlightRuntime.UNDEFINED;
    var thickness:Dynamic = cast FlightRuntime.UNDEFINED;
    var color:Dynamic = cast FlightRuntime.UNDEFINED;
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var g:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    threshold = FlightRuntime.coalesce(FlightRuntime.field(effect, 'threshold'), function():Dynamic return cast 0.2);
    thickness = FlightRuntime.coalesce(FlightRuntime.field(effect, 'thickness'), function():Dynamic return cast 1.0);
    color = FlightRuntime.coalesce(FlightRuntime.field(effect, 'color'), function():Dynamic return cast 255.0);
    r = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(color), Std.int(24.0))) & Std.int(255.0)) / 255.0);
    g = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(color), Std.int(16.0))) & Std.int(255.0)) / 255.0);
    b = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(color), Std.int(8.0))) & Std.int(255.0)) / 255.0);
    a = ((Std.int(color) & Std.int(255.0)) / 255.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.outline', EffectsWgpu.OUTLINE_FRAGMENT_WGSL__wgpuOutlineEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, threshold);
  FlightRuntime.setIndex(f32, 1.0, thickness);
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
  FlightRuntime.setIndex(f32, 4.0, r);
  FlightRuntime.setIndex(f32, 5.0, g);
  FlightRuntime.setIndex(f32, 6.0, b);
  FlightRuntime.setIndex(f32, 7.0, a);
}] : Array<Dynamic>));
  }

  public static final applyPipelines__wgpuGradientBevelEffect:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  @:keep public static function applyPixelateEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:PixelateEffect):Void {
    var size:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    size = FlightRuntime.coalesce(FlightRuntime.field(effect, 'size'), function():Dynamic return cast 8.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.pixelate', EffectsWgpu.PIXELATE_FRAGMENT_WGSL__wgpuPixelateEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, size] : Array<Dynamic>)));
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyPosterizeEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:PosterizeEffect):Void {
    var levels:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    levels = FlightRuntime.callProperty(HxMath, 'max', cast ([2.0, FlightRuntime.coalesce(FlightRuntime.field(effect, 'levels'), function():Dynamic return cast 8.0)] : Array<Dynamic>));
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'colorGrade.posterize', EffectsWgpu.POSTERIZE_FRAGMENT_WGSL__wgpuPosterizeEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, levels);
}] : Array<Dynamic>));
  }

  @:keep public static function applyRadialBlurEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:RadialBlurEffect):Void {
    var centerX:Dynamic = cast FlightRuntime.UNDEFINED;
    var centerY:Dynamic = cast FlightRuntime.UNDEFINED;
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var samples:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    centerX = FlightRuntime.coalesce(FlightRuntime.field(effect, 'centerX'), function():Dynamic return cast 0.5);
    centerY = FlightRuntime.coalesce(FlightRuntime.field(effect, 'centerY'), function():Dynamic return cast 0.5);
    strength = FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 0.2);
    samples = FlightRuntime.coalesce(FlightRuntime.field(effect, 'samples'), function():Dynamic return cast 16.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'motion.radialBlur', EffectsWgpu.RADIAL_BLUR_FRAGMENT_WGSL__wgpuRadialBlurEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, centerX);
  FlightRuntime.setIndex(f32, 1.0, centerY);
  FlightRuntime.setIndex(f32, 2.0, strength);
  FlightRuntime.setIndex(f32, 3.0, samples);
}] : Array<Dynamic>));
  }

  @:keep public static function applyScanlinesEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:ScanlinesEffect):Void {
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    count = FlightRuntime.coalesce(FlightRuntime.field(effect, 'count'), function():Dynamic return cast 240.0);
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 0.3);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.scanlines', EffectsWgpu.SCANLINES_FRAGMENT_WGSL__wgpuScanlinesEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, count);
  FlightRuntime.setIndex(f32, 1.0, intensity);
}] : Array<Dynamic>));
  }

  @:keep public static function applyScreenSpaceFogEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:ScreenSpaceFogEffect):Void {
    var packed:Dynamic = cast FlightRuntime.UNDEFINED;
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var g:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var density:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    packed = FlightRuntime.coalesce(FlightRuntime.field(effect, 'color'), function():Dynamic return cast 3369262335.0);
    r = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(packed), Std.int(24.0))) & Std.int(255.0)) / 255.0);
    g = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(packed), Std.int(16.0))) & Std.int(255.0)) / 255.0);
    b = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(packed), Std.int(8.0))) & Std.int(255.0)) / 255.0);
    density = FlightRuntime.coalesce(FlightRuntime.field(effect, 'density'), function():Dynamic return cast 1.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'atmospheric.screenSpaceFog', EffectsWgpu.SCREEN_SPACE_FOG_FRAGMENT_WGSL__wgpuScreenSpaceFogEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, density);
  FlightRuntime.setIndex(f32, 4.0, r);
  FlightRuntime.setIndex(f32, 5.0, g);
  FlightRuntime.setIndex(f32, 6.0, b);
}] : Array<Dynamic>));
  }

  @:keep public static function applySharpenEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:SharpenEffect):Void {
    var amount:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    amount = FlightRuntime.coalesce(FlightRuntime.field(effect, 'amount'), function():Dynamic return cast 0.5);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.sharpen', EffectsWgpu.SHARPEN_FRAGMENT_WGSL__wgpuSharpenEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, amount);
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applySketchEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:SketchEffect):Void {
    var strength:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    strength = FlightRuntime.coalesce(FlightRuntime.field(effect, 'strength'), function():Dynamic return cast 1.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'stylization.sketch', EffectsWgpu.SKETCH_FRAGMENT_WGSL__wgpuSketchEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, strength);
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applySmaaEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:SmaaEffect):Void {
    var threshold:Dynamic = cast FlightRuntime.UNDEFINED;
    var width:Dynamic = cast FlightRuntime.UNDEFINED;
    var height:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    threshold = FlightRuntime.coalesce(FlightRuntime.field(effect, 'threshold'), function():Dynamic return cast 0.1);
    width = FlightRuntime.field(source, 'width');
    height = FlightRuntime.field(source, 'height');
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'antialiasing.smaa', EffectsWgpu.SMAA_FRAGMENT_WGSL__wgpuSmaaEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, width);
  FlightRuntime.setIndex(f32, 1.0, height);
  FlightRuntime.setIndex(f32, 2.0, threshold);
}] : Array<Dynamic>));
  }

  @:keep public static function applySsaoEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:SsaoEffect):Void {
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    radius = FlightRuntime.coalesce(FlightRuntime.field(effect, 'radius'), function():Dynamic return cast 1.0);
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 1.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'atmospheric.ssao', EffectsWgpu.SSAO_FRAGMENT_WGSL__wgpuSsaoEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, radius);
  FlightRuntime.setIndex(f32, 1.0, intensity);
  FlightRuntime.setIndex(f32, 2.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applySsrEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:SsrEffect):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'atmospheric.ssr', EffectsWgpu.SSR_FRAGMENT_WGSL__wgpuSsrEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function() {

}] : Array<Dynamic>));
  }

  @:keep public static function applyTaaEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, _effect:TaaEffect):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'antialiasing.taa', EffectsWgpu.TAA_FRAGMENT_WGSL__wgpuTaaEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, EffectsWgpu._noopSetUniforms__wgpuTaaEffect] : Array<Dynamic>));
  }

  @:keep public static function applyTiltShiftEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:TiltShiftEffect):Void {
    var center:Dynamic = cast FlightRuntime.UNDEFINED;
    var width:Dynamic = cast FlightRuntime.UNDEFINED;
    var blur:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    center = FlightRuntime.coalesce(FlightRuntime.field(effect, 'center'), function():Dynamic return cast 0.5);
    width = FlightRuntime.coalesce(FlightRuntime.field(effect, 'width'), function():Dynamic return cast 0.3);
    blur = FlightRuntime.coalesce(FlightRuntime.field(effect, 'blur'), function():Dynamic return cast 4.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'lens.tiltShift', EffectsWgpu.TILT_SHIFT_FRAGMENT_WGSL__wgpuTiltShiftEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, center);
  FlightRuntime.setIndex(f32, 1.0, width);
  FlightRuntime.setIndex(f32, 2.0, blur);
  FlightRuntime.setIndex(f32, 4.0, FlightRuntime.field(source, 'width'));
  FlightRuntime.setIndex(f32, 5.0, FlightRuntime.field(source, 'height'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyToneMapEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:ToneMapEffect):Void {
    var operator_:Dynamic = cast FlightRuntime.UNDEFINED;
    var exposure:Dynamic = cast FlightRuntime.UNDEFINED;
    var white:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    operator_ = FlightRuntime.coalesce(FlightRuntime.field(effect, 'operator'), function():Dynamic return cast 'aces');
    exposure = FlightRuntime.coalesce(FlightRuntime.field(effect, 'exposure'), function():Dynamic return cast 1.0);
    white = FlightRuntime.coalesce(FlightRuntime.field(effect, 'white'), function():Dynamic return cast 1.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'toneMap.' + Std.string(operator_) + '', FlightRuntime.callValue(EffectsWgpu.buildToneMapFragment__wgpuToneMapEffect, cast ([operator_] : Array<Dynamic>)), 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, exposure);
  FlightRuntime.setIndex(f32, 1.0, white);
}] : Array<Dynamic>));
  }

  @:keep public static function applyVignetteEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:VignetteEffect):Void {
    var intensity:Dynamic = cast FlightRuntime.UNDEFINED;
    var radius:Dynamic = cast FlightRuntime.UNDEFINED;
    var softness:Dynamic = cast FlightRuntime.UNDEFINED;
    var color:Dynamic = cast FlightRuntime.UNDEFINED;
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var g:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    intensity = FlightRuntime.coalesce(FlightRuntime.field(effect, 'intensity'), function():Dynamic return cast 1.0);
    radius = FlightRuntime.coalesce(FlightRuntime.field(effect, 'radius'), function():Dynamic return cast 0.75);
    softness = FlightRuntime.coalesce(FlightRuntime.field(effect, 'softness'), function():Dynamic return cast 0.45);
    color = FlightRuntime.coalesce(FlightRuntime.field(effect, 'color'), function():Dynamic return cast 255.0);
    r = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(color), Std.int(24.0))) & Std.int(255.0)) / 255.0);
    g = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(color), Std.int(16.0))) & Std.int(255.0)) / 255.0);
    b = ((Std.int(FlightRuntime.unsignedShiftRight(Std.int(color), Std.int(8.0))) & Std.int(255.0)) / 255.0);
    a = ((Std.int(color) & Std.int(255.0)) / 255.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'lens.vignette', EffectsWgpu.VIGNETTE_FRAGMENT_WGSL__wgpuVignetteEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, intensity);
  FlightRuntime.setIndex(f32, 1.0, radius);
  FlightRuntime.setIndex(f32, 2.0, softness);
  FlightRuntime.setIndex(f32, 4.0, r);
  FlightRuntime.setIndex(f32, 5.0, g);
  FlightRuntime.setIndex(f32, 6.0, b);
  FlightRuntime.setIndex(f32, 7.0, a);
}] : Array<Dynamic>));
  }

  @:keep public static function applyWgpuBevelCompositePass__wgpuBevelEffect(state:WgpuRenderState, field:WgpuRenderTarget, source:WgpuRenderTarget, dest:WgpuRenderTarget, params:BevelCompositeParams__wgpuBevelEffect):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(EffectsWgpu.getWgpuBevelCompositeShader__wgpuBevelEffect, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuDualSourceEffectPass, cast ([state, field, source, dest, pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, ((Std.int((Std.int(FlightRuntime.field(params, 'highlightColor')) >> Std.int(16.0))) & Std.int(255.0)) / 255.0));
  FlightRuntime.setIndex(f32, 1.0, ((Std.int((Std.int(FlightRuntime.field(params, 'highlightColor')) >> Std.int(8.0))) & Std.int(255.0)) / 255.0));
  FlightRuntime.setIndex(f32, 2.0, ((Std.int(FlightRuntime.field(params, 'highlightColor')) & Std.int(255.0)) / 255.0));
  FlightRuntime.setIndex(f32, 3.0, FlightRuntime.field(params, 'highlightAlpha'));
  FlightRuntime.setIndex(f32, 4.0, ((Std.int((Std.int(FlightRuntime.field(params, 'shadowColor')) >> Std.int(16.0))) & Std.int(255.0)) / 255.0));
  FlightRuntime.setIndex(f32, 5.0, ((Std.int((Std.int(FlightRuntime.field(params, 'shadowColor')) >> Std.int(8.0))) & Std.int(255.0)) / 255.0));
  FlightRuntime.setIndex(f32, 6.0, ((Std.int(FlightRuntime.field(params, 'shadowColor')) & Std.int(255.0)) / 255.0));
  FlightRuntime.setIndex(f32, 7.0, FlightRuntime.field(params, 'shadowAlpha'));
  FlightRuntime.setIndex(f32, 8.0, FlightRuntime.field(params, 'offsetX'));
  FlightRuntime.setIndex(f32, 9.0, FlightRuntime.field(params, 'offsetY'));
  FlightRuntime.setIndex(f32, 10.0, FlightRuntime.field(params, 'intensity'));
  FlightRuntime.setIndex(f32, 11.0, FlightRuntime.field(params, 'clipMode'));
}] : Array<Dynamic>));
  }

  @:keep public static function applyWgpuEffectBlitOffsetPass(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, dx:Float, dy:Float):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(EffectsWgpu.getWgpuBlitOffsetShader__wgpuEffectBlitShader, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, source, dest, pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, (-dx / FlightRuntime.field(source, 'width')));
  FlightRuntime.setIndex(f32, 1.0, (dy / FlightRuntime.field(source, 'height')));
}] : Array<Dynamic>));
  }

  @:keep public static function applyWgpuEffectBlitPass(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(EffectsWgpu.getWgpuBlitShader__wgpuEffectBlitShader, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, source, dest, pipeline, function() {

}] : Array<Dynamic>));
  }

  @:keep public static function applyWgpuEffectBoxBlur(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, temp:WgpuRenderTarget, options:{ @:optional var blurX:Float; @:optional var blurY:Float; @:optional var passes:Float; @:optional var edgeColor:Array<Float>; }):Void {
    var passes:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurX:Dynamic = cast FlightRuntime.UNDEFINED;
    var blurY:Dynamic = cast FlightRuntime.UNDEFINED;
    var edgeColor:Dynamic = cast FlightRuntime.UNDEFINED;
    var read:WgpuRenderTarget = cast FlightRuntime.UNDEFINED;
    var write:WgpuRenderTarget = cast FlightRuntime.UNDEFINED;
    passes = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'round', cast ([FlightRuntime.coalesce(FlightRuntime.field(options, 'passes'), function():Dynamic return cast 1.0)] : Array<Dynamic>))] : Array<Dynamic>));
    blurX = FlightRuntime.coalesce(FlightRuntime.field(options, 'blurX'), function():Dynamic return cast 4.0);
    blurY = FlightRuntime.coalesce(FlightRuntime.field(options, 'blurY'), function():Dynamic return cast 4.0);
    edgeColor = FlightRuntime.field(options, 'edgeColor');
    read = source;
    write = temp;
    {
      var pass:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(pass, passes, '<'))) {
        var radiusX:Dynamic = FlightRuntime.callValue(computeBoxBlurPassRadius, cast ([blurX, passes, pass] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.compare(radiusX, 0.0, '>'))) {
  FlightRuntime.callValue(EffectsWgpu.applyBoxBlurPass__wgpuEffectBoxBlur, cast ([state, read, write, radiusX, 1.0, 0.0, edgeColor] : Array<Dynamic>));
  (read = cast (write : Dynamic));
  (write = cast (FlightRuntime.select(FlightRuntime.strictEquals(write, temp), function():Dynamic return cast dest, function():Dynamic return cast temp) : Dynamic));
}
        var radiusY:Dynamic = FlightRuntime.callValue(computeBoxBlurPassRadius, cast ([blurY, passes, pass] : Array<Dynamic>));
        if (FlightRuntime.truthy(FlightRuntime.compare(radiusY, 0.0, '>'))) {
  FlightRuntime.callValue(EffectsWgpu.applyBoxBlurPass__wgpuEffectBoxBlur, cast ([state, read, write, radiusY, 0.0, 1.0, edgeColor] : Array<Dynamic>));
  (read = cast (write : Dynamic));
  (write = cast (FlightRuntime.select(FlightRuntime.strictEquals(write, temp), function():Dynamic return cast dest, function():Dynamic return cast temp) : Dynamic));
}
        pass++;
      }
    }
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(read, dest))) {
  FlightRuntime.callValue(EffectsWgpu.applyBoxBlurPass__wgpuEffectBoxBlur, cast ([state, read, dest, 0.0, 0.0, 0.0, FlightRuntime.UNDEFINED] : Array<Dynamic>));
}
  }

  @:keep public static function applyWgpuEffectErasePass(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(EffectsWgpu.getWgpuEraseShader__wgpuEffectBlitShader, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, source, dest, pipeline, function() {

}] : Array<Dynamic>));
  }

  @:keep public static function applyWgpuEffectInnerClipPass(state:WgpuRenderState, glow:WgpuRenderTarget, source:WgpuRenderTarget, dest:WgpuRenderTarget):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(EffectsWgpu.getWgpuInnerClipShader__wgpuEffectTintShader, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuDualSourceEffectPass, cast ([state, glow, source, dest, pipeline, function() {

}] : Array<Dynamic>));
  }

  @:keep public static function applyWgpuEffectInvertTintPass(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, color:Float, alpha:Float, strength:Float):Void {
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var g:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure0 = FlightRuntime.callValue(EffectsWgpu.packColor__wgpuEffectTintShader, cast ([color] : Array<Dynamic>));
    r = FlightRuntime.getIndex(__destructure0, 0.0);
    g = FlightRuntime.getIndex(__destructure0, 1.0);
    b = FlightRuntime.getIndex(__destructure0, 2.0);
    pipeline = FlightRuntime.callValue(EffectsWgpu.getWgpuInvertTintShader__wgpuEffectTintShader, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, source, dest, pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, r);
  FlightRuntime.setIndex(f32, 1.0, g);
  FlightRuntime.setIndex(f32, 2.0, b);
  FlightRuntime.setIndex(f32, 3.0, alpha);
  FlightRuntime.setIndex(f32, 4.0, strength);
}] : Array<Dynamic>));
  }

  @:keep public static function applyWgpuEffectTintPass(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, color:Float, alpha:Float, strength:Float):Void {
    var __destructure1:Dynamic = cast FlightRuntime.UNDEFINED;
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var g:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure1 = FlightRuntime.callValue(EffectsWgpu.packColor__wgpuEffectTintShader, cast ([color] : Array<Dynamic>));
    r = FlightRuntime.getIndex(__destructure1, 0.0);
    g = FlightRuntime.getIndex(__destructure1, 1.0);
    b = FlightRuntime.getIndex(__destructure1, 2.0);
    pipeline = FlightRuntime.callValue(EffectsWgpu.getWgpuTintShader__wgpuEffectTintShader, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, source, dest, pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, r);
  FlightRuntime.setIndex(f32, 1.0, g);
  FlightRuntime.setIndex(f32, 2.0, b);
  FlightRuntime.setIndex(f32, 3.0, alpha);
  FlightRuntime.setIndex(f32, 4.0, strength);
}] : Array<Dynamic>));
  }

  @:keep public static function applyWgpuGaussianBlurPass__wgpuBlurEffect(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, sigma:Float, radius:Float, dirX:Float, dirY:Float):Void {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'blur.gaussian', EffectsWgpu.GAUSSIAN_BLUR_WGSL__wgpuBlurEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, (1.0 / FlightRuntime.field(source, 'width')));
  FlightRuntime.setIndex(f32, 1.0, (1.0 / FlightRuntime.field(source, 'height')));
  FlightRuntime.setIndex(f32, 2.0, dirX);
  FlightRuntime.setIndex(f32, 3.0, dirY);
  FlightRuntime.setIndex(f32, 4.0, radius);
  FlightRuntime.setIndex(f32, 5.0, sigma);
}] : Array<Dynamic>));
  }

  @:keep public static function applyWhiteBalanceEffectToWgpu(state:WgpuRenderState, source:WgpuRenderTarget, dest:WgpuRenderTarget, effect:WhiteBalanceEffect):Void {
    var temperature:Dynamic = cast FlightRuntime.UNDEFINED;
    var tint:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    temperature = FlightRuntime.coalesce(FlightRuntime.field(effect, 'temperature'), function():Dynamic return cast 0.0);
    tint = FlightRuntime.coalesce(FlightRuntime.field(effect, 'tint'), function():Dynamic return cast 0.0);
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'colorGrade.whiteBalance', EffectsWgpu.WHITE_BALANCE_FRAGMENT_WGSL__wgpuWhiteBalanceEffect, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), (cast dest : WgpuRenderTarget), pipeline, function(f32:Dynamic) {
  FlightRuntime.setIndex(f32, 0.0, temperature);
  FlightRuntime.setIndex(f32, 1.0, tint);
}] : Array<Dynamic>));
  }

  @:keep public static function beginEffectPass__wgpuEffectPass(state:WgpuRenderState, dest:Null<WgpuRenderTarget>, loadOp:Dynamic):Dynamic {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var view:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'commandEncoder'), null))) { throw FlightRuntime.error('No active command encoder — call renderWgpuBackground first'); }
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'renderPass'), null))) {
  FlightRuntime.callProperty(FlightRuntime.field(runtime, 'renderPass'), 'end', cast ([] : Array<Dynamic>));
  FlightRuntime.setField(runtime, 'renderPass', null);
}
    view = FlightRuntime.select(!FlightRuntime.strictEquals(dest, null), function():Dynamic return cast FlightRuntime.field(dest, 'view'), function():Dynamic return cast FlightRuntime.field(runtime, 'canvasTextureView'));
    pass = FlightRuntime.callProperty(FlightRuntime.field(runtime, 'commandEncoder'), 'beginRenderPass', cast ([{ colorAttachments: cast ([{ view: view, loadOp: loadOp, storeOp: 'store', clearValue: { r: 0.0, g: 0.0, b: 0.0, a: 0.0 } }] : Array<Dynamic>) }] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(dest, null))) {
  FlightRuntime.callProperty(pass, 'setViewport', cast ([0.0, 0.0, FlightRuntime.field(dest, 'width'), FlightRuntime.field(dest, 'height'), 0.0, 1.0] : Array<Dynamic>));
} else {
  var w:Dynamic = FlightRuntime.coalesce(FlightRuntime.optionalField(FlightRuntime.field(runtime, 'renderTargetViewport'), 'width'), function():Dynamic return cast FlightRuntime.field(FlightRuntime.field(state, 'canvas'), 'width'));
  var h:Dynamic = FlightRuntime.coalesce(FlightRuntime.optionalField(FlightRuntime.field(runtime, 'renderTargetViewport'), 'height'), function():Dynamic return cast FlightRuntime.field(FlightRuntime.field(state, 'canvas'), 'height'));
  FlightRuntime.callProperty(pass, 'setViewport', cast ([0.0, 0.0, w, h, 0.0, 1.0] : Array<Dynamic>));
}
    return cast pass;
    return cast null;
  }

  @:keep public static function beginWgpuRenderEffectPipeline(state:WgpuRenderState, pipeline:WgpuRenderEffectPipeline):Void {
    var w:Dynamic = cast FlightRuntime.UNDEFINED;
    var h:Dynamic = cast FlightRuntime.UNDEFINED;
    var format:Dynamic = cast FlightRuntime.UNDEFINED;
    var rgba:Dynamic = cast FlightRuntime.UNDEFINED;
    w = FlightRuntime.field(FlightRuntime.field(state, 'canvas'), 'width');
    h = FlightRuntime.field(FlightRuntime.field(state, 'canvas'), 'height');
    format = FlightRuntime.select(FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(pipeline, 'options'), 'format'), 'rgba16f'), function():Dynamic return cast 'rgba16float', function():Dynamic return cast FlightRuntime.field(state, 'format'));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(pipeline, 'sceneTarget'), null))) {
  FlightRuntime.setField(pipeline, 'sceneTarget', FlightRuntime.callValue(createWgpuRenderTarget, cast ([state, w, h, format] : Array<Dynamic>)));
} else {
  FlightRuntime.callValue(resizeWgpuRenderTarget, cast ([state, FlightRuntime.field(pipeline, 'sceneTarget'), w, h] : Array<Dynamic>));
}
    rgba = FlightRuntime.field(state, 'backgroundColorRgba');
    FlightRuntime.setField(FlightRuntime.field(pipeline, 'sceneTarget'), 'clearColors', FlightRuntime.select(FlightRuntime.andValue(!FlightRuntime.strictEquals(rgba, FlightRuntime.UNDEFINED), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(rgba, 'length'), 4.0, '>=')), function():Dynamic return cast cast ([FlightRuntime.callValue(EffectsWgpu.packBackgroundClearColor__wgpuRenderEffectPipeline, cast ([rgba] : Array<Dynamic>))] : Array<Dynamic>), function():Dynamic return cast cast ([] : Array<Dynamic>)));
    FlightRuntime.callValue(beginWgpuRenderPass, cast ([state, FlightRuntime.field(pipeline, 'sceneTarget')] : Array<Dynamic>));
  }

  public static final BEVEL_APPLY_FRAGMENT_WGSL__wgpuGradientBevelEffect:Dynamic = '\nstruct Uniforms { _u : f32, _pad0 : f32, _pad1 : f32, _pad2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var texEncoded : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n@group(2) @binding(0) var texRamp : texture_2d<f32>;\n@group(2) @binding(1) var smp2 : sampler;\n@group(3) @binding(0) var texSource : texture_2d<f32>;\n@group(3) @binding(1) var smp3 : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let bevelVal = textureSampleLevel(texEncoded, smp, uv, 0.0).r;\n  let color = textureSampleLevel(texRamp, smp2, vec2f(bevelVal, 0.5), 0.0);\n  let srcAlpha = textureSampleLevel(texSource, smp3, uv, 0.0).a;\n  return color * srcAlpha;\n}';

  public static final BEVEL_COMPOSITE_WGSL__wgpuBevelEffect:Dynamic = '\nstruct Uniforms {\n  highlight : vec4f,\n  shadow : vec4f,\n  offset : vec2f,\n  intensity : f32,\n  clipMode : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var fieldTex : texture_2d<f32>;\n@group(1) @binding(1) var fieldSmp : sampler;\n@group(2) @binding(0) var srcTex : texture_2d<f32>;\n@group(2) @binding(1) var srcSmp : sampler;\n\nfn sampleField(uv : vec2f) -> f32 {\n  if (uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) { return 0.0; }\n  return textureSampleLevel(fieldTex, fieldSmp, uv, 0.0).a;\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let lit = sampleField(uv - uni.offset);\n  let shade = sampleField(uv + uni.offset);\n  let gradient = lit - shade;\n  let srcA = textureSampleLevel(srcTex, srcSmp, uv, 0.0).a;\n  let isHighlight = gradient >= 0.0;\n  let color = select(uni.shadow.xyz, uni.highlight.xyz, isHighlight);\n  let colorAlpha = select(uni.shadow.w, uni.highlight.w, isHighlight);\n  var clip = 1.0;\n  if (uni.clipMode == 1.0) { clip = srcA; }\n  else if (uni.clipMode == 2.0) { clip = 1.0 - srcA; }\n  let edge = min(1.0, abs(gradient) * uni.intensity);\n  let a = edge * colorAlpha * clip;\n  return vec4f(color * a, a);\n}';

  public static final BEVEL_ENCODE_FRAGMENT_WGSL__wgpuGradientBevelEffect:Dynamic = '\nstruct Uniforms {\n  offset : vec2f,\n  _pad : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let high = textureSampleLevel(tex, smp, uv - uni.offset, 0.0).a;\n  let low  = textureSampleLevel(tex, smp, uv + uni.offset, 0.0).a;\n  let bevelVal = clamp((high - low) * 0.5 + 0.5, 0.0, 1.0);\n  return vec4f(bevelVal, 0.0, 0.0, 1.0);\n}';

  public static final bevelCompositePipelines__wgpuBevelEffect:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final BLIT_OFFSET_WGSL__wgpuEffectBlitShader:Dynamic = '\nstruct Uniforms {\n  offset : vec2f,\n  _pad : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let shifted = uv + uni.offset;\n  if (shifted.x < 0.0 || shifted.x > 1.0 || shifted.y < 0.0 || shifted.y > 1.0) {\n    return vec4f(0.0);\n  }\n  return textureSampleLevel(tex, smp, shifted, 0.0);\n}';

  public static final BLIT_WGSL__wgpuEffectBlitShader:Dynamic = '\nstruct Uniforms { _u : f32, _pad0 : f32, _pad1 : f32, _pad2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  return textureSampleLevel(tex, smp, uv, 0.0);\n}';

  public static final blitOffsetPipelines__wgpuEffectBlitShader:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final blitPipelines__wgpuEffectBlitShader:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final BLOOM_BRIGHT_FRAGMENT_WGSL__wgpuBloomEffect:Dynamic = '\nstruct Uniforms { u_threshold : f32, _pad0 : f32, _pad1 : f32, _pad2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let l = dot(c.rgb, vec3f(0.2126, 0.7152, 0.0722));\n  let k = step(uni.u_threshold, l);\n  return vec4f(c.rgb * k, c.a);\n}';

  public static final BLOOM_COMPOSITE_FRAGMENT_WGSL__wgpuBloomEffect:Dynamic = '\nstruct Uniforms { u_intensity : f32, _pad0 : f32, _pad1 : f32, _pad2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex0 : texture_2d<f32>;\n@group(1) @binding(1) var smp0 : sampler;\n@group(2) @binding(0) var tex1 : texture_2d<f32>;\n@group(2) @binding(1) var smp1 : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let scene = textureSampleLevel(tex0, smp0, uv, 0.0);\n  let bloom = textureSampleLevel(tex1, smp1, uv, 0.0);\n  return vec4f(scene.rgb + bloom.rgb * uni.u_intensity, scene.a);\n}';

  public static final BOKEH_DOF_FRAGMENT_WGSL__wgpuBokehDepthOfFieldEffect:Dynamic = '\nstruct Uniforms {\n  u_maxBlur : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = vec2f(1.0) / uni.u_resolution;\n  // No depth texture in WebGPU yet: circle of confusion is fixed, so the disc uses the full radius.\n  let blur = uni.u_maxBlur;\n  var sum = vec4f(0.0);\n  var total = 0.0;\n  for (var i = 0; i < 16; i = i + 1) {\n    let ang = f32(i) * 0.39269908; // golden-ish angular step over the disc\n    let rad = (f32(i % 4) + 1.0) * 0.25;\n    let offset = vec2f(cos(ang), sin(ang)) * rad * blur * texel;\n    sum = sum + textureSampleLevel(tex, smp, uv + offset, 0.0);\n    total = total + 1.0;\n  }\n  return sum / total;\n}';

  public static final BOX_BLUR_WGSL__wgpuEffectBoxBlur:Dynamic = '\nstruct Uniforms {\n  texelSize : vec2f,\n  direction : vec2f,\n  edgeColor : vec4f,\n  radius : f32,\n  useEdgeColor : f32,\n  _pad0 : f32, _pad1 : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn sampleBlur(uv : vec2f) -> vec4f {\n  if (uni.useEdgeColor > 0.5 && (uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0)) {\n    return uni.edgeColor;\n  }\n  return textureSampleLevel(tex, smp, uv, 0.0);\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let r = i32(uni.radius);\n  if (r == 0) { return sampleBlur(uv); }\n  var sum = vec4f(0.0);\n  let count = f32(2 * r + 1);\n  for (var i = -r; i <= r; i++) {\n    sum += sampleBlur(uv + f32(i) * uni.texelSize * uni.direction);\n  }\n  return sum / count;\n}';

  public static final boxBlurPipelines__wgpuEffectBoxBlur:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  @:keep public static function buildGodRaysFragment__wgpuGodRaysEffect(samples:Float):String {
    return cast ((EffectsWgpu.GOD_RAYS_FRAGMENT_HEAD__wgpuGodRaysEffect + FlightRuntime.callProperty(samples, 'toString', cast ([] : Array<Dynamic>))) + EffectsWgpu.GOD_RAYS_FRAGMENT_TAIL__wgpuGodRaysEffect);
    return cast null;
  }

  @:keep public static function buildRampData__wgpuEffectGradientRamp(colors:Array<Float>, alphas:Array<Float>, ratios:Array<Float>):Dynamic {
    var out:Dynamic = cast FlightRuntime.UNDEFINED;
    out = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Uint8ClampedArray'] : Array<Dynamic>)), [(256.0 * 4.0)]);
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(colors, 'length'), 0.0))) { return cast out; }
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, 256.0, '<'))) {
        var t:Dynamic = i;
        var r:Dynamic = 0.0;
        var g:Dynamic = 0.0;
        var b:Dynamic = 0.0;
        var a:Dynamic = 0.0;
        if (FlightRuntime.truthy(FlightRuntime.compare(t, FlightRuntime.getIndex(ratios, 0.0), '<='))) {
  var c:Dynamic = FlightRuntime.getIndex(colors, 0.0);
  (r = cast ((Std.int((Std.int(c) >> Std.int(16.0))) & Std.int(255.0)) : Dynamic));
  (g = cast ((Std.int((Std.int(c) >> Std.int(8.0))) & Std.int(255.0)) : Dynamic));
  (b = cast ((Std.int(c) & Std.int(255.0)) : Dynamic));
  (a = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.getIndex(alphas, 0.0) * 255.0)] : Array<Dynamic>)) : Dynamic));
} else { if (FlightRuntime.truthy(FlightRuntime.compare(t, FlightRuntime.getIndex(ratios, (FlightRuntime.field(ratios, 'length') - 1.0)), '>='))) {
  var c:Dynamic = FlightRuntime.getIndex(colors, (FlightRuntime.field(colors, 'length') - 1.0));
  (r = cast ((Std.int((Std.int(c) >> Std.int(16.0))) & Std.int(255.0)) : Dynamic));
  (g = cast ((Std.int((Std.int(c) >> Std.int(8.0))) & Std.int(255.0)) : Dynamic));
  (b = cast ((Std.int(c) & Std.int(255.0)) : Dynamic));
  (a = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.getIndex(alphas, (FlightRuntime.field(alphas, 'length') - 1.0)) * 255.0)] : Array<Dynamic>)) : Dynamic));
} else {
  {
    var j:Dynamic = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(j, (FlightRuntime.field(ratios, 'length') - 1.0), '<'))) {
      var r0:Dynamic = FlightRuntime.getIndex(ratios, j);
      var r1:Dynamic = FlightRuntime.getIndex(ratios, (j + 1.0));
      if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(t, r0, '>='), function():Dynamic return cast FlightRuntime.compare(t, r1, '<=')))) {
  var blend:Dynamic = FlightRuntime.select(FlightRuntime.compare(r1, r0, '>'), function():Dynamic return cast ((t - r0) / (r1 - r0)), function():Dynamic return cast 0.0);
  var c0:Dynamic = FlightRuntime.getIndex(colors, j);
  var c1:Dynamic = FlightRuntime.getIndex(colors, (j + 1.0));
  (r = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([(((Std.int((Std.int(c0) >> Std.int(16.0))) & Std.int(255.0)) * (1.0 - blend)) + ((Std.int((Std.int(c1) >> Std.int(16.0))) & Std.int(255.0)) * blend))] : Array<Dynamic>)) : Dynamic));
  (g = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([(((Std.int((Std.int(c0) >> Std.int(8.0))) & Std.int(255.0)) * (1.0 - blend)) + ((Std.int((Std.int(c1) >> Std.int(8.0))) & Std.int(255.0)) * blend))] : Array<Dynamic>)) : Dynamic));
  (b = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([(((Std.int(c0) & Std.int(255.0)) * (1.0 - blend)) + ((Std.int(c1) & Std.int(255.0)) * blend))] : Array<Dynamic>)) : Dynamic));
  (a = cast (FlightRuntime.callProperty(HxMath, 'round', cast ([(((FlightRuntime.getIndex(alphas, j) * 255.0) * (1.0 - blend)) + ((FlightRuntime.getIndex(alphas, (j + 1.0)) * 255.0) * blend))] : Array<Dynamic>)) : Dynamic));
  break;
}
      j++;
    }
  }
} }
        FlightRuntime.setIndex(out, (i * 4.0), r);
        FlightRuntime.setIndex(out, ((i * 4.0) + 1.0), g);
        FlightRuntime.setIndex(out, ((i * 4.0) + 2.0), b);
        FlightRuntime.setIndex(out, ((i * 4.0) + 3.0), a);
        i++;
      }
    }
    return cast out;
    return cast null;
  }

  @:keep public static function buildToneMapFragment__wgpuToneMapEffect(operator_:String):String {
    return cast ((EffectsWgpu.TONEMAP_FRAGMENT_HEAD__wgpuToneMapEffect + FlightRuntime.coalesce(FlightRuntime.getIndex(EffectsWgpu.TONEMAP_OPERATORS__wgpuToneMapEffect, operator_), function():Dynamic return cast EffectsWgpu.TONEMAP_OPERATORS__wgpuToneMapEffect.aces)) + EffectsWgpu.TONEMAP_FRAGMENT_TAIL__wgpuToneMapEffect);
    return cast null;
  }

  public static final CAMERA_MOTION_BLUR_FRAGMENT_WGSL__wgpuCameraMotionBlurEffect:Dynamic = '\nstruct Uniforms {\n  u_intensity : f32,\n  u_samples : f32,\n  _pad0 : f32,\n  _pad1 : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nconst SAMPLES : i32 = 16;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let toCenter = vec2f(0.5) - uv;\n  let count = min(uni.u_samples, 16.0);\n  var sum = vec4f(0.0);\n  var taken = 0.0;\n  for (var i = 0; i < SAMPLES; i = i + 1) {\n    if (f32(i) >= count) { break; }\n    let t = select(0.0, f32(i) / (count - 1.0), count > 1.0);\n    let p = uv + toCenter * (t * uni.u_intensity);\n    sum = sum + textureSampleLevel(tex, smp, p, 0.0);\n    taken = taken + 1.0;\n  }\n  return sum / max(taken, 1.0);\n}';

  public static final CHROMATIC_ABERRATION_FRAGMENT_WGSL__wgpuChromaticAberrationEffect:Dynamic = '\nstruct Uniforms {\n  u_intensity : f32,\n  u_radial : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let centered = uv - vec2f(0.5);\n  let scale = mix(1.0, length(centered) * 2.0, uni.u_radial);\n  let dir = mix(vec2f(1.0, 0.0), normalize(centered + vec2f(1e-5)), uni.u_radial);\n  let offset = dir * uni.u_intensity * scale;\n  let r = textureSampleLevel(tex, smp, uv + offset, 0.0).r;\n  let g = textureSampleLevel(tex, smp, uv, 0.0).g;\n  let b = textureSampleLevel(tex, smp, uv - offset, 0.0).b;\n  let a = textureSampleLevel(tex, smp, uv, 0.0).a;\n  return vec4f(r, g, b, a);\n}';

  @:keep public static function clamp01__wgpuColorLutPass(v:Float):Float {
    return cast FlightRuntime.select(FlightRuntime.compare(v, 0.0, '<'), function():Dynamic return cast 0.0, function():Dynamic return cast FlightRuntime.select(FlightRuntime.compare(v, 1.0, '>'), function():Dynamic return cast 1.0, function():Dynamic return cast v));
    return cast null;
  }

  @:keep public static function clearWgpuEffectTarget(state:WgpuRenderState, target:WgpuRenderTarget):Void {
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    pass = FlightRuntime.callValue(EffectsWgpu.beginEffectPass__wgpuEffectPass, cast ([state, target, 'clear'] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'end', cast ([] : Array<Dynamic>));
  }

  public static final COLOR_LUT_FRAGMENT_WGSL__wgpuColorLutPass:Dynamic = '\nstruct Uniforms { u_size : f32, _p0 : f32, _p1 : f32, _p2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n@group(2) @binding(0) var lut : texture_3d<f32>;\n@group(2) @binding(1) var lutSmp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let scale = (uni.u_size - 1.0) / uni.u_size;\n  let offset = 0.5 / uni.u_size;\n  let lc = clamp(c.rgb, vec3f(0.0), vec3f(1.0)) * scale + offset;\n  let graded = textureSampleLevel(lut, lutSmp, lc, 0.0).rgb;\n  return vec4f(graded, c.a);\n}';

  public static final COLOR_MATRIX_FRAGMENT_WGSL__wgpuColorMatrixPass:Dynamic = '\nstruct Uniforms { u_row_r : vec4f, u_row_g : vec4f, u_row_b : vec4f, u_row_a : vec4f, u_offset : vec4f, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let nr = uni.u_row_r.x * c.r + uni.u_row_r.y * c.g + uni.u_row_r.z * c.b + uni.u_row_r.w * c.a + uni.u_offset.x / 255.0;\n  let ng = uni.u_row_g.x * c.r + uni.u_row_g.y * c.g + uni.u_row_g.z * c.b + uni.u_row_g.w * c.a + uni.u_offset.y / 255.0;\n  let nb = uni.u_row_b.x * c.r + uni.u_row_b.y * c.g + uni.u_row_b.z * c.b + uni.u_row_b.w * c.a + uni.u_offset.z / 255.0;\n  let na = uni.u_row_a.x * c.r + uni.u_row_a.y * c.g + uni.u_row_a.z * c.b + uni.u_row_a.w * c.a + uni.u_offset.w / 255.0;\n  return vec4f(clamp(vec3f(nr, ng, nb), vec3f(0.0), vec3f(1.0)), na);\n}';

  public static final CRT_FRAGMENT_WGSL__wgpuCrtEffect:Dynamic = '\nstruct Uniforms {\n  u_curvature : f32,\n  u_scanlineIntensity : f32,\n  u_vignette : f32,\n  u_aberration : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn barrel(uv : vec2f) -> vec2f {\n  var c = uv * 2.0 - 1.0;\n  c += c * uni.u_curvature * dot(c, c);\n  return c * 0.5 + 0.5;\n}\n\n@fragment\nfn fs_main(@location(0) uvIn : vec2f) -> @location(0) vec4f {\n  let uv = barrel(uvIn);\n  if (uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) {\n    return vec4f(0.0, 0.0, 0.0, 1.0);\n  }\n  let off = vec2f(uni.u_aberration, 0.0);\n  let r = textureSampleLevel(tex, smp, uv + off, 0.0).r;\n  let g = textureSampleLevel(tex, smp, uv, 0.0).g;\n  let b = textureSampleLevel(tex, smp, uv - off, 0.0).b;\n  let a = textureSampleLevel(tex, smp, uv, 0.0).a;\n  var col = vec3f(r, g, b);\n  let line = sin(uv.y * uni.u_resolution.y * 3.14159265) * 0.5 + 0.5;\n  col *= 1.0 - uni.u_scanlineIntensity * (1.0 - line);\n  let vc = uv * 2.0 - 1.0;\n  col *= 1.0 - uni.u_vignette * dot(vc, vc);\n  return vec4f(col, a);\n}';

  @:keep public static function createWgpuDualSourceEffectPipeline(state:WgpuRenderState, fragmentWGSL:String, blend:WgpuEffectBlendMode = 'premul'):WgpuDualSourceEffectPipeline {
    var fs:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure1:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var shaderModule:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipelineLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var compileForFormat:Dynamic = cast FlightRuntime.UNDEFINED;
    fs = FlightRuntime.callValue(EffectsWgpu.getOrCreateEffectPassState__wgpuEffectPass, cast ([state] : Array<Dynamic>));
    __destructure1 = state;
    device = FlightRuntime.field(__destructure1, 'device');
    shaderModule = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: (EFFECT_VERTEX_WGSL + fragmentWGSL) }] : Array<Dynamic>));
    pipelineLayout = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([FlightRuntime.field(fs, 'uniformBGLayout'), FlightRuntime.field(fs, 'textureBGLayout'), FlightRuntime.field(fs, 'textureBGLayout')] : Array<Dynamic>) }] : Array<Dynamic>));
    compileForFormat = function(format:Dynamic) return FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: pipelineLayout, vertex: { module: shaderModule, entryPoint: 'vs_main' }, fragment: { module: shaderModule, entryPoint: 'fs_main', targets: cast ([{ format: format, blend: FlightRuntime.callValue(EffectsWgpu.getBlendState__wgpuEffectPass, cast ([blend] : Array<Dynamic>)) }] : Array<Dynamic>) }, primitive: { topology: 'triangle-list' } }] : Array<Dynamic>));
    return cast { pipeline: FlightRuntime.callValue(compileForFormat, cast ([FlightRuntime.field(fs, 'format')] : Array<Dynamic>)), blendMode: blend, compileForFormat: compileForFormat, variants: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []) };
    return cast null;
  }

  @:keep public static function createWgpuEffectGradientRampTexture__wgpuEffectGradientRamp(state:WgpuRenderState, colors:Array<Float>, alphas:Array<Float>, ratios:Array<Float>):Dynamic {
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var texture:Dynamic = cast FlightRuntime.UNDEFINED;
    data = FlightRuntime.callValue(EffectsWgpu.buildRampData__wgpuEffectGradientRamp, cast ([colors, alphas, ratios] : Array<Dynamic>));
    __destructure0 = state;
    device = FlightRuntime.field(__destructure0, 'device');
    texture = FlightRuntime.callProperty(device, 'createTexture', cast ([{ size: cast ([256.0, 1.0, 1.0] : Array<Dynamic>), format: 'rgba8unorm', usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUTextureUsage'] : Array<Dynamic>)), 'TEXTURE_BINDING')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUTextureUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(device, 'queue'), 'writeTexture', cast ([{ texture: texture, origin: cast ([0.0, 0.0, 0.0] : Array<Dynamic>) }, (cast FlightRuntime.field(data, 'buffer') : haxe.io.Bytes), { offset: 0.0, bytesPerRow: (256.0 * 4.0) }, cast ([256.0, 1.0, 1.0] : Array<Dynamic>)] : Array<Dynamic>));
    return cast texture;
    return cast null;
  }

  @:keep public static function createWgpuEffectPipeline(state:WgpuRenderState, fragmentWGSL:String, blend:WgpuEffectBlendMode = 'premul'):WgpuEffectPipeline {
    var fs:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure2:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var shaderModule:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipelineLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var compileForFormat:Dynamic = cast FlightRuntime.UNDEFINED;
    fs = FlightRuntime.callValue(EffectsWgpu.getOrCreateEffectPassState__wgpuEffectPass, cast ([state] : Array<Dynamic>));
    __destructure2 = state;
    device = FlightRuntime.field(__destructure2, 'device');
    shaderModule = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: (EFFECT_VERTEX_WGSL + fragmentWGSL) }] : Array<Dynamic>));
    pipelineLayout = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([FlightRuntime.field(fs, 'uniformBGLayout'), FlightRuntime.field(fs, 'textureBGLayout')] : Array<Dynamic>) }] : Array<Dynamic>));
    compileForFormat = function(format:Dynamic) return FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: pipelineLayout, vertex: { module: shaderModule, entryPoint: 'vs_main' }, fragment: { module: shaderModule, entryPoint: 'fs_main', targets: cast ([{ format: format, blend: FlightRuntime.callValue(EffectsWgpu.getBlendState__wgpuEffectPass, cast ([blend] : Array<Dynamic>)) }] : Array<Dynamic>) }, primitive: { topology: 'triangle-list' } }] : Array<Dynamic>));
    return cast { pipeline: FlightRuntime.callValue(compileForFormat, cast ([FlightRuntime.field(fs, 'format')] : Array<Dynamic>)), blendMode: blend, compileForFormat: compileForFormat, variants: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []) };
    return cast null;
  }

  @:keep public static function createWgpuRenderEffectPipeline(_state:WgpuRenderState, ?options:RenderEffectPipelineOptions):WgpuRenderEffectPipeline {
    if (options == null) options = cast ({  } : Dynamic);
    return cast { options: FlightRuntime.mergeObjects([options]), sceneTarget: null, pool: FlightRuntime.callValue(createWgpuRenderTargetPool, cast ([] : Array<Dynamic>)), lutCache: FlightRuntime.callValue(createColorLutCache, cast ([] : Array<Dynamic>)), lutTexture: { texture: null, size: 0.0, lut: null }, velocityTexture: null };
    return cast null;
  }

  public static final defaultWgpuBevelEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyBevelEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), FlightRuntime.field(ctx, 'pool'), (cast effect : BevelEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuBloomEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyBloomEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), FlightRuntime.field(ctx, 'pool'), (cast effect : BloomEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuBlurEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
  var temp:Dynamic = cast FlightRuntime.UNDEFINED;
  descriptor = { width: FlightRuntime.field(FlightRuntime.field(ctx, 'source'), 'width'), height: FlightRuntime.field(FlightRuntime.field(ctx, 'source'), 'height'), format: FlightRuntime.field(FlightRuntime.field(ctx, 'source'), 'format') };
  temp = FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'pool'), descriptor] : Array<Dynamic>));
  FlightRuntime.callValue(applyBlurEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), temp, (cast effect : BlurEffect)] : Array<Dynamic>));
  FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([FlightRuntime.field(ctx, 'pool'), temp] : Array<Dynamic>));
};

  public static final defaultWgpuBokehDepthOfFieldEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyBokehDepthOfFieldEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : BokehDepthOfFieldEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuCameraMotionBlurEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyCameraMotionBlurEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : CameraMotionBlurEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuChromaticAberrationEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyChromaticAberrationEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : ChromaticAberrationEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuConvolutionEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyConvolutionEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : ConvolutionEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuCrtEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyCrtEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : CrtEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuDirectionalBlurEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyDirectionalBlurEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : DirectionalBlurEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuDisplacementEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyDisplacementEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : DisplacementEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuDitherEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyDitherEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : DitherEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuDropShadowEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyDropShadowEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), FlightRuntime.field(ctx, 'pool'), (cast effect : DropShadowEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuFilmGrainEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyFilmGrainEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : FilmGrainEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuFxaaEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyFxaaEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : FxaaEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuGlitchEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyGlitchEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : GlitchEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuGodRaysEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyGodRaysEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : GodRaysEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuGradientBevelEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyGradientBevelEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), FlightRuntime.field(ctx, 'pool'), (cast effect : GradientBevelEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuGradientGlowEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyGradientGlowEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), FlightRuntime.field(ctx, 'pool'), (cast effect : GradientGlowEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuHalftoneEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyHalftoneEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : HalftoneEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuInnerGlowEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyInnerGlowEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), FlightRuntime.field(ctx, 'pool'), (cast effect : InnerGlowEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuInnerShadowEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyInnerShadowEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), FlightRuntime.field(ctx, 'pool'), (cast effect : InnerShadowEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuKuwaharaEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyKuwaharaEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : KuwaharaEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuLensDirtEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyLensDirtEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : LensDirtEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuLensDistortionEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyLensDistortionEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : LensDistortionEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuLensFlareEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyLensFlareEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : LensFlareEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuMedianEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyMedianEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : MedianEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuMotionBlurEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyMotionBlurEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), FlightRuntime.field(ctx, 'sceneVelocityTexture'), (cast effect : MotionBlurEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuOuterGlowEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyOuterGlowEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), FlightRuntime.field(ctx, 'pool'), (cast effect : OuterGlowEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuOutlineEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyOutlineEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : OutlineEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuPixelateEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyPixelateEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : PixelateEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuPosterizeEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyPosterizeEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : PosterizeEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuRadialBlurEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyRadialBlurEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : RadialBlurEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuScanlinesEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyScanlinesEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : ScanlinesEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuScreenSpaceFogEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyScreenSpaceFogEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : ScreenSpaceFogEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuSharpenEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applySharpenEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : SharpenEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuSketchEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applySketchEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : SketchEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuSmaaEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applySmaaEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : SmaaEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuSsaoEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applySsaoEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : SsaoEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuSsrEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applySsrEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : SsrEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuTaaEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyTaaEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : TaaEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuTiltShiftEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyTiltShiftEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : TiltShiftEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuToneMapEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyToneMapEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : ToneMapEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuVignetteEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyVignetteEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : VignetteEffect)] : Array<Dynamic>));
};

  public static final defaultWgpuWhiteBalanceEffectRunner:WgpuRenderEffectRunner = function(ctx:Dynamic, effect:Dynamic) {
  FlightRuntime.callValue(applyWhiteBalanceEffectToWgpu, cast ([FlightRuntime.field(ctx, 'state'), FlightRuntime.field(ctx, 'source'), FlightRuntime.field(ctx, 'dest'), (cast effect : WhiteBalanceEffect)] : Array<Dynamic>));
};

  public static final DIRECTIONAL_BLUR_FRAGMENT_WGSL__wgpuDirectionalBlurEffect:Dynamic = '\nstruct Uniforms {\n  u_angle : f32,\n  u_length : f32,\n  u_samples : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nconst SAMPLES : i32 = 16;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let dir = vec2f(cos(uni.u_angle), sin(uni.u_angle)) * (uni.u_length / uni.u_resolution);\n  let count = min(uni.u_samples, 16.0);\n  var sum = vec4f(0.0);\n  var taken = 0.0;\n  for (var i = 0; i < SAMPLES; i = i + 1) {\n    if (f32(i) >= count) { break; }\n    let t = select(0.0, (f32(i) / (count - 1.0)) - 0.5, count > 1.0);\n    let p = uv + dir * t;\n    sum = sum + textureSampleLevel(tex, smp, p, 0.0);\n    taken = taken + 1.0;\n  }\n  return sum / max(taken, 1.0);\n}';

  @:keep public static function destroyWgpuRenderEffectPipeline(state:WgpuRenderState, pipeline:WgpuRenderEffectPipeline):Void {
    if (FlightRuntime.truthy(FlightRuntime.field(pipeline, 'sceneTarget'))) {
  FlightRuntime.callValue(destroyWgpuRenderTarget, cast ([state, FlightRuntime.field(pipeline, 'sceneTarget')] : Array<Dynamic>));
  FlightRuntime.setField(pipeline, 'sceneTarget', null);
}
    FlightRuntime.callValue(destroyWgpuRenderTargetPool, cast ([state, FlightRuntime.field(pipeline, 'pool')] : Array<Dynamic>));
    FlightRuntime.callOptionalProperty(FlightRuntime.field(FlightRuntime.field(pipeline, 'lutTexture'), 'texture'), 'destroy', cast ([] : Array<Dynamic>));
    FlightRuntime.setField(FlightRuntime.field(pipeline, 'lutTexture'), 'texture', null);
    FlightRuntime.setField(FlightRuntime.field(pipeline, 'lutTexture'), 'size', 0.0);
    FlightRuntime.setField(FlightRuntime.field(pipeline, 'lutTexture'), 'lut', null);
    FlightRuntime.setField(FlightRuntime.field(pipeline, 'lutCache'), 'signature', null);
    FlightRuntime.setField(FlightRuntime.field(pipeline, 'lutCache'), 'lut', null);
  }

  public static final DISPLACEMENT_FRAGMENT_WGSL__wgpuDisplacementEffect:Dynamic = '\nstruct Uniforms {\n  u_intensity : f32,\n  u_frequency : f32,\n  u_seed : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let f = uni.u_frequency;\n  let warp = vec2f(\n    sin(uv.y * f + uni.u_seed) + sin(uv.y * f * 2.3 + uni.u_seed * 1.7) * 0.5,\n    cos(uv.x * f * 0.8 + uni.u_seed * 1.3)\n  );\n  let displaced = uv + warp * (uni.u_intensity / uni.u_resolution);\n  return textureSampleLevel(tex, smp, displaced, 0.0);\n}';

  public static final DITHER_FRAGMENT_WGSL__wgpuDitherEffect:Dynamic = '\nstruct Uniforms {\n  u_levels : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn bayer(p : vec2i) -> f32 {\n  let x = p.x & 3;\n  let y = p.y & 3;\n  var m = array<i32, 16>(0, 8, 2, 10, 12, 4, 14, 6, 3, 11, 1, 9, 15, 7, 13, 5);\n  return f32(m[y * 4 + x]) / 16.0;\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let px = vec2i(uv * uni.u_resolution);\n  let t = bayer(px) - 0.5;\n  let steps = uni.u_levels - 1.0;\n  let q = floor(c.rgb * steps + 0.5 + t) / steps;\n  return vec4f(clamp(q, vec3f(0.0), vec3f(1.0)), c.a);\n}';

  public static final EFFECT_VERTEX_WGSL:Dynamic = '\nstruct VertexOut {\n  @builtin(position) position : vec4f,\n  @location(0) uv : vec2f,\n}\n\n@vertex\nfn vs_main(@builtin(vertex_index) vi : u32) -> VertexOut {\n  let xi = vi == 1u || vi == 2u || vi == 4u;\n  let yi = vi == 2u || vi == 4u || vi == 5u;\n  var out : VertexOut;\n  out.position = vec4f(select(-1.0, 1.0, xi), select(-1.0, 1.0, yi), 0.0, 1.0);\n  out.uv = vec2f(select(0.0, 1.0, xi), select(1.0, 0.0, yi));\n  return out;\n}\n';

  @:keep public static function drawWgpuDualSourceEffectPass(state:WgpuRenderState, source0:WgpuRenderTarget, source1:WgpuRenderTarget, dest:Null<WgpuRenderTarget>, pipeline:WgpuDualSourceEffectPipeline, setUniforms:Dynamic):Void {
    var fs:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure3:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var slotOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    var source0BG:Dynamic = cast FlightRuntime.UNDEFINED;
    var source1BG:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    fs = FlightRuntime.callValue(EffectsWgpu.getOrCreateEffectPassState__wgpuEffectPass, cast ([state] : Array<Dynamic>));
    __destructure3 = state;
    device = FlightRuntime.field(__destructure3, 'device');
    slotOffset = FlightRuntime.callValue(EffectsWgpu.acquireUniformSlot__wgpuEffectPass, cast ([fs] : Array<Dynamic>));
    FlightRuntime.callValue(EffectsWgpu.writeUniformSlot__wgpuEffectPass, cast ([state, fs, slotOffset, setUniforms] : Array<Dynamic>));
    source0BG = FlightRuntime.callValue(EffectsWgpu.getOrCreateTextureBG__wgpuEffectPass, cast ([fs, device, FlightRuntime.field(source0, 'view')] : Array<Dynamic>));
    source1BG = FlightRuntime.callValue(EffectsWgpu.getOrCreateTextureBG__wgpuEffectPass, cast ([fs, device, FlightRuntime.field(source1, 'view')] : Array<Dynamic>));
    pass = FlightRuntime.callValue(EffectsWgpu.beginEffectPass__wgpuEffectPass, cast ([state, dest, 'load'] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setPipeline', cast ([FlightRuntime.callValue(EffectsWgpu.resolveEffectPipeline__wgpuEffectPass, cast ([state, pipeline, dest] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([0.0, FlightRuntime.field(fs, 'uniformBG'), cast ([slotOffset] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([1.0, source0BG] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([2.0, source1BG] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'draw', cast ([6.0] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'end', cast ([] : Array<Dynamic>));
  }

  @:keep public static function drawWgpuEffectPass(state:WgpuRenderState, source:WgpuRenderTarget, dest:Null<WgpuRenderTarget>, pipeline:WgpuEffectPipeline, setUniforms:Dynamic):Void {
    var fs:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure4:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var slotOffset:Dynamic = cast FlightRuntime.UNDEFINED;
    var sourceBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var pass:Dynamic = cast FlightRuntime.UNDEFINED;
    fs = FlightRuntime.callValue(EffectsWgpu.getOrCreateEffectPassState__wgpuEffectPass, cast ([state] : Array<Dynamic>));
    __destructure4 = state;
    device = FlightRuntime.field(__destructure4, 'device');
    slotOffset = FlightRuntime.callValue(EffectsWgpu.acquireUniformSlot__wgpuEffectPass, cast ([fs] : Array<Dynamic>));
    FlightRuntime.callValue(EffectsWgpu.writeUniformSlot__wgpuEffectPass, cast ([state, fs, slotOffset, setUniforms] : Array<Dynamic>));
    sourceBG = FlightRuntime.callValue(EffectsWgpu.getOrCreateTextureBG__wgpuEffectPass, cast ([fs, device, FlightRuntime.field(source, 'view')] : Array<Dynamic>));
    pass = FlightRuntime.callValue(EffectsWgpu.beginEffectPass__wgpuEffectPass, cast ([state, dest, 'load'] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setPipeline', cast ([FlightRuntime.callValue(EffectsWgpu.resolveEffectPipeline__wgpuEffectPass, cast ([state, pipeline, dest] : Array<Dynamic>))] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([0.0, FlightRuntime.field(fs, 'uniformBG'), cast ([slotOffset] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'setBindGroup', cast ([1.0, sourceBG] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'draw', cast ([6.0] : Array<Dynamic>));
    FlightRuntime.callProperty(pass, 'end', cast ([] : Array<Dynamic>));
  }

  public static final effectPassStates__wgpuEffectPass:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final encodePipelines__wgpuGradientBevelEffect:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final ERASE_BLEND__wgpuEffectPass:Dynamic = { color: { srcFactor: 'zero', dstFactor: 'one-minus-src-alpha', operation: 'add' }, alpha: { srcFactor: 'zero', dstFactor: 'one-minus-src-alpha', operation: 'add' } };

  @:keep public static function endWgpuRenderEffectPipeline(state:WgpuRenderState, pipeline:WgpuRenderEffectPipeline, operations:Array<Dynamic>):Void {
    var scene:Dynamic = cast FlightRuntime.UNDEFINED;
    var format:Dynamic = cast FlightRuntime.UNDEFINED;
    var descriptor:Dynamic = cast FlightRuntime.UNDEFINED;
    var source:WgpuRenderTarget = cast FlightRuntime.UNDEFINED;
    var scratchA:Null<WgpuRenderTarget> = cast FlightRuntime.UNDEFINED;
    var scratchB:Null<WgpuRenderTarget> = cast FlightRuntime.UNDEFINED;
    var pending:Array<Adjustment> = cast FlightRuntime.UNDEFINED;
    var ensureScratch:Dynamic = cast FlightRuntime.UNDEFINED;
    var flushAdjustments:Dynamic = cast FlightRuntime.UNDEFINED;
    scene = FlightRuntime.field(pipeline, 'sceneTarget');
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(scene, null))) { return; }
    FlightRuntime.callValue(endWgpuRenderPass, cast ([state] : Array<Dynamic>));
    format = FlightRuntime.field(scene, 'format');
    descriptor = { width: FlightRuntime.field(scene, 'width'), height: FlightRuntime.field(scene, 'height'), format: format };
    source = scene;
    scratchA = null;
    scratchB = null;
    pending = cast ([] : Array<Dynamic>);
    ensureScratch = function() {
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(scratchA, null))) { (scratchA = cast (FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, FlightRuntime.field(pipeline, 'pool'), descriptor] : Array<Dynamic>)) : Dynamic)); }
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(scratchB, null))) { (scratchB = cast (FlightRuntime.callValue(acquireWgpuRenderTarget, cast ([state, FlightRuntime.field(pipeline, 'pool'), descriptor] : Array<Dynamic>)) : Dynamic)); }
};
    flushAdjustments = function() {
  var dest:Dynamic = cast FlightRuntime.UNDEFINED;
  if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(pending, 'length'), 0.0))) { return; }
  FlightRuntime.callValue(ensureScratch, cast ([] : Array<Dynamic>));
  dest = FlightRuntime.select(FlightRuntime.strictEquals(source, scratchA), function():Dynamic return cast scratchB, function():Dynamic return cast scratchA);
  if (FlightRuntime.truthy(FlightRuntime.callProperty(pending, 'some', cast ([isColorLutAdjustment] : Array<Dynamic>)))) {
  FlightRuntime.callValue(applyColorLutPassToWgpu, cast ([state, source, dest, FlightRuntime.callValue(bakeColorLutForRun, cast ([FlightRuntime.field(pipeline, 'lutCache'), pending] : Array<Dynamic>)), FlightRuntime.field(pipeline, 'lutTexture')] : Array<Dynamic>));
} else {
  var matrices:Array<Array<Float>> = cast ([] : Array<Dynamic>);
  for (op in FlightRuntime.iterable(pending)) {
    var matrix:Dynamic = FlightRuntime.callValue(getAdjustmentColorMatrix, cast ([op] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(matrix, null))) { FlightRuntime.callProperty(matrices, 'push', cast ([matrix] : Array<Dynamic>)); }
  }
  FlightRuntime.callValue(applyColorMatrixPassToWgpu, cast ([state, source, dest, FlightRuntime.callValue(fuseColorMatrices, cast ([matrices] : Array<Dynamic>))] : Array<Dynamic>));
}
  (source = cast (dest : Dynamic));
  (pending = cast (cast ([] : Array<Dynamic>) : Dynamic));
};
    for (operation in FlightRuntime.iterable(operations)) {
      if (FlightRuntime.truthy(FlightRuntime.orValue(!FlightRuntime.strictEquals(FlightRuntime.callValue(getAdjustmentColorMatrix, cast ([operation] : Array<Dynamic>)), null), function():Dynamic return cast FlightRuntime.callValue(isColorLutAdjustment, cast ([operation] : Array<Dynamic>))))) {
  FlightRuntime.callProperty(pending, 'push', cast ([(cast operation : Adjustment)] : Array<Dynamic>));
  continue;
}
      var runner:Dynamic = FlightRuntime.callValue(getWgpuRenderEffectRunner, cast ([state, FlightRuntime.field(operation, 'kind')] : Array<Dynamic>));
      if (FlightRuntime.truthy(FlightRuntime.strictEquals(runner, null))) { continue; }
      FlightRuntime.callValue(flushAdjustments, cast ([] : Array<Dynamic>));
      FlightRuntime.callValue(ensureScratch, cast ([] : Array<Dynamic>));
      var dest:Dynamic = FlightRuntime.select(FlightRuntime.strictEquals(source, scratchA), function():Dynamic return cast scratchB, function():Dynamic return cast scratchA);
      FlightRuntime.callValue(runner, cast ([{ state: state, source: source, dest: dest, pool: FlightRuntime.field(pipeline, 'pool'), sceneDepthTexture: null, sceneVelocityTexture: FlightRuntime.field(pipeline, 'velocityTexture') }, operation] : Array<Dynamic>));
      (source = cast (dest : Dynamic));
    }
    FlightRuntime.callValue(flushAdjustments, cast ([] : Array<Dynamic>));
    FlightRuntime.callValue(EffectsWgpu.presentWgpuRenderEffectResult__wgpuRenderEffectPipeline, cast ([state, source] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(scratchA, null))) { FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([FlightRuntime.field(pipeline, 'pool'), scratchA] : Array<Dynamic>)); }
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(scratchB, null))) { FlightRuntime.callValue(releaseWgpuRenderTarget, cast ([FlightRuntime.field(pipeline, 'pool'), scratchB] : Array<Dynamic>)); }
  }

  public static final ERASE_WGSL__wgpuEffectBlitShader:Dynamic = '\nstruct Uniforms { _u : f32, _pad0 : f32, _pad1 : f32, _pad2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let a = textureSampleLevel(tex, smp, uv, 0.0).a;\n  return vec4f(0.0, 0.0, 0.0, a);\n}';

  public static final erasePipelines__wgpuEffectBlitShader:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final FILM_GRAIN_FRAGMENT_WGSL__wgpuFilmGrainEffect:Dynamic = '\nstruct Uniforms {\n  u_intensity : f32,\n  u_size : f32,\n  u_seed : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn hash(pIn : vec2f) -> f32 {\n  let p = floor(pIn / uni.u_size);\n  return fract(sin(dot(p, vec2f(127.1, 311.7)) + uni.u_seed) * 43758.5453123);\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let n = hash(uv * 1024.0) - 0.5;\n  return vec4f(c.rgb + n * uni.u_intensity, c.a);\n}';

  public static final FXAA_FRAGMENT_WGSL__wgpuFxaaEffect:Dynamic = '\nstruct Uniforms { u_resolution : vec2f, u_edgeThreshold : f32, _pad0 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn luma(c : vec3f) -> f32 {\n  return dot(c, vec3f(0.299, 0.587, 0.114));\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = 1.0 / uni.u_resolution;\n  let rgbM = textureSampleLevel(tex, smp, uv, 0.0).rgb;\n  let rgbNW = textureSampleLevel(tex, smp, uv + vec2f(-1.0, -1.0) * texel, 0.0).rgb;\n  let rgbNE = textureSampleLevel(tex, smp, uv + vec2f(1.0, -1.0) * texel, 0.0).rgb;\n  let rgbSW = textureSampleLevel(tex, smp, uv + vec2f(-1.0, 1.0) * texel, 0.0).rgb;\n  let rgbSE = textureSampleLevel(tex, smp, uv + vec2f(1.0, 1.0) * texel, 0.0).rgb;\n  let lumaM = luma(rgbM);\n  let lumaNW = luma(rgbNW);\n  let lumaNE = luma(rgbNE);\n  let lumaSW = luma(rgbSW);\n  let lumaSE = luma(rgbSE);\n  let lumaMin = min(lumaM, min(min(lumaNW, lumaNE), min(lumaSW, lumaSE)));\n  let lumaMax = max(lumaM, max(max(lumaNW, lumaNE), max(lumaSW, lumaSE)));\n  let range = lumaMax - lumaMin;\n  if (range < max(uni.u_edgeThreshold, lumaMax * 0.125)) {\n    return vec4f(rgbM, textureSampleLevel(tex, smp, uv, 0.0).a);\n  }\n  var dir : vec2f;\n  dir.x = -((lumaNW + lumaNE) - (lumaSW + lumaSE));\n  dir.y = ((lumaNW + lumaSW) - (lumaNE + lumaSE));\n  let dirReduce = max((lumaNW + lumaNE + lumaSW + lumaSE) * 0.03125, 0.0078125);\n  let rcpDirMin = 1.0 / (min(abs(dir.x), abs(dir.y)) + dirReduce);\n  dir = clamp(dir * rcpDirMin, vec2f(-8.0), vec2f(8.0)) * texel;\n  let rgbA = 0.5 * (\n    textureSampleLevel(tex, smp, uv + dir * (1.0 / 3.0 - 0.5), 0.0).rgb +\n    textureSampleLevel(tex, smp, uv + dir * (2.0 / 3.0 - 0.5), 0.0).rgb);\n  let rgbB = rgbA * 0.5 + 0.25 * (\n    textureSampleLevel(tex, smp, uv + dir * -0.5, 0.0).rgb +\n    textureSampleLevel(tex, smp, uv + dir * 0.5, 0.0).rgb);\n  let lumaB = luma(rgbB);\n  let result = select(rgbB, rgbA, lumaB < lumaMin || lumaB > lumaMax);\n  return vec4f(result, textureSampleLevel(tex, smp, uv, 0.0).a);\n}';

  public static final GAUSSIAN_BLUR_WGSL__wgpuBlurEffect:Dynamic = '\nstruct Uniforms {\n  texelSize : vec2f,\n  direction : vec2f,\n  radius : f32,\n  sigma : f32,\n  _pad0 : f32, _pad1 : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let r = i32(uni.radius);\n  if (r == 0) { return textureSampleLevel(tex, smp, uv, 0.0); }\n  let twoSigmaSq = 2.0 * uni.sigma * uni.sigma;\n  var sum = vec4f(0.0);\n  var weightSum = 0.0;\n  for (var i = -r; i <= r; i++) {\n    let w = exp(-f32(i * i) / twoSigmaSq);\n    sum += w * textureSampleLevel(tex, smp, uv + f32(i) * uni.texelSize * uni.direction, 0.0);\n    weightSum += w;\n  }\n  return sum / weightSum;\n}';

  public static final GLITCH_FRAGMENT_WGSL__wgpuGlitchEffect:Dynamic = '\nstruct Uniforms {\n  u_intensity : f32,\n  u_blockSize : f32,\n  u_colorShift : f32,\n  u_seed : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn glitchHash(n : f32) -> f32 { return fract(sin(n) * 43758.5453123); }\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let blockSize = max(2.0, uni.u_blockSize);\n  let block = floor(uv.y * uni.u_resolution.y / blockSize);\n  let r = glitchHash(block + uni.u_seed * 7.0);\n  // Higher intensity activates more blocks; a torn block tears horizontally.\n  let tear = step(1.0 - uni.u_intensity * 0.6, r);\n  let shiftPx = (glitchHash(block * 1.7 + uni.u_seed) - 0.5) * 2.0 * tear * uni.u_intensity * 40.0;\n  let baseUv = vec2f(uv.x + shiftPx / uni.u_resolution.x, uv.y);\n  // RGB channel separation, wider on torn blocks.\n  let cs = (uni.u_colorShift * (0.4 + tear)) / uni.u_resolution.x;\n  let rC = textureSampleLevel(tex, smp, vec2f(baseUv.x + cs, baseUv.y), 0.0).r;\n  let gC = textureSampleLevel(tex, smp, baseUv, 0.0).g;\n  let bC = textureSampleLevel(tex, smp, vec2f(baseUv.x - cs, baseUv.y), 0.0).b;\n  let a = textureSampleLevel(tex, smp, baseUv, 0.0).a;\n  var col = vec3f(rC, gC, bC);\n  // Occasional bright block corruption.\n  let corrupt = step(0.985 - uni.u_intensity * 0.04, glitchHash(block * 3.3 + uni.u_seed * 2.0));\n  col = mix(col, vec3f(1.0), corrupt * 0.6);\n  return vec4f(col, a);\n}';

  @:keep public static function getApplyPipeline__wgpuGradientBevelEffect(state:WgpuRenderState):WgpuEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.applyPipelines__wgpuGradientBevelEffect, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  var fs:Dynamic = FlightRuntime.callValue(getWgpuEffectPassState, cast ([state] : Array<Dynamic>));
  var __destructure1:Dynamic = state;
  var device:Dynamic = FlightRuntime.field(__destructure1, 'device');
  var format:Dynamic = FlightRuntime.field(__destructure1, 'format');
  var shaderModule:Dynamic = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: (EFFECT_VERTEX_WGSL + EffectsWgpu.BEVEL_APPLY_FRAGMENT_WGSL__wgpuGradientBevelEffect) }] : Array<Dynamic>));
  var pipelineLayout:Dynamic = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([FlightRuntime.field(fs, 'uniformBGLayout'), FlightRuntime.field(fs, 'textureBGLayout'), FlightRuntime.field(fs, 'textureBGLayout'), FlightRuntime.field(fs, 'textureBGLayout')] : Array<Dynamic>) }] : Array<Dynamic>));
  var pipeline:Dynamic = FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: pipelineLayout, vertex: { module: shaderModule, entryPoint: 'vs_main' }, fragment: { module: shaderModule, entryPoint: 'fs_main', targets: cast ([{ format: format }] : Array<Dynamic>) }, primitive: { topology: 'triangle-list' } }] : Array<Dynamic>));
  (p = cast ({ pipeline: pipeline, blendMode: 'premul' } : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.applyPipelines__wgpuGradientBevelEffect, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getAutoDivisor__wgpuConvolutionEffect(matrix:Array<Float>, length:Float):Float {
    var sum:Dynamic = cast FlightRuntime.UNDEFINED;
    sum = 0.0;
    {
      var i:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, length, '<'))) {
        (sum = cast ((sum + FlightRuntime.getIndex(matrix, i)) : Dynamic));
        i++;
      }
    }
    return cast FlightRuntime.select(FlightRuntime.strictEquals(sum, 0.0), function():Dynamic return cast 1.0, function():Dynamic return cast sum);
    return cast null;
  }

  @:keep public static function getBlendState__wgpuEffectPass(blend:WgpuEffectBlendMode):Dynamic {
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(blend, 'replace'))) { return cast EffectsWgpu.REPLACE_BLEND__wgpuEffectPass; }
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(blend, 'erase'))) { return cast EffectsWgpu.ERASE_BLEND__wgpuEffectPass; }
    return cast EffectsWgpu.PREMUL_BLEND__wgpuEffectPass;
    return cast null;
  }

  @:keep public static function getBloomCompositePipeline__wgpuBloomEffect(state:WgpuRenderState):WgpuDualSourceEffectPipeline {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callProperty(EffectsWgpu._compositePipelines__wgpuBloomEffect, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(pipeline, FlightRuntime.UNDEFINED))) {
  (pipeline = cast (FlightRuntime.callValue(createWgpuDualSourceEffectPipeline, cast ([state, EffectsWgpu.BLOOM_COMPOSITE_FRAGMENT_WGSL__wgpuBloomEffect, 'replace'] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu._compositePipelines__wgpuBloomEffect, 'set', cast ([state, pipeline] : Array<Dynamic>));
}
    return cast pipeline;
    return cast null;
  }

  @:keep public static function getBoxBlurPipeline__wgpuEffectBoxBlur(state:WgpuRenderState):WgpuEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.boxBlurPipelines__wgpuEffectBoxBlur, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  (p = cast (FlightRuntime.callValue(createWgpuEffectPipeline, cast ([state, EffectsWgpu.BOX_BLUR_WGSL__wgpuEffectBoxBlur, 'replace'] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.boxBlurPipelines__wgpuEffectBoxBlur, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getEncodePipeline__wgpuGradientBevelEffect(state:WgpuRenderState):WgpuEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.encodePipelines__wgpuGradientBevelEffect, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  var fs:Dynamic = FlightRuntime.callValue(getWgpuEffectPassState, cast ([state] : Array<Dynamic>));
  var __destructure2:Dynamic = state;
  var device:Dynamic = FlightRuntime.field(__destructure2, 'device');
  var format:Dynamic = FlightRuntime.field(__destructure2, 'format');
  var shaderModule:Dynamic = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: (EFFECT_VERTEX_WGSL + EffectsWgpu.BEVEL_ENCODE_FRAGMENT_WGSL__wgpuGradientBevelEffect) }] : Array<Dynamic>));
  var pipelineLayout:Dynamic = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([FlightRuntime.field(fs, 'uniformBGLayout'), FlightRuntime.field(fs, 'textureBGLayout')] : Array<Dynamic>) }] : Array<Dynamic>));
  var pipeline:Dynamic = FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: pipelineLayout, vertex: { module: shaderModule, entryPoint: 'vs_main' }, fragment: { module: shaderModule, entryPoint: 'fs_main', targets: cast ([{ format: format }] : Array<Dynamic>) }, primitive: { topology: 'triangle-list' } }] : Array<Dynamic>));
  (p = cast ({ pipeline: pipeline, blendMode: 'replace' } : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.encodePipelines__wgpuGradientBevelEffect, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getInvertTintEdgeColor__wgpuInnerGlowEffect(color:Float, alpha:Float, strength:Float):Array<Float> {
    var edgeAlpha:Dynamic = cast FlightRuntime.UNDEFINED;
    edgeAlpha = FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, (alpha * strength)] : Array<Dynamic>));
    return cast cast ([(((Std.int((Std.int(color) >> Std.int(16.0))) & Std.int(255.0)) / 255.0) * edgeAlpha), (((Std.int((Std.int(color) >> Std.int(8.0))) & Std.int(255.0)) / 255.0) * edgeAlpha), (((Std.int(color) & Std.int(255.0)) / 255.0) * edgeAlpha), edgeAlpha] : Array<Dynamic>);
    return cast null;
  }

  @:keep public static function getInvertTintEdgeColor__wgpuInnerShadowEffect(color:Float, alpha:Float, strength:Float):Array<Float> {
    var edgeAlpha:Dynamic = cast FlightRuntime.UNDEFINED;
    edgeAlpha = FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, (alpha * strength)] : Array<Dynamic>));
    return cast cast ([(((Std.int((Std.int(color) >> Std.int(16.0))) & Std.int(255.0)) / 255.0) * edgeAlpha), (((Std.int((Std.int(color) >> Std.int(8.0))) & Std.int(255.0)) / 255.0) * edgeAlpha), (((Std.int(color) & Std.int(255.0)) / 255.0) * edgeAlpha), edgeAlpha] : Array<Dynamic>);
    return cast null;
  }

  @:keep public static function getLookupPipeline__wgpuGradientGlowEffect(state:WgpuRenderState):WgpuEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.lookupPipelines__wgpuGradientGlowEffect, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  var fs:Dynamic = FlightRuntime.callValue(getWgpuEffectPassState, cast ([state] : Array<Dynamic>));
  var __destructure1:Dynamic = state;
  var device:Dynamic = FlightRuntime.field(__destructure1, 'device');
  var format:Dynamic = FlightRuntime.field(__destructure1, 'format');
  var shaderModule:Dynamic = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: (EFFECT_VERTEX_WGSL + EffectsWgpu.GRADIENT_LOOKUP_FRAGMENT_WGSL__wgpuGradientGlowEffect) }] : Array<Dynamic>));
  var pipelineLayout:Dynamic = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([FlightRuntime.field(fs, 'uniformBGLayout'), FlightRuntime.field(fs, 'textureBGLayout'), FlightRuntime.field(fs, 'textureBGLayout')] : Array<Dynamic>) }] : Array<Dynamic>));
  var pipeline:Dynamic = FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: pipelineLayout, vertex: { module: shaderModule, entryPoint: 'vs_main' }, fragment: { module: shaderModule, entryPoint: 'fs_main', targets: cast ([{ format: format }] : Array<Dynamic>) }, primitive: { topology: 'triangle-list' } }] : Array<Dynamic>));
  (p = cast ({ pipeline: pipeline, blendMode: 'premul' } : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.lookupPipelines__wgpuGradientGlowEffect, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getLutBindGroupLayout__wgpuColorLutPass(state:WgpuRenderState):Dynamic {
    var layout:Dynamic = cast FlightRuntime.UNDEFINED;
    layout = FlightRuntime.callProperty(EffectsWgpu.lutBindGroupLayouts__wgpuColorLutPass, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(layout, FlightRuntime.UNDEFINED))) {
  (layout = cast (FlightRuntime.callProperty(FlightRuntime.field(state, 'device'), 'createBindGroupLayout', cast ([{ entries: cast ([{ binding: 0.0, visibility: FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'FRAGMENT'), texture: { viewDimension: '3d' } }, { binding: 1.0, visibility: FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'FRAGMENT'), sampler: {  } }] : Array<Dynamic>) }] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.lutBindGroupLayouts__wgpuColorLutPass, 'set', cast ([state, layout] : Array<Dynamic>));
}
    return cast layout;
    return cast null;
  }

  @:keep public static function getLutPipeline__wgpuColorLutPass(state:WgpuRenderState, format:Dynamic):WgpuEffectPipeline {
    var byFormat:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    byFormat = FlightRuntime.callProperty(EffectsWgpu.lutPipelines__wgpuColorLutPass, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(byFormat, FlightRuntime.UNDEFINED))) {
  (byFormat = cast (FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.lutPipelines__wgpuColorLutPass, 'set', cast ([state, byFormat] : Array<Dynamic>));
}
    pipeline = FlightRuntime.callProperty(byFormat, 'get', cast ([format] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(pipeline, FlightRuntime.UNDEFINED))) {
  var fs:Dynamic = FlightRuntime.callValue(getWgpuEffectPassState, cast ([state] : Array<Dynamic>));
  var __destructure1:Dynamic = state;
  var device:Dynamic = FlightRuntime.field(__destructure1, 'device');
  var shaderModule:Dynamic = FlightRuntime.callProperty(device, 'createShaderModule', cast ([{ code: (EFFECT_VERTEX_WGSL + EffectsWgpu.COLOR_LUT_FRAGMENT_WGSL__wgpuColorLutPass) }] : Array<Dynamic>));
  var pipelineLayout:Dynamic = FlightRuntime.callProperty(device, 'createPipelineLayout', cast ([{ bindGroupLayouts: cast ([FlightRuntime.field(fs, 'uniformBGLayout'), FlightRuntime.field(fs, 'textureBGLayout'), FlightRuntime.callValue(EffectsWgpu.getLutBindGroupLayout__wgpuColorLutPass, cast ([state] : Array<Dynamic>))] : Array<Dynamic>) }] : Array<Dynamic>));
  var gpuPipeline:Dynamic = FlightRuntime.callProperty(device, 'createRenderPipeline', cast ([{ layout: pipelineLayout, vertex: { module: shaderModule, entryPoint: 'vs_main' }, fragment: { module: shaderModule, entryPoint: 'fs_main', targets: cast ([{ format: format, blend: EffectsWgpu.REPLACE_BLEND__wgpuColorLutPass }] : Array<Dynamic>) }, primitive: { topology: 'triangle-list' } }] : Array<Dynamic>));
  (pipeline = cast ({ pipeline: gpuPipeline, blendMode: 'replace' } : Dynamic));
  FlightRuntime.callProperty(byFormat, 'set', cast ([format, pipeline] : Array<Dynamic>));
}
    return cast pipeline;
    return cast null;
  }

  @:keep public static function getMotionBlurPipeline__wgpuMotionBlurEffect(state:WgpuRenderState):WgpuDualSourceEffectPipeline {
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    pipeline = FlightRuntime.callProperty(EffectsWgpu._motionBlurPipelines__wgpuMotionBlurEffect, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(pipeline, FlightRuntime.UNDEFINED))) {
  (pipeline = cast (FlightRuntime.callValue(createWgpuDualSourceEffectPipeline, cast ([state, EffectsWgpu.MOTION_BLUR_FRAGMENT_WGSL__wgpuMotionBlurEffect, 'replace'] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu._motionBlurPipelines__wgpuMotionBlurEffect, 'set', cast ([state, pipeline] : Array<Dynamic>));
}
    return cast pipeline;
    return cast null;
  }

  @:keep public static function getOrCreateEffectPassState__wgpuEffectPass(state:WgpuRenderState):WgpuEffectPassState__wgpuEffectPass {
    var fs:Dynamic = cast FlightRuntime.UNDEFINED;
    var __destructure0:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var format:Dynamic = cast FlightRuntime.UNDEFINED;
    var SLOTS:Dynamic = cast FlightRuntime.UNDEFINED;
    var STRIDE:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformBuffer:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformData:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformDataI32:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformBGLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var uniformBG:Dynamic = cast FlightRuntime.UNDEFINED;
    var textureBGLayout:Dynamic = cast FlightRuntime.UNDEFINED;
    var sampler:Dynamic = cast FlightRuntime.UNDEFINED;
    fs = FlightRuntime.callProperty(EffectsWgpu.effectPassStates__wgpuEffectPass, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(fs, FlightRuntime.UNDEFINED))) { return cast fs; }
    __destructure0 = state;
    device = FlightRuntime.field(__destructure0, 'device');
    format = FlightRuntime.field(__destructure0, 'format');
    SLOTS = 512.0;
    STRIDE = 256.0;
    uniformBuffer = FlightRuntime.callProperty(device, 'createBuffer', cast ([{ size: (SLOTS * STRIDE), usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'UNIFORM')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUBufferUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>));
    uniformData = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Float32Array'] : Array<Dynamic>)), [((SLOTS * STRIDE) / 4.0)]);
    uniformDataI32 = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Int32Array'] : Array<Dynamic>)), [FlightRuntime.field(uniformData, 'buffer')]);
    uniformBGLayout = FlightRuntime.callProperty(device, 'createBindGroupLayout', cast ([{ entries: cast ([{ binding: 0.0, visibility: FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'FRAGMENT'), buffer: { type: 'uniform', hasDynamicOffset: true } }] : Array<Dynamic>) }] : Array<Dynamic>));
    uniformBG = FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: uniformBGLayout, entries: cast ([{ binding: 0.0, resource: { buffer: uniformBuffer, size: STRIDE } }] : Array<Dynamic>) }] : Array<Dynamic>));
    textureBGLayout = FlightRuntime.callProperty(device, 'createBindGroupLayout', cast ([{ entries: cast ([{ binding: 0.0, visibility: FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'FRAGMENT'), texture: {  } }, { binding: 1.0, visibility: FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUShaderStage'] : Array<Dynamic>)), 'FRAGMENT'), sampler: {  } }] : Array<Dynamic>) }] : Array<Dynamic>));
    sampler = FlightRuntime.callProperty(device, 'createSampler', cast ([{ minFilter: 'linear', magFilter: 'linear', addressModeU: 'clamp-to-edge', addressModeV: 'clamp-to-edge' }] : Array<Dynamic>));
    (fs = cast ({ uniformBuffer: uniformBuffer, uniformData: uniformData, uniformDataI32: uniformDataI32, uniformOffset: 0.0, uniformStride: STRIDE, uniformSlots: SLOTS, uniformBGLayout: uniformBGLayout, uniformBG: uniformBG, textureBGLayout: textureBGLayout, textureBGs: FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []), sampler: sampler, format: format } : Dynamic));
    FlightRuntime.callProperty(EffectsWgpu.effectPassStates__wgpuEffectPass, 'set', cast ([state, fs] : Array<Dynamic>));
    return cast fs;
    return cast null;
  }

  @:keep public static function getOrCreateTextureBG__wgpuEffectPass(fs:WgpuEffectPassState__wgpuEffectPass, device:Dynamic, view:Dynamic):Dynamic {
    var bg:Dynamic = cast FlightRuntime.UNDEFINED;
    bg = FlightRuntime.callProperty(FlightRuntime.field(fs, 'textureBGs'), 'get', cast ([view] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(bg, FlightRuntime.UNDEFINED))) {
  (bg = cast (FlightRuntime.callProperty(device, 'createBindGroup', cast ([{ layout: FlightRuntime.field(fs, 'textureBGLayout'), entries: cast ([{ binding: 0.0, resource: view }, { binding: 1.0, resource: FlightRuntime.field(fs, 'sampler') }] : Array<Dynamic>) }] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(FlightRuntime.field(fs, 'textureBGs'), 'set', cast ([view, bg] : Array<Dynamic>));
}
    return cast bg;
    return cast null;
  }

  @:keep public static function getWgpuBevelCompositeShader__wgpuBevelEffect(state:WgpuRenderState):WgpuDualSourceEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.bevelCompositePipelines__wgpuBevelEffect, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  (p = cast (FlightRuntime.callValue(createWgpuDualSourceEffectPipeline, cast ([state, EffectsWgpu.BEVEL_COMPOSITE_WGSL__wgpuBevelEffect] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.bevelCompositePipelines__wgpuBevelEffect, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getWgpuBlitOffsetShader__wgpuEffectBlitShader(state:WgpuRenderState):WgpuEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.blitOffsetPipelines__wgpuEffectBlitShader, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  (p = cast (FlightRuntime.callValue(createWgpuEffectPipeline, cast ([state, EffectsWgpu.BLIT_OFFSET_WGSL__wgpuEffectBlitShader] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.blitOffsetPipelines__wgpuEffectBlitShader, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getWgpuBlitShader__wgpuEffectBlitShader(state:WgpuRenderState):WgpuEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.blitPipelines__wgpuEffectBlitShader, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  (p = cast (FlightRuntime.callValue(createWgpuEffectPipeline, cast ([state, EffectsWgpu.BLIT_WGSL__wgpuEffectBlitShader] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.blitPipelines__wgpuEffectBlitShader, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getWgpuEffectGradientRampTexture(state:WgpuRenderState, colors:Array<Float>, alphas:Array<Float>, ratios:Array<Float>):Dynamic {
    var cache:Dynamic = cast FlightRuntime.UNDEFINED;
    var key:Dynamic = cast FlightRuntime.UNDEFINED;
    var texture:Dynamic = cast FlightRuntime.UNDEFINED;
    cache = FlightRuntime.callProperty(EffectsWgpu.rampCaches__wgpuEffectGradientRamp, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(cache, FlightRuntime.UNDEFINED))) {
  (cache = cast (FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.rampCaches__wgpuEffectGradientRamp, 'set', cast ([state, cache] : Array<Dynamic>));
}
    key = '' + Std.string(FlightRuntime.join(colors, ',')) + '|' + Std.string(FlightRuntime.join(alphas, ',')) + '|' + Std.string(FlightRuntime.join(ratios, ',')) + '';
    texture = FlightRuntime.callProperty(cache, 'get', cast ([key] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(texture, FlightRuntime.UNDEFINED))) {
  (texture = cast (FlightRuntime.callValue(EffectsWgpu.createWgpuEffectGradientRampTexture__wgpuEffectGradientRamp, cast ([state, colors, alphas, ratios] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(cache, 'set', cast ([key, texture] : Array<Dynamic>));
}
    return cast texture;
    return cast null;
  }

  @:keep public static function getWgpuEffectPassState(state:WgpuRenderState):{ var uniformBG:Dynamic; var textureBGLayout:Dynamic; var uniformBGLayout:Dynamic; var sampler:Dynamic; var acquireSlot:Dynamic; var writeSlot:Dynamic; var beginPass:Dynamic; } {
    var fs:Dynamic = cast FlightRuntime.UNDEFINED;
    fs = FlightRuntime.callValue(EffectsWgpu.getOrCreateEffectPassState__wgpuEffectPass, cast ([state] : Array<Dynamic>));
    return cast { uniformBG: FlightRuntime.field(fs, 'uniformBG'), textureBGLayout: FlightRuntime.field(fs, 'textureBGLayout'), uniformBGLayout: FlightRuntime.field(fs, 'uniformBGLayout'), sampler: FlightRuntime.field(fs, 'sampler'), acquireSlot: function() return FlightRuntime.callValue(EffectsWgpu.acquireUniformSlot__wgpuEffectPass, cast ([fs] : Array<Dynamic>)), writeSlot: function(offset:Dynamic, fn:Dynamic) return FlightRuntime.callValue(EffectsWgpu.writeUniformSlot__wgpuEffectPass, cast ([state, fs, offset, fn] : Array<Dynamic>)), beginPass: function(dest:Dynamic, loadOp:Dynamic) return FlightRuntime.callValue(EffectsWgpu.beginEffectPass__wgpuEffectPass, cast ([state, dest, loadOp] : Array<Dynamic>)) };
    return cast null;
  }

  @:keep public static function getWgpuEffectPipeline(state:WgpuRenderState, key:String, fragmentWGSL:String, blend:String = 'replace'):WgpuEffectPipeline {
    var cache:Dynamic = cast FlightRuntime.UNDEFINED;
    var existing:Dynamic = cast FlightRuntime.UNDEFINED;
    var compiled:Dynamic = cast FlightRuntime.UNDEFINED;
    cache = FlightRuntime.callProperty(EffectsWgpu._pipelines__wgpuEffectProgramCache, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(cache, FlightRuntime.UNDEFINED))) {
  (cache = cast (FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu._pipelines__wgpuEffectProgramCache, 'set', cast ([state, cache] : Array<Dynamic>));
}
    existing = FlightRuntime.callProperty(cache, 'get', cast ([key] : Array<Dynamic>));
    if (FlightRuntime.truthy(!FlightRuntime.strictEquals(existing, FlightRuntime.UNDEFINED))) { return cast existing; }
    compiled = FlightRuntime.callValue(createWgpuEffectPipeline, cast ([state, fragmentWGSL, blend] : Array<Dynamic>));
    FlightRuntime.callProperty(cache, 'set', cast ([key, compiled] : Array<Dynamic>));
    return cast compiled;
    return cast null;
  }

  @:keep public static function getWgpuEraseShader__wgpuEffectBlitShader(state:WgpuRenderState):WgpuEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.erasePipelines__wgpuEffectBlitShader, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  (p = cast (FlightRuntime.callValue(createWgpuEffectPipeline, cast ([state, EffectsWgpu.ERASE_WGSL__wgpuEffectBlitShader, 'erase'] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.erasePipelines__wgpuEffectBlitShader, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getWgpuInnerClipShader__wgpuEffectTintShader(state:WgpuRenderState):WgpuDualSourceEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.innerClipPipelines__wgpuEffectTintShader, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  (p = cast (FlightRuntime.callValue(createWgpuDualSourceEffectPipeline, cast ([state, EffectsWgpu.INNER_CLIP_WGSL__wgpuEffectTintShader, 'replace'] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.innerClipPipelines__wgpuEffectTintShader, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getWgpuInvertTintShader__wgpuEffectTintShader(state:WgpuRenderState):WgpuEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.invertTintPipelines__wgpuEffectTintShader, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  (p = cast (FlightRuntime.callValue(createWgpuEffectPipeline, cast ([state, EffectsWgpu.INVERT_TINT_WGSL__wgpuEffectTintShader, 'replace'] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.invertTintPipelines__wgpuEffectTintShader, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  @:keep public static function getWgpuRenderEffectRunner(state:WgpuRenderState, kind:String):Null<WgpuRenderEffectRunner> {
    return cast FlightRuntime.coalesce(FlightRuntime.callOptionalProperty(FlightRuntime.callProperty(EffectsWgpu._registries__wgpuRenderEffectRegistry, 'get', cast ([state] : Array<Dynamic>)), 'get', cast ([kind] : Array<Dynamic>)), function():Dynamic return cast null);
    return cast null;
  }

  @:keep public static function getWgpuTintShader__wgpuEffectTintShader(state:WgpuRenderState):WgpuEffectPipeline {
    var p:Dynamic = cast FlightRuntime.UNDEFINED;
    p = FlightRuntime.callProperty(EffectsWgpu.tintPipelines__wgpuEffectTintShader, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(p, FlightRuntime.UNDEFINED))) {
  (p = cast (FlightRuntime.callValue(createWgpuEffectPipeline, cast ([state, EffectsWgpu.TINT_WGSL__wgpuEffectTintShader, 'replace'] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu.tintPipelines__wgpuEffectTintShader, 'set', cast ([state, p] : Array<Dynamic>));
}
    return cast p;
    return cast null;
  }

  public static final GOD_RAYS_FRAGMENT_HEAD__wgpuGodRaysEffect:Dynamic = '\nstruct Uniforms {\n  u_centerX : f32,\n  u_centerY : f32,\n  u_density : f32,\n  u_decay : f32,\n  u_weight : f32,\n  u_exposure : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nconst SAMPLES : i32 = ';

  public static final GOD_RAYS_FRAGMENT_TAIL__wgpuGodRaysEffect:Dynamic = ';\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let light = vec2f(uni.u_centerX, uni.u_centerY);\n  let delta = (uv - light) * (uni.u_density / f32(SAMPLES));\n  var coord = uv;\n  let base = textureSampleLevel(tex, smp, uv, 0.0);\n  var accum = base.rgb;\n  var illumination = 1.0;\n  for (var i = 0; i < SAMPLES; i = i + 1) {\n    coord = coord - delta;\n    var s = textureSampleLevel(tex, smp, coord, 0.0).rgb;\n    s = s * (illumination * uni.u_weight);\n    accum = accum + s;\n    illumination = illumination * uni.u_decay;\n  }\n  return vec4f(base.rgb + accum * uni.u_exposure, base.a);\n}';

  public static final GRADIENT_LOOKUP_FRAGMENT_WGSL__wgpuGradientGlowEffect:Dynamic = '\nstruct Uniforms { _u : f32, _pad0 : f32, _pad1 : f32, _pad2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var texBlurred : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n@group(2) @binding(0) var texRamp : texture_2d<f32>;\n@group(2) @binding(1) var smp2 : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let alpha = textureSampleLevel(texBlurred, smp, uv, 0.0).a;\n  return textureSampleLevel(texRamp, smp2, vec2f(alpha, 0.5), 0.0);\n}';

  public static final HALFTONE_FRAGMENT_WGSL__wgpuHalftoneEffect:Dynamic = '\nstruct Uniforms {\n  u_scale : f32,\n  u_angle : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let lum = dot(c.rgb, vec3f(0.2126, 0.7152, 0.0722));\n  let p = uv * uni.u_resolution;\n  let s = sin(uni.u_angle);\n  let co = cos(uni.u_angle);\n  let rp = vec2f(p.x * co - p.y * s, p.x * s + p.y * co);\n  let cell = (rp % vec2f(uni.u_scale)) - uni.u_scale * 0.5;\n  let dist = length(cell) / (uni.u_scale * 0.5);\n  let radius = sqrt(1.0 - lum);\n  let dot1 = step(dist, radius);\n  return vec4f(c.rgb * dot1, c.a);\n}';

  public static final INNER_CLIP_WGSL__wgpuEffectTintShader:Dynamic = '\nstruct Uniforms { _u : f32, _pad0 : f32, _pad1 : f32, _pad2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var texGlow : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n@group(2) @binding(0) var texSrc : texture_2d<f32>;\n@group(2) @binding(1) var smp2 : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let glow = textureSampleLevel(texGlow, smp, uv, 0.0);\n  let srcAlpha = textureSampleLevel(texSrc, smp2, uv, 0.0).a;\n  return glow * srcAlpha;\n}';

  @:keep public static function hasWgpuRenderEffectRunner(state:WgpuRenderState, kind:String):Bool {
    return cast FlightRuntime.coalesce(FlightRuntime.callOptionalProperty(FlightRuntime.callProperty(EffectsWgpu._registries__wgpuRenderEffectRegistry, 'get', cast ([state] : Array<Dynamic>)), 'has', cast ([kind] : Array<Dynamic>)), function():Dynamic return cast false);
    return cast null;
  }

  public static final innerClipPipelines__wgpuEffectTintShader:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final INVERT_TINT_WGSL__wgpuEffectTintShader:Dynamic = '\nstruct Uniforms {\n  colorAlpha : vec4f,\n  strength : f32,\n  _pad0 : f32, _pad1 : f32, _pad2 : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let a = min(1.0, (1.0 - textureSampleLevel(tex, smp, uv, 0.0).a) * uni.colorAlpha.w * uni.strength);\n  return vec4f(uni.colorAlpha.xyz * a, a);\n}';

  public static final invertTintPipelines__wgpuEffectTintShader:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final KUWAHARA_FRAGMENT_WGSL__wgpuKuwaharaEffect:Dynamic = '\nstruct Uniforms {\n  u_radius : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nconst R : i32 = 4;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = 1.0 / uni.u_resolution;\n  let r = i32(min(f32(R), uni.u_radius));\n  var means : array<vec3f, 4>;\n  var vars : array<f32, 4>;\n  var lo = array<vec2i, 4>(vec2i(-1, -1), vec2i(0, -1), vec2i(-1, 0), vec2i(0, 0));\n  for (var q = 0; q < 4; q++) {\n    var sum = vec3f(0.0);\n    var sumSq = vec3f(0.0);\n    var n = 0.0;\n    for (var y = 0; y <= R; y++) {\n      for (var x = 0; x <= R; x++) {\n        if (x > r || y > r) { continue; }\n        let d = vec2i(x, y) * sign(lo[q] + vec2i(1)) + lo[q] * r;\n        let off = vec2f(f32(d.x), f32(d.y)) * texel;\n        let col = textureSampleLevel(tex, smp, uv + off, 0.0).rgb;\n        sum += col;\n        sumSq += col * col;\n        n += 1.0;\n      }\n    }\n    let mean = sum / n;\n    means[q] = mean;\n    let v = sumSq / n - mean * mean;\n    vars[q] = v.r + v.g + v.b;\n  }\n  var minVar = vars[0];\n  var result = means[0];\n  for (var q = 1; q < 4; q++) {\n    if (vars[q] < minVar) {\n      minVar = vars[q];\n      result = means[q];\n    }\n  }\n  return vec4f(result, textureSampleLevel(tex, smp, uv, 0.0).a);\n}';

  public static final LENS_DIRT_FRAGMENT_WGSL__wgpuLensDirtEffect:Dynamic = '\nstruct Uniforms {\n  u_intensity : f32,\n  u_threshold : f32,\n  u_seed : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn dirtHash(p : vec2f) -> f32 { return fract(sin(dot(p, vec2f(127.1, 311.7))) * 43758.5453123); }\n\nfn dirtAmount(uv : vec2f, seed : f32) -> f32 {\n  var acc = 0.0;\n  for (var i = 0; i < 8; i = i + 1) {\n    let fi = f32(i);\n    let c = vec2f(dirtHash(vec2f(fi, seed)), dirtHash(vec2f(fi + 9.0, seed)));\n    let r = 0.06 + 0.16 * dirtHash(vec2f(fi + 3.0, seed));\n    let d = distance(uv, c) / r;\n    acc = acc + smoothstep(1.0, 0.0, d) * (0.3 + 0.7 * dirtHash(vec2f(fi + 5.0, seed)));\n  }\n  return clamp(acc, 0.0, 1.0);\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let lum = dot(c.rgb, vec3f(0.299, 0.587, 0.114));\n  let bright = max(0.0, lum - uni.u_threshold);\n  let dirt = dirtAmount(uv, uni.u_seed + 1.0);\n  return vec4f(c.rgb + bright * dirt * uni.u_intensity * 2.0, c.a);\n}';

  public static final LENS_DISTORTION_FRAGMENT_WGSL__wgpuLensDistortionEffect:Dynamic = '\nstruct Uniforms {\n  u_amount : f32,\n  u_scale : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let centered = (uv - vec2f(0.5)) / uni.u_scale;\n  let r2 = dot(centered, centered);\n  let distorted = centered * (1.0 + uni.u_amount * r2) + vec2f(0.5);\n  if (distorted.x < 0.0 || distorted.x > 1.0 || distorted.y < 0.0 || distorted.y > 1.0) {\n    return vec4f(0.0, 0.0, 0.0, 1.0);\n  }\n  return textureSampleLevel(tex, smp, distorted, 0.0);\n}';

  public static final LENS_FLARE_FRAGMENT_WGSL__wgpuLensFlareEffect:Dynamic = '\nstruct Uniforms {\n  u_threshold : f32,\n  u_intensity : f32,\n  u_ghosts : f32,\n  u_halo : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn brightPass(uv : vec2f) -> vec3f {\n  if (uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) { return vec3f(0.0); }\n  let c = textureSampleLevel(tex, smp, uv, 0.0).rgb;\n  let l = dot(c, vec3f(0.2126, 0.7152, 0.0722));\n  return c * max(0.0, l - uni.u_threshold);\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let scene = textureSampleLevel(tex, smp, uv, 0.0);\n  // Single-pass approximation of a flare: walk ghost samples along the vector toward the optical\n  // center and add a halo ring, all from the bright pass of the scene itself (no separate buffer).\n  let toCenter = vec2f(0.5) - uv;\n  var flare = vec3f(0.0);\n  let count = i32(clamp(uni.u_ghosts, 0.0, 8.0));\n  for (var i = 0; i < 8; i = i + 1) {\n    if (i >= count) { break; }\n    let t = (f32(i) + 1.0) / (f32(count) + 1.0);\n    let ghostUv = uv + toCenter * (2.0 * t);\n    flare = flare + brightPass(ghostUv);\n  }\n  let haloDir = normalize(toCenter + vec2f(1e-5));\n  flare = flare + brightPass(uv + haloDir * uni.u_halo) * uni.u_halo;\n  return vec4f(scene.rgb + flare * uni.u_intensity, scene.a);\n}';

  public static final lookupPipelines__wgpuGradientGlowEffect:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final lutBindGroupLayouts__wgpuColorLutPass:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final lutPipelines__wgpuColorLutPass:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final MAX_CONVOLUTION_EFFECT_WGPU_KERNEL_SIZE:Dynamic = 49.0;

  public static final CONVOLUTION_WGSL__wgpuConvolutionEffect:Dynamic = '\nstruct Uniforms {\n  texelSize : vec2f,\n  matrixX : i32,\n  matrixY : i32,\n  divisor : f32,\n  bias : f32,\n  clampEdge : i32,\n  preserveAlpha : i32,\n  edgeColor : vec4f,\n  matrix : array<f32, ' + Std.string(MAX_CONVOLUTION_EFFECT_WGPU_KERNEL_SIZE) + '>,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn sampleAt(uv : vec2f) -> vec4f {\n  if (uni.clampEdge != 0) {\n    return textureSampleLevel(tex, smp, clamp(uv, vec2f(0.0), vec2f(1.0)), 0.0);\n  }\n  if (uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) {\n    return uni.edgeColor;\n  }\n  return textureSampleLevel(tex, smp, uv, 0.0);\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let offsetX = uni.matrixX / 2;\n  let offsetY = uni.matrixY / 2;\n  var sum = vec4f(0.0);\n  for (var ky : i32 = 0; ky < uni.matrixY; ky++) {\n    for (var kx : i32 = 0; kx < uni.matrixX; kx++) {\n      let weight = uni.matrix[ky * uni.matrixX + kx];\n      let off = vec2f(f32(kx - offsetX), f32(ky - offsetY)) * uni.texelSize;\n      sum += sampleAt(uv + off) * weight;\n    }\n  }\n  sum = sum / uni.divisor;\n  sum += uni.bias / 255.0;\n  sum = clamp(sum, vec4f(0.0), vec4f(1.0));\n  if (uni.preserveAlpha != 0) {\n    let origAlpha = textureSampleLevel(tex, smp, uv, 0.0).a;\n    let straightRGB = select(vec3f(0.0), clamp(sum.rgb / sum.a, vec3f(0.0), vec3f(1.0)), sum.a > 0.0);\n    sum = vec4f(straightRGB * origAlpha, origAlpha);\n  }\n  return sum;\n}';

  public static final MAX_MEDIAN_EFFECT_WGPU_RADIUS:Dynamic = 2.0;

  public static final MAX_SAMPLES__wgpuMedianEffect:Dynamic = (((MAX_MEDIAN_EFFECT_WGPU_RADIUS * 2.0) + 1.0) * ((MAX_MEDIAN_EFFECT_WGPU_RADIUS * 2.0) + 1.0));

  public static final MEDIAN_WGSL__wgpuMedianEffect:Dynamic = '\nstruct Uniforms {\n  texelSize : vec2f,\n  radius : i32,\n  _pad : i32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn sortArr(arr : ptr<function, array<f32, ' + Std.string(EffectsWgpu.MAX_SAMPLES__wgpuMedianEffect) + '>>, n : i32) {\n  for (var i = 1; i < n; i++) {\n    let key = (*arr)[i];\n    var j = i - 1;\n    loop {\n      if (j < 0 || (*arr)[j] <= key) { break; }\n      (*arr)[j + 1] = (*arr)[j];\n      j -= 1;\n    }\n    (*arr)[j + 1] = key;\n  }\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let r = clamp(uni.radius, 0, ' + Std.string(MAX_MEDIAN_EFFECT_WGPU_RADIUS) + ');\n  if (r == 0) { return textureSampleLevel(tex, smp, uv, 0.0); }\n  let n = (2 * r + 1) * (2 * r + 1);\n  var rv : array<f32, ' + Std.string(EffectsWgpu.MAX_SAMPLES__wgpuMedianEffect) + '>;\n  var gv : array<f32, ' + Std.string(EffectsWgpu.MAX_SAMPLES__wgpuMedianEffect) + '>;\n  var bv : array<f32, ' + Std.string(EffectsWgpu.MAX_SAMPLES__wgpuMedianEffect) + '>;\n  var av : array<f32, ' + Std.string(EffectsWgpu.MAX_SAMPLES__wgpuMedianEffect) + '>;\n  var count = 0;\n  for (var dy = -' + Std.string(MAX_MEDIAN_EFFECT_WGPU_RADIUS) + '; dy <= ' + Std.string(MAX_MEDIAN_EFFECT_WGPU_RADIUS) + '; dy++) {\n    for (var dx = -' + Std.string(MAX_MEDIAN_EFFECT_WGPU_RADIUS) + '; dx <= ' + Std.string(MAX_MEDIAN_EFFECT_WGPU_RADIUS) + '; dx++) {\n      if (abs(dy) <= r && abs(dx) <= r) {\n        let s = textureSampleLevel(tex, smp, uv + vec2f(f32(dx), f32(dy)) * uni.texelSize, 0.0);\n        rv[count] = s.r;\n        gv[count] = s.g;\n        bv[count] = s.b;\n        av[count] = s.a;\n        count += 1;\n      }\n    }\n  }\n  sortArr(&rv, n);\n  sortArr(&gv, n);\n  sortArr(&bv, n);\n  sortArr(&av, n);\n  let mid = n / 2;\n  return vec4f(rv[mid], gv[mid], bv[mid], av[mid]);\n}';

  public static final MOTION_BLUR_FRAGMENT_WGSL__wgpuMotionBlurEffect:Dynamic = '\nstruct Uniforms {\n  u_intensity : f32,\n  u_samples : f32,\n  u_resolution : vec2f,\n  u_hasVelocity : f32,\n  _pad0 : f32,\n  _pad1 : f32,\n  _pad2 : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex0 : texture_2d<f32>;\n@group(1) @binding(1) var smp0 : sampler;\n@group(2) @binding(0) var tex1 : texture_2d<f32>;\n@group(2) @binding(1) var smp1 : sampler;\n\nconst SAMPLES : i32 = 16;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let base = textureSampleLevel(tex0, smp0, uv, 0.0);\n  if (uni.u_hasVelocity < 0.5) {\n    // Sentinel path: no velocity buffer written — passthrough copy.\n    return base;\n  }\n  // Velocity decode: rgba16f buffer stores screen-space velocity in pixels in the RG channels. Convert\n  // to a UV-space smear vector via u_resolution and scale by intensity.\n  let velocityPixels = textureSampleLevel(tex1, smp1, uv, 0.0).rg;\n  let smear = (velocityPixels / uni.u_resolution) * uni.u_intensity;\n  let count = min(uni.u_samples, 16.0);\n  var sum = vec4f(0.0);\n  var taken = 0.0;\n  for (var i = 0; i < SAMPLES; i = i + 1) {\n    if (f32(i) >= count) { break; }\n    // Center the taps on the fragment: t in [-0.5, 0.5] spreads the accumulation along the motion vector.\n    let t = select(0.0, (f32(i) / (count - 1.0)) - 0.5, count > 1.0);\n    let p = uv + smear * t;\n    sum = sum + textureSampleLevel(tex0, smp0, p, 0.0);\n    taken = taken + 1.0;\n  }\n  return sum / max(taken, 1.0);\n}';

  public static final OUTLINE_FRAGMENT_WGSL__wgpuOutlineEffect:Dynamic = '\nstruct Uniforms {\n  u_threshold : f32,\n  u_thickness : f32,\n  u_resolution : vec2f,\n  u_color : vec4f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn lum(uv : vec2f) -> f32 {\n  return dot(textureSampleLevel(tex, smp, uv, 0.0).rgb, vec3f(0.2126, 0.7152, 0.0722));\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = uni.u_thickness / uni.u_resolution;\n  let tl = lum(uv + texel * vec2f(-1.0, -1.0));\n  let t = lum(uv + texel * vec2f(0.0, -1.0));\n  let tr = lum(uv + texel * vec2f(1.0, -1.0));\n  let l = lum(uv + texel * vec2f(-1.0, 0.0));\n  let rr = lum(uv + texel * vec2f(1.0, 0.0));\n  let bl = lum(uv + texel * vec2f(-1.0, 1.0));\n  let b = lum(uv + texel * vec2f(0.0, 1.0));\n  let br = lum(uv + texel * vec2f(1.0, 1.0));\n  let gx = -tl - 2.0 * l - bl + tr + 2.0 * rr + br;\n  let gy = -tl - 2.0 * t - tr + bl + 2.0 * b + br;\n  let edge = sqrt(gx * gx + gy * gy);\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let k = step(uni.u_threshold, edge);\n  return mix(c, uni.u_color, k * uni.u_color.a);\n}';

  @:keep public static function packBackgroundClearColor__wgpuRenderEffectPipeline(rgba:Array<Float>):Float {
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var g:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var a:Dynamic = cast FlightRuntime.UNDEFINED;
    r = (Std.int(FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.coalesce(FlightRuntime.getIndex(rgba, 0.0), function():Dynamic return cast 0.0) * 255.0)] : Array<Dynamic>))) & Std.int(255.0));
    g = (Std.int(FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.coalesce(FlightRuntime.getIndex(rgba, 1.0), function():Dynamic return cast 0.0) * 255.0)] : Array<Dynamic>))) & Std.int(255.0));
    b = (Std.int(FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.coalesce(FlightRuntime.getIndex(rgba, 2.0), function():Dynamic return cast 0.0) * 255.0)] : Array<Dynamic>))) & Std.int(255.0));
    a = (Std.int(FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.coalesce(FlightRuntime.getIndex(rgba, 3.0), function():Dynamic return cast 0.0) * 255.0)] : Array<Dynamic>))) & Std.int(255.0));
    return cast FlightRuntime.unsignedShiftRight(Std.int((Std.int((Std.int((Std.int((Std.int(r) << Std.int(24.0))) | Std.int((Std.int(g) << Std.int(16.0))))) | Std.int((Std.int(b) << Std.int(8.0))))) | Std.int(a))), Std.int(0.0));
    return cast null;
  }

  @:keep public static function packColor__wgpuEffectTintShader(color:Float):Array<Float> {
    return cast cast ([((Std.int((Std.int(color) >> Std.int(16.0))) & Std.int(255.0)) / 255.0), ((Std.int((Std.int(color) >> Std.int(8.0))) & Std.int(255.0)) / 255.0), ((Std.int(color) & Std.int(255.0)) / 255.0)] : Array<Dynamic>);
    return cast null;
  }

  public static final PIXELATE_FRAGMENT_WGSL__wgpuPixelateEffect:Dynamic = '\nstruct Uniforms {\n  u_size : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uvIn : vec2f) -> @location(0) vec4f {\n  let blocks = uni.u_resolution / uni.u_size;\n  let uv = (floor(uvIn * blocks) + 0.5) / blocks;\n  return textureSampleLevel(tex, smp, uv, 0.0);\n}';

  public static final POSTERIZE_FRAGMENT_WGSL__wgpuPosterizeEffect:Dynamic = '\nstruct Uniforms { u_levels : f32, _pad0 : f32, _pad1 : f32, _pad2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let rgb = floor(c.rgb * uni.u_levels) / (uni.u_levels - 1.0);\n  return vec4f(clamp(rgb, vec3f(0.0), vec3f(1.0)), c.a);\n}';

  public static final PREMUL_BLEND__wgpuEffectPass:Dynamic = { color: { srcFactor: 'one', dstFactor: 'one-minus-src-alpha', operation: 'add' }, alpha: { srcFactor: 'one', dstFactor: 'one-minus-src-alpha', operation: 'add' } };

  public static final PRESENT_FRAGMENT_WGSL__wgpuRenderEffectPipeline:Dynamic = '\nstruct Uniforms { _u : f32, _pad0 : f32, _pad1 : f32, _pad2 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  return textureSampleLevel(tex, smp, uv, 0.0);\n}';

  @:keep public static function presentWgpuRenderEffectResult__wgpuRenderEffectPipeline(state:WgpuRenderState, source:WgpuRenderTarget):Void {
    var runtime:Dynamic = cast FlightRuntime.UNDEFINED;
    var pipeline:Dynamic = cast FlightRuntime.UNDEFINED;
    runtime = FlightRuntime.callValue(getWgpuRenderStateRuntime, cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(FlightRuntime.field(runtime, 'commandEncoder'), null))) { return; }
    pipeline = FlightRuntime.callValue(getWgpuEffectPipeline, cast ([state, 'effect.present', EffectsWgpu.PRESENT_FRAGMENT_WGSL__wgpuRenderEffectPipeline, 'replace'] : Array<Dynamic>));
    FlightRuntime.callValue(drawWgpuEffectPass, cast ([state, (cast source : WgpuRenderTarget), null, pipeline, function() {

}] : Array<Dynamic>));
  }

  public static final RADIAL_BLUR_FRAGMENT_WGSL__wgpuRadialBlurEffect:Dynamic = '\nstruct Uniforms {\n  u_center : vec2f,\n  u_strength : f32,\n  u_samples : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nconst SAMPLES : i32 = 16;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let toCenter = uni.u_center - uv;\n  let count = min(uni.u_samples, 16.0);\n  var sum = vec4f(0.0);\n  var taken = 0.0;\n  for (var i = 0; i < SAMPLES; i = i + 1) {\n    if (f32(i) >= count) { break; }\n    let t = select(0.0, f32(i) / (count - 1.0), count > 1.0);\n    let p = uv + toCenter * (t * uni.u_strength);\n    sum = sum + textureSampleLevel(tex, smp, p, 0.0);\n    taken = taken + 1.0;\n  }\n  return sum / max(taken, 1.0);\n}';

  public static final rampCaches__wgpuEffectGradientRamp:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  @:keep public static function registerAntialiasingWgpuRenderEffects(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'FxaaEffect', defaultWgpuFxaaEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'SmaaEffect', defaultWgpuSmaaEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'TaaEffect', defaultWgpuTaaEffectRunner] : Array<Dynamic>));
  }

  @:keep public static function registerBloomWgpuRenderEffects(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'BloomEffect', defaultWgpuBloomEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'ChromaticAberrationEffect', defaultWgpuChromaticAberrationEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'GodRaysEffect', defaultWgpuGodRaysEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'LensDirtEffect', defaultWgpuLensDirtEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'LensDistortionEffect', defaultWgpuLensDistortionEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'LensFlareEffect', defaultWgpuLensFlareEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'VignetteEffect', defaultWgpuVignetteEffectRunner] : Array<Dynamic>));
  }

  @:keep public static function registerBlurWgpuRenderEffects(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'BlurEffect', defaultWgpuBlurEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'BokehDepthOfFieldEffect', defaultWgpuBokehDepthOfFieldEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'CameraMotionBlurEffect', defaultWgpuCameraMotionBlurEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'DirectionalBlurEffect', defaultWgpuDirectionalBlurEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'MotionBlurEffect', defaultWgpuMotionBlurEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'RadialBlurEffect', defaultWgpuRadialBlurEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'TiltShiftEffect', defaultWgpuTiltShiftEffectRunner] : Array<Dynamic>));
  }

  @:keep public static function registerColorWgpuRenderEffects(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'ToneMapEffect', defaultWgpuToneMapEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'WhiteBalanceEffect', defaultWgpuWhiteBalanceEffectRunner] : Array<Dynamic>));
  }

  @:keep public static function registerCompositeWgpuRenderEffects(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'BevelEffect', defaultWgpuBevelEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'DropShadowEffect', defaultWgpuDropShadowEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'GradientBevelEffect', defaultWgpuGradientBevelEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'GradientGlowEffect', defaultWgpuGradientGlowEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'InnerGlowEffect', defaultWgpuInnerGlowEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'InnerShadowEffect', defaultWgpuInnerShadowEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'OuterGlowEffect', defaultWgpuOuterGlowEffectRunner] : Array<Dynamic>));
  }

  @:keep public static function registerScreenSpaceWgpuRenderEffects(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'DisplacementEffect', defaultWgpuDisplacementEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'ScreenSpaceFogEffect', defaultWgpuScreenSpaceFogEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'SharpenEffect', defaultWgpuSharpenEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'SsaoEffect', defaultWgpuSsaoEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'SsrEffect', defaultWgpuSsrEffectRunner] : Array<Dynamic>));
  }

  @:keep public static function registerStandardWgpuRenderEffects(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerAntialiasingWgpuRenderEffects, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(registerBloomWgpuRenderEffects, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(registerBlurWgpuRenderEffects, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(registerColorWgpuRenderEffects, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(registerCompositeWgpuRenderEffects, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(registerScreenSpaceWgpuRenderEffects, cast ([state] : Array<Dynamic>));
    FlightRuntime.callValue(registerStylizeWgpuRenderEffects, cast ([state] : Array<Dynamic>));
  }

  @:keep public static function registerStylizeWgpuRenderEffects(state:WgpuRenderState):Void {
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'ConvolutionEffect', defaultWgpuConvolutionEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'CrtEffect', defaultWgpuCrtEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'DitherEffect', defaultWgpuDitherEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'FilmGrainEffect', defaultWgpuFilmGrainEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'GlitchEffect', defaultWgpuGlitchEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'HalftoneEffect', defaultWgpuHalftoneEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'KuwaharaEffect', defaultWgpuKuwaharaEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'MedianEffect', defaultWgpuMedianEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'OutlineEffect', defaultWgpuOutlineEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'PixelateEffect', defaultWgpuPixelateEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'PosterizeEffect', defaultWgpuPosterizeEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'ScanlinesEffect', defaultWgpuScanlinesEffectRunner] : Array<Dynamic>));
    FlightRuntime.callValue(registerWgpuRenderEffect, cast ([state, 'SketchEffect', defaultWgpuSketchEffectRunner] : Array<Dynamic>));
  }

  @:keep public static function registerWgpuRenderEffect(state:WgpuRenderState, kind:String, runner:WgpuRenderEffectRunner):Void {
    var registry:Dynamic = cast FlightRuntime.UNDEFINED;
    registry = FlightRuntime.callProperty(EffectsWgpu._registries__wgpuRenderEffectRegistry, 'get', cast ([state] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(registry, FlightRuntime.UNDEFINED))) {
  (registry = cast (FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []) : Dynamic));
  FlightRuntime.callProperty(EffectsWgpu._registries__wgpuRenderEffectRegistry, 'set', cast ([state, registry] : Array<Dynamic>));
}
    FlightRuntime.callProperty(registry, 'set', cast ([kind, runner] : Array<Dynamic>));
  }

  public static final REPLACE_BLEND__wgpuColorLutPass:Dynamic = { color: { srcFactor: 'one', dstFactor: 'zero', operation: 'add' }, alpha: { srcFactor: 'one', dstFactor: 'zero', operation: 'add' } };

  public static final REPLACE_BLEND__wgpuEffectPass:Dynamic = { color: { srcFactor: 'one', dstFactor: 'zero', operation: 'add' }, alpha: { srcFactor: 'one', dstFactor: 'zero', operation: 'add' } };

  @:keep public static function resolveEffectPipeline__wgpuEffectPass(state:WgpuRenderState, pipeline:WgpuEffectPipeline, dest:Null<WgpuRenderTarget>):Dynamic {
    var canvasFormat:Dynamic = cast FlightRuntime.UNDEFINED;
    var targetFormat:Dynamic = cast FlightRuntime.UNDEFINED;
    var variant:Dynamic = cast FlightRuntime.UNDEFINED;
    canvasFormat = FlightRuntime.field(FlightRuntime.callValue(EffectsWgpu.getOrCreateEffectPassState__wgpuEffectPass, cast ([state] : Array<Dynamic>)), 'format');
    targetFormat = FlightRuntime.select(!FlightRuntime.strictEquals(dest, null), function():Dynamic return cast FlightRuntime.field(dest, 'format'), function():Dynamic return cast canvasFormat);
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.field(pipeline, 'compileForFormat'), FlightRuntime.UNDEFINED), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(pipeline, 'variants'), FlightRuntime.UNDEFINED)), function():Dynamic return cast FlightRuntime.strictEquals(targetFormat, canvasFormat)))) {
  return cast FlightRuntime.field(pipeline, 'pipeline');
}
    variant = FlightRuntime.callProperty(FlightRuntime.field(pipeline, 'variants'), 'get', cast ([targetFormat] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(variant, FlightRuntime.UNDEFINED))) {
  (variant = cast (FlightRuntime.callProperty(pipeline, 'compileForFormat', cast ([targetFormat] : Array<Dynamic>)) : Dynamic));
  FlightRuntime.callProperty(FlightRuntime.field(pipeline, 'variants'), 'set', cast ([targetFormat, variant] : Array<Dynamic>));
}
    return cast variant;
    return cast null;
  }

  public static final SCANLINES_FRAGMENT_WGSL__wgpuScanlinesEffect:Dynamic = '\nstruct Uniforms {\n  u_count : f32,\n  u_intensity : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let line = sin(uv.y * uni.u_count * 3.14159265) * 0.5 + 0.5;\n  return vec4f(c.rgb * (1.0 - uni.u_intensity * (1.0 - line)), c.a);\n}';

  public static final SCREEN_SPACE_FOG_FRAGMENT_WGSL__wgpuScreenSpaceFogEffect:Dynamic = '\nstruct Uniforms {\n  u_density : f32,\n  _pad0 : f32,\n  _pad1 : f32,\n  _pad2 : f32,\n  u_fogColor : vec3f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  // Color-only fallback: no depth G-buffer in Wgpu yet — screen-Y gradient as a depth proxy.\n  // The real version reads depth and computes fog = 1 - exp(-density * remap(depth, near, far)).\n  let fog = clamp((1.0 - uv.y) * uni.u_density, 0.0, 1.0);\n  return vec4f(mix(c.rgb, uni.u_fogColor, fog), c.a);\n}';

  @:keep public static function setWgpuRenderEffectVelocityTexture(pipeline:WgpuRenderEffectPipeline, texture:Null<Dynamic>):Void {
    FlightRuntime.setField(pipeline, 'velocityTexture', texture);
  }

  public static final SHARPEN_FRAGMENT_WGSL__wgpuSharpenEffect:Dynamic = '\nstruct Uniforms {\n  u_amount : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = 1.0 / uni.u_resolution;\n  let c = textureSampleLevel(tex, smp, uv, 0.0).rgb;\n  let n = textureSampleLevel(tex, smp, uv + vec2f(0.0, -texel.y), 0.0).rgb;\n  let s = textureSampleLevel(tex, smp, uv + vec2f(0.0, texel.y), 0.0).rgb;\n  let e = textureSampleLevel(tex, smp, uv + vec2f(texel.x, 0.0), 0.0).rgb;\n  let w = textureSampleLevel(tex, smp, uv + vec2f(-texel.x, 0.0), 0.0).rgb;\n  let high = c * 4.0 - n - s - e - w;\n  let a = textureSampleLevel(tex, smp, uv, 0.0).a;\n  return vec4f(clamp(c + high * uni.u_amount, vec3f(0.0), vec3f(1.0)), a);\n}';

  public static final SKETCH_FRAGMENT_WGSL__wgpuSketchEffect:Dynamic = '\nstruct Uniforms {\n  u_strength : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn lum(uv : vec2f) -> f32 {\n  return dot(textureSampleLevel(tex, smp, uv, 0.0).rgb, vec3f(0.2126, 0.7152, 0.0722));\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = 1.0 / uni.u_resolution;\n  let tl = lum(uv + texel * vec2f(-1.0, -1.0));\n  let t = lum(uv + texel * vec2f(0.0, -1.0));\n  let tr = lum(uv + texel * vec2f(1.0, -1.0));\n  let l = lum(uv + texel * vec2f(-1.0, 0.0));\n  let rr = lum(uv + texel * vec2f(1.0, 0.0));\n  let bl = lum(uv + texel * vec2f(-1.0, 1.0));\n  let b = lum(uv + texel * vec2f(0.0, 1.0));\n  let br = lum(uv + texel * vec2f(1.0, 1.0));\n  let gx = -tl - 2.0 * l - bl + tr + 2.0 * rr + br;\n  let gy = -tl - 2.0 * t - tr + bl + 2.0 * b + br;\n  let edge = sqrt(gx * gx + gy * gy);\n  let pencil = clamp(1.0 - edge * uni.u_strength, 0.0, 1.0);\n  let a = textureSampleLevel(tex, smp, uv, 0.0).a;\n  return vec4f(vec3f(pencil), a);\n}';

  public static final SMAA_FRAGMENT_WGSL__wgpuSmaaEffect:Dynamic = '\nstruct Uniforms { u_resolution : vec2f, u_threshold : f32, _pad0 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn luma(c : vec3f) -> f32 {\n  return dot(c, vec3f(0.299, 0.587, 0.114));\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = 1.0 / uni.u_resolution;\n  let center = textureSampleLevel(tex, smp, uv, 0.0);\n  let lumaC = luma(center.rgb);\n  let lumaL = luma(textureSampleLevel(tex, smp, uv + vec2f(-1.0, 0.0) * texel, 0.0).rgb);\n  let lumaR = luma(textureSampleLevel(tex, smp, uv + vec2f(1.0, 0.0) * texel, 0.0).rgb);\n  let lumaT = luma(textureSampleLevel(tex, smp, uv + vec2f(0.0, -1.0) * texel, 0.0).rgb);\n  let lumaB = luma(textureSampleLevel(tex, smp, uv + vec2f(0.0, 1.0) * texel, 0.0).rgb);\n  let edge = max(abs(lumaC - lumaL), max(abs(lumaC - lumaR), max(abs(lumaC - lumaT), abs(lumaC - lumaB))));\n  if (edge < uni.u_threshold) {\n    return center;\n  }\n  let blurred = (\n    textureSampleLevel(tex, smp, uv + vec2f(-1.0, 0.0) * texel, 0.0).rgb +\n    textureSampleLevel(tex, smp, uv + vec2f(1.0, 0.0) * texel, 0.0).rgb +\n    textureSampleLevel(tex, smp, uv + vec2f(0.0, -1.0) * texel, 0.0).rgb +\n    textureSampleLevel(tex, smp, uv + vec2f(0.0, 1.0) * texel, 0.0).rgb +\n    center.rgb) / 5.0;\n  return vec4f(blurred, center.a);\n}';

  public static final SSAO_FRAGMENT_WGSL__wgpuSsaoEffect:Dynamic = '\nstruct Uniforms {\n  u_radius : f32,\n  u_intensity : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn luma(c : vec3f) -> f32 {\n  return dot(c, vec3f(0.299, 0.587, 0.114));\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = (1.0 / uni.u_resolution) * max(uni.u_radius, 1.0);\n  let center = textureSampleLevel(tex, smp, uv, 0.0);\n  let lc = luma(center.rgb);\n  var variation = 0.0;\n  variation = variation + abs(lc - luma(textureSampleLevel(tex, smp, uv + vec2f(-1.0, 0.0) * texel, 0.0).rgb));\n  variation = variation + abs(lc - luma(textureSampleLevel(tex, smp, uv + vec2f(1.0, 0.0) * texel, 0.0).rgb));\n  variation = variation + abs(lc - luma(textureSampleLevel(tex, smp, uv + vec2f(0.0, -1.0) * texel, 0.0).rgb));\n  variation = variation + abs(lc - luma(textureSampleLevel(tex, smp, uv + vec2f(0.0, 1.0) * texel, 0.0).rgb));\n  variation = variation * 0.25;\n  let occlusion = clamp(variation * uni.u_intensity, 0.0, 1.0);\n  return vec4f(center.rgb * (1.0 - occlusion), center.a);\n}';

  public static final SSR_FRAGMENT_WGSL__wgpuSsrEffect:Dynamic = '\nstruct Uniforms {\n  _pad0 : vec4f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  return textureSampleLevel(tex, smp, uv, 0.0);\n}';

  public static final TAA_FRAGMENT_WGSL__wgpuTaaEffect:Dynamic = '\nstruct Uniforms { _pad0 : f32, _pad1 : f32, _pad2 : f32, _pad3 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  return vec4f(c.rgb, c.a + uni._pad0 * 0.0);\n}';

  public static final TILT_SHIFT_FRAGMENT_WGSL__wgpuTiltShiftEffect:Dynamic = '\nstruct Uniforms {\n  u_center : f32,\n  u_width : f32,\n  u_blur : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = vec2f(1.0) / uni.u_resolution;\n  let dist = abs(uv.y - uni.u_center);\n  let edge = uni.u_width * 0.5;\n  let amount = smoothstep(edge, edge + uni.u_width, dist);\n  let radius = amount * uni.u_blur;\n  var sum = vec4f(0.0);\n  var total = 0.0;\n  for (var i = -3; i <= 3; i = i + 1) {\n    let offset = vec2f(0.0, f32(i)) * radius * texel;\n    sum = sum + textureSampleLevel(tex, smp, uv + offset, 0.0);\n    total = total + 1.0;\n  }\n  return sum / total;\n}';

  public static final TINT_WGSL__wgpuEffectTintShader:Dynamic = '\nstruct Uniforms {\n  colorAlpha : vec4f,\n  strength : f32,\n  _pad0 : f32, _pad1 : f32, _pad2 : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let a = min(1.0, textureSampleLevel(tex, smp, uv, 0.0).a * uni.colorAlpha.w * uni.strength);\n  return vec4f(uni.colorAlpha.xyz * a, a);\n}';

  public static final tintPipelines__wgpuEffectTintShader:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['WeakMap'] : Array<Dynamic>)), []);

  public static final TONEMAP_FRAGMENT_HEAD__wgpuToneMapEffect:Dynamic = '\nstruct Uniforms { u_exposure : f32, u_white : f32, _pad0 : f32, _pad1 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn tonemap(x : vec3f) -> vec3f {';

  public static final TONEMAP_FRAGMENT_TAIL__wgpuToneMapEffect:Dynamic = '}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let mapped = tonemap(c.rgb * uni.u_exposure);\n  return vec4f(clamp(mapped, vec3f(0.0), vec3f(1.0)), c.a);\n}';

  public static final TONEMAP_OPERATORS__wgpuToneMapEffect:Dynamic = { aces: '\n  let a = x * (2.51 * x + 0.03);\n  let b = x * (2.43 * x + 0.59) + 0.14;\n  return a / b;', reinhard: '\n  return x / (1.0 + x / (uni.u_white * uni.u_white));', filmic: '\n  let X = max(vec3f(0.0), x - 0.004);\n  return (X * (6.2 * X + 0.5)) / (X * (6.2 * X + 1.7) + 0.06);', uncharted2: '\n  let A = 0.15; let B = 0.50; let C = 0.10; let D = 0.20; let E = 0.02; let F = 0.30;\n  let v = ((x * (A * x + C * B) + D * E) / (x * (A * x + B) + D * F)) - E / F;\n  return v;', agx: '\n  let v = clamp((x - 0.004) / (1.0 + x), vec3f(0.0), vec3f(1.0));\n  return pow(v, vec3f(0.8));' };

  @:keep public static function uploadLutTexture__wgpuColorLutPass(state:WgpuRenderState, lut:ColorLut, cache:WgpuColorLutTextureCache):Dynamic {
    var __destructure2:Dynamic = cast FlightRuntime.UNDEFINED;
    var device:Dynamic = cast FlightRuntime.UNDEFINED;
    var n:Dynamic = cast FlightRuntime.UNDEFINED;
    var samples:Dynamic = cast FlightRuntime.UNDEFINED;
    var data:Dynamic = cast FlightRuntime.UNDEFINED;
    __destructure2 = state;
    device = FlightRuntime.field(__destructure2, 'device');
    n = FlightRuntime.field(lut, 'size');
    if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(FlightRuntime.field(cache, 'texture'), null), function():Dynamic return cast FlightRuntime.strictEquals(FlightRuntime.field(cache, 'lut'), lut)))) { return cast FlightRuntime.field(cache, 'texture'); }
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.strictEquals(FlightRuntime.field(cache, 'texture'), null), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.field(cache, 'size'), n)))) {
  FlightRuntime.callOptionalProperty(FlightRuntime.field(cache, 'texture'), 'destroy', cast ([] : Array<Dynamic>));
  FlightRuntime.setField(cache, 'texture', FlightRuntime.callProperty(device, 'createTexture', cast ([{ size: cast ([n, n, n] : Array<Dynamic>), dimension: '3d', format: 'rgba8unorm', usage: (Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUTextureUsage'] : Array<Dynamic>)), 'TEXTURE_BINDING')) | Std.int(FlightRuntime.field(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['GPUTextureUsage'] : Array<Dynamic>)), 'COPY_DST'))) }] : Array<Dynamic>)));
  FlightRuntime.setField(cache, 'size', n);
}
    samples = FlightRuntime.field(lut, 'samples');
    data = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Uint8Array'] : Array<Dynamic>)), [(((n * n) * n) * 4.0)]);
    {
      var i:Dynamic = 0.0;
      var j:Dynamic = 0.0;
      var o:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(i, ((n * n) * n), '<'))) {
        FlightRuntime.setIndex(data, o++, FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.callValue(EffectsWgpu.clamp01__wgpuColorLutPass, cast ([FlightRuntime.getIndex(samples, j++)] : Array<Dynamic>)) * 255.0)] : Array<Dynamic>)));
        FlightRuntime.setIndex(data, o++, FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.callValue(EffectsWgpu.clamp01__wgpuColorLutPass, cast ([FlightRuntime.getIndex(samples, j++)] : Array<Dynamic>)) * 255.0)] : Array<Dynamic>)));
        FlightRuntime.setIndex(data, o++, FlightRuntime.callProperty(HxMath, 'round', cast ([(FlightRuntime.callValue(EffectsWgpu.clamp01__wgpuColorLutPass, cast ([FlightRuntime.getIndex(samples, j++)] : Array<Dynamic>)) * 255.0)] : Array<Dynamic>)));
        FlightRuntime.setIndex(data, o++, 255.0);
        i++;
      }
    }
    FlightRuntime.callProperty(FlightRuntime.field(device, 'queue'), 'writeTexture', cast ([{ texture: FlightRuntime.field(cache, 'texture') }, data, { bytesPerRow: (n * 4.0), rowsPerImage: n }, cast ([n, n, n] : Array<Dynamic>)] : Array<Dynamic>));
    FlightRuntime.setField(cache, 'lut', lut);
    return cast FlightRuntime.field(cache, 'texture');
    return cast null;
  }

  public static final VIGNETTE_FRAGMENT_WGSL__wgpuVignetteEffect:Dynamic = '\nstruct Uniforms {\n  u_intensity : f32,\n  u_radius : f32,\n  u_softness : f32,\n  _pad0 : f32,\n  u_color : vec4f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let centered = uv - vec2f(0.5);\n  let dist = length(centered) * 1.41421356;\n  let vig = smoothstep(uni.u_radius, uni.u_radius - uni.u_softness, dist);\n  let darken = (1.0 - vig) * uni.u_intensity * uni.u_color.a;\n  return vec4f(mix(c.rgb, uni.u_color.rgb, darken), c.a);\n}';

  public static final WHITE_BALANCE_FRAGMENT_WGSL__wgpuWhiteBalanceEffect:Dynamic = '\nstruct Uniforms { u_temperature : f32, u_tint : f32, _pad0 : f32, _pad1 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  var rgb = c.rgb;\n  rgb.r += uni.u_temperature * 0.5;\n  rgb.b -= uni.u_temperature * 0.5;\n  rgb.g += uni.u_tint * 0.5;\n  return vec4f(clamp(rgb, vec3f(0.0), vec3f(1.0)), c.a);\n}';

  @:keep public static function writeUniformSlot__wgpuEffectPass(state:WgpuRenderState, fs:WgpuEffectPassState__wgpuEffectPass, slotOffset:Float, setUniforms:Dynamic):Void {
    var f32Start:Dynamic = cast FlightRuntime.UNDEFINED;
    var slotF32:Dynamic = cast FlightRuntime.UNDEFINED;
    var slotI32:Dynamic = cast FlightRuntime.UNDEFINED;
    f32Start = (slotOffset / 4.0);
    slotF32 = FlightRuntime.field(fs, 'uniformData').subarray(Std.int(f32Start), Std.int((f32Start + (FlightRuntime.field(fs, 'uniformStride') / 4.0))));
    slotI32 = FlightRuntime.field(fs, 'uniformDataI32').subarray(Std.int(f32Start), Std.int((f32Start + (FlightRuntime.field(fs, 'uniformStride') / 4.0))));
    FlightRuntime.callValue(setUniforms, cast ([slotF32, slotI32] : Array<Dynamic>));
    FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(state, 'device'), 'queue'), 'writeBuffer', cast ([FlightRuntime.field(fs, 'uniformBuffer'), slotOffset, FlightRuntime.field(FlightRuntime.field(fs, 'uniformData'), 'buffer'), slotOffset, FlightRuntime.field(fs, 'uniformStride')] : Array<Dynamic>));
  }
}
