// Maintained host-type declaration for the Node/Electron process value.
package flighthq._internal.dom;

interface Process {
  var arch(default, never):String;
  var platform(default, never):String;
}
