// Maintained host-type declaration. The DOM alias is an array; the generic
// parameter preserves the checker-recovered element type in generated Haxe.
package flighthq._internal.dom;

typedef ClipboardItems<T> = Array<T>;
