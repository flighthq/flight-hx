// Maintained type support for generated Flight Haxe.
package flighthq._internal;

/**
 * An intentional TypeScript erasure boundary (`any`, `unknown`, or a value
 * supplied by an external toolkit). Generated checker-known values must never
 * acquire this type as a fallback.
 */
typedef _Any = Dynamic;
