// Generated by flight-hx. Do not edit.
package flighthq;

import Math as HxMath;
import flighthq.internal.FlightRuntime;
import flighthq.DisplayObjectApi.createDisplayObjectGeneric;
import flighthq.DisplayObjectApi.createDisplayObjectRuntime;
import flighthq.DisplayObjectApi.getDisplayObjectRuntime;
import flighthq.NodeApi.invalidateContent;
import flighthq.Types.BoundsNodeAny;
import flighthq.Types.CapsStyle;
import flighthq.Types.GradientType;
import flighthq.Types.ImageResource;
import flighthq.Types.InterpolationMethod;
import flighthq.Types.JointStyle;
import flighthq.Types.LineScaleMode;
import flighthq.Types.Matrix;
import flighthq.Types.PartialNode;
import flighthq.Types.Path;
import flighthq.Types.PathCommand;
import flighthq.Types.PathWinding;
import flighthq.Types.Rectangle;
import flighthq.Types.RectangleLike;
import flighthq.Types.Scale9Shape;
import flighthq.Types.Scale9ShapeData;
import flighthq.Types.Scale9ShapeRuntime;
import flighthq.Types.Shape;
import flighthq.Types.ShapeCommandKey;
import flighthq.Types.ShapeCommandToken;
import flighthq.Types.ShapeData;
import flighthq.Types.ShapeFillRegion;
import flighthq.Types.ShapeHitTestCommand;
import flighthq.Types.ShapeRuntime;
import flighthq.Types.SpreadMethod;
import flighthq.Types.TriangleCulling;

typedef AnyHitTestFn__shapeHitTestRegistry = Dynamic;

@:expose("flighthq.ShapeApi")
class ShapeApi {
  public static function appendEllipseToPath__shapeFill(path:Path, cx:Float, cy:Float, rx:Float, ry:Float):Void {
    var kx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ky:Dynamic = cast FlightRuntime.UNDEFINED;
    kx = (rx * ShapeApi.KAPPA__shapeFill);
    ky = (ry * ShapeApi.KAPPA__shapeFill);
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.MOVE_TO, (cx + rx), cy] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushCubic__shapeFill, cast ([path, (cx + rx), (cy + ky), (cx + kx), (cy + ry), cx, (cy + ry)] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushCubic__shapeFill, cast ([path, (cx - kx), (cy + ry), (cx - rx), (cy + ky), (cx - rx), cy] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushCubic__shapeFill, cast ([path, (cx - rx), (cy - ky), (cx - kx), (cy - ry), cx, (cy - ry)] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushCubic__shapeFill, cast ([path, (cx + kx), (cy - ry), (cx + rx), (cy - ky), (cx + rx), cy] : Array<Dynamic>));
  }

  public static function appendRawPath__shapeFill(path:Path, verbs:Array<Float>, data:Array<Float>):Void {
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    d = 0.0;
    {
      var v:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(v, FlightRuntime.field(verbs, 'length'), '<'))) {
        var verb:Dynamic = FlightRuntime.getIndex(verbs, v);
        var args:Dynamic = FlightRuntime.select(FlightRuntime.strictEquals(verb, Types.PathCommandValue.CUBIC_CURVE_TO), function():Dynamic return cast 6.0, function():Dynamic return cast FlightRuntime.select(FlightRuntime.strictEquals(verb, Types.PathCommandValue.CURVE_TO), function():Dynamic return cast 4.0, function():Dynamic return cast 2.0));
        FlightRuntime.callProperty(FlightRuntime.field(path, 'commands'), 'push', cast ([verb] : Array<Dynamic>));
        {
          var k:Dynamic = 0.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(k, args, '<'))) {
            FlightRuntime.callProperty(FlightRuntime.field(path, 'data'), 'push', cast ([FlightRuntime.getIndex(data, (d + k))] : Array<Dynamic>));
            k++;
          }
        }
        (d = cast ((d + args) : Dynamic));
        v++;
      }
    }
  }

  public static function appendRectangleToPath__shapeFill(path:Path, x:Float, y:Float, w:Float, h:Float):Void {
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.MOVE_TO, x, y] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.LINE_TO, (x + w), y] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.LINE_TO, (x + w), (y + h)] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.LINE_TO, x, (y + h)] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.LINE_TO, x, y] : Array<Dynamic>));
  }

  public static function appendRoundRectangleToPath__shapeFill(path:Path, x:Float, y:Float, w:Float, h:Float, rx:Float, ry:Float):Void {
    var right:Dynamic = cast FlightRuntime.UNDEFINED;
    var bottom:Dynamic = cast FlightRuntime.UNDEFINED;
    right = (x + w);
    bottom = (y + h);
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.MOVE_TO, (x + rx), y] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.LINE_TO, (right - rx), y] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushQuadratic__shapeFill, cast ([path, right, y, right, (y + ry)] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.LINE_TO, right, (bottom - ry)] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushQuadratic__shapeFill, cast ([path, right, bottom, (right - rx), bottom] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.LINE_TO, (x + rx), bottom] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushQuadratic__shapeFill, cast ([path, x, bottom, x, (bottom - ry)] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.LINE_TO, x, (y + ry)] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushQuadratic__shapeFill, cast ([path, x, y, (x + rx), y] : Array<Dynamic>));
  }

  public static function appendShapeArc(shape:Shape, cx:Float, cy:Float, radius:Float, startAngle:Float, endAngle:Float, anticlockwise:Dynamic = false):Void {
    var cmds:Dynamic = cast FlightRuntime.UNDEFINED;
    var sweep:Dynamic = cast FlightRuntime.UNDEFINED;
    var segmentCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var segmentAngle:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    cmds = FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands');
    sweep = FlightRuntime.callValue(ShapeApi.normalizeArcSweep__shapeCommands, cast ([startAngle, endAngle, anticlockwise] : Array<Dynamic>));
    segmentCount = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'ceil', cast ([(FlightRuntime.callProperty(HxMath, 'abs', cast ([sweep] : Array<Dynamic>)) / (HxMath.PI / 2.0))] : Array<Dynamic>))] : Array<Dynamic>));
    segmentAngle = (sweep / segmentCount);
    alpha = ((4.0 / 3.0) * FlightRuntime.callProperty(HxMath, 'tan', cast ([(segmentAngle / 4.0)] : Array<Dynamic>)));
    FlightRuntime.pushMany(cmds, cast (['moveTo', 2.0, (cx + (radius * FlightRuntime.callProperty(HxMath, 'cos', cast ([startAngle] : Array<Dynamic>)))), (cy + (radius * FlightRuntime.callProperty(HxMath, 'sin', cast ([startAngle] : Array<Dynamic>))))] : Array<Dynamic>));
    FlightRuntime.callValue(ShapeApi.pushArcCubics__shapeCommands, cast ([cmds, cx, cy, radius, startAngle, segmentCount, segmentAngle, alpha] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeArcTo(shape:Shape, x1:Float, y1:Float, x2:Float, y2:Float, radius:Float):Void {
    var cmds:Dynamic = cast FlightRuntime.UNDEFINED;
    var penX:Dynamic = cast FlightRuntime.UNDEFINED;
    var penY:Dynamic = cast FlightRuntime.UNDEFINED;
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    var d1x:Dynamic = cast FlightRuntime.UNDEFINED;
    var d1y:Dynamic = cast FlightRuntime.UNDEFINED;
    var d2x:Dynamic = cast FlightRuntime.UNDEFINED;
    var d2y:Dynamic = cast FlightRuntime.UNDEFINED;
    var len1:Dynamic = cast FlightRuntime.UNDEFINED;
    var len2:Dynamic = cast FlightRuntime.UNDEFINED;
    var cosHalf:Dynamic = cast FlightRuntime.UNDEFINED;
    var clampedCos:Dynamic = cast FlightRuntime.UNDEFINED;
    var halfAngle:Dynamic = cast FlightRuntime.UNDEFINED;
    var d:Dynamic = cast FlightRuntime.UNDEFINED;
    var n1x:Dynamic = cast FlightRuntime.UNDEFINED;
    var n1y:Dynamic = cast FlightRuntime.UNDEFINED;
    var tx1:Dynamic = cast FlightRuntime.UNDEFINED;
    var ty1:Dynamic = cast FlightRuntime.UNDEFINED;
    var n2x:Dynamic = cast FlightRuntime.UNDEFINED;
    var n2y:Dynamic = cast FlightRuntime.UNDEFINED;
    var tx2:Dynamic = cast FlightRuntime.UNDEFINED;
    var ty2:Dynamic = cast FlightRuntime.UNDEFINED;
    var bx:Dynamic = cast FlightRuntime.UNDEFINED;
    var by:Dynamic = cast FlightRuntime.UNDEFINED;
    var blen:Dynamic = cast FlightRuntime.UNDEFINED;
    var distToCenter:Dynamic = cast FlightRuntime.UNDEFINED;
    var ocx:Dynamic = cast FlightRuntime.UNDEFINED;
    var ocy:Dynamic = cast FlightRuntime.UNDEFINED;
    var startA:Dynamic = cast FlightRuntime.UNDEFINED;
    var endA:Dynamic = cast FlightRuntime.UNDEFINED;
    var cross:Dynamic = cast FlightRuntime.UNDEFINED;
    var isAnticlockwise:Dynamic = cast FlightRuntime.UNDEFINED;
    var sweep:Dynamic = cast FlightRuntime.UNDEFINED;
    var segmentCount:Dynamic = cast FlightRuntime.UNDEFINED;
    var segmentAngle:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    cmds = FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands');
    penX = 0.0;
    penY = 0.0;
    i = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(cmds, 'length'), '<'))) {
  var key:Dynamic = (cast FlightRuntime.getIndex(cmds, i) : String);
  var argCount:Dynamic = (cast FlightRuntime.getIndex(cmds, (i + 1.0)) : Float);
  var b:Dynamic = (i + 2.0);
  {
    var __switchValue = key;
    if (__switchValue == 'moveTo' || __switchValue == 'lineTo') {
      (penX = cast ((cast FlightRuntime.getIndex(cmds, b) : Float) : Dynamic));
      (penY = cast ((cast FlightRuntime.getIndex(cmds, (b + 1.0)) : Float) : Dynamic));
    }
    else if (__switchValue == 'curveTo') {
      (penX = cast ((cast FlightRuntime.getIndex(cmds, (b + 2.0)) : Float) : Dynamic));
      (penY = cast ((cast FlightRuntime.getIndex(cmds, (b + 3.0)) : Float) : Dynamic));
    }
    else if (__switchValue == 'cubicCurveTo') {
      (penX = cast ((cast FlightRuntime.getIndex(cmds, (b + 4.0)) : Float) : Dynamic));
      (penY = cast ((cast FlightRuntime.getIndex(cmds, (b + 5.0)) : Float) : Dynamic));
    }
  }
  (i = cast ((i + (argCount + 2.0)) : Dynamic));
}
    d1x = (penX - x1);
    d1y = (penY - y1);
    d2x = (x2 - x1);
    d2y = (y2 - y1);
    len1 = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((d1x * d1x) + (d1y * d1y))] : Array<Dynamic>));
    len2 = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((d2x * d2x) + (d2y * d2y))] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.compare(len1, 1e-10, '<'), function():Dynamic return cast FlightRuntime.compare(len2, 1e-10, '<')))) {
  FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, x1, y1] : Array<Dynamic>));
  FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  return;
}
    cosHalf = (((d1x * d2x) + (d1y * d2y)) / (len1 * len2));
    clampedCos = FlightRuntime.callProperty(HxMath, 'max', cast ([-1.0, FlightRuntime.callProperty(HxMath, 'min', cast ([1.0, cosHalf] : Array<Dynamic>))] : Array<Dynamic>));
    halfAngle = (FlightRuntime.callProperty(HxMath, 'acos', cast ([clampedCos] : Array<Dynamic>)) / 2.0);
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([FlightRuntime.callProperty(HxMath, 'sin', cast ([halfAngle] : Array<Dynamic>))] : Array<Dynamic>)), 1e-10, '<'))) {
  FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, x1, y1] : Array<Dynamic>));
  FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  return;
}
    d = (radius / FlightRuntime.callProperty(HxMath, 'tan', cast ([halfAngle] : Array<Dynamic>)));
    n1x = (d1x / len1);
    n1y = (d1y / len1);
    tx1 = (x1 + (n1x * d));
    ty1 = (y1 + (n1y * d));
    n2x = (d2x / len2);
    n2y = (d2y / len2);
    tx2 = (x1 + (n2x * d));
    ty2 = (y1 + (n2y * d));
    FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, tx1, ty1] : Array<Dynamic>));
    bx = ((n1x + n2x) / 2.0);
    by = ((n1y + n2y) / 2.0);
    blen = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([((bx * bx) + (by * by))] : Array<Dynamic>));
    distToCenter = (radius / FlightRuntime.callProperty(HxMath, 'sin', cast ([halfAngle] : Array<Dynamic>)));
    ocx = (x1 + ((bx / blen) * distToCenter));
    ocy = (y1 + ((by / blen) * distToCenter));
    startA = FlightRuntime.callProperty(HxMath, 'atan2', cast ([(ty1 - ocy), (tx1 - ocx)] : Array<Dynamic>));
    endA = FlightRuntime.callProperty(HxMath, 'atan2', cast ([(ty2 - ocy), (tx2 - ocx)] : Array<Dynamic>));
    cross = ((d1x * d2y) - (d1y * d2x));
    isAnticlockwise = FlightRuntime.compare(cross, 0.0, '<');
    sweep = FlightRuntime.callValue(ShapeApi.normalizeArcSweep__shapeCommands, cast ([startA, endA, isAnticlockwise] : Array<Dynamic>));
    segmentCount = FlightRuntime.callProperty(HxMath, 'max', cast ([1.0, FlightRuntime.callProperty(HxMath, 'ceil', cast ([(FlightRuntime.callProperty(HxMath, 'abs', cast ([sweep] : Array<Dynamic>)) / (HxMath.PI / 2.0))] : Array<Dynamic>))] : Array<Dynamic>));
    segmentAngle = (sweep / segmentCount);
    alpha = ((4.0 / 3.0) * FlightRuntime.callProperty(HxMath, 'tan', cast ([(segmentAngle / 4.0)] : Array<Dynamic>)));
    FlightRuntime.callValue(ShapeApi.pushArcCubics__shapeCommands, cast ([cmds, ocx, ocy, radius, startA, segmentCount, segmentAngle, alpha] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeBeginBitmapFill(shape:Shape, bitmap:ImageResource, ?matrix:Null<Matrix>, repeat:Dynamic = true, smooth:Dynamic = false):Void {
    if (matrix == null) matrix = cast (null : Dynamic);
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['beginBitmapFill', 4.0, bitmap, matrix, repeat, smooth] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeBeginFill(shape:Shape, color:Dynamic = 0.0, alpha:Dynamic = 1.0):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['beginFill', 2.0, color, alpha] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeBeginGradientFill(shape:Shape, gradientType:GradientType, colors:Array<Float>, alphas:Array<Float>, ratios:Array<Float>, ?matrix:Null<Matrix>, spreadMethod:SpreadMethod = 'pad', interpolationMethod:InterpolationMethod = 'rgb', focalPointRatio:Dynamic = 0.0):Void {
    if (matrix == null) matrix = cast (null : Dynamic);
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['beginGradientFill', 8.0, gradientType, colors, alphas, ratios, matrix, spreadMethod, interpolationMethod, focalPointRatio] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeCircle(shape:Shape, x:Float, y:Float, radius:Float):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['drawCircle', 3.0, x, y, radius] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeCubicCurveTo(shape:Shape, controlX1:Float, controlY1:Float, controlX2:Float, controlY2:Float, anchorX:Float, anchorY:Float):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['cubicCurveTo', 6.0, controlX1, controlY1, controlX2, controlY2, anchorX, anchorY] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeCurveTo(shape:Shape, controlX:Float, controlY:Float, anchorX:Float, anchorY:Float):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['curveTo', 4.0, controlX, controlY, anchorX, anchorY] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeDrawTriangles(shape:Shape, vertices:Array<Float>, ?indices:Null<Array<Float>>, ?uvtData:Null<Array<Float>>, culling:TriangleCulling = 'none'):Void {
    if (indices == null) indices = cast (null : Dynamic);
    if (uvtData == null) uvtData = cast (null : Dynamic);
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['drawTriangles', 4.0, vertices, indices, uvtData, culling] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeEllipse(shape:Shape, x:Float, y:Float, width:Float, height:Float):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['drawEllipse', 4.0, x, y, width, height] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeEndFill(shape:Shape):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['endFill', 0.0] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeLineBitmapStyle(shape:Shape, bitmap:ImageResource, ?matrix:Null<Matrix>, repeat:Dynamic = true, smooth:Dynamic = false):Void {
    if (matrix == null) matrix = cast (null : Dynamic);
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['lineBitmapStyle', 4.0, bitmap, matrix, repeat, smooth] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeLineGradientStyle(shape:Shape, gradientType:GradientType, colors:Array<Float>, alphas:Array<Float>, ratios:Array<Float>, ?matrix:Null<Matrix>, spreadMethod:SpreadMethod = 'pad', interpolationMethod:InterpolationMethod = 'rgb', focalPointRatio:Dynamic = 0.0):Void {
    if (matrix == null) matrix = cast (null : Dynamic);
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['lineGradientStyle', 8.0, gradientType, colors, alphas, ratios, matrix, spreadMethod, interpolationMethod, focalPointRatio] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeLineStyle(shape:Shape, thickness:Dynamic = 1.0, color:Dynamic = 0.0, alpha:Dynamic = 1.0, pixelHinting:Dynamic = false, scaleMode:LineScaleMode = 'normal', caps:CapsStyle = 'none', joints:JointStyle = 'round', miterLimit:Dynamic = 3.0):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['lineStyle', 8.0, thickness, color, alpha, pixelHinting, scaleMode, caps, joints, miterLimit] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeLineTo(shape:Shape, x:Float, y:Float):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['lineTo', 2.0, x, y] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeMoveTo(shape:Shape, x:Float, y:Float):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['moveTo', 2.0, x, y] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapePath(shape:Shape, commands:Array<Float>, pathData:Array<Float>, winding:PathWinding = 'evenOdd'):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['drawPath', 3.0, commands, pathData, winding] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapePolygon(shape:Shape, points:Array<Float>):Void {
    var cmds:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(points, 'length'), 4.0, '<'))) { return; }
    cmds = FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands');
    FlightRuntime.pushMany(cmds, cast (['moveTo', 2.0, FlightRuntime.getIndex(points, 0.0), FlightRuntime.getIndex(points, 1.0)] : Array<Dynamic>));
    {
      var k:Dynamic = 2.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, (FlightRuntime.field(points, 'length') - 1.0), '<'))) {
        FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, FlightRuntime.getIndex(points, k), FlightRuntime.getIndex(points, (k + 1.0))] : Array<Dynamic>));
        (k = cast ((k + 2.0) : Dynamic));
      }
    }
    FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, FlightRuntime.getIndex(points, 0.0), FlightRuntime.getIndex(points, 1.0)] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapePolyline(shape:Shape, points:Array<Float>):Void {
    var cmds:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.field(points, 'length'), 4.0, '<'))) { return; }
    cmds = FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands');
    FlightRuntime.pushMany(cmds, cast (['moveTo', 2.0, FlightRuntime.getIndex(points, 0.0), FlightRuntime.getIndex(points, 1.0)] : Array<Dynamic>));
    {
      var k:Dynamic = 2.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(k, (FlightRuntime.field(points, 'length') - 1.0), '<'))) {
        FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, FlightRuntime.getIndex(points, k), FlightRuntime.getIndex(points, (k + 1.0))] : Array<Dynamic>));
        (k = cast ((k + 2.0) : Dynamic));
      }
    }
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeRectangle(shape:Shape, x:Float, y:Float, width:Float, height:Float):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['drawRectangle', 4.0, x, y, width, height] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeRoundRectangle(shape:Shape, x:Float, y:Float, width:Float, height:Float, ellipseWidth:Float, ellipseHeight:Float):Void {
    FlightRuntime.pushMany(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), cast (['drawRoundRectangle', 6.0, x, y, width, height, ellipseWidth, ellipseHeight] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function appendShapeRoundRectangleVarying(shape:Shape, x:Float, y:Float, width:Float, height:Float, topLeftRadius:Float, topRightRadius:Float, bottomLeftRadius:Float, bottomRightRadius:Float):Void {
    var r:Dynamic = cast FlightRuntime.UNDEFINED;
    var b:Dynamic = cast FlightRuntime.UNDEFINED;
    var cmds:Dynamic = cast FlightRuntime.UNDEFINED;
    r = (x + width);
    b = (y + height);
    cmds = FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands');
    FlightRuntime.pushMany(cmds, cast (['moveTo', 2.0, (x + topLeftRadius), y] : Array<Dynamic>));
    FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, (r - topRightRadius), y] : Array<Dynamic>));
    FlightRuntime.pushMany(cmds, cast (['curveTo', 4.0, r, y, r, (y + topRightRadius)] : Array<Dynamic>));
    FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, r, (b - bottomRightRadius)] : Array<Dynamic>));
    FlightRuntime.pushMany(cmds, cast (['curveTo', 4.0, r, b, (r - bottomRightRadius), b] : Array<Dynamic>));
    FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, (x + bottomLeftRadius), b] : Array<Dynamic>));
    FlightRuntime.pushMany(cmds, cast (['curveTo', 4.0, x, b, x, (b - bottomLeftRadius)] : Array<Dynamic>));
    FlightRuntime.pushMany(cmds, cast (['lineTo', 2.0, x, (y + topLeftRadius)] : Array<Dynamic>));
    FlightRuntime.pushMany(cmds, cast (['curveTo', 4.0, x, y, (x + topLeftRadius), y] : Array<Dynamic>));
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function clearShapeCommands(shape:Shape):Void {
    FlightRuntime.setLength(FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands'), 0.0);
    FlightRuntime.callValue(invalidateContent, cast ([shape] : Array<Dynamic>));
  }

  public static function computeShapeLocalBoundsRectangle(out:Rectangle, source:BoundsNodeAny):Void {
    var shape:Dynamic = cast FlightRuntime.UNDEFINED;
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var minX:Dynamic = cast FlightRuntime.UNDEFINED;
    var minY:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxX:Dynamic = cast FlightRuntime.UNDEFINED;
    var maxY:Dynamic = cast FlightRuntime.UNDEFINED;
    var strokeHalf:Dynamic = cast FlightRuntime.UNDEFINED;
    var penX:Dynamic = cast FlightRuntime.UNDEFINED;
    var penY:Dynamic = cast FlightRuntime.UNDEFINED;
    var expand:Float->Float->Void = cast FlightRuntime.UNDEFINED;
    var quadPoint:Float->Float->Float->Float->Float = cast FlightRuntime.UNDEFINED;
    var cubicPoint:Float->Float->Float->Float->Float->Float = cast FlightRuntime.UNDEFINED;
    var expandCubicExtrema:Float->Float->Float->Float->Float->Float->Float->Float->Void = cast FlightRuntime.UNDEFINED;
    var expandCubicAxis:Float->Float->Float->Float->Float->Float->Float->Float->Void = cast FlightRuntime.UNDEFINED;
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    shape = (cast (cast source : Dynamic) : Shape);
    commands = FlightRuntime.field(FlightRuntime.field(shape, 'data'), 'commands');
    minX = HxMath.POSITIVE_INFINITY;
    minY = HxMath.POSITIVE_INFINITY;
    maxX = -HxMath.POSITIVE_INFINITY;
    maxY = -HxMath.POSITIVE_INFINITY;
    strokeHalf = 0.0;
    penX = 0.0;
    penY = 0.0;
    expand = function expand(x:Float, y:Float):Void {
  var lo_x:Dynamic = cast FlightRuntime.UNDEFINED;
  var hi_x:Dynamic = cast FlightRuntime.UNDEFINED;
  var lo_y:Dynamic = cast FlightRuntime.UNDEFINED;
  var hi_y:Dynamic = cast FlightRuntime.UNDEFINED;
  lo_x = (x - strokeHalf);
  hi_x = (x + strokeHalf);
  lo_y = (y - strokeHalf);
  hi_y = (y + strokeHalf);
  if (FlightRuntime.truthy(FlightRuntime.compare(lo_x, minX, '<'))) { (minX = cast (lo_x : Dynamic)); }
  if (FlightRuntime.truthy(FlightRuntime.compare(lo_y, minY, '<'))) { (minY = cast (lo_y : Dynamic)); }
  if (FlightRuntime.truthy(FlightRuntime.compare(hi_x, maxX, '>'))) { (maxX = cast (hi_x : Dynamic)); }
  if (FlightRuntime.truthy(FlightRuntime.compare(hi_y, maxY, '>'))) { (maxY = cast (hi_y : Dynamic)); }
};
    quadPoint = function quadPoint(t:Float, p0:Float, p1:Float, p2:Float):Float {
  var u:Dynamic = cast FlightRuntime.UNDEFINED;
  u = (1.0 - t);
  return cast ((((u * u) * p0) + (((2.0 * u) * t) * p1)) + ((t * t) * p2));
};
    cubicPoint = function cubicPoint(t:Float, p0:Float, p1:Float, p2:Float, p3:Float):Float {
  var u:Dynamic = cast FlightRuntime.UNDEFINED;
  u = (1.0 - t);
  return cast ((((((u * u) * u) * p0) + ((((3.0 * u) * u) * t) * p1)) + ((((3.0 * u) * t) * t) * p2)) + (((t * t) * t) * p3));
};
    expandCubicExtrema = function expandCubicExtrema(p0x:Float, p0y:Float, p1x:Float, p1y:Float, p2x:Float, p2y:Float, p3x:Float, p3y:Float):Void {
  FlightRuntime.callValue(expandCubicAxis, cast ([p0x, p1x, p2x, p3x, p0y, p1y, p2y, p3y] : Array<Dynamic>));
  FlightRuntime.callValue(expandCubicAxis, cast ([p0y, p1y, p2y, p3y, p0x, p1x, p2x, p3x] : Array<Dynamic>));
};
    expandCubicAxis = function expandCubicAxis(p0:Float, p1:Float, p2:Float, p3:Float, q0:Float, q1:Float, q2:Float, q3:Float):Void {
  var a:Dynamic = cast FlightRuntime.UNDEFINED;
  var b:Dynamic = cast FlightRuntime.UNDEFINED;
  var c:Dynamic = cast FlightRuntime.UNDEFINED;
  var disc:Dynamic = cast FlightRuntime.UNDEFINED;
  var sqrtDisc:Dynamic = cast FlightRuntime.UNDEFINED;
  var t1:Dynamic = cast FlightRuntime.UNDEFINED;
  var t2:Dynamic = cast FlightRuntime.UNDEFINED;
  a = (((-p0 + (3.0 * p1)) - (3.0 * p2)) + p3);
  b = (2.0 * ((p0 - (2.0 * p1)) + p2));
  c = (-p0 + p1);
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([a] : Array<Dynamic>)), 1e-12, '<'))) {
  if (FlightRuntime.truthy(FlightRuntime.compare(FlightRuntime.callProperty(HxMath, 'abs', cast ([b] : Array<Dynamic>)), 1e-12, '>'))) {
  var t:Dynamic = (-c / b);
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(t, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(t, 1.0, '<')))) { FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(cubicPoint, cast ([t, p0, p1, p2, p3] : Array<Dynamic>)), FlightRuntime.callValue(cubicPoint, cast ([t, q0, q1, q2, q3] : Array<Dynamic>))] : Array<Dynamic>)); }
}
  return;
}
  disc = ((b * b) - ((4.0 * a) * c));
  if (FlightRuntime.truthy(FlightRuntime.compare(disc, 0.0, '<'))) { return; }
  sqrtDisc = FlightRuntime.callProperty(HxMath, 'sqrt', cast ([disc] : Array<Dynamic>));
  t1 = ((-b + sqrtDisc) / (2.0 * a));
  t2 = ((-b - sqrtDisc) / (2.0 * a));
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(t1, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(t1, 1.0, '<')))) { FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(cubicPoint, cast ([t1, p0, p1, p2, p3] : Array<Dynamic>)), FlightRuntime.callValue(cubicPoint, cast ([t1, q0, q1, q2, q3] : Array<Dynamic>))] : Array<Dynamic>)); }
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(t2, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(t2, 1.0, '<')))) { FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(cubicPoint, cast ([t2, p0, p1, p2, p3] : Array<Dynamic>)), FlightRuntime.callValue(cubicPoint, cast ([t2, q0, q1, q2, q3] : Array<Dynamic>))] : Array<Dynamic>)); }
};
    i = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(commands, 'length'), '<'))) {
  var key:Dynamic = (cast FlightRuntime.getIndex(commands, i) : String);
  var argCount:Dynamic = (cast FlightRuntime.getIndex(commands, (i + 1.0)) : Float);
  var b:Dynamic = (i + 2.0);
  {
    var __switchValue = key;
    if (__switchValue == 'drawRectangle' || __switchValue == 'drawRoundRectangle') {
      {
        var x:Dynamic = (cast FlightRuntime.getIndex(commands, b) : Float);
        var y:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 1.0)) : Float);
        var w:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 2.0)) : Float);
        var h:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 3.0)) : Float);
        FlightRuntime.callValue(expand, cast ([x, y] : Array<Dynamic>));
        FlightRuntime.callValue(expand, cast ([(x + w), (y + h)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'drawCircle') {
      {
        var x:Dynamic = (cast FlightRuntime.getIndex(commands, b) : Float);
        var y:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 1.0)) : Float);
        var r:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 2.0)) : Float);
        FlightRuntime.callValue(expand, cast ([(x - r), (y - r)] : Array<Dynamic>));
        FlightRuntime.callValue(expand, cast ([(x + r), (y + r)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'drawEllipse') {
      {
        var x:Dynamic = (cast FlightRuntime.getIndex(commands, b) : Float);
        var y:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 1.0)) : Float);
        var w:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 2.0)) : Float);
        var h:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 3.0)) : Float);
        FlightRuntime.callValue(expand, cast ([x, y] : Array<Dynamic>));
        FlightRuntime.callValue(expand, cast ([(x + w), (y + h)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'moveTo') {
      {
        (penX = cast ((cast FlightRuntime.getIndex(commands, b) : Float) : Dynamic));
        (penY = cast ((cast FlightRuntime.getIndex(commands, (b + 1.0)) : Float) : Dynamic));
      }
    }
    else if (__switchValue == 'lineTo') {
      {
        var x:Dynamic = (cast FlightRuntime.getIndex(commands, b) : Float);
        var y:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 1.0)) : Float);
        FlightRuntime.callValue(expand, cast ([penX, penY] : Array<Dynamic>));
        FlightRuntime.callValue(expand, cast ([x, y] : Array<Dynamic>));
        (penX = cast (x : Dynamic));
        (penY = cast (y : Dynamic));
      }
    }
    else if (__switchValue == 'curveTo') {
      {
        var controlX:Dynamic = (cast FlightRuntime.getIndex(commands, b) : Float);
        var controlY:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 1.0)) : Float);
        var anchorX:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 2.0)) : Float);
        var anchorY:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 3.0)) : Float);
        FlightRuntime.callValue(expand, cast ([penX, penY] : Array<Dynamic>));
        var denomX:Dynamic = ((penX - (2.0 * controlX)) + anchorX);
        if (FlightRuntime.truthy(!FlightRuntime.strictEquals(denomX, 0.0))) {
  var tx:Dynamic = ((penX - controlX) / denomX);
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(tx, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(tx, 1.0, '<')))) { FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(quadPoint, cast ([tx, penX, controlX, anchorX] : Array<Dynamic>)), FlightRuntime.callValue(quadPoint, cast ([tx, penY, controlY, anchorY] : Array<Dynamic>))] : Array<Dynamic>)); }
}
        var denomY:Dynamic = ((penY - (2.0 * controlY)) + anchorY);
        if (FlightRuntime.truthy(!FlightRuntime.strictEquals(denomY, 0.0))) {
  var ty:Dynamic = ((penY - controlY) / denomY);
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(ty, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(ty, 1.0, '<')))) { FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(quadPoint, cast ([ty, penX, controlX, anchorX] : Array<Dynamic>)), FlightRuntime.callValue(quadPoint, cast ([ty, penY, controlY, anchorY] : Array<Dynamic>))] : Array<Dynamic>)); }
}
        FlightRuntime.callValue(expand, cast ([anchorX, anchorY] : Array<Dynamic>));
        (penX = cast (anchorX : Dynamic));
        (penY = cast (anchorY : Dynamic));
      }
    }
    else if (__switchValue == 'cubicCurveTo') {
      {
        var control1X:Dynamic = (cast FlightRuntime.getIndex(commands, b) : Float);
        var control1Y:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 1.0)) : Float);
        var control2X:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 2.0)) : Float);
        var control2Y:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 3.0)) : Float);
        var anchorX:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 4.0)) : Float);
        var anchorY:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 5.0)) : Float);
        FlightRuntime.callValue(expand, cast ([penX, penY] : Array<Dynamic>));
        FlightRuntime.callValue(expand, cast ([anchorX, anchorY] : Array<Dynamic>));
        FlightRuntime.callValue(expandCubicExtrema, cast ([penX, penY, control1X, control1Y, control2X, control2Y, anchorX, anchorY] : Array<Dynamic>));
        (penX = cast (anchorX : Dynamic));
        (penY = cast (anchorY : Dynamic));
      }
    }
    else if (__switchValue == 'lineStyle') {
      {
        (strokeHalf = cast (((cast FlightRuntime.getIndex(commands, b) : Float) / 2.0) : Dynamic));
      }
    }
    else if (__switchValue == 'drawPath') {
      {
        var pathCmds:Dynamic = (cast FlightRuntime.getIndex(commands, b) : Array<Float>);
        var data:Dynamic = (cast FlightRuntime.getIndex(commands, (b + 1.0)) : Array<Float>);
        var di:Dynamic = 0.0;
        for (pc in FlightRuntime.iterable(pathCmds)) {
          {
            var __switchValue = pc;
            if (__switchValue == 1.0) {
              (penX = cast (FlightRuntime.getIndex(data, di) : Dynamic));
              (penY = cast (FlightRuntime.getIndex(data, (di + 1.0)) : Dynamic));
              (di = cast ((di + 2.0) : Dynamic));
            }
            else if (__switchValue == 2.0) {
              FlightRuntime.callValue(expand, cast ([penX, penY] : Array<Dynamic>));
              FlightRuntime.callValue(expand, cast ([FlightRuntime.getIndex(data, di), FlightRuntime.getIndex(data, (di + 1.0))] : Array<Dynamic>));
              (penX = cast (FlightRuntime.getIndex(data, di) : Dynamic));
              (penY = cast (FlightRuntime.getIndex(data, (di + 1.0)) : Dynamic));
              (di = cast ((di + 2.0) : Dynamic));
            }
            else if (__switchValue == 3.0) {
              {
                var qcx:Dynamic = FlightRuntime.getIndex(data, di);
                var qcy:Dynamic = FlightRuntime.getIndex(data, (di + 1.0));
                var qax:Dynamic = FlightRuntime.getIndex(data, (di + 2.0));
                var qay:Dynamic = FlightRuntime.getIndex(data, (di + 3.0));
                FlightRuntime.callValue(expand, cast ([penX, penY] : Array<Dynamic>));
                var qdx:Dynamic = ((penX - (2.0 * qcx)) + qax);
                if (FlightRuntime.truthy(!FlightRuntime.strictEquals(qdx, 0.0))) {
  var qt:Dynamic = ((penX - qcx) / qdx);
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(qt, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(qt, 1.0, '<')))) { FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(quadPoint, cast ([qt, penX, qcx, qax] : Array<Dynamic>)), FlightRuntime.callValue(quadPoint, cast ([qt, penY, qcy, qay] : Array<Dynamic>))] : Array<Dynamic>)); }
}
                var qdy:Dynamic = ((penY - (2.0 * qcy)) + qay);
                if (FlightRuntime.truthy(!FlightRuntime.strictEquals(qdy, 0.0))) {
  var qt:Dynamic = ((penY - qcy) / qdy);
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.compare(qt, 0.0, '>'), function():Dynamic return cast FlightRuntime.compare(qt, 1.0, '<')))) { FlightRuntime.callValue(expand, cast ([FlightRuntime.callValue(quadPoint, cast ([qt, penX, qcx, qax] : Array<Dynamic>)), FlightRuntime.callValue(quadPoint, cast ([qt, penY, qcy, qay] : Array<Dynamic>))] : Array<Dynamic>)); }
}
                FlightRuntime.callValue(expand, cast ([qax, qay] : Array<Dynamic>));
                (penX = cast (qax : Dynamic));
                (penY = cast (qay : Dynamic));
                (di = cast ((di + 4.0) : Dynamic));
              }
            }
            else if (__switchValue == 4.0) {
              (penX = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
              (penY = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
              (di = cast ((di + 4.0) : Dynamic));
            }
            else if (__switchValue == 5.0) {
              FlightRuntime.callValue(expand, cast ([penX, penY] : Array<Dynamic>));
              FlightRuntime.callValue(expand, cast ([FlightRuntime.getIndex(data, (di + 2.0)), FlightRuntime.getIndex(data, (di + 3.0))] : Array<Dynamic>));
              (penX = cast (FlightRuntime.getIndex(data, (di + 2.0)) : Dynamic));
              (penY = cast (FlightRuntime.getIndex(data, (di + 3.0)) : Dynamic));
              (di = cast ((di + 4.0) : Dynamic));
            }
            else if (__switchValue == 6.0) {
              {
                FlightRuntime.callValue(expand, cast ([penX, penY] : Array<Dynamic>));
                var ax:Dynamic = FlightRuntime.getIndex(data, (di + 4.0));
                var ay:Dynamic = FlightRuntime.getIndex(data, (di + 5.0));
                FlightRuntime.callValue(expand, cast ([ax, ay] : Array<Dynamic>));
                FlightRuntime.callValue(expandCubicExtrema, cast ([penX, penY, FlightRuntime.getIndex(data, di), FlightRuntime.getIndex(data, (di + 1.0)), FlightRuntime.getIndex(data, (di + 2.0)), FlightRuntime.getIndex(data, (di + 3.0)), ax, ay] : Array<Dynamic>));
                (penX = cast (ax : Dynamic));
                (penY = cast (ay : Dynamic));
                (di = cast ((di + 6.0) : Dynamic));
              }
            }
          }
        }
      }
    }
    else if (__switchValue == 'drawTriangles') {
      {
        var vertices:Dynamic = (cast FlightRuntime.getIndex(commands, b) : Array<Float>);
        {
          var vi:Dynamic = 0.0;
          while (FlightRuntime.truthy(FlightRuntime.compare(vi, FlightRuntime.field(vertices, 'length'), '<'))) {
            FlightRuntime.callValue(expand, cast ([FlightRuntime.getIndex(vertices, vi), FlightRuntime.getIndex(vertices, (vi + 1.0))] : Array<Dynamic>));
            (vi = cast ((vi + 2.0) : Dynamic));
          }
        }
      }
    }
  }
  (i = cast ((i + (argCount + 2.0)) : Dynamic));
}
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(minX, HxMath.POSITIVE_INFINITY))) {
  FlightRuntime.setField(out, 'x', 0.0);
  FlightRuntime.setField(out, 'y', 0.0);
  FlightRuntime.setField(out, 'width', 0.0);
  FlightRuntime.setField(out, 'height', 0.0);
} else {
  FlightRuntime.setField(out, 'x', minX);
  FlightRuntime.setField(out, 'y', minY);
  FlightRuntime.setField(out, 'width', (maxX - minX));
  FlightRuntime.setField(out, 'height', (maxY - minY));
}
  }

  public static function copyShapeCommands(out:Shape, source:Shape):Void {
    FlightRuntime.setLength(FlightRuntime.field(FlightRuntime.field(out, 'data'), 'commands'), 0.0);
    FlightRuntime.callProperty(FlightRuntime.field(FlightRuntime.field(out, 'data'), 'commands'), 'push', FlightRuntime.concatArrays([FlightRuntime.toArray(FlightRuntime.field(FlightRuntime.field(source, 'data'), 'commands'))]));
    FlightRuntime.callValue(invalidateContent, cast ([out] : Array<Dynamic>));
  }

  public static function createScale9Shape(scale9Grid:RectangleLike, ?obj:PartialNode<Scale9Shape>):Scale9Shape {
    return cast (cast FlightRuntime.callValue(createDisplayObjectGeneric, cast ([Types.Scale9ShapeKind, (cast obj : PartialNode<Scale9Shape>), function(data:Dynamic) return FlightRuntime.callValue(createScale9ShapeData, cast ([scale9Grid, data] : Array<Dynamic>)), createScale9ShapeRuntime] : Array<Dynamic>)) : Scale9Shape);
    return cast null;
  }

  public static function createScale9ShapeData(scale9Grid:RectangleLike, ?data:Dynamic):Scale9ShapeData {
    return cast { commands: FlightRuntime.coalesce(FlightRuntime.optionalField(data, 'commands'), function():Dynamic return cast cast ([] : Array<Dynamic>)), scale9Grid: scale9Grid };
    return cast null;
  }

  public static function createScale9ShapeRuntime():Scale9ShapeRuntime {
    return cast (cast FlightRuntime.callValue(createShapeRuntime, cast ([] : Array<Dynamic>)) : Scale9ShapeRuntime);
    return cast null;
  }

  public static function createShape(?obj:PartialNode<Shape>):Shape {
    return cast (cast FlightRuntime.callValue(createDisplayObjectGeneric, cast ([Types.ShapeKind, obj, createShapeData, createShapeRuntime] : Array<Dynamic>)) : Shape);
    return cast null;
  }

  public static function createShapeData(?data:Dynamic):ShapeData {
    return cast { commands: FlightRuntime.coalesce(FlightRuntime.optionalField(data, 'commands'), function():Dynamic return cast cast ([] : Array<Dynamic>)) };
    return cast null;
  }

  public static function createShapeRuntime():ShapeRuntime {
    return cast (cast FlightRuntime.callValue(createDisplayObjectRuntime, cast ([{ computeLocalBoundsRectangle: computeShapeLocalBoundsRectangle }] : Array<Dynamic>)) : ShapeRuntime);
    return cast null;
  }

  public static function enableShapeHitTesting():Void {
    FlightRuntime.callValue(registerShapeHitTestCommand, cast ([{ key: 'drawCircle', hitTest: function(x:Float, y:Float, buf:Array<ShapeCommandToken>, i:Float) {
  var cx:Dynamic = cast FlightRuntime.UNDEFINED;
  var cy:Dynamic = cast FlightRuntime.UNDEFINED;
  var r:Dynamic = cast FlightRuntime.UNDEFINED;
  var dx:Dynamic = cast FlightRuntime.UNDEFINED;
  var dy:Dynamic = cast FlightRuntime.UNDEFINED;
  cx = (cast FlightRuntime.getIndex(buf, i) : Float);
  cy = (cast FlightRuntime.getIndex(buf, (i + 1.0)) : Float);
  r = (cast FlightRuntime.getIndex(buf, (i + 2.0)) : Float);
  dx = (x - cx);
  dy = (y - cy);
  return cast FlightRuntime.compare(((dx * dx) + (dy * dy)), (r * r), '<=');
} }] : Array<Dynamic>));
    FlightRuntime.callValue(registerShapeHitTestCommand, cast ([{ key: 'drawEllipse', hitTest: function(x:Float, y:Float, buf:Array<ShapeCommandToken>, i:Float) {
  var ex:Dynamic = cast FlightRuntime.UNDEFINED;
  var ey:Dynamic = cast FlightRuntime.UNDEFINED;
  var ew:Dynamic = cast FlightRuntime.UNDEFINED;
  var eh:Dynamic = cast FlightRuntime.UNDEFINED;
  var rx:Dynamic = cast FlightRuntime.UNDEFINED;
  var ry:Dynamic = cast FlightRuntime.UNDEFINED;
  var cx:Dynamic = cast FlightRuntime.UNDEFINED;
  var cy:Dynamic = cast FlightRuntime.UNDEFINED;
  var nx:Dynamic = cast FlightRuntime.UNDEFINED;
  var ny:Dynamic = cast FlightRuntime.UNDEFINED;
  ex = (cast FlightRuntime.getIndex(buf, i) : Float);
  ey = (cast FlightRuntime.getIndex(buf, (i + 1.0)) : Float);
  ew = (cast FlightRuntime.getIndex(buf, (i + 2.0)) : Float);
  eh = (cast FlightRuntime.getIndex(buf, (i + 3.0)) : Float);
  rx = (ew / 2.0);
  ry = (eh / 2.0);
  cx = (ex + rx);
  cy = (ey + ry);
  nx = ((x - cx) / rx);
  ny = ((y - cy) / ry);
  return cast FlightRuntime.compare(((nx * nx) + (ny * ny)), 1.0, '<=');
} }] : Array<Dynamic>));
    FlightRuntime.callValue(registerShapeHitTestCommand, cast ([{ key: 'drawRectangle', hitTest: function(x:Float, y:Float, buf:Array<ShapeCommandToken>, i:Float) {
  var rx:Dynamic = cast FlightRuntime.UNDEFINED;
  var ry:Dynamic = cast FlightRuntime.UNDEFINED;
  var rw:Dynamic = cast FlightRuntime.UNDEFINED;
  var rh:Dynamic = cast FlightRuntime.UNDEFINED;
  rx = (cast FlightRuntime.getIndex(buf, i) : Float);
  ry = (cast FlightRuntime.getIndex(buf, (i + 1.0)) : Float);
  rw = (cast FlightRuntime.getIndex(buf, (i + 2.0)) : Float);
  rh = (cast FlightRuntime.getIndex(buf, (i + 3.0)) : Float);
  return cast FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.andValue(FlightRuntime.compare(x, rx, '>='), function():Dynamic return cast FlightRuntime.compare(x, (rx + rw), '<=')), function():Dynamic return cast FlightRuntime.compare(y, ry, '>=')), function():Dynamic return cast FlightRuntime.compare(y, (ry + rh), '<='));
} }] : Array<Dynamic>));
    FlightRuntime.callValue(registerShapeHitTestCommand, cast ([{ key: 'drawRoundRectangle', hitTest: function(x:Float, y:Float, buf:Array<ShapeCommandToken>, i:Float) {
  var rx:Dynamic = cast FlightRuntime.UNDEFINED;
  var ry:Dynamic = cast FlightRuntime.UNDEFINED;
  var rw:Dynamic = cast FlightRuntime.UNDEFINED;
  var rh:Dynamic = cast FlightRuntime.UNDEFINED;
  var arx:Dynamic = cast FlightRuntime.UNDEFINED;
  var ary:Dynamic = cast FlightRuntime.UNDEFINED;
  var cx:Dynamic = cast FlightRuntime.UNDEFINED;
  var cy:Dynamic = cast FlightRuntime.UNDEFINED;
  var inLeft:Dynamic = cast FlightRuntime.UNDEFINED;
  var inRight:Dynamic = cast FlightRuntime.UNDEFINED;
  var inTop:Dynamic = cast FlightRuntime.UNDEFINED;
  var inBottom:Dynamic = cast FlightRuntime.UNDEFINED;
  rx = (cast FlightRuntime.getIndex(buf, i) : Float);
  ry = (cast FlightRuntime.getIndex(buf, (i + 1.0)) : Float);
  rw = (cast FlightRuntime.getIndex(buf, (i + 2.0)) : Float);
  rh = (cast FlightRuntime.getIndex(buf, (i + 3.0)) : Float);
  arx = FlightRuntime.orValue((Std.int(((cast FlightRuntime.getIndex(buf, (i + 4.0)) : Float) / 2.0)) | Std.int(0.0)), function():Dynamic return cast ((cast FlightRuntime.getIndex(buf, (i + 4.0)) : Float) / 2.0));
  ary = FlightRuntime.orValue((Std.int(((cast FlightRuntime.getIndex(buf, (i + 5.0)) : Float) / 2.0)) | Std.int(0.0)), function():Dynamic return cast ((cast FlightRuntime.getIndex(buf, (i + 5.0)) : Float) / 2.0));
  cx = FlightRuntime.callProperty(HxMath, 'min', cast ([arx, (rw / 2.0)] : Array<Dynamic>));
  cy = FlightRuntime.callProperty(HxMath, 'min', cast ([ary, (rh / 2.0)] : Array<Dynamic>));
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.compare(x, rx, '<'), function():Dynamic return cast FlightRuntime.compare(x, (rx + rw), '>')), function():Dynamic return cast FlightRuntime.compare(y, ry, '<')), function():Dynamic return cast FlightRuntime.compare(y, (ry + rh), '>')))) { return cast false; }
  inLeft = FlightRuntime.compare(x, (rx + cx), '<');
  inRight = FlightRuntime.compare(x, ((rx + rw) - cx), '>');
  inTop = FlightRuntime.compare(y, (ry + cy), '<');
  inBottom = FlightRuntime.compare(y, ((ry + rh) - cy), '>');
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.orValue(inLeft, function():Dynamic return cast inRight), function():Dynamic return cast FlightRuntime.orValue(inTop, function():Dynamic return cast inBottom)))) {
  var ecx:Dynamic = FlightRuntime.select(inLeft, function():Dynamic return cast (rx + cx), function():Dynamic return cast ((rx + rw) - cx));
  var ecy:Dynamic = FlightRuntime.select(inTop, function():Dynamic return cast (ry + cy), function():Dynamic return cast ((ry + rh) - cy));
  var dx:Dynamic = ((x - ecx) / cx);
  var dy:Dynamic = ((y - ecy) / cy);
  return cast FlightRuntime.compare(((dx * dx) + (dy * dy)), 1.0, '<=');
}
  return cast true;
} }] : Array<Dynamic>));
  }

  public static function getScale9ShapeRuntime(source:Scale9Shape):Scale9ShapeRuntime {
    return cast (cast FlightRuntime.callValue(getDisplayObjectRuntime, cast ([source] : Array<Dynamic>)) : Scale9ShapeRuntime);
    return cast null;
  }

  public static function getShapeBounds(out:Rectangle, source:Shape):Void {
    FlightRuntime.callValue(computeShapeLocalBoundsRectangle, cast ([out, source] : Array<Dynamic>));
  }

  public static function getShapeCommandCount(source:Shape):Float {
    var commands:Dynamic = cast FlightRuntime.UNDEFINED;
    var count:Dynamic = cast FlightRuntime.UNDEFINED;
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    commands = FlightRuntime.field(FlightRuntime.field(source, 'data'), 'commands');
    count = 0.0;
    i = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(commands, 'length'), '<'))) {
  var argCount:Dynamic = (cast FlightRuntime.getIndex(commands, (i + 1.0)) : Float);
  count++;
  (i = cast ((i + (argCount + 2.0)) : Dynamic));
}
    return cast count;
    return cast null;
  }

  public static function getShapeFillRegions(commands:Array<ShapeCommandToken>):Null<Array<ShapeFillRegion>> {
    var regions:Array<ShapeFillRegion> = cast FlightRuntime.UNDEFINED;
    var path:Null<Path> = cast FlightRuntime.UNDEFINED;
    var color:Dynamic = cast FlightRuntime.UNDEFINED;
    var alpha:Dynamic = cast FlightRuntime.UNDEFINED;
    var winding:PathWinding = cast FlightRuntime.UNDEFINED;
    var startX:Dynamic = cast FlightRuntime.UNDEFINED;
    var startY:Dynamic = cast FlightRuntime.UNDEFINED;
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    if (FlightRuntime.truthy(FlightRuntime.callValue(hasNonSolidShapeFill, cast ([commands] : Array<Dynamic>)))) { return cast null; }
    regions = cast ([] : Array<Dynamic>);
    path = null;
    color = 0.0;
    alpha = 1.0;
    winding = 'nonZero';
    startX = 0.0;
    startY = 0.0;
    i = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(commands, 'length'), '<'))) {
  var name:Dynamic = (cast FlightRuntime.getIndex(commands, i) : String);
  var argCount:Dynamic = (cast FlightRuntime.getIndex(commands, (i + 1.0)) : Float);
  var a:Dynamic = (i + 2.0);
  (i = cast ((a + argCount) : Dynamic));
  {
    var __switchValue = name;
    if (__switchValue == 'beginFill') {
      {
        if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(path, null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(path, 'commands'), 'length'), 0.0, '>')))) { FlightRuntime.callProperty(regions, 'push', cast ([{ path: path, color: color, alpha: alpha }] : Array<Dynamic>)); }
        (color = cast ((cast FlightRuntime.getIndex(commands, a) : Float) : Dynamic));
        (alpha = cast ((cast FlightRuntime.getIndex(commands, (a + 1.0)) : Float) : Dynamic));
        (winding = cast ('nonZero' : Dynamic));
        (path = cast ({ commands: cast ([] : Array<Dynamic>), data: cast ([] : Array<Dynamic>), winding: winding } : Dynamic));
      }
    }
    else if (__switchValue == 'endFill') {
      {
        if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(path, null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(path, 'commands'), 'length'), 0.0, '>')))) { FlightRuntime.callProperty(regions, 'push', cast ([{ path: path, color: color, alpha: alpha }] : Array<Dynamic>)); }
        (path = cast (null : Dynamic));
      }
    }
    else if (__switchValue == 'moveTo') {
      {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(path, null))) { break; }
        (startX = cast ((cast FlightRuntime.getIndex(commands, a) : Float) : Dynamic));
        (startY = cast ((cast FlightRuntime.getIndex(commands, (a + 1.0)) : Float) : Dynamic));
        FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.MOVE_TO, startX, startY] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'lineTo') {
      {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(path, null))) { break; }
        FlightRuntime.callValue(ShapeApi.pushVerb__shapeFill, cast ([path, Types.PathCommandValue.LINE_TO, (cast FlightRuntime.getIndex(commands, a) : Float), (cast FlightRuntime.getIndex(commands, (a + 1.0)) : Float)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'curveTo') {
      {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(path, null))) { break; }
        FlightRuntime.callValue(ShapeApi.pushQuadratic__shapeFill, cast ([path, (cast FlightRuntime.getIndex(commands, a) : Float), (cast FlightRuntime.getIndex(commands, (a + 1.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 2.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 3.0)) : Float)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'cubicCurveTo') {
      {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(path, null))) { break; }
        FlightRuntime.callValue(ShapeApi.pushCubic__shapeFill, cast ([path, (cast FlightRuntime.getIndex(commands, a) : Float), (cast FlightRuntime.getIndex(commands, (a + 1.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 2.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 3.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 4.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 5.0)) : Float)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'drawCircle') {
      {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(path, null))) { break; }
        FlightRuntime.callValue(ShapeApi.appendEllipseToPath__shapeFill, cast ([path, (cast FlightRuntime.getIndex(commands, a) : Float), (cast FlightRuntime.getIndex(commands, (a + 1.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 2.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 2.0)) : Float)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'drawEllipse') {
      {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(path, null))) { break; }
        var w:Dynamic = (cast FlightRuntime.getIndex(commands, (a + 2.0)) : Float);
        var h:Dynamic = (cast FlightRuntime.getIndex(commands, (a + 3.0)) : Float);
        FlightRuntime.callValue(ShapeApi.appendEllipseToPath__shapeFill, cast ([path, ((cast FlightRuntime.getIndex(commands, a) : Float) + (w / 2.0)), ((cast FlightRuntime.getIndex(commands, (a + 1.0)) : Float) + (h / 2.0)), (w / 2.0), (h / 2.0)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'drawRectangle') {
      {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(path, null))) { break; }
        FlightRuntime.callValue(ShapeApi.appendRectangleToPath__shapeFill, cast ([path, (cast FlightRuntime.getIndex(commands, a) : Float), (cast FlightRuntime.getIndex(commands, (a + 1.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 2.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 3.0)) : Float)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'drawRoundRectangle') {
      {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(path, null))) { break; }
        FlightRuntime.callValue(ShapeApi.appendRoundRectangleToPath__shapeFill, cast ([path, (cast FlightRuntime.getIndex(commands, a) : Float), (cast FlightRuntime.getIndex(commands, (a + 1.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 2.0)) : Float), (cast FlightRuntime.getIndex(commands, (a + 3.0)) : Float), ((cast FlightRuntime.getIndex(commands, (a + 4.0)) : Float) / 2.0), ((cast FlightRuntime.getIndex(commands, (a + 5.0)) : Float) / 2.0)] : Array<Dynamic>));
      }
    }
    else if (__switchValue == 'drawPath') {
      {
        if (FlightRuntime.truthy(FlightRuntime.strictEquals(path, null))) { break; }
        FlightRuntime.setField(path, 'winding', (cast FlightRuntime.getIndex(commands, (a + 2.0)) : PathWinding));
        FlightRuntime.callValue(ShapeApi.appendRawPath__shapeFill, cast ([path, (cast FlightRuntime.getIndex(commands, a) : Array<Float>), (cast FlightRuntime.getIndex(commands, (a + 1.0)) : Array<Float>)] : Array<Dynamic>));
      }
    }
    else  {
    }
  }
}
    if (FlightRuntime.truthy(FlightRuntime.andValue(!FlightRuntime.strictEquals(path, null), function():Dynamic return cast FlightRuntime.compare(FlightRuntime.field(FlightRuntime.field(path, 'commands'), 'length'), 0.0, '>')))) { FlightRuntime.callProperty(regions, 'push', cast ([{ path: path, color: color, alpha: alpha }] : Array<Dynamic>)); }
    return cast regions;
    return cast null;
  }

  public static function getShapeRuntime(source:Shape):ShapeRuntime {
    return cast (cast FlightRuntime.callValue(getDisplayObjectRuntime, cast ([source] : Array<Dynamic>)) : ShapeRuntime);
    return cast null;
  }

  public static function hasNonSolidShapeFill(commands:Array<ShapeCommandToken>):Bool {
    var i:Dynamic = cast FlightRuntime.UNDEFINED;
    i = 0.0;
    while (FlightRuntime.truthy(FlightRuntime.compare(i, FlightRuntime.field(commands, 'length'), '<'))) {
  var name:Dynamic = (cast FlightRuntime.getIndex(commands, i) : String);
  var argCount:Dynamic = (cast FlightRuntime.getIndex(commands, (i + 1.0)) : Float);
  if (FlightRuntime.truthy(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.orValue(FlightRuntime.strictEquals(name, 'beginGradientFill'), function():Dynamic return cast FlightRuntime.strictEquals(name, 'beginBitmapFill')), function():Dynamic return cast FlightRuntime.strictEquals(name, 'lineStyle')), function():Dynamic return cast FlightRuntime.strictEquals(name, 'lineGradientStyle')), function():Dynamic return cast FlightRuntime.strictEquals(name, 'lineBitmapStyle')))) {
  return cast true;
}
  if (FlightRuntime.truthy(FlightRuntime.andValue(FlightRuntime.strictEquals(name, 'drawTriangles'), function():Dynamic return cast !FlightRuntime.strictEquals(FlightRuntime.getIndex(commands, ((i + 2.0) + 2.0)), null)))) {
  return cast true;
}
  (i = cast ((i + (2.0 + argCount)) : Dynamic));
}
    return cast false;
    return cast null;
  }

  public static final hitTests__shapeHitTestRegistry:Dynamic = FlightRuntime.construct(FlightRuntime.callProperty(FlightRuntime, 'globalValue', cast (['Map'] : Array<Dynamic>)), []);

  public static function hitTestShapeCommandPoint(buf:Array<ShapeCommandToken>, i:Float, x:Float, y:Float):Null<Bool> {
    var key:Dynamic = cast FlightRuntime.UNDEFINED;
    var fn:Dynamic = cast FlightRuntime.UNDEFINED;
    key = (cast FlightRuntime.getIndex(buf, i) : String);
    fn = FlightRuntime.callProperty(ShapeApi.hitTests__shapeHitTestRegistry, 'get', cast ([key] : Array<Dynamic>));
    if (FlightRuntime.truthy(FlightRuntime.strictEquals(fn, FlightRuntime.UNDEFINED))) { return cast null; }
    return cast FlightRuntime.callValue(fn, cast ([x, y, buf, (i + 2.0)] : Array<Dynamic>));
    return cast null;
  }

  public static function isShapeEmpty(source:Shape):Bool {
    return cast FlightRuntime.strictEquals(FlightRuntime.field(FlightRuntime.field(FlightRuntime.field(source, 'data'), 'commands'), 'length'), 0.0);
    return cast null;
  }

  public static final KAPPA__shapeFill:Dynamic = 0.5522847498307936;

  public static function normalizeArcSweep__shapeCommands(startAngle:Float, endAngle:Float, anticlockwise:Bool):Float {
    var sweep:Dynamic = cast FlightRuntime.UNDEFINED;
    sweep = (endAngle - startAngle);
    if (FlightRuntime.truthy(anticlockwise)) {
  if (FlightRuntime.truthy(FlightRuntime.compare(sweep, 0.0, '>'))) { (sweep = cast ((sweep - (HxMath.PI * 2.0)) : Dynamic)); }
} else {
  if (FlightRuntime.truthy(FlightRuntime.compare(sweep, 0.0, '<'))) { (sweep = cast ((sweep + (HxMath.PI * 2.0)) : Dynamic)); }
}
    return cast sweep;
    return cast null;
  }

  public static function pushArcCubics__shapeCommands(cmds:Array<ShapeCommandToken>, cx:Float, cy:Float, radius:Float, startAngle:Float, segmentCount:Float, segmentAngle:Float, alpha:Float):Void {
    var angle:Dynamic = cast FlightRuntime.UNDEFINED;
    angle = startAngle;
    {
      var s:Dynamic = 0.0;
      while (FlightRuntime.truthy(FlightRuntime.compare(s, segmentCount, '<'))) {
        var nextAngle:Dynamic = (angle + segmentAngle);
        var cosA:Dynamic = FlightRuntime.callProperty(HxMath, 'cos', cast ([angle] : Array<Dynamic>));
        var sinA:Dynamic = FlightRuntime.callProperty(HxMath, 'sin', cast ([angle] : Array<Dynamic>));
        var cosB:Dynamic = FlightRuntime.callProperty(HxMath, 'cos', cast ([nextAngle] : Array<Dynamic>));
        var sinB:Dynamic = FlightRuntime.callProperty(HxMath, 'sin', cast ([nextAngle] : Array<Dynamic>));
        FlightRuntime.pushMany(cmds, cast (['cubicCurveTo', 6.0, (cx + (radius * (cosA - (alpha * sinA)))), (cy + (radius * (sinA + (alpha * cosA)))), (cx + (radius * (cosB + (alpha * sinB)))), (cy + (radius * (sinB - (alpha * cosB)))), (cx + (radius * cosB)), (cy + (radius * sinB))] : Array<Dynamic>));
        (angle = cast (nextAngle : Dynamic));
        s++;
      }
    }
  }

  public static function pushCubic__shapeFill(path:Path, c1x:Float, c1y:Float, c2x:Float, c2y:Float, ax:Float, ay:Float):Void {
    FlightRuntime.callProperty(FlightRuntime.field(path, 'commands'), 'push', cast ([Types.PathCommandValue.CUBIC_CURVE_TO] : Array<Dynamic>));
    FlightRuntime.pushMany(FlightRuntime.field(path, 'data'), cast ([c1x, c1y, c2x, c2y, ax, ay] : Array<Dynamic>));
  }

  public static function pushQuadratic__shapeFill(path:Path, cx:Float, cy:Float, ax:Float, ay:Float):Void {
    FlightRuntime.callProperty(FlightRuntime.field(path, 'commands'), 'push', cast ([Types.PathCommandValue.CURVE_TO] : Array<Dynamic>));
    FlightRuntime.pushMany(FlightRuntime.field(path, 'data'), cast ([cx, cy, ax, ay] : Array<Dynamic>));
  }

  public static function pushVerb__shapeFill(path:Path, verb:Float, x:Float, y:Float):Void {
    FlightRuntime.callProperty(FlightRuntime.field(path, 'commands'), 'push', cast ([verb] : Array<Dynamic>));
    FlightRuntime.pushMany(FlightRuntime.field(path, 'data'), cast ([x, y] : Array<Dynamic>));
  }

  public static function registerShapeHitTestCommand<K>(command:ShapeHitTestCommand<Dynamic>):Void {
    FlightRuntime.callProperty(ShapeApi.hitTests__shapeHitTestRegistry, 'set', cast ([FlightRuntime.field(command, 'key'), (cast FlightRuntime.field(command, 'hitTest') : AnyHitTestFn__shapeHitTestRegistry)] : Array<Dynamic>));
  }
}
