// Generated by @flighthq/tool-compiler from @flighthq/effects/packages/effects/src/kuwaharaMath.ts#KuwaharaMath at 65889a191bb0d2f5f2f95148590874f3e49bf9d4. Do not edit.
package flight._hx.effects;

import flight._hx.types.KuwaharaEffect.KuwaharaEffect;

function computeKuwaharaGaussianWeights(radius:Float, out:flight._internal._Float32Array):Float {
  final r:Dynamic = flight._internal._Math.max(1, flight._internal._Math.floor(radius));
  final size:Float = r + 1;
  final sigma:Float = r / 2;
  final twoSigmaSq:Float = (2 * sigma) * sigma;
  var sum:Float = 0;
  {
    var y:Float = 0;
    while (y < size) {
      {
        {
          var x:Float = 0;
          while (x < size) {
            {
              final d:Float = (x * x) + (y * y);
              out[Std.int(((y * size) + x))] = flight._internal._Math.exp((- d / twoSigmaSq));
              sum += out[Std.int(((y * size) + x))];
            }
            x += 1;
          }
        }
      }
      y += 1;
    }
  }
  final invSum:Float = (sum > 1e-10) ? (1 / sum) : 1;
  {
    var i:Float = 0;
    while (i < (size * size)) {
      {
        out[Std.int(i)] *= invSum;
      }
      i += 1;
    }
  }
  return size * size;
}

function computeKuwaharaSectorOffsets(radius:Float, out:Array<Float>):Void {
  final r:Dynamic = flight._internal._Math.max(1, flight._internal._Math.floor(radius));
  final half:Dynamic = r;
  final v0:Float = - half;
  final v1:Float = - half;
  final v2:Float = 0;
  final v3:Float = - half;
  final v4:Float = - half;
  final v5:Float = 0;
  final v6:Float = 0;
  final v7:Float = 0;
  out[0] = v0;
  out[1] = v1;
  out[2] = v2;
  out[3] = v3;
  out[4] = v4;
  out[5] = v5;
  out[6] = v6;
  out[7] = v7;
}

function computeKuwaharaSectorPixelCount(effect:KuwaharaEffect):Float {
  final s:Float = computeKuwaharaSectorSize(effect);
  return s * s;
}

function computeKuwaharaSectorSize(effect:KuwaharaEffect):Float {
  return flight._internal._Math.max(1, flight._internal._Math.floor((effect.radius ?? 3))) + 1;
}
