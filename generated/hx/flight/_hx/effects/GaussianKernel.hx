// Generated by @flighthq/tool-compiler from @flighthq/effects/packages/effects/src/gaussianKernel.ts#GaussianKernel at 65889a191bb0d2f5f2f95148590874f3e49bf9d4. Do not edit.
package flight._hx.effects;

function computeGaussianKernelWeights(sigma:Float, out:Array<Float>):Array<Float> {
  final size:Float = getGaussianKernelSize(sigma);
  out.resize(Std.int(size));
  if (size == 1) {
    out[0] = 1;
    return out;
  }
  final radius:Float = (size - 1) / 2;
  final twoSigmaSquared:Float = (2 * sigma) * sigma;
  var sum:Float = 0;
  {
    var i:Float = 0;
    while (i < size) {
      {
        final x:Float = i - radius;
        final weight:Dynamic = Math.exp((- (x * x) / twoSigmaSquared));
        out[Std.int(i)] = weight;
        sum += weight;
      }
      i += 1;
    }
  }
  final inverseSum:Float = 1 / sum;
  {
    var i_2:Float = 0;
    while (i_2 < size) {
      {
        out[Std.int(i_2)] *= inverseSum;
      }
      i_2 += 1;
    }
  }
  return out;
}

function getGaussianKernelSize(sigma:Float):Float {
  if (sigma <= 0) {
    return 1;
  }
  final radius:Dynamic = Math.ceil((3 * sigma));
  return (radius * 2) + 1;
}
