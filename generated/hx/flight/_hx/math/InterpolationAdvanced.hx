// Generated by @flighthq/tool-compiler from @flighthq/math/packages/math/src/interpolationAdvanced.ts#InterpolationAdvanced at 65889a191bb0d2f5f2f95148590874f3e49bf9d4. Do not edit.
package flight._hx.math;

function damp(current:Float, target:Float, lambda:Float, deltaTime:Float):Float {
  if ((deltaTime <= 0) || (lambda <= 0)) {
    return current;
  }
  return target + ((current - target) * Math.exp((- lambda * deltaTime)));
}

function lerpAngle(a:Float, b:Float, t:Float):Float {
  final TAU:Float = Math.PI * 2;
  var diff:Float = (((b - a) % TAU) + TAU) % TAU;
  if (diff > Math.PI) {
    diff -= TAU;
  }
  return a + (diff * t);
}

function moveTowards(current:Float, target:Float, maxDelta:Float):Float {
  final delta:Float = target - current;
  if (Math.abs(delta) <= maxDelta) {
    return target;
  }
  return current + (flight._internal._Math.sign(delta) * maxDelta);
}

function pingPong(t:Float, length:Float):Float {
  final cycle:Float = 2 * length;
  final mod:Float = ((t % cycle) + cycle) % cycle;
  return (mod <= length) ? mod : (cycle - mod);
}

function repeat(t:Float, length:Float):Float {
  return ((t % length) + length) % length;
}

function smootherStep(edge0:Float, edge1:Float, x:Float):Float {
  final t:Float = (x - edge0) / (edge1 - edge0);
  final s:Float = (t < 0) ? 0 : ((t > 1) ? 1 : t);
  return ((s * s) * s) * ((s * ((s * 6) - 15)) + 10);
}
