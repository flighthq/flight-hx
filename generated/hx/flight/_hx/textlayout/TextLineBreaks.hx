// Generated by @flighthq/tool-compiler from @flighthq/textlayout/packages/textlayout/src/textLineBreaks.ts#TextLineBreaks at 65889a191bb0d2f5f2f95148590874f3e49bf9d4. Do not edit.
package flight._hx.textlayout;

function getTextLineBreakIndex(lineBreaks:Array<Float>, startIndex:Float = 0):Float {
  if (lineBreaks.length == 0) {
    return - 1;
  }
  var lo:Float = 0;
  var hi:Float = lineBreaks.length - 1;
  var result:Float = - 1;
  while (lo <= hi) {
    final mid:Float = Std.int((lo + hi)) >>> Std.int(1);
    if (lineBreaks[Std.int(mid)] >= startIndex) {
      result = lineBreaks[Std.int(mid)];
      hi = (mid - 1);
    }
    else {
      lo = (mid + 1);
    }
  }
  return result;
}

function getTextLineBreaks(out:Array<Float>, text:String):Void {
  out.resize(Std.int(0));
  var index:Float = - 1;
  while (index < text.length) {
    final lf:Dynamic = text.indexOf("\n", Std.int((index + 1)));
    final cr:Dynamic = text.indexOf("\r", Std.int((index + 1)));
    if ((lf == - 1) && (cr == - 1)) {
      break;
    }
    index = ((cr == - 1) ? lf : ((lf == - 1) ? cr : flight._internal._Math.min(cr, lf)));
    out.push(index);
  }
}
