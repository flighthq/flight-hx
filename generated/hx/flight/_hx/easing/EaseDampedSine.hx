// Generated by @flighthq/tool-compiler from @flighthq/easing/packages/easing/src/easeDampedSine.ts#EaseDampedSine at 65889a191bb0d2f5f2f95148590874f3e49bf9d4. Do not edit.
package flight._hx.easing;

import flight._hx.types.EasingFunction.EasingFunction;

function easeInDampedSine(amplitude:Float, period:Float):EasingFunction {
  final wavelength:Float = toDampedSineWavelength(period);
  final overshoot:Float = toDampedSineOvershoot(amplitude);
  final phase:Float = toDampedSinePhase(overshoot, wavelength);
  return function(t:Float) {
  if ((t == 0) || (t == 1)) {
    return t;
  }
  final time:Float = t - 1;
  return - ((overshoot * Math.pow(2, (10 * time))) * Math.sin((((time - phase) * TAU) / wavelength)));
};
}

function easeInOutDampedSine(amplitude:Float, period:Float):EasingFunction {
  final wavelength:Float = toDampedSineWavelength(period);
  final overshoot:Float = toDampedSineOvershoot(amplitude);
  final phase:Float = toDampedSinePhase(overshoot, wavelength);
  return function(t:Float) {
  if ((t == 0) || (t == 1)) {
    return t;
  }
  final time:Float = (t * 2) - 1;
  if (time < 0) {
    return ((- 0.5 * overshoot) * Math.pow(2, (10 * time))) * Math.sin((((time - phase) * TAU) / wavelength));
  }
  return (((0.5 * overshoot) * Math.pow(2, (- 10 * time))) * Math.sin((((time - phase) * TAU) / wavelength))) + 1;
};
}

function easeOutDampedSine(amplitude:Float, period:Float):EasingFunction {
  final wavelength:Float = toDampedSineWavelength(period);
  final overshoot:Float = toDampedSineOvershoot(amplitude);
  final phase:Float = toDampedSinePhase(overshoot, wavelength);
  return function(t:Float) {
  if ((t == 0) || (t == 1)) {
    return t;
  }
  return ((overshoot * Math.pow(2, (- 10 * t))) * Math.sin((((t - phase) * TAU) / wavelength))) + 1;
};
}

function toDampedSinePhase(overshoot:Float, wavelength:Float):Float {
  return (wavelength / TAU) * Math.asin((1 / overshoot));
}

function toDampedSineOvershoot(amplitude:Float):Float {
  return (amplitude < 1) ? 1 : amplitude;
}

function toDampedSineWavelength(period:Float):Float {
  return (period > 0) ? period : DEFAULT_DAMPED_SINE_PERIOD;
}

final TAU:Float = (2 * Math.PI);

final DEFAULT_DAMPED_SINE_PERIOD:Float = 0.4;
