// Generated by @flighthq/tool-compiler from @flighthq/color/packages/color/src/hslColor.ts#HslColor at 65889a191bb0d2f5f2f95148590874f3e49bf9d4. Do not edit.
package flight._hx.color;

import flight._hx.types.HslColor.HslColor;

function allocateHslColor():HslColor {
  return [0, 0, 0];
}

function hslToRgb(out:Array<Float>, h:Float, s:Float, l:Float):Void {
  if (s == 0) {
    out[0] = l;
    out[1] = l;
    out[2] = l;
    return;
  }
  final q:Float = (l < 0.5) ? (l * (1 + s)) : ((l + s) - (l * s));
  final p:Float = (2 * l) - q;
  final hn:Float = h / 360;
  out[0] = hueToRgbChannel(p, q, (hn + (1 / 3)));
  out[1] = hueToRgbChannel(p, q, hn);
  out[2] = hueToRgbChannel(p, q, (hn - (1 / 3)));
}

function rgbToHsl(out:HslColor, color:Float):HslColor {
  final r:Float = (Std.int((Std.int(color) >>> Std.int(24))) & Std.int(255)) / 255;
  final g:Float = (Std.int((Std.int(color) >>> Std.int(16))) & Std.int(255)) / 255;
  final b:Float = (Std.int((Std.int(color) >>> Std.int(8))) & Std.int(255)) / 255;
  final max:Dynamic = flight._internal._Math.max(flight._internal._Math.max(r, g), b);
  final min:Dynamic = flight._internal._Math.min(flight._internal._Math.min(r, g), b);
  final l:Float = (max + min) / 2;
  if (max == min) {
    out[0] = 0;
    out[1] = 0;
    out[2] = l;
    return out;
  }
  final d:Float = max - min;
  final s:Float = (l > 0.5) ? (d / ((2 - max) - min)) : (d / (max + min));
  var h:Float;
  if (max == r) {
    h = ((((g - b) / d) + ((g < b) ? 6 : 0)) / 6);
  }
  else {
    if (max == g) {
      h = ((((b - r) / d) + 2) / 6);
    }
    else {
      h = ((((r - g) / d) + 4) / 6);
    }
  }
  out[0] = (h * 360);
  out[1] = s;
  out[2] = l;
  return out;
}

function hueToRgbChannel(p:Float, q:Float, t:Float):Float {
  final tn:Float = ((t % 1) + 1) % 1;
  if (tn < (1 / 6)) {
    return p + (((q - p) * 6) * tn);
  }
  if (tn < (1 / 2)) {
    return q;
  }
  if (tn < (2 / 3)) {
    return p + (((q - p) * ((2 / 3) - tn)) * 6);
  }
  return p;
}
