// Generated by @flighthq/tool-compiler from @flighthq/color/packages/color/src/hsvColor.ts#HsvColor at 65889a191bb0d2f5f2f95148590874f3e49bf9d4. Do not edit.
package flight._hx.color;

import flight._hx.types.HsvColor.HsvColor;

function allocateHsvColor():HsvColor {
  return [0, 0, 0];
}

function hsvToRgb(out:Array<Float>, h:Float, s:Float, v:Float):Void {
  if (s == 0) {
    out[0] = v;
    out[1] = v;
    out[2] = v;
    return;
  }
  final hn:Float = ((h % 360) + 360) % 360;
  final i:Float = flight._internal._Math.floor((hn / 60)) % 6;
  final f:Float = (hn / 60) - flight._internal._Math.floor((hn / 60));
  final p:Float = v * (1 - s);
  final q:Float = v * (1 - (f * s));
  final t:Float = v * (1 - ((1 - f) * s));
  switch (i) {
    case 0:
      out[0] = v;
      out[1] = t;
      out[2] = p;
    case 1:
      out[0] = q;
      out[1] = v;
      out[2] = p;
    case 2:
      out[0] = p;
      out[1] = v;
      out[2] = t;
    case 3:
      out[0] = p;
      out[1] = q;
      out[2] = v;
    case 4:
      out[0] = t;
      out[1] = p;
      out[2] = v;
    default:
      out[0] = v;
      out[1] = p;
      out[2] = q;
  }
}

function rgbToHsv(out:HsvColor, color:Float):HsvColor {
  final r:Float = (Std.int((Std.int(color) >>> Std.int(24))) & Std.int(255)) / 255;
  final g:Float = (Std.int((Std.int(color) >>> Std.int(16))) & Std.int(255)) / 255;
  final b:Float = (Std.int((Std.int(color) >>> Std.int(8))) & Std.int(255)) / 255;
  final max:Dynamic = flight._internal._Math.max(flight._internal._Math.max(r, g), b);
  final min:Dynamic = flight._internal._Math.min(flight._internal._Math.min(r, g), b);
  final d:Float = max - min;
  final v:Dynamic = max;
  final s:Float = (max == 0) ? 0 : (d / max);
  var h:Float;
  if (d == 0) {
    h = 0;
  }
  else {
    if (max == r) {
      h = ((((g - b) / d) + ((g < b) ? 6 : 0)) / 6);
    }
    else {
      if (max == g) {
        h = ((((b - r) / d) + 2) / 6);
      }
      else {
        h = ((((r - g) / d) + 4) / 6);
      }
    }
  }
  out[0] = (h * 360);
  out[1] = s;
  out[2] = v;
  return out;
}
